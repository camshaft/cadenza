node_modules/
dist/
*.local
*.tsbuildinfo

# Staged build artifacts (regenerated by `npm run wasm`): the compiler pkg, the value-heap runtime, and
# the CAD + MUSIC library sources (/cad + /music preload them via compile_with_preloaded — staged from
# implementation/cad + implementation/music). Never committed; rebuilt by `cargo xtask guide-wasm`.
src/wasm/pkg/
src/wasm/runtime.wasm
src/wasm/runtime-hash.txt
src/wasm/cad/
src/wasm/music/
