node_modules/
dist/
*.local
*.tsbuildinfo

# Staged build artifacts (regenerated by `npm run wasm`): the compiler pkg, the value-heap runtime, and
# the CAD library source (/cad preloads it via compile_with_preloaded — staged from implementation/cad).
src/wasm/pkg/
src/wasm/runtime.wasm
src/wasm/runtime-hash.txt
src/wasm/cad/
