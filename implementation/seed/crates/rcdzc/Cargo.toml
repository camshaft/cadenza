# rcdzc — the reference Cadenza → WebAssembly-component compiler, rebuilt to the reference
# architecture (spec/architecture/*.md, 2026-07-10).
#
# From-scratch Stage 0 (build-order.md §Stage 0): the columns substrate, a thin vertical slice
# through every nanopass rung (each a real, separate column producer — NOT fused), one scalar
# wasm backend hand-emitted against the frozen component ABI, and the single query entry that
# drives compilation as TWO programs (a subject + a query program, code-as-data).
#
# TWO operator directives shape the code:
#   1. COPY, DON'T DEPEND. The two-arena leaf-pool AST + binary codec are COPIED into this crate
#      (`ast.rs`, `codec.rs`, `leb128.rs`) rather than taken as a `cadenza-syntax` dependency —
#      because the compiler is destined to be ported to Cadenza itself, then verbatim-copied back
#      to Rust, and a shared external crate would break that round-trip. The copies carry ZERO
#      external dependencies: an integer literal is a plain (sign, big-endian magnitude bytes)
#      struct, not a bignum-library type — arbitrary precision with nothing to depend on (the AST
#      only CARRIES the value; arithmetic on it is a later compile-time-evaluation concern). The
#      NFC text-normalization dep stayed with the text reader, which Stage 0 does not copy.
#   2. CADENZA-IN-RUST STYLE. Written the way it would be written in Cadenza: plain sum types
#      matched exhaustively, arena indices instead of pointer graphs, columns keyed by node
#      identity. Deliberately un-idiomatic Rust so the later Cadenza port is mechanical.
#
# Validated by INTERNAL UNIT TESTS ONLY (no `cadenza-seed` dependency — that crate is reworked
# elsewhere): `wasmparser`/`wasm-encoder` are the independent byte oracles. These tests never RUN a
# component (no `wasmtime`/`cdz-run` dev-dep — behavioral verification lives in the language-agnostic
# corpus at `spec/semantics/*.sexp`, gated on all backends); they compile + byte-check emitted artifacts.
# Nothing here enters the compile path (the byte path is hand-emitted so it ports to the self-host).
[package]
name = "rcdzc"
version = "0.0.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
# COPY-DON'T-DEPEND, with ONE sanctioned exception: `cadenza-ast` (operator directive 2026-08-28,
# "Cadenza-AST is the only exception to the no dep policy"). The AST value model + canonical binary
# codec are NO LONGER copied in (`ast.rs`/`codec.rs`/`leb128.rs` deleted); the compiler depends on the
# single shared `cadenza-ast` crate so there is ONE source of truth, not diverging copies. Taken with
# `default-features = false` = its `no_std`+alloc CORE (no num-bigint / unicode-normalization / canon /
# fxhash), so the compiler stays dependency-light and ports cleanly to the Cadenza self-host — the
# reason cadenza-ast was kept lightweight. An integer literal is still a plain (sign, magnitude bytes)
# `IntValue`, no arbitrary-precision library pulled in.
[dependencies.cadenza-ast]
path = "../cadenza-ast"
default-features = false

[dependencies.cadenza-compile-abi]
# The SECOND sanctioned exception to COPY-DON'T-DEPEND, on the same rationale as `cadenza-ast`: the
# compile-BOUNDARY contract types (`Target`, `OptLevel`, and — later slices — the kinded `Artifact`
# list, the sidecar `Request`/`Query` codec, the `spans` side-table) are the wire vocabulary the build
# tool speaks across the compiler seam, and BOTH sides of that boundary must agree on ONE definition:
# `rcdzc` PRODUCES them, and `cdz` (in a `!standalone` build) DELEGATES to `cdz-compile` and must build
# the identical request + read the identical result. Extracting them to one shared crate that both
# depend on is the compile-boundary analogue of `cadenza-ast` (one source of truth for a cross-process
# wire format), which is why the same operator carve-out applies. `rcdzc` `pub use`s what moves here so
# its public API (`rcdzc::Target`, `rcdzc::OptLevel`, …) is byte-stable and every consumer path keeps
# resolving; the compiler IMPLEMENTATIONS (query eval over a live `Db`, `compile`, the backends) stay
# here. Dependency-light: no deps for the leaf-enum slice; one `cadenza-ast` dep when the codec moves.
path = "../cadenza-compile-abi"

[dependencies.blake3]
# The ONE exception to COPY-DON'T-DEPEND, waived by the operator (2026-08-25) specifically for blake3:
# the compile-time `Blake3.of` fold calls `blake3::hash` so its 32-byte digest is BYTE-IDENTICAL to the
# runtime `hash-blake3` heap op (idx 91), which uses the SAME crate (design-compiler-primitives §9 —
# same hash both places). BLAKE3 is a fixed algorithm, so the digest is version-independent. Same crate
# + version + no-default-features the runtime (`cdz-runtime`) uses, plain UNKEYED BLAKE3-256, no key /
# derive-key / domain tag (all domain separation is userspace, D7).
version = "1.8"
default-features = false

[dependencies.tracing]
# TRACE-LEVEL DECISION LOGGING. The query producers (resolve/infer/lower/eval) emit a `trace!` event
# at each decision — a name's resolution, a solved type, a fold outcome, a decline — so a debugging
# run sees exactly what the compiler chose and why. `trace!` is ZERO-COST when no subscriber is
# installed (the default), so the corpus gate and normal builds pay nothing; a subscriber is set up
# only at the HOST boundary (the `rcdzc-cli` crate's `run`, tests). In the eventual Cadenza port these
# call sites map to a trace EFFECT — a structured event emitted to an ambient handler — so instrumenting
# with events (not the crate's span/subscriber internals threaded through logic) keeps the port clean.
# NOTE: the LIBRARY takes only `tracing` (event-emission macros, ~zero cost). The trace SINK
# (`tracing-subscriber`) and the arg parser (`clap`) are HOST-BOUNDARY deps that now live in the separate
# `rcdzc-cli` crate, NOT here — the compiler is a PURE LIBRARY (operator directive 2026-08-30: "the
# compiler should not be pulling clap in as a dependency … should be a pure library"). Do NOT re-add
# `clap`/`tracing-subscriber` to the compiler; a new arg or trace-sink concern belongs in `rcdzc-cli`.
version = "0.1"
default-features = false
features = ["std"]

[dependencies.wasmparser]
# OUTPUT SELF-VALIDATION at the HOST CLI boundary (`cli::run_with_specs`), NOT the compile path. The
# hand-written wasm encoder can, on a not-yet-fixed codegen path, emit a `"component"` artifact that
# `cdz compile` accepts but that fails validation and is then silently rejected far downstream at
# `wasmtime::Component::new` (returns None) — the adv-cdz-invalid-wasm latent hazard. `cli` validates
# the produced component with `wasmparser` before writing it and errors (CDZ0910) instead of shipping
# an invalid module. Used ONLY by the `cli` host driver (which already does file I/O + is not part of
# the Cadenza self-host port); the PORT-RELEVANT compile core (resolve/infer/lower/eval/backend) stays
# wasmparser-free, so this does not compromise "the compiler is a pure library". `wasm-encoder` remains
# a TESTS-ONLY byte oracle in `[dev-dependencies]` — only the read-side validator is promoted here.
version = "0.239"

# BUILD-TIME ONLY (build.rs): regenerates the bundled verify_kernel.bin from verify_kernel.cdz via the
# REAL cadenza-syntax codec (codec::encode(sexpr::read(...))) into OUT_DIR, so there is no committed
# golden to go stale. Acyclic — cadenza-syntax does not depend on rcdzc. NOT in the compile path (the
# "copy, don't depend" invariant holds: rcdzc's own copied codec decodes these bytes at compile time).
[build-dependencies]
cadenza-syntax = { path = "../cadenza-syntax" }

[dev-dependencies]
# The real front-end reader (TESTS ONLY — like wasm-encoder below, never in the compile
# path, so the "copy, don't depend" round-trip is preserved). Test fixtures read the s-expression
# surface with the SAME reader the corpus gate uses (`sexpr::read`, which desugars `a.b` → `(. a b)`)
# rather than a hand-rolled reader that would drift from it. The bridge is BYTES: `sexpr::read` → its
# `codec::encode` → rcdzc's `codec::decode` — so every test also EXERCISES that the copied `codec.rs`
# stays byte-identical to `cadenza-syntax` (the invariant the copy depends on), turning a silent
# assumption into a checked one.
cadenza-syntax = { path = "../cadenza-syntax" }
# The independent byte ORACLE (tests only): licenses the hand-emitted byte path by asserting it is
# byte-identical to the authoritative component-model encoder. NEVER in the compile path.
wasm-encoder = "0.239"
# (`wasmparser` was PROMOTED to `[dependencies]` above — the host `cli` output self-validator uses it in
# the non-test compile path. It stays available to tests as a normal dep, so tests keep using it as a
# component/core validator that LOCALIZES a byte error, `Component::from_binary`'s error being opaque.)
# The shared native runtime interface an emitted ASYNC module links against (`cdz_rt::CdzEnv`). A
# dev-dependency so `cargo test` builds its rlib into the target dir, where the Rust-async round-trip
# tests locate it to pass `rustc --extern cdz_rt=…`. Tests only — never in the compile path.
cdz-rt = { path = "../cdz-rt" }
# The bignum an emitted program links against (`cdz_num::Big`) — a dev-dependency so `cargo test` builds
# its rlib into the target dir, where the BigInt round-trip tests locate it to pass `rustc --extern
# cdz_num=…` (mirroring `cdz-rt`). Tests only — never in the compile path. `cdz-num` source-shares the
# runtime's bigint.rs, so this does NOT link the runtime; it's a plain host rlib of `Big`.
cdz-num = { path = "../cdz-num" }
