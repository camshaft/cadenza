# multiplicative-state — geometric (tripling) state thread
## pymt1 — tick answers s, threads (* s 3); 3 dispatches read s/3s/9s packed into digit ranges. Model 270903/180602. PASS x3.
Powers-of-three progression through the resume seam; wrong multiplier or mis-threaded state scrambles the packed powers. Round-trip-safe. Promotable.
