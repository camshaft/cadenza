# mnc1 — greedy change-making counter (2026-08-15, tick 1547)

Scalar drawer total + a CALLEE (coins-for) that walks the 25/10/(7)/1 ladder
via chained match binders over arithmetic compounds — quotient/remainder
pairs bound stepwise (q25/r25/q10/r10), the DEEPEST match-binder chain in
the pool (4 levels), all arithmetic scrutinees (the fence-safe kind). The
seed decides whether a 7-coin exists in the ladder — the arm-level answer
for 8 cents is 2 coins (7+1) vs 8 coins (8x1), 9 cents 3 vs 9; seven-free
amounts agree (5,3,5 rows shared), totals 18 vs 30.

Also NOTE: the match binder in the ARM is over a CALL compound (coins-for)
— fence-safe per rps2/brd1, and it passes, further confirming call
scrutinees are fine where perform/if scrutinees decline.

PASS ×3. **Pool.**
