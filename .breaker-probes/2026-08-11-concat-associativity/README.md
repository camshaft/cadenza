# Concat associativity (2026-08-11) — 05-compound target (14-effects HOLD active)

Angle: left-nested vs right-nested three-way concat — different ROPE TREES,
same sequence. Structural = must see through the tree shape; element reads
land identically on both.

GREEN x3:
- as1: (= lr rl) true + len + boundary elements both nestings — 1060516/1060596
  (first draft was a WEAK pin — both seeds identical since n sat at an unread
  index; strengthened by reading index 0. Seed-differentiation check pays.)

Pin candidate: staged pool (05 batch during the 14-effects hold).
