# threeway-arg-state — 3-way branch on sign of (op-arg - captured state)
## py3w1 — cmd(v): v>s -> *10/+1, v<s -> *100/+2, equal -> 0/+3. Model 50100/50000. PASS x3.
Three-way sign branch selects answer + a per-branch state advance; two dispatches fire greater + equal/less across seeds. Promotable.
