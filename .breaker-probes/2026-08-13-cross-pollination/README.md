# 2026-08-13 cross-pollination pair (tick 1393)

- `xcl1.sexp` — TWO closures in a tuple state, each dispatch rebuilds each one
  capturing the OTHER's fresh arm-local result: UNIFORM DECLINE ×3 ("declined —
  can't compile yet"; arm-built closures into the next state = the same
  capture-locus family as fac1/the known arm-closure TODO, here in PAIR form).
  Decline witness + flip-watch.
- `xsw1.sexp` — the SCALAR cross-swap twin (same dataflow, no closures): each
  dispatch computes both successors then installs them SWAPPED — a-lineage and
  b-lineage cross sides every step. Identical outputs to the closure form's
  model (50400715/20400706). PASS ×3. Pin candidate; the pair pins the boundary
  (dataflow fine, closure-wrapping of it = TODO).
