# Symbol-keyed Map states (2026-08-11)

Angle: Symbols as CHAMP keys THROUGH the effect state thread (17-symbols pins
symbol-keyed maps in pure position; the sy-family pins symbol MODES as state
scalars, not keys).

GREEN x3:
- sk1: arm-minted symbol keys (parity routes to even/odd counters); interned
  identity survives the thread — 21/30
- sk2: the Symbol key CROSSES the dispatch as an op argument; body-side
  literals key the state map (re-tag of alpha increments, not duplicates) —
  201/706

Pin candidates: staged pool.

## v-effects (2026-08-12): sk1 REDUNDANT — symbol-keyed-Map-as-threaded-state with in-arm Symbol.of key-minting + interned-identity routing is already pinned at 14b:4435 (route-table accumulator). SKIPPED sk1. sk2 (symbol key crosses as op ARG) may be distinct — checking.

## v-effects (2026-08-12): sk2 MARGINAL — symbol-as-op-arg keying the state Map overlaps the 14b:4435 route-table (interned-symbol Map keys across the thread) + 14c:1504/14-3314 (symbol args as commands/identity). Thin distinct sliver; SKIPPED to avoid corpus weight without a clearly-new shape.
