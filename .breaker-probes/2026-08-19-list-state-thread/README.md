# list-state-thread — List-state handler, prepend-accumulate + head across the seam
## pyls1 — push(v) threads (List.prepend l v) answering len; head reads List.at 0. Model 2310/2300. PASS x3.
Heap list threaded as handler state; prepend accumulation + head read survive the resume seam. Promotable.
