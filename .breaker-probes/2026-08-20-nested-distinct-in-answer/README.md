# nested-distinct-in-answer — closed distinct-effect handle in the outer arm's ANSWER

## pysh5 — answer = (* s 100) + (closed handle F reducing to 30), thread +1
Inner handle F seed 7: ff answers t*2 thread t+1; body (F.ff)+(F.ff) = 14+16 = 30.
Outer body tick#1 + tick#2 = (sO0*100+30)+((sO0+1)*100+30) = 200*sO0 + 160.
Model n=10 360, n=0 160. Verified x3 + opt-sweep 0-div.
Answer-hole nested closed handle (pyre6 axis) but here of a DISTINCT effect and driven
per outer dispatch — folds correctly. (Contrast pysh6, banked below as a decline note:
the SAME nested handle in the NEXT-STATE position declines — pyre3 next-state territory.)

## pysh6 (NOT promoted) — same nested handle in NEXT-STATE declines
(resume (* s 100) (+ s <handle F ...>)) -> DECLINES uniformly (next-state nested handle,
the harder pyre3-family position, even for a distinct closed effect). Known deferred; no finding.
