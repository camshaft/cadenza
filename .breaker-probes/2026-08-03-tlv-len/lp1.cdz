(do
  (def (main (: k UInt8))
    (let ((payload (Bytes.of (list k (UInt8.wrap 200) (UInt8.wrap 201)))))
      (match (bin (u8 7) (u16 (UInt16.of (Bytes.len payload))) (bytes payload))
        ((bin (u8 7) (u16 n) (bytes body n))
          (+ (Int64.of n)
             (* 10 (match (Bytes.at body 0) ((Some v) (Int64.of v)) ((None _u) -1)))))
        (_ -2))))
  (def (go) (main 5))
  (export go))
