# flt1 — two-stage sediment filter (2026-08-16, tick 1588)

SCALAR trap state: `pass` traps v mod seed (stage 1) then half the remainder
(stage 2) answering the survivor — the two stages CHAIN through two match
binders (r1 feeds r2's scrutinee; each binder ≤2 consumers, tnk-axis safe);
`backwash` resets answering the contents.

Finer mesh (mod 2) traps LESS in stage one for odd inputs... actually traps
the parity bit, leaving MORE for stage two — the interplay makes survivors
differ (10,6 vs 11,7) while the 9-row and final backwash CONVERGE (4, 5 both:
different r1/r2 splits totalling the same trap). Convergent-total pin: 1+4
= 1+4 on n=10 vs n=0's different split summing equal.

PASS ×3. **Pool — fills prs1/flt1 toward the 12th trio (+1).**
