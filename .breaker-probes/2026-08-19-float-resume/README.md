# float-resume — Float64 handler state threaded by half-steps
## pyfl1 — tick answers s, threads (+ s 0.5); 3 dispatches sum to 3s+1.5; seed selected by n%3 among 0.0/1.0/2.0. Model 4.5/1.5. PASS x3.
Float64 resume value + exact half-step threading through the seam. (14-effects part1 has some Float-handler cases; this is the 14c threaded-accumulation variant.) Promotable.
