# 2026-08-13 snapshot closures (tick 1378)

- `fac1.sexp` — the ARM manufactures a closure over the current state as the op
  RESULT: UNIFORM CLEAN DECLINE ×3 (CDZ0201-coded; arm-built closure crossing
  resume = the known escaping-closure capture-locus). Decline witness, banked.
- `fac2.sexp` — the BODY-side twin: snap answers the scalar state, the BODY
  builds the snapshot closures from the answered values, applies both AFTER two
  bumps (f1 keeps the OLD snapshot: 53·1000+55 / 90·1000+92). PASS ×3.
  Together they pin the boundary: snapshot-closure semantics work when the
  closure is built OUTSIDE the arm; the arm-built factory is a fold TODO.
