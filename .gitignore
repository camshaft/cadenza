# =============================================================================
# This is the Cadenza specification branch. It contains ONLY the specification
# tree — the durable source of truth. The Cadenza compiler is a disposable,
# regenerable projection of these specs and is NEVER committed here; it is
# regenerated into implementation/ (below).
# =============================================================================

# The generated compiler / any synthesized implementation.
/implementation/

# Editor / OS / agent-local
.vscode/
.idea/
*.swp
*.swo
.DS_Store
.direnv/
result
.claude/
