on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

name: k-framework

jobs:
  k-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install Nix
        uses: cachix/install-nix-action@v27
        with:
          nix_path: nixpkgs=channel:nixos-unstable

      - name: Setup Nix cache
        uses: cachix/cachix-action@v15
        with:
          name: cadenza
          authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'
          skipPush: true

      - name: Build Cadenza CLI
        run: nix develop --command cargo build --bin cadenza

      - name: Compile K definition
        run: nix develop --command cargo xtask k kompile

      - name: Run K framework tests
        run: nix develop --command cargo xtask k test
