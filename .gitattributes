# Git attributes for the Cadenza repo.
#
# ── Concurrent-append merge policy ───────────────────────────────────────────────────────────────
# `spec/semantics/.gate-baseline` (and its per-backend siblings) is the corpus verdict baseline that
# EVERY corpus-adding agent appends to (`cargo xtask gate --save` writes one `verdict<TAB>description`
# line per case). Under the fleet, many agents add cases concurrently, so a plain 3-way merge conflicts
# constantly on this one file — it was the #1 source of merge-request bounces (a fix agent could be
# rejected 4× purely on baseline contention, never touching the same case).
#
# The custom `fleet-baseline` driver (registered by `fleet up` in the hub's .git/config; see
# `register_merge_drivers`) resolves such a conflict by UNIONING both sides then VERDICT-AWARE DEDUPING
# (`xtask merge-baseline`): the file is line-oriented + order-independent (`gate --check` parses it into a
# description-keyed map — see `check_baseline`), so a union is correct, and the dedup collapses the
# duplicate line two agents produce when they touch the SAME case. This REPLACES the old built-in
# `merge=union` (which kept BOTH duplicate lines → a benign dup that re-warned every `gate --check` until
# hand-cleaned; corpus-bugfix hand-cleaned it repeatedly). A same-description/DIFFERENT-verdict conflict
# is left for a human (never silently resolved). If the driver isn't registered (config missing), git
# falls back to a normal conflict — safe, just the old behavior. Net: baseline appends stop blocking
# integration AND stop leaving duplicate residue.
spec/semantics/.gate-baseline            merge=fleet-baseline
spec/semantics/.gate-baseline-rust       merge=fleet-baseline
spec/semantics/.gate-baseline-rust-async merge=fleet-baseline
spec/semantics/.gate-baseline-platform   merge=fleet-baseline
#
# NOTE: the executable-semantics corpus files (`spec/semantics/NN-*.sexp`) are deliberately NOT
# union-merged. Each `(case …)` is a multi-line s-expression BLOCK; unioning interleaved lines from two
# branches would corrupt the s-expr structure (mismatched parens / spliced cases). Those need
# case-level (block) merging or per-vertical file splitting, not line-union — a separate change.
#
# ── Coverage-floor max-merge ─────────────────────────────────────────────────────────────────────
# `.duvet/coverage-floor.json` is a single monotone counter ({cited,total}) that EVERY citation-adding
# agent bumps, so concurrent slices textually CONFLICT on it (cited 644 vs 645) — a recurring tax on
# exactly the coverage growth the gate encourages. It's JSON, so `union` (line-keep) would corrupt it.
# Instead use the custom `fleet-maxfloor` driver (registered by `fleet up` in the hub's .git/config;
# see `register_merge_drivers`): it resolves a conflict by taking the field-wise MAX of the two sides
# (`xtask fleet merge-floor`). The floor only ever moves UP, so max(ours,theirs) is always the correct
# merged floor — concurrent bumps merge cleanly, never conflict. If the driver isn't registered (config
# missing), git falls back to a normal conflict — safe, just the old behavior.
.duvet/coverage-floor.json               merge=fleet-maxfloor
