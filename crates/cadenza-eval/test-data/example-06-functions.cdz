# Functions
# Define and call functions

# Simple function
fn double x = x * 2
double 5

# Multi-parameter function
fn add x y = x + y
add 3 7

# Function with closure
let outer = 100
fn capture = outer + 1
capture
