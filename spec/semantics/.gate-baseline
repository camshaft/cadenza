# gate baseline — per-case verdicts (verdict\tdescription). Regenerate with `cargo xtask gate --save`.
pass	(if cmp 1 0) materializes the comparison to 0/1 and (if cmp 0 1) its negation
pass	(if p 1 0) materializes a bare bool param to its 0/1 int
pass	200 recursive dispatches each crossing a heap LIST argument — the marshal at depth
pass	20000 owned-temporary List.push results each reclaim the fresh list (leak-drift stress)
pass	@ensures / @requires predicate PROJECTS a TUPLE component (`(. ret N)` on the result, `(. p N)` on a param)
pass	@ensures holds when the guarded def arrives through a runtime branch
pass	@ensures on a MODULE export is enforced when the def is called across the module-access boundary
pass	@ensures on a RECURSIVE def whose body is a HANDLE is re-checked at every recursive exit (recursion x effects-fold seam)
pass	@ensures on a def returning an @invariant type: establish (on construction) AND the postcondition (on ret) both fire (design §10, (D))
pass	@ensures on a def with a parameter named ret is REJECTED (would silently not enforce — rename the param)
pass	@ensures over a BIGINT (arbitrary-precision heap) result checks it with structural equality — the postcondition reads a non-scalar heap value
pass	@ensures over a HANDLE-bodied def called with a runtime arg is enforced (the verify_enforce let-over-handle shape)
pass	@ensures over a def whose body uses the `?` try-operator under an Option boundary — the postcondition matches the FALLIBLE result
pass	@ensures predicate PROJECTS a NESTED component (`(. (. ret 0) 1)` — a tuple inside a tuple), the deepest projection face
pass	@ensures predicate PROJECTS a RECORD field of the result (single field, and a two-field relation)
pass	@ensures-over-@requires stacked on an EFFECTFUL body is order-insensitive: compiles + enforces like the forward order
pass	@invariant ESTABLISH (divert) over a heap payload: a NonEmptyList newtype traps on the empty list, constructs a non-empty one (design §10.1/§10.2, (D))
pass	@invariant ESTABLISH Part 1: a bare-scalar invariant on a newtype auto-unwraps + type-checks; a satisfying value constructs and is usable
pass	@invariant ESTABLISH Part 2 (divert): a plain `(Percent.Pct v)` construction auto-establishes — a satisfying value constructs, a violating value traps at the call site (design §10.2, (D))
pass	@invariant ESTABLISH Part 2: the synthesized checked constructor enforces the invariant at run time — a satisfying value constructs, a violating value traps (design §10.2, (D))
pass	@invariant ESTABLISH divert reaches a CALL-ARGUMENT construction site — a violating value traps before the callee runs (design §10.2, (D))
pass	@invariant ESTABLISH divert reaches a LET-INIT construction site and a NESTED-invariant constructor argument
pass	@invariant ESTABLISH divert reaches a Set ELEMENT and a Map VALUE construction site (heap-collection positions beyond List)
pass	@invariant ESTABLISH divert reaches indirect construction sites: a lambda body, a reconstruct-after-update, and a list element all establish
pass	@invariant ESTABLISH fires through a ROW-OP re-wrap (Record.without + Record.extend rebuild the payload)
pass	@invariant ESTABLISH fires when the constructor is directly a MATCH ARM's selected result (v-patterns divert site)
pass	@invariant ESTABLISH fires when the constructor is inside a @requires PREDICATE — the divert reaches predicate-position code
pass	@invariant ESTABLISH fires when the type is IMPORTED from another module and constructed in the entry (cross-package divert site)
pass	@invariant ESTABLISH over a multi-variant sum: each variant's construction auto-establishes — a satisfying value constructs, a violating one traps (design §10.2, (D))
pass	@invariant ESTABLISH over a nullary variant: a rejected nullary variant traps at construction, an accepted payload variant constructs (design §10.2, (D))
pass	@invariant ESTABLISH over a single-variant multi-payload newtype: an ordered Range constructs, a misordered one traps (design §10.2, (D))
pass	@invariant ESTABLISH: a constructor's @requires discharges the type invariant as an implicit @ensures (design §10.2)
pass	@invariant PRESERVE: an operation returning T discharges the result invariant USING the input invariant as a free gift (design §10.2)
pass	@invariant destructure-arm predicate: a stray name inside the arm is still REJECTED (arm binder scope does not mask it)
pass	@invariant over a RECORD-payload newtype establishes on the record's relational fields
pass	@invariant with an unbound predicate name is REJECTED (CDZ0101 — only `self` + prelude are in scope)
pass	@requires and @ensures read a HEAP String param's byte-len — enforce on the empty-string violation
pass	@requires is enforced when the guarded def is applied through a first-class fn value
pass	@requires on a MODULE export is enforced when the def is called across the module-access boundary
pass	@requires on the EXPORTED ENTRY POINT is enforced — the contract holds when the harness calls main directly
pass	@requires over a BYTES parameter reads Bytes.len at body entry — completes the heap-param domain (List/Map/String/Bytes)
pass	@requires over a HANDLE-bodied def, and @ensures over a handle arm that uses state 3x — both enforce (effects-fold robustness)
pass	@requires over a HEAP argument reads the list's content at body entry
pass	@requires over a MAP argument walks the CHAMP at body entry
pass	@requires over an @invariant-typed PARAMETER: the type establish (at construction) and the precondition (at body entry) are independent obligations at distinct sites
pass	@requires stacked OVER @ensures: the precondition is still enforced when an @ensures wrapper sits between it and the def
pass	ACKERMANN evaluates — a recursive call in the ARGUMENT of a recursive call (not primitive-recursive)
pass	Ast nodes as Map keys: equal nodes collide to an overwrite, different node KINDS stay distinct
pass	Ast values key a map across quote and Ast.* construction routes
pass	Ast.decode converts bytes to an AST sum type value
pass	Ast.decode decodes bytes that arrive as a function argument
pass	Ast.decode of a RUNTIME byte sequence round-trips (not const-folded)
pass	Ast.encode and Ast.decode round-trip
pass	Ast.encode emits the canonical cdzast container header (byte 0 is 'c')
pass	Ast.encode folds a Map query composed inside a const-param fn (const_eval demand path)
pass	Ast.gensym mints a fresh Ast.Name, distinct per call site, stable per binding (manual macro hygiene)
pass	Ast.module reflects the enclosing module as an Ast.List value
pass	Ast.print of a reflected type definition renders its canonical (type …) source form
pass	BETA over the capture-avoiding substitution still proves the identity theorem (no regression)
pass	BIJECTIVE base-26 column codec biases each step by one and round-trips at the boundaries
pass	BINARY SEARCH against a hidden state target — the body bisects on the arm's ordering verdicts, eight probes find any target in 0..100
pass	BINOMIAL nCk multiplies incrementally with exact interleaved division and uses the symmetry cut
pass	BIPARTITE check two-colors components and rejects the odd cycle
pass	BOYER-MOORE majority vote pairs a cancel-count sweep with a verify recount
pass	BST DELETE-MIN walks the left spine, returns the minimum and the rebuilt tree
pass	BigInt.neg negates an arbitrary-precision integer, total
pass	BigInt.of a UInt32 at its high bit is a positive BigInt (narrow zero-extend, not sign)
pass	BigInt.of a UInt64 at 2^63 - 1 is positive (the sub-high-bit control)
pass	BigInt.of a UInt64 at 2^63 is a positive BigInt, not a wrong negative
pass	BigInt.of a UInt64 at its high bit still works (the full-width control)
pass	BigInt.of a UInt64 at u64::MAX is a positive BigInt
pass	BigInt.of a UInt64 widens to the exact value (round-trips back through Int64.of where it fits)
pass	BigInt.of a UInt8 at its high bit widens to the exact value (smallest-width zero-extend)
pass	BigInt.of a signed Int32 is a positive BigInt (the narrow path breaks regardless of sign)
pass	Blake3.of composes with Ast.encode — hashing an encoded AST folds to a 32-byte digest
pass	Blake3.of is a 32-byte digest
pass	Blake3.of is deterministic — the same bytes hash to the same digest
pass	Blake3.of is sensitive — a one-bit-different input hashes to a different digest
pass	Blake3.of of a runtime Bytes is a 32-byte digest (runtime op 91)
pass	Blake3.of of the empty byte string is the official empty-input BLAKE3-256 digest
pass	Bytes.concat of two runtime SLICES splices window content in order
pass	Bytes.concat of two sliced-view to-bytes is read twice and both reads see the concatenated bytes
pass	Bytes.of of a computed (concatenated) runtime list builds a byte sequence of that length
pass	Bytes.slice of a map-looked-up Bytes with perform-threaded start and len threads without a scratch collision
fail	CATALAN numbers grow by convolving the table with itself and agree with the binomial closed form
pass	CHUNK splits a list into fixed-size sublists with a short final chunk that reflattens intact
pass	COIN CHANGE builds a min-coins table where greedy fails and unreachable targets report -1
pass	COMPOUND RESULT PAYLOAD: (Result (Tuple Int64 Int64) (Tuple Int64 Int64)) — BOTH sides compound
pass	COMPOUND RESULT PAYLOAD: (Result (Tuple Int64 Int64) Int64) arg, drive Err (scalar side)
pass	COMPOUND RESULT PAYLOAD: (Result (Tuple Int64 Int64) Int64) arg, drive Ok (tuple side)
pass	COMPOUND RESULT PAYLOAD: (Result Int64 (Tuple Int64 Int64)) — the ERR side is compound
pass	COMPOUND RESULT PAYLOAD: a compound-Result closure beside a scalar closure (distinct-sig)
pass	COMPOUND RESULT PAYLOAD: two same-sig closures share one call (multi-export)
pass	COMPOUND SUM PAYLOAD: (Option (Record (: x Int64) (: y Int64))) — a RECORD payload
pass	COMPOUND SUM PAYLOAD: (Option (Tuple Int64 (Tuple Int64 Int64))) — a NESTED tuple payload
pass	COMPOUND SUM PAYLOAD: (Option (Tuple Int64 Int64)) arg, drive None
pass	COMPOUND SUM PAYLOAD: (Option (Tuple Int64 Int64)) arg, drive Some
pass	COMPOUND SUM PAYLOAD: an Option-tuple closure beside a scalar closure (distinct-sig)
pass	COMPOUND SUM PAYLOAD: mixed-width tuple payload (Int32, Int64, Bool)
pass	COMPOUND SUM PAYLOAD: two same-sig Option-tuple closures share one call (multi-export)
pass	CONTROL: a CONST-start slice as a Map-lookup key hits on wasm
pass	CROSS-TYPE @invariant: a value deconstructed from one @invariant newtype and RE-constructed into ANOTHER establishes BOTH invariants at their own sites
pass	CSE Convert-distinctness holds under the reversed arm order (the swap gave the OTHER wrong value)
pass	CSE keeps two width-DIFFERENT .wrap conversions of one operand DISTINCT (target width is off the node, not the Prim)
pass	Char equality in a recursion counts matches under const
pass	Char.from-int at U+E000 (first scalar after the surrogate block) is Some
pass	Char.from-int at the last surrogate U+DFFF is None
pass	Char.from-int at the maximum valid scalar U+10FFFF is Some
pass	Char.from-int of a surrogate folds to None under const
pass	Char.from-int on a genuinely-runtime integer computes Some/None at run time (Char-rep 4/N follow-on)
pass	Char.from-int then to-int round-trips under const
pass	Char.to-int reads a genuinely-runtime char (if-selected) by scalar value
pass	D1 an if-joined dual-used rope sharing a pre-if concat child in BOTH arms reclaims cleanly (was a 980-residual double-free, FIXED #5424)
pass	DIAMOND effect: two middles each perform base's ONE effect; the entry's single handler serves both
pass	DIAMOND: two middles import one base; the entry composes both and base's abstract type unifies
pass	DIFF-WIDTH RESULT: (Result Int32 Int64) — the NARROW side is Ok, negative
pass	DIFF-WIDTH RESULT: (Result Int64 Int32) arg, drive Err with a NEGATIVE narrow payload
pass	DIFF-WIDTH RESULT: (Result Int64 Int32) arg, drive Ok with a big s64
pass	DIFF-WIDTH RESULT: a (Result Int64 Int32) group + a (Result Int32 Int64) group, distinct-sig
pass	DIFF-WIDTH RESULT: two same-sig (Result Int64 Int32) closures share one call
pass	DIGITAL ROOT iterates digit sums to a fixed point and agrees with the mod-9 congruence
pass	DISTINCT-SIG among-scalars: driving the Int64/Bool-tuple LIST group (tuple then suffix scalar)
pass	DISTINCT-SIG among-scalars: driving the Int64/Bool-tuple group (Bool field in the rebuild)
pass	DISTINCT-SIG among-scalars: two scalar-then-Tuple closures of DIFFERENT sigs each returning a LIST
pass	DISTINCT-SIG among-scalars: two scalar-then-Tuple closures of DIFFERENT sigs — driving the Int64 group
pass	DISTINCT-SIG: a nested-Tuple-arg closure with a LIST result
pass	DISTINCT-SIG: a scalar-then-NESTED-tuple closure with a LIST result
pass	DISTINCT-SIG: a two-Tuple-arg group with a LIST result
pass	DISTINCT-SIG: an Option-arg closure + a Result-arg closure each cross the boundary
pass	DISTINCT-SIG: an Option-arg group BESIDE a Tuple-arg group
pass	DISTINCT-SIG: capturing two-Tuple-arg closures of different signatures
pass	DISTINCT-SIG: driving the (Tuple Int64 Bool)-arg closure of the distinct-sig List pair
pass	DISTINCT-SIG: driving the Bool-returning Tuple-arg closure of the distinct-sig pair
pass	DISTINCT-SIG: driving the Int32 two-Tuple-arg group
pass	DISTINCT-SIG: driving the Int64/Bool-inner nested-Tuple-arg closure (Bool leaf at depth)
pass	DISTINCT-SIG: driving the Int64/Bool-inner scalar-then-NESTED closure (Bool leaf at depth)
pass	DISTINCT-SIG: driving the Result-arg group (Err)
pass	DISTINCT-SIG: one group takes TWO tuples, the other ONE tuple
pass	DISTINCT-SIG: sum-arg closures ALONGSIDE a plain export
pass	DISTINCT-SIG: two DIFFERENT-sig scalar-then-NESTED-tuple closures — driving the Int64-inner group
pass	DISTINCT-SIG: two DIFFERENT-signature Tuple-arg closures each returning a List
pass	DISTINCT-SIG: two DIFFERENT-signature nested-Tuple-arg closures each cross the direct-call boundary
pass	DISTINCT-SIG: two DIFFERENT-signature two-Tuple-arg closures each cross the boundary
pass	DISTINCT-SIG: two Option-arg closures of DIFFERENT payload widths
pass	DISTINCT-SIG: two Tuple-arg closures of DIFFERENT signatures each cross the direct-call boundary
pass	DISTINCT-SIG: two capturing sum-arg closures of different signatures
pass	DISTINCT-SIG: two-Tuple-arg closures of different signatures ALONGSIDE a plain export
pass	DIVISOR SUM pairs each small divisor with its cofactor and classifies perfect numbers
pass	DOMINATOR CSE does not reuse a condition's perform-product in the taken branch
pass	EDIT DISTANCE rolls a two-row Levenshtein table over string scalars
pass	EQUILIBRIUM INDEX finds where left and right sums balance using total minus running
pass	Euclid's GCD runs over MULTI-LIMB BigInts — the remainder loop carries full precision
pass	FAST-DOUBLING fib recurses on the halved index returning a pair, checked against linear iteration
pass	FOUR bit-fields with widths 5+7+9+3 decode across three runtime bytes
pass	FOUR nested resumptive frames dispatched innermost-out
pass	FULL CONTRACT: @requires + @ensures on ONE def fire INDEPENDENTLY — a pre-violation and a post-violation trap on DIFFERENT inputs
pass	Float32 arithmetic runs at 24-bit mantissa precision — 2^24+1 absorbs, 2^24+2 is exact
pass	Float32 division const-folds at f32 (0.3f32 / 3.0f32 = 0.1f32)
pass	Float32 subtraction const-folds at f32 (0.4f32 - 0.1f32 = 0.3f32)
pass	Float32 tuple-key components key at binary32 width (distinctness, lookup, signed zero)
pass	Float32.Infinity is greater than a finite Float32
pass	Float32.of demotes a runtime Float64 to the nearest binary32
pass	Float64 comparison in a recursion counts matches under const
pass	Float64 equality by canonical bits folds through a recursion
pass	Float64.Infinity equals itself under the canonical byte form
pass	Float64.Infinity is greater than any finite float
pass	Float64.Infinity is not equal to a finite float
pass	Float64.Infinity is not less than a finite float
pass	Float64.neg negates a constant float
pass	Float64.neg negates a runtime float
pass	Float64.neg of negative zero is positive zero (sign-correct, total)
pass	Float64.of promotes a runtime Float32 exactly
pass	Float64.of-int collapses adjacent integers at the 2^53 precision boundary
pass	Float64.of-int converts a runtime integer, rounding a large Int64 to the nearest f64
pass	Float64.of-int is exact at 2^53 and rounds a just-past-2^53 odd integer to nearest-even
pass	GRAY CODE encodes by shift-xor, decodes by xor-folding, and adjacent codes differ by one bit
pass	HAPPY NUMBER iteration detects the 4-cycle with a seen-set and counts steps to resolution
pass	HORNER polynomial evaluation folds coefficients high-to-low and agrees with a power-sum oracle
pass	IDIOMATIC HeadOp-sum encoding discharges no-overflow: for x<=100, (x+1)<=MAXINT (sum-typed head, not a magic-int Const tag)
pass	INTERLEAVE alternates two spines and drains the longer tail after the shorter empties
pass	INTERLEAVED pops and inserts keep the event queue min-ordered across live mutation
pass	INT_MIN divided by the state — the minus-one seed overflows, other signs give exact halves
pass	INVALID UTF-8 crosses as a Bytes op argument — the arm's decode declines with None
pass	INVERSION COUNT pairs every element with its successors, zero when sorted and maximal when reversed
pass	Int64 MAX threads the handler state intact (representation at the boundary)
pass	Int64.checked-add over a u64 host-op RESULT evaluates the host call ONCE (the overflow formula names the operand)
pass	Int64.min crosses the parameter boundary and back unchanged
pass	Int64.min crossing the boundary is still negative
pass	Int64.neg of a constant folds to the negated value
pass	Int64.neg of a runtime expression negates at runtime, both signs
pass	Int64.neg of the minimum integer traps as a compile-provable overflow
pass	Int64.of over a runtime Float64 operand rejects — no float-to-int narrowing is blessed
pass	Int64.of over a u64 host-op RESULT evaluates the host call ONCE (the range-check names the operand)
pass	Int8.neg negates at a narrower signed width
pass	Int8.wrap at the sign-bit boundary is the most-negative Int8
pass	Int8.wrap of a constant with the sign bit set reinterprets as negative
pass	Int8.wrap of all-ones-byte is negative one
pass	KADANE max-subarray threads a best-ending-here and a global best through one fold
pass	KERNIGHAN popcount clears the lowest set bit per step and agrees with a shift-walk oracle
pass	KTH PERMUTATION decodes the factorial number system by indexed removal from the pool
pass	LCM derives from gcd via overflow-safe divide-first and satisfies the product identity
pass	LOCAL PEAKS counts strict interior maxima through a three-element sliding window
pass	LONGEST COMMON SUBSEQUENCE rolls match-diagonal against carry-forward maxima
pass	LONGEST INCREASING SUBSEQUENCE fills a per-index table from strictly-smaller predecessors
pass	LONGEST RUN tracks the current and best streak, first run winning ties
pass	LOOK-AND-SAY iterates run-length description over its own prior output
pass	List.at const-index over a list with a TRAPPING dropped sibling preserves the trap (does not fold it away)
pass	List.at const-index over an all-trap-free list still folds to the element
pass	List.at in-bounds control alongside the huge-index guard reads the right elements
pass	List.at over a let-bound constant list folds through the binder on both backends
pass	List.at with near-i64::MAX and i32-wrap indices declines to None, not a wrapped read
pass	List.len does NOT fold away a trapping element construction (runtime denominator)
pass	List.len of a concat of call-site lists with runtime elements folds to the summed arity
pass	List.len of a list built at the call site from runtime elements folds to the arity
pass	List.len of a trap-free constant list still folds to its arity
pass	List.len over a NESTED owned-producer chain (concat of two pushes) reclaims every temporary (no live objects)
pass	List.len over a let-bound constant list folds to the arity on both backends
pass	List.len over a list with a TRAPPING element construction preserves the trap (does not fold it away)
pass	List.len over a trap-free pure-data list still folds to the length
pass	List.len over an owned-temporary List.concat result reclaims it (no live objects)
pass	List.len over an owned-temporary List.push result reclaims it (no live objects)
pass	List.len over an owned-temporary List.update result reclaims it (no live objects)
pass	List.update at BOTH extremes of a 2000-element list lands and preserves the original
pass	List.update at the 32-boundary changes one slot and leaves both neighbors shared
pass	List.update through a concat-built RELAXED node changes one slot with exact neighbors
pass	MAX PROFIT tracks the running minimum and best spread in one forward pass
pass	MERGE INTERVALS coalesces overlapping spans, touching endpoints join, containment absorbs
pass	MISSING NUMBER agrees between the arithmetic-sum gap and the XOR cancellation
pass	MIXED among-scalars: a scalar-then-Tuple closure ALONGSIDE a plain export — driving the closure
pass	MIXED among-scalars: a scalar-then-Tuple closure with a COMPOUND result ALONGSIDE a plain export
pass	MIXED among-scalars: a scalar-then-Tuple closure with a LIST result ALONGSIDE a plain export
pass	MIXED among-scalars: driving the PLAIN export alongside a scalar-then-Tuple closure
pass	MIXED: a List-returning Tuple-arg closure ALONGSIDE a plain export — driving the closure
pass	MIXED: a NESTED tuple + flat tuple arg closure ALONGSIDE a plain export
pass	MIXED: a TWO-Tuple-arg closure ALONGSIDE a plain (non-closure) export
pass	MIXED: a Tuple-arg closure export ALONGSIDE a plain (non-closure) export
pass	MIXED: a nested-Tuple-arg closure (scalar result) ALONGSIDE a plain export — driving the closure
pass	MIXED: a nested-Tuple-arg closure with a LIST result ALONGSIDE a plain export
pass	MIXED: a scalar-then-NESTED-tuple closure with a LIST result ALONGSIDE a plain export
pass	MIXED: a two-Tuple-arg closure with a LIST result ALONGSIDE a plain export
pass	MIXED: an Option-arg closure ALONGSIDE a plain (non-closure) export
pass	MIXED: driving the PLAIN export alongside a List-returning Tuple-arg closure
pass	MIXED: driving the PLAIN export alongside a Tuple-arg closure
pass	MIXED: driving the PLAIN export alongside a nested-Tuple-arg closure
pass	MIXED: driving the PLAIN export alongside a two-Tuple-arg LIST closure
pass	MIXED: driving the PLAIN export alongside a two-Tuple-arg closure
pass	MIXED: driving the PLAIN export alongside an Option-arg closure
pass	MK_COMB unions its operands' hypotheses — an assumption threaded through MK_COMB survives (soundness)
pass	MK_COMB unions the hypotheses of BOTH operands when each carries a distinct assumption
pass	MOVE-ZEROS stably shifts zeros to the tail, preserving non-zero order and total length
pass	MP should match its antecedent up to α-equivalence: aconv-MP accepts an α-variant a structural equality rejects
pass	MULTI-EXPORT: THREE tuple args with a LIST result, shared `call`
pass	MULTI-EXPORT: a nested-Tuple-arg closure with a LIST result
pass	MULTI-EXPORT: among-scalars tuple arg with a BYTE-ROPE result
pass	MULTI-EXPORT: among-scalars tuple arg with a LIST result (shared interleaving list-`call`)
pass	MULTI-EXPORT: among-scalars tuple arg with a fixed-shape COMPOUND result
pass	MULTI-EXPORT: driving the FIRST two-Tuple-arg closure (add)
pass	MULTI-EXPORT: driving the SECOND Option-arg closure (Some and None)
pass	MULTI-EXPORT: driving the SECOND two-Tuple-arg LIST closure
pass	MULTI-EXPORT: driving the second among-scalars LIST closure (tuple then suffix scalar)
pass	MULTI-EXPORT: driving the second among-scalars closure (subtract)
pass	MULTI-EXPORT: driving the second nested-Tuple-arg closure (subtract)
pass	MULTI-EXPORT: driving the second scalar-then-NESTED closure (subtract)
pass	MULTI-EXPORT: two Tuple args with a scalar BETWEEN them, shared `call`
pass	MULTI-EXPORT: two Tuple-arg closures sharing a List-returning `call`
pass	MULTI-EXPORT: two Tuple-arg closures sharing a Tuple-returning `call`
pass	MULTI-EXPORT: two capturing closures each taking TWO Tuple args
pass	MULTI-EXPORT: two closures each taking a scalar arg THEN a NESTED tuple
pass	MULTI-EXPORT: two closures each taking a scalar arg THEN a Tuple arg
pass	MULTI-EXPORT: two nested-Tuple-arg closures share one call — driving the sum
pass	MULTI-EXPORT: two same-sig Option-arg closures share one `call`
pass	MULTI-EXPORT: two same-sig Result-arg closures share one `call`
pass	MULTI-EXPORT: two same-sig Tuple-arg closures share one direct-call `call`
pass	MULTI-EXPORT: two same-sig closures each taking THREE Tuple args
pass	MULTI-EXPORT: two same-sig closures each taking TWO Tuple args share one `call`
pass	MULTI-EXPORT: two two-Tuple-arg closures with a BYTE-ROPE result
pass	MULTI-EXPORT: two two-Tuple-arg closures with a LIST result share one `call`
pass	MULTI-EXPORT: two two-Tuple-arg closures with a fixed COMPOUND result
pass	MULTI-LIMB BigInts built by three arithmetic routes hash to one set element
pass	MULTI-TYPE bracket matching pushes openers on a list stack and rejects the interleave
pass	MUTUALLY recursive helpers BOTH perform against the same handler
pass	MUTUALLY-recursive MODULE-exported performers are both homed by the importer's handler
pass	Map.lookup of a map literal with a runtime key finds the value by value, not a const-fold
pass	Map.merge unions two runtime maps, last-writer (right operand) wins on an overlapping key
pass	Map.remove and Map.lookup by different-route MULTI-LIMB keys agree with the inserted key
pass	Map.remove at a RUNTIME key shrinks only on a hit
pass	Map.remove by a slice-view key drops the flat-keyed entry
pass	Map.remove drops the owned boxed key temporary it only borrows (large-int key, no leak)
pass	Map.remove of a Rational key canonicalizes: a normalized-equal key removes the entry
pass	Map.remove then len const-folds
pass	Map.remove then lookup misses
pass	Map.swap and Map.take canonicalize a runtime COMPOUND (tuple) key on their value-yielding paths
pass	Map.swap at a RUNTIME key replaces on a hit and adds on a miss through one compiled body
pass	Map.swap keyed by a runtime Bytes ROPE replaces the flat-keyed entry
pass	Map.swap keyed by a runtime Rational canonicalizes: a normalized-equal key replaces, a distinct one adds
pass	Map.take at a RUNTIME key takes the hit and miss arms through one compiled body
pass	Map.take by a slice-view key yields the flat-keyed value
pass	Map.take keyed by a runtime Rational removes the arithmetic-built canonical twin
pass	Map.to-list canonical key order is independent of insertion order
pass	Map.to-list enumerates runtime Bytes keys by unsigned-lexicographic byte order — same key-cmp as the Set element order
pass	Map.to-list enumerates the FULL interior key order, not just the smallest
pass	Map.to-list enumerates the entries as a List of (key value) tuples in canonical KEY order
pass	Map.to-list length is the map's entry count
pass	Map.to-list of a runtime-empty but key/value-typed map is the empty list
pass	Map.to-list of an empty map is the empty list
pass	Map.to-list of an empty map through an inlined helper is the empty list
pass	Map.to-list of an undetermined-type empty map literal is the empty list
pass	Map.to-list orders by SYMBOL keys while float values — NaN included — ride along
pass	Map.to-list over Float64 KEYS enumerates by canonical byte key order
pass	Map.to-list over Rational keys enumerates ascending by key value with the integer form last
pass	Map.to-list over TUPLE keys enumerates in lexicographic key order (the minimum first)
pass	Map.to-list over TUPLE keys places the middle key second (a full sort, not a min-pick)
pass	Map.to-list over a BORROWED param source reused across a loop borrows it (no consume) -- value-correct, no UAF
pass	Map.to-list over a MULTI-LEVEL trie enumerates all 100 keys fully sorted
pass	Map.to-list over a RUNTIME-built map enumerates entries in canonical key order (first key is min)
pass	Map.to-list over an owned-temporary runtime map reclaims the source, leaving no live heap objects
pass	Map.to-list over runtime STRING keys enumerates in content byte order (first is byte-smallest)
pass	McCarthy 91 — a self-call nested INSIDE the argument of another self-call
pass	N-suffixed literals compose under BigInt arithmetic
pass	NEGATIVE and HUGE indices cross the dispatch — List.at in the arm answers None-fallback for both out-of-range directions
pass	NEGATIVE values thread every effect slot — state, argument, and result stay signed
pass	NESTED CONTRACT: an @ensures predicate that CALLS a @requires-guarded helper enforces the HELPER's precondition during postcondition eval
pass	NESTED effect-free calls in the continuation compose and fold
pass	NaN equality survives the hoisted float compare by canonical byte form
pass	Num.neg in a lambda body negates the argument
pass	Num.neg negates a BigInt generically
pass	Num.neg negates a Rational generically
pass	Num.neg negates a float generically
pass	Num.neg negates a runtime signed integer generically
pass	Num.neg negates a signed integer generically
pass	Num.neg of a SECOND unusual width's minimum (Int 12) is also rejected CDZ0304
pass	Num.neg of a bound integer name yields its opposite
pass	Num.neg of a float flips the sign, preserving signed zero
pass	Num.neg of a non-numeric value is a type error
pass	Num.neg of a nonzero float
pass	Num.neg of a quantity preserves its unit
pass	Num.neg of a runtime integer parameter negates at run time
pass	Num.neg of a runtime integer returns the negation and traps at the minimum integer
pass	Num.neg of an arbitrary-precision integer
pass	Num.neg of an exact rational negates the numerator
pass	Num.neg of an in-range unusual-width value computes at the declared width
pass	Num.neg of an unusual-width minimum overflows its declared range and is rejected CDZ0304
pass	Num.neg on a NON-number operand is a compile-time type error (CDZ0201)
pass	Num.neg on an UNSIGNED integer is a compile-time type error — unsigned has no negate (CDZ0310)
pass	ONE inner map shared under TWO outer keys updates independently (no aliasing cross-talk)
pass	ONE-EDIT-APART checks strings for exactly one substitution or one insertion by length case
pass	OPEN-sum values as SET elements hash by tag and payload
pass	OR-saturation to a full-width mask on an unsigned runtime value
pass	OR-then-mask absorption does not discard a trapping runtime operand
pass	OR-then-mask absorption folds to the mask constant on a runtime operand
pass	OVERLAPPING quote patterns dispatch first-match-wins on the ambiguous scrutinee
pass	Ok carrying a Some keeps both variant tags
pass	Option.expect over a dead-after-borrowed String.slice leaks the Some shell each iteration
pass	Option.expect over an owned Some shell from List.at reclaims the shell across a loop (no live objects)
pass	Option.expect over an owned Some shell from Map.lookup reclaims the shell across a loop (no live objects)
pass	Ordering values order Less below Equal below Greater
pass	Ordering.of orders strings lexicographically in the three-way comparison
pass	Ordering.of yields the three-way ordering of two Int64 operands
pass	PAINT FENCE threads same/diff state tracks where no three consecutive posts match
pass	PAIRWISE SWAP exchanges adjacent pairs, the odd tail survives, and it self-inverts
pass	PREFIX-order edges — the state string compares against crossed op-arg strings: equal, longer-prefix, and shorter-prefix faces
pass	PRODUCT-EXCEPT-SELF composes a prefix pass and a suffix pass without division
pass	Qty arithmetic INLINE in a handler resume VALUE slot keeps the state binder's Qty type (#44 fix)
pass	Qty arithmetic on the handler state binder via an arm-local def threads and runs
pass	Qty map VALUES unwrap, ADD, and COMPARE in one chain preserving the unit through the collection
pass	Qty.pow and repeated multiplication produce the same dimension for a matching add
pass	Qty.unit recovers a derived unit
pass	Qty.unit recovers a quantity's unit to build another quantity of the same unit
pass	Qty.unit recovers the unit of a quantity whose magnitude is a runtime value
pass	Qty.value of a NON-NUMERIC operand names the type, not a self-contradictory plain number
pass	Qty.value of a conversion result is a compile-time error — the conversion already unwrapped
pass	Qty.value recovers the underlying numeric value, discarding the unit
pass	RANGE-SUM answers interval queries from a prefix table and agrees with a direct walk
pass	RLE encode/decode round-trips a list — the runtime element merges or splits a run
pass	ROTATE-90 composes row-reverse with transpose and four rotations restore the matrix
pass	Rational.neg negates an exact rational, total
pass	Rational.of over TWO perform results — ctor-arg order observable through the dispatches
pass	Record.extend ADDS a closure field beside two existing ones and all three apply
pass	Record.extend chained onto Record.without of a map-borne record computes, not traps
pass	Record.extend widens a map-extracted record and the wide row enters a new map
pass	Record.merge carries HEAP-list fields from both operands into the merged layout
pass	Record.merge carries a CLOSURE field into the union layout and it applies
pass	Record.merge of two runtime records leaves no live heap objects
pass	Record.pop hands back a CLOSURE field as the popped value and it applies
pass	Record.pop over a runtime record (value, rest) leaves no live heap objects
pass	Record.project over a runtime record leaves no live heap objects
pass	Record.project over runtime field values keeps the named fields and their live values
pass	Record.with REPLACES one heap-list field while the sibling and the ORIGINAL stay live
pass	Record.with grows a LIST field by pushing onto the projected old value
pass	Record.with on a map-extracted record leaves the stored original untouched
pass	Record.with over a RUNTIME field leaves the original record readable (persistence)
pass	Record.with over a perform result — the ORIGINAL record survives beside the update
pass	Record.with over a runtime record: a PRESERVED sibling reads its source value
pass	Record.with over a runtime-produced record leaves no live heap objects
pass	Record.with replaces a nested-record field wholesale and the original keeps its inner
pass	Record.without over a runtime record leaves no live heap objects
pass	Record.without over runtime field values drops the named field and keeps the rest live
pass	Record.without strips a field on a map-extracted record and both maps stay typed
pass	Result ordering agrees across targets — Ok below Err on the shared declaration order
pass	Result propagates errors without trapping
pass	Result type for fallible operations
pass	SIGNED branch refinement over NEGATIVE bounds must not over-refine the inner compare
pass	STACKED @requires and @ensures both enforce at runtime on one def with a heap precondition
pass	STACKED @requires: EVERY precondition is enforced — a violated OUTER @requires traps (not only the innermost)
pass	STACKED @requires: value-transparent when ALL preconditions are satisfied
pass	STRING COMPRESSION emits char-count pairs but keeps the original unless strictly shorter
pass	STRING ROTATION check searches the needle inside the doubled haystack at equal length
pass	SUBSET-SUM COUNT folds each element into a fresh ways-table so no element is reused
todo	SUM ARG + LIST RESULT declines cleanly (Option arg, List result)
pass	SYMMETRIC difference built two ways from primitives agrees and dedups the overlap
pass	Set.contains dispatches on an Option element at a runtime payload
pass	Set.contains does not fold a set whose element is a runtime scalar
pass	Set.contains finds a runtime STRING-rope element built via Set.of
pass	Set.contains finds a separately-built equal quantity but not a non-member
pass	Set.contains on a Map-looked-up Set with a perform-threaded element
pass	Set.contains over nan finds nan
pass	Set.contains over negative zero does not find positive zero
pass	Set.difference const-folds a membership count
pass	Set.difference over two owned-temporary runtime sets reclaims both operands and the result (no live objects)
pass	Set.insert does not fold a duplicate against a runtime element
pass	Set.intersection and Set.difference match a view against a flat element across operands
pass	Set.intersection const-folds a membership count
pass	Set.intersection over two owned-temporary runtime sets reclaims both operands and the result (no live objects)
pass	Set.intersection unifies an arithmetic-produced rational with its constructor-built normalized twin
pass	Set.of of a Set.to-list result reconstructs the same set (the enumeration round-trips)
pass	Set.of of a computed (concatenated) runtime list dedups by value
pass	Set.of over Rational elements dedupes a normalized-equal literal and to-list enumerates ascending
pass	Set.of over Set.to-list round-trips a 100-element trie to the identical set
pass	Set.of over a runtime list of TUPLE elements reconstructs the same set through the synthesized fold
pass	Set.remove does not fold a set whose element is a runtime scalar
pass	Set.remove drops the owned boxed element temporary it only borrows (large-int elem, no leak)
pass	Set.remove of a rope-element set built via Set.of lowers the cardinality
pass	Set.remove then contains const-folds to false
pass	Set.to-list canonical order is SIGNED at the integer extremes
pass	Set.to-list canonical order is independent of insertion order
pass	Set.to-list enumerates the FULL interior order, not just the smallest element
pass	Set.to-list enumerates the elements as a List in canonical (sorted) order
pass	Set.to-list length is the deduped distinct-element count
pass	Set.to-list length is the set's cardinality (deduped)
pass	Set.to-list of a CONSTANT empty set through an inlined nullary is the empty list
pass	Set.to-list of a runtime-empty but element-typed set is the empty list
pass	Set.to-list of the state crosses resume ORDERED — the body reads elements positionally
pass	Set.to-list orders (Int,Bytes) tuples with the Bytes component as the tie-breaker
pass	Set.to-list orders a multibyte string element AFTER ascii by unsigned byte order
pass	Set.to-list orders a set of COMPOUND (tuple) elements lexicographically
pass	Set.to-list orders a set of RECORD elements canonically
todo	Set.to-list orders float-carrying sums discriminant-first then by float payload
pass	Set.to-list orders numerically across the fixnum/boxed representation seam
pass	Set.to-list orders runtime Bytes elements by unsigned-lexicographic byte order — 0x80 sorts LAST, not as signed -128
pass	Set.to-list over Float32 elements enumerates by canonical byte order at f32 width
pass	Set.to-list over Float64 elements enumerates by canonical byte order
pass	Set.to-list over Option elements enumerates Some-first per the declared order
pass	Set.to-list over Rational elements enumerates ascending by value, not by numerator/denominator pair
pass	Set.to-list over STRING elements orders by content with a rope participating
pass	Set.to-list over a 100-element trie enumerates strictly increasing end to end
pass	Set.to-list over a BORROWED param source reused across a loop borrows it (no consume) -- value-correct, no UAF
pass	Set.to-list over a RUNTIME-built set yields canonical order (first element is the minimum)
pass	Set.to-list over a newtype (Int64-payload) element enumerates via the newtype's orderable payload
pass	Set.to-list over a runtime set sums its distinct elements
pass	Set.to-list over an all-nullary sum enumerates in discriminant order
pass	Set.to-list over an owned-temporary runtime set reclaims the source, leaving no live heap objects
pass	Set.to-list over float-leaf tuple elements is a coded CDZ0203 — a float-containing compound offers no total order (§319, 03:626 companion)
pass	Set.to-list over set/map-leaf elements is a coded CDZ0203 — a set/map leaf carries no blessed total order (all-leaf sibling of the float case)
pass	Set.to-list over symbols enumerates in content-byte order regardless of insertion order
pass	Set.to-list places a NaN element after the positives but before the negatives, by canonical byte order
pass	Set.to-list sorts a PREFIX string before its extension (length tiebreak on equal prefixes)
pass	Set.to-list sorts runtime ROPE string elements smallest-first by content byte order
pass	Set.union const-folds a membership count
pass	Set.union dedups a slice-view element against a flat twin ACROSS the operand boundary
pass	Set.union over two owned-temporary runtime sets reclaims both operands and the result (no live objects)
pass	Sim.now reads the scheduler clock — a tail-resumptive handler arm that works today
pass	String ordering drives an insort over runtime ROPES, verified by content
pass	String.at addresses scalars across the seam of a runtime multibyte rope
pass	String.at and String.slice with near-i64::MAX indices decline to None, not wrap
pass	String.at on a decomposed literal returns the composed scalar at the normalized index
pass	String.at reads a multibyte scalar whole at its position in a runtime two-chunk rope
pass	String.at through a slice VIEW of a rope reads view-relative scalars at every byte width
pass	String.at with an index at 2^32+2 declines to None, not an i32-wrapped read
pass	String.byte-len of a runtime rope agrees with Bytes.len of its to-bytes image
pass	String.concat with an empty operand is the identity on a RUNTIME string (the emit path, not the fold)
pass	String.from-bytes accepts a two-byte sequence split across a concat seam
pass	String.from-bytes decodes a 400-byte 200-seam rope end to end
pass	String.from-bytes decodes a RUNTIME byte sequence built by a recursive appender
pass	String.from-bytes decodes a multibyte RUNTIME Bytes ROPE, flattening before validation
pass	String.from-bytes does not NFC-normalize — a decomposed form stays distinct (known gap)
pass	String.from-bytes of a runtime slice decodes the WINDOW only
pass	String.from-bytes of an ill-formed RUNTIME byte sequence yields None (never traps)
pass	String.from-bytes over a dead-after-borrowed compound Some shell leaks it each iteration
pass	String.from-bytes over a rope-backed slice accepts aligned windows and rejects a mid-scalar cut
pass	String.from-bytes rejects a lone continuation byte
pass	String.from-bytes rejects a slice that splits a multibyte scalar and accepts aligned cuts
pass	String.from-bytes rejects an overlong encoding
pass	String.from-bytes validates a multi-byte scalar straddling a runtime byte-rope's seam
pass	String.scalar-at over a runtime index reads the scalar-th Unicode scalar as a Char, fallibly
pass	String.scalar-len of a string selected by a runtime if
pass	String.scalar-len of a string selected by a runtime match
pass	String.scalar-len over an owned-temporary runtime rope leaves no live heap objects
pass	String.slice across a multibyte scalar carries byte-len 2 with scalar-len 1, and degenerate ranges answer Some-empty / None
pass	String.slice of a Map-looked-up String with perform-threaded start and end folds
pass	String.slice spanning a rope seam maps a scalar range to the exact byte extent
pass	String.to-bytes of a genuinely-runtime string yields its UTF-8 byte length
pass	String.to-bytes of a rope round-trips through from-bytes equal, and a mid-scalar cut declines
pass	String.to-bytes of a runtime rope reused across four branch arms compiles to a VALID module
pass	String.to-bytes of a runtime string threaded through recursion round-trips its bytes
pass	Symbol.of over a slice VIEW interns exactly the window, flat or rope-backed
pass	Symbol.of over a slice of a dying rope interns the WINDOW content
pass	Symbol.to-string output compares equal to a slice VIEW of matching content
pass	THREE Tuple args cross the direct-call boundary
pass	THREE Tuple args with a LIST result
pass	THREE performs nested in one expression — each op's result is the next op's argument, three distinct arms and strides
pass	THREE stacked guards on one constructor classify a heap payload into bands in order
pass	TO-DIGITS and FROM-DIGITS round-trip a number through its base-10 digit list
pass	TOP-K keeps the k largest via a bounded descending insort that drops the smallest overflow
pass	TRANS carries a hypothesis borne by the RIGHT operand only (mirror of the left-only pin)
pass	TRANS chained three deep accumulates every operand's hypotheses
pass	TRANS should match its middle term up to α-equivalence: aconv-TRANS chains an α-variant a structural equality rejects
pass	TRANS unions its operands' hypotheses — an assumption threaded through TRANS survives (soundness)
pass	TRANS unions the hypotheses of BOTH operands when each carries a distinct assumption
pass	TRIAL-DIVISION primality steps odd candidates to the square bound and reports the witness divisor
pass	TWO @ensures-guarded defs COMPOSE along a tail call — the inner postcondition fires on the returned value, the outer on the same value at its own exit
pass	TWO Bytes fields project from a record and re-concat into a frame (the packet idiom)
pass	TWO Map-stated handlers stacked route each op to its own Map with no cross-contamination
pass	TWO Record args cross the direct-call boundary
pass	TWO Tuple args cross the direct-call boundary
pass	TWO Tuple args with a BYTE-ROPE result
pass	TWO Tuple args with a LIST result cross the direct-call boundary
pass	TWO Tuple args with a fixed-shape COMPOUND result
pass	TWO Tuple args with a scalar BETWEEN them (tuple, scalar, tuple)
pass	TWO closures capture DIFFERENT generations of one shadowed name and each sees its own
pass	TWO closures capture one let-bound perform result — the effect fires ONCE
pass	TWO closures capture the SAME heap map and the map outlives both applications
pass	TWO closures capturing one let-bound host call in a HELPER def share ONE firing
pass	TWO closures capturing one let-bound host call in a HELPER def's RECORD share ONE firing
pass	TWO closures capturing one let-bound host call share ONE firing (in the exported def)
pass	TWO closures escaping one handle carry DISTINCT captured state reads
pass	TWO closures minted by the same op at DIFFERENT states each keep their own snapshot
pass	TWO different two-site ops dispatched in segments fold (multi-multi mixing, A A B B)
pass	TWO distinct @invariant-typed PARAMETERS of one def each establish at their OWN argument construction site
pass	TWO distinct nonzero BigInt literal probes dispatch in one recursive fn
pass	TWO guarded map arms over a PARTIAL-CONST map fall through, each re-resolving its value binder
pass	TWO loop-carried ropes each rebind to derived strings every recursion step
pass	TWO match inits in one let SEQUENCE - the second scrutinizes the first's binding
pass	TWO modules' recursive performers interleave under ONE handler's shared state
pass	TWO outer op-results as SIBLING args of one inner perform evaluate left-to-right
pass	TWO performs in an if condition both fold on the strict-first spine
pass	TWO prefix scalars then a Tuple then a suffix scalar cross the direct-call boundary
pass	TWO sequential handles of the same effect — the second starts fresh, no state bleed
pass	TWO sequential inner handles seed from the outer's ADVANCING state
pass	TWO sibling inner handlers observe through ONE outer counter
pass	TWO stacked @ensures COMPOSE: BOTH postconditions are enforced — value-transparent when both hold
pass	TWO stacked @ensures: a violated INNER postcondition traps even when the OUTER holds
pass	TWO-SUM finds a complement pair via a seen-set built during one walk
pass	Tuple.concat over a non-tuple operand names the op and the non-tuple type
pass	Tuple.remove over a non-tuple operand names the op and the non-tuple type
pass	Tuple.size over a non-tuple operand names the op and the non-tuple type
pass	Tuple.split-at over a non-tuple operand names the op and the non-tuple type
pass	Type is a first-class value
pass	Type.ast INSTANTIATES a generic type's params; Type.ast-generic keeps them verbatim (they differ)
pass	Type.ast instantiates a generic with a COMPOUND type argument, keeping the nested application named
pass	Type.ast of a NON-CONCRETE type is a coded rejection — a type variable has no definition to reflect
pass	Type.ast on a MUTUALLY-recursive generic stays finite — a cross-type reference is not unfolded
pass	Type.ast on a RECURSIVE generic stays finite — a nested self-reference is not unfolded
pass	Type.ast on a non-generic type coincides with Type.ast-generic (verbatim decl)
pass	Type.ast on a transparent alias reflects its own declaration verbatim and both forms coincide
pass	Type.ast reflects a BUILT-IN type's definition (Option, Result) like a user type
pass	Type.ast reflects the type-of-types Type itself as the bare Ast.Name Type
pass	Type.ast-generic of a FUNCTION type reflects its arrow surface (-> …)
pass	Type.ast-generic reflects a STRUCTURAL type's canonical type-surface AST (increment 2)
pass	Type.ast-generic reflects a nominal sum type's verbatim declaration AST
pass	Type.eq branches on a type-valued parameter, monomorphized per passed type
pass	Type.eq carries an integer type's full width and signedness
pass	Type.eq compares a RECORD type-value structurally, by field types
pass	Type.eq compares a TUPLE type-value structurally, by element types
pass	Type.eq compares a reflected type against a written type
pass	Type.eq compares two type-valued parameters to each other
pass	Type.eq distinguishes a constructed generic sum by its type argument
pass	Type.eq distinguishes quantities by their unit
pass	Type.eq is false for values of different types
pass	Type.eq is true for two values of the same type
pass	Type.eq of a type-valued parameter against itself is always true
pass	Type.eq on a constructed generic sum compares the full instantiated type
pass	Type.eq on a type-valued parameter against a COMPOUND type is structural
pass	Type.of carries a quantity's unit into a same-type annotation
pass	Type.of grounds a Map-value function nested inside a tuple
pass	Type.of grounds a RETURNED function's domain — a closure whose result is a function
pass	Type.of grounds a function nested TWO containers deep — a sum payload inside a list
pass	Type.of grounds a function nested TWO containers deep — a tuple inside a sum payload
pass	Type.of grounds a function stored as a runtime Map value
pass	Type.of grounds a function stored in a RECORD FIELD
pass	Type.of grounds a function stored inside a SUM VARIANT payload
pass	Type.of grounds a function stored inside a compound value's element
pass	Type.of on a bare nullary variant reflects its element as UNDETERMINED
pass	Type.of over a PERFORM result reflects the op's declared result type
pass	Type.of reflects a RETURNED function's body-solved domain, at every currying level
pass	Type.of reflects a function's BODY-SOLVED domain, distinguishing different parameter types
pass	Type.of reflects a runtime parameter's type at compile time
pass	Type.of reflects a value's type for use as an annotation
pass	Type.of reflects equal for two functions of the SAME TYPE stored as a MAP VALUE
pass	Type.try-as at a DIFFERENT type yields None
pass	Type.try-as at a value's own type yields Some carrying the value
pass	Type.try-as branches on which type a value has, folded at compile time
pass	Type.try-as views a RUNTIME value at its declared type
pass	UInt16.wrap at exactly two-to-the-sixteenth is zero
pass	UInt16.wrap one past two-to-the-sixteenth is one
pass	UInt64.max crosses the parameter boundary and back unchanged
pass	UInt64.max crossing the boundary compares as a large unsigned, not -1
pass	UInt8.wrap of a constant past the byte boundary truncates to the low byte
pass	UInt8.wrap of a negative constant reads the low byte as unsigned
pass	Unit.in converts a quantity to a chosen larger unit (Float)
pass	Unit.in converts a quantity to a chosen smaller unit exactly (Int)
pass	Unit.in of a NON-NUMERIC operand names the type, not a self-contradictory plain number
pass	Unit.in over a BigInt quantity truncates a non-dividing ratio
pass	Unit.in over a BigInt-magnitude quantity converts in unbounded arithmetic and unwraps
pass	Unit.in over a runtime Rational quantity keeps a fractional scale exact
pass	Unit.in over a runtime Rational-magnitude quantity converts exactly and unwraps
pass	Unit.in to a unit of a different dimension is a compile-time error
pass	WAVE reorder alternates <= and >= by index parity through a carried-element walk
pass	XOR cancellation returns the original runtime value (constant key)
pass	XOR cancellation returns the original value even when the key is a runtime value
pass	ZERO threads every effect slot (zero seed, zero args, zero results)
pass	`?` on the failure variant short-circuits the boundary (Option, None path)
pass	`?` on the success variant unwraps the payload (Option, happy path)
pass	`?` unwraps a COMPOUND (tuple) payload
pass	`?` unwraps an Ok payload under a Result boundary (happy path)
pass	`at` advances an Instant by a Duration (wake-time computation)
pass	`before?` is a STRICT order — an Instant is not before an equal Instant (same-time is a tie-break)
pass	`before?` orders two Instants (the event-queue comparison)
pass	`since` is the span between two Instants (later minus earlier)
pass	`sleep-until` sleeps to an absolute Instant — the span is target minus now (since t now)
pass	`try` takes exactly one operand — a two-operand form is malformed
pass	`try` takes exactly one operand — a zero-operand form is malformed
pass	a 100-key runtime map resolves every key through the trie
pass	a 1000-element set drains through Set.remove to empty
pass	a 1000-iteration concat loop builds a 4000-byte rope measured exactly
pass	a 1000-step UPSERT fold builds a 7-bucket histogram — persistent-map churn under real load
pass	a 100k-iteration pure tail loop under a handler runs in constant stack
pass	a 1100-element prepend-built runtime list reads every index through a multi-level RRB front-spine
pass	a 1100-element runtime list resolves every index through a multi-level RRB trie
pass	a 1100-element runtime set resolves every member through a multi-level CHAMP trie and rejects non-members
pass	a 16-bit bit-field packs a two-nibble-distinct value across the byte boundary big-endian
pass	a 16-field MIXED-type record projects across the layout and survives a mid-layout Record.without
pass	a 2-arm all-literal Float32 match grounds its arms to f32 via the select path
pass	a 2-arm match→SELECT with a performed scrutinee and pure arms stays sound
pass	a 200-element runtime Set of BYTES resolves every member through a multi-level CHAMP over the blessed byte order
pass	a 200-element runtime Set resolves every member through a multi-level CHAMP trie and rejects non-members
pass	a 2000-deep runtime Bytes.concat rope flattens iteratively and reads its content stack-safe
pass	a 2000-entry built map answers first, middle, and last keys through deep CHAMP levels
pass	a 2000-entry map fully drains to empty and regrows on the emptied trie
pass	a 2x2 MATRIX POWER by squaring recovers fibonacci and satisfies the determinant identity
pass	a 2x2 matrix MULTIPLY composes row extraction, column extraction, and a zipped dot product
pass	a 3-FIELD Tuple ARG among scalars crosses the direct-call boundary
pass	a 3-arg effect op binds its operands POSITIONALLY — an argument-order swap is caught
pass	a 3-arm all-literal Float32 match grounds its arms to f32 via the probe-chain path
pass	a 32-bit-float closure crosses the host boundary
pass	a 32-bit-integer closure crosses the host boundary
pass	a 33-VARIANT sum dispatches across the discriminant range with a payload variant last
pass	a 40-element SET op result — a multi-node CHAMP payload crosses resume
pass	a 40-element list as op ARGUMENT — the arm folds a multi-leaf RRB payload
pass	a 40-element list op RESULT crosses resume — a multi-leaf RRB payload survives the marshal
pass	a 40-entry trie of LIST values keeps each heap payload addressable at depth
pass	a 500-iteration Bytes.concat loop measures exactly and indexes both extremes
pass	a 5000-deep recursive-sum spine builds, walks, and compares — the eq walk survives depth
pass	a 5000-element pushed list reads head, middle, and last through the deeper trie
pass	a 60-key trie captured ACROSS a host call reads intact after the response folds in
pass	a 64-bit-unsigned closure crosses the host boundary
pass	a >= Int64.min tautology keeps a trapping div operand (still traps at z=0, folds otherwise)
pass	a >= Int64.min tautology keeps an overflowing operand (checked-add traps at z=MAX)
pass	a @ensures annotation with two predicate arguments is rejected
pass	a @ensures annotation with zero predicate arguments is rejected
pass	a @ensures postcondition over a HEAP result checks at exit and returns the live collection
pass	a @ensures relating the RESULT to a PARAMETER enforces the relation at exit
pass	a @param accessor read inside a RECURSIVE walk consumes one response per iteration
pass	a @param accessor splices into a quasiquote and the eval computes with the host value
pass	a @param drives a recursive fold's iteration count
pass	a @param of type (Qty Rational unit) desugars to num/den scalars + a guest Qty.of with the unit (#13 B2)
pass	a @param of type Rational desugars to two scalar num/den accessors the guest recombines (#13)
pass	a @param positions a slice window over a guest-built byte rope
pass	a @param selects a match arm and a SECOND param feeds the selected body
pass	a @param slice window at an interior cut reads the non-straddling bytes
pass	a @param slice window whose length exceeds the remaining bytes yields None
pass	a @param value SEEDS an in-program handler's state
pass	a @param value crosses into a closure capture and applies
pass	a @param value drives a CHAMP map lookup as the KEY
pass	a @param value sizes a guest-built HEAP structure
pass	a @requires annotation with two predicate arguments is rejected
pass	a @requires annotation with zero predicate arguments is rejected
pass	a @requires on a UNIT-returning def is enforced — the precondition traps before the unit body
pass	a @requires on a recursive def is re-checked at every entry including self-calls
pass	a @requires over a FLOAT64 parameter enforces at runtime — the check fires on float ordering, not only Int64
pass	a @requires precondition is enforced for a genuinely-runtime argument
pass	a @requires predicate over a HEAP collection is enforced at entry and value-transparent when satisfied
pass	a @requires predicate that CALLS a user-defined function resolves and enforces — not only prelude ops
pass	a @requires predicate that MATCHES on a sum-typed parameter dispatches and enforces (v-patterns seam)
pass	a @requires predicate that calls a RECURSIVE user helper evaluates soundly — predicate eval drives recursion to a fixpoint
pass	a @requires predicate that is NOT Bool-typed is REJECTED CDZ0203 — a predicate must denote a truth value
pass	a @requires predicate that itself traps keeps its own trap kind
pass	a @requires predicate that references `ret` is REJECTED CDZ0101 — only @ensures binds the result
pass	a @requires reads a BOOL parameter DIRECTLY as the predicate — the flag itself is the precondition, false traps
pass	a @requires relating TWO heap params enforces the zip precondition at runtime
pass	a @requires whose predicate is a DISJUNCTION (or) enforces: either disjunct satisfies, only the all-false input traps
pass	a @requires whose predicate is a NEGATION (not) enforces: the wrapped condition must be FALSE, its truth traps
pass	a @requires-guarded def PERFORMS in its body — contract check and effect specialization compose
pass	a @tag annotation over a non-string bare name is rejected
pass	a @tag annotation over a non-string number is rejected
pass	a @tag annotation with two arguments is rejected
pass	a @tag annotation with zero arguments is rejected
pass	a @test-stacked @requires+@ensures def keeps full contract enforcement for a direct call
pass	a @test-tier @ensures on a performing def runs and checks
pass	a BARE (unqualified) constructor pattern over an abstract proof type outside the kernel is also a withheld-constructor rejection
pass	a BARE mixed-width variant as the direct host-op arg emits, loads, and runs (via an imposed WIT world)
pass	a BARE nested nullary-variant arm is a ctor, not a binder — never warns unused or unreachable
pass	a BARE pattern on a withheld constructor is rejected like the qualified spelling (encapsulation soundness)
pass	a BARE-param escaping closure capturing a handled value FOLDS to 17 (matches the annotated-param twin)
pass	a BARE-param recursive state-threading walk compiles without leaking the specialization name
pass	a BASE-N conversion round-trips one value through three runtime radices
pass	a BIGINT handler state multiplies per perform and each resume reads the prior product
pass	a BINARY SEARCH halves a lo/hi window over a sorted list read by List.at
pass	a BLOCK-wrapped OUTER perform in a do-statement inside a nested handle body folds to 73 (adv-69 c3-nested)
pass	a BLOCK-wrapped branch perform in a MATCH-SCRUTINEE folds through the block (adv-69 g3)
todo	a BLOCK-wrapped branch perform in a NESTED handler-arm resume-value declines cleanly (adv-69 a3 sub-face)
pass	a BLOCK-wrapped branch perform in a let-init folds through the block (adv-69 Site-6 commuting conversion)
pass	a BLOCK-wrapped branch perform in a non-tail DO-STATEMENT folds to 73 (adv-69 c3 sub-face)
pass	a BLOCK-wrapped branch perform in a non-tail DO-STATEMENT folds to 73 (adv-69 c3)
pass	a BOOL as a Map key routes a runtime lookup to the right entry
pass	a BOOL-field Tuple closure ARG flattens + rebuilds (box-bool imported)
pass	a BOOL-field Tuple closure ARG, false discriminant
pass	a BOOL-result effect op threads state across two performs on a boolean connective's operands
pass	a BORROWED runtime Bytes rope map key is compacted at the key site and found by its flat twin
pass	a BRANCHING tree walk performs once per leaf at 200-leaf scale
pass	a BST built by comparison-driven inserts reads back sorted via in-order traversal
pass	a BigInt arithmetic tower over one perform draw — cube then divide, narrowed once
pass	a BigInt comparison drops both owned operands and leaves no live heap objects
pass	a BigInt computed across the limb boundary keys a Map like its literal twin
todo	a BigInt entry argument is marshalled through the value's own constructor
todo	a BigInt entry parameter added to a beyond-i64 annotated literal
todo	a BigInt entry parameter is COMPUTED on, not just compared
pass	a BigInt factorial accumulator computes 25! exactly
pass	a BigInt is usable as a map key, matched by its arbitrary-precision value
pass	a BigInt is usable as a set element, deduplicated by its arbitrary-precision value
pass	a BigInt mixed with a fixed-width integer is the numeric no-promotion error, with a BigInt.of coercion fix
pass	a BigInt narrowed to the exact Int8 maximum and minimum fits and converts
pass	a BigInt narrowed to the exact UInt8 maximum fits and converts
pass	a BigInt operation does not silently promote a fixed-width operand
pass	a BigInt-inner quantity ADDITION past Int64.max stays exact through the wrapper
pass	a BigInt-inner quantity Set dedups equal elements and keeps distinct ones
pass	a BigInt-inner quantity grows past Int64.max without trapping
pass	a BigInt-inner quantity scales by a bare BigInt on the bigint path
pass	a BigInt-inner quantity stored in a List reads back through List.at
pass	a Bool annotation on a Bool value is transparent
pass	a Bool argument to a parameter used in integer addition is rejected
pass	a Bool generator derived from the seed is reproducible (same seed, same Bool)
pass	a Bool host arg BESIDE a scalar and a String composes in one mixed-arity op
pass	a Bool let-binder init annotated Int64 has no coercion and carries no fix
pass	a Bool match with both true and false arms is exhaustive and runs
pass	a Bool match with its arms in either order is exhaustive
pass	a Bool match with only the false arm is non-exhaustive
pass	a Bool match with only the true arm is non-exhaustive
pass	a Bool match with two of the same literal is non-exhaustive
pass	a Bool value under a narrow-width let-binder annotation is a type clash, not a width fault
pass	a Bool-payload ctor BINDER used as a Bool in an if computes on the compiled path
pass	a Bool-returning comparison operator passed to an inline HOF
pass	a Bytes ROPE handler state GROWS by bin-append per perform and each resume reads the prior length
pass	a Bytes entry argument is marshalled as a Vec<u8>
pass	a Bytes field projected off a nullary record with Ast siblings folds byte-exact and eliminates the record
pass	a Bytes slice VIEW and a rope compare equal by content in both directions
pass	a Bytes slice of a slice composes offsets against the VIEW and bounds against its length
pass	a Bytes value SENT to the host is still readable after the call (the arg marshal borrows)
pass	a Bytes where a String is expected offers no fix (the decode is fallible)
pass	a Bytes-compact key and a flat rebuild both hit a rope-keyed map entry; a proper-prefix slice misses
pass	a Bytes-returning closure alongside a plain export — the closure runs
pass	a Bytes-returning closure alongside a plain export — the plain runs
pass	a Bytes-returning closure on a different argument
pass	a Bytes-to-Bytes transformer op — the arm frames the payload it received and the body re-reads
pass	a Bytes.slice VIEW crosses as op ARGUMENT — the arm reads through the window it was handed
pass	a Bytes.slice into the to-bytes output decodes at a scalar boundary and rejects mid-scalar
pass	a CAESAR cipher shifts alphabet positions modulo 26, involutes at ROT13, passes non-letters
pass	a CALL-returned single-variant newtype with a literal-payload arm spills at the right width
pass	a CAPTURING closure carried in a sum payload keeps its capture through the match binder
pass	a CAPTURING closure returning a List
pass	a CAPTURING closure returning a tuple
pass	a CAPTURING closure taking TWO Tuple args
pass	a CAPTURING closure taking TWO Tuple args with a LIST result
pass	a CAPTURING closure taking a Result Int64 Int64 ARG crosses the direct-call boundary
pass	a CAPTURING closure taking a Result Int64 Int64 ARG — Err arm uses the captured value
pass	a CAPTURING closure taking a Tuple ARG crosses the DIRECT-CALL boundary
pass	a CAPTURING closure taking an Option Int64 ARG crosses the direct-call boundary
pass	a CAPTURING closure taking an Option Int64 ARG — None takes the captured value
pass	a CBOR atom decodes each scalar major type to its value
pass	a CBOR head decodes its major type and big-endian argument from the input bytes
pass	a CBOR reader walks a variable-length array using its decoded length as the element count
pass	a CBOR simple value that is not a known boolean is classified as not-a-boolean
pass	a CBOR skip steps over a tagged item to the value it wraps
pass	a CBOR skip walks past a whole nested item to the next offset
pass	a CLASSIFY-and-BUILD fold maps ints to letter pieces and the built rope equals the literal
pass	a CLOSED single-variant sum's sole-constructor arm is exhaustive without a `_` (the newtype-erasure control)
pass	a CLOSURE as the op ARGUMENT — the arm applies the caller's strategy to its own state
pass	a CLOSURE handler state captures the enclosing function's parameter and applies per dispatch
pass	a CLOSURE state whose body performs an OUTER effect when the arm applies it
pass	a COMPOUND generated value is reproducible from its seed (a tuple draws identically twice)
pass	a COMPOUND map key with a Rational leaf hashes+matches by the leaf's normalized form
pass	a COMPOUND-inner single-variant newtype takes the heap escape and its inner is read back
pass	a CONCRETELY-exported error sum flows back through two call layers and dispatches at the entry
pass	a CONDITIONALLY-recursive helper reaching a performer in one branch folds where no outer recursion needs the boundary guard
pass	a CONSECUTIVE dedup collapses runs but keeps recurrences separated by other values
pass	a CONST in-range rational ceil folds toward positive infinity with the remainder-sign adjustment
pass	a CONST in-range rational floor folds toward negative infinity with the remainder-sign adjustment
pass	a CONST in-range rational truncate still folds to its integer part, not over-rejected as overflow
pass	a CONST narrow wrap folds with sign-extension at the target width, matching the runtime wrap
pass	a CONST rational ceil whose integer part exceeds Int64 rejects CDZ0304 at the fold, not a span-less CDZ0302
pass	a CONST rational floor whose integer part exceeds Int64 rejects CDZ0304 at the fold, not a span-less CDZ0302
pass	a CONST rational round above the half rounds up to the nearer integer
pass	a CONST rational round below the half rounds down to the nearer integer
pass	a CONST rational round folds an exact half away from zero
pass	a CONST rational round whose integer part exceeds Int64 rejects CDZ0304 at the fold, not a span-less CDZ0302
pass	a CONST rational truncate whose integer part exceeds Int64 rejects CDZ0304 at the fold, not a span-less CDZ0302
pass	a CONST-FOLDED taken branch with a Float32-overflowing literal is rejected, not folded past the check
pass	a CONSTANT algebraic identity over a high-bit UInt64 operand folds to the operand, not a spurious CDZ0304 decline
pass	a CONSTANT bare-Int64 left shift whose result overflows Int64 is rejected
pass	a CONSTANT comparison over a FOLDED wide-UInt64 subexpression carries the folded value unsigned
pass	a CONSTANT comparison over a high-bit UInt64 operand folds unsigned, not through a signed i64 carrier
pass	a CONSTANT in-range Int 12 truncation keeps its value (no sign bit set)
pass	a CONSTANT match on a high-bit UInt64 scalar decides its literal patterns unsigned
pass	a CONSTANT match on the Int64.min/max boundary decides its literal patterns exactly
pass	a CONSTANT recursive sum compares equal to a differently-built RUNTIME one
pass	a CONSTANT shift by a count at or beyond the UInt64 bit width is rejected
pass	a CONSTANT signed nibble truncation of an all-ones nibble is -1
pass	a CONSTANT signed nibble truncation sign-extends — the const-fold companion of the runtime .wrap
pass	a CONSTANT signed truncation of an all-ones Int 12 is -1
pass	a CONSTANT signed truncation sign-extends at a WIDER storage width (Int 12, i16 storage)
pass	a CONSTANT wide-UInt64 left shift whose result overflows the width is rejected
pass	a CONSTANT wide-UInt64 shift/bitwise op folds over the solved width, not the i64-only path
pass	a CONSTANT-failure `?` inside the arm's helper — the cut stays in the helper, dispatch unharmed
pass	a CONTINUED FRACTION evaluates bottom-up through recursive Rational division (a + 1/rest)
pass	a CONTRACTED fn consumes perform results — @requires fires on a handler-produced value
pass	a CROSS-handler op whose inline ARG performs an OUTER handler's op folds (op-arg let-lift)
pass	a CURRIED closure capturing an inner-handled perform result closes over it through partial application
pass	a Char against a number keeps its total-conversion wrap fix, not a dead-end boundary
pass	a Char const param folds a VALUE through a bare recursive call
pass	a Char threaded through a recursion const-folds (cb04)
pass	a Char-payload sum value escapes across the boundary in its canonical form
pass	a DEAD-branch Float32 overflow folds away and the taken branch runs (no over-rejection)
pass	a DEEP recursive NAME-HEAD tuple destructure-let terminates within deadline — guards the ML/paren-surface path (#6042)
pass	a DEEP recursive tuple destructure-let TERMINATES within deadline — the #6000 init-re-eval hang regression guard
pass	a DEFERRED resume-thunk escaping to another function re-installs the handler at apply, over a re-performing do-continuation
pass	a DEPTH-2 block-wrapped branch perform in a let-init folds through the block (adv-69 Site-6, nested wrappers)
pass	a DEPTH-3 nested-op chain WITHOUT a post-observer folds (the no-observer control for the observer-gated guard)
todo	a DEPTH-3 nested-op chain whose deepest resume performs the outer effect declines cleanly (no silent drop)
pass	a DEPTH-3 op-arg chain threads across two handler layers
pass	a DEPTH-3 same-effect shadow ladder — each shadow's seed draws from the ENCLOSING handler, strides 1 2 3 stay separate
pass	a DERIVED-dimension (velocity) quantity used as a Map key hits by content
pass	a DETERMINED nested-list Set key bakes its shape and compiles (the determinacy control)
pass	a DIFFERENT-effect abort after an inner recursive advance reads its own un-advanced seed (cx1)
todo	a DIRECT branch perform in a NESTED handler-arm resume-value declines cleanly (adv-69 a3-direct sub-face)
pass	a DIRECT-conditional OUTER-effect perform in a let-init inside a nested handler body folds (adv-69 a4 control)
pass	a DIRECT-init branch perform in a let-init threads its state advance (adv-69 control, still computes)
pass	a DOTTED nullary-variant arm binds nothing and never warns unused (a ctor, not a binder)
pass	a DOUBLE-NESTED tuple trailing-rest binding param recurses into the nested rest pattern
pass	a DOUBLY-nested Tuple ARG (three levels deep) crosses the direct-call boundary
pass	a DOUBLY-nested projected list, consumed then read, is unchanged (child retain through a proj chain)
pass	a DRAINED set compares equal to the literal empty set — removal restores the canonical empty rep
pass	a DUTCH-FLAG three-way partition routes below/equal/above and reassembles sorted
pass	a Duration constructor `secs` scales a UInt64 count to nanoseconds
pass	a Duration constructor scaling a count past the UInt64 nanosecond range traps rather than wrapping
pass	a Euclid gcd over MULTI-LIMB BigInts converges with limb-level mod steps
pass	a FILTER-COUNT fold over a list of records tallies by a field predicate
pass	a FILTERED rebuild keeps only entries passing a runtime predicate
pass	a FITTING multi-variant user sum payload compiles and matches back (no over-rejection)
pass	a FITTING narrow record-field literal grounds to the field width and computes (emit twin of the reject)
pass	a FLAT runtime string nested in a tuple is unaffected (control)
pass	a FLATTEN fold over a runtime list of lists joins across an empty inner spine
pass	a FLETCHER-16 checksum walks a concat-built byte rope across its seams
pass	a FLOAT literal grounded to Float32 through an arith operand overflows to infinity and is rejected
pass	a FLOAT literal that fits its contextually-grounded narrow width through an arith op computes
pass	a FLOAT-KEYED map result round-trips via the run/encode envelope, rendering its float keys (float map-key render, #6211 key-adopt/use + #6274 render)
pass	a FLOAT-field Tuple closure ARG flattens + rebuilds (box-float imported)
pass	a FLOAT-result effect op threads a float state and its resume folds under a float-dispatched operator
pass	a FLOAT32-key Map.to-list with an undetermined value is rejected CDZ0203, not a codeless decline
pass	a FLOAT64 as op ARGUMENT — the arm accumulates fractional values into Float64 state
pass	a FOUR-arg effect op binds positionally (place-value checksum)
pass	a FRAMED-Bytes handler state decoded and re-encoded by the arm per dispatch
pass	a FRESHLY-BUILT recursive-sum payload returns through a Result and unwraps in the caller
pass	a FULL handle expression in the resume-value slot — the arm runs a nested handler per dispatch
pass	a FULLY applied built-in operation is not flagged as a partial
pass	a Fletcher-16 over a seam-spanning slice VIEW equals the checksum of its logical bytes
pass	a Float KEY inserted into an empty (runtime) map boxes with box-float, not box-int
pass	a Float VALUE inserted into an empty (runtime) map boxes with box-float, not box-int
pass	a Float element inserted into an empty (runtime) set boxes with box-float, not box-int
pass	a Float32 NaN map key is found by a differently-produced NaN
pass	a Float32 add whose operand is an if of two bare literals grounds to f32
pass	a Float32 add with a bare LEFT literal grounds it to f32 (position-independent)
pass	a Float32 add with a bare right literal grounds the literal to f32
pass	a Float32 bare-literal match arm over a self-recursive fn grounds each tail-loop break to f32
pass	a Float32 divide with a bare right literal grounds the literal to f32
pass	a Float32 equality const-folds at f32 (demoted) precision, agreeing with the runtime answer
pass	a Float32 if-branch literal rounds through binary32, not binary64
pass	a Float32 literal compared to Float64.nan is a type error
pass	a Float32 match arm with a bare literal body grounds it to f32 (runtime arm, no fold)
pass	a Float32 multiply with a bare right literal grounds the literal to f32
pass	a Float32 quantity stored as a map value round-trips through Map.lookup
pass	a Float32 set dedups NaN elements and keeps zero signs distinct
pass	a Float32 subtract with a bare right literal grounds the literal to f32
pass	a Float32-conversion whose constant result overflows to infinity is rejected (no value form)
pass	a Float32-overflowing branch literal in a runtime if is rejected at check
pass	a Float32-overflowing branch literal reaching a narrow Float32 parameter is rejected at check
pass	a Float32-overflowing literal grounded CONTEXTUALLY through arith is rejected
pass	a Float32-overflowing literal in a Result Err payload is rejected
pass	a Float32-overflowing literal in a Result Ok payload is rejected
pass	a Float32-overflowing literal in a compound payload inside a runtime conditional branch is rejected
pass	a Float32-overflowing literal in a const-folded conditional is rejected at check
pass	a Float32-overflowing literal in a record field is rejected
pass	a Float32-overflowing literal in a runtime if branch is rejected at check
pass	a Float32-overflowing literal in a runtime match arm is rejected at check
pass	a Float32-overflowing literal in argument position under a narrow parameter is rejected at check
pass	a Float32-overflowing literal nested in a compound payload is rejected
pass	a Float32-overflowing magnitude literal under a Float32 Qty inner type is rejected
pass	a Float32-overflowing match-arm literal is rejected at check
pass	a Float32.nan compared to a Float64 literal is a type error
pass	a Float64 accumulator threaded through a recursion const-folds
pass	a Float64 closure captures a Float64 build-time host effect result
pass	a Float64 handler state advances fractionally through a two-site arm
pass	a Float64 op RESULT crosses resume and float state arithmetic is observed by comparison
pass	a Float64 quantity scaled by a bare Float64 is well-formed and runs
pass	a Float64 quantity scaled by a bare Int64 is a numeric mismatch with the widening fix
pass	a Float64-inner Qty host result crosses as its float magnitude
pass	a GENERIC Set-consuming def monomorphizes at a determined call — no false reject
pass	a GENERIC recursive sum branching through a tuple folds at a concrete instantiation
pass	a GENERIC scalar newtype instantiated at Int64 escapes a param'd export as a valid module
pass	a GENERIC user sum ((Box Int64)) as op result — nominal tag + instantiated payload cross resume
pass	a GENERIC user sum crosses a module boundary at a concrete instantiation
pass	a GENSYM effect derives fresh symbols from a counter state — same base yields distinct symbols
pass	a GENUINELY-RUNTIME Rational map/set key is found by a normalized-equal key
todo	a GENUINELY-recursive pqueue insert (recursion taken) declines cleanly, never folds the wrong entry
pass	a GROUP-BY then REDUCE-over-groups pipeline — build a multimap, enumerate it, fold each bucket
pass	a GUARD over a QUOTE pattern compares two bound subtrees — the x=x algebraic rewrite
pass	a GUARDED set membership arm threads its guard, and a false guard falls through to a later arm
pass	a HANDLE as a NON-FINAL multi-binding let init completes before the sibling binding
pass	a HANDLE of an undeclared effect in an uncalled def is rejected
pass	a HEAP element pushed across the RRB 32→33 boundary reads back intact at every index
pass	a HEAP handler seed stays readable in the body after performs advance the state
todo	a HEAP-accumulator block-wrapped branch perform in a let-init declines cleanly (adv-69 safe floor)
pass	a HEAP-state branch perform in a self-recursive performer threads the pushes across recursion (rw5)
pass	a HETEROGENEOUS 4-arg op (Int64/String/Bool/Int64) marshals every type to its arm binder
pass	a HEX DECODER finds each digit's value by alphabet scan and rejects a bad digit
pass	a HEX ENCODER splits each byte into nibbles and indexes a digit alphabet string
pass	a HOF callback matching a tuple argument computes through the nested reduction
pass	a HOF callback matching its sum argument reaches the nullary arm
pass	a HOST-delegated perform in a nested arm's NEXT-STATE slot is served (sequences at the boundary)
pass	a KNOWN type's UNKNOWN variant constructed in an uncalled def is rejected
pass	a LAZY comparator chain (Ordering + thunk) short-circuits on decisive and falls through on Equal
pass	a LEB128 final byte is the shifted remainder masked to seven bits
pass	a LEB128 non-final byte composes mask, continuation bit, and truncation
pass	a LET shadow of a do-def heap binding closes its scope and the do-def survives
pass	a LET-BOUND perform after a cross-function recursive helper observes the helper's out-state
pass	a LET-bound escaping-effect closure returned from a host block cannot cross the host boundary
pass	a LET-bound local (not a param) survives the multi-site continuation rebuild
pass	a LET-bound outer perform under an inner handle threads its state advance
pass	a LET-derived value between the op-arg match and the state match — interposed bindings re-evaluate per dispatch
pass	a LIST OF BIGINTS as op ARGUMENT — the arm folds heap-numeric elements it is handed
pass	a LIST OF SETS as op ARGUMENT — the arm indexes into the nested payload it is handed
pass	a LIST OF SETS op result — the body indexes, measures, and probes the nested elements
pass	a LIST OF STRINGS op result — the body indexes and measures rope elements after the marshal
pass	a LIST OF SUMS resumed from a handler feeds an event-sourcing fold in the body
pass	a LIST built from three performs ESCAPES the handle — read intact outside the region
pass	a LIST built in the handle body from perform results crosses the handle exit live
pass	a LIST of Rationals as a map key normalizes EVERY element for the key hash
pass	a LIST of closures as a set element is rejected (the collection-element face of the descent)
pass	a LIST of closures rides one perform and the arm picks by state per call
pass	a LIST of strategies is walked recursively, each applied to a fresh perform result
pass	a LIST shrinker drops elements greedily and converges to a minimal failing sublist
pass	a LIST-literal scrutinee with performing elements — draws fire once, bind by position
pass	a LIST-param entry + a reachable recursive fn emits VALID wasm (a different lift-op set, a different index shift)
pass	a LONGEST-COMMON-PREFIX walks two strings in scalar lockstep to the first mismatch
pass	a List entry argument is marshalled as a vec!
pass	a List-returning closure alongside a plain export — the closure
pass	a List-returning closure alongside a plain export — the plain
pass	a List.concat of a checked-arith list with a recursive-call list is disjoint-slotted
pass	a List.push of a checked-arith element behind a recursive-call list is disjoint-slotted
pass	a List.update at a checked-arith element behind a recursive-call list is disjoint-slotted
pass	a List.update at a second-RRB-node index on a large runtime list lands there and preserves the rest
pass	a List.update at a third-level RRB index lands and preserves persistence and siblings
pass	a List.update at depth-3 RRB path-copies one spine and shares the rest
pass	a MAP MERGE folds one map's entries into another, summing values on key collision
pass	a MAP OF LISTS op result — the body looks up a key and folds the inner list
pass	a MAP argument to an effect op is looked up inside the arm at the handler's own state
pass	a MAP built from generated pairs is equal regardless of insertion order (the Map analogue of set convergence)
pass	a MAP keyed by perform results in the handle body crosses the exit and looks up by those keys
pass	a MAP valued with perform results ESCAPES the handle — looked up outside the region
pass	a MAP-OF-LISTS handler state accumulates per dispatch — the upsert idiom end to end
pass	a MAP-valued key normalizes its Rational VALUES for the outer key hash
pass	a MATCH as a NON-FINAL multi-binding let init selects the binding value per call
pass	a MATCH-arm perform in a self-recursive performer threads the advance across recursion (rw-match)
pass	a MATCH-dispatched mutual group with the perform in one arm and the mutual call in another folds
pass	a MATCH-shaped arm with three resume sites folds (sum-dispatch, not if)
pass	a MAX-fold over quantities compares through the erased unit wrapper per step
pass	a MAX-fold threads a CONDITIONAL accumulator over a list with a runtime element
pass	a MEDIAN-OF-3 filter smooths interior spikes while endpoints pass through unchanged
pass	a MEMOIZED recursion threads a (tuple result memo-Map) through both self-call branches
pass	a MIXED Int-and-Float Tuple closure ARG flattens + rebuilds (box-int + box-float)
pass	a MIXED mutual SCC where only ONE partner performs — the non-performing arithmetic leg threads through the group fold
pass	a MIXED payload+nullary SUM compound = has DISCRIMINATING power (separates tags AND a payloaded vs a nullary variant)
pass	a MIXED-endian frame packs big and little fields side by side and round-trips both
pass	a MIXED-shape compound scales its quantity leaf beside a non-quantity element
pass	a MODULE-exported NON-recursive performer is homed by the importer's handler (single perform)
pass	a MODULE-exported closure that performs a delegated effect cannot cross the host boundary
pass	a MODULE-exported recursive callee performing per step is homed by the importer's handler
pass	a MONOTONE BISECTION finds the least r with r*r >= n over answer space, certified from both sides
pass	a MULTI-LEVEL concat-built list SET element (n>=33) is found by a push-built equal element
pass	a MULTI-LEVEL concat-built list SET element does not over-match a genuinely different element
pass	a MULTI-LEVEL concat-built list map key (n>=33) is found by a push-built equal key
pass	a MULTI-PAYLOAD variant with a nested same-sum variant reads the right payload depth (known outer disc)
pass	a MULTI-argument op mixing a heap list and two scalars — the arm consumes all three
pass	a MULTI-guard chain on a perform-result scrutinee with a performing fallback folds
pass	a MULTI-leading-rest list pattern destructures a PREPEND-built list
pass	a MULTI-parameter user generic resolves by name in a type annotation applied with two arguments
pass	a MULTI-payload struct newtype escapes as its payload tuple under the type header
pass	a MULTI-shot arm folds a perform in a let-bound lambda applied in the body
pass	a MULTI-shot arm folds a perform under a CURRIED lambda applied to pure arguments
pass	a MULTI-shot arm folds a perform wrapped in an inline lambda application
pass	a MULTI-shot arm keeps a pure applied lambda in its duplicated continuation
pass	a MULTI-shot arm over a distributed match arm-body hole folds
pass	a MULTI-shot arm over a pure effect-free call in the continuation folds
pass	a MULTI-shot arm over an if-condition hole duplicates the whole pure if safely
pass	a MULTI-shot arm re-reduces a continuation that reads a captured heap list per resume
pass	a MULTI-shot arm whose FIRST resume value is chosen by an if on the state
pass	a MULTI-shot arm whose each resume CONSUMES a captured heap list dups it per resume
pass	a MULTI-shot continuation APPLIES a captured closure per re-reduction
pass	a MULTI-shot continuation PUSHES onto a captured list per re-reduction (fresh copy each)
pass	a MULTI-shot handler arm folds a two-hole body by re-reducing per resume
pass	a MULTIBYTE and an EMPTY runtime String entry arg measure their UTF-8 byte lengths
pass	a MULTIBYTE symbol map key is found by its runtime-CONCAT-interned twin
pass	a MULTIPLY on an unusual signed width computes in range at the declared width
pass	a MULTIPLY overflowing an unusual signed width is rejected CDZ0304
pass	a MUTUALLY-recursive effectful group is specialized under a state handler
pass	a MUTUALLY-recursive performing pair threads one handler state through both fns
pass	a Map ACCUMULATOR threaded through a const recursion then Map.lookup folds
pass	a Map VALUE that is a list-of-quantities round-trips through Map.lookup and List.at
pass	a Map accumulator threaded through a non-tail double tree recursion accumulates both subtrees
pass	a Map keyed by a (Bytes, Int64) tuple is looked up by content through the compound key descent
pass	a Map keyed by a COMPOUND containing an abstract type is rejected (opacity — the key path compares the abstract leaf)
pass	a Map keyed by a DEEPLY-nested compound containing an abstract type is rejected (opacity recurses to any depth)
pass	a Map keyed by a record CONTAINING a Set hits through the triple-nested descent
pass	a Map keyed by two full-hash-colliding keys stores + retrieves each value by identity
pass	a Map over three full-hash-colliding keys retrieves each value; removing one keeps siblings' values
pass	a Map rebuilt by folding Map.insert over its own Map.to-list equals the original
pass	a Map-returning closure alongside a parameterized plain export — the closure
pass	a Map-returning closure alongside a parameterized plain export — the plain
pass	a Map-stated handler threads 50 recursive puts and reads the accumulated size
pass	a Map.insert replacing an existing key does not grow the constant map
pass	a Map.len over a projected field of a fresh record whose producer binds an i64 let is disjoint-slotted
pass	a Map.lookup of an absent key over a constant map folds to None
pass	a Map.lookup over a map literal with a runtime key finds it by value
pass	a Map.remove of a checked-arith key behind a recursive-call map is disjoint-slotted
pass	a Map.remove of a checked-arith key that is PRESENT drops exactly it (disjoint-slot value-parity)
pass	a Map.remove on a derived generation unshares the path without touching the ancestor
pass	a NARROW UInt32 checked-mul fits the UInt64 accumulator exactly (the widest narrow product)
pass	a NARROW-inner scalar newtype escape at a NEGATIVE value renders the nominal (: -300 I16)
pass	a NARROW-inner scalar newtype escaping a param'd export emits a valid module (i32-slot box)
pass	a NARROW-int-field Tuple closure ARG flattens + rebuilds (exercises the i32->i64 extend)
pass	a NARROW-width checked-add over a RUNTIME operand range-checks the narrow bounds: Some when it fits, None on overflow
pass	a NARROW-width checked-mul over a RUNTIME operand range-checks the narrow bounds (unsigned, UInt64 accumulator)
pass	a NARROW-width checked-sub over a RUNTIME operand range-checks the LOWER bound (Int8 underflow)
pass	a NARROW-width effect op parameter grounds a fitting perform argument
pass	a NARROW-width signed checked-add/sub range-checks BOTH bounds (Int8 over/underflow)
pass	a NARROW-width signed checked-mul range-checks BOTH bounds incl. the min×-1 edge (Int8)
pass	a NARROWER integer operand to a float operator nests the Int64 widening in the of-int wrap
pass	a NEGATIVE Float32-overflowing literal in a runtime branch is rejected
pass	a NEGATIVE large-magnitude float renders its full expansion with its sign
pass	a NEGATIVE prefixed quantity carries its sign through the reference scale-fold
pass	a NEGATIVE-constant point fact folds an inner comparison with the correct SIGN — the negative-point twin
pass	a NESTED Map-of-Map state — the arm updates the inner map through the outer per dispatch
pass	a NESTED Record ARG (a record containing a record) crosses the direct-call boundary
pass	a NESTED Record ARG with a LIST result crosses the direct-call boundary
pass	a NESTED Tuple ARG (a tuple containing a tuple) crosses the direct-call boundary
pass	a NESTED Tuple ARG among scalar args crosses the direct-call boundary
pass	a NESTED Tuple ARG among scalars with a LIST result
pass	a NESTED Tuple ARG between two scalar args (prefix + suffix)
pass	a NESTED Tuple ARG with a BYTE-ROPE result crosses the direct-call boundary
pass	a NESTED Tuple ARG with a LIST result crosses the direct-call boundary
pass	a NESTED Tuple ARG with a fixed-shape COMPOUND result crosses the direct-call boundary
pass	a NESTED Tuple ARG with a narrow Bool leaf crosses the direct-call boundary
pass	a NESTED Tuple arg ALONGSIDE a flat Tuple arg
pass	a NESTED bare-None in a tuple field and a sibling bare-None field solve independently as a direct arg
pass	a NESTED const recursion — a recursive helper called per element inside a recursive build — folds
pass	a NESTED float tuple as a SET element dedups by deep content
pass	a NESTED list-of-lists-of-quantities used as a Map key canonicalizes and hits
pass	a NESTED macro expansion (macro whose output calls another macro) introduces a recursive helper that lowers + infers
pass	a NESTED mixed ARG (a tuple containing a record) crosses the direct-call boundary
pass	a NESTED record inside a binding-param tuple binds its field (Patterns Compose, binding position)
pass	a NESTED runtime bin match re-parses a bin-decoded payload
pass	a NESTED runtime-if branch literal overflowing a narrow annotation is rejected (recursive descent)
pass	a NESTED sum-in-sum map key discriminates by the inner variant
pass	a NESTED tuple of quantities renders every element scaled at depth
pass	a NESTED tuple rest inside a binding-param tuple binds the inner leading + residual
pass	a NESTED tuple-of-tuples let pattern destructures a call's runtime result to all four leaves
pass	a NESTED-tuple ARG composes with a NESTED-tuple RESULT
pass	a NOMINAL-Unit-typed host-call statement in a do-body is not spuriously dropped
pass	a NOMINAL-over-tuple ARG crosses as the underlying tuple (direct-call)
pass	a NON-FINAL dependent-size bin-match with a dynamic-offset rest read reclaims every slice (no live objects)
pass	a NON-LAST handler arm whose body is a MATCH round-trips through the ML printer
pass	a NON-TAIL host call consumes rows on the unwind, deepest frame first
pass	a NON-TAIL recursion rebinding its rope arg to a slice-concat accumulates after each return
pass	a NON-TAIL recursive helper called per element inside a recursive build folds (general const-eval)
pass	a NON-identity constant add over a high UInt64 operand still rejects the overflow
pass	a NON-literal int let-binder init annotated Float wraps in of-int
pass	a NON-recursive helper calling a nested op whose resume performs the outer effect folds
pass	a NON-tail heap-spine build 5000 deep constructs and drains through real frames
pass	a NON-tail one-shot arm that ADVANCES the state threads the advance through the re-reduced continuation
pass	a NON-tail outer handler reduces a reducible inner handle first, then folds its own perform
pass	a NON-tail recursion 10000 deep computes through real stack frames
pass	a NON-tail state-advancing arm threads the advance through a perform in a let INIT
pass	a NON-tail state-advancing arm threads the advance through a perform in a match SCRUTINEE and its arm body
pass	a NON-tail state-advancing arm threads the advance through a perform in a matched literal arm reached via the scrutinee
pass	a NON-tail state-advancing arm threads the advance through a perform in an if CONDITION
pass	a NON-tail state-advancing arm threads through a NON-commutative continuation
pass	a NON-tail-resumptive arm projects a tuple parameter twice in its pure one-hole context
pass	a NONZERO BigInt literal probe in a recursive quasiquote-pattern simp matches its own constructor
pass	a NORMAL unused let init is elided with NO dead-computation warning
pass	a NULLARY variant applied to nothing (None) is not under-applied — it constructs its value
pass	a NaN float leaf in a tuple Map key is found by a differently-computed NaN
pass	a NaN map key is found by a differently-produced NaN
pass	a NaN nested in a list compares equal under the canonical byte form
pass	a NaN nested in a sum payload compares equal under the canonical byte form
pass	a NaN nested in a tuple compares equal under the canonical byte form
pass	a NaN operand makes every runtime float ordering relation false (unordered)
pass	a NaN selected through a runtime if-join stays self-equal and unordered downstream
pass	a ONE-shot ctl arm whose continuation reads an enclosing-fn param folds (mv single-splice control)
pass	a PAIR shrinker coordinate-descends each component to a jointly 1-minimal failing pair
pass	a PALINDROME two-pointer walk closes from both ends of a runtime rope
pass	a PARAM-parameterized generic payload (Box a) inside another generic is valid, not wrong-arity
pass	a PARAMETERIZED handler helper chained through itself — the step size is a function param
pass	a PARENTHESIZED literal magnitude adopts its arith sibling's fixed width like a bare one
pass	a PARSE-INT walks scalars, decodes digits by comparison, and handles a leading minus
pass	a PARTIALLY-applied multi-payload constructor is a first-class function value
pass	a PARTITION fold splits one spine into two lists by a runtime predicate
pass	a PASCAL row derives from its predecessor by pairwise sums, certified by the 2^n row-sum
pass	a PER-CALL-selected runtime symbol dispatches through the = chain
pass	a PERFORM of an undeclared op on a declared effect in an uncalled def is rejected
pass	a PERFORMING tail loop of 10000 iterations threads state in constant space
pass	a PLAIN @ensures on a NULLARY def (no parameters) enforces — ret binds the body, predicate reads only ret
pass	a PLAIN @ensures over a HEAP result (List) TRAPS when violated — the postcondition checks the heap value
pass	a PLAIN @ensures over a HEAP result (List) is enforced — ret binds a heap value, value-transparent when satisfied
pass	a PLAIN @ensures postcondition is ENFORCED at body-exit: a VIOLATED postcondition traps when the def is called
pass	a PLAIN @ensures postcondition is value-transparent when SATISFIED: the def returns its computed value
pass	a PLAIN @ensures relating ret to a PARAMETER (> ret x) TRAPS when violated — the result-vs-input postcondition is checked
pass	a PLAIN @ensures relating ret to a PARAMETER (> ret x) is enforced — the param stays in scope alongside ret in the predicate
pass	a PLAIN @ensures whose predicate reads ONLY a parameter (not ret) is enforced — the dual of the nullary case
pass	a PLAIN @ensures with a constant-FALSE predicate always traps — the postcondition fires unconditionally
pass	a PLAIN @requires precondition is ENFORCED at body-entry: a VIOLATED precondition traps when the def is called
pass	a PLAIN @requires precondition is value-transparent when SATISFIED: the def returns its computed value
pass	a PLAIN @requires relating TWO parameters (< a b) TRAPS when violated — the two-param precondition is checked
pass	a PLAIN @requires relating TWO parameters (< a b) is enforced — BOTH params stay in scope in the predicate
pass	a PREFIX-SUM scan emits a running total whose each element feeds the next
pass	a PRIME LIST accumulates by testing each candidate against earlier primes up to its square root
pass	a PRIORITY insert orders by key with FIFO tie-break carried as a sequence number
pass	a PURE closure iterated by a recursive combinator composes with a perform in the same body
pass	a PURE dead binding beside a perform is harmless (the eliminable control)
pass	a QUASIQUOTED #map-rest pattern reflects through reify_active and splices an unquote
pass	a QUICKSELECT finds the kth smallest by partitioning and recursing into ONE side
pass	a Qty STATE threads via a def-bound arm computation — the workaround shape runs end to end
pass	a Qty handler state advances via Qty.value / re-wrap in the next-state slot
pass	a Qty payload with a GENERIC inner type rejects a bare use with no type argument
pass	a Qty payload's INNER type stays a real type parameter — the unit-arg skip does not over-skip
pass	a Qty state's next-state slot computes (+ s s) INLINE — the formerly-rejected shape runs
pass	a Qty with a bad inner TYPE and a bad unit gets position-aware guidance — type guidance inner, unit guidance outer
pass	a Qty-keyed trie churned with differently-normalized magnitudes equals the direct build
pass	a Qty-typed export carries its unit frame across the module boundary into caller algebra
pass	a Qty-typed variant payload keeps its type non-generic and matches back
pass	a Qty.+ magnitude from a function-application result reconciles against an Int8 sibling
pass	a Qty.of magnitude that is a bool is rejected as not numeric
pass	a Qty.of magnitude that is a string is rejected as not numeric
pass	a Qty.of magnitude that is a tuple is rejected as not numeric
pass	a Qty.of second argument that is a bare integer is rejected as not a unit
pass	a Qty.of second argument that is a string is rejected as not a unit
pass	a Qty.of second argument that is a tuple is rejected as not a unit
pass	a Quantity @param generates a Qty-result accessor the host supplies as a scalar magnitude
pass	a RADIX literal carries the N type suffix
pass	a RATIONAL compound = has DISCRIMINATING power (1/2 ≠ 1/3, but 0/2 = 0/3)
pass	a RATIONAL handler state accumulates unit fractions exactly and resumes read the prior sum
pass	a RATIONAL leaf in a compound compares by CANONICAL form (2n/4 = n/2 inside a tuple)
pass	a RECORD as op ARGUMENT — the arm projects the fields it is handed
pass	a RECORD closure ARG among scalars crosses the direct-call boundary (scalar result)
pass	a RECORD closure ARG on the MULTI-EXPORT path
pass	a RECORD closure ARG whose fields are NOT in sorted source order
pass	a RECORD closure ARG with a LIST result
pass	a RECORD closure ARG with a narrow Bool field, among scalars
pass	a RECORD field built from a runtime param boxes its Map handle correctly (function[27] record-path twin)
pass	a RECORD handler state evolves a table and a counter that genuinely DIVERGE
pass	a RECORD handler state — the arm projects one field and rebuilds the record
pass	a RECORD op result crosses resume; the body projects both fields
pass	a RECORD used as a map key hashes and matches by its field SET, independent of written order
pass	a RECORD with a closure field as a map key is rejected (the record face of the compound descent)
pass	a RECORD with a nested TUPLE field crosses the direct-call boundary
pass	a RECURSIVE effectful walk accumulates into a list-state handler
pass	a RECURSIVE function's parameter used only in the recursive call is NOT falsely flagged unused
pass	a RECURSIVE memoizing demand with let-wrapped resumes folds through the group over a state handler
pass	a RECURSIVE runtime tuple escaping as a resource is reclaimed to zero after the host drop
pass	a RECURSIVE sum as a Map key hits and misses by structural content
pass	a RECURSIVE sum with a record payload walks an environment chain by depth and tag search
pass	a RECURSIVE tree evaluator resolves variables through a READER effect with a Map-state handler
pass	a RECURSIVE tuple-state handler threads a rebuilt pair per perform — the superseded state tuple is reclaimed each iteration (no per-perform leak)
pass	a RECURSIVE user sum (Tree) crosses resume; the body folds it
pass	a RECURSIVE-sum value of runtime depth rides a handler resume
pass	a REFERENCED non-exported definition is used and does not warn
pass	a REFL tactic closes a reflexivity goal by driving the kernel, and cannot close a false goal
pass	a RELATIONAL @requires over two parameters enforces their order at entry
pass	a RESULT as op ARGUMENT — the arm branches on Ok/Err payloads it was handed
pass	a RESULT handler state is matched per variant with one resume per arm (Ok accumulates, Err echoes)
pass	a RESULT-returning effect op is matched on Ok / Err — the fallible-step idiom
pass	a RESUMING handler around a contracted body still enforces @ensures on the resumed-through value
pass	a RIGHT-arity generic variant payload constructs and matches (no false wrong-arity)
pass	a ROMAN DECODER subtracts on lookahead and round-trips through the pinned renderer
pass	a ROMAN NUMERAL renderer walks a value-symbol table greedily with subtractive pairs
pass	a ROPE Bytes arg (recursive concat, uncompacted) crosses the host boundary
pass	a ROTATE-BY-K splits at the wrapped offset and swaps the halves, identity at multiples of len
pass	a RUNTIME Ast structural equality compares by value (Ast.Int scalar leaf and Ast.List compound)
pass	a RUNTIME Int64 argument to a narrow effect-op parameter is rejected as a type mismatch
pass	a RUNTIME Result-of-Result from chained fallible helpers dispatches all three arms
pass	a RUNTIME String match with no wildcard is non-exhaustive — the if-chain desugar does not relax the check
pass	a RUNTIME branch selects between an abortive perform and a plain value per call
pass	a RUNTIME byte-slice torn mid-scalar is rejected by from-bytes at both tear points
pass	a RUNTIME erased Qty add is still CHECKED at the inner width (overflow traps at run time)
pass	a RUNTIME guarded sum-match arm dispatches through its guard
pass	a RUNTIME if-chain selects among four different-length heap lists
pass	a RUNTIME list of floats compares equal element-wise (the value-eq-shaped walk)
pass	a RUNTIME list of floats with a NaN element compares equal (value-eq-shaped canonical byte form)
pass	a RUNTIME list of floats with a differing element compares unequal (value-eq-shaped)
pass	a RUNTIME map's wrong-type key pattern is rejected too, not silently dead
pass	a RUNTIME negative signed field beside an unsigned one round-trips with independent sign handling
pass	a RUNTIME payload-literal refinement dispatches through its literal test
pass	a RUNTIME quantity is returned WITH its unit (the unit label is injected at compile time)
pass	a RUNTIME shared list prepended-onto leaves the original unchanged (rc>1 path-copy census control)
pass	a RUNTIME string scrutinee matches string literals by equality
pass	a RUNTIME-DISC `?` unwraps Ok or short-circuits Err by a per-call value (Result)
pass	a RUNTIME-DISC `?` unwraps or short-circuits by a per-call value (Option — the operator repro)
pass	a RUNTIME-built Rational is usable as a map key, matched by its exact value
pass	a RUNTIME-built Rational set deduplicates by exact value
pass	a RUNTIME-built unreduced Rational and its reduced form are ONE map key
pass	a RUNTIME-conditioned branch perform in a self-recursive performer threads the advance (rw3)
pass	a RUNTIME-count generated batch insorts fully time-ordered, verified by a sortedness walk
pass	a RUNTIME-dependent hole threads through the tag application per call
pass	a RUNTIME-selected combiner closure crosses a join and drives a fold
pass	a Rational ARITHMETIC result probes a map key by its reduced canonical value
todo	a Rational ENTRY parameter crosses the boundary and reduces per call
pass	a Rational SQUARE over a perform draw — 1/5 squared stays exactly 1/25
pass	a Rational accumulator threaded through recursion sums exactly
pass	a Rational compare whose naive cross-multiply would OVERFLOW i64 still orders exactly
todo	a Rational entry argument is marshalled through Rational::new
pass	a Rational fanned into a compound then re-read survives (Perceus retain over a BigInt/Rational leaf)
pass	a Rational is usable as a map key, matched by its exact value
pass	a Rational leaf in a tuple orders by exact value with canonical-form ties falling through
pass	a Rational map from a Rational sum key and a Rational.of-over-arithmetic key has both entries
pass	a Rational set deduplicates by exact value regardless of how each was written
pass	a Rational set from a Rational sum and a Rational.of over integer arithmetic has both elements
pass	a Rational sum's numerator drops every owned handle and leaves no live heap objects
pass	a Rational-MAGNITUDE Quantity host value composes the num/den ops with the unit erasure (#13, B2)
pass	a Rational-annotated literal composes with exact rational arithmetic
pass	a Rational-inner quantity Map key matches an equal value written with a different spelling
pass	a Rational-inner quantity Set dedups equal elements and keeps distinct ones
pass	a Rational-inner quantity addition stays exact through the quantity wrapper
pass	a Rational-inner quantity stored in a List reads back canonicalized through List.at
pass	a Rational-keyed trie churned with DIFFERENTLY-normalized spellings equals the direct build
pass	a Rational-payload sum value escapes across the boundary in its canonical form
pass	a Record.merge of two runtime records reads a field from the second operand
pass	a Record.pop over a runtime record splits off the named field's value
pass	a Record.project over a runtime record keeping two fields reads a kept field's value
pass	a Record.project over a runtime record reads a kept NON-FIRST field's value
pass	a Record.with over a runtime record with MULTIPLE preserved fields evaluates the operand once
pass	a Record.with whose TARGET is a runtime record (a projection) builds a fresh record, not declines
pass	a Record.without over a runtime record dropping one field reads a surviving field's value
pass	a Record.without over a runtime record reads a surviving NON-FIRST field after the drop
pass	a Result built by an if with the Err branch first type-checks
pass	a Result carrying a Result is matched by a nested pattern to its inner value
pass	a Result payload numeric mismatch retypes the differing first type param
pass	a Result pipeline short-circuits on the first Err
pass	a Result-of-Option threads both wrappers through one runtime match
pass	a Result-valued `?` under an Option boundary is a type error
pass	a SAME-width Float32 nan comparison compiles and folds true (structural =, not IEEE)
pass	a SAME-width Float64 nan comparison compiles and folds true (structural =, not IEEE)
pass	a SCALAR do-def resumed in both slots stays in scope (scalar twin of the dual-slot fix)
pass	a SCALAR-capturing closure-state replacement folds at three dispatches (no env chain)
pass	a SCALAR-param export returning a RECORD lifts the result (not a raw handle) (via an imposed WIT world)
pass	a SELECT-axiom theorem is unforgeable — its Thm cannot be fabricated outside the kernel
pass	a SELF-RECURSIVE named def passed as a value is applied twice through the parameter
pass	a SET as a Map key hits by ORDER-INDEPENDENT inner-set content
pass	a SET as a Map key hits by canonical content and a different set misses
pass	a SET as a set element hashes by content: three build orders, one element
pass	a SET as op ARGUMENT — the arm measures and probes the set it is handed
pass	a SET churned to empty equals a fresh empty set and re-accepts elements
pass	a SET handler state grows per perform and dedupes a repeated event
pass	a SET of BigInts as a map key matches its construction-order and arithmetic twin
pass	a SET of SETS dedups elements by set CONTENT — insertion-order twins collapse
pass	a SET of arm-interned symbols dedups across dispatches — warm appears twice, stored once
pass	a SET of tuples as op ARGUMENT — the arm probes compound membership including order sensitivity
pass	a SET op RESULT crosses resume — membership-probed and measured per dispatch
pass	a SET state churns — inserts and removes interleave across dispatches, canonical at each read
pass	a SET state dedup accumulator — the condition PROBES the state, the pass branch inserts
pass	a SET-valued handler state accumulates uniques across resumes (the visited-set idiom)
pass	a SEVEN-op handler — the widest arm table in the corpus, each op a distinct answer shape and stride, order scrambled
pass	a SINGLE-variant open sum still requires an open-tail arm
pass	a SITE-A eta-closure held in a tuple is applied and its owned env cell reclaims (residual = shell gap)
pass	a SITE-A eta-closure stored in a SUM is applied and its owned env cell reclaims (residual = shell gap)
pass	a SITE-A two-arg-payload eta-closure is applied and its owned env cell reclaims (wider capture)
pass	a SIX-deep alternating A-B perform chain threads both nested states through every crossing
pass	a SLIDING-WINDOW max walks a k=3 window by paired index reads
pass	a SOLE RECORD closure ARG crosses the direct-call boundary
pass	a SPLIT on separator slices fields between hits and round-trips through the pinned JOIN
pass	a STABLE DEDUP keeps first occurrences in input order via a seen-Set threaded through the fold
pass	a STACK-MACHINE interpreter dispatches instruction sums over a list operand stack
pass	a STACKED @requires + @ensures contract on a performing def threads all three layers
pass	a STALLED counter makes all Set.of perform results collide to a singleton
pass	a STATS record threads min/max/sum/count as one fold accumulator with data-dependent field updates
pass	a STD-SUM (Option) literal scrutinee — single eval, payload binds by position
pass	a STRING run-length grouping counts adjacent equal scalars across a rope seam
pass	a STRING-result effect op resumes with a string that folds through a concat
pass	a STRUCTURALLY-IDENTICAL mutual-performer SCC round-trips through the cadenza backend
pass	a SUM compound = has DISCRIMINATING power (a runtime-selected constructor separates two tagged values)
pass	a SUM constructor payload that is a compound built from performs threads and destructures
pass	a SUM is threaded as a handler's folded state across operations
pass	a SUM-payload shrinker descends a tagged variant's payload to its minimal failing value, preserving the variant
pass	a SYM tactic reduces a goal to a subgoal plus a kernel justification (backward proof)
pass	a SYMBOL as op ARGUMENT — the arm compares interned identity against its own intern
pass	a SYMBOL compound = has DISCRIMINATING power (two runtime-selected interned names separate)
pass	a SYMBOL handler STATE threads dispatches — each resume reads the prior symbol's identity
pass	a SYMBOL-keyed Map built from performs — interned keys look up across separate interns
pass	a SYMBOL-keyed Map state routes hits to different keys (route-table accumulator)
pass	a SYMBOL-returning effect op interns through the handler and results compare by content
pass	a Set ACCUMULATOR threaded through a const recursion then Set.len folds
pass	a Set ACCUMULATOR threaded through a const recursion then to-list folds (m7)
pass	a Set of (Int64, Bytes) tuples orders by int then Bytes lexicographically and dedups by content
pass	a Set of MAPS-OF-SETS dedupes by full-depth content
pass	a Set of lists-of-quantities dedups equal members and keeps distinct ones
pass	a Set of nested tuple-in-tuple float keys dedups equal members by content
pass	a Set of nominal-over-quantity newtypes dedups equal members
pass	a Set of structural records with a Float64 field dedups equal members by content
pass	a Set of tuples with a Float64 element dedups equal members by content
pass	a Set of tuples with an f32 NaN element dedups by the canonical quiet NaN
pass	a Set of tuples-containing-a-quantity dedups equal members and keeps distinct ones
pass	a Set taken out of a map grows independently of the original binding
pass	a Set.contains over a concrete Int64-keyed Set param stays legal (the read-side gate does not over-reject a concrete element)
pass	a Set.of over PRELUDE-Option elements dedups Some by payload and keeps None distinct
pass	a Set.of over RECURSIVE-sum elements dedups by spine content
pass	a Set.of over Result elements separates Ok from Err with EQUAL payloads
pass	a Set.of over user-sum variants keeps same-payload different-variant elements distinct
pass	a Set.remove of a checked-arith element behind a recursive-call set is disjoint-slotted
pass	a Set.remove of a checked-arith element that is PRESENT drops exactly it (disjoint-slot value-parity)
pass	a Set.union of a recursive-call set and a Set.of over checked-arith is disjoint-slotted
pass	a Set.union over abstract-element Set params is rejected (set-algebra compares stored elements)
pass	a Set/Map lookup over an abstract-element collection reached via a param is rejected (read-side opacity)
pass	a Sign match missing two of three variants is non-exhaustive
pass	a Some carrying a bare None type-checks under a whole-expression annotation
pass	a Some carrying an inner-annotated None matches its arm (the control)
todo	a String ENTRY arg rides a rope into an effect-op argument and the arm reads its bytes
pass	a String argument handle passed to a peer stays live for a later local read (borrow not move)
pass	a String built by folding String.concat over a runtime list of chunks has the right content and length
pass	a String compound export under a non-main name crosses to the host
pass	a String guard condition is likewise rejected — the Bool check is general
fail	a String host RESULT crosses the boundary and is read twice (byte-len + scalar-len of a multibyte response)
pass	a String let-binder init annotated Bytes wraps in to-bytes
pass	a String literal pattern over a Symbol scrutinee is rejected at the nominal boundary
pass	a String match with a BOUND default arm equals its (= s literal) if-chain with a bound else
pass	a String match with disjoint literal arms is order-independent (reordering the arms preserves the result)
pass	a String match with no wildcard is non-exhaustive
pass	a String op RESULT selected by the op argument, composed via concat
pass	a String param threaded UNCHANGED to a self-call AND consumed by String.concat each step is retained
pass	a String param threaded through a self-recursive loop and concatenated each step is retained
pass	a String slice OF a slice over a MULTIBYTE concat rope composes scalar offsets
pass	a String slice VIEW keys a map by content in both directions, rope-backed included
pass	a String value in an integer bin segment is a kind mismatch
pass	a String where Bytes is expected offers a to-bytes conversion wrap (annotated-param call site)
pass	a String where Bytes is expected offers a to-bytes conversion wrap (operator-arg position)
pass	a String-keyed map of MIXED-field records reads both leaves of a looked-up value (the config idiom)
pass	a String-literal pattern over a Bytes scrutinee is a type error
pass	a String-returning closure alongside a parameterized plain export
pass	a String-returning closure alongside a parameterized plain export — the plain runs
pass	a String-to-String op transforms a rope arg in the arm and its RESULT feeds the next perform
pass	a String-to-String transformer op — a rope argument crosses in, a wrapped rope crosses back
pass	a String.at result then reuse of the source does not double-free
pass	a String.slice VIEW built in the ARM crosses back through resume — the body measures it
pass	a String.slice view returned from a helper OUTLIVES the helper's local parent
pass	a SumExpect result beside an i64 arith element in a tuple gets a disjoint scratch slot
pass	a SumExpect-unwrapped slice view BOUND and read TWICE (count>1) is NOT reclaimed (SumExpect single-consumer/escape must-hold)
todo	a Symbol entry parameter compares to an interned constant
pass	a Symbol interned from a rope-slice view keys maps and removes set elements
pass	a Symbol leaf in a tuple orders content-lexicographically and decisively before later fields
pass	a Symbol literal pattern over a String scrutinee is rejected at the nominal boundary
pass	a Symbol map key looks up by content and an overwrite does not grow the map
pass	a Symbol nested in a TUPLE map key keys by content through the compound descent
pass	a Symbol stored as a map VALUE round-trips through lookup to its content string
pass	a Symbol-payload literal probe inside a RECURSIVE fn matches (recursive-specialization control)
pass	a THREE-DEEP compose chain stacks closure envs with a runtime capture mid-chain
pass	a THREE-WAY scalar classifier splits vowels, consonants, and other by range and membership
pass	a THREE-argument closure round-trips — flat arrow spelling
pass	a THREE-arm match joins a borrowed alias, an owned fresh rope, and a const literal
pass	a THREE-cycle argument rotation permutes correctly through repeated tail calls
pass	a THREE-deep constructor-woven Ast destructures through nested patterns to its runtime leaf
pass	a THREE-deep member-access chain reads a RUNTIME leaf through nested records
pass	a THREE-deep transitive closure-capture chain applies with each level's own capture live
pass	a THREE-hole one-shot handle body folds via the recursing refold
pass	a THREE-level nest — Option of Result of a USER sum — matches through all three at runtime
pass	a THREE-parameter op arm binds all three parameters and the state
pass	a THREE-phase state machine — Idle to Running(count,peak) to Done(total), the sentinel input drives the final transition
pass	a THREE-site and a TWO-site arm interleaved fold (site counts mix freely)
pass	a THREE-site arm (nested if, three resume sites) folds
pass	a THREE-way mutually-recursive effectful cycle specializes and runs
pass	a TOGGLE fold flips set membership per occurrence — odd count in, even count out
pass	a TOPOLOGICAL sort drains min-ready nodes and verifies every edge points forward
pass	a TRANSPOSE of a 2x3 list-of-rows walks columns across row spines
pass	a TRIPLY-nested Record ARG crosses the direct-call boundary
pass	a TUPLE containing a closure as a map key is rejected (CDZ0216 descends into compound keys)
pass	a TUPLE handler state — the arm destructures, branches, and rebuilds both slots
pass	a TUPLE mixing Float64 and Int64 crosses as op ARGUMENT — the arm scales by the int
pass	a TUPLE op argument with a HEAP leaf destructures inside the arm
pass	a TUPLE-keyed Map STATE grown across dispatches — compound-key inserts and lookups thread, the flipped key misses
pass	a TUPLE-keyed Map op result — the body looks up by a reconstructed compound key
pass	a TUPLE-literal scrutinee with performing fields — draws fire once, bind by position
pass	a TUPLE-result perform's projected field feeds a SECOND perform's argument, threading state across both
pass	a TUPLE-returning operation resumes a pair built from the handler state, then projected
pass	a TUPLE-wrapped runtime slice as a Map key hits by content through the compound descent
pass	a TWICE combinator stacks — a closure capturing a closure capturing a closure applies through the tower
pass	a TWO-LIST FIFO queue reverses the back list exactly when the front drains
pass	a TWO-PASS transform over Ast.module — one recursion consuming another's result — const-evaluates
pass	a TWO-argument capture closure through a fold-style HOF — the capture rides both applications
pass	a TWO-field open-row projection instantiates at three widths with slot-shifting extras
pass	a TWO-of-THREE partial application binds a prefix and the LET-BOUND residual completes
pass	a TWO-resume-site arm branching on a CAPTURED Map folds — the branch reads heap, not state
pass	a TWO-site arm over a BigInt state (heap-scalar state through the refold)
pass	a TWO-site arm over a Qty state gates on the unwrapped magnitude
pass	a Thm rides a MAP value through outside code and comes back to the kernel accessor
pass	a Thm rides a collection through outside code without destructure rights
pass	a Tuple ARG AFTER a scalar arg crosses the direct-call boundary (scalar, then tuple)
pass	a Tuple ARG BEFORE a scalar arg crosses the direct-call boundary (tuple, then scalar)
pass	a Tuple ARG BEFORE a scalar with a LIST result crosses the direct-call boundary
pass	a Tuple ARG BETWEEN two scalar args crosses the direct-call boundary (scalar, tuple, scalar)
pass	a Tuple ARG among scalars with a BYTE-ROPE result crosses the direct-call boundary
pass	a Tuple ARG among scalars with a LIST result crosses the direct-call boundary
pass	a Tuple ARG among scalars with a fixed-shape COMPOUND result crosses the direct-call boundary
pass	a Tuple ARG composes with a NESTED-tuple RESULT
pass	a Tuple ARG of FLOAT fields crosses the direct-call boundary
pass	a Tuple ARG of MIXED widths (Int32, Int64) crosses the direct-call boundary
pass	a Tuple ARG with a Bool field between Int64 fields crosses the direct-call boundary
pass	a Tuple const param folds a VALUE through a bare recursive call (ca06)
pass	a Type.eq width dispatcher answers per WRITTEN type at two call sites in one body
pass	a UInt32 addition that overflows the 32-bit width is rejected at compile time
pass	a UInt32 holds a wasm section size at the width boundary
pass	a UInt48 wrap-multiply traps on a storage-slot wrap, not just a type-overflow
pass	a UInt64 closure round-trips through a consumer export
pass	a UInt64 holds a value above the signed 64-bit maximum
pass	a UInt8 comparison against its own type bound folds to a constant
pass	a UInt8 sum-payload literal-match hits the literal arm, result widens to Int64
pass	a UInt8 sum-payload literal-match misses, binds the payload widened to Int64
pass	a UInt8 wrapping-add result feeds a CHECKED add — the MASKED value is what the checked op sees
pass	a USED anonymous-lambda parameter is clean — no unused-parameter warning
pass	a USED non-normalizing binding is a hard CDZ0999 error, not merely warned
pass	a USED variant-payload match binder is clean — no unused-match-binding warning
pass	a USER sum as handler state — a countdown mode machine (Fast k -> Slow)
pass	a USER variant naming a value-shadowed prelude type name as its payload still faults CDZ0203
pass	a USER-DEFINED multi-variant sum payload quantity renders scaled to its reference
pass	a USER-SUM op result (Status) crosses resume; the body matches per variant
pass	a USER-SUM-literal scrutinee with a performing payload — the ctor pattern binds once
pass	a USER-declared monomorphic sum's Some variant escapes to the host rendering its bare name
pass	a USER-declared monomorphic sum's nullary None variant escapes as its unit payload
pass	a Unit element between two Int64s in a tuple, a later element read
pass	a Unit element in a multi-payload sum variant compiles to valid wasm
pass	a Unit payload MATCH-bound from a still-live sum at a dup-site drops the sentinel
pass	a Unit payload expected from a still-live sum at a dup-site drops the sentinel
pass	a Unit projected from a function-returned tuple (owned-reclaim path)
pass	a Unit projected out of a multi-payload sum and bound to a name
pass	a Unit record field beside an Int64 field, the Int64 read
pass	a Unit.* at the wrong arity is rejected
pass	a Unit.^ at the wrong arity is rejected
pass	a Unit.define with a non-integer scale is rejected
pass	a Unit.define with the wrong arity is rejected
pass	a Unit.of named with a non-symbol value is rejected
pass	a VALID narrow width in an unused parameter is not over-rejected
pass	a VARIABLE catch-all arm binds the whole runtime scrutinee as a Bytes value
pass	a VARIANT below a record MATCH field switches on its discriminant and binds the payload (deep variant checking)
pass	a VIOLATED @ensures on a performing def traps at body-exit
pass	a VIOLATED @ensures traps when the def arrives through a runtime branch
pass	a VIOLATED @requires traps at body-entry BEFORE the body's perform fires
pass	a Value.decode round-trip grounds its target from a LET-BINDER annotation, not only an inline ascription
pass	a Value.decode round-trip through a let-binder preserves a newtype-over-RECORD nominal
pass	a Value.encode then Value.decode round-trips a compound value back to Some of the SAME structure
pass	a Value.encode/Value.decode round-trip of a Float64 single-ctor uses box-float
pass	a Value.encode/Value.decode round-trip of a NARROW signed-int single-ctor sign-extends correctly
pass	a Value.encode/Value.decode round-trip of a NULLARY single-ctor newtype is Some(unit)
pass	a Value.encode/Value.decode round-trip preserves a BOOL element of a compound
pass	a Value.encode/Value.decode round-trip preserves a BYTES element of a compound
pass	a Value.encode/Value.decode round-trip preserves a BigInt (arbitrary precision, multi-limb)
pass	a Value.encode/Value.decode round-trip preserves a CHAR element of a compound
pass	a Value.encode/Value.decode round-trip preserves a FLOAT element of a compound
pass	a Value.encode/Value.decode round-trip preserves a MAP's entries
pass	a Value.encode/Value.decode round-trip preserves a MULTI-PAYLOAD sum variant (spread)
pass	a Value.encode/Value.decode round-trip preserves a NESTED compound (a tuple inside a record field)
pass	a Value.encode/Value.decode round-trip preserves a RECORD's fields
pass	a Value.encode/Value.decode round-trip preserves a Rational (normalized num/den name leaf)
pass	a Value.encode/Value.decode round-trip preserves a SET's elements
pass	a Value.encode/Value.decode round-trip preserves a STRING element of a compound
pass	a Value.encode/Value.decode round-trip preserves a SUM's NULLARY variant (Option None)
pass	a Value.encode/Value.decode round-trip preserves a SUM's payload variant (Option Some)
pass	a Value.encode/Value.decode round-trip preserves a SYMBOL element of a compound
pass	a Value.encode/Value.decode round-trip preserves a newtype-over-RECORD nominal
pass	a Value.encode/Value.decode round-trip preserves a runtime-length LIST
pass	a Value.encode/Value.decode round-trip preserves a scalar-erased single-ctor newtype
pass	a Value.encode/Value.decode round-trip preserves a scalar-erased single-ctor newtype NESTED as a record field
pass	a Value.encode/Value.decode round-trip preserves a scalar-erased single-ctor newtype NESTED as a tuple element
pass	a Value.encode/Value.decode round-trip preserves the EMPTY list (zero-element collection edge)
pass	a Value.encode/Value.decode round-trip preserves the EMPTY map (zero-entry collection edge)
pass	a Value.encode/Value.decode round-trip preserves the EMPTY set (zero-element collection edge)
pass	a WIDE integer literal pattern (beyond ±2^31) matches a runtime scrutinee by equality
pass	a WORD COUNT threads an in-word flag through a scalar scan, counting entries not characters
pass	a WRAP-composing closure state (the replacement captures the PREVIOUS closure) folds at two dispatches
pass	a WRITER effect accumulates a PRE-ORDER trace string during a recursive tree walk
pass	a _-led name is an ordinary silenced binder, not the bare wildcard
pass	a `(const …)` block destructures a tuple in a match and folds
pass	a `(const …)` block folds a constant Float expression (never stricter than the ordinary fold)
pass	a `(const …)` block folds a recursion over a tuple const value
pass	a `(const …)` block whose expression depends on runtime data is rejected (CDZ0201)
pass	a `(const …)` block with the wrong operand count is a malformed reject (arity guard)
pass	a `(const …)` force-eval block folds a helper call over constant data (no const params needed)
pass	a `(const …)` force-eval block preserves its operand's ascribed width
pass	a `(const …)` map insert replaces a key in place (latest write wins)
pass	a `(const …)` map lookup MISS folds to Option.None
pass	a `(const …)` map lookup folds to the associated value
pass	a `(const …)` set contains after insert folds
pass	a `(const …)` set contains of an ABSENT member folds to false
pass	a `(const …)` set membership + dedup-len fold
todo	a `..` spread in a MAP construction entry names the pattern-only rule, not a malformed key-value entry
todo	a `..` spread in a RECORD construction entry names the pattern-only rule, not a malformed key-value entry
pass	a `?` in a CALLED (inlined, non-exported) helper finds its boundary
pass	a `?` in a GENERIC function monomorphizes correctly at two element types
pass	a `?` in an anonymous LAMBDA body resolves the lambda as its boundary (happy path)
pass	a `?` in an anonymous LAMBDA with a non-fallible result is rejected (CDZ0230)
pass	a `?` inside a match-arm resolves against the enclosing function boundary
pass	a `?` inside an if-branch resolves against the enclosing function boundary
pass	a `?` mid-body under a provably non-fallible boundary is rejected
pass	a `?` on a String operand is a type error
pass	a `?` on a non-fallible operand is a type error
pass	a `?` on an ill-typed operand reports the operand's error, not a `?`-shape cascade
pass	a `?` result is usable mid-body, not only in tail position
pass	a `?` whose Result error type disagrees with the boundary's is rejected
pass	a `?` whose operand kind is not yet definite names both fallible forms in the boundary hint
pass	a `?` with no fallible enclosing function boundary is rejected
pass	a `not` with too many operands offers a delete-the-surplus fix
pass	a bad binary digit is a malformed literal, not an unbound name
pass	a bakeable nullary type-value export crosses the boundary (the control)
pass	a balanced-paren scan tracks depth over a runtime string and fails fast on early close
pass	a bare (key value) map entry VALUE is no longer accepted
pass	a bare (key value) record field VALUE is no longer accepted
pass	a bare (map) pattern with no keys matches a non-empty map, not just the empty one
pass	a bare 2-parameter user generic echoes both parameters
pass	a bare Ast literal used as an arithmetic operand names the compile-time-metadata misuse
pass	a bare BIGINT as op ARGUMENT — the arm does exact wide arithmetic on the crossed box
pass	a bare Float width-constructor names the sized default with a replace fix
pass	a bare Float32 value renders its own shortest decimal, not the f32->f64 promotion
pass	a bare Int width-constructor names the sized default with a replace fix
pass	a bare List constructor in a parameter type names the missing element argument
pass	a bare MONOMORPHIC type in a variant payload is valid (not a missing-argument ctor)
pass	a bare Map constructor in a parameter type names its two missing arguments
pass	a bare Map.empty const-folds its length to zero
pass	a bare Map.empty threaded through Map.insert folds its length
pass	a bare Map.empty threaded through Map.insert then Map.lookup folds the found value
pass	a bare Map.empty threaded through two distinct Map.inserts folds its length to two
pass	a bare Map.insert FLOAT KEY adopts the sibling Float32 width + keys/looks-up through the wrapper
pass	a bare Map.insert FLOAT value adopts the sibling's Float32 width (map unifies, no rust E0308)
pass	a bare Map.insert value that adopts Float32 and OVERFLOWS binary32 is rejected (CDZ0302, adopt range-check)
pass	a bare Qty constructor in a parameter type names its missing arguments
pass	a bare RATIONAL as op ARGUMENT — the arm reads exact numerator/denominator off the crossed value
pass	a bare SYMBOL in the Qty unit position names a unit expression, not a bare symbol, with a wrap fix
pass	a bare Set constructor in a parameter type names the missing element argument
pass	a bare TUPLE export result crosses as a typed WIT tuple (declared world)
pass	a bare TUPLE of quantities renders each element scaled to its reference (mixed inner types)
pass	a bare UInt width-constructor names the sized default with a replace fix
pass	a bare UInt8 literal-match (no newtype) is the width template
todo	a bare Value.decode with no grounding annotation is rejected CDZ0203 (unsolved target type — annotate it)
pass	a bare `=` on two identical TYPE values is refused with a guided CDZ0203 — type equality is Type.eq
pass	a bare `?` NOT in head position stays an ordinary unbound name with no try hint
pass	a bare arithmetic keyword is rejected, not a crash
pass	a bare binding form with no bindings and no body is rejected, not a crash
pass	a bare built-in constructor Some is passed as a first-class function
pass	a bare built-in generic in a VALUE annotation needs a type argument
pass	a bare built-in generic in a variant payload needs a type argument
pass	a bare conditional keyword is rejected, not a crash
pass	a bare constant string escapes across the boundary and renders
pass	a bare constant width UInt8 literal crosses to the host as its value
pass	a bare construct and a qualified match name the same colliding variant
pass	a bare data-constructor-colliding variant's OTHER arm fires on the other discriminant
pass	a bare decimal in a default-float=Float32 module grounds to Float32
pass	a bare decimal outside any default-float module keeps the Float64 default
pass	a bare def declaration keyword form declares nothing and is rejected
pass	a bare effect declaration keyword form declares nothing and is rejected
pass	a bare effect declaration that is never performed is well-formed
pass	a bare empty list reflects an undetermined element, distinct from a concrete list type
pass	a bare enum RESULT member of a typed export interface crosses (payloadless-enum disc passthrough)
pass	a bare equality keyword is rejected, not a crash
pass	a bare float list element adopts a sibling's Float32 width — the mixed-float list unifies (no rust E0308)
pass	a bare in-range ConstFloat if-branch under a Float32 annotation emits valid f32 and runs
pass	a bare list of scalars in a record result VALUE round-trips via the run/encode envelope (no wit-world clause; a typed record/sum EXPORT is a separate gap)
pass	a bare list<scalar>/List PARAM member of a typed export interface crosses (mem_leaf List-arm coverage)
pass	a bare list<u8>/Bytes PARAM member of a typed export interface crosses (multi-export list<u8> param — operator §2 decode-check half)
pass	a bare list<u8>/Bytes RESULT member of a typed export interface crosses (multi-export list<u8> result — operator §2 encode-quoted half)
pass	a bare literal adopts its equality peer's BigInt type (== against a BigInt)
pass	a bare literal adopts its ordering peer's BigInt type (< against a BigInt)
pass	a bare literal annotated Float32 is grounded at binary32 precision (literal path, not a runtime demote)
pass	a bare literal argument out of range for a narrow parameter width is rejected at check
pass	a bare literal at the narrow default's max fits and runs (no over-reject at the boundary)
pass	a bare literal effect clause is a malformed effect declaration
pass	a bare literal exceeding the default-integer pragma's narrow type is rejected
pass	a bare literal grounded to a narrow width THROUGH an arith spine is range-checked (contextual, transitive)
pass	a bare literal grounded to a narrow width through a BITWISE spine is range-checked
pass	a bare literal grounded to a narrow width through a DIVISION spine is range-checked
pass	a bare literal grounded to a narrow width through a SHIFT spine is range-checked
pass	a bare literal grounded to a narrow width through a SUBTRACTION spine is range-checked
pass	a bare literal grounds to BigInt in a constructor payload and round-trips the exact value
pass	a bare literal in a variant position of a type declaration is rejected
pass	a bare literal in an if branch grounds to the narrow result width of the sibling branch
pass	a bare literal just past Int64.max (2^63) takes the same annotate-UInt64 fix
pass	a bare literal magnitude adopts its arith sibling's fixed integer width (unify with construction)
pass	a bare literal one past the narrow default's max is rejected at the exact boundary
pass	a bare literal overflowing a narrow parameter TRANSITIVELY through a call chain is rejected at check
pass	a bare literal past Int64.max that still fits UInt64 offers an annotate-UInt64 wrap fix
pass	a bare literal past UInt64.max escalates the annotate fix to BigInt
pass	a bare literal that FITS its contextually-grounded narrow width through an arith spine computes
pass	a bare literal that overflows the annotated width does not fit it
pass	a bare literal whose width is fixed TRANSITIVELY through an ancestor arith op is range-checked
pass	a bare lowercase name in the Qty unit position names it a unit misuse, not a type variable
pass	a bare name in Unit.base names the symbol requirement with a hash-quote fix
pass	a bare name in Unit.define names the symbol requirement with a hash-quote fix
pass	a bare name in Unit.of names the symbol requirement with a hash-quote fix
pass	a bare nullary constructor is the nullary sum value
pass	a bare number bound to a Qty let-binder offers the Qty.of wrap fix
pass	a bare number directly annotated a Qty names the Qty.of repair (message-only)
pass	a bare number on the LEFT of a quantity add is wrapped in the quantity's unit
pass	a bare number on the left scales the quantity and keeps its dimension
pass	a bare number to a Qty parameter offers the Qty.of wrap fix
pass	a bare operator keeps the generic mismatch message, not a member-op phrasing
pass	a bare option<record> PARAM member crosses (compound option payload — sum_params + payload rebuild)
pass	a bare option<scalar>/Option PARAM member of a typed export interface crosses (sum_params option arm, both variants)
pass	a bare prelude Option is built via if and matched to its payload or a default
pass	a bare prelude-TYPE-colliding variant matches as the local variant in a MULTI-variant sum beside a nullary arm
pass	a bare prelude-colliding variant name matches as the local variant
pass	a bare primitive operator passed as a first-class function value folds via the annotation see-through
pass	a bare rational literal and Rational.of build one identical normalized value
pass	a bare rational literal compiles and escapes as its normalized value
pass	a bare rational literal normalizes to lowest terms (gcd-reduce), like Rational.of
pass	a bare rational literal quotes as a first-class Ast.Rational of its numerator and denominator
pass	a bare reference to a nullary function denotes its body value
pass	a bare result<ok,err>/Result PARAM member of a typed export interface crosses (sum_params Result shape, both arms)
pass	a bare string/String PARAM member of a typed export interface crosses (mem_leaf Str-arm coverage)
pass	a bare subset map pattern (fewer keys, no rest) matches a larger runtime map — open key-presence semantics
pass	a bare tuple<scalar,scalar>/Tuple PARAM member of a typed export interface crosses (positional cell rebuild)
pass	a bare type declaration keyword form declares nothing and is rejected
pass	a bare type-name-colliding variant name constructs as the local variant
pass	a bare undeclared capitalized ctor on the called path is rejected (an open sum does not sanction it)
pass	a bare under-applied generic nested as a type argument declines, does not stack-overflow the compiler
pass	a bare uppercase name in the Qty unit position names it a unit misuse, not an unknown type
pass	a bare user constructor is passed as a first-class function to a higher-order function
pass	a bare user generic ctor in a VALUE annotation needs a type argument
pass	a bare user generic ctor in a variant payload needs a type argument
pass	a bare user generic ctor in an effect-operation type needs a type argument
pass	a bare user generic in a parameter type echoes its own parameter
pass	a bare variant reusing a prelude DATA-constructor name constructs and matches as the local variant
pass	a bare wildcard used as a def body names the binding-position misuse
pass	a bare withheld-constructor pattern over an abstract value is rejected CDZ0214
pass	a bare-binder guard over a STRING (heap) param scrutinee in a helper binds and evaluates
pass	a bare-literal-arm float match under Float32 emits valid f32 across the select and probe-chain paths
pass	a bare-name match arm far from any variant is a genuine catch-all binder and runs
pass	a bare-name match arm near a variant is a typo suggesting the variant, not a binder
pass	a bare-name module body wrapped in a (do …) block is rejected at the module form
pass	a bare-scalar if guarding a MIN/-1 overflow is not select-ified
pass	a bare-scalar if guarding a div-by-zero is not select-ified — the divisor guard shields the trap
pass	a base member not re-exported by the middle module is unreachable from the entry
pass	a below-len guard lets List.at shed its own bounds check, yet an out-of-range index still returns None
pass	a below-single-precision float width is rejected as a non-admitted IEEE width
pass	a between-widths float width is rejected even though it is within the integer 1..=64 range
pass	a beyond-i64 BigInt constant operand is materialized and dropped, leaving no live heap objects
pass	a beyond-i64 constant BigInt is an operand of a runtime bigint op
pass	a big literal is NOT grounded to a narrow width across a COMPARISON boundary (arith-spine climb stops at `<`)
pass	a big-endian runtime construction read back through an le pattern crosses the byte order
pass	a bin PATTERN destructures a perform-result Bytes (the wire round-trip through a handler)
pass	a bin arm whose fixed-width segment overruns the input falls through
pass	a bin pattern binds an integer read from a fixed-width segment
pass	a bin pattern must consume the whole scrutinee or it does not match
todo	a bin pattern over a Bytes scrutinee is NOT a type error (declines only on the non-scalar param boundary)
pass	a bin pattern over a List scrutinee is a type error
pass	a bin pattern over a String scrutinee is a type error
pass	a bin pattern over a runtime non-Bytes scrutinee is a type error
pass	a bin pattern over an Int64 scrutinee is a type error
pass	a bin segment width too far from a byte-aligned kind keeps the bits guidance but carries no rename fix
pass	a bin tag-dispatch over a RUNTIME-START slice reads the re-based window per call
pass	a bin whose bit-fields do not close to a whole byte is ill-formed
pass	a bin-arm guard cond that traps at the decoded binder TRAPS the match — it does not fall through
pass	a bin-decoded frame body drives per-byte performs through a recursive walk
pass	a bin-pattern arm binds a parsed byte and PERFORMS again with it (parse-then-act)
pass	a binary arithmetic operator with too many operands offers a delete-the-surplus fix
pass	a binary form whose bit-fields do not close a byte is ill-formed
pass	a binary integer literal
pass	a binary literal with digit separators
pass	a binary set operation leaves BOTH its operands unchanged — set-algebra persistence
pass	a binary-radix literal carries the N suffix and an underscore group
pass	a binary-search isqrt with an overflow-safe hi bound computes at i64::MAX
pass	a bind directive naming a value definition rather than an effect is rejected
pass	a bind directive to a package name with no interface projection is rejected
pass	a bind directive with a malformed peer interface name is rejected
pass	a binder read off a non-variant-0 list-payload element reads the right value (vec-get read walk)
pass	a binding consumed in the left call arg while read in the right is retained
pass	a binding consumed in the right call arg while read in the left is retained
pass	a binding form with bindings but no body is rejected, not a crash
pass	a bit-field run spanning a byte boundary reads as one big-endian int split by field width
pass	a bit-field sizes a dependent bytes segment
pass	a bit-field value that needs more bits than its width does not fit its segment
pass	a bit-field wider than a byte packs across the byte boundary big-endian
pass	a bit-field width that is not a compile-time constant is ill-formed
pass	a bit-field with a negative width is ill-formed (width must be a constant natural)
pass	a bit-field with a non-constant width is ill-formed (width must be a constant natural)
pass	a bit-field-sized dependent bytes segment that overruns the input falls through
pass	a bits pattern segment reads sub-byte fields back into integers
pass	a bits pattern with a 3/5 split reads the high and low fields most-significant-first
pass	a bits run spanning TWO bytes decodes MSB-first over a runtime scrutinee
pass	a bitwise AND on an unusual signed width clears the sign bit to a non-negative result
pass	a bitwise OR on an unusual signed width sign-extends a result that sets the sign bit
pass	a bitwise XOR on an unusual signed width sign-extends a result that sets the sign bit
pass	a bitwise operator on a boolean operand names the integer requirement
pass	a block-wrapped OUTER-effect perform in a do-statement TWO nested handlers deep folds to 73 (adv-69 nh7 depth-3)
pass	a block-wrapped OUTER-effect perform in a let-init THREE handlers deep folds through the block (adv-69 a4-depth3)
pass	a block-wrapped OUTER-effect perform in a match-SCRUTINEE inside a nested handler body folds through the block (adv-69 g3-nested)
pass	a block-wrapped OUTER-effect perform in a nested handle's INIT folds to 34 (adv-69 a4-init sub-face)
pass	a block-wrapped OUTER-effect perform in a non-tail do-statement INSIDE a nested handler body folds to 73 (adv-69 c3-nested sub-face)
pass	a block-wrapped branch perform of the OUTER effect in a let-init INSIDE a nested handler body folds through the block (adv-69 a4)
pass	a block-wrapped conditional whose CONDITION performs (not a branch) folds correctly (adv-69 boundary control)
pass	a body that TRAPS keeps its OWN trap kind under an @ensures — the postcondition wrapper is transparent to an aborting body
pass	a body-internal let-local is unaffected by the free-variable currying capture
pass	a bool match missing the false arm is non-exhaustive
pass	a bool match missing the true arm is non-exhaustive
pass	a bool match on a constant scrutinee is non-exhaustive even when the constant hits the sole arm
pass	a bool value annotated a float type carries NO add-fraction fix
pass	a bool value annotated an integer type carries NO coercion fix
pass	a bool-lead list match covering only one bool value still rejects (the other first-element value is uncovered)
pass	a bool-lead-saturating list match is exhaustive without a wildcard and routes by first-match
pass	a bool-lead-saturating list match with no empty arm still rejects (length 0 uncovered)
pass	a bool-materialized (if c 1 0) compared to 0 or 1 folds to the condition (value parity)
pass	a boolean compared to a true/false literal folds to the boolean or its negation (both orders)
pass	a boolean connective with a non-boolean operand is a type error
pass	a boolean function result bound by let is still a boolean
pass	a boolean in integer-width position is rejected
pass	a boolean in integer-width position is rejected as non-natural
pass	a boolean is greater-than-or-equal to itself
pass	a boolean is less-than-or-equal to itself
pass	a boolean literal inside a constructor pattern refines the match
pass	a boolean literal pattern against an integer scrutinee is a type error
pass	a boolean literal pattern matches a runtime scrutinee
pass	a boolean parameter forwarded to a conditional result
pass	a boolean record field is projected as the program result
pass	a boolean result propagates through a three-deep chain of forwarding functions
pass	a boolean result threaded through a second function
pass	a boolean tuple element is projected as the program result
pass	a boolean-parameter function applied to false
pass	a boolean-returning function called with a false result
pass	a borrow-only compound-payload match shell is left un-dropped -- known gap (all-scalar reclaim floor)
pass	a borrowed BigInt let-binding used by three ops leaves no live heap objects
pass	a borrowed List.push result read twice is not freed by the first len (no double-free)
pass	a borrowed closure handle is called twice on the same handle (repeatable)
pass	a borrowed heap-payload sum param in a self-recursive fn is reclaimed at the loop exit (no leak)
pass	a borrowed runtime String rope compares equal to its flat twin and its compaction is leak-neutral
pass	a both-diverge MATCH nested in a value position yields the outer value or traps
pass	a both-diverge if nested in a value position yields the outer value
pass	a bottom-up fold matches a tuple of its recursive results with constructor patterns
pass	a bound effect performed with neither a handler nor a host delegation has no home
pass	a bound name in a map key is used by its value, not the literal name
pass	a bound value name as a let-binder annotation type is named a value, not a type
pass	a bound value name as a parameter's annotation type is named a value, not a type
pass	a bound value name as a value's annotation type is named a value, not a type
pass	a bounded generator produces only in-range values (0 <= roll < 64) for a runtime seed
pass	a boxed Unit-parameter closure (a lazy thunk) is forced and runs
pass	a boxed nested-unary curried closure applies without trapping
pass	a boxed runtime closure returning Unit applies without declining
pass	a boxed runtime closure taking Unit (a lazy thunk) is forced without declining
pass	a branch condition refines a variable's range so a dead underflow guard is elided (value + trap parity)
pass	a branch perform under two nested handlers threads the inner state to the continuation
pass	a branch refinement does not leak into the else arm's overflow guard
pass	a branch refinement elides a redundant AND-mask covering the refined range (value parity)
pass	a branch refinement folds a comparison of two disjoint refined ranges (value parity)
pass	a branch selects between MUTUALLY-recursive defs and the selected one runs its cycle
pass	a branch value fitting i32 but overflowing the sub-i32 Int16 target is rejected
pass	a branch-dead host-call leaks no import or host-call at any optimization level
pass	a branch-divergent next-state under a multi-perform body folds to 18
pass	a branch-performing conditional in a self-recursive performer threads the advance across recursion (rw1)
pass	a branching recursive tree fold infers its unannotated callback across both arms
pass	a branchless and over two comparisons is the conjunction of the two relations
pass	a branchless and over two leaf booleans is the full conjunction truth table
pass	a branchless or over two leaf booleans is the full disjunction truth table
pass	a build-time delegated effect whose result a returned closure captures does not escape
pass	a built map renders its entries in canonical key order
pass	a built-in Ast constructor applied to a wrong-type payload is a type error
pass	a built-in Ast.Bool constructor applied to a wrong-type payload is a type error
pass	a built-in Ast.Float constructor applied to a wrong-type payload is a type error
pass	a built-in Ast.Str constructor applied to a wrong-type payload is a type error
pass	a built-in Option is unwrapped by a helper that binds its payload
pass	a built-in Option tuple payload consumed INLINE in the Some arm projects
pass	a built-in Result whose two type arguments DIFFER is matched on both variants
pass	a built-in comparison on a COMPOUND containing an abstract type is rejected (opacity walks the compound)
pass	a built-in comparison on a LIST of an abstract type is rejected (opacity walks the list spine)
pass	a built-in comparison on a concrete-only RECORD stays legal (no over-reject through the record recursion)
pass	a built-in comparison on a concrete-only compound stays legal (the opacity recursion finds no abstract leaf)
pass	a built-in comparison on an abstract theorem value outside the kernel is rejected
pass	a built-in comparison on an abstract type's value is rejected outside its module
pass	a built-in comparison on two MAPS with abstract VALUES is rejected (direct-`=` walks the value spine)
pass	a built-in comparison on two RECORDS with an abstract FIELD is rejected (opacity walks the record spine)
pass	a built-in generic Option over-applied with two type arguments is rejected with the arity
pass	a built-in operation applied to too many arguments names its arity
pass	a byte of a runtime string's encoding is its exact UTF-8 value, not merely the right count
pass	a byte read back from a runtime-list-built Bytes has the right value
pass	a byte sequence carrying a runtime byte value is a first-class value
pass	a byte sequence is constructed from a list of integers in range
pass	a byte sequence written as a literal renders back to the same literal
pass	a byte-closing bit-field composes with a following byte-aligned integer segment
pass	a byte-decode loop advancing by a tuple projection while accumulating sum nodes compiles and runs
pass	a byte-string literal equals the explicit byte sequence it desugars to
pass	a byte-string literal nested in a sum payload refines a runtime Bytes
pass	a byte-string literal with escapes equals its explicit byte sequence
pass	a byte-string pattern over a String scrutinee is a type error
pass	a byte-walk lexer performing per byte — Bytes.at pairs with an advancing draw per position
pass	a byte-wise reversal over a seamed rope is an involution and lands bytes at mirrored offsets
pass	a bytes match with only a bin arm and no catch-all is non-exhaustive
pass	a bytes segment sized by a CONSTANT LITERAL binds a fixed number of bytes then composes
pass	a bytes value chosen by a runtime if is length-measured with valid wasm
pass	a bytes-param leaf and a spilled record result in one member run through both memory paths (via an imposed WIT world)
pass	a call-returned Bool newtype literal-payload arm spills correctly
pass	a call-returned Int32 newtype literal-payload arm spills at the narrow width
pass	a call-returned UInt8 newtype literal-payload arm spills at the narrow width
pass	a called def's zero-arm scalar match reports the non-exhaustive reject exactly once, not at the call site too
pass	a called function's body resolves its free names lexically, not in the caller's scope
pass	a called scalar member access surfaces the type-naming message, not the bare one
pass	a called tuple index out of range surfaces the arity-naming message, not the bare one
pass	a callee whose body LET-BINDS a handle seeded by its param, called with the caller's runtime arg, keeps that arg bound
pass	a caller-observed pure-mutual SCC (neither member self-recurses) folds via the group-wide multi-value fold
pass	a camelCase round-trip resolves through kebab boundary-name normalization
pass	a camelCase same-signature multi-export normalizes each make-<name>
pass	a cancelling add-then-subtract chain keeps the intermediate overflow
pass	a canonical nullary arrow operation type compiles (the control)
pass	a canonical-order set drain EMITS each element through an effect and the state digit-encodes the order
pass	a capture-free de Bruijn beta-reduction over a nameless term sum reduces correctly
pass	a captured lambda applied at two shadowing sites resolves each to its DEF-SITE capture (reduce-cache share soundness)
pass	a captured list survives a structural rebuild of the same source between capture and application
pass	a captured pre-shadow value survives the re-binding
pass	a capturing 32-bit-integer closure crosses and is called
pass	a capturing closure crosses as a repeatable (borrow<t>) callback handle
pass	a capturing closure export tracks both the captured value and the call argument
pass	a capturing closure is applied through a recursive higher-order function
pass	a capturing closure passed AS AN ARGUMENT is applied twice by the callee and the capture reads after
pass	a capturing closure returning Bytes
pass	a capturing closure returning a COMPOUND is a repeatable (borrow<t>) callback handle
pass	a capturing closure returning a String
pass	a capturing closure returning a nominal-over-scalar
pass	a capturing closure stored as a runtime list element is extracted by List.at and applied
pass	a capturing closure stored in a let-bound record is projected and applied
pass	a capturing closure stored in a let-bound tuple is projected and applied
pass	a capturing closure stored in a list and also called directly emits a valid artifact
pass	a capturing closure stored in a map and also called directly emits a valid artifact
pass	a capturing closure stored in a sum payload and also called directly emits a valid artifact
pass	a capturing closure stored in a tuple and also called directly folds through its capture
pass	a capturing closure stored in a tuple is extracted and applied
pass	a capturing closure stored in a variant is applied via a devirtualized direct call
pass	a capturing closure whose body uses the capture after an inner computation
pass	a capturing closure whose surviving map store and direct call both feed the result
pass	a capturing closure with a let binding in its body
pass	a capturing unannotated closure in a payload is typed from the declared arrow
pass	a catch-all after the specific arms already cover a finite type is unreachable and earns a CDZ0213 warning
pass	a chain of let bindings each referencing the immediately-preceding one accumulates
pass	a chain of perform-fed let inits — each binding feeds the next perform's argument
pass	a chained Option.expect(Option.expect s) payload consumed per iteration accumulates stably
pass	a chained Unit.in re-wrapped with Qty.of converts inch to cm exactly (127/50)
pass	a chained access through an if-of-records composes and shields the untaken branch's trap
pass	a chained float ordering is not foldable by transitivity — a NaN middle breaks the chain
pass	a chained generic instantiates at a MAP type and both tuple slots share the CHAMP
pass	a char compared to a number is not comparable and offers the Char.to-int wrap fix
pass	a char equated to a number offers the Char.to-int wrap fix
pass	a char literal at a non-head fixed-arity list position matches by value
pass	a char literal list element dispatches a runtime list by its value
pass	a char literal list element falls through on a non-matching head
pass	a char literal naming a surrogate is a reader error
pass	a char literal past the maximum scalar is a reader defect
pass	a char not among the literal arms falls through to the wildcard
pass	a char order agrees with its scalar value
pass	a char summed into a NARROW int takes the int-width coercion, not Char.to-int
pass	a char where an integer is expected offers the Char.to-int wrap fix
pass	a char-literal list-element pattern matches a runtime list whose head is that char
pass	a char-literal list-element pattern misses when the head differs
pass	a char-literal pattern nested in a variant payload matches by scalar value
pass	a char-literal pattern over a non-char scrutinee is a type error
pass	a char-literal pattern over an Int scrutinee is a shape error
pass	a char-literal pattern selects the arm whose char matches
pass	a char-literal payload matches over a RUNTIME sum scrutinee (boxed char payload, Char-rep 4/N)
pass	a checked addition of two runtime-wrapped bytes still guards at the narrow width
pass	a checked conversion of a negative CONSTANT into an unsigned type is rejected
pass	a checked conversion of a negative constant into an unsigned type is rejected
pass	a checked integer conversion at the signed boundary fits and converts unchanged
pass	a checked integer conversion in range yields the value unchanged
pass	a checked integer conversion of an out-of-range CONSTANT is rejected at compile time
pass	a checked integer conversion that fits succeeds
pass	a checked result is consumed by matching its Option at run time
pass	a checked-add over RUNTIME UInt64 operands emits the unsigned wrap-below check
pass	a checked-add over a RUNTIME operand emits the overflow check: Some when it fits, None on overflow
pass	a checked-mul over a RUNTIME operand emits the division round-trip: Some when it fits, None on overflow
pass	a checked-sub over RUNTIME UInt64 operands emits the unsigned underflow check
pass	a checked-sub over RUNTIME operands emits the underflow check: Some when it fits, None on underflow
pass	a churned-back map IS a CHAMP key equal to the direct build (history-independence as a key)
pass	a clean program with no dead computation earns no dead-trap warning
pass	a close-typo handler arm op names the declared op with a replace fix
todo	a closed literal closure forwarded through const-wrapper hops is not a false reject
pass	a closed sum match covering every named variant is exhaustive without a wildcard
pass	a closure NESTED IN A TUPLE that performs a delegated effect cannot cross the host boundary
pass	a closure RETURNS its captured map and the alias plus the original both read after
pass	a closure applied through an unbuilt sibling variant compiles and runs
pass	a closure argument is another closure's result
pass	a closure built OUTSIDE a handler is applied INSIDE it beside performs
pass	a closure built OVER another closure applies both capture layers
pass	a closure built in a fused-match arm captures the payload binder and reads it after the match
pass	a closure captures TWO inner-handled perform results across NESTED lets and escapes under an outer handler
pass	a closure captures a MAP and serves lookups at call time
pass	a closure captures a boolean and reads it back inside its lifted body through a recursive HOF
pass	a closure captures a capturing closure and calls it through a recursive HOF
pass	a closure captures a function value and applies it through a recursive HOF
pass	a closure captures a heap binding BEFORE a shadow and applies AFTER seeing the original
pass	a closure captures a runtime-depth RECURSIVE SUM and folds it at call time
pass	a closure captures a sum value and matches it inside its lifted body through a recursive HOF
pass	a closure captures a tuple and projects it inside its lifted body through a recursive HOF
pass	a closure captures another closure and applies it
pass	a closure captures another closure and applies it at RUNTIME
pass	a closure captures its environment by value at creation, unaffected by a later same-named binding
pass	a closure captures the binding in scope where it was created
pass	a closure captures the param generation BEFORE a shadow and applies after
pass	a closure captures the result of a build-time host op called with an argument
pass	a closure captures two inner-handled results across nested lets
pass	a closure capturing THREE scalars is made and called
pass	a closure capturing a RUNTIME String.slice→to-bytes VIEW reads it at host-call dispatch
pass	a closure capturing a RUNTIME-BUILT list indexes the captured spine at call time
pass	a closure capturing a build-time host effect preserves the order of two host calls
pass	a closure capturing a handler-computed VALUE escapes the handle and applies outside
pass	a closure capturing a heap map is applied twice and the map is read once more after
pass	a closure capturing a runtime String ROPE reads its byte length at call time
pass	a closure capturing a value computed under a handler may escape the handle applied directly
pass	a closure capturing an inner-handled perform result closes over the value under an outer handler
pass	a closure capturing an inner-handled perform result is applied under an OUTER handler of the same effect
pass	a closure capturing an inner-handled perform result needs no outer handler
pass	a closure capturing perform results escapes to NO handler at all and applies pure
pass	a closure capturing two enclosing bindings folds through nested arithmetic
pass	a closure capturing two enclosing bindings threads a multi-slot environment through a recursive HOF
pass	a closure capturing two values is made and called by the host
pass	a closure capturing values of DIFFERENT types (Float64 + Int64)
pass	a closure carried in a USER-declared sum's payload is extracted and applied
pass	a closure carried in a sum payload is extracted by a match and applied
pass	a closure crosses the perform boundary as an operation ARGUMENT and applies in the arm
pass	a closure driving control flow off a captured boolean
pass	a closure export alongside a plain scalar export — the closure runs
pass	a closure export alongside a plain scalar export — the plain export runs
pass	a closure exported to the host is called by the host
pass	a closure factory's returned capturing closure is applied at the call site
pass	a closure looked up from a map by a perform result, applied to a perform result, threads through call_indirect
pass	a closure performing a HANDLED (non-delegated) effect does NOT trip the escape reject
pass	a closure returned from a match arm carries the arm's HEAP payload binding
pass	a closure returned from a recursive function is applied through a recursive HOF
pass	a closure returning Bytes crosses to the host as list<u8>
todo	a closure returning Unit crosses the boundary — unit is a zero-result (should-work)
pass	a closure returning a LIST of tuples (direct-call)
pass	a closure returning a List crosses as the value form
pass	a closure returning a Map — canonical key order
pass	a closure returning a NESTED List
pass	a closure returning a NESTED record crosses as the typed value form
pass	a closure returning a NESTED tuple
pass	a closure returning a SUM of a tuple (direct-call)
pass	a closure returning a Set — canonical member order
pass	a closure returning a boolean is called by the host
pass	a closure returning a constant String crosses as its UTF-8 bytes
pass	a closure returning a nominal-over-Bool erases to bool at the boundary
pass	a closure returning a nominal-over-scalar crosses as the underlying scalar
pass	a closure returning a record crosses as the typed value form
pass	a closure returning a runtime String (concat) crosses as its bytes
pass	a closure returning a tuple crosses as the typed value form
pass	a closure returning a tuple whose element is a LIST (compound-with-collection)
pass	a closure returning a tuple with a Bool leaf
pass	a closure returning a tuple with a negative int leaf
pass	a closure returning an EMPTY List
pass	a closure returning an empty Bytes crosses as the empty list
pass	a closure returning an empty String crosses as the empty list
pass	a closure stored as a MAP value is looked up by a runtime key and applied
pass	a closure taking a Result Bool Bool ARG crosses the direct-call boundary
pass	a closure taking a Result Int64 Int64 ARG — Err crosses the direct-call boundary
pass	a closure taking a Result Int64 Int64 ARG — Ok crosses the direct-call boundary
pass	a closure taking a nominal-over-scalar argument receives the underlying scalar
pass	a closure taking an Option Bool ARG crosses the direct-call boundary
pass	a closure taking an Option Float64 ARG crosses the direct-call boundary
pass	a closure taking an Option Int32 ARG (narrow-int payload) crosses the direct-call boundary
pass	a closure taking an Option Int64 ARG — None crosses the direct-call boundary
pass	a closure taking an Option Int64 ARG — Some crosses the direct-call boundary
pass	a closure that performs a delegated effect cannot cross the host boundary
pass	a closure whose CALL returns a tuple CONTAINING a list
pass	a closure whose CALL returns a user sum crosses as the value form
pass	a closure whose CALL returns an Option crosses as the value form
pass	a closure whose body applies a nested lambda in place runs through a recursive HOF
pass	a closure whose body calls a top-level helper function
pass	a closure's captured perform result survives a LATER state advance (capture-time, not re-read)
pass	a closure-driver result feeds a perform's ARGUMENT
pass	a closure-payload sum built by an if-helper with a reused arg compiles and runs, not CDZ0101 (value parity)
pass	a closure-payload sum picked by an if-helper, applied by a match-consumer, with a binding reused as both args
pass	a coercible mistyped resume value carries an of-conversion wrap fix
pass	a collection-carrying recursion whose base arm does a fallible indexed read compiles
pass	a collection-carrying recursion whose base arm reads via a raw match compiles
pass	a collision node holds a FIXNUM immediate and a BOXED Int64 sharing one hash as distinct keys
pass	a combined left shift that overflows Int64 traps like the single shift it folds to
pass	a common constructor composes through a hoisted tuple field to build once
pass	a common float equality hoisted out of both if arms compares once over the selected operand
pass	a common float ordering hoist reproduces the IEEE partial order including NaN
pass	a common integer comparison hoisted out of both if arms compares once over the selected operand
pass	a common operator hoisted out of both if arms selects the operand and computes once
pass	a comparison against a constant outside a masked value's range folds, but an in-range one stays a runtime test
pass	a comparison against a derived (masked) range bound folds or stays per the range
pass	a comparison operator passed to a recursive HOF (emit-call)
pass	a comparison pair split across a nested connective reassociates and folds to the interval
pass	a comparison walls the FLOAT width from a literal above it
pass	a comparison's trapping shared bound fires under a trap-free condition
pass	a comparison-driven MERGE of two sorted lists interleaves and drains the remainder
pass	a comparison-predicate function returns its boolean result
pass	a comparison-select over the two zeros returns the UNTAKEN operand and the canonical eq sees which
pass	a compile-time Char comparison folds beside performs (the runtime-Char boundary's served face)
pass	a compile-time expansion residual combines with a runtime param per call
pass	a compile-time type branch reads a runtime parameter's static type
pass	a complementary comparison pair nested past a third operand still folds via reassociation
pass	a complementary comparison split across a nested connective folds to false
pass	a complete handler inside a CLOSURE body — each application instantiates fresh
pass	a component's manifest is the union of two entrypoints' distinct rows
pass	a compose combinator captures TWO function values and applies them in declared order
pass	a compose combinator over HEAP-typed functions pipelines list transformers
pass	a composed call over a record-transforming function with an arithmetic field compiles
pass	a composed proof chains the equational, λ, and logical rule families into one derivation
pass	a composed record-transform call bumps a field, applied twice, and the result field is read
pass	a composed three-field record-transform triples a field twice and reads it last
pass	a compound Ast SUBTREE rides a template hole into the expansion intact
pass	a compound built from two distinct peers escapes the entrypoint as a resource
pass	a compound child borrowed out of a sum shell via a match binder is not use-after-freed -- UAF guard + known leak
pass	a compound element projected from a let-bound runtime tuple is matched
pass	a compound export clause whose head is a name is rejected with a recover-the-name fix
pass	a compound if condition is a type error, not an unbound name
pass	a compound map key containing a Bytes rope is found by its flat-twin key
pass	a compound map key containing a rope is found by its flat-twin key
pass	a compound map key whose Bytes element is a rope is found by its flat twin
pass	a compound map key whose LIST element is a rope is found by its flat-twin key
pass	a compound map key whose string element is a rope is found by its flat-twin key
pass	a compound-capturing residual closure built once and CALLED TWICE keeps the capture live across both calls
pass	a compound-element float renders the same full expansion as the scalar path
pass	a compound-result borrowed closure handle is called twice on the same handle (repeatable)
pass	a computed (runtime-branched) label operand to Record.with is rejected — labels are static
pass	a computed Float32 map key is found by its literal twin
pass	a computed float map key is found by its literal twin
pass	a computed integer second operand against a float wraps in Float64.of-int
pass	a computed match scrutinee is evaluated once and its overflow guard still fires
pass	a computed out-of-range value under a narrow let-binder annotation is range-checked
pass	a computed-index Bytes.at over a to-bytes view of an effect-grown rope emits valid wasm
pass	a computed-index List.update then push across three dispatches emits valid wasm (ListUpdate index scratch width-partitioned)
pass	a computed-index String.at over an effect-grown rope emits valid wasm
pass	a concat of two runtime SLICES joins the sliced views, not the originals
pass	a concat-built and a push-built SMALL list (single-leaf, n<=32) with the same elements are the same map key
pass	a concat-built list = a push-built list with the same elements (element-wise, shape-independent, n>=33)
pass	a concat-built rope of 1-, 2-, and 3-byte scalars measures and indexes at every width
pass	a concat-of-concat chain keeps every intermediate binding readable
pass	a concat-reached string equals a slice-reached string of the same content and not a decoy
pass	a conditional abort hoisted alongside a second abortive sibling declines cleanly
pass	a conditional evaluates only the selected branch
pass	a conditional inside a function with a constant condition and mismatched branches is a type error
pass	a conditional let-INIT whose abort branch is NOT taken folds the non-aborting branch
pass	a conditional on a negated runtime condition selects the correct branch and shields the other
pass	a conditional performs only the taken branch's effect — else
pass	a conditional performs only the taken branch's effect — then
pass	a conditional selects a branch by a runtime value that is not known at compile time
pass	a conditional selects the false branch when the condition is false
pass	a conditional shields a trap in a nested unselected branch
pass	a conditional type error is caught even when the mismatched branch is the one taken
pass	a conditional whose branches are unit yields unit
pass	a conditional whose condition folds to a constant still drops the untaken trapping branch
pass	a conditional with a compound branch and a scalar branch is a type error even when the compound branch is dead
pass	a conditional with a missing branch is rejected, not a crash
pass	a conditional with an integer then-branch and a boolean else-branch is a type error
pass	a conditional with integer and floating-point branches is a type error
pass	a conditional with too many operands is rejected, not a crash
pass	a conditional with two list branches of different length is well-typed
pass	a conditional with two tuple branches of different arity is a type error
pass	a conditional with two tuple branches of different element type is a type error
pass	a conditional's condition may itself be a conditional
pass	a conditional-resume (two-site) arm folds across TWO performs via the two-hole refold
pass	a conditional-resume (two-site) arm folds across a SINGLE perform
pass	a conditionally-reached trap in a loop body fires exactly when its iteration arrives
todo	a conditionally-resuming (abortive-or-resume) arm reading the enclosing fn's param declines cleanly (not-yet-reducible, not a false unbound)
pass	a conditionally-selected nullary variant is returned as a runtime sum result
pass	a conditionally-selected variant is returned as a runtime sum result
pass	a confident prelude-module member typo carries an applyable rename fix
pass	a conjunction guards a let over a runtime value inside a conditional
pass	a conjunction of two float compares preserves NaN-false through the connective fold
pass	a conjunction/disjunction range condition refines both bounds so guards are elided (value + trap parity)
pass	a connective-wrapped perform in an if condition threads its state advance to the taken branch
pass	a consistent annotation type-checks against the inferred type
pass	a const (<) of a LIST-leaf compound folds (nested list inside a tuple)
pass	a const (<) of two constant LISTS folds element-wise under the const demand
pass	a const (<) of two constant TUPLES folds to a Bool under the const demand
pass	a const (>) of two same-discriminant SUMS folds by payload
pass	a const (>=) of two constant RECORDS folds in canonical (name-lexicographic) field order
pass	a const (Ordering.of) of two constant LISTS folds to an Ordering (longer-with-greater-tail)
pass	a const (Ordering.of) of two constant RECORDS folds in canonical (name-lexicographic) field order
pass	a const (Ordering.of) of two constant TUPLES folds to an Ordering under the const demand
pass	a const (Ordering.of) of two constant tuples byte-matches the RUNTIME value-cmp (cross-check)
pass	a const (Ordering.of) of two same-discriminant SUMS folds by payload
pass	a const Ast-list FILTERED by a nested recursive comment-peeler const-evaluates at compile time
pass	a const Map keyed on a SUM variant folds a lookup by structural key equality
pass	a const Map keyed on a TUPLE folds a lookup by structural key equality
pass	a const Map lookup of an absent TUPLE key folds to None
pass	a const Map.to-list carries the value at each index
pass	a const Map.to-list enumerates key-sorted — the head entry is the smallest key
pass	a const Map.to-list folds its length under a const demand
pass	a const Map.to-list order byte-matches the RUNTIME map-to-list op (cross-check)
pass	a const Map.to-list sorts STRING keys lexicographically
pass	a const Set of RECORDS dedups by structural field equality
pass	a const Set of TUPLES dedups by structural equality
pass	a const Set.to-list folds to a canonically-sorted list under a const demand
pass	a const Set.to-list of BYTES elements byte-matches the RUNTIME set-to-list (cross-check)
pass	a const Set.to-list of BYTES elements folds, dedup'd, in unsigned byte order
pass	a const Set.to-list of CHAR elements folds sorted by Unicode scalar, dedup'd
pass	a const Set.to-list of RECORD elements byte-matches the RUNTIME set-to-list (cross-check)
pass	a const Set.to-list of RECORD elements folds in canonical (name-lexicographic) field order, dedup'd
pass	a const Set.to-list of SUM (Option) elements folds by discriminant-then-payload, dedup'd
pass	a const Set.to-list of SUM elements byte-matches the RUNTIME set-to-list (cross-check)
pass	a const Set.to-list of TUPLE elements byte-matches the RUNTIME set-to-list (cross-check)
pass	a const Set.to-list of TUPLE elements folds, dedup'd, in element-wise lexicographic order
pass	a const Set.to-list order byte-matches the RUNTIME set-to-list op (cross-check)
pass	a const Set.to-list orders negative ints numerically, not by encoding
pass	a const Set.to-list orders strings lexicographically
pass	a const Set.to-list over a RECURSION-built set folds to the sorted list
pass	a const Set.to-list result flows into a const-param helper
pass	a const Set.to-list sorts ints in canonical (numeric-ascending) value order, dedup'd
pass	a const closure re-passed through a recursive driver specializes and computes correctly
pass	a const collection recursively folded UNROLLS to its result at compile time
pass	a const handle threading a growing List state folds its query answers
pass	a const handle threading a growing Set state folds its query answers
pass	a const list PREFIX orders less than its extension (shorter-is-less tiebreak) under the const demand
pass	a const list recursively FILTERED (conditional include) folds to a constant
pass	a const list whose elements are BYTES LITERALS recursively folds — a BytesOf is a const value
pass	a const param re-passed on a mixed-match recursive arm does not drop, single and two-const forms (value parity)
pass	a const parameter rejects an argument that depends on runtime data
pass	a const top-bit-set u64 bin binding folds unsigned
pass	a const trap consumed by Ast.encode surfaces its message, not the generic decline
pass	a const-folded RECORD descriptor holds a recursive-transform result, projected at compile time
pass	a const-key entry is found beside a distinct runtime-keyed entry
pass	a constant +(-1) add (negative constant) traps only at Int64.min
pass	a constant +1 add traps only at Int64.max, never spuriously near min
pass	a constant -(-1) subtract (= a+1) traps only at Int64.max
pass	a constant -1 subtract traps only at Int64.min
pass	a constant BigInt crosses the host boundary as its value form
pass	a constant BigInt operand of a runtime BigInt op materializes as a heap value
pass	a constant MUL over a high UInt64 operand that overflows still rejects
pass	a constant MUL-by-0 identity over a high UInt64 operand folds to zero
pass	a constant MUL-by-1 identity over a high UInt64 operand folds to the operand
pass	a constant SIGNED shift-right sign-extends (the unsigned-width fold bails on a signed type)
pass	a constant add of two high UInt64 operands within range folds to the sum
pass	a constant algebraic identity over a high UInt64 operand folds to the operand, not a spurious reject
pass	a constant argument out of a narrow parameter's range is rejected, not laundered
pass	a constant arithmetic operation overflowing a narrow parameter width is a const trap
pass	a constant bin construction folds alongside performs in the same handle body
pass	a constant bit-and over a high UInt64 operand masks over the solved width
pass	a constant bit-or over a high UInt64 operand folds over the solved width
pass	a constant division by zero over a high UInt64 operand still rejects
pass	a constant division over a high UInt64 operand folds exactly to the quotient
pass	a constant float ordering with a NaN operand folds to false, not a decline
pass	a constant integer widens to BigInt and narrows back through Int64 unchanged
pass	a constant left shift of -1 by 63 is exactly the minimum integer
pass	a constant left shift of 1 by 63 overflows and is rejected
pass	a constant list update at an index that wraps below the length is also rejected
pass	a constant list update at an out-of-bounds index is rejected at compile time
pass	a constant logical shift-right over a high UInt64 operand folds over the solved width
pass	a constant mixed-leaf tuple (Int64 beside Bool) is returned as a program result
pass	a constant modulo over a high UInt64 operand folds to the remainder
pass	a constant multi-payload variant match binds both payloads and folds to a scalar
pass	a constant multi-payload variant match on the IA-variant nested payload folds to the right depth
pass	a constant multi-payload variant match reaches each payload and dispatches a nested variant
pass	a constant nested tuple is returned as a program result
pass	a constant non-Map operand to Map.lookup is a coded type mismatch, not an uncoded decline
pass	a constant non-Set operand to Set.contains is a coded type mismatch, not an uncoded decline
pass	a constant non-Set operand to Set.insert is a coded type mismatch, not an uncoded decline
pass	a constant non-String operand to String.at is a type mismatch, not an uncoded decline
pass	a constant non-String operand to String.byte-len is a type mismatch, not a const-fold decline
pass	a constant non-String operand to String.concat is a type mismatch, not an uncoded decline
pass	a constant non-String operand to String.slice is a type mismatch, not an uncoded decline
pass	a constant non-integer operand to Int64.of is a coded type mismatch, not a misleading conversion decline
pass	a constant rational with a zero denominator is rejected and names the nonzero-denominator repair
pass	a constant record is returned as a program result
pass	a constant shift by an OUT-OF-RANGE count is rejected at compile time with CDZ0304
pass	a constant shift count at or beyond the width over a high UInt64 still rejects
pass	a constant shift-left of a small UInt32 operand into its high bit folds (width-generic guard)
pass	a constant shift-left of a small UInt64 operand whose result overflows i64 folds over the solved width
pass	a constant shift-left that overflows a high UInt64 width still rejects
pass	a constant shift-left whose result exceeds i64 but fits the UInt64 width folds, not declines
pass	a constant subtraction over a high UInt64 operand folds to the difference
pass	a constant success `?` folds INLINE in a subexpression with no let-binding
pass	a constant tuple is returned as a program result
pass	a constant value out of range for a non-aliased bit-field bin segment names the (UInt k) type and range
pass	a constant value out of range for a signed bin segment names the signed type and negative-inclusive range
pass	a constant value out of range for its unsigned bin segment is a provable trap naming value, type, range
pass	a constant variant carrying a tuple payload destructures by a nested tuple pattern and folds
pass	a constant-condition if folds to its fitting branch under a narrow annotation and runs
pass	a constant-count shift and constant-divisor remainder are trap-free so a tautology comparison folds (value + trap parity)
pass	a constant-failure `?` in a NESTED let elides a trapping OUTER-let init it discards
pass	a constant-failure `?` short-circuit ELIDES an earlier trapping let-init whose value it discards
pass	a constant-folded u64 bin binding with the top bit set compares unsigned
pass	a constant-index String.at result compares equal to a literal by content
pass	a constant-index String.at result compares unequal to a different literal
pass	a constant-lhs and whose right operand aborts desugars and folds to the arm value
pass	a constant-on-the-left +1 add is commutative and traps only at Int64.max
pass	a constant-returning closure threads through a recursive-generic HOF
pass	a constant-scrutinee guarded match with a fall-through folds to the guarded body when its guard holds
pass	a constant-size utf8 segment over ill-formed UTF-8 is a non-match, not a trap
pass	a constant-try helper folds and its result feeds a handler arm's resume
todo	a constant-try helper that fails feeds the None arm's fallback into resume
pass	a constructed Ast.Bool leaf drives an evaluated conditional
pass	a constructed huge Ast.Int equals its quoted twin
pass	a construction spread of a single list with no inline elements is that list
pass	a construction spread of an empty list contributes no elements
pass	a construction spread with a leading and a trailing spread around an inline element
pass	a construction spread with only spreads concatenates the operands (no inline element)
pass	a constructor CHOSEN by matching one sum is applied to build another
pass	a constructor STORED in a tuple is extracted and applied as a first-class closure
pass	a constructor list element dispatches a runtime list by its discriminant
pass	a constructor pattern in a tuple payload slot is matched in one arm
pass	a constructor pattern nested under Some matches a runtime list element
pass	a constructor pattern of the wrong arity is a type error naming the field count
pass	a constructor selected at RUNTIME is a first-class value that is then applied
pass	a constructor whose payload is a constructor keeps the outer variant tag
pass	a constructor with an all-irrefutable payload shadows a later refining arm of the same variant
pass	a constructor-export naming a near-miss variant suggests the constructor with a rename fix
pass	a constructor-export of a named variant is well-formed and not flagged
pass	a constructor-export of all variants (. T *) is well-formed and not flagged
pass	a constructor-export whose TYPE head is a near-miss suggests the declared type with a rename fix
pass	a constructor-export whose head is a FAR-MISS undeclared name gets no spurious did-you-mean
pass	a constructor-export whose head is a value definition names the value-definition category
pass	a constructor-export whose head is an undeclared name names the not-a-declared-type category
pass	a constructor-export whose head names a VALUE (not a sum type) gets no spurious did-you-mean
pass	a constructor-pattern list element over a NON-SUM element type gets no spurious variant suggestion
pass	a constructor-tree eval's folded result composes with a runtime parameter per call
pass	a consumed and a borrowed read of one loop invariant coexist per iteration
pass	a consumed bare None type-checks without annotation (ambiguity is escape-only)
pass	a consumed closure is applied once per iteration of a recursive loop
pass	a consumed empty list type-checks without annotation (ambiguity is escape-only)
pass	a consumed heap param on a self-recursive loop's non-tail spine is not double-freed
pass	a consumer applies TWO handed-back closures
pass	a consumer applies the handed-back closure inside a larger expression
pass	a consumer applies the handed-back closure to an internal constant
pass	a consumer bound to two distinct peer interfaces combines their results
pass	a consumer bound to two scalar peer interfaces runs with no runtime
pass	a consumer calls a scalar op across a source peer provider
pass	a consumer returns a different type than the closure it applies
pass	a consumer takes the handed-back closure as its SECOND parameter
pass	a consuming op inside a hoisted list element respects a still-live binding
pass	a consuming op on a still-live binding inside one match arm's payload keeps its retain
pass	a context-typed empty float map accepts a runtime float key insert
pass	a context-typed empty float map needs no key inserted to ground its wrapper type
pass	a context-typed empty float set grounds its element wrapper type
pass	a context-typed empty float32 map grounds the narrow-float wrapper
todo	a continuation call whose body ITSELF performs is not effect-free and declines
todo	a continuation filed through a RECURSIVE pqueue insert (base arm) declines cleanly, never miscompiles
pass	a contradictory ARROW annotation on a function value is rejected
pass	a contradictory arrow annotation on a function INSIDE a compound is rejected
pass	a contradictory arrow annotation on a function in a SUM payload is rejected
pass	a contradictory arrow annotation on a function in a runtime Map value is rejected
pass	a contradictory arrow annotation on a function stored as a Map KEY is rejected
pass	a control-flow operand of a narrow op still range-checks the enclosing overflow
pass	a correctly-annotated function parameter is accepted
pass	a correctly-applied unary constructor (Some 5) compiles and matches (the control)
pass	a correctly-typed map-pattern key compiles and matches (no over-rejection)
pass	a cross-arm perform with the handler nesting FLIPPED has no home — arm bodies resolve under the handlers enclosing their handle
pass	a cross-dimension if-join clash stays a plain mismatch (no same-dimension scale tail)
pass	a cross-dimension list-join clash stays a plain mismatch (no same-dimension scale tail)
pass	a cross-function fold whose out-state is NOT observed stays single-return
pass	a cross-function fold's out-state is observed by a read-out op in the continuation
pass	a cross-function perform is discharged by the caller's handler
pass	a cross-function recursive fold's out-state threads to a later perform in the caller's continuation
pass	a cross-handler foreign-perform op-arg into a MATCH-shaped-resume arm folds under nested handlers (nv1f)
pass	a cross-handler op-arg performing an outer effect runs EXACTLY ONCE though the arm uses it thrice
pass	a cross-kind clash message picks the indefinite article by SOUND, not letter (a UInt8)
pass	a cross-kind equality of a tuple against a list is one coded error with no uncoded heap-walk decline
pass	a cross-kind ordering comparison is a single coded type error, not a double with the ordering decline
pass	a cross-kind ordering of a tuple against a list is one coded error with no uncoded heap-walk decline
pass	a cross-type constructor pattern NESTED in a matching outer payload is rejected
pass	a cross-type variant pattern of the SAME payload width and colliding tag is rejected
pass	a cross-type variant pattern whose payload width differs is rejected, not miscompiled
pass	a cross-width nan comparison (Float32.nan vs Float64.nan) is a type error
pass	a crossed rope String compares EQUAL to an arm-local flat literal — content equality over the marshal
pass	a crossing text-literal arm is rejected even beside a same-kind sibling arm
pass	a ctl arm with an unused k binder is an ordinary resumptive arm and runs
pass	a ctl-style arm applying its continuation inside a match scrutinee resolves and folds
pass	a ctl-style arm that LET-BINDS its continuation result then reads the state binder folds
pass	a ctl-style arm that applies its continuation lexically folds through the delimited context
pass	a ctl-style arm that reads the STATE binder AROUND its continuation application folds
pass	a ctl-style arm whose continuation ESCAPES to another function reifies it as a closure and applies it there
pass	a ctor-in-tuple-slot match is expressible by binding the tuple then re-matching
pass	a ctor-lead list match missing a variant still rejects (that first-element value is uncovered)
pass	a ctor-lead-saturating list match is exhaustive without a wildcard and binds the moved payload
pass	a ctor-lead-saturating list match with no empty arm still rejects (length 0 uncovered)
pass	a cube dimension renders as Unit.^ with exponent 3
pass	a curried closure capturing an inner-handled result closes over the value across both applications
pass	a curried function can be partially applied
pass	a curried function-type annotation checks its inner result type against the argument
pass	a curried lambda stored in a tuple is projected and applied through both levels
pass	a curried newtype constructor applies to completion
pass	a curried-syntax application of a multi-payload constructor reaches full arity and constructs
pass	a curried-syntax application of a runtime closure flattens to one full-arity indirect call
pass	a cycle of module imports is rejected
pass	a data-rate quantity converts a binary-prefix composite unit to its byte/second reference
pass	a dead (unreferenced) argument is still checked for its own fault
pass	a dead-let SET construction with a trapping scalar arg traps — a set constructor is strict in its element args too
pass	a dead-let list construction with a HEAP-producing trapping arg also traps — force-eval extends to heap leaves
pass	a dead-let list construction with a trapping scalar arg traps — args forced though the list is discarded
pass	a decimal integer literal
pass	a decimal kilobyte and a binary kibibyte are distinct units of one dimension
pass	a decimal kilobyte and a binary kibibyte convert distinctly over Int64
pass	a decimal literal annotated Rational equals its explicit constructor form
pass	a decimal literal annotated Rational grounds to its exact fraction
pass	a decimal literal annotated Rational is exact for a value no float rounds cleanly
pass	a decimal token trailing letters is a malformed literal, not an unbound name
pass	a declaration in a do block shadows an outer binding
pass	a declared-world enum EXPORT whose guest case order MISMATCHES the WIT remaps by case NAME to the declared order
pass	a decoded field re-encodes into the same-width segment without an explicit narrow
pass	a decoded length field re-encodes a length-prefixed frame end to end
pass	a decomposed literal indexes and measures by its normalized length, not the raw scalar count
pass	a deep 50-argument curried application spine reduces to the right value
pass	a deep BOOL literal matches a record field nested in a tuple (hit + fall-through)
pass	a deep CHAR literal matches at a record field (hit + fall-through)
pass	a deep INT literal matches at a record field (hit + fall-through)
pass	a deep NEGATIVE int literal matches a record field nested in a tuple (hit + fall-through)
pass	a deep NESTED-VARIANT pattern selects a runtime recursive sum by spine depth
pass	a deep STRING literal matches at a record field (hit + fall-through)
pass	a deep balanced tree is built and folded to a scalar, consuming both children
pass	a deep chain of runtime-list let-bindings compiles and returns the final length
pass	a deep compound nesting all six leaf kinds round-trips through encode and decode
pass	a deep consume leaves the intermediate compound and a sibling element readable
pass	a deep four-leaf tree round-trips through encode and decode
pass	a deep many-chunk runtime byte rope indexes and measures correctly through its depth
pass	a deep many-chunk runtime string rope indexes and measures correctly through its depth
pass	a deep nested-constructor pattern matching a nullary variant two layers deep solves its switch path
pass	a deep out-of-order queue drains fully time-sorted with FIFO across every tie-group
pass	a deep push chain beside two other live bindings retains each correctly
pass	a deep rope indexes by scalar at both extremes
pass	a deep trie built BETWEEN two host calls reads correctly after the second
pass	a deep uniform arith accumulator chain (CSE-partition shape) computes the same value across opt levels
pass	a deep-nested constructor pattern dispatches a nullary variant through an erased multi-field newtype
pass	a deeper compound below a record MATCH field binds via the RecordField sub_path (§235)
pass	a deeper compound below a record field NESTED in a tuple match arm binds (§235 sub_path descent)
pass	a deeper nested call chain still folds in linear time near the inliner limit
pass	a deeper nested-record MATCH field binds via the RecordField sub_path (§235, match twin, tuple→record→tuple)
pass	a deeper positional compound below a record BINDING field binds via the RecordField sub_path (§235)
pass	a deeply nested constant expression compiles or declines without crashing
pass	a deeply nested expression is diagnosed by the parser, never crashes it
pass	a deeply nested literal in a tuple let-binder is refutable and rejected
pass	a deeply-nested generic-sum value type-checks and matches its outermost variant
pass	a deeply-nested match on an inline if-of-ctors scrutinee compiles bounded (fuse_match_into_if clone cap)
pass	a deeply-nested tuple map value sub-pattern binds through two tuple levels
pass	a def BODY that binds `ret` in an inner let still has its @ensures checked against the ACTUAL result — traps on violation
pass	a def BODY that itself binds `ret` in an inner let composes soundly with @ensures — lexical shadowing, not a collision
pass	a def body that is a bare N-suffixed BigInt literal round-trips (no spurious return-type hoist)
pass	a def named after a prelude type name is a plain rebind and compiles
pass	a def parameter may be a record pattern that destructures by field
pass	a def shadowing the prelude String type name is likewise a plain rebind
pass	a def with a mix of annotated and unannotated parameters binds and folds
pass	a def-bound perform composes with a recursive performing loop in one handle body
pass	a def-boundary conditional abort with PURE arguments folds and homes to its handler
pass	a def-boundary conditional abort with a FOREIGN op-result argument declines cleanly
pass	a def-returning-lambda partial application passes through a HOF parameter
pass	a default-float pragma leaves a bare integer literal at its Int64 default
pass	a default-float pragma naming a non-float integer type is rejected
pass	a default-float pragma naming a non-float rational type is rejected
pass	a default-float pragma naming an unbound type is rejected as unbound
pass	a default-float pragma with no type argument is a malformed directive
pass	a default-fraction pragma fixes a type but adds no conversion — no-promotion still holds
pass	a default-fraction pragma grounds a bare DECIMAL literal to its exact fraction
pass	a default-fraction pragma makes a bare literal exact — 1/3 not 0
pass	a default-fraction pragma naming a FLOAT type is rejected (the Float64 leg of the domain check)
pass	a default-fraction pragma naming a non-rational type is rejected
pass	a default-integer pragma fixes a type but adds no conversion — no-promotion still holds
pass	a default-integer pragma makes a bare literal take the declared integer type
pass	a default-integer pragma naming a non-integer type is rejected
pass	a default-integer pragma naming an unbound type is rejected as unbound, like an annotation
pass	a deferred resume-thunk STORED IN A SUM and match-extracted through a helper before apply folds
pass	a deferred resume-thunk filed by a non-recursive helper then popped folds
pass	a deferred resume-thunk stored in a MULTI-PAYLOAD pqueue entry and tuple-match-extracted folds
pass	a deferred-float Some-payload if branch takes its width from a sibling Float32 payload (constructor hoist)
pass	a deferred-float Some-payload match arm takes its width from a sibling Float32 payload (constructor sink)
pass	a definition named like a variant shadows the bare-variant constructor
pass	a definition with a non-name signature head has no name and is rejected
pass	a definition with a signature but no body is rejected
pass	a definition with an empty signature has no name and is rejected
pass	a definition with more than one body is rejected with a delete-the-surplus fix
pass	a degenerate empty let (no bindings and no body) carries no fix
pass	a delegated capability is not itself an export field
pass	a delegated effect performed in a closure passed to a HOF is reached and fires
pass	a delegated effect performed inside an intra-program handler
pass	a delegated effect reached only through an if-branch is granted and fires when the branch is taken
pass	a delegated host effect composes with the value-heap runtime
pass	a delegation naming an effect that is never reached is rejected as latent authority
pass	a dense all-literal Float32 match grounds its arms to f32 via the br_table path
pass	a dense if-equality chain dispatches every value including the default
pass	a dense if-equality chain dispatches on a let-bound value
pass	a dense many-arm match dispatches by value to a covered arm and to the default
pass	a dense zero-based jump-table match hits a covered arm
pass	a dense zero-based jump-table match routes an out-of-range scrutinee to the default
pass	a dependent size of zero binds an empty body and leaves the rest untouched
pass	a dependent size one past the remaining bytes overruns and falls through
pass	a dependent utf8 bin segment whose length exceeds the remaining bytes is a non-match
pass	a dependent-size bin-match payload read reclaims its slice across a loop (no live objects)
pass	a dependent-size body over a runtime-start slice re-bases its size arithmetic per call
pass	a dependent-size body re-matches under a second bin pattern
pass	a dependent-size body straddling a rope seam stitches, and an oversize count fails the match
pass	a dependent-size framing loop walks frames that straddle every rope seam
pass	a dependent-size segment binding exactly the remaining bytes leaves an empty rest
pass	a dependent-size segment binds exactly the number of bytes an earlier segment named
pass	a dependent-size segment that overruns the remaining bytes falls through
pass	a dependent-size utf8 segment decodes a length-prefixed string
pass	a dependent-size utf8 whose length prefix OVERRUNS the frame is a non-match, not a trap
pass	a depth-3 erased-and-boxed nested-sum match reads every level's discriminant at the right offset
pass	a depth-3 transitive arm-perform cascade threads the innermost state end to end
pass	a derived boolean compared to a literal composes into the complementary comparison
pass	a derived dimension composes across a def boundary through annotated Qty parameters
pass	a derived-range < 16 tautology keeps a trapping div operand
pass	a derived-range remainder keeps the modulo when the range is not strictly below the divisor
pass	a derived-unit type annotation in INPUT position round-trips and the param computes
pass	a destructure-let whose heap init is ALSO borrowed whole in the body reclaims the init ONCE (no double-free)
pass	a destructuring let over a runtime value binds its parts
pass	a destructuring record let over a runtime value binds its fields
pass	a diamond peer graph shares one provider across two middle peers
pass	a dictionary consumer called at two dictionaries is monomorphized per dictionary
pass	a different function argument through the same recursive higher-order function
pass	a different type witness (String) still checks its sibling arg against it
pass	a digit separator adjacent to a float's decimal point is a malformed literal
pass	a dimensionless quantity carries the group identity Unit.one
pass	a direct bare literal overflowing a narrow parameter width is rejected at check
pass	a direct record field projection const-folds
pass	a discarded do-statement that performs still advances the handler state
pass	a discarded do-statement whose value would overflow is elided, so its trap does not occur
pass	a discarded let binder _ compiles clean
pass	a discarded pure trapping item in a host do-body is elided beside a host call (dead-init ruling)
pass	a discarded trapping item in a do sequence is elided per the dead-init ruling
pass	a distinct-signature per-group call-g is a repeatable (borrow<t>) callback
pass	a distinct-signature per-group call-g is repeatable on one make-<name> handle
pass	a distinct-signature round-trip alongside a plain export — the Int64->Bool side runs
pass	a distinct-signature round-trip alongside a plain export — the Int64->Int64 side runs
pass	a distinct-signature round-trip alongside a plain export — the plain export runs
pass	a distinct-signature round-trip applies the Int64->Bool closure
pass	a distinct-signature round-trip applies the Int64->Int64 closure
pass	a distinct-signature round-trip's Bool closure on a nonzero input
pass	a distributed if-branch reading an enclosing parameter folds through the one-hole continuation
pass	a distributed if-branch that folds to an ill-typed arm composition declines
pass	a distributed match arm re-resolves its pattern binder inside the pushed handle
pass	a diverging expression unifies with an integer position
pass	a dividend provably below its divisor folds div to zero and rem to the dividend
pass	a dividend-below-divisor div fold keeps a trapping dividend's trap
pass	a division whose divisor folds to zero still traps
pass	a division-REACHED quantity equals the inverse-composed unit and cancels to Unit.one
pass	a do block whose last form is a declaration rather than a value form is rejected
pass	a do item OBSERVED by the final expression keeps its trap
pass	a do-def NOT in the perform argument stays in scope for a later reference
pass	a do-def passed to a performing helper stays in scope
pass	a do-def referenced twice WITHIN one resume operand compiles (the within-slot control)
pass	a do-def shadowing a LET binding rebinds and its init evaluates
pass	a do-def shadowing a function parameter is a well-defined shadow
pass	a do-def shadows a MATCH BINDER and a prior def correctly
pass	a do-def shared across BOTH resume slots stays in scope (the accumulator-arm shape)
pass	a do-def value in a handle body flows into a perform's argument and stays in scope
pass	a do-def value in a handle body flows into an ABORTIVE perform's argument and stays in scope
todo	a do-def-bound perform inside a recursive fn called under a handle declines cleanly (specializer floor, not a mangled-name CDZ0201)
pass	a do-discarded OUTER perform under an inner handle threads its state advance across the level
pass	a do-local def function references a sibling do-local def function by name
pass	a do-local def shadowing a parameter rebinds the name (does not unbind it)
pass	a do-local fn capturing a CONSTANT sibling local lowers (capture guard)
pass	a do-local fn capturing a runtime-computed sibling local survives the enclosing fn's inlining
pass	a do-local fn capturing a runtime-computed sibling via a LET binding survives inlining
pass	a do-local fn capturing the enclosing fn PARAMETER directly lowers (capture guard)
pass	a do-local function declaration is recursive
pass	a do-local generic function is instantiated per type
pass	a do-local value-def bound to a record is projected as a bare-name binding, not misread as a destructure
pass	a do-sequence of unit-returning performs runs each for effect, then yields the tail value
pass	a do-sequenced perform train under its OWN inner handle threads state
pass	a do-sequenced self-call preceding a perform folds via multi-value return
pass	a do-spine item abort ABANDONS its enclosing operator, not splicing the value (do-item abort-abandon)
pass	a doc wrapping a definition from outside names the in-definition placement
pass	a documented payload sum keeps its variant discriminants
pass	a documented value definition binding a record is projected by a sibling
pass	a dot-access of an absent field on a let-bound record is also CDZ0212
pass	a dot-access of an absent record field is CDZ0212, the Record.project twin
pass	a dotted nullary arm whose body nests a same-type sum match dispatches correctly
pass	a double fused match chain through a shared callee computes both calls
pass	a double same-dimension annotation preserves the original scale
pass	a double-negated condition unwinds to the original selection
pass	a double-negated float compare unwinds to the compare, preserving NaN-false
pass	a double-nominal narrow newtype (newtype of a newtype of UInt8) boxed in a list refines by value
pass	a doubled payload binder shares one read across a counter fold
pass	a doubling add computes in range and traps on overflow like the general add
pass	a doubly-call-returned narrow newtype spills correctly through two calls
pass	a doubly-nested strength-reduced multiply chains shift into shift
pass	a doubly-nested tuple map key containing a rope is found by its flat twin
pass	a dropped closure handle leaves no live objects after make + call
pass	a dup-aware let binding consumed in a nested-if then borrowed in the sibling arm reclaims correctly (no live objects)
pass	a dup-aware let binding consumed twice does not double-free the last-consumed slot ref (no live objects)
pass	a dup-aware let binding whose consuming use is a captured-then-inlined push reclaims its surviving ref (no live objects)
pass	a duplicate NESTED-CONSTRUCTOR arm is dead code but the program still runs
pass	a duplicate TUPLE-shaped arm is dead code but the program still runs
pass	a duplicate definition is rejected with a delete-the-redundant-def fix
pass	a duplicate effect operation declaration carries a delete fix
pass	a duplicate export clause for the same name is rejected
pass	a duplicate export of the entry is rejected
pass	a duplicate field in a variant's record-type payload is a type error
pass	a duplicate literal map key carries a delete fix
pass	a duplicate match arm is dead code but the program still runs
pass	a duplicate module declaration in one scope is rejected
pass	a duplicate name WITHIN one multi-name export clause is rejected with a delete fix
pass	a duplicate parameter name is rejected as a nonlinear binder with a rename fix
pass	a duplicate sum variant declaration carries a delete fix
pass	a duplicate type declaration is rejected with a delete fix
pass	a factorial that overflows the integer type traps through the recursion
pass	a factory RETURNS a closure capturing the def's SCALAR parameter
pass	a factory RETURNS a closure that captures a let-bound inner closure
pass	a factory-returned closure over its list parameter keeps the capture live across two applications
pass	a factory-returned closure that BOTH escapes into a list AND is directly applied at the same binder
pass	a failed multi-segment bin guard falls through and the NEXT arm re-decodes its own binders
pass	a failing guard falls through to a later arm
pass	a failing payload-binder guard falls to a later arm re-binding the payload
pass	a failing tuple guard falls to a later arm re-binding the elements
pass	a failure `?` preserves a COMPOUND error value through the short-circuit
pass	a failure `?` preserves a USER-SUM error value (a ctor + payload) through the short-circuit
pass	a failure `?` propagates through TWO nested fallible boundaries
pass	a failure `?` short-circuits BEFORE later computation runs
pass	a failure `?` short-circuits a RUNTIME error payload (static Err ctor, per-call value)
pass	a failure `?` short-circuits the enclosing LAMBDA, not the outer function
pass	a fall-through arm body performs and reads the post-scrutinee state
pass	a fallible Result peer op result crosses and its Err arm matches
pass	a fallible Result peer op result crosses and its Ok arm matches
pass	a fallible helper with a `?` called from INSIDE a handler ARM (success path)
pass	a fallible helper with a runtime-payload `?` is called from INSIDE a handle body
pass	a fallible parser threads a Result((value,cursor),String) through mutual recursion, short-circuiting on Err
pass	a false first operand falls through — the OR's second perform fires (same program)
pass	a false variant guard shields its arm's trapping body
pass	a family quantity displays scaled exactly to its reference (Rational)
pass	a family quantity displays scaled to its reference over Int64, truncating a non-whole ratio
pass	a far absent-field label in Record.without lists the available fields with no confident fix
pass	a far-miss misspelled export states the fault but offers no baseless suggestion or fix
pass	a far-miss record match pattern field carries no baseless suggestion or fix
pass	a far-miss segment kind keeps the plain unrecognized message
pass	a far-miss variant in a list-element pattern lists the closest matches with no fix
pass	a far-typo BARE variant pattern head is an unbound name with no confident suggestion
pass	a far-typo ctor over a single-variant newtype lists its sole variant
pass	a far-typo qualified variant pattern head lists the sum's variants and offers no confident fix
pass	a fault inside a guard condition surfaces — the condition is walked
pass	a field is projected from a record chosen across a call boundary by a runtime conditional
pass	a field is projected from a record returned across a call boundary
pass	a field is projected from a record returned by a function
pass	a field is projected from a record returned by a let-bound function
pass	a field is projected from a record returned by a nullary function
pass	a field is projected off a record bound through a match arm
pass	a field is projected off a record unwrapped from a result with expect
pass	a field is projected off a record unwrapped from an optional with expect
pass	a field projection off a nullary record whose field came from a RECURSIVE helper folds
pass	a field read follows the record's SORTED field order, not its written order
pass	a file-level (module-doc) header before an import is inert and round-trips
pass	a file-top default-fraction pragma does not leak into an imported module's literals
pass	a file-top default-fraction pragma fixes a type but adds no conversion — no-promotion still holds
pass	a file-top default-fraction pragma grounds a bare literal to its exact fraction
pass	a file-top default-integer pragma fixes a bare literal's type
pass	a final bytes segment binds the remainder after the sized segments
pass	a final splice in a quote pattern binds the remaining elements as a list
pass	a final unquote-splice binds the remaining elements as a list
pass	a finite float is less than Float64.Infinity
pass	a finite float is unequal to a not-a-number value regardless of operand order
pass	a fitting Float32 Qty magnitude computes at the narrow inner width
pass	a fitting Float32 branch literal under a narrow-float annotation runs (no over-rejection)
pass	a fitting Float32 const-folded conditional runs (no over-rejection)
pass	a fitting Float32 literal in a runtime MATCH arm computes
pass	a fitting Float32 literal in a runtime if branch computes
pass	a fitting Float32 record field passed through a typed parameter computes
pass	a fitting bare literal to a narrow parameter runs (no over-rejection)
pass	a fitting contextual float literal computes through the same arith grounding
pass	a fitting cross-function match projection computes at the narrow width
pass	a fitting float literal grounded to Float32 through an arith spine runs (no over-rejection)
pass	a fitting handler arm resume value computes under a narrow annotation
pass	a fitting payload projected through Option.expect computes
pass	a fitting payload projected through Option.expect under a narrow annotation runs (no over-rejection)
pass	a fitting recursive annotated result computes at the narrow width
pass	a fitting runtime-if branch literal under a narrow annotation runs (no over-rejection)
pass	a fitting runtime-match arm literal under a narrow annotation runs (no over-rejection)
pass	a five-arm match consumed in non-tail position yields into the enclosing expression
pass	a fixed parameter before a list-rest parameter binds positionally
pass	a fixed-arity list binding pattern is refutable and rejected
pass	a fixed-arity list destructuring binding param is refutable and rejected CDZ0210
pass	a fixed-arity nested-list element binds when the inner length matches exactly
pass	a fixed-arity nested-list element falls through on a longer inner list
pass	a fixed-arity-only list match is non-exhaustive — a finite set of lengths cannot cover every list
pass	a fixed-only non-exhaustive list match adds a covering wildcard arm
pass	a fixed-shape Tuple ARG with a Bytes RESULT crosses the direct-call boundary
pass	a fixed-shape Tuple ARG with a List RESULT crosses the direct-call boundary
pass	a fixed-shape Tuple ARG with a Map RESULT crosses the direct-call boundary
pass	a fixed-shape Tuple ARG with a RECORD RESULT crosses the direct-call boundary
pass	a fixed-shape Tuple ARG with a STRING RESULT crosses the direct-call boundary
pass	a fixed-shape Tuple ARG with a SUM (Option) RESULT crosses the direct-call boundary
pass	a fixed-shape Tuple ARG with a Tuple RESULT crosses the direct-call boundary
pass	a fixed-shape scalar RECORD closure ARG crosses the DIRECT-CALL boundary
pass	a fixed-shape scalar Tuple closure ARG crosses the DIRECT-CALL boundary (host supplies the tuple)
pass	a fixed-width integer segment of a non-byte-aligned width is rejected
pass	a fixpoint loop that threads a growing list accumulator returns that list
pass	a fixpoint that re-seeds a fresh list each round via a helper returns a list
pass	a flat String inserted into an empty set boxes by its own type (not box-int) and adds no leak
pass	a flat multi-param lambda stored in a tuple is applied through curried syntax
pass	a flat multi-shot arm with two performs on a strict spine folds the cross-product
pass	a flat single-scalar-field record EXPORT result crosses (returned directly, not by pointer)
pass	a flat string map key is found by a runtime rope twin
pass	a flattened nested-if combined condition short-circuits a trapping second condition
pass	a flattened nested-if shared arm preserves trap shielding (reached only when a condition selects it)
pass	a float annotation reaches the 32-bit width
pass	a float annotation to a wider precision offers an of-conversion wrap fix
pass	a float at f64::MAX renders its full 309-digit expansion (large-significand codec)
pass	a float divide by zero takes the DIVIDEND's sign into the infinity
pass	a float in integer-width position is rejected
pass	a float leaf and a Bytes leaf compare together in one compound
pass	a float literal compared against a Float32 param computes at the narrow width
pass	a float literal grounded to Float32 through an arith spine that overflows f32 is rejected
pass	a float precision mismatch under an operator names floats and offers an of-conversion wrap fix
pass	a float width from runtime data is rejected, like an integer width
pass	a float width outside the admitted set is rejected
pass	a float-carrying SUM as a Set key dedupes by payload and probes by content
pass	a float-carrying sum as a MAP key is found by a reconstructed equal key
pass	a float-field RECORD as a SET element dedups by content including the float leaf
pass	a float-first arithmetic mix retypes the trailing integer literal up to a float
pass	a float-form literal with an N (BigInt) suffix explains the slip and suggests the R suffix
pass	a float-form literal with the N (BigInt) suffix is rejected with a swap-to-R fix
pass	a float-var-first comparison against an int literal retypes the literal up (mirror order)
pass	a floating-point literal
pass	a flow refinement folds a compare over a no-overflow arith operand, but the trap survives when overflow is possible
pass	a flow-refined match scrutinee dispatches with the refinement-dead arm elided
pass	a fn missing its body offers to add a trap-placeholder body
pass	a fold composes through a nested arithmetic tree
pass	a fold of unit fractions sums EXACTLY to one - the harmonic identity float cannot hold
pass	a fold over Map.to-list REBUILDS a filtered map at depth (the select-rebuild idiom)
pass	a fold over Map.to-list rebuilds a TRANSFORMED map — the map-over-values idiom
pass	a fold walks 40 keys with a FRESH lookup per iteration (the map as a loop-carried borrow)
pass	a fold whose accumulator applies each list closure IN SEQUENCE (apply-all chain)
pass	a folded recursive-newtype ANNOTATION unifies with the unfolded value type
pass	a forced-choice wrap into the only fitting Result arm offers that arm
pass	a foreign-performing op ARGUMENT bound to a multiply-used arm param runs the effect exactly once
pass	a four-level nested-constructor pattern with a generic user sum in the middle matches
pass	a frame body ACCUMULATED by a fold of per-element bin constructions round-trips
pass	a fresh-id supply threads state across two sibling recursive calls in one arm
pass	a fresh-id walk over a THREE-constructor sum threads state across mixed-arity arms
pass	a full MERGE SORT (alternating split + ordered merge) sorts a runtime list, duplicates kept
pass	a full reducer-step-shaped guest writes every field of the step and runs (via an imposed WIT world)
pass	a full-range signed division keeps its MIN over -1 overflow guard and traps
pass	a full-string String.slice byte-len equals the whole string's byte-len
pass	a full-string String.slice recovers the whole string's length by both measures
pass	a full-width UInt64 comparison over a HIGH-BIT operand is unsigned
pass	a full-width add overflow names the integer overflow trap reason
pass	a full-width const-multiplier bound check traps at the exact boundary
pass	a fully-constant construction spread folds to a single constant list
pass	a fully-constant record construction spread folds to one record
pass	a fully-constant set construction spread folds and dedups
pass	a fully-constant tuple construction spread folds to one tuple
pass	a function KEY in a NATIVE MAP LITERAL is rejected (CDZ0216 — the #map sibling of the native #set bypass)
pass	a function applied to an argument
pass	a function argument is applied through a recursive higher-order function
pass	a function argument of the wrong ARITY names the argument-count delta
pass	a function argument with the wrong RESULT type names the result axis
pass	a function binding the same parameter name twice is rejected
pass	a function bound to a name and then applied
pass	a function chosen by a runtime condition is applied (false branch)
pass	a function chosen by a runtime condition is applied (true branch)
pass	a function declaration in a do block calls an earlier sibling declaration
pass	a function declaration in a do block is callable by the following forms
pass	a function definition may carry a leading doc, ignored for the computation
pass	a function deriving a velocity from a distance and a time
pass	a function in a NATIVE SET LITERAL is rejected (CDZ0216 — the native-literal face must not bypass the prim-app check)
pass	a function is passed as an argument (higher-order)
pass	a function is returned as a result
pass	a function open over a record's extra fields accepts any record with the used field
pass	a function returning None or a nested Some infers its compound result shape
pass	a function returning a boolean predicate result
pass	a function returns a heap sub-node selected by a match arm
pass	a function stored as a runtime Map KEY is rejected (CDZ0216 — a function is not equatable/orderable)
pass	a function stored as a runtime SET element is rejected (CDZ0216 — the Set face of the Map-key reject)
pass	a function stored in a record field is called after extraction
pass	a function stored in a record field of a sum payload is called after a match
pass	a function stored in a tuple element is called after extraction
pass	a function takes a boolean parameter and branches on it
pass	a function using only its first parameter accepts all its well-typed arguments
pass	a function whose body always diverges has result type Never
pass	a function whose name is capitalized is called, not treated as a constructor
pass	a function with an unused parameter compiles and runs but the build surfaces a CDZ0306 unused-parameter warning
pass	a function with two SAME-NAMED parameters is a non-linear binding rejection
pass	a function's reflected CODOMAIN is body-solved, distinguishing an Int64 result from a Bool result
pass	a function's reflected domain matches an equivalent explicitly-annotated signature
pass	a function's return type ascription agreeing with the body is transparent
pass	a function's return type contradicting the body is rejected
pass	a function-typed parameter annotation is not silently discarded in the body
pass	a function-typed parameter annotation's result type is checked against the argument
pass	a function-typed parameter with a matching Bool-returning argument is accepted
pass	a further over-ceiling UNSIGNED width well past 64 is rejected naming the written width
pass	a fused match arm reading a heap binder twice is UAF-free and reclaim-neutral vs the non-fused shape
pass	a general recursive-sum recursion (Nil|Cons count) reads the right payload through the fold
pass	a generated LIST value is reproducible from its seed AND distinct seeds give distinct lists (runtime list = walks the spine)
pass	a generated MIXED payload+nullary SUM value is reproducible from its seed (a 3-variant sum with a bare-name nullary variant)
pass	a generated RECORD value is reproducible from its seed (compound = walks the record fields)
pass	a generated Rational's equality is INDEPENDENT of sign placement (n/d = -n/-d)
pass	a generated SUM value is reproducible from its seed (compound = walks the variant tag and its payload)
pass	a generated insert/remove Set workload agrees with a BITMASK model at every step's end
pass	a generated list reverses twice to itself — an involution property over generated content
pass	a generated map workload agrees with a linear-scan model at EVERY key of the domain
pass	a generated value with a BIGINT leaf is reproducible (compound = walks the bignum)
pass	a generated value with a FLOAT leaf is reproducible (compound = over the canonical float byte form)
pass	a generated value with a SYMBOL leaf is reproducible (compound = walks the interned-name byte leaf)
pass	a generic bare-parameter inline lambda compiles clean (no spurious binding fault at depth)
pass	a generic def rewrap that RETURNS the user generic is monomorphized at Int64 and Bool
pass	a generic def unwrap is monomorphized at Bool and Int64 element types in one program
pass	a generic definition takes the type as a type-valued parameter
pass	a generic fold instantiated at an Int64 AND a String accumulator in one program
pass	a generic newtype at two DIFFERENT instantiations stays distinct
pass	a generic newtype compared to its bare instantiated inner also crosses the nominal boundary
pass	a generic newtype instantiated at a FUNCTION type carries a closure through erasure
pass	a generic newtype with a structural-record payload mentioning the type parameter constructs
pass	a generic pair helper returns a mixed-type tuple projected through a match
pass	a generic pair-swap crosses a scalar and a heap payload between positions
pass	a generic recursive HOF taking a closure is monomorphized at three distinct closure types
pass	a generic recursive HOF taking a closure is monomorphized at two distinct closure types
pass	a generic recursive sum references itself bare and carries its own type parameter
pass	a generic recursive sum with a bare self-reference nested in a list parameter
pass	a generic result flows into a second generic call
pass	a generic same-name constructor inlined into a caller resolves to the constructor not the type
pass	a generic sum NESTS ITSELF at a different instantiation and destructures through both layers
pass	a generic sum instantiated at a HEAP payload ((Box (List Int64))) crosses resume
pass	a generic sum type constructor applied to a value is rejected
pass	a generic sum whose payload NESTS the parameter escapes with the instantiated inner type
pass	a generic sum with TWO type parameters instantiates each independently
pass	a generic sum with a type parameter inside a record payload constructs and projects
pass	a generic sum with a type parameter inside a tuple payload constructs and destructures
pass	a generic sum with a type parameter nested inside another sum payload matches through both
pass	a generic tuple-wrapper referenced mid-solve in a recursive group then at a second type
pass	a generic type parameter inside a List payload resolves and the value computes
pass	a generic type parameter inside a Tuple payload threads and the value computes
pass	a generic type parameter used twice in one variant unifies both fields
pass	a generic unwrap helper consumes a fallible Bytes.at result
pass	a generic user sum holds a CLOSURE payload at one instantiation and a scalar at another
pass	a genuine Int64 VALUE beside a BigInt still rejects (only a bare literal grounds, no promotion)
pass	a genuine effect-op typo (no shadowing) keeps the ordinary did-you-mean
pass	a genuine narrowing wrap still masks to the target width
pass	a genuine top-level stray resume with no enclosing handler arm is rejected
pass	a genuine type mismatch in an annotation still rejects (no over-rejection)
pass	a genuine value misused as a parameter type keeps the is-a-value message (not the constructor one)
pass	a genuinely unbound plain name reports its own plain unbound, not the eval teaching message
pass	a genuinely unknown export name keeps the plain names-no-definition message
pass	a genuinely unrecognized bin segment kind keeps the generic message, not the width guidance
pass	a genuinely-escaping continuation (deferred resume-thunk) resumes cross-activation at the advanced clock
pass	a genuinely-missing field in a record ARGUMENT carries an add (insert) fix, not a rename
pass	a genuinely-missing nested field gets no rename fix
pass	a genuinely-narrow op with a wrapped-down MATCH-operand yields the exact value
pass	a genuinely-narrow op with a wrapped-down if-operand still traps on overflow
pass	a genuinely-narrow op with a wrapped-down if-operand yields the exact value when in range
pass	a genuinely-runtime MULTIPLY on an unusual signed width traps on overflow rather than wrapping to storage
pass	a genuinely-runtime NARROW-width negation returns the negation and traps at the narrow minimum
pass	a genuinely-runtime NARROW-width unsigned overflow traps on the emitted operation
pass	a genuinely-runtime addition overflow traps on the emitted operation
pass	a genuinely-runtime in-range shift count works
pass	a genuinely-runtime left-shift count beyond the bit width traps rather than masking to a small count
pass	a genuinely-runtime left-shift count equal to the bit width traps rather than masking
pass	a genuinely-runtime multiplication overflow traps on the emitted operation
pass	a genuinely-runtime negation of the minimum integer traps
pass	a genuinely-runtime negative right-shift count traps rather than masking
pass	a genuinely-runtime shift count that is a multiple of 2^32 traps rather than truncating to zero
pass	a genuinely-runtime subtraction underflow at the minimum traps
pass	a genuinely-runtime symbol orders by content-lexicographic byte order
pass	a genuinely-runtime symbol three-way compare agrees with the boolean ordering
pass	a genuinely-unequal Float32 constant equality const-folds false (adv-61 over-fire control)
pass	a grafted template is structurally equal to the directly-quoted tree
pass	a guard binder resolves over a COMPUTED scrutinee inside a helper fn
pass	a guard binder resolves over a raw param scrutinee inside a helper fn
pass	a guard combines the payload binder with the rest binder
pass	a guard compares two distinct sum payloads from one tuple
pass	a guard composes with a string-literal arm
pass	a guard condition applies a let-bound closure
pass	a guard condition reads a heap list bound outside the match
pass	a guard destructures a perform-result TUPLE and its condition reads both binders
pass	a guard expression calling a recursive helper inside a self-tail-loop match compiles
pass	a guard on a MULTI-payload ctor list element reads both payload binders
pass	a guard on a ctor list element reads the constructor's payload binder
pass	a guard on a literal-element list pattern reads an enclosing let binding through inlining
pass	a guard on a nested-list-in-list element reads the inner list's binder
pass	a guard on a tuple arm reads a NESTED ctor payload binder plus a sibling element binder
pass	a guard on a tuple arm reads the tuple's element binders
pass	a guard over a variant pattern gates on the payload
pass	a guard reads a binding from the enclosing scope, not only its pattern's
pass	a guard reads payload binders from two ctor list elements
pass	a guard reads the payload binder and an enclosing parameter together
pass	a guard runs a NESTED MATCH on the quote-bound subtree — value-dependent rewriting
pass	a guard sees the names its pattern binds and arms are tried in order
pass	a guard whose CONDITION itself performs (pure scrutinee) folds
pass	a guard-elided FLOW-REFINED add computes identically on both backends
pass	a guard-elided masked add at a NARROW width computes identically on both backends
pass	a guard-elided masked add computes inline over runtime operands
pass	a guard-elided masked left shift computes identically on both backends
pass	a guard-elided masked multiply computes inline over runtime operands
pass	a guard-elided masked subtract computes inline over runtime operands
pass	a guard-elided signed division by a provably-non-neg-one divisor computes identically on both backends
pass	a guard-nested BARE pattern on a withheld constructor is also rejected (encapsulation soundness, guard-desugar route)
pass	a guard-nested withheld-constructor pattern over an abstract value is rejected CDZ0214
pass	a guarded arm among same-constructor match arms keeps first-match-wins
pass	a guarded arm is selected when its guard holds
pass	a guarded arm over a masked scrutinee stays gated by its guard when the probe is in range
pass	a guarded bin arm with a CONST leading segment reads a binder from a LATER segment
pass	a guarded bin-match arm over a CONSTANT scrutinee folds the guard and selects the arm
pass	a guarded bin-match arm over a CONSTANT scrutinee whose guard fails falls to the next arm
pass	a guarded bin-match arm reads its decoded binder and falls through when the guard fails
pass	a guarded binder referenced in NEITHER the guard cond nor the body still warns unused
pass	a guarded binder used in the guard COND (not the body) is used — no unused-match-binding warning
pass	a guarded byte-string-literal arm gates a runtime Bytes match on a condition
pass	a guarded list-match arm body reads its element binder
pass	a guarded list-match arm over a RUNTIME list scrutinee gates on its decoded head
pass	a guarded list-match arm whose guard fails falls through to the catch-all
pass	a guarded list-splat arm returning an Option beside a tail-recursive arm compiles to valid wasm
pass	a guarded map arm over a PARTIAL-CONST map resolves its value binder in both the guard and the body
pass	a guarded map arm over a partial-const map resolves its value binder in guard and body
pass	a guarded map-match arm over a CONSTANT map folds the guard and selects the arm
pass	a guarded map-match arm over a CONSTANT map whose guard fails falls to the next arm
pass	a guarded map-match arm reads its value binder in the guard and falls through when it fails
pass	a guarded match on a perform-result scrutinee with a PERFORMING fallback arm folds
pass	a guarded match over a runtime scrutinee whose guard fails takes the wildcard
pass	a guarded match over a runtime scrutinee with constant arms computes correctly
pass	a guarded out-of-bounds indexed read is not hoisted by the sharing
pass	a guarded pattern with a surplus element offers a delete-the-surplus fix
pass	a guarded record match arm gates the fields it binds and falls through on a failing guard
pass	a guarded variant arm falls through when the guard fails
pass	a guarded wildcard arm falls through to a tail-recursive call
pass	a handle as an exported fn's DIRECT body resumes the caller's runtime-param seed
pass	a handle body reads an enclosing function parameter
pass	a handle body reads an enclosing function parameter beside a resuming perform
pass	a handle head that is a prelude scalar type names the head must be an effect
pass	a handle head that is a prelude sum type names the head must be an effect
pass	a handle head that is a type names the head must be an effect
pass	a handle head that is a value def names the head must be an effect
pass	a handle held in a helper seeded by the caller's runtime param resumes the seed
pass	a handle seeded from a runtime caller argument advances from that seed
pass	a handle whose body NEVER performs is exactly its body (zero dispatches)
pass	a handle whose body is a closure is applyable (the handle result IS that closure)
pass	a handle whose head names a value rather than an effect is rejected
pass	a handle's RESULT seeds the NEXT handle of the same effect — explicit state handoff between instances
pass	a handle's TUPLE value pairing a scalar with a built list escapes and is destructured
pass	a handler ARM enumerates a 60-key trie op argument and resumes its fold
pass	a handler ARM gates its answer on a CAPTURED Set from the enclosing scope
pass	a handler SEEDED with a 40-key trie reads it across resumes
pass	a handler STATE that is a recursive sum GROWS one constructor per operation
pass	a handler accumulates into a list and a read-out operation reads it back
pass	a handler arm RECURSES through a named helper before resuming
pass	a handler arm applies a closure capturing a heap map and the map outlives the handle
pass	a handler arm binding too few parameters for a genuine one-param op is rejected
pass	a handler arm binding too many parameters for a unit op is rejected
pass	a handler arm capturing an enclosing fn param folds under a multi-arm nested handler
pass	a handler arm computes its NEXT-STATE with a recursive pure helper
pass	a handler arm computes its RESUME VALUE with a deeply RECURSIVE pure helper
pass	a handler arm consumes its resume value through an effect-free helper call
pass	a handler arm for an operation the effect does not declare is rejected
pass	a handler arm forwarding an effect its enclosing scope does not hold is rejected
pass	a handler arm forwards an effect its enclosing scope DOES hold and runs
pass	a handler arm interposes on another intra-program effect and forwards
pass	a handler arm naming a later-effect op explains the shadowing on its CDZ0403
pass	a handler arm resumes with a CLOSURE (in a tuple) capturing the op param and state
pass	a handler arm resuming per if-branch over the op arg folds
pass	a handler arm that never resumes aborts and the handle yields the arm's own value
pass	a handler arm that resumes NON-tail folds a perform in a match arm body by handler distribution
pass	a handler arm that resumes NON-tail folds a perform in a short-circuit connective right operand
pass	a handler arm that resumes NON-tail folds a perform in an if branch by handler distribution
pass	a handler arm that resumes NON-tail folds through a PURE one-hole continuation
pass	a handler arm that resumes NON-tail folds through a pure continuation containing an effect-free call
pass	a handler arm that resumes NON-tail folds when the perform is in a let init
pass	a handler arm that resumes NON-tail folds when the perform is in a match scrutinee
pass	a handler arm that resumes NON-tail folds when the perform is in an and lhs
pass	a handler arm that resumes NON-tail folds when the perform is in an if condition
pass	a handler arm that resumes NON-tail folds when the perform is the whole body
pass	a handler arm with two lookup-matches and a computed perform key emits valid wasm (checked-arith scratch slot-width partition)
pass	a handler distributes into a match on a pure scrutinee whose selected arm performs
pass	a handler distributes into an if whose BOTH branches perform
pass	a handler distributes into an if whose ELSE branch performs
pass	a handler folds a counter across the operations it discharges
pass	a handler mixing arms of two different effects is rejected
pass	a handler resumes its continuation at most once by default
pass	a handler state GROWS a list across 100 dispatches — the accumulated spine reads back intact
pass	a handler state SHRINKS per dispatch — Map.remove down to empty across resume cycles
pass	a handler state of TWO tries (tuple) updates each side independently across resumes
pass	a handler that does not discharge every operation of its effect is rejected
pass	a handler threads a MAP as its state — a key-value store deduping keys across performs
pass	a handler threads a SET as its state — the seen-set idiom, deduping across performs
pass	a handler threads a TUPLE of two heaps as state with different ops touching different halves
pass	a handler whose STATE is a RECORD combining a scalar counter and a heap LIST field
pass	a handler whose STATE is a TUPLE reads both fields and rebuilds the pair per performance
pass	a handler whose STATE is a sum destructures it in the arm
pass	a handler's heap STATE grows to a 40-key trie across resumes and enumerates at the end
pass	a hash-prefixed atom quotes to an Ast.Name carrying the hash in its text
pass	a hash-prefixed name is distinct from the bare name without the hash
pass	a head typo nearer to a real def suggests the def over a grammar keyword
pass	a header of packed nibbles and a length-prefixed body round-trips
pass	a heap Map crosses the module boundary whole and the importer's extension stays local
pass	a heap arg threaded UNCHANGED to a self-recursive call while a sibling arg consumes it is retained
pass	a heap capture persists across two applications of a curried closure
pass	a heap list conditionally escaping into a Some in one branch is still usable after the join
pass	a heap list consumed in only one if-branch stays live for a use after the if
pass	a heap result of effect A pipes directly into effect B's argument — cross-effect heap flow
pass	a heap-inner (Rational) quantity crosses to and from a peer as a shared runtime handle
pass	a heap-typed EXPORTED-entry param + a reachable RECURSIVE fn emits VALID wasm (the def-call index survives the entry's lift-op imports)
pass	a helper RETURNS a derived-dimension (m squared) quantity and same-dimension results add
pass	a helper called TWICE threads its first write so the second call HITS the memoized value
pass	a helper decodes bytes to a string and consumes the fallible result
pass	a helper projects a record parameter constrained by its argument
pass	a helper projects a tuple parameter constrained by its argument
pass	a helper returning a partial constructor is over-applied to complete the variant
pass	a helper returns the shorter of two borrowed ropes and both caller handles stay live
pass	a helper sums two fields of a record parameter
pass	a heterogeneous TUPLE as op ARGUMENT — the arm destructures both components
pass	a heterogeneous TUPLE op result (String, Int64) crosses resume and destructures
pass	a heterogeneous native #map literal NAMES the two clashing value types (diagnostic quality)
pass	a hex bare literal that overflows signed Int64 is a malformed literal
pass	a hex code-point char spelling names the same scalar as the plain char
pass	a hexadecimal integer literal
pass	a hexadecimal literal is case-insensitive
pass	a hexadecimal literal past Int64.max is a malformed literal, not a bit pattern
pass	a hexadecimal literal with digit separators
pass	a high UInt64 literal operand takes UInt64 from context, not Int64
pass	a high-bit scrutinee misses an offset jump table whose bias cancels its low bits
pass	a high-power product cancels exactly to dimensionless (meter^100 * meter^-100)
pass	a higher-order closure applied to a GUEST-produced closure arg (should-work)
pass	a higher-order function applies a callback that matches its own sum argument
pass	a higher-order left fold applies its combiner, not a fixed operator
pass	a higher-order left fold over a built-in list with an annotated callback
pass	a higher-order left fold over a built-in list with an annotated fn parameter
pass	a hoisted division traps by the selected divisor only
pass	a hoisted trapping invariant loop-bound still traps when it overflows, even at zero iterations
pass	a homogeneous distinct-key name-alias (map …) literal is accepted and compares equal
pass	a homogeneous nested Option distinguishes its outer and inner None
pass	a host call SANDWICHED between two in-program dispatches — state survives the boundary crossing
pass	a host call's response is an ordinary value that feeds a LATER host call
pass	a host delegating a value definition rather than an effect is rejected
pass	a host delegating the same effect twice is rejected
pass	a host delegation of an effect the body never reaches is latent authority and is rejected
pass	a host effect with a non-kebab NAME crosses under a normalized interface extern name
pass	a host effect with a non-kebab OPERATION name crosses under a normalized func extern name
pass	a host op interleaving runtime Bytes and scalar args keeps regions and slots distinct
pass	a host op mixing a const String and a runtime Bytes arg routes each to its own path
pass	a host op result escapes as a single-byte Bytes resource
pass	a host op returning Bool crosses the boundary and drives a branch
pass	a host op whose result is a QUANTITY crosses the boundary as its inner scalar (unit erased)
pass	a host op with a Bytes arg AND a scalar arg crosses both parameters (list<u8> beside a scalar)
pass	a host op with a String parameter composes with the value-heap runtime
pass	a host op with three runtime Bytes args marshals each to a disjoint region
pass	a host op with two runtime Bytes args marshals each to a disjoint region
pass	a host response SIZES a guest-built collection which is then folded and keyed
pass	a host response captured BEFORE a multi-shot region is shared by both branches — one host call
pass	a host result captured by closures in a NESTED tuple fires the host op once (adv-62 nested face)
pass	a host result captured by two closures stored in a RECORD fires the host op once (adv-62b)
pass	a host with too many operands is rejected with a delete-the-surplus fix, no consequent no-home error
pass	a host-block scrutinee folding to a multi-arm sum switch fires the host op once (adv-62 switch face)
pass	a host-called closure applied to a different argument tracks the input
pass	a host-called closure capturing a HEAP LIST indexes it at call dispatch
pass	a host-called closure with a different body dispatches the right code
pass	a host-crossing closure captures a CHAMP map and set and reads both per call
pass	a host-delegated result SEEDS an in-program handler's initial state
pass	a host-op argument COMPUTED by guest arithmetic crosses the boundary evaluated
pass	a kept let-binding carries its masked initializer's range and the guard-elided doubling computes correctly (value parity)
pass	a kept let-binding's masked range lets a guard-elided multiply compute correctly (value parity)
pass	a kernel may deliberately export its rule as a first-class value
pass	a kernel rule using α-equivalence accepts an α-variant premise a structural equality would reject
pass	a key-directed pattern matches a RUNTIME map by looking up its keys
pass	a keyed lookup of an updated map is not shared with the original's
pass	a keyed map binding param rejects the same in the name-alias (map …) spelling
pass	a keyed map destructuring binding param is refutable and rejected CDZ0210
pass	a keyed map pattern matches a SUPERSET map, binding the present key and ignoring the rest
pass	a known generic effect operation type does not false-positive as an unknown type
pass	a known-constructor match selects the NON-first arm, eliding the other arms' bodies
pass	a known-type let-binder annotation compiles and the binding reads back
pass	a lambda applied in the scope of the binding it captures observes that binding
pass	a lambda applying its forwarded fn param TWICE runs through a recursive HOF
pass	a lambda captures an enclosing function parameter and is applied in its body
pass	a lambda forwarding to a substituted fn param runs through a recursive HOF
pass	a lambda parameter shadows the outer constant for its body
pass	a lambda's return type ascription agreeing with the body is transparent
pass	a large branch literal with NO narrow context stays Int64 and runs (the check fires only under narrowing)
pass	a large match-arm literal with NO narrow context stays Int64 and runs
pass	a large multiply-called definition is emitted once by the cost heuristic
pass	a large runtime List folds every element across the RRB spine and indexes at depth
pass	a large runtime Map builds a deep CHAMP trie, looks up every key, shrinks to empty and regrows
pass	a large value in a NON-projected tuple slot does not trip the projection width check
pass	a large whole float crosses the boundary preserving its full value
pass	a large whole-valued float renders its full value, not an integer saturation
pass	a large-magnitude float renders its full expansion as a LIST element
pass	a large-magnitude float renders its full expansion as a TUPLE element (compound matches scalar)
pass	a large-magnitude float renders its full expansion as an OPTION (sum) payload
pass	a large-magnitude float whose shortest form differs from its exact value renders the full decimal expansion
pass	a later declaration in a do block sees an earlier one
pass	a later let binding sees an earlier one in the same let
pass	a later let binding sees an earlier pattern's binders
pass	a later let binding sees an earlier record pattern's field binders
pass	a later let-binding abort preserves an EARLIER binding's committed advance (multi-binding prefix)
pass	a le pattern segment reads a fixed-width integer little-endian
pass	a leading literal bit-field tag that misses falls through to the catch-all
pass	a leading nested-list element binds the inner head on a non-empty inner list
pass	a leading nested-list element dispatches on the inner list's length
pass	a leading nested-list element with an empty inner list falls through the inner-length guard
pass	a leading-element list rest binding param is refutable (does not match empty) and rejected CDZ0210
pass	a leading-element list rest pattern in a def parameter is refutable and rejected
pass	a leading-element list rest pattern in a let binder is refutable and rejected
pass	a leading-rest arm matches an exactly-minimum list with an empty rest
pass	a left shift by a floating-point count does not silently promote
pass	a left shift by the bit width or more traps rather than wrapping
pass	a left shift of a float by a float count rejects on the value operand
pass	a left shift overflowing a SECOND unusual width (Int 12) is also rejected CDZ0304
pass	a left shift overflowing an unusual signed width is rejected CDZ0304
pass	a left shift that overflows Int64 traps like multiplication
pass	a legitimate wildcard match arm compiles and selects
pass	a length-prefixed frame built FRESH from a runtime payload round-trips through its own parser
pass	a length-prefixed frame is built from a size segment and a bytes segment
pass	a let binder may be a record pattern that destructures by field
pass	a let binder may be a single-variant-sum pattern that destructures the payload
pass	a let binder may be a tuple pattern that destructures the value
pass	a let binder shadowing a parameter rebinds within its scope (same type)
pass	a let binder shadowing a parameter with a DIFFERENT type rebinds correctly
pass	a let binding is in scope in its body
pass	a let binding shadows a built-in constructor name in application-head position
pass	a let binding shadows a built-in type-module name in value position
pass	a let binding shadows the record constructor in application-head position
pass	a let binding shadows the tuple constructor in application-head position
pass	a let binding whose value references a parameter compiles under a call
pass	a let binding with no value expression is rejected, not a crash
pass	a let body of unit yields unit
pass	a let missing its body offers to add a trap-placeholder body
pass	a let record pattern destructures a helper CALL's runtime record result
pass	a let shadowing a parameter whose initializer references that parameter computes
pass	a let shadowing a parameter with a differently-typed value is not an invalid component
pass	a let shadowing a parameter with a same-typed value runs, not miscompiles
pass	a let tuple pattern destructures a helper CALL's runtime tuple result
pass	a let with more than one body is rejected with a delete-the-surplus fix
pass	a let with the added trap-placeholder body compiles and runs
pass	a let-bound Ast splices into a template
pass	a let-bound Bytes.compact result read twice (eq then order-compare) computes without an OOB fault
pass	a let-bound Bytes.concat result is read three ways (len, index, order-compare)
pass	a let-bound CONSTANT handle seed stays byte-identical and folds to the constant
pass	a let-bound Rational borrowed by two reads is dropped once and leaves no live heap objects
pass	a let-bound handle whose seed is an EXPRESSION over the caller's runtime arg folds
pass	a let-bound handle with a runtime seed composes with arithmetic after the let
pass	a let-bound host result captured by two escaping closures fires the host op once (adv-62)
pass	a let-bound if-selected partial constructor is applied and reclaimed once (borrowed-operand path)
pass	a let-bound lambda passed to a returned-closure factory
pass	a let-bound lambda whose body performs is applied in the handle body
pass	a let-bound partial application of a runtime closure builds a residual closure and completes
pass	a let-bound peer compound is read twice (both live) then reclaimed
pass	a let-bound record produced by an if is read at both fields
pass	a let-bound record produced by an if, two fields read, compiles and runs
pass	a let-bound record with an extra field capturing a let-local is projected and applied
pass	a let-bound record's projected capturing closure applied twice folds each application
pass	a let-bound returned closure applied twice folds each application independently
pass	a let-bound runtime tuple inside a parameterized function projects element 0
pass	a let-bound runtime-arg handle seed with the state used three times in the arm folds
pass	a let-bound runtime-arg handle seed with two performs threads and advances the seed
pass	a let-bound runtime-if list is length-measured with valid wasm
pass	a let-bound runtime-if map is looked up with valid wasm
pass	a let-bound self-call preceding an out-state-observing perform folds via multi-value return
pass	a let-bound set shared across a consuming Set.union and a later read is dup'd and reclaimed once (no live objects)
pass	a let-bound tuple produced by an if is projected at both indices
pass	a let-bound tuple produced by an if, projected at two indices, compiles and runs
pass	a let-bound value in a handle body flows into a perform's argument (the always-worked twin)
pass	a let-bound value inside a function taking a parameter compiles and runs
pass	a let-bound variable derived from a runtime parameter passed as a call argument
pass	a let-bound variable is in scope inside a boolean connective operand
pass	a let-bound variable passed as a function-call argument resolves at the call site
pass	a let-bound variable passed to a let-bound lambda resolves at the call site
pass	a let-shaped control-flow operand of a narrow op is width-normalized
pass	a let-wrapped resume in the arm body threads a computed next-state (stateful PRNG)
pass	a let-wrapped tail resume in a sleep arm folds and runs
pass	a line comment wrapping a top-level form does not hide it
pass	a line comment wrapping a type declaration is seen through
pass	a line comment wrapping the entry point is seen through
pass	a linear index-of search returns the POSITION of a runtime key, or -1 past the end
pass	a linear non-tail recursion over a non-associative operator preserves its exact result
pass	a linear non-tail sum is accumulator-transformed into a constant-stack loop
pass	a list PROJECTED from a shared aggregate, consumed then read, is unchanged (persistence via the child)
pass	a list annotated with the wrong element type is rejected
pass	a list arm with one constructor element binds its payload and its bare siblings
pass	a list arm with two constructor elements binds both payloads
pass	a list arm's constructor element at a non-head position
pass	a list arm's constructor element that does not match the tag falls through
pass	a list binder repeated across a leading position and the rest is non-linear
pass	a list binder repeated inside a NESTED tuple element is non-linear
pass	a list built at run time by a push-loop escapes to the host
pass	a list built by a recursive push-loop is then iterated by index
pass	a list built by a runtime-length loop has that many elements
pass	a list captured by a closure that consumes it is unchanged for a later read of the binding
pass	a list chosen by a runtime if is length-measured without emitting invalid wasm
pass	a list compound export under a non-main name crosses to the host
pass	a list consumed by List.concat in one operand keeps its element view for a later read
pass	a list consumed by List.push in one operand is unchanged for a later read of the same binding
pass	a list consumed by List.update in one operand keeps its element view for a later read
pass	a list ctor force-evaluates a SHIFT-COUNT-out-of-range element — the guard's unreachable trap kind
pass	a list ctor force-evaluates a recursively-OVERFLOWING element — the specific integer-overflow trap kind
pass	a list element out of range for a SIBLING element's annotated width is rejected
pass	a list element over the max of a SIBLING-inferred narrow width is rejected
pass	a list element position composes with a nested tuple pattern
pass	a list element that is a sum with a tuple payload binds the tuple fields
pass	a list element-type mismatch names the element axis and offers no mechanical fix
pass	a list int/float clash retypes a LATER integer element
pass	a list int/float retype fix preserves a negative literal's sign
pass	a list is exhaustive when the empty, singleton, and two-or-more arms jointly cover every length
pass	a list literal of records differing in one field type names the differing field
pass	a list literal of tuples of different arity names the arity delta at the join
pass	a list match arm may carry a guard on its element binders
pass	a list match pattern with TWO surplus rest binders names the shape, no unbound leak
pass	a list match pattern with a malformed rest names the shape, not an unbound surplus binder (CDZ0201)
pass	a list mixing integer and boolean elements is a type error
pass	a list mixing integer and float elements is a type error
pass	a list of Bool built at run time by a push-loop escapes with its Bool element type
pass	a list of Options orders its elements by the declared Some-below-None
pass	a list of RATIONALS op result — exact fractions cross resume and fold to a canonical sum
pass	a list of a nullary sum written in the bare-member form is exhaustive across every variant
pass	a list of a sum is exhaustive when the empty and every variant's first-element arm are covered
pass	a list of bools is exhaustive when the empty and both first-element values are covered
pass	a list of closure-carrying variants interprets as a command pipeline over an accumulator
pass	a list of closures each keeps its own capture, selected by a runtime index
pass	a list of maps with different keys is homogeneous, not a type error
pass	a list of maps: a runtime index then looks a runtime key up in the found map
pass	a list of nominal-over-quantity newtypes is a compound Map key
pass	a list of records differing by a misspelled field offers the rename fix
pass	a list of records in a record result VALUE round-trips via the run/encode envelope (no wit-world clause; a typed record/sum EXPORT is a separate gap)
pass	a list of records with a bytes leaf crosses the export boundary via an imposed WIT world
pass	a list of records with different field sets is a type error
pass	a list of records: a runtime index then reads a field of the found record
pass	a list of sets: a runtime index then tests membership in the found set
pass	a list of tuples with different arities is a type error
pass	a list of two same-dimension quantities at different units names the scale difference (CDZ0201)
pass	a list operation applied to a non-list names the expected argument type
pass	a list past 1024 elements builds a three-level RRB trie and indexes correctly at every level
pass	a list pattern at a tuple slot dispatches on list length and binds an element (empty + non-empty)
pass	a list pattern in a sum-variant payload matches a runtime node by its child count
pass	a list pattern on a record value is a shape error naming the type, not the payload
pass	a list pattern on a tuple value says a list pattern matches only a list
pass	a list pattern over a Map scrutinee is a type error
pass	a list pattern over a String scrutinee is a type error
pass	a list pattern over a map scrutinee is a type error
pass	a list pattern over a non-list scrutinee is a type error
pass	a list pattern over an Int64 scrutinee is a type error naming both kinds
pass	a list pattern over its matching List scrutinee kind is valid and matches (no over-rejection)
pass	a list pattern with a repeated leading binder is non-linear
pass	a list rest pattern binds an EMPTY rest when the fixed heads exactly consume a runtime list
pass	a list rest pattern takes exactly ONE binder after `..` — a trailing anchor is rejected
pass	a list rest pattern with a binder BEFORE the rest (leading rest) is a malformed shape, rejected CDZ0201
pass	a list rest pattern with more than one tail binder after `..` is malformed
pass	a list splat feeds a whole list into a list-rest parameter
pass	a list type whose element position holds a value is rejected
pass	a list value consumed by two operations in one function is not freed early
pass	a list with a runtime element is returned as a program result
pass	a list-aliased record read after Record.with sees the ORIGINAL value (no in-place clobber)
pass	a list-arm constructor element over a runtime-built list
pass	a list-arm map element binds both of two named keys
pass	a list-arm map element whose key is absent falls through
pass	a list-carrying recursion whose base arm does a fallible indexed read compiles
pass	a list-concatenating helper threads lists through a call
pass	a list-driven map build resolves a RUNTIME key collision last-wins, and the length tracks the collision
pass	a list-element join over different-unit same-dimension quantities names the scale
pass	a list-of-quantities used as a Map key canonicalizes and hits
pass	a list-payload split is total regardless of arm ORDER — non-empty arm before the empty arm
pass	a list-payload split missing the empty arm is non-exhaustive
pass	a list-payload split missing the non-empty arm is non-exhaustive
pass	a list-payload sum arm dispatches a runtime node by EXACT child count and binds a fixed child
pass	a list-rest argument of the wrong element type is rejected
pass	a list-rest parameter gathers the trailing arguments into a list
pass	a list-rest parameter with zero trailing arguments is the empty list
pass	a list-scrutinee match arm whose head is an unbound name is rejected in a recursive body (CDZ0101)
pass	a list-to-list TRANSFORMER op — heap payloads cross BOTH slots of one dispatch
pass	a list<MIXED-WIDTH-variant<scalar>> host arg emits, loads, and runs (via an imposed WIT world)
pass	a literal MAP KEY that overflows the annotated key width is rejected
pass	a literal MAP VALUE that overflows the annotated value width is rejected
pass	a literal SIGNED narrow-newtype element in a constructed list grounds its ConstInt to the inner width
pass	a literal amid valid variants is rejected, not silently dropped
pass	a literal arm wins over a later guard arm that also matches
pass	a literal as a parameter's annotation type is rejected
pass	a literal at a tuple slot dispatches with a binder-arm fall-through (runtime tuple)
pass	a literal at the deeper slot of a 3-tuple dispatches while earlier slots bind
pass	a literal dispatches two levels down (record inside a record)
pass	a literal in a DOUBLY-NESTED compound payload that overflows the annotated width is rejected
pass	a literal in a LIST element that overflows the annotated width is rejected
pass	a literal in a MULTI-PAYLOAD user sum that overflows the annotated width is rejected
pass	a literal in a MULTI-VARIANT user sum payload that overflows the annotated width is rejected
pass	a literal in a RECORD FIELD that overflows the annotated field width is rejected
pass	a literal in a TUPLE element that overflows the annotated width is rejected
pass	a literal in a USER-DECLARED sum payload that overflows the annotated width is rejected
pass	a literal in a let binder is refutable and rejected
pass	a literal in the list rest slot is rejected as a non-name rest binder
pass	a literal inside a NESTED constructor pattern refines the match
pass	a literal inside a constructor pattern matches a runtime payload
pass	a literal inside a tuple pattern matches a runtime element
pass	a literal inside a variant payload tuple dispatches under the Some tag
pass	a literal inside an Ok pattern refines a Result match
pass	a literal just past the Float32 finite max overflows in a runtime branch
pass	a literal list element and an author guard on the same arm both gate it
pass	a literal list element dispatches a runtime list by its value
pass	a literal match arm pins the scrutinee to a point, folding an arm-body compare and shedding an arith guard — the match-arm face of the point fact
pass	a literal narrow-newtype element in a constructed list grounds its ConstInt to the inner width
pass	a literal narrow-width newtype list element in a constructed list grounds to its inner width
pass	a literal nested in a compound payload that overflows the annotated width is rejected
pass	a literal nested in a tuple def-parameter is refutable and rejected
pass	a literal nested in a tuple let-binder is refutable and rejected
pass	a literal one past the Int8 min rejects in a runtime branch
pass	a literal pattern at MID-SPINE depth gates the arm while deeper binders still bind
pass	a literal segment before a binder dispatches then reads the payload
pass	a literal segment matches a magic-number header by equality
pass	a literal subterm in a quote pattern matches by equality
pass	a literal-record REBUILD from a map-borne record's fields computes (the #45 chain's working sibling)
pass	a literal-refined recursive-sum arm folds under a statically-known discriminant
pass	a literal-refined variant arm does not satisfy exhaustiveness
pass	a little-endian u16 bin field stitches its bytes across a rope seam in le order
pass	a local let binding shadows a same-named top-level definition
pass	a logical right shift that drops every significant bit of a masked unsigned value folds to zero
pass	a lone guarded list arm does NOT count toward exhaustiveness — the empty list stays uncovered
pass	a lone vacuous list-rest arm covers every length on its own
pass	a lone-condition `if` (no then either) keeps the generic arity message with no fix
pass	a lookup at a runtime key finds the present value, else None
pass	a loop whose bound is an invariant computation runs correctly (the bound is hoisted)
pass	a loop-invariant bitwise op hoisted out of a tail loop preserves the value
pass	a loop-invariant computed in both the condition and the body is shared, computing correctly
pass	a loop-invariant heap projection consumed in the loop body is not LICM-hoisted
pass	a loop-invariant multiply in the loop condition preserves the value
pass	a loop-invariant subexpression in a MATCH scrutinee is hoisted to valid wasm
pass	a loop-invariant trapping expression is not evaluated when the loop runs zero iterations
pass	a loop-invariant used in both the condition and the body is hoisted once with the same value
pass	a lower-bound branch refinement does NOT license eliding the upper-overflow guard
pass	a lowercase type variable in a USER-GENERIC parameter annotation is rejected
pass	a lowercase-named type cross-references another lowercase-named type in a field
pass	a lowercase-named type is referenceable in a field (types are values, not gated by case)
pass	a macro builds new syntax from its argument via quasiquote and the expansion is type-checked directly
pass	a macro may INTRODUCE a local binding in its expansion and reference it within that expansion
pass	a macro may introduce LET bindings in its expansion and reference them (a distinct binder form)
pass	a macro may introduce a HELPER FUNCTION and its parameter resolves in the function's body
pass	a macro may introduce a RECURSIVE helper function and its recursive self-call lowers + its parameter is inferred
pass	a macro mixing a body literal with an argument grounds both to Int64 (unless as a plain function)
pass	a macro wrapping the spliced body in a HANDLE keeps the caller's names in scope
pass	a macro-introduced LET binder does NOT capture a caller identifier of the same name (hygiene)
pass	a macro-introduced MATCH resolves its arm-pattern binders in the arm body (accessor macros)
fail	a macro-introduced REFERENCE resolves in the macro's definition scope, not captured by a caller binder (hygiene dir-2)
pass	a macro-introduced binder does NOT capture a caller identifier of the same name (hygiene, do-local def)
pass	a magic header and a length-prefixed chunk parse together
pass	a magnitude literal that overflows the annotated Qty INNER width is rejected
pass	a malformed @tag wrapping a non-def is one wraps-no-definition error, not a double
pass	a malformed application in a DEAD parameter's argument still rejects (a non-unbound fault kind)
pass	a malformed constructor-export with no ctor segment gets the constructor-export message
pass	a malformed constructor-export with too many segments gets the constructor-export message
pass	a malformed file-top pragma is rejected with the same code a nested one gets
pass	a malformed integer width — negative or non-natural — is rejected at compile time
pass	a malformed list rest NESTED in a variant payload names the rest shape, no unbound leak
pass	a malformed list-rest pattern rejects with no consequent unused-binding warning
pass	a malformed literal in a non-exported nullary def body still surfaces
pass	a malformed literal in a parameterized (unreached) body still surfaces
pass	a malformed read is a coded reject, not a not-yet-reducible decline
pass	a many-arm match consumed in non-tail position yields into the enclosing expression
pass	a many-arm match let-bound then consumed yields into the enclosing expression
pass	a many-arm scalar match in tail position selects each arm by a runtime scrutinee
pass	a many-arm string match consumed by concat beside a recursive call keeps both operands
pass	a map INVERSION flips keys and values, last-writer-wins on duplicate values
pass	a map KEY-type mismatch names the key axis
pass	a map KEYED by a user sum looks up by the variant
pass	a map VALUE-type mismatch names the value axis
pass	a map as a map value: a nested lookup queries the inner map through the outer lookup
pass	a map built at run time escapes to the host as its value form
pass	a map built at run time with list values escapes with its nested value type
pass	a map built from a BigInt sum key and a BigInt.of-over-arithmetic key has both entries
pass	a map can be a key, compared by its value
pass	a map can hold a map as a value
pass	a map chosen by a runtime if is size-measured with valid wasm
pass	a map churned all the way DOWN TO EMPTY equals Map.empty canonically
pass	a map churned up to 200 entries and back to its original 2 EQUALS the direct 2-entry build
pass	a map construction spread merges a runtime map's entries last-writer-wins
pass	a map construction spread with a later inline entry overrides a spread key
pass	a map construction spread with only spreads merges the operands last-writer-wins
pass	a map consumed by Map.insert in one operand is unchanged for a later read of the same binding
pass	a map consumed by Map.remove in one operand is unchanged for a later read of the same binding
pass	a map consumed by Map.swap in one operand is unchanged for a later read of the same binding
pass	a map consumed by Map.take in one operand is unchanged for a later read of the same binding
pass	a map entry with no value expression is rejected, not a crash
pass	a map grown by a recursive accumulator is looked up by a runtime key
pass	a map insert forces a trapping value at construction — Map.len traps, not returns the count
pass	a map inserted-into in one recursive sub-call is unchanged for a sibling sub-call's read
pass	a map inserted-into on one path and size-read on another keeps copy-on-write across recursion
pass	a map int-vs-float KEY clash offers the float-literal retype fix on the first key
pass	a map int-vs-float VALUE clash offers the float-literal retype fix on the first value
pass	a map keyed by inserting under a runtime Bytes ROPE is found by its flat twin and adds no leak
pass	a map literal with keys of two different types is a type error
pass	a map match over a map literal with a non-matching runtime key falls through
pass	a map match over a map literal with a runtime key selects by value, not a const fold
pass	a map match over a map literal with a runtime key selects the arm by value
pass	a map match pattern with a malformed rest names the shape, not an unbound binder (CDZ0201)
pass	a map mixing integer and float values is a type error
pass	a map nested as a VALUE persists across a two-level update via Map.swap
pass	a map of lists of tuples of RECORDS: a four-level mixed query reads a named leaf
pass	a map of lists of tuples: a three-level mixed query with a miss at each level
pass	a map of meter quantities rejects a second-dimension value insert
pass	a map operation applies to a map passed as a function parameter
pass	a map pattern as a list-arm element binds its value binder
pass	a map pattern binds the rest of the map after the named key
pass	a map pattern falls through when the key is absent
pass	a map pattern matches a present key and binds its value
pass	a map pattern naming a key the scrutinee lacks does NOT match (falls to the next arm)
pass	a map pattern nested in a tuple binds its value binder
pass	a map pattern over a non-map scrutinee is a type error
pass	a map pattern over an Int64 scrutinee (definite) is a type error
pass	a map pattern over its matching Map scrutinee kind is valid and matches (no over-rejection)
pass	a map pattern value sub-pattern may be a literal that refines the match
pass	a map pattern value sub-pattern may be an irrefutable compound that binds
pass	a map pattern with TWO `..` rest markers names the shape, not an unbound binder (CDZ0201)
pass	a map pattern with a wrong-type (Int) key on a String-keyed map is a type error (symmetric)
pass	a map pattern with a wrong-type (String) key on an Int-keyed map is a type error
pass	a map rest pattern with a keyed entry after the rest is a malformed shape, rejected CDZ0201
pass	a map rest post-entry rejects the same in the name-alias (map …) spelling
pass	a map threaded as a recursive accumulator is grown then measured
pass	a map threaded through MUTUAL recursion is read in both functions at alternating depths
pass	a map threaded through recursion is read at every depth and once more at the base
pass	a map value sub-pattern with binders reads a runtime map
pass	a map whose value is a list: a runtime lookup then indexes the returned list
pass	a map with BOTH axes wrong reports the leftmost (key) axis deterministically
pass	a map with a String value is looked up and its value compared in the arm
pass	a map with a String value: a missing key takes the None arm
pass	a map with a String value: the looked-up value's byte length is read in the arm
pass	a map with a computed key equals the same map with a constant key
pass	a map with a duplicate bool key is a type error
pass	a map with a duplicate int key is a type error
pass	a map with a duplicate key is a type error
pass	a map with a duplicate unit key is a type error
pass	a map with a spelling-variant duplicate key is a type error
pass	a map with a user-sum VALUE looks up and matches the stored variant
pass	a map with heterogeneous VALUES names both types and offers no fix for a cross-kind clash
pass	a map with record values of different field sets is a type error
pass	a map with tuple values of different arities is a type error
pass	a map with values of two different types is a type error
pass	a map-over-map rebuild transforms every retrieved value into a NEW trie in one walk
pass	a map-pattern literal value at one key plus a bare binder at another both match
pass	a map-pattern literal value sub-pattern falls through when the value differs
pass	a map-pattern literal value sub-pattern selects the arm when the value matches
pass	a map-pattern non-matching literal value falls through even if the other key matches
pass	a map-pattern single-variant ctor value sub-pattern binds its payload over a runtime map
pass	a map-pattern tuple value sub-pattern binds both elements over a runtime map
pass	a map-pattern tuple value sub-pattern falls through when the key is absent
pass	a map-scrutinee match arm whose head is an unbound name is rejected in a recursive body (CDZ0101)
pass	a map-to-map transformer op CHAINED — the second dispatch receives the first's result
pass	a map-via-effects walk — each element transformed by a dispatch, output order preserved
pass	a masked-divisor signed division still traps on a zero divisor (zero guard never elided)
pass	a match all of whose non-wildcard arms are unreachable collapses to the wildcard body
pass	a match arm an earlier arm already fully covers compiles but earns a CDZ0213 unreachable-arm warning
pass	a match arm binds a nested tuple inside a sum payload
pass	a match arm building a fresh runtime compound infers its shape
pass	a match arm mixes a payload LITERAL, a wildcard, and a nested-Option pattern
pass	a match arm ordering a compound sum-shell child via a borrowing compare computes correctly
pass	a match arm reading two elements of a boxed payload tuple shares the sum-payload prefix
pass	a match arm whose list length an earlier arm already covers is unreachable and earns a CDZ0213 warning
pass	a match arm's DESTRUCTURED payload is the argument to a perform in that arm's body
pass	a match binder over a narrow scrutinee returns the bound value
pass	a match binder used twice binds its recursive scrutinee once (A-normalized, value-correct)
pass	a match binds an Ast.Bool payload
pass	a match binds an Ast.Float payload
pass	a match binds an Ast.Str payload
pass	a match building one Some per arm sinks to a single Some around a payload match
pass	a match every unguarded arm of which yields the same value collapses to that value
pass	a match guard CALLS a user helper on the variant's heap payload to decide arm selection
pass	a match guard performs a MAP lookup keyed by the pattern binder (guard x CHAMP)
pass	a match guard reads a HEAP payload's length and the arm body reuses the same binder
pass	a match guard reads a NESTED record-field binder (a record inside the matched Some)
pass	a match init binds a per-arm HEAP list and the sibling init consumes it
pass	a match not covering the scrutinee is a compile-time rejection
pass	a match on a BigInt with a single catch-all binder binds and uses it
pass	a match on a computed runtime value dispatches on the result
pass	a match on a genuinely-runtime char (from a runtime Char.from-int) dispatches by scalar value
pass	a match on a record field selects a later literal arm
pass	a match on a record-field-access scrutinee dispatches on the field value
pass	a match on a runtime integer scrutinee producing a boolean
pass	a match on a tuple of RECURSIVE CALLS types its elements from the constructor pattern
pass	a match on a tuple-element-access scrutinee dispatches on the element value
pass	a match on a value of one sum type rejects a pattern from a different sum type
pass	a match on an open sum omitting the open-tail arm is rejected
pass	a match on an open sum with an open-tail arm is exhaustive
pass	a match on an uninhabited scrutinee is exhaustive with zero arms
pass	a match on the result of a match threads sum values through nested control flow
pass	a match over a masked scrutinee keeps an in-range arm at the range boundary
pass	a match over a masked scrutinee whose out-of-range arm is unreachable dispatches to the live arms
pass	a match over a match-selected Option folds through the false arm (None)
pass	a match over a match-selected Option folds through the true arm (Some)
pass	a match over a match-selected sum folds into the inner arms
pass	a match over a quoted float dispatches the Ast.Float arm past a preceding Ast.Int arm
pass	a match over a quoted string dispatches the Ast.Str arm past a preceding Ast.Name arm
pass	a match over a statically-known constructor with a runtime payload elides the dispatch
pass	a match over an if-selected Option folds through the else branch (None)
pass	a match over an if-selected Option folds through the then branch (Some)
pass	a match over an if-selected sum folds each branch's constructor to its arm body
pass	a match over an owned None shell from an out-of-range List.at reclaims it across a loop (no live objects)
pass	a match over an owned all-scalar-payload Some shell from List.at reclaims it across a loop (no live objects)
pass	a match over an owned all-scalar-payload Some shell from Map.lookup reclaims it across a loop (no live objects)
pass	a match over an owned compound-payload sum shell whose arm only borrows the child computes correctly
pass	a match over bytes with only a bin arm and no catch-all is non-exhaustive
pass	a match over multiple nullary constructors with uniform patterns
pass	a match pattern naming a non-existent variant is a coded rejection
pass	a match returning multi-argument closures applies at full arity
pass	a match takes another match's result as its scrutinee
pass	a match through an erased single-variant newtype dispatches on the inner sum's discriminant
pass	a match whose all arms diverge is Never and always traps
pass	a match whose arm bodies have different types is a type error even when a constant scrutinee selects one
pass	a match whose arm tuples differ in one position names the differing position
pass	a match whose only arm is guarded by a literally-true condition is still non-exhaustive
pass	a match whose only arm is guarded is non-exhaustive
pass	a match whose only variant arm is guarded is non-exhaustive
pass	a match with a float arm and a later int-literal arm retypes the int-literal arm up
pass	a match with an int-literal arm and a later float arm retypes the int-literal arm up
pass	a match-arm binder shadowing a parameter binds the scrutinee, not the argument
pass	a match-based self-tail-recursion loops in constant stack
pass	a match-based tail recursion with an earlier literal arm loops correctly
todo	a match-discard chain sequencing 3 dispatches over LIST-OF-LISTS state declines cleanly (let-chain twin folds)
pass	a match-join of two capturing lambdas let-bound and called applies the selected closure
pass	a match-of-closures on a runtime-selected variant is applied per arm
pass	a match-scrutinee self-call preceding an arm-body perform folds via multi-value return
pass	a match-shaped arm body peels its resume per branch when performed in a sequence
pass	a match-shaped control-flow operand of a narrow op is width-normalized
pass	a matched arm body performs and reads the state the scrutinee advanced
pass	a matching-width handler state folds across two sequential performs
pass	a max-FOLD over a list containing a computed NaN keeps the ordered maximum
pass	a member access with no field operand is rejected, not a crash
pass	a member access with too few operands (no key) carries no fix
pass	a member access with too many operands offers a delete-the-surplus fix and names the form
pass	a member read of an if-selected record selects the branch's field by name
pass	a member with TWO top-level mem-leaf params (list<u8> + list<s64>) threads the flattened cursor across both
pass	a member with a mem-leaf param interleaved with a scalar param threads the cursor across mixed widths
pass	a member with an option<s64> param beside a mem-leaf param composes the sum rebuild with the byte copy-in
pass	a member-access field with no close match lists the available fields with no confident fix
pass	a member-operand typo suggests a member-accessible module sharing a variant name, with a rename fix
pass	a memoize helper on a pre-populated key takes the hit branch and does not re-put
pass	a memoize helper's on-miss put threads to a later read of the same key
pass	a memoizing fold caches computed results in a Map and counts its hits
pass	a mid-scalar byte window crosses to the arm and String.from-bytes declines it
pass	a middle curry STAGE is reused — one s1 residual yields an s2 applied twice
pass	a middle module re-exports a base function and also uses it in its own exported function
pass	a middle peer is both a consumer and a provider (A to B to C chain)
pass	a min-heap in a list — push sifts UP, popmin sifts DOWN — the finding-24 match-scrutinee hoist at integration scale
pass	a mirrored equality pair under and folds by commutativity to one equality
pass	a mirrored equality pair under or folds by commutativity to one equality
pass	a miscased boolean False suggests the lowercase false literal with a replace fix
pass	a miscased boolean True suggests the lowercase true literal with a replace fix
pass	a mismatch against a sum with no fitting variant carries no wrap suggestion
pass	a mismatch between two same-named distinct types disambiguates the shared name (argument site)
pass	a missed literal tag falls to a BINDER bin arm that decodes the scrutinee
pass	a misspelled and keyword in head position suggests and
pass	a misspelled bits segment kind is rejected with the bits rename
pass	a misspelled field in a SHARED nested record drills the rename into the inner literal
pass	a misspelled field in a constructed record names the field-diff and offers a rename fix
pass	a misspelled field in a record ARGUMENT names the field-diff and offers a rename fix
pass	a misspelled field in a record LET-BINDER carries the same field rename fix
pass	a misspelled field on a runtime record reached through a parameter suggests the nearest field the same way
pass	a misspelled field on a visible record literal suggests the nearest field with a heuristic rename fix
pass	a misspelled if keyword in head position suggests if
pass	a misspelled let keyword in head position suggests let
pass	a misspelled match keyword in head position suggests match
pass	a misspelled segment kind is rejected with a rename suggestion
pass	a misspelled utf8 segment kind is rejected
pass	a misspelled variant in a list-element pattern suggests the near variant with a rename fix
pass	a mistyped argument to a sum parameter offers a wrap-in-variant fix at the call site
pass	a mixed String and scalar argument cross to a peer in one op, each in its own ABI lane
pass	a mixed handler's ABORTIVE arm value reads BOTH the op arg and the state binder
pass	a mixed-int-width operator with a non-aliased target width carries NO conversion fix
pass	a mixed-key map insert chain takes the same malformed-collection code
pass	a mixed-match tail loop whose Option payload is a heap String finds and reads the element
pass	a mixed-ownership inlined-match operand leaves the source map intact across repeated compares
pass	a mixed-payload error SUM in Result.Err dispatches all four arms through nested patterns
pass	a mixed-scale Int comparison truncates each operand to the reference (lossy, can compare equal)
pass	a mixed-scale combine over a computed BigInt quantity operand converts across scales
pass	a mixed-scale equality finds GENUINE equality after a whole-ratio conversion (5000 m = 5 km)
pass	a mixed-scale equality is FALSE when the converted values differ (5000 m ≠ 6 km)
pass	a mixed-scale narrow-width Int quantity combine that overflows the inner width traps
pass	a mixed-scale narrow-width quantity combine whose reference-converted sum overflows is rejected
pass	a mixed-width float tuple with a computed f32 NaN dedupes against the canonical spelling
pass	a mixed-width mutual-recursion SCC emits valid wasm (per-member scratch floor)
pass	a mode-REPLACING arm swaps the Symbol state; a conditional-value arm reads it
pass	a model-oracle property — CHAMP Map.len agrees with a counting model over generated inserts
pass	a module declaration binds its name in the enclosing scope
pass	a module export is a CLOSURE FACTORY capturing a private sibling
pass	a module export rides an OUTER combinator's fn parameter a runtime number of times
pass	a module factory's escaping closure carries a private HEAP value in its env
pass	a module function calls a sibling export by name
pass	a module function is recursive
pass	a module in a top-level do sequence type-checks its members
pass	a module may nest three deep
pass	a module member body resolves a sibling module in the enclosing scope
pass	a module member named by the export clause is reachable
pass	a module member not named by the export clause is private
pass	a module member reading a sibling constant with an unannotated param compiles and runs
pass	a module nested in a module projects as a nested record field
pass	a module reflects itself via Ast.module and exports a compile-time content-address a caller imports
pass	a module value definition registers a reachable export field
pass	a module with two definitions of the same name is rejected
pass	a module's delegated capability is reachable as metadata, not as an export
pass	a module-PRIVATE heap Map is read through an exported accessor across repeated calls
pass	a module-exported closure factory builds a PERFORMING closure homed at the apply site
pass	a module-exported recursive performer called from a HANDLER ARM homes under the outer handler
pass	a module-private Symbol-keyed table resolves the IMPORTER's own symbol literals by content
pass	a monomorphic user sum applied to a type argument in an annotation is rejected
pass	a multi-arg constructor with mixed-kind payloads binds each by its own kind
pass	a multi-arg non-recursive helper filing a resume-thunk folds, substituting all its args
pass	a multi-argument call node is evaluated by iterating its payload arg list
pass	a multi-argument closure among distinct-signature exports
pass	a multi-argument closure returning a COMPOUND round-trips — flat arrow spelling
pass	a multi-argument closure round-trips through a consumer
pass	a multi-argument closure round-trips through a consumer — FLAT arrow spelling
pass	a multi-argument closure with COMPOUND args round-trips — flat arrow spelling
pass	a multi-argument op with TWO heap arguments — a String key and a Map to search
pass	a multi-binder pattern on a single-value constructor points at the one-sub-pattern form
pass	a multi-byte runtime bit-field takes the matching wide unsigned type
pass	a multi-export VALUE-FORM shared call is repeatable on one make-<name> handle
pass	a multi-export compound-result shared call is a repeatable (borrow<t>) callback
pass	a multi-export record interface guest emits a wrapper per member and runs both (via an imposed WIT world)
pass	a multi-export set of parameterized capturing closures is driven per export
pass	a multi-export shared call is a repeatable (borrow<t>) callback
pass	a multi-field record result VALUE round-trips via the run/encode envelope (no wit-world clause; a typed record/sum EXPORT is a separate gap)
pass	a multi-field record with one param-named key survives the call
pass	a multi-key map pattern is order-independent and binds each named key's value
pass	a multi-key map pattern requires EVERY named key present, else it does not match
pass	a multi-level RRB list persistently updates deep interior-level indices leaving the original intact
pass	a multi-level prepend-built list reclaims every intermediate version (build-phase leak-free)
pass	a multi-limb BigInt leaf in a tuple orders by the high limb and ties fall to the next field
pass	a multi-limb BigInt-keyed trie churned back equals the direct build with the seed resolving
pass	a multi-limb modulo keeps the truncating dividend-sign convention across the limb boundary
pass	a multi-param lambda mixes a tuple-destructuring param with a bare param
pass	a multi-parameter closure keeps its captured environment distinct from its arguments
pass	a multi-parameter user generic Pair under-applied with one type argument names the plural arity
pass	a multi-parameter user generic UNDER-applied in an annotation is rejected
pass	a multi-parameter user generic sum under-applied names its two-argument arity
pass	a multi-payload destructure binds the head and recurses on the tail
pass	a multi-payload nested same-sum match falls through when the nested variant differs
pass	a multi-payload pattern of the wrong arity is a malformed destructure
pass	a multi-payload pattern with too FEW binders is a malformed destructure
pass	a multi-payload variant built from a runtime param boxes its Map field correctly (function[27] runtime twin)
pass	a multi-payload variant destructures its payloads positionally
pass	a multi-payload variant whose spread element is itself a compound escapes correctly
pass	a multi-payload variant with MIXED-width, MIXED-type payloads deconstructs each field
pass	a multi-payload variant with a scalar AND two recursive payloads escapes flat
pass	a multi-payload variant with an EMPTY-Map first payload compiles + deconstructs (function[27] regression pin)
pass	a multi-segment bin concatenates mixed-width signed and unsigned segments in order
pass	a multi-shot arm with an enclosing-param resume value and a MATCH-binder consumer folds
pass	a multi-shot arm's resume VALUE reads the enclosing param — the let-bound body folds correctly
pass	a multi-shot continuation contains a NESTED handler — each re-reduction re-enters it fresh
pass	a multi-shot continuation reaching an outer effect via an if branch is rejected
pass	a multi-shot continuation reaching an outer handler's effect is rejected, never doubling it
pass	a multi-shot continuation spanning a host call inside a do is rejected
pass	a multi-shot continuation spanning a host call is rejected, never doubling the host call
pass	a multi-shot ctl arm reading an enclosing param with NO let frame folds (mv no-let control)
pass	a multi-shot ctl arm whose continuation reads an ENCLOSING-fn param folds
pass	a multi-shot handle SEEDED by the enclosing param folds with a let-bound body
pass	a multi-site arm folds while the handle BODY reads an enclosing binding (the re-anchored free var)
pass	a multi-site arm's resume value routes through a pure helper call
pass	a multi-step kernel derivation composes several primitive rules into one theorem
pass	a multi-use param bound to a pure recursive-helper call inlines and computes
pass	a multi-use rope escapes a select through a Some shell and its twin survives the unwrap
pass	a multi-use runtime Map do-def escapes an if-select and both CHAMP handles stay live
pass	a multi-use runtime bool binding is reused in a nested condition
pass	a multi-use runtime let binding is computed once and reused (value-transparent)
pass	a multi-use runtime list do-def escapes an if-select inside a match arm and stays live
pass	a multi-variant abstract proof type's constructor match outside the kernel is a withheld-constructor rejection
pass	a multi-variant constructor nested in a tuple let-binder is refutable and rejected
pass	a multi-variant op arg matched against a sum state with an abortive-and-resumptive arm mix threads per dispatch
pass	a multi-variant sum box is REAL not erased: (Some k) wrapped then matched round-trips
pass	a multi-variant sum compared to a bare value is a generic kind mismatch with no misleading unwrap fix
pass	a multi-variant-sum binding pattern is refutable and rejected
pass	a multi-way string-head dispatch resolves an operator name to its operation
pass	a multibyte-UTF-8 Ast.Str round-trips through encode and decode
pass	a multibyte-UTF-8 Ast.Str round-trips through print and read
pass	a multibyte-length runtime String host arg copies the full length
pass	a multibyte-scalar char orders by scalar value, not UTF-8 byte length
pass	a multiply by a constant power of two on the left strength-reduces
pass	a multiply by a constant power of two strength-reduces and traps like the multiply
pass	a multiply crossing the single-limb boundary lands exactly and divides back
pass	a mutual AST walker re-extracts a node's payload in a sibling operand while a mutual call consumes it
pass	a mutual group performing in an IF base-case branch and recursing in the other threads and runs
pass	a mutual walk where BOTH partners perform threads one shared counter across the cycle
pass	a mutual-group demand-perform-demand arm in a let-wrapped dispatch folds down a runtime-opaque spine
pass	a mutual-recursion group with two concrete-different return widths is rejected (no silent unify)
pass	a mutual-recursion group's bare-literal return adopts a sibling's concrete width (seq-40 piece 4, #6049)
pass	a mutual-recursion pair where each side performs against its OWN handler (two nested frames)
pass	a mutually tail-recursive even/odd over a large runtime count iterates in constant stack
pass	a mutually-recursive AST walker reads a node's child list without corrupting a sibling head-read
pass	a mutually-recursive check/check-if returning a Result compiles and runs (if-cond is a call)
pass	a mutually-recursive decoder infers its params from the call site and emits valid wasm
pass	a mutually-recursive decoder returns a heap value and cursor and its heap slot is dispatched
pass	a mutually-recursive even/odd guard computes correctly (the call-branch is not rewritten)
pass	a mutually-recursive fold matching a rebuilt list with a payload binder builds and computes
pass	a mutually-recursive fresh-id walk assigns a fresh id at each node and sums them
pass	a mutually-recursive generic group is instantiated per type
pass	a mutually-recursive group performs in its entry def while its partner only dispatches
pass	a mutually-recursive group performs in one branch and recurses in the OTHER
pass	a mutually-recursive group performs through a shared non-recursive helper
pass	a mutually-recursive group threads two nested handlers' states at once
todo	a mutually-recursive group with a BRANCH-PERFORM sharing a strict expr with the mutual call declines cleanly (adv-69 rw4 sub-face)
pass	a mutually-recursive group with the perform in a DIFFERENT branch from the mutual call folds
pass	a mutually-recursive pair fully named by the export clause resolves
pass	a mutually-recursive parser projects a return tuple and rebuilds one with a boxed sum across the edge
pass	a mutually-recursive performer pair whose out-state a trailing caller draw observes folds via group-wide multi-value
pass	a mutually-recursive sum value stored as a MAP VALUE round-trips and evaluates
pass	a naive (non-renaming) substitution captures a free variable — the bug capture-avoidance prevents
pass	a name bound to a string keys a map by its value, not the literal name
pass	a name pattern binds a RUNTIME scrutinee read from an exported parameter
pass	a name pattern binds the runtime scrutinee when no literal arm matches
pass	a name pattern over a NARROW-width scrutinee normalizes the literal arm to the match's width
pass	a name resolves to the nearest enclosing binding
pass	a name-alias (map …) compared against a record is a cross-kind type error
pass	a name-alias (map …) literal mixing integer and float values is a type error
pass	a name-alias (map …) literal with a duplicate integer key is a type error
pass	a name-alias (map …) literal with a duplicate string key is a type error
pass	a name-alias (map …) literal with keys of two different types is a type error
pass	a name-alias (map …) literal with values of two different types is a type error
pass	a name-alias (map …) with a duplicate bool key is a type error
pass	a name-alias (map …) with a duplicate unit key is a type error
pass	a name-alias (map …) with record values of different field sets is a type error
pass	a name-alias (map …) with tuple values of different arity is a type error
pass	a name-only top-level annotation wraps no definition and is rejected
pass	a named arm still dispatches directly rather than forwarding through the open tail
pass	a named capturing closure applied directly folds through its capture
pass	a named capturing closure applied more than once folds at each use
pass	a named catch-all binder captures the whole sum and re-matches it
pass	a named control char reads to its scalar code point
pass	a named family unit combines across DIFFERENT dimensions is a compile-time error
pass	a named float width alias and its width-indexed expansion name the same type
pass	a named function is partially applied, bound, and used
pass	a named higher-order function receives a lambda argument
pass	a named multi-argument function applies in explicit curried form
pass	a named multi-argument function applies to all its arguments at once
pass	a named rate unit converts to the reference rate (Float)
pass	a named runtime binding computes its value exactly once
pass	a named variant-with-payload field in a record result VALUE round-trips via the run/encode envelope both arms (no wit-world clause; a typed record/sum EXPORT is a separate gap)
pass	a named width alias and its width-indexed expansion name the same type
pass	a named-variant result writer name-matches (not positionally) with a reversed guest decl (via an imposed WIT world)
pass	a named-wildcard guard binder over a string binds in both the guard and the body
pass	a nan compared to a non-float is the cross-kind type error
pass	a narrow UInt8 multiply by two strength-reduces and traps on width overflow
pass	a narrow addition reconciles a constant LEFT operand to the variable's width
pass	a narrow comparison reconciles a constant LEFT operand to the variable's width
pass	a narrow const-multiplier bound check keeps both bounds (Int8 a*3)
pass	a narrow match binder used in arithmetic with the scrutinee
pass	a narrow multiplication reconciles a constant LEFT operand to the variable's width
pass	a narrow newtype boxed as a map value round-trips through lookup at the widened cell
pass	a narrow newtype boxed in a list round-trips two payloads through binder reads
pass	a narrow op takes its width from a PARAM while a control-flow operand is normalized
pass	a narrow value in the else branch opposite a bare literal computes at the narrow width
pass	a narrow-Int quantity stored in a List reads back through List.at and unwraps
pass	a narrow-UInt8-guarded div is not select-ified — the guard shields the trap
pass	a narrow-body arithmetic on an in-range constant arg overflows the parameter width
pass	a narrow-width Int quantity add that fits the inner width runs normally
pass	a narrow-width Int quantity add that overflows the inner width traps at compile time
pass	a narrow-width Int quantity stored as a map value round-trips through Map.lookup
pass	a narrow-width accumulator that never changes threads through the recursion
pass	a narrow-width add overflow names the integer overflow trap reason
pass	a narrow-width name binder is usable in a downstream op at its width
pass	a narrow-width newtype boxed as a list element matches by its literal payload
pass	a narrow-width newtype boxed as a sum payload matches by its literal payload
pass	a narrow-width newtype boxed as a tuple element falls through on a non-matching payload
pass	a narrow-width newtype boxed as a tuple element matches by its literal payload
pass	a narrow-width overflow in a RECURSIVE function's annotated result is rejected
pass	a narrow-width overflow in a handler arm resume value under a narrow annotation is rejected
pass	a narrow-width overflow projected through Option.expect over a runtime branch is rejected
pass	a narrow-width overflow projected through Option.expect over a runtime sum is rejected at check
pass	a narrow-width overflow projected through Result.expect over a runtime branch is rejected
pass	a narrow-width overflow projected through Result.expect over a runtime sum is rejected at check
pass	a narrow-width overflow reached ACROSS a function boundary through a match projection is rejected
pass	a narrow-width overflow reached through a MATCH projection of a runtime sum is rejected
pass	a narrow-width overflow reached through a TUPLE-field projection of a runtime branch is rejected
pass	a narrow-width parameter compared to a bare literal computes at the parameter width
pass	a narrow-width parameter plus a bare literal computes at the parameter width
pass	a narrow-width parameter plus a bare literal computes inside a helper function
pass	a narrow-width quantity ADD whose sum overflows the inner type is rejected (same-unit arith path)
pass	a narrow-width quantity add whose sum FITS the inner type runs normally (no spurious overflow trap)
pass	a narrow-width quantity whose reference-scaled magnitude overflows its inner type is rejected
pass	a narrow-width two-parameter recursion compiles to valid wasm and computes
pass	a narrow-width unsigned wrapping-add constant folds by wrapping to the masked outcome
pass	a narrow-width wrapping-mul constant folds by wrapping, not by rejecting
pass	a narrower UInt8 into a u16 segment is not silently widened — a width type error
pass	a narrower-typed runtime value is not silently widened into a wider segment
pass	a narrowing wrap of a wider wrap elides the inner wrap
pass	a near typo of a real type in a variant payload keeps its did-you-mean
pass	a near-eval typo keeps the ordinary unbound did-you-mean to a near def, not the eval teaching message
pass	a near-miss absent field in Record.without suggests the near field with a replace fix
pass	a near-miss field in Record.pop suggests the near field with a replace fix
pass	a near-miss field in Record.with suggests the near field and keeps the extend hint
pass	a near-miss integer bin segment width carries a rename fix to the byte-aligned width
pass	a near-miss misspelled export carries a did-you-mean and an applyable replace fix
pass	a near-miss pragma key suggests the registry key with a rename fix
pass	a near-miss wrong-sum variant pattern head suggests the matched sum's variant with a fix
pass	a near-typo BARE variant pattern head is an unbound name that still suggests the nearest variant
pass	a near-typo ctor over a single-variant newtype suggests its sole variant with a fix
pass	a near-typo qualified variant pattern head suggests the nearest variant with a replace fix
pass	a nearer handler on the call chain shadows an outer one
pass	a negated repeated condition in a nested if collapses (value parity)
pass	a negated zero equality composes with the negation fold
pass	a negative and its positive twin encode to distinct bytes
pass	a negative bare literal under an unsigned narrow default is rejected (the sign face)
pass	a negative branch literal in an unsigned annotation via a runtime if is rejected at check
pass	a negative const-multiplier bound check traps at the exact boundary
pass	a negative constant BigInt crosses the boundary with its sign
pass	a negative constant argument to an unsigned parameter is rejected
pass	a negative constant argument to an unsigned parameter is rejected, not laundered
pass	a negative decimal literal annotated Rational normalizes its sign onto the numerator
pass	a negative exponent scales the mantissa down
pass	a negative hexadecimal literal negates its value
pass	a negative integer literal pattern matches a runtime scrutinee
pass	a negative integer nested in a compound round-trips by byte identity
pass	a negative integer width is rejected
pass	a negative integer width is rejected as non-natural
pass	a negative literal under an UNSIGNED width rejects in a runtime branch
pass	a negative out-of-range integer literal in an unsigned type retypes to the smallest signed width
pass	a negative out-of-range integer literal too large for Int8 escalates to the next signed width
pass	a negative power is the reciprocal, deriving an inverse unit
pass	a negative power over a BigInt magnitude truncates the reciprocal exactly
pass	a negative power over a Rational magnitude is the EXACT reciprocal
pass	a negative power over an integer magnitude truncates the reciprocal
pass	a negative right-shift count traps rather than masking
pass	a negative runtime BigInt result crosses the host boundary with its sign
pass	a negative shift count traps rather than masking
pass	a negative string index yields None rather than wrapping to a large offset
pass	a negative unsigned integer width is rejected
pass	a negative width is not honored as a narrow type
pass	a negative zero in a record field is distinct from positive zero
pass	a negative zero nested in a tuple is distinct from positive zero
pass	a negative-denominator Rational sign-normalizes on the set-element path and dedupes its positive-denominator twin
pass	a negative-exponent unit cancels its base through a multiply to dimensionless
pass	a negative-key trie churned back keys and enumerates like the direct build
pass	a negative-power SI prefix converts over Float
pass	a negative-power SI prefix is an exact rational scale
pass	a nested (* (* x 2) 4) strength-reduces to shifts and preserves value + Int64 overflow-trap parity
pass	a nested @!param is a misplaced module directive (CDZ0602), not a module parameter
pass	a nested @param annotation in expression position cannot appear here
pass	a nested @tag annotation in expression position cannot appear here
pass	a nested Ast.List match falls through when the inner leaf variant differs
pass	a nested Ast.List match over a constant quote reads the deep leaf at the right depth
pass	a nested OR by the same constant collapses to a single OR
pass	a nested Option is double-matched to its inner value
pass	a nested RECORD below a record BINDING field binds v (§235 full nested-record descent, name-keyed sub_path)
pass	a nested RECORD below a record MATCH field binds v (§235 full nested-record descent)
pass	a nested Record.with rewrites an inner record field through the outer, inline
pass	a nested Result pattern selects the inner Err arm
pass	a nested all-nullary sum renders the correct variant across the boundary (render half, v-runtime f9f8717c)
pass	a nested application of a let-bound lambda resolves each argument at its call site
pass	a nested call chain whose callee duplicates its parameter folds without exponential blowup
pass	a nested chain of function calls compiles in linear time and folds to a constant
pass	a nested char-literal payload falls through on a non-matching char
pass	a nested checked op computes over runtime args after dest-threading
pass	a nested checked op traps when the inner add overflows despite the shared slot
pass	a nested checked op traps when the outer multiply overflows
pass	a nested conditional (the sign idiom) folds to branchless nested selects — negative
pass	a nested conditional (the sign idiom) folds to branchless nested selects — positive
pass	a nested conditional (the sign idiom) folds to branchless nested selects — zero
pass	a nested conjunction accumulates all three operand hypotheses
pass	a nested constant multiply preserves the inner overflow rather than combining the factors
pass	a nested constant tuple projection folds through both levels
pass	a nested constructor in a multi-payload position destructures two levels
pass	a nested constructor match on a variant past the first dispatches on that variant's payload
pass	a nested constructor pattern dispatches a RUNTIME two-sum value
pass	a nested constructor payload that misses the inner variant falls through to the outer sibling
pass	a nested constructor value dispatches on both tags in a match
pass	a nested effectful let inlined into a re-performing body keeps its inner binder
pass	a nested element kind mismatch in equality is a type error
pass	a nested element pattern dispatches on a MIDDLE-variant list payload
pass	a nested element pattern dispatches on a NON-variant-0 list payload
pass	a nested element pattern dispatches on a non-variant-0 list-payload sum (disc read via vec-get, not arr-get)
pass	a nested handle's INIT expression performs against the OUTER handler before installing
pass	a nested handler arm whose RESUME-VALUE performs the outer effect threads the advance to the continuation
pass	a nested if of a common constructor builds it once and dispatches to each arm
pass	a nested if on the same condition collapses the inner test to the known branch
pass	a nested inner handler that re-threads its own state folds (merged-context seed from init)
pass	a nested lambda captures+applies a closure-typed def PARAMETER (inline lambda argument)
pass	a nested let shadows an outer let of a different type and both resolve, outer surviving
pass	a nested list element composes over a runtime list of lists
pass	a nested literal pattern of the wrong type is a type error
pass	a nested map pattern falls through when its key is absent
pass	a nested map pattern with a malformed rest names the shape, not an unbound binder (CDZ0201)
pass	a nested match missing an inner variant under a DISC-0 user-sum variant is non-exhaustive
pass	a nested match missing an inner variant under a DISC-≥1 variant is non-exhaustive
pass	a nested match on a recursive sum falls through when the inner variant differs (known outer disc)
pass	a nested match on a recursive sum with a KNOWN outer discriminant reads the right payload depth
pass	a nested match on a recursive sum with a statically-known outer disc reads the inner payload at the right depth
pass	a nested match on a runtime scrutinee
pass	a nested match on the SAME runtime scrutinee already refined to a variant by the enclosing arm
pass	a nested module and a sibling def in the same outer module both stay reachable
pass	a nested module exporting a name it does not declare is rejected
pass	a nested module's exported member IS reachable through the chain despite a private sibling
pass	a nested module's function export is applied through the projection chain
pass	a nested module's inner function calls its inner sibling by bare name
pass	a nested modulo by a dividing constant collapses to one modulo (across signs)
pass	a nested modulo by a multiple collapses to a single modulo on a runtime operand
pass	a nested modulo does NOT collapse when the inner divisor does not divide the outer
pass	a nested narrow (Int8) addition computes at the consuming width
pass	a nested narrow (Int8) arith range-checks the INNER op and traps on inner overflow
pass	a nested narrow (Int8) multiply operand computes at the consuming width
pass	a nested narrow Int8 add with two if operands computes at the consuming width
pass	a nested narrow Int8 arith range-checks the inner op so an inner overflow traps
pass	a nested narrow Int8 arith with a multiply inner op computes at the consuming width
pass	a nested non-exhaustive match names MULTIPLE uncovered inner variants
pass	a nested non-exhaustive match names the uncovered inner variant (CDZ0210 not the generic message)
pass	a nested non-exhaustive match names the uncovered inner variant (no flat-append fix)
pass	a nested option value annotated with the wrong inner payload type is rejected
pass	a nested option<option<s64>> EXPORT result crosses — a SUM inside an option payload
pass	a nested pattern under an open sum's named variant still requires the outer open-tail arm
pass	a nested quote pattern matches a compound-within-a-compound two levels deep
pass	a nested record param with a bytes leaf compiles and runs (via an imposed WIT world)
pass	a nested runtime checked multiply composes and traps in the inner add
pass	a nested runtime tuple built behind a recursive call escapes to the host
pass	a nested runtime tuple is returned as a result
pass	a nested sum match missing an inner variant is non-exhaustive
pass	a nested symbol-literal payload falls through on a non-matching symbol
pass	a nested symbol-literal payload pattern falls through on a non-match
pass	a nested symbol-literal payload pattern matches by content
pass	a nested tuple in the list rest slot is the same rest-shape error, no unbound leak
pass	a nested tuple list element is bound over a runtime list and folded across the rest
pass	a nested tuple pattern of the wrong arity is a type error
pass	a nested tuple-arity mismatch in equality is a type error
pass	a nested tuple-destructuring lambda binds both levels
pass	a nested tuple-in-tuple float key MISSES a Map key with a different deep float leaf
pass	a nested tuple-in-tuple float key is a Map key found by a separately-built equal key
pass	a nested tuple-pattern parameter destructures a runtime tuple
pass	a nested unit power multiplies exponents — (m^2)^2 is the same dimension as m^4
pass	a nested unquote Bool pattern does not match a non-boolean operand
pass	a nested unquote pattern binds an Ast.Name operand's identifier
pass	a nested unquote pattern matches a Bool sub-AST by shape
pass	a nested unquote pattern matches a Float sub-AST by shape
pass	a nested unquote pattern matches a Str sub-AST by shape
pass	a nested unquote pattern matches a sub-AST by shape
pass	a nested wrong-arity type constructor keeps its own arity message
pass	a nested-compound Float32 equality const-folds through the demoting compare
pass	a nested-handler ctl arm whose continuation-consuming body ALSO performs an OUTER effect folds
pass	a nested-if tower sharing an arm flattens and preserves the truth table
pass	a nested-let chain that reuses each binding folds to one value
pass	a nested-list DAG — one perform-derived inner list aliased TWICE in the outer
pass	a nested-list-rest arm reads BOTH the inner rest and the outer rest binders
pass	a nested-map BUMP (lookup-inner, insert-inner, reinsert-outer) builds a multimap
pass	a nested-operand abort preserves a HEAP-state advance across a mid-mutation frame drop
pass	a nested-path mismatch mixes a record field name and a tuple index
pass	a nested-record parameter projects its inner fields through two dot levels
pass	a nested-rest list inside a variant payload gets the same rest-shape reject
pass	a nested-rest sub-pattern binding nothing still rejects the list rest shape
pass	a neutral and-true keeps both the trap and the value of its left operand
pass	a neutral or-false keeps both the trap and the value of its left operand
pass	a newtype match-arm destructure reads the erased scalar back
pass	a newtype over a record reads a field through the tag
pass	a newtype over a record reads a field through the tag at runtime
pass	a newtype over a record still rejects a missing field through the tag
pass	a newtype over a sum erases without a second box and matches through both layers
pass	a newtype over a sum escapes to the host as its erased underlying sum value-form
pass	a newtype whose variant name equals its type name resolves in both head and annotation positions
pass	a newtype wrapping a heap list compares by the inner heap walk
pass	a newtype-typed ACCUMULATOR threads a recursive fold through a wrapping add
pass	a newtype-wrapped scalar as a map key looks up through the erasure
pass	a newtype-wrapped scalar as a set element deduplicates through the erasure
pass	a next-state computed by a match over the state threads a transition across performs
pass	a nibble OPCODE dispatches arms of different shapes with a byte-aligned dependent size
pass	a no-overflow obligation is DISCHARGED: for x <= 100, (x + 1) <= MAXINT via monotonicity + a CHECKED numeral fact
pass	a nominal newtype over a Float32 quantity stored as a map value round-trips
pass	a nominal newtype over a quantity is a Map key that hits by content
pass	a nominal record compared to a plain record of the same shape is a type error
pass	a non-Bool guard condition is a type error naming the offending type
pass	a non-String Ast.read argument names the String requirement (Bool operand)
pass	a non-String Ast.read argument names the String requirement (Int64 operand)
pass	a non-String trap message names the String requirement (Bool operand)
pass	a non-String trap message names the String requirement (tuple operand)
pass	a non-`-` partial in value position keeps the GENERIC not-fully-applied message (no suggest-`Num.neg`)
pass	a non-admitted float width NESTED in a compound annotation is rejected
pass	a non-admitted float width in a parameter annotation is rejected
pass	a non-admitted float width in a type-declaration payload is rejected
pass	a non-aliased-width result crosses the boundary widened to the next aliased width
pass	a non-arrow effect operation type is rejected with a wrap-into-arrow fix
pass	a non-arrow generic effect operation type is rejected
pass	a non-byte-aligned bit-field width takes its exact arbitrary-width unsigned type
pass	a non-byte-aligned integer bin segment width names the supported widths and the bits alternative
pass	a non-canonical effect-name-less handle is rejected, with no consequent no-home error
pass	a non-capturing closure stored and also called directly emits a valid artifact
pass	a non-capturing recursive local function compiles and runs (only capture+recursion declines)
pass	a non-coercible mistyped resume value carries no coercion fix
pass	a non-constructor type applied to arguments in type position is rejected
pass	a non-declaration member of a bare-name module is rejected at the member
pass	a non-dividing conversion ratio truncates on an Int magnitude
pass	a non-empty scrutinee does not match an empty bin pattern
pass	a non-exhaustive Bool match names the missing literal and inserts exactly that arm
pass	a non-exhaustive guarded match is rejected even when a constant scrutinee folds its guard true
pass	a non-exhaustive match lists MULTIPLE missing variants and appends an arm for each
pass	a non-exhaustive scalar match names the wildcard gap and offers an add-wildcard-arm fix
pass	a non-exhaustive sum match names the uncovered variant and offers an add-arms fix
pass	a non-final splice in a quote pattern is rejected CDZ0221
pass	a non-final unquote-splice in a quote pattern is ill-formed
pass	a non-final unsized bytes segment is an ill-formed binary form
pass	a non-final unsized bytes segment is ill-formed
pass	a non-first field is projected from a record returned across a call boundary
pass	a non-integer exponent in a Unit.^ power is named, not silently shipped
pass	a non-integer exponent in a Unit.^ power is rejected
pass	a non-integer float literal annotated an integer type carries NO drop-fraction fix
pass	a non-kebab (camelCase) peer op name agrees across both sides and runs
pass	a non-linear list binding pattern is rejected CDZ0102 before the refutability guard
pass	a non-linear tuple binding pattern is rejected
pass	a non-linear tuple def-parameter is rejected CDZ0102
pass	a non-linear tuple match pattern is rejected CDZ0102 with a rename fix
pass	a non-literal NARROWER integer annotated a float nests the Int64 widening in the of-int wrap
pass	a non-literal integer annotated a float offers an of-int wrap fix
pass	a non-matching literal inside a constructor pattern binds the runtime payload
pass	a non-member intrinsic-module arm head over a recursive body is a coded CDZ0201
pass	a non-name element of a multi-name export is rejected, not silently dropped
pass	a non-name export argument is rejected
pass	a non-numeric compound leaf (Bool vs Int) offers no coercion fix
pass	a non-numeric operand mixed with a float operator carries NO coercion fix
pass	a non-numeric operand to an arithmetic operator fails the operator-scheme unification (CDZ0203, not the numeric no-promotion)
pass	a non-numeric sum payload names the axis but offers no fix
pass	a non-op list effect clause is a malformed effect declaration
pass	a non-performing guard under a handle compiles and selects
pass	a non-power-of-two float width is rejected
pass	a non-recursive generic producer composes at two element types via inlining
pass	a non-recursive helper that performs the discharged op UNDER a conditional, in a recursive self-call arg, folds
pass	a non-scalar char in a parameterized (unreached) body still surfaces
pass	a non-tail accumulable recursion runs to a large runtime N in constant stack
pass	a non-tail call passing a heap-match argument compiles to valid wasm
pass	a non-tail conditional abort hoists out of a strict operand and folds per branch
pass	a non-tail conditional abort in the untaken branch folds to the pure sibling sum
pass	a non-tail cross-function conditional abort declines cleanly
todo	a non-tail inner handle with a foreign perform sibling stays declined (needs frames)
pass	a non-tail list fold over a large runtime list is accumulator-transformed to constant stack
pass	a non-tail mutual-recursive group observing a partner's out-state folds via the group multi-value fold
pass	a non-tail recursion with a fallible read in its base arm composes
pass	a non-tail recursive abort abandons the pending frames and folds to the abort value (99)
pass	a non-tail resume runs the continuation as a statement and the arm tail is the handle value
pass	a non-tail-resumptive outer handler reduces a reducible inner handle before its own fold
pass	a non-terminating self-recursive macro is a hard CDZ0999 error, not a compiler hang
pass	a non-type in a List element position names the element type
pass	a non-type in a Map key position names the key type
pass	a non-type in a Map value position names the value type
pass	a non-type in a Set element position names the element type
pass	a non-type in a Tuple element position names the element index
pass	a non-type in a function parameter position names the parameter index
pass	a non-type in a function result position names the result type
pass	a non-unit base in a Unit.^ power is named, not silently shipped
pass	a non-unit base in a Unit.^ power is rejected
pass	a non-unit factor in a Unit.* composition is rejected
pass	a non-unit factor in a Unit.* product is named, not silently shipped
pass	a non-unit factor in a Unit./ composition is rejected
pass	a non-unit factor in a Unit./ quotient is named, not silently shipped
pass	a none-only option<s64> record field resolves its element type from an imposed WIT world
pass	a nonlinear-parameter rename fix dodges an existing later binder name
pass	a nonneg-dividend signed division elides the MIN over -1 guard and divides by -1 as a plain negation
pass	a nonzero BigInt literal probe dispatches across MUTUAL recursion post-fix
pass	a nonzero BigInt probe over a scrutinee REBUILT each recursive frame dispatches every time
pass	a not connective with a non-boolean operand is a type error
pass	a not of a runtime conjunction computes the negated conjunction
pass	a not-a-number leaf makes a compound unequal to the same compound with a finite leaf
pass	a not-a-number value is unequal to a finite float
pass	a nullary (Set.of) is an arity error, not a compiler crash
pass	a nullary Sign variant applied to a non-unit payload is a type error
pass	a nullary constructor is a plain first-class value stored in a tuple
pass	a nullary constructor is a single-arity function taking unit
pass	a nullary do-local def followed by a use of it computes over its result
pass	a nullary function called from another function's body
pass	a nullary function returning a scalar is callable
pass	a nullary generic producer argument with no element source declines with a nullary-specific message
pass	a nullary helper called and used in arithmetic
pass	a nullary lambda applied yields its body
pass	a nullary module export applied to a non-unit argument is rejected
pass	a nullary module export's non-unit argument does not swallow the argument's own fault
pass	a nullary single-variant newtype is a nominal unit tag that matches unconditionally
pass	a nullary sum type declared inside a do block resolves and matches
pass	a nullary variant applied to a non-unit payload is a type error
pass	a nullary variant applied to a non-unit payload rejects even when constructed via application
pass	a nullary variant applied to a payload names the variant and that it cannot be applied
pass	a nullary variant applied to unit constructs correctly through the application path
pass	a nullary variant list element dispatches by its discriminant
pass	a nullary-variant dotted pattern matches inside a match nested directly in an arm body
pass	a number-first char comparison offers the Char.to-int wrap fix (either operand order)
pass	a numeric NESTED-record leaf offers the inner bare-literal retype fix
pass	a numeric list-element leaf offers the inner bare-literal retype fix
pass	a numeric map-KEY leaf offers the inner bare-literal retype fix
pass	a numeric map-VALUE leaf offers the inner bare-literal retype fix
pass	a numeric record-field leaf offers the inner bare-literal retype fix
pass	a numeric tuple-position leaf offers the inner bare-literal retype fix
pass	a numeric-mix bare value into a Qty Int64 names the arg-type mismatch, not the Qty.of wrap
pass	a one-concat rope nested in a tuple equals its flat twin
pass	a one-element tuple pattern against a two-tuple is a type error
pass	a one-node Ast rewrite over a runtime leaf computes per call
pass	a one-shot handler arm folds a body with TWO performs by re-reducing the continuation
pass	a one-shot two-hole body folds across a let binding
pass	a one-shot two-hole body folds with the leading perform in an if condition
pass	a package entry's exported def wins over a same-named def in a library file
pass	a package entry's exported def wins over an alphabetically-later same-named library def
pass	a pairwise MAX-GAP walk over an Instant timeline subtracts adjacent newtype payloads
pass	a param-colliding record key does not corrupt a non-colliding field
pass	a param-keyed map literal selects its literal-pattern arm by value in both directions
pass	a param-left arith-right Float64.of-int equality compiles to a valid module and computes
pass	a param-shadowing let with a param-referencing initializer folds at a constant argument
pass	a parameter annotation contradicting its use is rejected
pass	a parameter carries a type annotation in the signature
pass	a parameter named tuple is applied as the bound function
pass	a parameter passed to a polymorphic callee is not over-constrained
pass	a parameterized called function's free names resolve lexically while its argument substitutes
pass	a parameterized compound-return export forwards distinct arguments to make
pass	a parameterized export builds a list of tuples by a runtime-length loop and measures it
pass	a parameterized export drives an Option-of-runtime-BigInt on both arms
pass	a parameterized export returning a capturing closure is made and called by the host
pass	a parameterized export returning a multi-argument capturing closure
pass	a parameterized export returns a built-in Option computed from its argument
pass	a parameterized export returns a list computed from its argument
pass	a parameterized export returns a list of lists built from its argument
pass	a parameterized export returns a list of tuples built from its argument
pass	a parameterized export returns a record computed from its argument
pass	a parameterized export returns a runtime BigInt computed from its argument
pass	a parameterized export returns a runtime Rational computed from its argument
pass	a parameterized export returns a runtime-selected empty-or-nonempty list
pass	a parameterized export returns a runtime-selected empty-or-nonempty map
pass	a parameterized export returns a runtime-selected empty-or-nonempty set
pass	a parameterized export returns a sum value computed from its argument
pass	a parameterized export returns a tuple computed from its argument
pass	a parameterized plain export alongside a closure export applies its argument
pass	a parameterized plain export alongside a closure export — the closure captures and applies
pass	a parameterized type-valued export is one coded error, not a no-runtime-form cascade
pass	a parametric variant payload does not false-positive as an unknown type
pass	a parenthesized-head generic type de-dups a repeated head parameter to arity one and resolves
pass	a partial application as an operator operand names the forgotten call with a polished function-value lead
pass	a partial application captures a let-bound variable in the residual closure
pass	a partial application captures a runtime parameter in the residual closure
pass	a partial application capturing a fresh-literal List in the residual closure runs and reclaims (live-objects 0)
pass	a partial application of a named def keeps its annotated param type across the recursive-HOF beta copy
pass	a partial application of a performing closure that escapes its handler is rejected
pass	a partial application persists its capture across two applications
pass	a partial application still needing two arguments pluralizes the count
pass	a partial application where a scalar is expected names the forgotten call
pass	a partial application whose residual reads the compound capture in TWO escaping positions reclaims cleanly (per-occurrence dup)
todo	a partial built-in operation (List.at at 1 of 2 args) curries — completing it yields a value (should-work)
todo	a partial built-in operation (at at 1 of 2 args) curries — completing it yields a value (should-work)
todo	a partial built-in operation (slice at 2 of 3 args) curries — completing it yields a value (should-work)
todo	a partial built-in operation spelled as a NESTED spine curries identically to the flat form (should-work)
pass	a partial constructor bound in a let completes when applied
pass	a partial constructor bound in a let-tuple completes when applied to a runtime arg
pass	a partial constructor in a runtime tuple element completes via an eta-closure lift
pass	a partial constructor passed to a higher-order function completes the variant
pass	a partial constructor projected from a tuple literal completes when applied
pass	a partial constructor stored in a record field completes when applied
pass	a partial constructor stored in a runtime tuple is projected and completed by application
pass	a partial record match naming one scalar field drops the unnamed heap fields
pass	a partially applied USER function is legitimate and is not flagged as a wrong-arity built-in
pass	a partially-applied RUNTIME closure binds its residual to a let, then completes it
pass	a partially-applied function escapes as a value and runs through a recursive HOF
pass	a partially-applied operator passed to an annotated named HOF applies its remaining operand
pass	a pass-through closure that returns its arg via a trivial let composes at one element type
pass	a pass-through parameter re-passed unchanged across a tail loop is not corrupted
pass	a pattern binds a name scoped to its branch
pass	a pattern that binds the same name across nested sub-patterns is rejected
pass	a pattern that binds the same name twice is rejected
pass	a payload binder read in two different subexpressions shares its read
pass	a payload op arg feeds BOTH the resume value and the next-state through DIFFERENT operators, dispatched twice
pass	a payload projected through Option.expect with NO narrow annotation stays Int64 and runs
pass	a payload sum type declared inside a do block resolves and matches its payload
pass	a payload-in-payload nested list pattern dispatches through two recorded types
pass	a payload-typed let-binder init annotated its sum wraps in the variant constructor
pass	a payloadless enum EXPORT result crosses as a TYPED WIT enum (imposed world) — Direction A
pass	a payloadless enum PARAM member (order matches) crosses as a TYPED WIT enum — Direction B (the param twin of SHAPE 60)
pass	a payloadless enum in a record result VALUE round-trips via the run/encode envelope (no wit-world clause; typed export is a separate gap)
pass	a payloadless enum result VALUE round-trips via the run/encode envelope (no wit-world clause; typed enum export is a separate gap)
pass	a peephole rewrite rule reads in the shape of the code it rewrites
pass	a peer compound is projected and rebuilt into a fresh escaping compound (mixed ownership reclaims)
pass	a peer missing a bound op is rejected naming the op
pass	a peer op arity mismatch is rejected at compose time, not a trap
pass	a peer op returning a tuple result is projected to a scalar over the shared runtime
pass	a peer op type mismatch is rejected at compose time (same arity, different type)
pass	a peer result handle passes straight through to another peer's arg without local inspection
pass	a peer string result is consumed in-program via equality and byte-len
pass	a peer-bound operation cannot take or return a closure
pass	a peer-bound operation takes a String argument (it crosses as a runtime handle)
pass	a per-element perform in a cross-function fold threads to a read-out in the base case
pass	a perform BEFORE the self-call in a subtraction folds via the single-return path
pass	a perform BETWEEN two `?`s advances state that survives the second's cut
pass	a perform BETWEEN two sibling recursive calls threads the intervening state
pass	a perform SHORT-CIRCUITED out of an or's right operand is NOT executed (empty host-calls)
pass	a perform bound to an UNUSED binding still dispatches (DCE must not eliminate it)
pass	a perform composes as the SOURCE of a pipeline
pass	a perform crosses TWO module boundaries to the entry's handler through a re-export chain
pass	a perform in a match-arm guard is discharged by the enclosing handle
pass	a perform in a taken if-branch threads its state to the continuation after the if
pass	a perform in a taken match-arm threads its state to the continuation after the match
pass	a perform in an arm BODY (not the guard) under a handle folds and runs
pass	a perform in an if-CONDITION with a NON-performing else selected folds via the one-shot refold
pass	a perform in an if-CONDITION with a PERFORMING taken branch folds via refold + distribution
pass	a perform in the body's IF CONDITION gates a second perform in the branch
pass	a perform in the scrutinee fires exactly ONCE whichever of three arms is selected
pass	a perform result LET-bound then fed to a bin segment builds Bytes under a handler
pass	a perform threads through a NOT / boolean one-operand form
pass	a perform threads through a tuple PROJECTION one-operand form
pass	a perform under NOT in a condition (the negated dispatch gate)
pass	a perform's result flowing as the ARGUMENT of an enclosing perform threads state inner-to-outer
pass	a perform-capture through a TUPLE-returning HOF — both results carry the capture
pass	a perform-capturing closure passed to a HIGHER-ORDER helper applies twice under the handler
pass	a perform-derived byte-rope SELF-concatenated — both halves read the same draw
pass	a perform-derived list SHARED by two tuples — both readers see one allocation's content
pass	a perform-derived rope SELF-concatenated — the shared subtree measures correctly
pass	a perform-drawn quantity DIVIDES across dimensions — the meter/second quotient value
pass	a perform-drawn quantity MULTIPLIES across dimensions — meter·second product value
pass	a perform-free do-def feeding a bin-construction operand under a handler stays bound (F2)
pass	a perform-seeded inner handle composed with a SECOND same-effect instantiation after it
pass	a performed no-type operation reports one decl-site error, deduping the op-record + CDZ0401 leaks
pass	a performed non-arrow operation reports one decl-site error, not the internal op-record leak
pass	a performed operation composes as a RECORD field value that is then projected
pass	a performed operation composes under a projection and a negation
pass	a performed operation is the scrutinee of a match that dispatches on its result
pass	a performed operation whose result is a sum is matched on its variant
pass	a performed sum carrying a TUPLE payload is matched and the arm reads MULTIPLE payload elements
pass	a performing argument to a multiply-using performing callee is not duplicated
pass	a performing closure applied twice DIRECTLY in the handle body threads state per call
pass	a performing closure called TWICE observes the state advance between its calls
pass	a performing closure duplicated through a generic tuple applies twice with stepping state
pass	a performing closure homed TRANSITIVELY through a pass-through function is not falsely rejected
pass	a performing closure passed to a function that applies it UNDER a handler is homed at the apply site
pass	a performing closure passed to a helper that applies it never miscompiles (folds to 204 or declines)
pass	a performing closure stored in a TUPLE and applied IN-GUEST fires normally
pass	a performing condition's advance survives an inner abort and is read by a post-handle observer
pass	a performing do-def feeding a bin-construction operand under a handler stays bound (F2)
pass	a performing guard on a TUPLE-destructuring pattern folds
pass	a performing guard on a refutable inner pattern is CDZ0407 too
pass	a performing guard on a refutable pattern whose guard FAILS falls to the catch-all
pass	a performing guard on an irrefutable inner pattern is CDZ0407
pass	a performing guard with a WILDCARD fallback is ALSO a COMPILE ERROR (finding #9 sibling, CDZ0407)
pass	a performing helper called from TWO sites — each call site is its own dispatch
pass	a performing helper whose body BINDS the result and BRANCHES on it, called from two sites, folds
pass	a performing if-condition composes with a cross-fn fold and a trailing perform
pass	a performing match SCRUTINEE threads its state into a performing arm body
pass	a performing match-arm guard folds with WILDCARD patterns (no binder to let-bind)
pass	a performing match-arm guard on a REFUTABLE pattern folds (keeps the match, hoists the guard)
pass	a performing record nested in a TUPLE scrutinee — bound by position, record-matched after
pass	a performing scrutinee matched by a PERFORMING GUARD is a COMPILE ERROR (breaker finding #9, now CDZ0407)
pass	a pipeline chain applies its stages left to right
pass	a pipeline chain threads handler STATE left-to-right through effectful stages
pass	a plain integer list is unaffected by the sibling-inferred width range check
pass	a plain number def is unaffected by a following statement (the `as` sugar stays in its statement)
pass	a plain prelude-member typo gets the ordinary unknown-member error, not a rename hint
pass	a plain quote does not evaluate a quasiquote's unquote nested inside it
pass	a plain record compared to a nominal record of the same shape is a type error
pass	a plain-sum recursive CONSTANT BigInt literal probe matches its own constructor
pass	a plural family-unit spelling names the same unit as its singular
pass	a point fact flows through a let binding into a compare fold — the point-fact composition face
pass	a positional tuple access out of the tuple's static arity is a type error
pass	a possibly-trapping if arm evaluates only the taken branch (no eager trap on the untaken arm)
pass	a post-order effectful walk draws each node's id AFTER both children (SSA reg-alloc shape)
pass	a post-order labeling walk returns a labeled tree (heap result, id drawn after children)
pass	a pre-abort HOST call inside a NESTED strict operand is ISSUED before the abort abandons
pass	a predicate closure returning Bool drives an early-exit recursive HOF
pass	a prefix literal does not shadow a longer string arm
pass	a prefixed quantity displays scaled to its reference over BigInt (exact, no truncation)
pass	a prefixed quantity displays scaled to its reference over Float32
pass	a prefixed quantity displays scaled to its reference unit (Float)
pass	a prefixed unit combines with its base by converting to the reference (Float)
pass	a prefixed unit of a different dimension still rejects CDZ0501 (prefix scales within a dimension, never across)
pass	a prefixed-unit magnitude whose reference-scaled value JUST fits Int64 is not rejected
pass	a prelude List over-applied with two type arguments names its arity
pass	a prelude Map under-applied with one type argument names its two-argument arity
pass	a prelude Option value crosses a module boundary as an export result
pass	a prelude Qty under-applied with one type argument names its two-argument arity
pass	a prelude Qty with zero type arguments names its two-argument arity
pass	a prelude Set over-applied with two type arguments names its one-argument arity
pass	a prelude type name as an arithmetic operand names the kind boundary, not a phantom Int64
pass	a prelude-TYPE-colliding variant reached QUALIFIED still type-checks its payload
pass	a prepend-built list drains in order through head-tail matching
pass	a private constructor cannot be MATCHED from outside its module either
pass	a private helper's value escapes legitimately when an exported member calls it
pass	a private member of a nested module is not reachable through the projection chain
pass	a private module member is still visible to a sibling
pass	a private module member participates in mutual recursion with an exported sibling
pass	a private sibling defined after its exported caller still resolves
pass	a produced closure is handed back into a consumer export (the round trip)
todo	a producer capturing a host-supplied COMPOUND List parameter crosses via host→guest decode (should-work)
pass	a product of two DIFFERENT base dimensions renders as Unit.*
pass	a product type of three Qty fields with concrete units constructs and projects
pass	a product-REACHED quantity equals the directly-composed unit in either written order
pass	a program declares its own family unit and converts with it
pass	a program that delegates no effect is pure and never suspends
pass	a program that makes a host call has that call in its observable behavior
pass	a program uses a response-returning delegated host function's return value
pass	a program's syntax tree is an ordinary value walked recursively across calls
pass	a program's unary variant reusing a prelude nullary variant name is unary
pass	a projected boxed-sum accumulator survives the tail-loop step (escape, not reclaimed)
pass	a projected trapping element of a hoisted same-constructor if traps
pass	a projected tuple element IS observed, so its trap occurs (the anchor)
pass	a projection chain rooted at a parameter retains the consumed child in the callee
pass	a projection chain through a record inside a tuple retains the consumed child
pass	a projection chain through a tuple inside a record retains the consumed child
pass	a projection of an if-selected tuple selects the branch's element
pass	a projection-only runtime tuple folds to the sum of its elements
pass	a projection-only tuple over a runtime element folds to the sum of its elements
pass	a proper fraction truncates to zero from both signs and an exact integer to itself
pass	a provably-in-range left shift of a masked operand computes the guarded value (shl parity)
pass	a provably-in-range multiply of a masked operand strength-reduces and drops its guard (mul parity)
pass	a provably-trapping dropped RECORD field is elided but earns one CDZ0305 dead-trap warning
pass	a provably-trapping unused ARGUMENT is elided but earns one CDZ0305 dead-trap warning
pass	a provably-trapping unused LET init is elided but earns one CDZ0305 dead-trap warning
pass	a pure HELPER's arguments evaluate left-to-right when each performs
pass	a pure closure-driver call beside a perform in one handle body
pass	a pure lambda passed as an argument to a performing callee folds
pass	a pure let-bound lambda and a performing one are both applied in the handle body
pass	a pure non-final do-statement whose value is discarded compiles but earns a CDZ0307 discarded-value warning
pass	a pure one-hole MATCH-scrutinee whose selected arm binds and uses the scrutinee re-resolves after the splice
pass	a pure one-hole arm reading the SEED state folds against a non-zero seed
pass	a pure one-hole conditional nested inside a strict operator folds
pass	a pure one-hole continuation body reads an enclosing function parameter and folds
pass	a pure one-hole continuation folds with pure siblings around the hole
pass	a pure one-hole fold passes a PURE perform-arg the op reads
pass	a pure one-hole fold that would yield an ill-typed term is rejected, not miscompiled
pass	a pure one-hole in a FIRST let init with a later init using its binder folds
pass	a pure one-hole in a let BODY (pure init) folds
pass	a pure one-hole locates the hole NESTED several operators deep
pass	a pure one-hole locates the hole at a NON-LEADING operand
pass	a pure one-hole match-scrutinee fold selects a non-wildcard arm by the resume value
pass	a pure shared subexpression computes the same value however the backend optimizes it
pass	a purely-logical composed proof over a quantified proposition: DISCH then MP
pass	a push-loop tuple-list accumulator infers its tuple element component types
pass	a qualified (. Ast Ctor) pattern binds each variant's payload at its own type
pass	a qualified construct and a bare match name the same colliding variant
pass	a qualified nullary variant applied to a payload names the bare variant
pass	a qualified nullary variant pattern resolves in a nested match on the same scrutinee
pass	a qualified withheld-constructor pattern over an abstract value is rejected CDZ0214
pass	a quantity annotation at a same-unit out-of-range magnitude is rejected
pass	a quantity annotation at an in-range narrow width grounds and accepts
pass	a quantity annotation range-checks the inner width — an out-of-range magnitude is rejected
pass	a quantity annotation rejects a negative magnitude at an unsigned inner width
pass	a quantity built from an extracted unit is dimensionally checked
pass	a quantity captured in a closure env adds against per-call quantities
pass	a quantity in a TUPLE inside a LIST element renders scaled (nested composition)
pass	a quantity in a recursive sum payload folds through construction and match
pass	a quantity in an OPTION inside a MAP value renders scaled (nested composition)
pass	a quantity is constructed from a numeric value and a unit
pass	a quantity over a BigInt magnitude compares by its exact value
pass	a quantity over a BigInt magnitude runs unbounded arithmetic on the erased handles
pass	a quantity over a Rational magnitude compares by its exact value
pass	a quantity over an exact rational magnitude divides exactly and derives its dimension
pass	a quantity raised to the zeroth power is a dimensionless one
pass	a quantity read from a map COMBINES with a fresh same-dimension quantity
pass	a quantity whose reference-scaled magnitude overflows its inner Int is rejected
pass	a quantity's erased value is the identical numeric value the bare literal has
pass	a quasiquote pattern dispatches on the head symbol of a runtime Ast
pass	a quasiquote with too many operands is rejected with a delete-the-surplus fix
pass	a quote carrying a symbol literal reifies to an Ast.Symbol value
pass	a quote inside a tagged-template hole reaches the tag function as an Ast value
pass	a quote of a char literal reifies to an Ast.Char value
pass	a quote pattern binds an unquoted operand of a compound form
pass	a quote pattern is equivalent to the Ast.* constructor pattern
pass	a quote pattern matches a fixed arity
pass	a quote pattern used to recognize a compiler form reads as that form
pass	a quote pattern's Int literal does not match a Float literal of the same value
pass	a quote pattern's literal operand is position-sensitive
pass	a quote pattern's literal operand matches only the exact literal
pass	a quote with too many operands is rejected with a delete-the-surplus fix
pass	a quote-built and constructor-built AST of the same tree encode to identical bytes
pass	a quote-built and constructor-built FLOAT AST encode to identical bytes
pass	a quote-built and ctor-built equal Ast collide as ONE Set element among 40 at CHAMP depth
pass	a quote-parameter macro receives its argument as unevaluated Ast and returns it (identity expansion)
pass	a quote-pattern match with no catch-all is non-exhaustive
pass	a quoted boolean equals the same node built by the Ast.Bool constructor
pass	a quoted collection or member access equals the node built by its dedicated Ast ctor, never name-headed
pass	a quoted compound form containing a boolean reifies with an Ast.Bool element
pass	a quoted compound form containing a string reifies with an Ast.Str element
pass	a quoted compound form equals the same AST built by the Ast.List constructor
pass	a quoted def form is inert data and is not flagged as a malformed declaration
pass	a quoted float equals the same node built by the Ast.Float constructor
pass	a quoted float is distinct from the same magnitude quoted as an integer
pass	a quoted form's head reifies as a Name while its string argument is a Str
pass	a quoted integer equals the same node built by the Ast.Int constructor
pass	a quoted name equals the same node built by the Ast.Name constructor
pass	a quoted string equals the same node built by the Ast.Str constructor
pass	a quoted string is distinct from the same text quoted as a name
pass	a radix literal carries the R (Rational) suffix
pass	a radix literal with a bad digit is a malformed literal, not an unbound name
pass	a radix literal with an empty body is a malformed literal, not an unbound name
pass	a rate derived by division mixes with a named rate unit of the same dimension
pass	a rational built from RUNTIME operands normalizes its sign onto the numerator
pass	a rational built from a RUNTIME zero denominator traps rather than being a value
pass	a rational built from a negative denominator reads a negative numerator (sign on top)
pass	a rational built from a negative denominator reads a strictly positive denominator
pass	a rational ceilings a sub-one positive to 1, a negative non-whole toward zero, and an exact integer to itself
pass	a rational ceilings toward positive infinity, narrowed to Int64
pass	a rational floors a positive non-whole down, a sub-one negative to -1, and an exact integer to itself
pass	a rational floors toward negative infinity, narrowed to Int64
pass	a rational is normalized to lowest terms on construction
pass	a rational moves a negative denominator's sign to the numerator (canonical sign placement)
pass	a rational of Int64 minimum over Int64 minimum normalizes to one
pass	a rational operation does not silently promote an integer operand
pass	a rational rounds a negative half away from zero
pass	a rational rounds small halves away from zero and leaves a whole rational unchanged
pass	a rational rounds to nearest, ties half-away-from-zero, narrowed to Int64
pass	a rational truncates a negative value toward zero, not toward negative infinity
pass	a rational truncates to its integer part toward zero, narrowed to Int64
pass	a rational with near-Int64 numerator and denominator normalizes by gcd exactly
pass	a rational with numerator and denominator both negative normalizes to a positive value
pass	a rational with numerator and denominator both negative normalizes to positive
pass	a rational with the Int64 minimum as denominator normalizes past the i64 sign-flip
pass	a rational's denominator reads the reduced pair's positive denominator at runtime
pass	a rational's denominator reads the reduced pair's positive denominator, folded from a constant
pass	a rational's numerator and denominator are read as BigInt from the normalized pair
pass	a rational's sign is normalized onto the numerator
pass	a rational-magnitude quantity erases to its exact rational value
pass	a re-declared same-name theorem type in another module is a distinct type a kernel value does not satisfy
pass	a re-masked UInt8 wrapping-add result compares EQUAL to its modular value at the narrow width
pass	a re-performing escaping continuation over a `do`-sequenced body re-installs its handler at apply
pass	a reachable constant divide-by-zero in a runtime branch earns a CDZ0309 warning naming the trap kind
pass	a reachable constant overflow in a runtime branch earns a CDZ0309 warning naming the trap kind
pass	a reachable constant shift-out-of-range in a runtime branch earns a CDZ0309 warning naming the trap kind
pass	a reachable malformed literal reports exactly once, not doubled
pass	a reachable unbound name found by two passes is reported exactly once
pass	a read result destructures through nested Ast patterns — the analyze path computes
pass	a read-modify-write of a List value in a Map grows the entry and leaves the original map unchanged
pass	a reader literal carries a qualified name with a dot
pass	a reader→writer PUMP then a LET-bound flush reads the FULL accumulation (breaker tk3d class)
pass	a reciprocal dimension renders as Unit.one over the unit
pass	a recognized pragma with a malformed argument list is rejected
pass	a recognized string escape denotes its one scalar value
pass	a record ARGUMENT missing fields carries an add (insert) fix with a placeholder per missing field
pass	a record ARGUMENT with a lone surplus field carries a delete fix
pass	a record BUILT from a const param then read through a function const-folds
pass	a record MATCH pattern with a LITERAL field sub-pattern dispatches on the field value
pass	a record STATE with a Set field rebuilt per dispatch — dedup observed through the field
pass	a record TYPE with a duplicate field name is a type error
pass	a record WILDCARD trailing-rest binding param binds the named field and discards the residual
pass	a record annotated with the wrong field type is rejected
pass	a record binding pattern admits a wildcard field value
pass	a record binding pattern binds fields by name, out of order and partial
pass	a record binding pattern in a let destructures by field
pass	a record binding pattern in a parameter destructures by field
pass	a record binding pattern may leave a field's value a wildcard
pass	a record binding pattern naming an absent field is rejected
pass	a record binding pattern over a runtime record reads fields at run time
pass	a record binding pattern's field binders feed a later binding in the same let
pass	a record carrying a field the annotation does not name is rejected
pass	a record carrying an Ast-typed field materializes as a WHOLE runtime value and its Ast field is matched
pass	a record construction spread after an inline field overrides it (spread wins)
pass	a record construction spread splices a runtime record's fields (disjoint)
pass	a record construction spread with an overriding field is last-writer-wins
pass	a record field VALUE referencing the parameter still substitutes
pass	a record field VALUE still reads the parameter it names after the call
pass	a record field binder shadowing a param in a called def binds the field
pass	a record field built by a RUNTIME BRANCH holds a user-sum, projected then matched
pass	a record field holding a sum is projected then matched
pass	a record field holding a via-remove map equals the direct-field record
pass	a record field key coinciding with a parameter name survives the call, projected
pass	a record field key that coincides with a parameter name survives the call
pass	a record field with no value expression is rejected, not a crash
pass	a record is built and consumed inside the arm (structural product per dispatch)
pass	a record is constructed and a field is read
pass	a record key with a SET field matches by set content across build orders
pass	a record key with a float-bearing TUPLE field keys by content
pass	a record map key with a Rational field matches by the field's normalized form
pass	a record match arm + a wildcard alternative selects by shape
pass	a record match field may be a LITERAL that probes the field by equality
pass	a record match pattern binds fields by name — partial and out of order
pass	a record match pattern destructures a record scrutinee by field
pass	a record match pattern typo field suggests the nearest field with a rename fix
pass	a record missing a field the annotation names is rejected
pass	a record missing a required field names the missing field
pass	a record missing two fields names both, plural and sorted
pass	a record of quantities RETURNED as a value renders every field scaled to its reference
pass	a record op argument with a HEAP field crosses the perform and its scalar field accumulates state
pass	a record open-row rest is IRREFUTABLE, so it binds in a BINDING-PARAM position (not only a match arm)
pass	a record param carrying a bytes leaf beside a scalar compiles and runs (via an imposed WIT world)
pass	a record param field is read by NAME when the WIT field order is not name-lexicographic (via an imposed WIT world)
pass	a record param interface export builds the record via the boundary wrapper and runs (via an imposed WIT world)
pass	a record pattern destructures a ROW-OP derived record (with-chain result)
pass	a record pattern over a Map-STORED record reads live heap fields after retrieval
pass	a record pattern with a trailing rest binds the named fields and the residual record
pass	a record reached VIA Record.with keys a Map like the directly-built record
pass	a record reached via a with-CHAIN keys like the final direct build, generations intact
pass	a record result field is written by NAME when the WIT field order is not name-lexicographic (via an imposed WIT world)
pass	a record scrutinee is bound whole by a match binder
pass	a record sub-pattern nested inside a tuple match binds its field (nested-record match binder)
pass	a record sub-pattern nested under a variant, tuple, or list binds its field by name at the real width
pass	a record threaded through a record-param function const-folds
pass	a record threaded through self-recursion with per-step Record.with leaves the SEED intact
pass	a record trailing-rest pattern binds in a LET binder position, like a fn parameter
pass	a record variant payload mentioning a type param does not false-positive
pass	a record whose field is a List projects the list handle and indexes it, alongside a scalar field
pass	a record whose field is a runtime tuple nests across the boundary
pass	a record whose field is a tuple with a Float64 leaf is a Map key found by content
pass	a record whose field is misnamed is both missing and extra
pass	a record with a LIST field crosses resume — the body projects and folds the collection field
pass	a record with a SET field as op ARGUMENT — the arm probes the collection beside the scalar
pass	a record with a duplicate field name is a type error
pass	a record with a non-adjacent duplicate field name is a type error
pass	a record with a runtime field is returned as a program result
pass	a record with an extra field names the field the type has no place for
pass	a record-LITERAL scrutinee whose fields perform, destructured by a `(record …)` arm, DECLINES (breaker finding #8)
pass	a record-field loop invariant consumed per iteration accumulates stably
pass	a record-keyed trie churned back equals the direct build with the seed resolving
pass	a record-parameter export returns a tuple computed from its fields
pass	a record-returning closure alongside a parameterized plain export — the closure
pass	a record-returning closure alongside a parameterized plain export — the plain
pass	a record-type mismatch message renders the type capitalized, matching the annotation spelling
pass	a record-with-a-MIXED-WIDTH-variant-field host arg emits, loads, and runs (via an imposed WIT world)
pass	a recursion carrying a Bytes parameter does a fallible read in its base arm
pass	a recursion carrying a List parameter does a fallible read in its base arm
pass	a recursion that builds Bytes via Bytes.concat const-folds byte-exact
pass	a recursion that builds a Map collection state const-folds a key lookup
pass	a recursion that builds a Map collection state const-folds its length
pass	a recursion that builds a Set collection state const-folds its length
pass	a recursion that builds a fresh RECORD each step const-folds a field read
pass	a recursion-driven host-call sequence consumes one response row per iteration in order
todo	a recursive Ast walk via a List.fold closure over the sub-trees DECLINES cleanly (no compile overflow)
pass	a recursive BigInt fn whose bound result feeds a mod compiles and computes (FINDING)
pass	a recursive CONSTANT-FOLD pass rewrites int-add subtrees bottom-up and counts folds
pass	a recursive HOF applies a bare operator passed as its function argument
pass	a recursive HOF infers a callback whose RESULT is a sum matched in the body
pass	a recursive MERGE SORT splits alternately, sorts halves, and merges — duplicates survive
pass	a recursive NEWTYPE traversal recurses on its projected recursive field
pass	a recursive NEWTYPE-wrapped linked list folds to a scalar
pass	a recursive RENAME pass rewrites every matching Name leaf at any depth and counts them
pass	a recursive TLV frame walk over sliced runtime bytes decodes LE payloads per frame
pass	a recursive TREE as a transformer op — crosses IN, the arm wraps it, crosses back OUT
pass	a recursive abortive callee as the handle-body TAIL folds to the arm value
pass	a recursive abortive callee feeding a PENDING handle-body continuation abandons it (folds to 507)
pass	a recursive bool predicate types as Bool with the self-call in either branch order
pass	a recursive builder PERFORMS per step and a recursive pure fold consumes the built list
pass	a recursive byte fold calling two helpers emits valid wasm (disjoint scratch slots)
pass	a recursive byte walk sums a runtime sequence via Bytes.at and match
pass	a recursive byte walk threading a Bytes parameter through a helper infers without annotation
pass	a recursive call in TAIL position ascribed to a NARROWER int width wraps its result (valid wasm)
pass	a recursive callee drawing the SAME op twice per round threads the out-state to a trailing observer
pass	a recursive callee performing TWO effects per round threads both slots' out-state to a trailing observer
pass	a recursive combinator applies a captured closure a RUNTIME number of times
pass	a recursive const (Option Int64) param reaching a trap folds to CDZ0304, not a runtime hang
pass	a recursive const (Option Int64) param with a VALUE base case folds to the value
pass	a recursive consumer of a dictionary record inlines and erases the dictionary
pass	a recursive def computes over its argument
pass	a recursive def named a target-language keyword runs
pass	a recursive def returning a closure is applied directly as the call head
pass	a recursive def with a match base case computes over its argument
pass	a recursive do-local function nested in an inlined helper recurses
pass	a recursive do-local function survives two inlinings of its helper
pass	a recursive drop-byte walk rebinding its Bytes param to a slice-concat converges
pass	a recursive drop-scalar walk over a rope converges (string-shrinker shape)
pass	a recursive effectful walk BUILDS a list as its return value, one fresh element per step
pass	a recursive effectful walk accumulates into a STRING-state handler
pass	a recursive effectful walk under a state-threading handler compiles without leaking an internal name
pass	a recursive emitter dispatches on a sum's variants to build bytes per node
pass	a recursive evaluator destructures a built Ast by quote patterns and recurses
pass	a recursive fn threads TWO nested stateful handlers' states at once
pass	a recursive fn under a two-arm single-effect handler specializes with one state param
pass	a recursive fold SUMS a list of same-unit quantities threading a Qty accumulator
pass	a recursive fold infers a callback applied to the RECURSIVE-CALL RESULT
pass	a recursive fold of a cons-list to bytes is the whole program result
pass	a recursive fold over a runtime Peano spine recovers its depth
pass	a recursive fold over a sum list infers its unannotated callback parameter
pass	a recursive fold returns a MIXED-representation tuple destructured at the caller
pass	a recursive fold with an unannotated two-argument callback parameter
pass	a recursive function counts down through a stateful effect and bails at zero
pass	a recursive function folds a linked list of runtime-determined length
pass	a recursive function folds a runtime linked list to a scalar
pass	a recursive function in a nested module runs through the projection chain
pass	a recursive function over a generic recursive sum is monomorphized per element type
pass	a recursive function sums a range it walks by performing a fresh-index effect
pass	a recursive function that installs a fresh handler on each call grows its context without bound
pass	a recursive function that threads a tuple accumulator returns it
pass	a recursive function threads two nested handlers' states at once
pass	a recursive function with an annotated parameter walks and bails through an abortive handler
pass	a recursive generic function called at one type twice shares a single instantiation
pass	a recursive generic function is instantiated at three distinct machine shapes
pass	a recursive generic function is instantiated at two different types
pass	a recursive generic function threading another generic is itself generic at two types
pass	a recursive generic with a type-valued parameter monomorphizes per type, erasing the type argument
pass	a recursive guard in a negated-connective shape stays correct
pass	a recursive linked-list fold leaks every heap node (O(N) reclaim gap)
pass	a recursive list accumulator grown by push and returned in the base arm stays a list
pass	a recursive list consumer whose element is inferred solely through List.at iterates by index
pass	a recursive list-map reusing a destructured sum payload beside an i64 let is disjoint-slotted
pass	a recursive list-match called in an IF-branch inside a match arm builds on all backends
pass	a recursive list-match in an arm returning a tuple of TWO lists (no scalar) builds on all backends
pass	a recursive list-match in an arm returning a tuple of a scalar and two lists builds on all backends
pass	a recursive list-match in an arm whose arms return a LIST builds on all backends
pass	a recursive local function that captures TWO enclosing parameters is lambda-lifted and runs
pass	a recursive local function that captures its enclosing parameter is lambda-lifted and runs
pass	a recursive loop performs to TWO handlers per iteration with independent states
pass	a recursive loop whose CONDITION performs — each iteration RE-dispatches (never hoisted)
pass	a recursive lower from a sum to Bytes assembles its output in match arms
pass	a recursive map rebuilding a sum list infers its unannotated callback
pass	a recursive match binder scrutinee is materialized once (linear, not 2^depth)
pass	a recursive match binder used twice over a heap-list sum payload projects a live field
pass	a recursive multiplication overflow traps across the call chain
pass	a recursive nested-op performer whose per-iteration outer advance is read by a POST-loop observer threads it
pass	a recursive nested-op performer whose resume-VALUE reads the inner state around the outer perform folds (the inner state re-binds to the slot param)
pass	a recursive newtype escapes to the host tagged with its own name at each recursion point
pass	a recursive param used only as a call argument infers its type from the callee
pass	a recursive parameter used only as a call argument infers from the callee's parameter type
pass	a recursive parser drains a STREAM of length-prefixed frames to the empty base case
pass	a recursive performer of a nested-handler op whose resume performs the outer effect threads the advance
pass	a recursive performer whose recursion result feeds a HELPER call folds — no slot-less spec-body param
pass	a recursive performer whose recursion result is fed DIRECTLY to a helper arg folds
pass	a recursive performer whose self-call arg calls a non-recursive helper that PERFORMS the discharged op folds
pass	a recursive predicate with the self-call in the else branch is a Bool condition
pass	a recursive predicate with the self-call in the then branch is a Bool condition
pass	a recursive re-wrap of a matched owned sum child accumulates shells -- known gap, DEEP (6 frames, leaks strictly more)
pass	a recursive re-wrap of a matched owned sum child accumulates shells -- known gap, SHALLOW (2 frames)
pass	a recursive reader decodes a CBOR application tree and evaluates it by head index
pass	a recursive resolver transforms one runtime sum tree into another, then consumes it
pass	a recursive resolver whose dead arm builds a trapping compound compiles
pass	a recursive round with a let draw and a bare do-discarded draw threads the out-state to a trailing observer
pass	a recursive same-effect advance observed by an ABORTIVE arm — must fold to the advanced state or decline, never the pre-recursion seed (sr5)
todo	a recursive scalar-walk classifies characters across a multibyte String entry arg
pass	a recursive scalar-walk over a guest-built rope classifies characters on every backend
pass	a recursive scan counts a runtime string's matching characters
pass	a recursive sum as op ARGUMENT — the arm dispatches on its shape
pass	a recursive sum built then folded by a NESTED recursive call composes on every backend
pass	a recursive sum carrying a Float32 renders its full runtime spine
pass	a recursive sum carrying a Float64 renders its full runtime spine
pass	a recursive sum carrying a String renders its full runtime spine
pass	a recursive sum constructor with a CHECKED-ARITH payload and a recursive-call sibling payload
pass	a recursive sum consumer whose arguments are recursive sum producers compiles
pass	a recursive sum recurses THROUGH a record-typed payload
pass	a recursive sum summing two self-call BigInt results types the arm as BigInt and folds
pass	a recursive sum type works with pattern matching
pass	a recursive tag builds a compound Ast.List at compile-time fold
pass	a recursive tag function const-folds to a compile-time Ast
pass	a recursive tag scans its chunk text byte-by-byte and folds to a constant Ast
pass	a recursive transform composed over Ast.module const-evaluates at compile time
pass	a recursive transformer passing its element to an incompatibly-typed callee is rejected
pass	a recursive unifier distinguishes arrow components through the payload slots
pass	a recursive user sum built in a lib is folded by the entry over the imported type
pass	a recursive user sum type is built at run time and renders its variant names
pass	a recursive walk consulting a node's own and its child's String key computes correctly
pass	a recursive walk over a runtime-SHAPED tree dispatches per call
pass	a recursive walk over the built-in Ast recurses into its List payload via a rest pattern
pass	a recursive walk threads THREE nested handlers' states at once
pass	a recursive walk threads TWO effects at once — a fresh-index counter and a running total
pass	a recursive walker applies two different capture closures per element
pass	a recursive-call result ascribed to a wider int width is coerced when used as an arithmetic operand
pass	a recursive-generic filter threading a predicate closure composes at two element types
pass	a recursive-generic flatten over a nested generic iterator composes at two element types
pass	a recursive-generic flatten over a nested generic iterator monomorphizes and runs at one element type
pass	a recursive-generic producer composes at two element types (the tied result var)
pass	a recursive-generic producer over a list of its own generic-producer results monomorphizes and runs
pass	a recursive-generic producer wrapping elements in a user sum is consumed at one type
pass	a recursive-generic producer's element is usable at each instantiation
pass	a recursive-generic producer's result is consumed by an element-typed consumer
pass	a recursive-generic transformer composes with itself
pass	a recursive-generic transformer maps a closure to an aggregate result at two distinct domains
pass	a recursive-generic transformer threading a closure composes at two element types
pass	a recursive-generic transformer threading an IDENTITY closure composes at TWO element types
pass	a recursive-generic transformer threading an IDENTITY closure composes at a single element type
pass	a recursive-generic transformer with a bare-Nil STOP branch composes at two element types
pass	a recursive-generic transformer with an element-CHANGING closure ties domain to codomain
pass	a recursive-sum payload projected more than once folds to a scalar
pass	a recursively-built MULTI-PAYLOAD-variant list renders its spine FLAT
pass	a recursively-built binary tree renders its full runtime structure
pass	a recursively-built byte sequence assembles its bytes at run time
pass	a recursively-built linked list renders its full runtime spine
pass	a reduce-shaped wrapper seeding a second generic helper at two element types monomorphizes via the transitive-binder tie
pass	a reducer emitting no effects builds an empty-requests step and runs (via an imposed WIT world)
pass	a reducer performing a RECORD host import reads a field of the result (via an imposed WIT world)
pass	a reducer performing a scalar host import threads the u64 result into the step (via an imposed WIT world)
pass	a redundant full-width mask on an unsigned runtime value is elided
pass	a redundant relational check inside its own truthy branch is known-true and its dead branch is eliminated
pass	a reference buried under many non-binding forms resolves to its outer let binder
pass	a reference to an unbound name is rejected before running
pass	a reference-unit magnitude at Int64 max is not rejected (no scale to overflow)
pass	a referenced let binding whose init overflows IS observed, so its trap occurs (the anchor)
pass	a refined-newtype shrink stays IN-DOMAIN — it converges to the minimal value the invariant still admits, not below the floor
pass	a refining arm shadowed by an earlier full-variant cover is unreachable and earns a CDZ0213 warning
pass	a reflected module AST is a well-formed Ast value that round-trips through encode/decode
pass	a reflected quantity type rejects a value of a different dimension
pass	a reflected type-definition AST round-trips byte-identically through Ast.encode / Ast.decode
pass	a reframed packet with a TRANSFORMED header compares byte-equal to its independent twin
pass	a refutable constructor def-parameter is rejected
pass	a refutable constructor parameter on an inline lambda is rejected
pass	a refutable constructor pattern in a let binder is rejected
pass	a refutable let inside an inline lambda body is rejected
pass	a refutable let nested two inline-lambda levels deep is rejected
pass	a refutable literal list element does NOT count toward length-coverage — a catch-all is still required
pass	a refutable map value sub-pattern binds the ctor payload on a matching value
pass	a refutable map value sub-pattern dispatches by the value's constructor
pass	a refutable map value sub-pattern falls through on a non-matching variant
pass	a refutable map value sub-pattern folds the same over a constant map
pass	a refutable-ctor list element still requires a catch-all covering every length
pass	a region's RESULT seeds a second same-effect region with a DIFFERENT arm shape
pass	a reified (sum-of-step-shapes) lazy pull-iterator compiles, runs, and is lazy
pass	a remainder by a constant bounds its result so a downstream checked op sheds its guard (value + trap parity)
pass	a rename pass DERIVES each new Name from the old payload (concat suffix), quote-verified deep
pass	a repeated condition in a nested if collapses to the outer condition (value parity)
pass	a repeated condition inside a branch propagates the known truth value
pass	a repeated conjunct absorbs to the plain conjunction
pass	a repeated disjunct absorbs to the plain disjunction
pass	a repeated do-local declaration shadows the earlier one for what follows
pass	a repeated float comparison is computed once and reused (value-transparent CSE)
pass	a repeated let binding shadows the earlier one for what follows
pass	a repeated runtime checked-multiply is computed once and still traps on overflow
pass	a repeated runtime subexpression is computed once (CSE) and reused across an operator
pass	a repeated same-index List.at shared by CSE leaves the list live for a later length read
pass	a repeated same-key Map.lookup shared by CSE leaves the map live for a later size read
pass	a repeated trapping divide in a short-circuited and's right operand stays shielded (CSE must not hoist it past the connective)
pass	a repeated trapping divide in a short-circuited or's right operand stays shielded (CSE must not hoist it past the connective)
pass	a repeated trapping divide inside one if-arm is not speculated past the branch
pass	a repeated trapping divide inside one match arm is not hoisted past the match
pass	a repeated trapping division in a guarded arm stays shielded — a CSE class must not hoist past the branch
pass	a rest binding with a refutable literal leading element is rejected
pass	a rest parameter that is not last is rejected
pass	a rest-binder over a runtime-start slice measures the SLICE length, not the parent's
pass	a rest-pattern head binder read by an inlined match-arg callee resolves
pass	a rest-pattern head binder read inside an inlined match-arg callee's scrutinee resolves
pass	a rest-pattern list match missing the empty case adds the SPECIFIC empty-list arm, not a wildcard
pass	a result<Bytes, enum> param field is read and rebuilt by the wrapper on Ok and Err arms (via an imposed WIT world)
pass	a result<record,record> EXPORT result crosses — BOTH arms carry a compound payload
pass	a result<record,s64> EXPORT result crosses — a COMPOUND ok payload (declared world)
pass	a result<s64,s64> EXPORT result crosses as a typed WIT result (declared world)
pass	a resume missing its next-state is rejected
pass	a resume outside any handler arm is rejected
pass	a resume value that MATCHES a COMPOUND scrutinee over the growing state binds it once — finding-24 match-scrutinee face
pass	a resume with too many operands is rejected with a delete-the-surplus fix
pass	a resuming arm whose resume VALUE is a transform of state and next-state is a distinct advance, observed twice
pass	a resuming observer of a recursive same-effect advance reads the advanced state (sr4)
pass	a resuming perform in an if-condition threads its state advance to the taken branch
pass	a retired collection-op name is rejected with a rename fix-it (CDZ0603)
pass	a returned String with a backslash and a quote RENDERS re-escaped (canonical value form)
pass	a returned capturing closure bound with let and applied folds like the inline form
pass	a returned capturing closure bound with let and applied twice folds each application independently
pass	a returned lambda captures+applies a closure param bound to a TOP-LEVEL def
pass	a returned lookup result consumed with a wildcard never inspects the payload
pass	a returned multi-parameter capturing closure applied at full arity (flat) folds
pass	a returned multi-parameter capturing closure applied curried folds
pass	a returned runtime string with a multi-byte scalar renders the scalar verbatim
pass	a returned runtime string with a non-printable scalar renders it verbatim, not a u-escape
pass	a reused rest binder across sibling rest-lists is non-linear
pass	a reverse wrapper seeding a second generic helper with an accumulator at two element types declines pending the transitive tie
pass	a rewrite iterates to a FIXPOINT detected by structural equality and fully collapses a nest
pass	a rewrite-then-eval pipeline over a runtime tree preserves meaning through the rewrite
pass	a right shift by a floating-point count does not silently promote
pass	a right shift by the bit width or more traps rather than masking
pass	a right shift of a negative by a RUNTIME amount stays arithmetic
pass	a rope String nested in a tuple compares equal to its flat twin and reclaims both tuples (no live objects)
pass	a rope accumulates across perform/resume boundaries and content-checks at the exit
pass	a rope built before a perform survives the resume and the arm's own heap does not leak into it
pass	a rope in a record field equals its flat-twin field
pass	a rope in a sum payload inside a record field equals its flat twin
pass	a rope in an Option payload equals its flat-twin payload
pass	a rope label rides a pqueue entry through insort to its time-ordered slot
pass	a rope threads TWO chained if-selects, each operand multi-use, and every length stays live
pass	a rope two levels deep (tuple in a tuple) equals its flat twin
pass	a rope-built String key probes the trie entry stored under its FLAT twin at depth
pass	a round trip releases the closure cell even when the consumer ignores it
pass	a round trip releases the produced closure cell (no live objects)
pass	a round-trip alongside a parameterized plain export applies its argument
pass	a round-trip alongside a plain export — the plain export runs
pass	a round-trip alongside a plain export — the round-trip still works
pass	a round-trip at a widened scalar width (UInt32)
pass	a round-trip consumer applies a closure whose result is a nominal-over-scalar
pass	a round-trip consumer applies the closure in a match wildcard arm
pass	a round-trip consumer applies the closure only in the taken if-branch
pass	a round-trip consumer binds the applied closure through a let
pass	a round-trip consumer takes TWO produced closure params
pass	a round-trip consumer takes the non-applying match arm
pass	a round-trip consumer takes the produced closure in a NON-LEADING param position
pass	a round-trip consumer that does NOT apply the closure on the taken branch
pass	a row op over a constant record folds through a multi-use let binding
pass	a row op over a constant record folds through a single-use let binding
pass	a row-op pipeline — merge then without then two projections — over runtime values
pass	a row-polymorphic accessor applies a FN field across two record widths
pass	a run's result is a deterministic function of a host call's recorded response
pass	a run-sim scheduler fast-forwards the clock through two sequential sleeps in one task
pass	a run-sim task's suspension count is DATA-DRIVEN by a list of sleep durations
pass	a run-time-index String.at result compares equal to the same character as a literal
pass	a run-time-index scalar read is present in bounds and absent out of bounds
pass	a runtime -0.0 float leaf two levels deep stays distinct from positive zero
pass	a runtime = on a NON-recursive sum with a Float payload walks it on both backends
pass	a runtime = on a RECURSIVE-through-List sum walks runtime-built trees on every backend
pass	a runtime = on a sum recursive through a RECORD-of-TUPLE payload walks structurally
pass	a runtime = over MUTUALLY-recursive sums walks across the type cycle
pass	a runtime Ast value round-trips through the R2 value codec (Value.encode/decode)
pass	a runtime BEYOND-64-bit BigInt map key looks up by exact multi-limb value
pass	a runtime BRANCH selects between two named functions and the selected one applies
pass	a runtime BigInt absent from a Set is not found
pass	a runtime BigInt accumulator threaded through recursion grows past Int64
pass	a runtime BigInt comparison sees an intermediate that overflows Int64
pass	a runtime BigInt divide crosses the host boundary as its truncated quotient
pass	a runtime BigInt divide over negative operands truncates toward zero
pass	a runtime BigInt in an Option payload crosses the host boundary
pass	a runtime BigInt intermediate that overflows Int64 does not trap
pass	a runtime BigInt is found as a Set element by value
pass	a runtime BigInt narrowed to Int64 at the EXACT maximum fits and one past traps
pass	a runtime BigInt narrowed to Int64 at the EXACT minimum fits and one below traps
pass	a runtime BigInt operation does not silently promote a fixed-width operand
pass	a runtime BigInt ordering compares by the arbitrary-precision value
pass	a runtime BigInt remainder takes the dividend's sign
pass	a runtime BigInt three-way compare agrees with the boolean ordering
pass	a runtime BigInt three-way compare reports Equal at a BEYOND-Int64 magnitude
pass	a runtime BigInt three-way compare reports equality as Equal
pass	a runtime BigInt wrapped in Ast.Int round-trips and reclaims the boxed payload on drop (no live objects)
pass	a runtime BigInt-inner quantity used as a Map key hits, and a different magnitude misses
pass	a runtime Bool host arg crosses the boundary and drives two response bindings
pass	a runtime Bool-payload ctor binder threads its Bool through construction and destructure
pass	a runtime Bytes host arg crosses as list<u8> and the host call returns its response
pass	a runtime Bytes host-arg BEFORE a scalar arg keeps distinct core slots (no width-clobber)
pass	a runtime Bytes match without a catch-all is non-exhaustive
pass	a runtime Bytes rope compares equal to its flat twin and leaves no live heap objects
pass	a runtime Bytes rope in a SUM payload compares equal to its flat twin
pass	a runtime Bytes rope in a record field compares equal to its flat twin
pass	a runtime Bytes rope is found as a Set element by its flat twin
pass	a runtime Bytes rope map key is looked up by its flat twin
pass	a runtime Bytes rope nested in a tuple compares equal to its flat twin
pass	a runtime Bytes value crosses a host op boundary as list<u8> (H-bytes-arg)
pass	a runtime Bytes value dispatches on byte-string literals
pass	a runtime Bytes value exposes a repeatable is-empty member (call-method)
pass	a runtime Bytes value exposes a repeatable len member (call-method)
pass	a runtime Bytes value exposes a to-bytes member returning the raw payload (call-method)
pass	a runtime Bytes value's encode member still renders after other member calls (call-method)
pass	a runtime Bytes value's is-empty member crosses (call-method, bool result)
pass	a runtime Bytes value's to-bytes member crosses (call-method, raw bytes result)
pass	a runtime Bytes.at byte-sum reads each byte and terminates on the out-of-bounds None
pass	a runtime Bytes.compact preserves the content length
pass	a runtime Bytes.concat length is the sum of operand lengths
pass	a runtime Bytes.of value is equality-comparable (the runtime-Bytes control)
pass	a runtime Bytes.slice in bounds yields Some of the sub-length
pass	a runtime Bytes.slice out of bounds is None
pass	a runtime FLOAT width nested in a compound annotation is rejected
pass	a runtime Float quantity comparison against NaN is the IEEE partial order (false)
pass	a runtime Float32 addition emits the f32 machine add
pass	a runtime Float32 division emits f32.div
pass	a runtime Float32 equality compares by the canonical byte form
pass	a runtime Float32 indeterminate op yields nan at binary32 width
pass	a runtime Float32 multiplication emits f32.mul
pass	a runtime Float32 subtraction emits f32.sub
pass	a runtime Float64 equality compares by the canonical byte form
pass	a runtime Int16 division of the minimum by -1 overflows and traps
pass	a runtime Int16 wrapping-add overflow wraps with sign-extension (32767 + 1 = -32768)
pass	a runtime Int32 division of the minimum by -1 overflows and traps
pass	a runtime Int32 division of the minimum by -2 is a normal division
pass	a runtime Int64->UInt64 .of range-checks the lower bound (nonneg passes, negative traps)
pass	a runtime Int8 division in range computes and division by zero traps
pass	a runtime Int8 division of the minimum by -1 overflows and traps
pass	a runtime Int8 wrapping-add overflow wraps with sign-extension (127 + 1 = -128)
pass	a runtime Int8 wrapping-mul overflow wraps with sign-extension (-128 * -1 = -128)
pass	a runtime LIST argument to an effect op is WALKED by a recursive fold inside the arm
pass	a runtime List built past 32 elements reads correctly by index across the RRB leaf-to-trie boundary
pass	a runtime List of Option-BigInt round-trips each Some/None payload through List.at
pass	a runtime List.update at exactly the length index is the first out-of-bounds and traps
pass	a runtime MIXED-SCALE BigInt comparison converts to the reference before comparing
pass	a runtime MIXED-SCALE BigInt-magnitude sum converts to the reference in unbounded arithmetic
pass	a runtime MIXED-SCALE Rational-magnitude sum converts to the reference EXACTLY
pass	a runtime Map built by folding Map.insert over a list of entries dedups keys last-write-wins
pass	a runtime NaN equals a runtime NaN under the canonical byte form
pass	a runtime NaN float leaf two levels deep (a record-of-tuple) compares equal by the canonical byte form
pass	a runtime NaN leaf in a tuple compares equal, a runtime -0.0 leaf stays distinct
pass	a runtime Option carrying a user sum is matched by a nested constructor pattern
pass	a runtime RECURSIVE Ast (a list of nodes) round-trips through the R2 value codec
pass	a runtime Rational MAP KEY is found by a normalized-equal key
pass	a runtime Rational built from a parameter compares by its exact value
pass	a runtime Rational comparison of a parameter is false when it should be
pass	a runtime Rational three-way compare agrees with the boolean ordering
pass	a runtime Rational three-way compare reports canonically-equal fractions as Equal
pass	a runtime Rational three-way compare reports the greater operand as Greater
pass	a runtime Rational-inner quantity used as a Map key hits, and a different magnitude misses
pass	a runtime Result carrying a String error is matched
pass	a runtime Result constructed from a condition selects its Ok or Err arm
pass	a runtime SIGNED nibble truncation sign-extends from the top kept bit
pass	a runtime String host arg is marshaled into shared memory and the host call returns its response
pass	a runtime String match and its (= s literal) if-chain agree on the DEFAULT fall-through arm
pass	a runtime String match on a hit arm agrees with its desugared (= s literal) if-chain
pass	a runtime String ordering compare over a let-bound rope operand leaves no live heap objects
pass	a runtime String rope compares equal to its flat twin and leaves no live heap objects
pass	a runtime String rope inserted into an empty set boxes by its own type and its compaction is leak-neutral
pass	a runtime String.at result compares by content in a recursive scan
pass	a runtime String.concat then byte-len measures the built rope
pass	a runtime String.from-bytes decodes a recursively built buffer
pass	a runtime String.from-bytes of ill-formed bytes takes the None arm
pass	a runtime String.from-bytes result is read twice and both reads see the decoded string
pass	a runtime String.slice STORED as a map key is found by a flat probe
pass	a runtime String.slice probing a flat-keyed map hits by content
pass	a runtime String.to-bytes encodes a recursively built string
pass	a runtime Symbol value dispatches on symbol literals
pass	a runtime Symbol.of of a different-length rope does not equal the literal
pass	a runtime Symbol.of of a rope equals a symbol literal by content
pass	a runtime Symbol.of then Symbol.to-string round-trips the bytes
pass	a runtime UInt64 underflow inside a newtype constructor argument still traps
pass	a runtime UInt64->Int64 .of range-checks the upper bound (fits passes, > i64::MAX traps)
pass	a runtime UInt8 left shift range-checks the count and the result
pass	a runtime UInt8 subtraction underflowing below zero traps
pass	a runtime UInt8 unsigned right shift is logical
pass	a runtime UInt8 wrapping-add overflow wraps modulo 256 (the result is re-masked to width)
pass	a runtime UInt8->Int8 .of range-checks: an in-range value passes, an out-of-range value traps
pass	a runtime Unit.in conversion emits the scale multiply (Int)
pass	a runtime arithmetic right shift preserves the sign
pass	a runtime bin ENCODE lays out le and signed fields byte-exactly
pass	a runtime bin construction borrowed by Bytes.len leaves no live heap objects
pass	a runtime bin construction result compares equal to the Bytes it builds
pass	a runtime bin construction result unequal to different content is false
pass	a runtime bin match binds a dependent-size bytes segment over a runtime scrutinee
pass	a runtime bin match binds the tail after a fixed header via a final rest segment
pass	a runtime bin match decodes a Bytes.slice sub-range of a larger buffer
pass	a runtime bin match decodes a FINAL DEPENDENT-size utf8 segment
pass	a runtime bin match decodes a FINAL constant-size utf8 segment
pass	a runtime bin match decodes a FINAL utf8 segment at a NON-ZERO static offset
pass	a runtime bin match decodes a NON-FINAL dependent-size utf8 segment before a trailing field
pass	a runtime bin match decodes a byte-aligned bit-field run
pass	a runtime bin match dispatches on a leading literal bit-field tag
pass	a runtime bin match dispatches on a literal tag across arms
pass	a runtime bin match feeds its decoded segment binders into a sum+tuple constructor, then projects back
pass	a runtime bin match reads an int segment after a byte-aligned bit-field run
pass	a runtime bin match whose catch-all BINDS and reads the whole Bytes scrutinee
pass	a runtime bin match with a NON-FINAL dependent-size segment binds the body then the rest
pass	a runtime bin pattern decodes a fixed-width segment from a runtime scrutinee
pass	a runtime bin-match binds a dependent-size bytes segment named by an earlier binder
pass	a runtime bin-match binds a final rest bytes segment (tail length)
pass	a runtime bin-match final rest after a 2-byte header binds the tail
pass	a runtime bin-match final rest is empty when the scrutinee is exactly the header
pass	a runtime bin-match leading literal tag dispatches to the decode
pass	a runtime bin-match little-endian read matches the little-endian build
pass	a runtime bin-match mismatched literal tag falls to the catch-all
pass	a runtime bin-match multi-arm dispatch selects by the leading literal tag
pass	a runtime bin-match signed i8 decodes two's complement
pass	a runtime bin-match too short for the fixed prefix falls through
pass	a runtime bin-match u16 round-trips the built value
pass	a runtime bin-match whole-scrutinee length mismatch falls to the catch-all
pass	a runtime bit-field bin construction borrowed by Bytes.len leaves no live heap objects
pass	a runtime bit-field literal tag that does not match falls through
pass	a runtime bit-field packs a runtime value into a nibble
pass	a runtime bit-field run spans two bytes and composes with an int segment
pass	a runtime bitwise AND masks bits
pass	a runtime bitwise OR combines bits
pass	a runtime bitwise XOR toggles bits
pass	a runtime branch selects WHICH host op consumes the first response row
pass	a runtime branch selects between two ANONYMOUS closures fed to the iterate combinator
pass	a runtime byte truncation of the sign extremes keeps exactly the low byte
pass	a runtime bytes value is spliced into a length-prefixed frame
pass	a runtime char payload binds and reads back its code point (Char-rep 4/N)
pass	a runtime char reaches a later char-literal arm by scalar value
pass	a runtime checked-mul hitting Int64.min EXACTLY fits (the naive-magnitude-check killer)
pass	a runtime checked-mul of Int64.min by -1 overflows to None (the sign-flip edge)
pass	a runtime checked-mul over UInt64 operands emits the unsigned division round-trip
pass	a runtime closure compares its argument to a captured value in a branch
pass	a runtime closure whose body calls a recursive top-level function
pass	a runtime compound compare COMPUTES the three-way Ordering via the value-cmp heap walk (§331)
pass	a runtime compound compare with a float leaf is a coded CDZ0203 — the three-way twin of the compound `<` decline
pass	a runtime compound containing a float leaf is rejected CDZ0203 for ordering — floats offer no total order (§319)
pass	a runtime compound structural equality is expressible as a hand-written recursive comparator
pass	a runtime compound with a String leaf orders by the blessed content-lexicographic String order
pass	a runtime compound with a float leaf still equality-compares though it declines ordering (§313 vs §319)
pass	a runtime concat places the second operand's elements after the first's
pass	a runtime concat-reached Bytes equals the directly-built Bytes and not a decoy
pass	a runtime condition selects an abortive branch reading an enclosing parameter
pass	a runtime conditional selects its then-branch when the runtime condition holds
pass	a runtime conjunction still shields a comparison right operand whose subexpression traps
pass	a runtime const-multiply at its exact fitting boundary does not trap
pass	a runtime const-multiply one past its fitting boundary traps
pass	a runtime cross-scale scalar-Int quantity comparison converts before comparing, not a raw magnitude compare
pass	a runtime dependent-size bytes segment sized by a bit-field field
pass	a runtime dependent-size match falls through when the scrutinee is too short for the size prefix
pass	a runtime dependent-size match reads a MULTI-BYTE (u16) length prefix
pass	a runtime dependent-size match with a signed negative size falls through
pass	a runtime division by zero traps
pass	a runtime division of the minimum integer by -1 overflows and traps
pass	a runtime division truncates toward zero
pass	a runtime element collapses against a constant one at build
pass	a runtime exact mixing of inch and millimeter keeps the fractional scale
pass	a runtime final rest segment binds the empty tail when the scrutinee is only the header
pass	a runtime fixed-width segment is emitted big-endian and read back by index
pass	a runtime float absent from a Set is not found
pass	a runtime float addition emits the machine add
pass	a runtime float addition with nan propagates the nan
pass	a runtime float compare is a coded CDZ0203 — a float offers the IEEE partial order, not a total three-way
pass	a runtime float divide by zero yields a non-trapping infinity
pass	a runtime float divided by ITSELF is not folded to 1.0 — 0.0/0.0 is nan, no trap
pass	a runtime float division rounds under the fixed mode
pass	a runtime float equality feeds the true-literal boolean coercion
pass	a runtime float equality negated by the false-literal coercion
pass	a runtime float is found as a Set element by canonical byte form
pass	a runtime float leaf in a tuple compares by the canonical byte form
pass	a runtime float multiplication emits the machine mul
pass	a runtime float promotes to a wider width with the machine promote
pass	a runtime float subtraction emits the machine sub
pass	a runtime full-width Int32 addition traps on carry overflow
pass	a runtime full-width UInt64 addition that overflows traps rather than wrapping
pass	a runtime full-width UInt64 multiplication that overflows traps rather than wrapping
pass	a runtime full-width UInt64 subtraction that underflows traps rather than wrapping
pass	a runtime function value is manually eta-wrapped and applied through nested recursive HOFs
pass	a runtime greater-than compares by signed order
pass	a runtime greater-than-or-equal holds at the equal-operand boundary
pass	a runtime heap-carried Option Float32 renders shortest-f32, keeping its 2 reachable return cells
pass	a runtime heap-carried Result Float32 (Ok) renders shortest-f32, keeping its 2 reachable return cells
pass	a runtime hole woven into a compound Ast is read back by a nested match
pass	a runtime if-produced Bytes is length-measured with valid wasm
pass	a runtime increment traps exactly at the maximum integer
pass	a runtime infinity in an indeterminate op yields nan (inf minus inf, inf times zero)
pass	a runtime integer converts to a float with the machine convert
pass	a runtime left shift by 63 fits for -1 and overflow-traps for 1
pass	a runtime left shift by the bit width or more traps
pass	a runtime left shift multiplies by a power of two
pass	a runtime less-than at the integer extremes is signed
pass	a runtime less-than-or-equal holds at the equal-operand boundary
pass	a runtime less-than-or-equal is signed at the integer extremes
pass	a runtime list aliased into two record fields is read intact through both
pass	a runtime list aliased into two tuples and its original binding all read consistently
pass	a runtime list consumed by push in one operand is unchanged when read in a later operand
pass	a runtime list of lists escapes with its nested element type
pass	a runtime list of record elements escapes rendering each element as its named-field record form
pass	a runtime list of sum-typed elements escapes rendering each element as its sum value-form
pass	a runtime list of tuples is equal element-wise, descending into each tuple component
pass	a runtime list orders lexicographically by its elements
pass	a runtime list parameter consumed by push in a self-recursive call's sibling operand is unchanged
pass	a runtime list that is a proper prefix orders less than its extension
pass	a runtime literal int probe reads at a dynamic offset after a dependent-size body
pass	a runtime literal-tag probe gates a dependent-size body in one arm
pass	a runtime magnitude unwraps through a unit conversion
pass	a runtime map drained by remove equals the EMPTY map literal
pass	a runtime map match binds a present key's value
pass	a runtime map match falls through when the named key is absent
pass	a runtime map match fires a two-key arm only when both keys are present
pass	a runtime map match's rest binder reads the map minus the named key
pass	a runtime map match's two-key arm falls through when one key is missing
pass	a runtime map reached VIA insert-then-remove equals the directly-built map
pass	a runtime map shared across two recursive-call operands is not mutated by an insert in one
pass	a runtime mixed-scale combine converts a COMPUTED BigInt quantity operand
pass	a runtime mixed-scale combine converts a COMPUTED Float quantity operand
pass	a runtime mixed-unit sum emits the scale conversion (Int)
pass	a runtime mixed-unit sum emits the scale conversion before the add
pass	a runtime modulo of the minimum integer by -1 is zero on every backend
pass	a runtime multibyte rope matches a MULTIBYTE string-literal arm by content
pass	a runtime multiplication that overflows traps
pass	a runtime nan subtracted from itself is nan, not zero
pass	a runtime negative dividend divided by a constant power of two truncates toward zero
pass	a runtime negative dividend mod a constant power of two takes the dividend's sign
pass	a runtime negative zero is not equal to positive zero
pass	a runtime negative-const-multiply one past its lower fitting boundary traps
pass	a runtime nibble truncation keeps the low four bits
pass	a runtime non-String operand to String.at is the same CDZ0203 as its constant twin
pass	a runtime non-String operand to String.byte-len is the same CDZ0203 as its constant twin
pass	a runtime non-String operand to String.concat is the same CDZ0203 as its constant twin
pass	a runtime non-String operand to String.slice is the same CDZ0203 as its constant twin
pass	a runtime non-final dependent-size body of zero binds empty and rest absorbs everything
pass	a runtime non-final dependent-size body that overruns the frame falls through
pass	a runtime out-of-bounds list update traps rather than aliasing a valid slot
pass	a runtime overflowing left shift traps
pass	a runtime product's derived dimension cancels correctly through a divide
pass	a runtime product's derived dimension is enforced against an incompatible add
pass	a runtime quantity Map key MISSES when the looked-up magnitude differs
pass	a runtime quantity stored in a List reads back through List.at and unwraps
pass	a runtime quantity used as a Map key hits when a separately-built equal key is looked up
pass	a runtime record built behind a recursive call escapes to the host
pass	a runtime record nested in a tuple compares component-wise
pass	a runtime record whose field is a runtime tuple escapes to the host
pass	a runtime remainder takes the dividend's sign
pass	a runtime right shift by the bit width or more traps
pass	a runtime rope slice maps scalar offsets to bytes across the seam for a multi-byte scalar
pass	a runtime same-dimension multiply derives the SQUARED dimension and computes the area
pass	a runtime same-dimension quotient is dimensionless and its value joins bare integer arithmetic
pass	a runtime scrutinee gates through a multi-guard chain and falls through to the wildcard
pass	a runtime scrutinee keeps the full switch and nested dispatch
pass	a runtime scrutinee matching no arm traps
pass	a runtime scrutinee of one sum type rejects a foreign sum's pattern
pass	a runtime scrutinee selects a non-first literal arm
pass	a runtime set reached VIA remove equals the directly-built set and not a decoy
pass	a runtime set shared across two recursive-call operands is not mutated by an insert in one
pass	a runtime set-difference result escapes to the host as its value form
pass	a runtime set-union result escapes to the host as its value form
pass	a runtime shift with an out-of-range CONSTANT count and a TRAP-FREE lhs still traps unreachable (control)
pass	a runtime signed division by zero traps as divide-by-zero, not overflow
pass	a runtime signed little-endian segment lays the low byte first
pass	a runtime signed little-endian segment round-trips a negative value
pass	a runtime signed modulo by zero traps as divide-by-zero, the companion of runtime divide-by-zero
pass	a runtime slice PROBING a Map keyed by flat Bytes must hit by content
pass	a runtime slice STORED as a Map key must be found by a flat Bytes probe
pass	a runtime slice VIEW crosses a helper-function boundary intact
pass	a runtime slice probes a Set of flat Bytes by content
pass	a runtime square multiply traps exactly past the Int64 square-root boundary
pass	a runtime string bound from a sum payload compares equal to a string parameter
pass	a runtime string compared against a literal folds against the literal side
pass	a runtime string equality selects a branch by comparing a parameter to a literal
pass	a runtime string interns to a symbol matched by content
pass	a runtime string is indexed by scalar and the extracted scalar is returned
pass	a runtime string is sliced by scalar offsets
pass	a runtime string orders by content-lexicographic byte order
pass	a runtime string pattern dispatches by content (value-eq compare)
pass	a runtime string pattern falls through to the wildcard when no literal matches
pass	a runtime string returned across the run boundary renders as its quoted text
pass	a runtime string rope compared through a borrowed operand
pass	a runtime string rope compares equal to its flat twin
pass	a runtime string rope inserted into a set is a member
pass	a runtime string rope map key is found by its flat twin
pass	a runtime string rope matches a string-literal arm
pass	a runtime string rope set element is found by its flat twin
pass	a runtime string slice addresses scalar values, not bytes
pass	a runtime string slice out of range yields None
pass	a runtime string slice spans the seam of a runtime-assembled rope
pass	a runtime string slice with an empty in-range span is Some of the empty string
pass	a runtime structural = on a sum with List and Float payloads walks it (value-eq-shaped over a Sum)
pass	a runtime subtraction that overflows traps
pass	a runtime sum equality as a match SCRUTINEE drives a tail-recursive loop
pass	a runtime sum equality drives a tail-recursive loop
pass	a runtime sum match drives a tail-recursive loop
pass	a runtime sum orders by discriminant then payload
pass	a runtime sum whose payload comes from recursion compares equal by a heap walk
pass	a runtime sum whose payload comes from recursion compares unequal by a heap walk
pass	a runtime sum whose payload is a runtime tuple nests across the boundary
pass	a runtime symbol compared to an interned constant matches by content
pass	a runtime symbol is found among a set of known symbols
pass	a runtime symbol matches a symbol-literal pattern arm by content
pass	a runtime symbol not among the known symbols is rejected
pass	a runtime symbol not among the literal arms falls through to the wildcard
pass	a runtime symbol round-trips back to its string
pass	a runtime symbol that differs from the interned constant does not match
pass	a runtime symbol-literal pattern dispatches to the first arm by content
pass	a runtime symbol-literal pattern dispatches to the second arm by content
pass	a runtime symbol-literal pattern falls through to the wildcard on an unlisted symbol
pass	a runtime three-level nested tuple compares equal by a deep walk
pass	a runtime three-variant classifier dispatches to the middle arm
pass	a runtime three-variant classifier dispatches to the negative arm
pass	a runtime truncation into a signed byte sign-extends
pass	a runtime truncation to an unsigned byte keeps the low bits, total on negatives
pass	a runtime tuple built behind a recursive call escapes to the host
pass	a runtime tuple orders lexicographically by field
pass	a runtime tuple whose component is a list compares the list element-wise in order
pass	a runtime tuple with a boolean element is returned as a result
pass	a runtime two-operand Int16 multiplication range-checks its product
pass	a runtime two-operand Int8 multiplication range-checks its product
pass	a runtime two-operand UInt16 multiplication range-checks its product
pass	a runtime two-operand UInt8 addition range-checks at the width boundary
pass	a runtime two-operand UInt8 multiplication range-checks its product
pass	a runtime unsigned division at the maximum by 1 does not trap
pass	a runtime unsigned division by zero traps as divide-by-zero
pass	a runtime unsigned-byte addition traps exactly past its width maximum
pass	a runtime unsigned-byte addition traps on overflow of its width
pass	a runtime unsigned-byte multiplication traps exactly past its width maximum
pass	a runtime unsigned-byte subtraction traps below zero
pass	a runtime value is constructed into a fixed-width segment and its length read
pass	a runtime value narrowed to the segment width constructs without trapping
pass	a runtime value under a const key binds through the map pattern
pass	a runtime value-eq in a tail-loop condition does not clash the arithmetic scratch slot
pass	a runtime velocity multiplied by a time cancels back to the base dimension
pass	a runtime wider integer is truncated into a byte by wrap
pass	a runtime width NESTED in a compound annotation is rejected
pass	a runtime wrap never traps at the widest input boundary and reads the low byte
pass	a runtime wrapper sum whose payload is a variant is matched by a nested constructor pattern
pass	a runtime zero denominator PRODUCED BY ARITHMETIC traps at construction
todo	a runtime-DISC `?` inside a stored closure applies per-call once BRICK 3b lands
pass	a runtime-PARAMETERIZED recursive sum renders per-call depths from one export
pass	a runtime-assembled tree round-trips equal to its quoted twin
pass	a runtime-branched tuple returned from a callee is projected in the caller
pass	a runtime-built BigInt sum-payload literal probe falls through on a non-matching payload
pass	a runtime-built BigInt sum-payload literal probe matches its constructor
pass	a runtime-built generic user sum escapes with its parametric type frame
pass	a runtime-built list nested in a record field crosses the host boundary rendering the nested collection
pass	a runtime-built list nested in a tuple crosses the host boundary rendering the nested collection
pass	a runtime-built string crosses the boundary as its value form
pass	a runtime-computed BigInt result crosses the host boundary
pass	a runtime-computed Rational crosses the host boundary as its exact value
pass	a runtime-conditioned abortive branch and its fall-through both read an enclosing parameter
pass	a runtime-conditioned if-branch perform distributes the handler per branch
pass	a runtime-constructed Ast equals its quoted twin by structural content
pass	a runtime-element Bytes.at read widens the byte to the Int64 Option payload
pass	a runtime-element set compares equal to the constant set of the same elements
pass	a runtime-element set equals itself
pass	a runtime-element set's equality is independent of written order
pass	a runtime-if branch literal overflowing a narrow annotation is rejected at check
pass	a runtime-if branch literal reaching a narrow parameter is rejected at check
pass	a runtime-index Bytes.at at a NEGATIVE index is None, not an unsigned wrap
pass	a runtime-index List.update LOOP rewrites every 100th slot of a 1000-element list
pass	a runtime-index list read at a NEGATIVE index is None, not an unsigned wrap
pass	a runtime-index list read out of bounds is None, not a trap
pass	a runtime-index list read yields the element at that index
pass	a runtime-index read of a runtime-built list
pass	a runtime-index string slice compares equal to the expected multi-byte scalar by content
pass	a runtime-index string slice isolates a multi-byte scalar by its scalar offset
pass	a runtime-index string slice isolates a supplementary-plane scalar (four UTF-8 bytes)
pass	a runtime-index string slice of the ASCII prefix before a multi-byte scalar
pass	a runtime-index string slice spanning ASCII and a multi-byte scalar
pass	a runtime-index update leaves the other elements unchanged
pass	a runtime-index update replaces exactly that element, read back at the same index
pass	a runtime-key swap-then-take chain threads both value-yields and the shrinking map
pass	a runtime-keyed map entry enumerates as its key-value tuple
pass	a runtime-length list dispatches a leading-rest arm by its actual length
pass	a runtime-magnitude product multiplies the erased magnitudes
pass	a runtime-magnitude quantity raised to a negative power emits the reciprocal
pass	a runtime-magnitude quantity raised to a power emits the repeated multiply
pass	a runtime-magnitude quotient divides the erased magnitudes
pass	a runtime-magnitude same-unit difference subtracts the erased magnitudes
pass	a runtime-magnitude same-unit sum adds the erased magnitudes
pass	a runtime-match arm literal overflowing a narrow annotation is rejected at check
pass	a runtime-match arm literal reaching a narrow parameter is rejected at check
todo	a runtime-operand try as a list element declines pending brick 3b
pass	a runtime-parameter start and length are bounds-checked at run time, yielding Some or None
pass	a runtime-payload `?` result feeds a SECOND runtime-payload `?` in sequence
pass	a runtime-produced -inf crosses inside a compound alongside nan and inf
pass	a runtime-produced String argument crosses to a peer as its handle
pass	a runtime-reached shift with an out-of-range CONSTANT count and a TRAPPING lhs surfaces the lhs trap first (eval-order)
pass	a runtime-rope do-def that escapes an if-select in a match arm survives (multi-use heap keep, not a UAF)
pass	a runtime-scrutinee match with a bare-binder first arm and a differently-typed second arm is a type error
pass	a runtime-selected Name payload inside a rebuilt Ast compares against a read result
pass	a runtime-selected branch with a runtime-divisor div-by-zero traps only when taken
pass	a runtime-selected closure from a list of DISTINCT-BODY closures dispatches indirectly and reclaims
pass	a runtime-selected closure survives as a value threaded through a recursive HOF (false branch)
pass	a runtime-selected closure survives as a value threaded through a recursive HOF (true branch)
pass	a runtime-selected function chosen directly at the application head is applied
pass	a runtime-selected function chosen directly at the application head is applied (false branch)
pass	a runtime-selected trapping branch with a non-zero divisor computes normally
pass	a runtime-valued integer width is rejected, not dropped to the default Int64
pass	a runtime-woven Ast and its quote twin resolve to ONE Map key
pass	a runtime-woven Ast compares structurally equal to an equivalent quote
pass	a same-arity function PARAMETER difference resolves at the inner argument, no fn-signature tail
pass	a same-arity per-position tuple type mismatch keeps its element message with no add or delete fix
pass	a same-arity tuple element-type mismatch names the specific position, not an arity delta
pass	a same-constant equality pair still folds idempotently and ordering subsumption is unaffected
pass	a same-dimension annotation is the identity under its own unit's conversion
pass	a same-dimension annotation preserves the value's own scale through a conversion
pass	a same-dimension quantity annotation does not rebrand: annotated 1 km converts to 1000 m
pass	a same-dimension quantity annotation whose inner numeric type differs is still an error
pass	a same-dimension-annotated quantity combines at its own scale: 1 km + 2 km = 3000 m
pass	a same-dimension-annotated quantity converts back to its own unit as the identity
pass	a same-direction and of two upper-bound comparisons subsumes to the tighter bound
pass	a same-direction or of two upper-bound comparisons subsumes to the looser bound
pass	a same-effect abortive arm with two INLINE advancing performs reads the advanced state (no recursion)
pass	a same-field-set record with a differing field TYPE names the field and its expected/actual type
pass	a same-field-set record with a differing field type names the field, not a field-set difference
pass	a same-kind symbol-literal payload sub-pattern still dispatches (nested control)
pass	a same-kind symbol-literal tuple sub-pattern still dispatches (tuple control)
pass	a same-name GENERIC constructor via a called helper resolves to the constructor (adv-63)
pass	a same-name GENERIC ctor CONSTRUCTED AND MATCHED inside an inlined callee resolves (adv-63 b1)
pass	a same-name GENERIC ctor in an inlined callee resolves with a CONST argument (adv-63 b2)
pass	a same-name generic type applied in a variant payload denotes the type
pass	a same-name generic type at TWO instantiations in one payload each denotes its type
pass	a same-name monomorphic constructor in a called helper resolves to the constructor
pass	a same-name monomorphic constructor over a COMPOUND payload in a called helper constructs
pass	a same-name multi-variant constructor in a called helper resolves to the constructor
pass	a same-name variant of a multi-variant sum constructs bare in a body
pass	a same-scrutinee re-match whose inner arm set OMITS the refined variant falls to the inner default
pass	a same-sign widening wrap is the identity
pass	a same-signature multi-export shared call is repeatable on one make-<name> handle
pass	a same-signature mutual tail cycle shares one constant-stack loop
pass	a same-sum comparison is valid (no false kind-boundary reject)
pass	a same-type Float64 comparison is valid (no false numeric-mix reject)
pass	a same-type Int64 comparison is valid (no false numeric-mix reject)
pass	a same-type sum match is accepted and binds the payload (the control)
pass	a same-unit quantity + over a NARROW runtime magnitude beside a wider one widens the narrow operand (no invalid wasm)
pass	a same-unit quantity join flows and its magnitude reads back
pass	a satisfied @ensures on a performing def passes the effectful result through
pass	a saturating OR-mask over a refined range folds to the constant (value + trap parity)
pass	a scalar Ast.Int built in a helper from a runtime param compares = a constant Ast.Int
pass	a scalar BETWEEN two Tuple args with a LIST result
pass	a scalar Int64 match nested as an op operand beside an i32-handle sibling emits valid wasm
pass	a scalar abort in a CONDITIONAL tuple operand declines (type-consistency)
pass	a scalar abort in a TUPLE-typed handle body declines (type-consistency)
pass	a scalar do-def shared across both resume slots in a state-advancing add handler
pass	a scalar element is projected DIRECTLY from a named function's runtime tuple result
pass	a scalar element is projected directly from a fn-return tuple with runtime content
pass	a scalar element is projected directly from a function's runtime tuple result
pass	a scalar element is projected from a let-bound runtime tuple
pass	a scalar export crosses the WIT boundary and returns its argument
pass	a scalar field projected off a nullary const fn's record folds to the field, dropping the record
pass	a scalar match beyond the jump-table cap falls back to a probe cascade and still dispatches
pass	a scalar newtype matched back in place round-trips the erased inner
pass	a scalar newtype wrapped by a param'd helper then matched back crosses the internal call boundary
pass	a scalar quantity argument crosses to and from a peer as its inner scalar
pass	a scalar slice spanning a width TRANSITION carries its multibyte content exactly
pass	a scalar tuple argument crosses INBOUND to a peer and both fields are summed there
pass	a scalar walk over a rope spanning ALL FOUR UTF-8 widths counts the wide scalars
pass	a scalar-aware string shrinker converges to the 1-minimal failing string
pass	a scalar-erased newtype returned directly from a parameterized def renders its nominal type
pass	a scalar-erased single-variant newtype escaping a param'd export emits a valid module and renders the nominal name
pass	a scalar-indexed split over a MULTIBYTE string bounds its walk by scalar-len, not byte-len
pass	a scalar-literal pattern over a List scrutinee is a coded CDZ0203 — the literal type cannot match the list
pass	a scalar-mismatched perform argument names the operation with no structural-delta tail
pass	a scalar-payload sum looked up from a map, returned via a ctor, is matched in the caller
pass	a scalar-result match over an owned compound-payload sum reclaims the shell AFTER its borrowed reads
pass	a scalar-wise string reversal is an involution over a multibyte rope and reverses scalars, not bytes
pass	a scaled cube dimension raises its prefix scale to the power (km³ → ×1e9 meter³)
pass	a scaled derived quantity displays with BOTH scales folded into the magnitude
pass	a scaled reciprocal dimension folds the denominator prefix scale (1/ms → ×1000 per second)
pass	a scaled-unit PARAMETER annotation keeps its scale across the type round-trip (mixed-scale combine)
pass	a scientific-notation float literal denotes the scaled value
pass	a scientific-notation literal annotated Rational scales the numerator
pass	a scrutinee FAILING the pattern reaches the catch-all WITHOUT running the guard's perform
pass	a scrutinee of two to the sixty-fourth-adjacent magnitude misses a zero-based jump table
pass	a second same-signature closure export shares the one call method
pass	a seconds quantity is rejected by a meter-typed import at the call site
fail	a select between a base map and its derived generation frees the loser but not shared nodes
pass	a self-aliased map-merge dups the shared handle, merges idempotently, and reclaims with no leak
pass	a self-applying term is declined at the reduction budget, not hung on
pass	a self-call and a sibling perform as direct list-push operands thread out-state via multi-value return
pass	a self-call and a sibling perform as direct subtraction operands thread out-state via multi-value return
todo	a self-call gated behind a nested if IN a match-scrutinee declines cleanly, never hoisted
todo	a self-call gated behind a nested if IN an if-condition declines cleanly, never hoisted
todo	a self-call gated behind an and short-circuit in an if-condition declines cleanly, never hoisted
pass	a self-comparison keeps a trapping operand (b=0 still traps)
pass	a self-comparison less-equal still traps on a zero divisor but folds true for a nonzero one
pass	a self-comparison of a scalar folds to a constant (value parity)
pass	a self-comparison of a trapping operand still traps (less-than)
pass	a self-divide (/ v v) fold over a TRAPPING lazy binding preserves the trap (does not fold to 1)
pass	a self-loop-tail sum-spine walk that ALSO threads the whole node forward SUPPRESSES the reclaim (no UAF)
pass	a self-recursive AST walker consumes a node's child list while the node is threaded unchanged
pass	a self-recursive Bool-returning function whose recursive call is the then-branch
pass	a self-recursive effectful loop sums a fresh-id draw per step — the gensym idiom
pass	a self-recursive variant payload does not false-positive as an unknown type
pass	a self-referential-shaped HOF that is not actually recursive folds
pass	a self-shielding or does not hoist its trapping right operand past the short-circuit
pass	a self-tail call passing a heap-match argument compiles to valid wasm
pass	a self-tail loop advancing by a tuple projection while accumulating compound-sum nodes compiles
pass	a self-tail-loop inorder tree-fold reclaims its whole traversed spine (INC1 pt3)
pass	a self-tail-recursive SUM consumer loops (tail call in a MatchSum arm) and computes the fold
pass	a self-tail-recursive accumulator runs a million iterations in constant stack
pass	a self-tail-recursive fn with a MIXED Option-returning innermost match emits valid wasm
pass	a self-tail-recursive mixed match whose recursive arm returns an Option compiles to valid wasm
pass	a separator JOIN over a runtime parts list handles first-vs-rest and the empty list
pass	a sequencing block discards a pure compound intermediate
pass	a sequencing block discards a pure list intermediate
pass	a sequencing block whose last form is unit yields unit
pass	a sequencing block yields the value of its last form
pass	a sequential let binding names an earlier binding in a later initializer
pass	a set REST pattern #set(e… .. rest) binds rest to the residual set (the scrutinee MINUS the named elements)
pass	a set accumulator is queried for membership after building
pass	a set as a map value: runtime membership tested through the lookup
pass	a set built at run time escapes to the host as its value form
pass	a set built from a BigInt sum and a BigInt.of over integer arithmetic has both elements
pass	a set built from generated values is equal regardless of insertion order (order-independent equality on generated inputs)
pass	a set chosen by a runtime if is membership-tested with valid wasm
pass	a set chosen by a runtime if is size-measured with valid wasm
pass	a set collapses a duplicate STRING element (a non-int scalar key path)
pass	a set collapses a duplicate element
pass	a set construction spread splices a runtime set's elements and dedups
pass	a set construction spread with only spreads unions the operands
pass	a set consumed by Set.insert in one operand is unchanged for a later read of the same binding
pass	a set consumed by Set.remove in one operand is unchanged for a later read of the same binding
pass	a set cross-kind element clash names both types and offers no fix
pass	a set dedups NaN elements and keeps zero signs distinct
pass	a set int-vs-float element clash offers the float-literal retype fix
pass	a set is constructed from a list of its elements
pass	a set match with no terminal catch-all is rejected as non-exhaustive (CDZ0210)
pass	a set match with no whole-set catch-all is non-exhaustive (a set's element set is unbounded)
pass	a set membership element may be a RUNTIME value expression, and compound elements match by value equality
pass	a set membership match `#set(e…)` matches when the set contains every named element (with fall-through + catch-all)
pass	a set nested in a tuple compares equal with a runtime element, order-independent
pass	a set of MAPS deduplicates by full map content
pass	a set of SETS deduplicates by inner-set value and finds a member built in a different order
pass	a set of a list with a trapping element forces it at construction — Set.len traps, not returns the count
pass	a set of lists deduplicates by the whole list, distinguishing element order
pass	a set of negative zero and positive zero keeps both (distinct sign bits)
pass	a set of records deduplicates by the whole record and its membership is field-order-INDEPENDENT
pass	a set of symbols deduplicates by content — Set.len materializes the symbol set
pass	a set of tuples deduplicates by the whole tuple and its membership is component-order-sensitive
pass	a set of two NaN floats dedups to one (canonical quiet-NaN)
pass	a set of two identical positive floats dedups to one
pass	a set of user-sum elements tests membership by variant
pass	a set over a NULLARY-SUM key enumerates canonically — permutation-invariant AND discriminant-ordered
pass	a set pattern disjoint from the scrutinee does not match
pass	a set pattern is order-independent (a set is unordered)
pass	a set pattern matches a scrutinee set with the SAME elements
pass	a set pattern naming a SUBSET matches by containment, not equality
pass	a set pattern naming an element the scrutinee lacks does NOT match
pass	a set pattern with a RUNTIME in-scope element matches by membership of its value
pass	a set reached via UNION (overlapping and self) equals the directly-built set
pass	a set rest pattern binds the residual = scrutinee MINUS the named element
pass	a set rest pattern naming every element binds the EMPTY residual set
pass	a set rest pattern still requires the named elements PRESENT (absent named refutes the arm)
pass	a set threaded as a recursive accumulator dedups during accumulation
pass	a set type whose element position holds a value is rejected
pass	a set with elements of two different types is a type error
pass	a set-pattern element that names no in-scope value is an unbound value reference (CDZ0101)
pass	a shadowed-constructor application types at the binding's return type
pass	a shared CHAMP map survives per-level path-copied inserts that are read or dropped
pass	a shared and a differing element hoist across same-length list arms
pass	a shared inner map retrieved through BOTH outer keys stays ONE canonical value for keying
pass	a shared list concatenated with itself has twice its length
pass	a shared list is consumed element-wise twice by rest-pattern recursion
pass	a shared runtime rope survives per-level extension ropes that are read or dropped
pass	a shared trapping operand before the differing one does not preempt a trapping condition
pass	a shared trapping operand before the differing one does not preempt a trapping condition (compare head)
pass	a shared trapping operand traps under a trap-free condition
pass	a shared trapping second operand does not preempt a trapping condition
pass	a shift by a RUNTIME amount computes (both operands off the fold)
pass	a shift of a non-numeric operand names the integer requirement
pass	a short-circuit and of a boolean and its negation folds to false
pass	a short-circuit and with identical operands folds idempotently to the operand
pass	a short-circuit connective threads a branch perform to the continuation
pass	a short-circuit connective with a perform in the RHS preserves short-circuit (rhs not run)
pass	a short-circuit or of a boolean and its negation folds to true
pass	a short-circuit or with identical operands folds idempotently to the operand
pass	a shorter string that is a prefix of a longer one compares Less
pass	a sibling arg agreeing with a Bool type witness compiles and runs
pass	a sibling arg agreeing with an Int64 type witness compiles and runs
pass	a sibling-inferred out-of-range list element is CDZ0302 regardless of element order
pass	a sibling-recursive walk threads a HEAP list accumulator across the siblings
pass	a sign-changing wrap still sign-extends the low bits
pass	a signature mismatch in ONE of several bound peers is rejected at compose time
pass	a signature mismatch in a peer-into-peer (transitive chain) binding is rejected at compose time
pass	a signature mixing a plain parameter and a tuple-pattern parameter binds both
pass	a signed dependent size that reads negative falls through, not a backward read
pass	a signed i16 bin field spanning a rope seam sign-extends from the stitched bytes
pass	a signed i8 segment encodes as two's complement
pass	a signed narrow if-branch opposite a bare literal computes at the narrow width
pass	a signed narrow match binder returns the bound value
pass	a signed narrow tuple element round-trips through the heap with its sign
pass	a signed narrow-width newtype boxed as a tuple element matches by its literal payload
pass	a signed narrow-width parameter plus a bare literal computes at the parameter width
pass	a signed narrow-width wrapping-add constant folds by wrapping to the negative outcome
pass	a signed pattern segment reads a two's-complement integer as negative
pass	a signed segment reads the sign-bit-only byte as the minimum, not its magnitude
pass	a signed wrap of a value the range lattice proves in-range elides its sign-extend
pass	a signed zero survives a TUPLE round-trip and its sign still steers arithmetic after unboxing
pass	a signed-byte entrypoint returns its runtime argument
pass	a signed-zero tuple-key leaf MISSES the opposite-signed-zero entry on lookup
pass	a simplifier pass applied TWICE equals one application (idempotence at the fixpoint)
pass	a single MUTUAL-recursion chain performs at its base — the cross-function fold serves it
pass	a single Map.swap reports the OLD prior value WHILE the new map holds the NEW value (combined atomicity)
pass	a single component exports BOTH a list<u8>-result member and a list<u8>-param member of one interface (operator §2 two-export capstone)
pass	a single handler with both a resuming and an abortive arm dispatches each op to its own arm kind
pass	a single handler with three ops each mutating the state by a DIFFERENT function, composed in sequence
pass	a single mixed handler uses only its resuming arm when the abortive op is never performed
pass	a single-body let whose one body is a (do …) sequence is well-formed and runs
pass	a single-field variant whose one payload is a Tuple binds the whole payload with one sub-pattern
pass	a single-form body admits a sequence by holding a do block
pass	a single-perform body over a state-destructuring arm folds to 6
pass	a single-scalar String.slice equals the String.at of the same index (the two scalar-addressing paths agree)
pass	a single-task DES scheduler sleeps a task and fast-forwards the clock to its wake instant
pass	a single-variant Int32 newtype literal-payload arm compiles at the payload width
pass	a single-variant Int64 newtype literal-payload arm still compiles (the wide control)
pass	a single-variant NEWTYPE escapes with its tag ERASED, only the type header naming it
pass	a single-variant UInt8 newtype literal-payload arm compiles at the payload width
pass	a single-variant UInt8 newtype literal-payload arm falls through to the binding arm on a miss
pass	a single-variant abstract theorem's constructor match outside the kernel is a withheld-constructor rejection (CDZ0214)
pass	a single-variant abstract type's constructor match outside its module is rejected CDZ0214 (withheld-ctor)
pass	a single-variant newtype BOOL literal-payload arm falls through on false
pass	a single-variant newtype BOOL literal-payload arm is reached, not silently dead
pass	a single-variant newtype list element falls through on a non-matching payload
pass	a single-variant newtype list element with a binder payload stays irrefutable
pass	a single-variant newtype list element with a literal payload matches by value
pass	a single-variant newtype literal-payload arm falls through to the binding arm on a miss
pass	a single-variant newtype literal-payload arm is selected when the payload matches
pass	a single-variant newtype over a COMPOUND escapes the bare compound under its type header
pass	a single-variant newtype value escaping a parameterized def emits a valid module and matches back
pass	a single-variant newtype's own constructor pattern matches and binds its inner value
pass	a single-variant nominal value escapes to the host as its erased payload tagged with the nominal name
pass	a single-variant open sum with an open-tail arm dispatches its named variant
pass	a single-variant-sum binding pattern destructures a multi-payload constructor positionally
pass	a single-variant-sum binding pattern nests inside another
pass	a slice OF a seam-spanning slice re-offsets into the logical bytes of the parent view
pass	a slice OF a slice composes the offsets at runtime bounds
pass	a slice OF a slice over a CONCAT rope composes offsets across the seam
pass	a slice VIEW and a rope compare by content across the two non-flat reps
pass	a slice at a RUNTIME start re-bases indexing per call
pass	a slice crosses the seam of a runtime-assembled byte rope
pass	a slice of a runtime Bytes rope equals the directly-built window
pass	a slice of a runtime rope equals the flat literal of its window
pass	a slice of a runtime-start SLICE composes both offsets
pass	a slice of a slice composes scalar offsets against the VIEW, not the base string
pass	a slice reads correctly after its parent binding's last direct use
pass	a slice returned from a helper OUTLIVES the helper's local parent
pass	a slice spanning a concatenation sees the logical bytes
pass	a slice whose end is beyond the string length yields None
pass	a slice whose end precedes its start yields None
pass	a slice whose start equals its end is Some of the empty string
pass	a slice window spanning MANY seams of a built rope reads the logical bytes
pass	a slice with a negative start yields None
pass	a slice-view SET element dedups against its flat twin in a Set.of batch build
pass	a small single-level prepend-built runtime list reads every index (no relaxed root spawned)
pass	a sparse many-arm match dispatches identically to its dense sibling
pass	a sparse three-arm match's terminal pair dispatches branchlessly — the default
pass	a sparse three-arm match's terminal pair dispatches branchlessly — the middle probe
pass	a specific constructor export exposes one variant and keeps the rest private
pass	a spilled record EXPORT result RECLAIMS the def's result handle — ZERO live objects
pass	a spliced boolean drives an evaluated conditional through the lifted leaf
pass	a spliced string participates in an evaluated equality
pass	a split-branch mutual-effect group whose recursion never reaches the base-case perform runs to 0
pass	a squared BigInt-inner quantity added to a linear one is a dimension mismatch
pass	a squared quantity does not add to a linear quantity
pass	a stacked @test @ensures def called as a function returns its value, not unit (value-transparent)
pass	a stacked @test @ensures with a FALSE postcondition traps when the def is called (test semantics preserved)
pass	a standard unit abbreviation resolves to its canonical unit in a converting sum
pass	a state byte built into a Bytes and decoded by a from-bytes match in a tail-resumptive arm folds
pass	a state-advancing helper called before a later read threads its write through the continuation
pass	a state-destructuring arm under a multi-perform body folds to 6
pass	a stateful counter is observed left-to-right by sibling performs in one expression
pass	a stateful counter threads through a RECURSIVE callee performing inside the handled region
pass	a stateful handler threads its counter across a function boundary
pass	a stateful handler threads its state across three sequential performs, the do yielding the last
pass	a stateful perform inside the arm of a fused match on a call result threads state once
pass	a stored continuation is popped from a time-ordered pqueue (tuple-payload match) and resumed
pass	a straight-line non-recursive performing helper under an outer recursion folds (finding #19 boundary)
pass	a straight-line tuple built from callee params is projected in the caller
pass	a stray resume in a plain def body (no enclosing arm) is rejected CDZ0201
pass	a stray resume nested in a strict operand (no enclosing arm) is rejected CDZ0201
pass	a strength-reduced multiply nested as an operand computes in place
pass	a strict greater-than of equal operands is false
pass	a strict less-than of equal operands is false
pass	a strict-operand abort in a DEEP-nested handler stack keeps its 99 when the advances are UNOBSERVED
pass	a string argument crosses to a peer and the doubled result byte-len is read
pass	a string carried as a sum-variant payload is bound and measured at run time
pass	a string compared to a symbol is a type error
pass	a string equality on an inlined match operand
pass	a string literal
pass	a string literal in a variant position is rejected
pass	a string literal is normalized to the canonical text form
pass	a string literal payload refines a ctor list element
pass	a string passed to a function as a runtime argument is measured by its byte length
pass	a string result chained through two peers escapes the entrypoint via the methods envelope
pass	a string spelled like a keyword round-trips as an Ast.Str, not an Ast.Bool or Ast.Name
pass	a string value annotated as a non-String type is a mismatch
pass	a string-literal pattern in a Symbol sum payload is a type error
pass	a string-literal pattern in a match selects by string equality, distinguishing Unicode
pass	a string-literal pattern over a symbol scrutinee is a type error
pass	a string-literal sub-pattern in a Symbol tuple element is a type error
pass	a structural record with a Float64 field MISSES a Map key with a different float field
pass	a structural record with a Float64 field is a Map key found by a separately-built equal key
pass	a structurally-duplicate tuple arm is unreachable and earns a CDZ0213 warning
pass	a structurally-mismatched List.push element names the operation and the field-level delta
pass	a structurally-mismatched perform argument names the operation and the field-level delta
pass	a subnormal-range tiny literal under Float32 computes in a runtime branch
pass	a subtract that can underflow int8 keeps its trap — elision is opt-in on a proof
pass	a success `?` observes an earlier trapping let-init so the trap is not elided
pass	a success `?` on a LET-BOUND variable operand unwraps through the binding
pass	a success `?` then a failure `?` short-circuits at the second
pass	a success `?` unwraps a RUNTIME Ok payload under a Result boundary
pass	a success `?` unwraps a RUNTIME payload (static ctor, per-call value)
pass	a suffixed BigInt literal annotated Int64 is a type mismatch, not a range-fit fault
pass	a suffixed BigInt literal annotated as Int64 is a type mismatch
pass	a suffixed BigInt literal passed to an Int64 parameter is the same type mismatch
pass	a suffixed BigInt literal that FITS Int64's range is still a type mismatch (the fault is the type, not the magnitude)
pass	a suffixed Rational literal annotated as Int64 is a type mismatch
pass	a sum TYPE and its constructors are imported by a wildcard and constructed in the entry
pass	a sum chosen by a runtime if is matched with valid wasm
pass	a sum declaring a variant name twice is a type error
pass	a sum match missing a variant is non-exhaustive even when the scrutinee is the covered one
pass	a sum nested as another sum's payload orders by the deep scalar payload when both levels tie
pass	a sum nested as another sum's payload orders by the inner discriminant when outers tie
pass	a sum payload numeric mismatch names the payload axis and offers the retype fix
pass	a sum type NAMED after a primitive scalar does not collide with the primitive
pass	a sum type can mix nullary and payload-carrying variants
pass	a sum type is declared with named variants
pass	a sum type variant can carry data
pass	a sum value annotated as a scalar type is rejected
pass	a sum value is threaded through a multi-binding let chain, flipping variant each step
pass	a sum variant carrying a Float64 payload boxes it and escapes it as a float
pass	a sum variant carrying a Map payload is deconstructed and the map queried
pass	a sum variant carrying a NARROW-int payload boxes and reads it at its width
pass	a sum variant carrying a String payload matches and reads the string
pass	a sum variant's list payload split across empty and rest arms is exhaustive and dispatches
pass	a sum whose variants are named after target-language keywords constructs and matches
pass	a sum with FOUR type parameters instantiates and matches each at a distinct type
pass	a sum with a rope String payload compares Equal exactly when = is true
pass	a sum with a rope String payload orders by content when variants tie
pass	a sum with a rope String payload orders by discriminant before any content
pass	a sum-match payload's heap child consumed while the scrutinee is live is retained
pass	a sum-match recursion that accumulates a built-in list returns a list
pass	a sum-payload list match with exact-0 and at-least-2 arms still rejects (length 1 is a gap)
pass	a sum-payload list match with only the non-empty arm still rejects (length 0 uncovered)
pass	a sum-payload loop invariant consumed per iteration accumulates stably
pass	a sum-payload-wrapped self-application is rejected in bounded time, not a compiler stack overflow
pass	a sum-type value is constructed through a variant
pass	a sum-type value is deconstructed by an exhaustive match
pass	a sum-type variant miss stays CDZ0201 (not a record field)
pass	a sum-typed STATE MACHINE steps through transitions with an ABSORBING terminal state
pass	a supplementary-plane char literal matches by scalar value
pass	a surplus map entry (name-alias form) carries a delete-the-surplus fix
pass	a surplus map entry (primitive form) carries a delete-the-surplus fix
pass	a surplus record field element carries a delete-the-surplus fix
pass	a surviving closure capturing a heap List reclaims its env cell and captured handle (no live objects)
pass	a swapped-operand ordering pair is a contradiction that does not mis-fold to one comparison
pass	a symbol argument crosses to and from a peer as a runtime handle like a string
pass	a symbol compared to a number is a kind-boundary type error
pass	a symbol compared to a string is a type error
pass	a symbol converts back to its content string
pass	a symbol is constructed from a string
pass	a symbol literal list element dispatches a runtime list by its content
pass	a symbol round-trip between TWO ops of one effect — interned by one, judged by the other
pass	a symbol where a string is expected offers the Symbol.to-string wrap fix
pass	a symbol's identity is its content, not how the content was derived
pass	a symbol-literal list-element pattern matches a list whose head is that symbol
pass	a symbol-literal match agrees with its if-(= s lit) chain on the DEFAULT fall-through arm
pass	a symbol-literal match agrees with the equivalent if-(= s lit) chain
pass	a symbol-literal match reaches the second arm by content
pass	a symbol-literal pattern in a String sum payload is a type error
pass	a symbol-literal pattern nested in a variant payload matches by content
pass	a symbol-literal pattern over a multi-byte symbol matches by content
pass	a symbol-literal pattern over a string scrutinee is a type error
pass	a syntactically loop-invariant perform advances per iteration — effectful is not hoistable
pass	a tag DISPATCHES ON CHUNK TEXT to weave different operator spines around runtime holes
pass	a tag function INTROSPECTS its chunk and hole counts and returns both
pass	a tag function recurses over its HOLES list and folds their sum
pass	a tag function resolves through a re-export CHAIN and expands at the entry's site
pass	a tag that DUPLICATES its single hole splices the same runtime subtree into both slots
pass	a tag that SWAPS its two holes delivers each runtime subtree to the exchanged slot
pass	a tag-length-value record round-trips through construct then match
pass	a tagged template expands via its binding-dispatched tag function
pass	a tagged template nested in another's hole composes — the inner's result reaches the outer tag
pass	a tagged template threads MULTIPLE holes and an interior chunk to its tag function
pass	a tagged template weaves a hole into its expansion
pass	a tagged template whose tag is not a function is rejected
pass	a tagged template whose tag is unbound is a scope error
pass	a tagged template whose tag returns a non-Ast is a type error at the spliced site
pass	a tagged template's TAG resolves through an import and expands at the importer's site
pass	a tagged template's hole is threaded to the tag function and read
pass	a tagged template's spliced result folds through the ordinary path
pass	a tagged-template tag returning a finite Ast.Float expands normally (expander control)
pass	a tagged-template tag returning a non-canonical Ast.Float is rejected (the guard fires through the expander)
pass	a tail list fold reads its scrutinee handle from its own parameter slot
pass	a tail loop threading a projected boxed-sum accumulator and a projected cursor decodes correctly
pass	a tail loop threads a boxed-sum accumulator and a cursor projected from one builder tuple
pass	a tail mutual-recursive group where both partners perform folds
pass	a tail resume under a do inside a match arm folds and runs
pass	a tail resume under a let wrapping a do folds and runs
pass	a tail resume under a match inside a let folds and runs
pass	a tail resume under a three-level let-match-do nesting folds and runs
pass	a tail resume under nested lets folds and runs
pass	a tail-recursive HEAP accumulator builds and folds a 10000-deep spine in constant stack
pass	a tail-recursive accumulator loop runs to a large runtime N in constant stack
pass	a tail-recursive accumulator over a large runtime count iterates in constant stack
pass	a tail-recursive countdown loop runs to a very large runtime N
pass	a tail-recursive function returning a tuple is tuple-valued
pass	a tail-recursive list fold over a large runtime list runs in constant stack
pass	a tail-recursive string accumulator builds a runtime string and its length is measured
pass	a tail-resumptive arm is not hijacked by the pure one-hole fold when its body performs non-tail
pass	a tail-resumptive perform BEFORE an abort on the same spine abandons the pending continuation
pass	a taken const-fold trap surfaces its message as a compile error (CDZ0304)
pass	a taken trap over a Char const param on the bare-call path surfaces CDZ0304
pass	a taken trap over a Float64 const param on the bare-call path surfaces CDZ0304
pass	a taken trap over a Tuple const param on the bare-call path surfaces CDZ0304 (ca06)
pass	a task that sleeps then reads `now` observes the fast-forwarded clock (E5 step-3 gate)
pass	a task-facing `sleep` wrapper performs Sim.sleep — the surface a simulation is written against
pass	a tautology comparison against a masked value's derived range still traps on the operand
pass	a tautology comparison against the type minimum still traps on a div-by-zero operand
pass	a tautology comparison still folds when its operand cannot trap
pass	a telescoping Rational fold over 20 recursive steps equals the closed form exactly
pass	a template EXPANSION is a CHAMP Map key found by its directly-woven twin
pass	a third-generation overwrite path-copies away from both ancestors and all three read true
pass	a threaded String param consumed as the right concat operand survives the loop
pass	a threaded String param consumed twice per step survives the loop
pass	a three-argument closure exported to the host is called with all three
pass	a three-arm match consumed as an operand yields into the enclosing expression
pass	a three-arm runtime literal-tag dispatch hits the middle and last arms and misses past all three
pass	a three-deep known-disc nested match reads the right payload at every level
pass	a three-deep tuple projection chain retains the innermost consumed child
pass	a three-element runtime tuple is returned as a result
pass	a three-hole tag reads its holes in exact left-to-right order
pass	a three-level curried spine applies through both intermediate closures
pass	a three-level nested multi-arg constructor destructures to every leaf payload
pass	a three-level nested record mismatch grows the dotted path
pass	a three-member memo group with a let-var-body recursion arm folds — the group threads the cache state
pass	a three-member mutual tail cycle shares one loop
pass	a three-parameter closure is applied at full arity through a recursive HOF
pass	a three-payload constructor fully curried builds the variant
pass	a three-payload constructor partially applied in a runtime tuple completes via eta-closure
pass	a too-FEW-operand `and` carries no fix (nothing to delete)
pass	a too-big literal inside BigInt.of carries no wrap fix and names the drop-the-wrapper repair
pass	a too-few map entry carries NO delete fix
pass	a too-few record field element carries NO delete fix
pass	a too-few-element guarded pattern carries no fix (nothing to delete)
pass	a too-short list refutes a leading-rest arm and falls through
pass	a too-wide tuple pattern rejects with no consequent unused-binding warning for its dead binders
pass	a top-bit u64 binding re-encodes to its exact source bytes
pass	a top-bit u64 binding widens to BigInt unsigned
pass	a top-bit-set u64 bin binding reads UNSIGNED not signed-wrapped
pass	a top-level annotation over a malformed inner def wraps no definition and is rejected
pass	a top-level annotation over a non-def list target wraps no definition and is rejected
pass	a top-level annotation over a non-form target wraps no definition and is rejected
pass	a top-level do-local fn capturing a constant lowers (capture control)
pass	a top-level module is named by a top-level def's body
pass	a top-level sum value definition may reference a sum value defined later
pass	a top-level tuple pattern matches a tuple produced by a runtime conditional
pass	a top-level value definition binds a name usable by the program's functions
pass	a top-level value definition binds a record projected by the program's functions
pass	a top-level value definition binds a sum value matched by the program's functions
pass	a top-level value definition may reference a value defined later in the module
pass	a top-level-do module member's Bool/Int mix is type-checked and rejected CDZ0203
pass	a total match on a partly-un-built sum compiles its dead arm's unconstrained payload
pass	a trailing ABORT after multi-site performs reads the fully-advanced state
pass	a trailing bytes segment accepts the leftover the fixed segments leave
pass	a trailing digit separator is a malformed literal, not the digits with it dropped
pass	a trailing state-READING single-site op is served after one two-site perform
pass	a trailing state-REPLACING single-site op is served after a two-site arm's performs
pass	a transformation maps a syntax tree to a syntax tree and preserves meaning
pass	a transformation observably rewrites the tree, not just its value
pass	a transformation over the built-in Ast composes across a function call
pass	a transitive chain of erasable newtypes in reversed declaration order composes
pass	a transitive import (app <- lib <- kernel) can use a theorem through re-exported kernel functions
pass	a transitive import cannot forge a theorem — the kernel's private constructor is unreachable from the app
pass	a transitive re-export reaches the base module's function through the middle module
pass	a transitively-homed lambda's perform result composes in the pass-through caller
pass	a trap DEFERRED in a tuple slot is FORCED when the tuple materializes into a list — even in a dropped element
pass	a trap message that is not a String is rejected
pass	a trap raised inside a host-called closure body reaches the host as a trap
pass	a trap raised on a consumed closure's result surfaces through the consumer as a trap
pass	a trap-init let in one handler branch short-circuits while the sibling threads state
pass	a trapping condition blocks the equal-branch collapse and still traps
pass	a trapping condition of a same-constructor if traps before either arm
pass	a trapping condition preempts the hoisted operator's trap
pass	a trapping element in the taken arm of a same-length list if still traps
pass	a trapping element in the untaken arm of a same-arity tuple if does not trap
pass	a trapping element in the untaken arm of a same-length list if does not trap
pass	a trapping field in the untaken arm of a same-shape record if does not trap
pass	a trapping guard on a non-matching literal arm is never evaluated
pass	a trapping init observed inside the `?`'s own operand is not elided
pass	a trapping payload in a non-taken arm of a same-constructor match does not trap
pass	a trapping payload in the TAKEN arm of a same-constructor if still traps
pass	a trapping payload in the taken arm of a same-constructor match still traps
pass	a trapping payload in the untaken arm of a same-constructor if does not trap
pass	a trapping scrutinee of a same-constructor match traps before any arm
pass	a trapping scrutinee of an all-same-body match is still evaluated
pass	a trapping shared payload before the differing position must not preempt a trapping cond
pass	a trie of 30 QUANTITY keys resolves a cross-normalized magnitude lookup at depth
pass	a trie of 40 MULTI-LIMB BigInt keys enumerates in magnitude order
pass	a trie of 40 RATIONAL keys with all-different denominators enumerates in numeric order
pass	a trie of 40 RECORD keys resolves descriptor-ordered field descent at depth
pass	a trie of 40 TUPLE keys resolves compound-key descent at depth
pass	a trie of 40 rope-built String keys resolves content descent at depth
pass	a trie spanning NEGATIVE and positive keys enumerates in signed numeric order
pass	a triple-nested match on the SAME variant-refined scrutinee folds every level to the known arm
pass	a triply-nested narrow Int8 add computes at the consuming width
pass	a trivially-true guard does not make its variant arm unconditional
pass	a truncating conversion keeps the low bits rather than trapping
pass	a truncating conversion of a negative value uses two's complement
pass	a truncating conversion to an unusual width keeps that width's low bits
pass	a tuple = whose Bool element derives from a const-divisor rem is emitted as valid wasm
pass	a tuple = with differing const-divisor Bool elements compares unequal
pass	a tuple ARGUMENT with one too many elements carries a delete fix on the trailing element
pass	a tuple ARGUMENT with too few elements carries an add (insert) fix appending a placeholder
pass	a tuple ARGUMENT with two too many elements is not one clean delete — no mechanical fix
pass	a tuple Map key inserted twice with different values collapses to one entry and last write wins
pass	a tuple PARAM member whose element is a list<u8>/Bytes leaf rebuilds the compound element in-cell
pass	a tuple PARAM member whose element is a nested record rebuilds the compound element in-cell
pass	a tuple WILDCARD trailing-rest binding param binds the leading element and discards the residual
pass	a tuple access out of arity on a function-returned tuple is a type error, not a trap
pass	a tuple annotated as a scalar type is rejected
pass	a tuple annotated with the wrong arity is rejected
pass	a tuple annotated with too many elements is rejected
pass	a tuple binding pattern against a non-tuple value is a shape error
pass	a tuple binding pattern nests to any depth
pass	a tuple binding pattern over a non-tuple value is a shape error
pass	a tuple compound export under a non-main name crosses to the host
pass	a tuple construction spread builds a tuple passed whole to a function
pass	a tuple construction spread flattens a runtime tuple's elements by position
pass	a tuple construction spread of a single tuple with no inline elements is that tuple
pass	a tuple construction spread with two spreads concatenates their elements in order
pass	a tuple containing an Option leaf orders by the declared Some-below-None
pass	a tuple destructure-let over a RECURSIVE heap-tuple init evaluates the init ONCE and reclaims it (no re-eval, no leak)
pass	a tuple element path drills into a nested record field
pass	a tuple element pattern against a scalar list element is a shape error, not a decline
pass	a tuple guard mixes an element binder with an enclosing parameter
pass	a tuple guard reads a ctor payload binder inside a tuple element
pass	a tuple guard reads a nested tuple's inner binders
pass	a tuple is constructed with positional elements
pass	a tuple key mixing int, rope, and float hashes and matches by all three leaf kinds
pass	a tuple key whose element is itself a float-bearing tuple keys by content
pass	a tuple key with a RUNTIME Rational leaf hits and misses by canonical content
pass	a tuple key with a computed-NaN leaf is found by the canonical NaN probe
pass	a tuple key with an ARITH-built BigInt leaf hits and misses by content
pass	a tuple mixing an int, a runtime ROPE, and a FLOAT compares whole by per-kind leaf walks
pass	a tuple of a bool and a sum is exhaustive when every bool×variant combination is covered
pass	a tuple of bools is exhaustive when every combination is covered
pass	a tuple of calls to a sibling function on recursive-sum values matches with constructor patterns
pass	a tuple of two bools enumerated as all four explicit combinations is exhaustive and dispatches
pass	a tuple of two bools missing one combination is non-exhaustive
pass	a tuple of two branch-built Options matches all four Some/None quadrants jointly
pass	a tuple of two rest-lists binds each leading head (two rest markers in one arm are linear)
pass	a tuple of two sums is exhaustive when every variant×variant combination is covered
pass	a tuple of two sums is matched with nested constructor patterns
pass	a tuple passed with too FEW elements names the arity delta
pass	a tuple passed with too MANY elements names the arity delta the other direction
pass	a tuple pattern against a Sign scrutinee is a type error
pass	a tuple pattern against a sum scrutinee is a type error
pass	a tuple pattern of the wrong arity is a type error
pass	a tuple pattern over a Map scrutinee is a type error
pass	a tuple pattern over a map scrutinee is a type error
pass	a tuple pattern over a non-tuple scrutinee is a type error
pass	a tuple pattern over an Int64 scrutinee is a type error
pass	a tuple pattern over its matching Tuple scrutinee kind is valid and matches (no over-rejection)
pass	a tuple pattern with a trailing rest binds the leading elements and the trailing sub-tuple
pass	a tuple payload at a late variant projects both fields by its own layout
pass	a tuple payload consumed INLINE in the sum arm projects and re-matches
pass	a tuple payload extracted through a helper return must not be rejected as a type error
pass	a tuple payload returned through a helper from a built-in Option must not trap
pass	a tuple scrutinee built from TWO performs — the guard relates both dispatch results
pass	a tuple splat composes with inline arguments
pass	a tuple splat feeds a rest parameter — the caller spreads, the callee gathers
pass	a tuple splat spreads a tuple held in a variable
pass	a tuple splat spreads its elements into a call's arguments
pass	a tuple splat's element values may be runtime
pass	a tuple trailing-rest is IRREFUTABLE, so it binds in a BINDING-PARAM position (not only a match arm)
pass	a tuple trailing-rest match gathers the trailing sub-tuple over a RUNTIME tuple
pass	a tuple trailing-rest parameter binds the leading element (irrefutable binding position)
pass	a tuple trailing-rest parameter binds the trailing sub-tuple to rest
pass	a tuple trailing-rest pattern binds in a LET binder position, like a fn parameter
pass	a tuple with a Char leaf orders by codepoint — Char is a blessed compound-ordering leaf
pass	a tuple with a Float64 element MISSES a Map key with a different float leaf
pass	a tuple with a Float64 element is a Map key found by a separately-built equal key
pass	a tuple with a Qty leaf as a map key hits by magnitude-and-unit content
pass	a tuple with a Symbol leaf as a SET element dedups and membership-checks by content
pass	a tuple with a runtime element is returned as a program result
pass	a tuple with every element computed at run time is returned as a result
pass	a tuple wrapping a runtime list projects both fields and reclaims to no live heap objects
pass	a tuple's size is static even when it holds a runtime element
pass	a tuple-destructuring fn (lambda) parameter binds its parts
pass	a tuple-destructuring lambda parameter binds like a def param
pass	a tuple-of-HEAP state — every dispatch grows BOTH components in one rebuild
pass	a tuple-of-two-sums match reads each sum's payload from its own slot (HM-unify shape)
pass	a tuple-parameter export returns a BigInt computed from its fields
pass	a tuple-parameter export returns a list computed from its fields
pass	a tuple-pattern parameter binds the halves of its pair argument
pass	a tuple-pattern parameter destructures a runtime-built tuple
pass	a tuple-pattern parameter over a tuple threaded from a helper call
pass	a tuple-pattern with BOTH binders used is clean — no unused-match-binding warning
pass	a tuple-projected loop invariant consumed per iteration accumulates stably
pass	a tuple-rest function branches on the type of a passed argument at compile time
pass	a tuple-returning closure alongside a plain export — the closure
pass	a tuple-returning closure alongside a plain export — the plain
pass	a tuple-wrapped self-application is rejected in bounded time, not a compiler stack overflow
pass	a twenty-deep push chain retains the still-live source binding
pass	a twice-used heap binder without a Some shell reclaims fully (the fusion-neutrality anchor)
pass	a two-argument built-in applied to three arguments names its arity
pass	a two-argument closure exported to the host is called with both arguments
pass	a two-argument list operation applied to a non-list names the expected argument type
pass	a two-arm Bool match selects its second (false) arm
pass	a two-arm if selecting between two runtime FLOAT values computes the chosen one (float select-ification)
pass	a two-arm if selecting between two runtime NARROW UInt8 values computes the chosen one at the payload width
pass	a two-arm if selecting between two runtime SIGNED Int8 values preserves the sign of the chosen leaf
pass	a two-arm list match reading an element binder keeps the branch — no speculative empty read
pass	a two-arm list match with constant arms dispatches branchlessly on the length — empty
pass	a two-arm match does not evaluate the unselected arm's trapping body
pass	a two-arm match whose wildcard binds the scrutinee and applies a trap-free op — selects, binder reads it
pass	a two-arm match whose wildcard binds the scrutinee — the zero-probe arm
pass	a two-constructor-element list arm falls through when the second tag differs
pass	a two-entry directly-built pqueue pops the HEAD continuation, not the tail
pass	a two-fixed-width magnitude clash under quantity arithmetic is CDZ0301 (no silent widening)
pass	a two-hole perform in a match scrutinee AND an arm folds via the one-shot refold
pass	a two-level UPDATE of a map-of-maps rebuilds the inner and leaves the old outer intact
pass	a two-level index reads an inner element of a list of lists
pass	a two-level nested record mismatch drills to the dotted leaf path
pass	a two-op composition where the second op's String argument is BUILT from the first's result
pass	a two-parameter closure is applied at full arity through a recursive HOF
pass	a two-parameter sum whose variants use the parameters in different orders instantiates correctly
pass	a two-payload constructor applied in curried form builds the variant
pass	a two-payload sum escapes its second variant with a bare name
pass	a two-sided squeeze refines to an exact point and folds an inner comparison — the intersection face of the point fact
pass	a two-site arm branches on SYMBOL equality of the state
pass	a two-site arm branching on the OP ARGUMENT with a hit-count state folds (threshold sift)
pass	a two-site arm branching on the STATE folds (the refold re-anchor serves state-reading conditions)
pass	a two-site arm gates on a PROJECTED field of the record state (rate limiter)
pass	a two-site arm over a BYTES state (bin-built seed, concat growth) with a trailing size reader
pass	a two-site arm over a HEAP STATE with a body free-var and a second state-reading op folds
pass	a two-site arm over a STRING state (concat on pass, hold on fail) with a trailing length reader
pass	a two-site arm whose condition reads the ARG AND the STATE together folds
pass	a two-site arm with HEAP resume values (empty vs two-element list) folds
pass	a two-site arm with a second op that replaces the state mid-chain folds
pass	a two-site resume arm reading the enclosing SEED param in its resume value folds
pass	a two-site resume arm whose resume VALUE reads an enclosing-fn param folds
pass	a two-site resume arm whose resume value is a heap LIST reading an enclosing param folds
pass	a two-step seed sequence advances (successive draws differ)
pass	a two-variant enum match with constant arms dispatches branchlessly — first variant
pass	a two-variant enum match with constant arms dispatches branchlessly — second variant
pass	a type annotation consistent with the value is transparent
pass	a type annotation with too few operands names the one part present
pass	a type annotation with too many operands names the three parts present
pass	a type declaration may carry a leading doc, ignored for the variants
pass	a type stored in a compound result cannot cross the boundary
pass	a type variable nested in a generic parameter annotation is an unbound name
pass	a type-name-colliding variant construct coexists with the width type it collides with
pass	a type-position typo does not suggest a nearer VALUE def
pass	a type-position typo still suggests a prelude type with a rename fix
pass	a type-position typo still suggests a real user type with a rename fix
pass	a type-suffixed numeric literal quotes as the (: <body> Type) annotation the suffix denotes
pass	a type-value flows through nested let bindings and crosses as its own type-of-types
pass	a type-value in integer-width position is rejected
pass	a type-value in integer-width position is rejected as non-natural
pass	a type-value nested in a compound result is one coded error, no cascade
pass	a type-valued parameter as a Bool-checked if condition is one coded error, no cascade
pass	a type-valued parameter in an arithmetic position is one coded error, no cascade
pass	a type-valued parameter threaded through a second call reaches Type.eq with its type intact
pass	a type-valued parameter threads through TWO relay layers beside a runtime operand
pass	a type-valued parameter under a function-arrow annotation dispatches an ad-hoc-polymorphic dict
pass	a type-variable effect operation type does not false-positive as an unknown type
pass	a typed RECORD result with a VARIANT field crosses the export boundary (declared world)
pass	a typed list<option<s64>> EXPORT result crosses as a WIT list of options
pass	a typed list<record> EXPORT result crosses as a WIT list of records (declared world)
pass	a typed list<tuple<s64, list<s64>>> EXPORT result — a NESTED-list element field crosses (declared world)
pass	a typed list<tuple<s64, variant>> EXPORT result — a VARIANT field inside a tuple element (declared world)
pass	a typed list<tuple<s64,s64>> EXPORT result crosses as a WIT list of tuples (declared world)
pass	a typed list<variant> EXPORT result crosses as a WIT list of variants (declared world)
pass	a typed option<Bytes> EXPORT result crosses (top-level option<list<u8>>)
pass	a typed record result with an option<COMPOUND> field crosses the export boundary (declared world)
pass	a typed reducer branching on a bool host-op result emits a request when true (via an imposed WIT world)
pass	a typed reducer performing a bytes host arg with a scalar result threads the u64 into the step (via an imposed WIT world)
pass	a typed reducer performing a list-of-scalars host arg emits, loads, and runs (via an imposed WIT world)
pass	a typed reducer performing a list<option<s64>> host arg emits, loads, and runs (via an imposed WIT world)
pass	a typed reducer performing a list<record-with-a-variant-field> host arg emits, loads, and runs (via an imposed WIT world)
pass	a typed reducer performing a list<record-with-option-field> host arg emits, loads, and runs (via an imposed WIT world)
pass	a typed reducer performing a list<record> host arg emits, loads, and runs (via an imposed WIT world)
pass	a typed reducer performing a list<tuple-with-a-variant-element> host arg emits, loads, and runs (via an imposed WIT world)
pass	a typed reducer performing a list<tuple> host arg emits, loads, and runs (via an imposed WIT world)
pass	a typed reducer performing a list<variant<scalar>> host arg emits, loads, and runs (via an imposed WIT world)
pass	a typed reducer performing a nested list<list<s64>> host arg emits, loads, and runs (via an imposed WIT world)
pass	a typed reducer performing a nested-record host arg with a variant<scalar> leaf emits, loads, and runs (via an imposed WIT world)
pass	a typed reducer performing a nested-record host arg with an option + bytes leaf emits, loads, and runs (via an imposed WIT world)
pass	a typed reducer performing a record-with-a-direct-bytes-field host arg emits, loads, and runs (via an imposed WIT world)
pass	a typed reducer performing a record-with-a-list-field host arg emits, loads, and runs (via an imposed WIT world)
pass	a typed reducer performing a record-with-a-tuple-field host arg emits, loads, and runs (via an imposed WIT world)
pass	a typed reducer performing a record-with-a-variant-f64-field host arg emits, loads, and runs (via an imposed WIT world)
pass	a typed reducer performing a record-with-a-variant-scalar-field host arg emits, loads, and runs (via an imposed WIT world)
pass	a typed reducer performing a record-with-a-variant-u32-field host arg emits, loads, and runs (via an imposed WIT world)
pass	a typed reducer performing a record-with-an-option-bytes-field host arg emits, loads, and runs (via an imposed WIT world)
pass	a typed reducer performing a record-with-an-option-scalar-field host arg emits, loads, and runs (via an imposed WIT world)
pass	a typed reducer performing a run.run result host op threads the Ok bytes into the step (via an imposed WIT world)
pass	a typed reducer performing an all-scalar record host arg emits, loads, and runs (via an imposed WIT world)
pass	a typed reducer threading a list<tuple<bytes,bytes>> host-op result branches on its length (via an imposed WIT world)
pass	a typed reducer threading a string host-op result branches on its byte-len (via an imposed WIT world)
pass	a typo'd handle effect name does not suggest a prelude variant constructor with no members
pass	a typo'd handle effect name prefers a member-accessible effect over an equidistant variant
pass	a u16 bin field reads correctly at every runtime slice offset, odd or even
pass	a u16 bin field straddles a byte-rope seam through a runtime-start slice
pass	a u16 segment encodes an integer big-endian by default
pass	a u16 segment round-trips at its maximum value
pass	a u32 bin binding with the top bit set does unsigned arithmetic
pass	a u32 segment encodes four big-endian bytes
pass	a u32 segment round-trips at its maximum value
pass	a u64 bin binding with the top bit set does unsigned arithmetic
pass	a u64 bin field with the top bit set reads unsigned across a rope seam
pass	a u64 pattern segment reads eight big-endian bytes back into an integer
pass	a u64 segment encodes eight big-endian bytes
pass	a unary constructor applied to a runtime value is returned as a program result
pass	a unary constructor is a single-arity function
pass	a unary variant applied to a wrong-arity tuple payload is a type error
pass	a unary variant applied to a wrong-type payload is a type error
pass	a unit MISMATCH in the op argument is rejected — the program cannot perform with a different unit
pass	a unit MISMATCH in the resume value is rejected — the arm cannot resume a different unit
pass	a unit multiplied by its own inverse cancels to the dimensionless unit
pass	a unit raised to the ZEROTH power is the dimensionless identity Unit.one
pass	a unit-result host op consumes its response row so the next value op reads its own (adv-65)
pass	a used NESTED-payload match binder is clean — no unused-match-binding warning
pass	a user (type Ast …) shadows the built-in, so Ast.module is no longer the self-reflection
pass	a user (type Ast …) shadows the built-in, so Ast.print is no longer the printer
pass	a user (type Ast …) shadows the built-in, so Ast.read is no longer the reader
pass	a user (type Ordering …) shadows the built-in, so Ordering.of is no longer the three-way comparison
pass	a user Name variant does not capture the Ast sum's Name variant
pass	a user SUM is constructed AND matched inside the arm (per-dispatch classification)
pass	a user SUM leaf inside a tuple key discriminates hits by variant AND payload
pass	a user function may be named quote — its signature is a binding form, not a quote
pass	a user generic Box applied with ZERO type arguments names the arity and the zero supplied
pass	a user generic Box over-applied with two type arguments is rejected with the same arity shape
pass	a user generic NESTED as the type argument of a built-in generic resolves in an annotation
pass	a user generic NESTED as the type argument of another user generic resolves in an annotation
pass	a user generic sum OVER-applied with too many type arguments in an annotation is rejected
pass	a user generic sum named BARE (unapplied) in an annotation needs a type argument
pass	a user generic sum over-applied with two type arguments names its one-argument arity
pass	a user generic sum whose nullary variant carries a lowercase-unit payload has exactly one type param
pass	a user sum NAMED `Box` wrapping a recursive type does not collide with the heap pointer
pass	a user sum NAMED after a prelude sum shadows it without a duplicate definition
pass	a user sum named Ast renders BARE with RUNTIME payloads through one compiled body
pass	a user sum named Ast with NON-colliding variants renders its ctor heads BARE on all backends
pass	a user sum whose variant name collides with a built-in renders that head QUALIFIED on all backends
pass	a user sum's nullary variant escapes with its bare variant name
pass	a user sum's payload variant escapes with its bare variant name
pass	a user variant named like a prelude MODULE (`Num`) constructs directly through mk-inline over a runtime arg
pass	a user variant named like a prelude MODULE (`Num`) constructs through a FULLY const-folded call (no misresolution to the module)
pass	a user-declared generic sum resolves by name in a parameter type annotation
pass	a user-generic constructor pattern binds and computes through nesting, self-nesting, and a record field
pass	a user-module member miss stays CDZ0201 (not a record field)
pass	a user-sum variant value as a Map KEY keys by variant and payload
pass	a user-written EUCLID gcd loops on remainder and certifies divisibility of both operands
pass	a user-written FILTER combinator keeps elements passing a captured predicate
pass	a user-written FRACTION ADD over tuples reduces by gcd and agrees with the built-in Rational
pass	a user-written MAP combinator builds a transformed list through its fn parameter
pass	a user-written ZIP walks two lists in lockstep, truncating at the shorter
pass	a utf8 bin match with no catch-all is non-exhaustive
pass	a utf8 bin segment binds a decoded string when the bytes are well-formed
pass	a utf8 bin segment is a non-match on ill-formed bytes, forcing the catch-all
pass	a utf8 segment sized by a CONSTANT LITERAL decodes a fixed-width string
pass	a utf8-decoding bin match with no catch-all is non-exhaustive
pass	a valid @tag string annotation is accepted and unwraps to its def
pass	a valid Float magnitude Qty.of is accepted (no over-rejection of the numeric case)
pass	a valid Unit.one Qty.of is accepted and Qty.value recovers the magnitude
pass	a valid base-unit Qty.of is accepted and Qty.value recovers the magnitude (no over-rejection)
pass	a valid deeply-nested generic type argument still resolves and runs (the crash-guard control)
pass	a valid one-predicate @ensures is accepted and the def runs
pass	a valid one-predicate @requires is accepted and the def runs
pass	a value compound in a type annotation's type position is rejected
pass	a value declaration in a do block is in scope for the following forms
pass	a value definition may carry a leading doc, like a function definition
pass	a value definition with no body is rejected
pass	a value juxtaposed with a COMPOUND type names the shape but carries no fix
pass	a value juxtaposed with a type without the colon names the missing-colon annotation with an add-: fix
pass	a value survives a self-aliased map-merge — lookup after (Map.merge m m) returns m's own value
pass	a value-eq guard on a LITERAL-probe arm drives a tail-recursive loop
pass	a value-eq guard on a SUM-match arm drives a tail-recursive loop
pass	a value-eq guard on a wildcard arm drives a tail-recursive loop
pass	a value-equality over a borrowed heap list leaves no live heap objects
pass	a value-leaving host-call statement in a do-body runs and its result is dropped (dead-init sibling)
pass	a value-pick (if p a b) selects the arg matching the runtime condition's truth value
pass	a variadic Ast form is folded via a tail-splice rest-binder over its operands
pass	a variant carrying a Char payload constructs, matches, and binds it
pass	a variant carrying a RECORD payload constructs and matches
pass	a variant carrying a Rational payload constructs and matches
pass	a variant carrying a Symbol payload constructs, matches, and binds it
pass	a variant carrying a record payload escapes to the host
pass	a variant carrying a structural-record payload in the (: name type) field spelling is non-nullary and readable
pass	a variant carrying an EMPTY-sum payload lays out and its dead arm zero-arm-matches
pass	a variant constructor String-into-Bytes payload mismatch offers the to-bytes wrap fix
pass	a variant constructor int-valued-float payload mismatch offers the drop-the-fraction fix
pass	a variant constructor int-width payload mismatch offers the of-conversion wrap fix
pass	a variant constructor payload mismatch with no coercion carries the bare reject and no fix
pass	a variant form headed by a non-name is rejected
pass	a variant guard reads the payload binder AND an enclosing binding
pass	a variant named Bool shadows the prelude Bool within its declaring module
pass	a variant named like a Result DATA constructor (Ok) is reachable bare in its declaring module
pass	a variant named like a prelude DATA constructor is reachable BARE and builds the local variant
pass	a variant named like a prelude type is constructed and matched in its declaring module
pass	a variant pattern over a Bool scrutinee is a type error
pass	a variant pattern over a scalar scrutinee is a type error
pass	a variant with a TUPLE payload crosses as a typed WIT variant (declared world)
pass	a variant with a wrong-type payload as a direct match scrutinee is a type error
pass	a variant with an explicit EMPTY-TUPLE payload keeps its tuple form, distinct from a nullary unit
pass	a variant-WITH-PAYLOAD host RESULT is lifted into a guest Sum and matched (via an imposed WIT world)
pass	a variant-payload binder shadow is scoped to its arm, not the whole def
pass	a variant-payload binder shadowing a param in a called def binds the payload
pass	a variant-with-a-COMPOUND-PAYLOAD host RESULT is lifted (bytes payload case) (via an imposed WIT world)
pass	a variant-with-payload EXPORT result crosses as a typed WIT variant (declared world)
pass	a velocity multiplied by a time recovers the distance dimension
pass	a versioned interface name agrees across both sides and runs
pass	a view-interned Symbol participates in Set.to-list content order
pass	a violated @requires traps BEFORE the body's abortive perform can fire
pass	a well-formed String-message trap in a polymorphic position compiles and runs clean
pass	a well-formed annotation still checks and accepts a matching type (the control)
pass	a well-formed member access raises no arity fault and projects the field
pass	a well-formed non-type in a let-binder annotation is rejected
pass	a well-formed non-type in a variant payload is rejected CDZ0203
pass	a well-formed overflow pragma is accepted and does not block module registration
pass	a well-formed record parameter annotation compiles and the field reads back
pass	a well-formed rest-last map pattern matches the keyed entry and binds the rest
pass	a well-formed tuple pattern with BOTH binders unused warns each — exactly two CDZ0306
pass	a well-formed two-operand annotation does not false-positive as a malformed-arity reject
pass	a well-typed Bool guard condition compiles and runs clean
pass	a well-typed same-shape tuple ordering compiles and runs the lexicographic walk
pass	a whole LIST of quantities renders every element scaled to reference in the value form
pass	a whole MAP holding a quantity VALUE renders it scaled to reference in the value form
pass	a whole rational carries a denominator of one
pass	a whole-module alias projects an exported FUNCTION and applies it
pass	a wide NEGATIVE integer literal pattern matches its runtime scrutinee
pass	a wide UInt64 constant add overflows Int64 but fits UInt64 and folds
pass	a wide fixed-size integer width is reserved, not yet a valid type
pass	a wide multi-column literal match returns the matched arm's value over a runtime scrutinee (S2 emit-reshape value guard)
pass	a wide multi-column match over a bound tuple PARAMETER returns the matched arm's value (S2 emit-reshape value guard, param shape)
pass	a wide record with one wrong field TYPE pinpoints the culprit field
pass	a wide unsigned constant comparison folds by magnitude, not the i64 bit pattern
pass	a wide-unsigned match probe past i64 is not mistaken for out-of-range and still fires
pass	a widening runtime wrap ZERO-extends an UNSIGNED source
pass	a widening runtime wrap extends a SIGNED source by its source sign
pass	a wider narrow-width wrapping-mul constant folds by wrapping to the type width
pass	a wider runtime value in a fixed-width segment is a compile-time type error
pass	a wider signed Int16 into a signed i8 segment is a width type error
pass	a wildcard in a nested pattern position ignores that element
pass	a wildcard in the list rest slot is a valid rest binder and matches
pass	a wildcard used as a function argument names the binding-position misuse
pass	a wildcard used as an operator operand names the binding-position misuse
pass	a wildcard-exported variant whose name shadows a prelude type is constructible in an importer
pass	a wildcard-less Bool match returning Bool negates its scrutinee
pass	a wildcard-less char match is non-exhaustive (Char is an open type)
pass	a without-extend re-wrap inside a sum payload keeps the map-borne record's new field value
pass	a wrap from a type that already fits the target is the identity
pass	a wrap-narrowed MULTI-BYTE segment decodes the truncated value back
pass	a wrapped byte's zero equality tests the masked value, not the wide slot
pass	a wrapper (Rational) field projected off a nullary record folds through a rational op — no borrow-ownership decline
pass	a wrong-arity built-in generic in a variant payload is rejected at the declaration
pass	a wrong-arity prelude ctor in a VALUE annotation routes through the same arity helper
pass	a wrong-arity trap keeps the over-application message, not the message-type reject
pass	a wrong-arity tuple binding pattern is a shape error
pass	a wrong-arity tuple pattern nested under a constructor pattern is a type error
pass	a wrong-arity user generic in a variant payload is rejected at the declaration
pass	a wrong-arity user generic sum in a VALUE annotation routes through the same arity check
pass	a wrong-constructor pattern over a newtype scrutinee is a type error
pass	a wrong-element-type List.push names the operation and its expected/actual types
pass	a wrong-sum variant pattern head lists the MATCHED type's variants
pass	a wrong-type argument to a conversion op names the operation
pass	a wrong-typed Option field in a direct record arg still rejects
pass	a wrong-typed sibling arg against a type-valued parameter is rejected at the checking boundary
pass	a wrong-width value in a multi-byte bit-field is a compile-time type error
pass	a zero float width is rejected as non-admitted and carries no fix
pass	a zero integer width is rejected and carries no fix (no confident target)
pass	a zero rational canonicalizes to zero over one however it was constructed
pass	a zero rational is the same value regardless of the denominator's sign or magnitude
pass	a zero rational produced by arithmetic is the same canonical zero
pass	a zero rational with a NEGATIVE denominator still canonicalizes to 0/1 — sign placement is moot at zero
pass	a zero-Duration sleep files at the current instant — pop-min then reinsert keeps order
pass	a zero-arm match on an empty-sum scrutinee is exhaustive
pass	a zero-arm match on an inhabited scrutinee is non-exhaustive
pass	a zero-arm match on an inhabited sum is non-exhaustive
pass	a zero-bit integer width is rejected
pass	a zero-leading list rest pattern binds the whole list in a def parameter
pass	a zero-leading nested rest-list element binds the empty inner list (len 0)
pass	a zero-length slice is the empty byte sequence
pass	a zero-operand binary operator faults CDZ0201 takes-exactly-2
pass	a zero-width integer in an unused parameter is rejected
pass	a2p1 the abort VALUE is a three-op chain — every hop advances the enclosing thread before the region tears down
pass	aa1 the resume VALUE is a deep pure expression over the op arg AND state — (v+s)^2 - v*s per dispatch
pass	aa2 a QUADRATIC next-state (s^2+1) — three dispatches, the state squaring away from the seed
pass	aa3 REMAINDER arithmetic in the resume value — (% s 7) cycles as the +5 stride wraps the modulus
pass	aa4 a THREE-WAY comparison arm — the resume value encodes gt/eq/lt as 1/10/100 against the advancing state
pass	aa5 DIVISION in the resume value with a subtracting stride — quotients shrink as the state walks down
pass	ab1d the scalar twin — a conditional abort under a SCALAR-state outer, pre-abort advance committed
pass	ab1e a branch-conditional abort under a STRING-state outer handler — the taken abort skips the post-abort emit, the untaken row grows the rope
pass	ab2 a conditional abort under a MAP-state outer — the pre-abort insert is committed either way
pass	ab21 the arm ABORTS (no resume) with a value built from two lookup-matches over a computed-key insert — the width-partition holds on the abort path
pass	ab3 the abort VALUE is itself a draw from the outer STRING-state handler — the pre-abort dispatch commits before the unwind
pass	ab4 TWO sequential abort regions under one STRING-state outer — an aborted region leaves the rope where it was
pass	ab8 a draw picks WHICH of two nested abort handlers fires — outer-abort skips the inner arm's scale and the tail draw
pass	abd1 an inner ABORT discards its continuation — the outer handler's state advances only for dispatches that ran
pass	abd2 an abort arm observes the state threaded by earlier resuming dispatches
pass	abd3 an abort VALUE computed by delegating to the outer handler threads the outer state
pass	abf1 one handler mixes a RESUMPTIVE op and an ABORTIVE op — two marks advance the state, the abort carries their mix out
pass	abl1 a FOREIGN LEVY BEFORE AN ABORT — the inner arm levies the outer handler then answers WITHOUT resuming so the body's pending addition is abandoned, the levy's state write survives the abandonment and surfaces in the outer audit, and dropping the doomed frame's levy (it precedes an abort so a lowering might skip the whole arm prefix) shifts the audit digit while the abort value holds
pass	abm2 the ABORT ARM ITSELF PERFORMS ON AN OUTER HANDLER — the bail arm builds its aborting answer from a tenfold of the state its two resumed ticks accumulated PLUS an outer audit drawn while aborting, the audit advances the outer thread so the body's later audit reads five higher, and the abort's own foreign perform threads exactly once through the surviving outer state
pass	abmin10 TWO nested foreign abort-only handles under the conditional abort
pass	abmin12 BOTH branches abort (to different handlers) — no value path remains
pass	abmin2 outer-abort under a LET+IF inside the unrelated inner handle
pass	abmin3 a pure-binding LET wrapping the conditional foreign abort homes correctly
pass	abmin4 if WITHOUT let
pass	abmin9 the RESUMPTIVE flip of abmin4 — A's arm resumes, so 900307 IS the correct answer here
pass	abr1 an abortive arm drops the LIST handler state (no live objects)
pass	abr3 a heap-valued abort ARGUMENT is consumed by the arm and dropped
pass	abstract theorems stored in a MAP stay unforgeable and usable — lookup returns a real Thm
pass	abstract-type values live in an importer's map and round-trip through module ops
pass	abx1 two-op effect, BOTH arms resuming (control)
pass	abx2 two-op effect, one resuming one ABORTIVE (scalar state)
todo	abx3 mixed resume+abortive arms over a growing LIST state whose ABORT arm reads the state declines cleanly (HANDLER_NOT_REDUCIBLE, not a CDZ0101 mis-reject)
pass	abx4 mixed arms, list state, abort arm IGNORES the state
pass	abx5 single-op ABORTIVE with list state read by the arm (ab1 twin, resume absent entirely)
pass	ac1 a pipeline chain alternating a 1-ary and a 2-ary op — each result feeds the next call's argument while the thread advances
pass	ac1 a runtime-BUILT Ast equals the quoted constant structurally and reclaims clean (the construction/quote identity contract)
pass	ac2 ten build+walk frames over runtime-constructed Ast chains leak linearly (the construction face of the walk-leak)
pass	ac2 the chain's MIDDLE result routes the branch that picks WHICH op finishes the pipeline
pass	ac3 the pipeline BOUNCES between two effects — inner B's result feeds outer E's op, whose result feeds B again
pass	accessing through an empty-side split-at is usable, like the equivalent literal
pass	accumulator introduction preserves the zero-iteration exit and the body's trap
pass	accumulator introduction threads a transformed extra parameter through a multi-parameter recursion
pass	adding a boolean to a number names the scalar kind boundary
pass	adding a cubed length to a squared length is a dimension mismatch
pass	adding a data-rate to a length is a compile-time dimension error over a composite dimension
pass	adding a degree quantity to a radian quantity is a dimension mismatch
pass	adding a length and a time is a dimensional mismatch naming the operation and bare units
pass	adding a quantity and a bare number offers the same-unit Qty.of wrap fix
pass	adding a quantity to a NON-numeric value is a plain type mismatch, not a dimension slip
pass	adding a quantity to a non-numeric Option operand is a plain CDZ0203 type mismatch, not a dimension slip
pass	adding a squared length to a plain length is a dimension mismatch
pass	adding adjacent high-power quantities is a mismatch (meter^100 + meter^101)
pass	adding an integer to a nominal newtype names the kind boundary
pass	adding an integer to a user sum names the kind boundary, not a phantom Int64 clash
pass	adding quantities of incompatible dimension is a compile-time error
pass	adding rational-magnitude quantities of incompatible dimension is still a compile-time error
pass	adding two data-rate quantities of the same composite dimension is exact
pass	adding two equal high-power quantities joins (meter^100 + meter^100)
pass	adding two named family units of one dimension converts to the reference (Float)
pass	adding two quantities of the same dimension keeps that dimension
pass	adding two squared-length quantities is well-dimensioned
pass	adding two units of one dimension converts to the reference unit exactly
pass	addition of a string and a byte sequence names both text types
pass	addition of a string and a list names both non-numeric types
pass	addition of two bytes is rejected — offers the Bytes.concat rewrite
pass	addition of two lists is rejected — a list is not a number
pass	addition of two records is rejected — a record is not a number
pass	addition of two sets is rejected — a set is not a number
pass	addition of two strings is rejected — text is not a number
pass	addition of two symbols is rejected — a symbol is not a number
pass	adv-20 DEEP nest — a THREE-level nested let in the let-init folds, guarding arbitrary recursion depth
pass	adv-20 class is BROADER than the Bytes decode — a state-derived OPTION LET-bound then consumed by a plain Some/None match also folds (flatten+pin fix landed; regression-guard)
pass	adv-20 nested-let — a NESTED let inside the let-init folds (recursive flatten; regression-guard)
pass	adv54 a let-bound to-bytes of a runtime string slice read twice sees both bytes
pass	adv54b a let-bound Bytes.concat of slice-view to-bytes read twice sees the concatenated bytes
pass	advancing the clock past the UInt64 nanosecond range traps rather than silently wrapping
pass	ae1 SUBTRACTION of two same-op draws as a 2-ary op's args — the antisymmetry pins left-to-right order exactly
pass	ae10 TUPLE literal of three draws matched immediately as scrutinee — positions survive the construct-then-destructure round trip
pass	ae11 a DO-block as an argument — its DISCARDED interior draw still advances the state before the block's value draw
pass	ae12 an IF-expression as an argument whose CONDITION draws — the taken branch decides whether a second draw fires before the next arg
pass	ae2 draw-PURE-draw argument positions to a pure 3-ary fn — the middle constant sits between two advancing draws
pass	ae3 THREE same-op draws as a 3-ary OP's own args — order pinned inside the op's argument list itself
pass	ae4 draw / performing-HELPER / draw argument positions — the middle arg performs through a def boundary
pass	ae5 short-circuit AND with DRAWS on both sides — the skipped right draw leaves the state thread untouched
pass	ae6 short-circuit OR with DRAWS on both sides — the right draw fires only when the left is false
pass	ae7 draw NESTED under a pure unary call in the first arg slot, second slot a bare draw — nesting must not reorder
pass	ae8 LIST literal of three draws — element positions carry the draw order into the structure
pass	ae9 RECORD literal of two draws read back by PROJECTION — field-init order in the literal drives the state thread
pass	aeq1 bare equality over two runtime Ast.encode results
pass	aeq2 bare equality of String.to-bytes twins
pass	aeq3 arena Bytes laundered through fn params bare-compare equal
pass	aeq4 a slice VIEW bare-compares equal to its flat twin
pass	ag2 TWO aggregator effects interleaved — each keeps its own running sum, weighted reads pin the four-feed order
pass	ag3 one resumptive dispatch then an ABORT on the same handler — the abort arm reads the advanced state
pass	ai1 the algebraic identities compute the operand or its annihilator (17-fold checksum)
pass	ai2 an identity that keeps the operand preserves its overflow trap ((+ (* a b) 0))
pass	ai3 a same-operand & keeps the operand and preserves its overflow trap ((& (* a b) (* a b)))
pass	al1 chained LET locals inside the arm — intermediate names feed both the resume value and the next-state
pass	al2 a PURE helper called from the arm for BOTH slots — the arm delegates its computation to a def
pass	al3 a two-site resume where EACH branch has its own LET local — gap/overshoot named per path, different strides
pass	al4 ONE arm-local feeds BOTH slots — the score accumulates into the base while the count rides beside
pass	al5 an arm-LOCAL named the same as a body-side binder — hygiene keeps the two w's separate across dispatches
pass	all four leaf kinds in one quoted form dispatch their own tags
pass	all-nullary enum equality compares discriminants
pass	alternating prepend and push keep both list ends straight
pass	an 8-bit-integer closure crosses the host boundary
pass	an @ensures on a PERFORMING def called TWICE under one handler — both effectful results checked
pass	an @ensures on a recursive def is re-checked at every EXIT including self-call returns (not only the outermost)
pass	an @ensures over a MATCH-bodied def wraps the whole match — the postcondition checks the match's result
pass	an @ensures predicate reading a top-level GLOBAL alongside ret resolves and enforces (resolution seam)
pass	an @ensures predicate referencing an unbound name is rejected CDZ0101
pass	an @ensures predicate that is NOT Bool-typed is REJECTED CDZ0203 — the postcondition must denote a truth value too
pass	an @ensures predicate that itself traps keeps its own trap kind (not the @ensures-failed unreachable)
pass	an @ensures relating the RESULT to a PARAM over heap values enforces growth
pass	an @invariant newtype REBUILT through a chain of constructor calls re-establishes each time
pass	an @invariant newtype constructed INSIDE a handler arm establishes per resume
pass	an @invariant newtype is constructed from PERFORM results
pass	an @param of a Bool type generates a Bool-typed accessor
pass	an @param of a Float64 type generates a Float64-typed accessor
pass	an @param site generates a Param accessor a host delegation reads at run time
pass	an @param with no widget config still generates its typed accessor
pass	an @requires predicate referencing an unbound name is rejected CDZ0101
pass	an ABORT in the first handle leaves the SECOND handle's dispatch untouched
pass	an ABORTING arm applies the CLOSURE STATE for its final answer
pass	an ABORTING arm derives its answer from an Ast op-arg and discards the continuation
pass	an ABORTIVE arm whose value is a COMPOUND matches the handle body type and folds
pass	an ABSORBING (* v 0) fold over a TRAPPING lazy binding preserves the trap (does not fold to 0)
pass	an ANAGRAM check builds per-string scalar frequency maps and compares them by map equality
pass	an ANNOTATED empty-list Set key determines the element type and compiles
pass	an AST from quasiquoting a runtime value equals the same AST built by quote
pass	an Ast as a MAP key looks up by structural content
pass	an Ast node as the effect OP ARGUMENT is destructured by the arm
pass	an Ast-value-spliced template round-trips through encode and decode
pass	an Ast.Bool and an Ast.Int are distinct by variant tag not by numeric value
pass	an Ast.Bytes carrying NUL and high bytes round-trips through encode and decode
pass	an Ast.Bytes nested in an Ast.List round-trips through encode and decode
pass	an Ast.Bytes node constructs and deconstructs by pattern matching
pass	an Ast.Bytes node round-trips through encode and decode
pass	an Ast.Char node round-trips through encode and decode
todo	an Ast.Float node cannot carry Float64.Infinity (declines, like nan)
pass	an Ast.Int carries a BEYOND-64-bit literal losslessly through quote
pass	an Ast.Int stores an integer wider than Int64 without loss
pass	an Ast.List handler STATE accumulates a node per perform and each resume reads the prior length
pass	an Ast.Symbol node round-trips through encode and decode
pass	an EFFECTFUL @ensures predicate is value-transparent when SATISFIED — the body's own perform runs first
pass	an EFFECTFUL @ensures predicate performs under the caller's handler at body-EXIT, after the body's own perform
pass	an EFFECTFUL @requires predicate performs under the caller's handler and advances its state before the body
pass	an EMPTY Bytes arg crosses the host boundary
pass	an EMPTY splice mid-list closes the gap, a singleton fills it, and two splices around a literal both land
pass	an EQUALITY test refines the same test in its own else to known-false
pass	an EVENT-SOURCING fold replays a mixed-variant list, a RESET discarding prior state
pass	an EXPORT naming an unbound definition is rejected
pass	an EXPORTED definition is a reachable entry and does not warn unused
pass	an IEC binary prefix converts exactly over Int64
pass	an IEC binary prefix scales a unit by a power of two
pass	an IEC-prefixed quantity displays scaled to its reference over Int64
pass	an IF nested in a match arm re-tests the binder with a MAP lookup (the unguarded twin)
pass	an IF-join with an unsolved-Var arm and an empty-list sibling grounds like a match join
pass	an IN-PROGRAM arm resumes a Qty built in the arm — the erased-scalar crossing without a host
pass	an IN-RANGE Map.insert value via a sibling-inferred width computes
pass	an IN-RANGE Set.of element via a sibling-inferred width computes
pass	an IN-RANGE literal in a Set under a narrow annotation compiles and runs (no over-rejection)
pass	an IN-RANGE literal in a tuple nested inside a set element still compiles — the nested check is not over-broad
pass	an IN-RANGE literal in the transitive-arith-width position compiles and runs (no over-rejection)
pass	an IN-RANGE literal through a Map.insert builder chain compiles (the CDZ0302 control)
pass	an IN-RANGE literal through a Map.insert builder chain compiles and runs (no over-rejection)
pass	an INCLUSIVE ordering refinement folds at the boundary but must not over-refine one past it
pass	an INFINITY-axiom theorem is unforgeable — its Thm cannot be fabricated outside the kernel
pass	an ISOLATED multiply with no narrow-width ancestor leaves the literal at its Int64 default
pass	an Instant and a Duration are DISTINCT nominal types — a point cannot be used where a span is expected
pass	an Int 12 sum past its maximum is rejected CDZ0304
pass	an Int argument to a Bool-annotated parameter is rejected
pass	an Int argument to a parameter used as a Bool condition is rejected
pass	an Int-literal pattern over a String scrutinee is a type error
pass	an Int16 sum-payload literal-match hits the literal arm, result widens to Int64
pass	an Int32 sum-payload literal-match misses, binds the payload widened to Int64
pass	an Int64 into a non-aliased bits field names the member-form wrap conversion
pass	an Int64 sum-payload literal-match needs no widening (width control)
pass	an Int64 value into a u8 segment is a width type error naming the aliased wrap+of conversions
pass	an Int8 comparison against its own type bound folds to a constant
pass	an Int8 sum-payload literal-match misses, binds the payload widened to Int64
pass	an Int8 value into an unsigned u8 segment is a signedness mismatch
pass	an Int8 wrapping-add result drives a comparison — the SIGN-EXTENDED masked value is compared
pass	an N suffix lets a huge literal need no annotation
pass	an N-suffixed integer literal is a BigInt
pass	an N-suffixed token with a NON-numeric body gets the generic malformed message, not the N-suffix explanation
pass	an OBSERVED performing tail loop of 20k iterations keeps constant stack (repackage-tail-call)
pass	an OPEN-ROW helper projects from the record state INSIDE the arm
pass	an OPEN-sum value stored as a MAP VALUE matches back out with its open-tail arm
pass	an OPTION as op ARGUMENT — the arm matches Some/None it was handed, per dispatch
pass	an OPTION handler state TOGGLES its variant per perform
pass	an OPTION payload quantity renders scaled to its reference in the value form
pass	an ORDER-PRESERVING dedup threads a seen-Set and an out-List as twin accumulators
pass	an OVERFLOWING literal to a narrow effect-op parameter is rejected
pass	an Ok carrying a bare None type-checks under a whole-expression annotation
pass	an Option (sum) entry argument is marshalled as a native Option
pass	an Option built by an if with the nullary branch first type-checks
pass	an Option built by an if with the payload branch first type-checks (the control)
pass	an Option built by one dispatch is STORED into the tuple state and drained by a later dispatch — Some and None both persist
pass	an Option carrying a Result is matched across two different sum types
pass	an Option pipeline chains the SAME fallible step twice and reports WHICH step failed
pass	an Option pipeline short-circuits on the first None
pass	an Option-of-HEAP handler state transitions None to Some and grows the payload
pass	an Option-of-Option classifies three ways at RUNTIME through one compiled match
pass	an Option-of-Option survives a Map VALUE cell with its exact nesting
pass	an Option-of-list match covering only Some still rejects (the None sibling is uncovered)
pass	an Option-producing recursive generic ties its payload to the list element
pass	an Option.expect payload consumed per iteration while the option is threaded accumulates stably
pass	an R-suffixed decimal literal is its exact fraction
pass	an R-suffixed integer literal is that integer over one
pass	an R-suffixed literal composes with exact rational arithmetic
pass	an RPN evaluator drives a LIST-as-STACK through token dispatch — push, pop-two, apply
pass	an SI decimal prefix scales a unit by a power of ten
pass	an UNANNOTATED generic compose instantiates at a scalar arrow AND a String arrow in one program
pass	an UNBOUND unit name in Qty.of's second-arg position surfaces its own unbound error, not ALSO the not-a-unit reject
pass	an UNCALLED stored closure with a type-conflicting param is REJECTED at compile, not miscompiled
pass	an UNCONSTRAINED add is NOT dischargeable: with no precondition bound, the no-overflow obligation cannot be closed (the check must stay)
pass	an UNDER-applied binary operator curries — completing it yields a value
pass	an UNEVEN large-vector RRB concat (short left, multi-node right) rebalances the seam correctly
pass	an UNPROVABLE (runtime) trap in a dropped position earns NO dead-trap warning
pass	an UNREFERENCED non-scalar-param helper stays opt-level-equivalent under force-lower-all
pass	an UNSIGNED branch refinement decides a nested comparison and its soundness twin must not over-refine
pass	an UNSIGNED lower-bound refinement must not fabricate an i64::MAX ceiling for a UInt64 above it
pass	an UNZIP walk splits a list of pairs into two parallel lists that re-zip to the original
pass	an UPSERT into a Map of Lists creates the fresh entry on miss and appends on hit
pass	an `_`-prefixed anonymous-lambda parameter is silenced — no unused-parameter warning
pass	an `_`-prefixed variant-payload match binder is silenced — no unused-match-binding warning
pass	an `and` whose first RESUMING perform is true evaluates the second: both advances land
pass	an `and` with too many operands offers a delete-the-surplus fix
pass	an `if` missing its else branch names the missing else and offers to add a trap placeholder
pass	an `if` with the added trap-placeholder else branch compiles and runs
pass	an `if` with too many operands offers a delete-the-surplus fix
pass	an `or` short-circuit SKIPS a resuming perform, and the skip is observable through the state
pass	an `or` with too many operands offers a delete-the-surplus fix
pass	an abort BEFORE a tail-resumptive perform wins and the later perform never runs
pass	an abort abandons a pending borrowed map lookup and the map survives
pass	an abort abandons a pending consuming op and the shared binding survives
pass	an abort deeply nested under pure operators is still abandoned to the arm value
pass	an abort in a MATCH-SCRUTINEE abandons the whole match
pass	an abort under an OUTER tail-resumptive handler of a DIFFERENT effect abandons to the arm value
pass	an abort value COMPUTED from the op argument folds
pass	an aborting arm's unread op-argument still threads its foreign perform's state advance
pass	an abortive arm READS the heap LIST op argument it was handed — the payload survives the abort
pass	an abortive arm returns a MAP built FROM its heap op argument as the handle's value
pass	an abortive arm that READS a heap-typed (List) handler state folds — the List face
pass	an abortive arm that READS a heap-typed (Map) handler state folds — seed-let-lift on the abort path
pass	an abortive arm whose value type mismatches the op RESULT type declines
pass	an abortive arm yields a RECURSIVE-SUM spine as the handle's value
pass	an abortive arm yields a heap LIST built in the arm as the handle's value
pass	an abortive handler ISSUES a host call sequenced BEFORE the abort in its discarded body
pass	an abortive handler abandons a host call in the path it discards
pass	an abortive handler arm never resumes, so its value becomes the handle's value
pass	an abortive handler arm performed with a RUNTIME argument abandons the computation and returns it
pass	an abortive handler arm with a runtime perform argument yields that runtime value as the handle's value
pass	an abortive handler discards a suspended body holding live rope and map handles
pass	an abortive handler over a non-tail MUTUAL recursion folds to the abort value (99)
pass	an abortive let-init after an effectful binding folds via inner-handle pre-reduction
pass	an abortive perform MID-WALK carries the accumulated state out as its argument
pass	an abortive perform beside an effectful sibling under a nested handle folds via inner pre-reduction
pass	an abortive perform deep in a call chain unwinds every intervening frame to the top handler
pass	an abortive perform in a MATCH ARM body is branch-local and folds per arm
pass	an abortive perform in a STRICT OPERAND referencing a let-local binding stays in scope
pass	an abortive perform in a body tail referencing a do-local binding stays in scope
pass	an abortive perform in a conditional let binding abandons the computation
pass	an abortive perform in a conditional let-INIT hoists (aborting branch) and folds
pass	an abortive perform in a connective that is an if-condition abandons the computation when the connective reaches it
pass	an abortive perform in a contracted body skips the @ensures (an abandoned body has no result)
pass	an abortive perform in a fused-match arm carries the payload binder out and abandons the rest
pass	an abortive perform in a let body tail referencing the let binding stays in scope
todo	an INDIRECT (helper-hidden) abortive perform in a non-tail accumulator recursion declines cleanly (safe floor; was a miscompile pre-fix)
todo	an abortive perform in a non-tail accumulator-introduced recursion declines cleanly (safe floor; flips to the abort value under the non-local-exit vertical)
pass	an abortive perform in a recursive callee with a PENDING continuation in the handle body abandons it
pass	an abortive perform in a short-circuit connective's right operand abandons the computation
pass	an abortive perform in an if condition abandons the computation before branching
pass	an abortive perform in the NON-taken if branch is never evaluated (no speculation)
pass	an abortive perform in the tail of an if branch abandons only that branch
pass	an abortive perform in the tail of an if branch inside a let body abandons only that branch
pass	an abortive perform of a bare parameter argument abandons the surrounding operator
pass	an abortive perform short-circuited out of a connective condition is never performed
pass	an abortive perform under THREE nested handlers abandons the two resumptive frames above it
pass	an abortive perform under a non-tail conditional abandons the enclosing computation
pass	an above-double-precision float width is rejected as non-admitted
pass	an absent EFFECT operation names the effect and 'operation', with a did-you-mean
pass	an absent field with no close match lists the record's available fields
pass	an absent member of a PRELUDE module names the module, not a record
pass	an absent user-RECORD field keeps 'record has no field' (a record is a record to the author)
pass	an absent user-sum VARIANT names the type and 'variant', with a did-you-mean
pass	an absolute-value branch traps on the minimum integer it cannot represent
pass	an absorbed conjunction composes as a live disjunction's operand
pass	an absorbing and-false discards a trap-free left operand's value
pass	an absorbing and-false keeps its trapping left operand
pass	an absorbing or-true keeps an effectful left operand's perform
pass	an abstract theorem is obtained only through the kernel's exported inference rule and accessor
pass	an abstract theorem type cannot be forged by constructing its rule constructor outside the kernel
pass	an abstract type is used through the module's exported constructor and accessor
pass	an abstract type's constructor is not reachable outside its module
pass	an abstract-typed value held as a Map VALUE under a concrete key is legal (value-holding never triggers the opacity reject)
pass	an abstract-typed value used as a CHAMP map key is rejected (opacity — the key path invokes the forbidden structural comparison)
pass	an absurd runtime width is rejected, not silently ignored
pass	an acceleration dimension renders as a quotient with a squared denominator
pass	an active unquote lifts a RUNTIME boolean operand, not only a constant
pass	an active unquote lifts a RUNTIME float operand, not only a constant
pass	an active unquote lifts a RUNTIME integer operand, not only a constant
pass	an active unquote of a BigInt lifts to Ast.Int (the payload type, no widen)
pass	an active unquote of a RUNTIME BigInt (from arithmetic) lifts to Ast.Int
pass	an active unquote of a boolean literal lifts to an Ast.Bool node
pass	an active unquote of a computed boolean expression lifts to Ast.Bool
pass	an active unquote of a constant NaN is rejected (the ast-lift path, consistent with the ctor)
pass	an active unquote of a finite float lifts to Ast.Float (ast-lift control)
pass	an active unquote of a float literal lifts to an Ast.Float node
pass	an active unquote of a let-bound boolean lifts to Ast.Bool by inferred type
pass	an active unquote of a let-bound float lifts to Ast.Float by inferred type
pass	an active unquote of a let-bound integer still lifts to Ast.Int
pass	an active unquote of a let-bound string lifts to Ast.Str by inferred type
pass	an active unquote of a string literal lifts to an Ast.Str node
pass	an active unquote of an Ast-valued expression splices the subtree as identity
pass	an active-unquoted boolean literal equals the quoted form
pass	an addition-REACHED runtime Rational equals its constructed canonical form and not a decoy
pass	an agreeing Result error type short-circuits through the boundary
pass	an agreeing arrow annotation on a lambda is accepted and the function applies normally
pass	an agreeing redeclaration compares the normalized ratio, not the literal numerator and denominator
pass	an all-UInt8 handler (state, op result, arm arithmetic) computes at narrow width
pass	an all-constant Set.of still folds membership with a constant query (control)
pass	an all-escapes Ast.Str round-trips through print and read
pass	an all-nullary enum dispatches its runtime-selected variant and escapes
pass	an all-nullary enum nested in a tuple carries its discriminant through the heap
pass	an all-nullary enum reached through a tuple element inside a boxed sum survives the heap round-trip
pass	an all-nullary user sum orders by discriminant — Lo below Hi
pass	an all-owned-arms match operand compares correctly through the ownership join
pass	an all-wildcard tuple arm is a catch-all that shadows a later refining arm
pass	an all-wildcard tuple arm is an irrefutable catch-all that shadows later arms and earns a CDZ0213 warning
pass	an ambiguous missing-and-extra record field set (not a typo) gets no mechanical fix
pass	an ambiguous two-field constructed-record slip guides with the field-diff but offers no confident fix
pass	an ambiguous wrap (both Result arms fit the payload) offers no fix
pass	an and of two runtime negations computes their conjunction
pass	an annihilator identity folds to zero when its operand is trap-free
pass	an annotated argument joins a callee-side quantity at its own scale
pass	an annotated destructuring let binder
pass	an annotated destructuring let binder that contradicts an element type is rejected
pass	an annotated empty list escapes fine (the undetermined-empty-list control)
pass	an annotated escaped None renders its canonical nullary-variant form (the control)
pass	an annotated lambda parameter binds
pass	an annotated let binder at a fitting narrow float computes
pass	an annotated let binder constrains the value's type
pass	an annotated let binder narrower than its literal value is rejected (float width)
pass	an annotated let binder narrower than its literal value is rejected (int width)
pass	an annotated let binder range-checks its narrow-width bound value
pass	an annotated let binder that contradicts the value is rejected
pass	an annotated quantity joins additions at its own scale, no silent mixed-scale bypass
pass	an annotated tuple-pattern parameter binds its pattern's names
pass	an annotated tuple-pattern parameter still checks its annotation against the argument
pass	an annotated value takes a narrower integer width
pass	an annotation by a reflected type that contradicts the value is rejected
pass	an annotation mismatch to a sum offers a wrap-in-variant fix
pass	an annotation on a nested generic-call argument grounds the self-nested producer and it runs
pass	an annotation on an arithmetic operand is transparent
pass	an annotation that contradicts the value is rejected
pass	an annotation that contradicts the value is rejected — the mirror direction
pass	an annotation whose type is a reduced (Int 64) constructor grounds the value
pass	an applied lambda whose body enters a mutually-recursive performing group folds
pass	an applied lambda whose body performs an ABORTIVE op abandons the enclosing computation
pass	an apply-site-homed lambda's perform result composes in the caller's arithmetic
pass	an arbitrary-precision integer multiplication does not overflow
pass	an arbitrary-precision literal beyond 64 bits is an exact BigInt
pass	an argument bound to a used parameter IS observed, so its trap occurs (the anchor)
pass	an argument bound to an unused parameter is not evaluated, so its trap does not occur
pass	an argument swap weaves with perform results through a recursive handler body
pass	an arithmetic op with a user-type VALUE operand and a scalar names the kind boundary, not a phantom Int64
pass	an arithmetic operator with a single operand curries into a partial-application closure
pass	an arm CONDITIONS its shot count — the multi-shot branch
pass	an arm REPLACES the trie state wholesale and the next op reads the replacement
pass	an arm SUMS one resumption with a constant (the 1.5-shot shape)
pass	an arm binder SHADOWS an outer heap list and the outer survives the shadow's scope
pass	an arm chooses its resume value by an if on the handler state
pass	an arm performs the outer effect TWICE and the results feed the resume value
pass	an arm re-performing its own effect with no outer handler has no home
pass	an arm re-performs its OWN effect to a SAME-EFFECT outer handler — the true self-shadow forward
pass	an arm resuming an OVERFLOWING literal into a narrow op RESULT is rejected
pass	an arm resuming with a re-perform of its OWN effect forwards to an outer handler of that effect
pass	an arm that binds its resume in a lambda and applies it immediately folds
pass	an arm that matches over a COMPUTATION on the op-param and resumes a match binder folds (nv1e/nvC)
pass	an arm's NEXT-STATE expression saturates via a conditional on the current state
pass	an arm-body shadow of the param composes with the scrutinee's pre-shadow read
pass	an arrow parameter annotation compiles and applies (a positive control)
pass	an association list is searched by key with a tuple-carrying Option match
pass	an axiom composes with the inference rules like any other theorem (SYM of ETA)
pass	an axiom theorem is unforgeable — new_axiom's Thm cannot be fabricated outside the kernel
pass	an earlier literal arm is chosen over a later name-binding arm for a runtime scrutinee
pass	an earlier name-binding arm shadows a later literal arm — first-match-wins, not specificity-ordered
pass	an effect bound to a peer routes a two-argument op to the boundary
pass	an effect declaration with a leading doc clause is well-formed and its op handles + runs
pass	an effect declared in a MODULE is performed by its helper and handled by the IMPORTER
pass	an effect discharged by a handler does not escape to the manifest
pass	an effect in a statically-dead branch is still syntactically reached, so its delegation is not latent
pass	an effect in a statically-dead branch still requires a grant — ungranted is rejected
pass	an effect op RESUMED with a constructed Ast node crosses the arm boundary and matches in the body
pass	an effect op RESUMED with a slice-view Bytes crosses the arm boundary intact
pass	an effect op resumed with a whole MAP threads the CHAMP through the continuation
pass	an effect op whose declared RESULT is a QUANTITY resumes with a Qty value
pass	an effect op with a structural-record return types the perform at the declared record
pass	an effect operation declared with no name is rejected
pass	an effect operation may be named `bind` — the interop directive keyword is not reserved for op names
pass	an effect operation reached with neither a handler nor a delegation is rejected
pass	an effect operation returning a SUM is resumed with a sum value and matched
pass	an effect operation taking a SUM parameter matches it in the handler arm
pass	an effect operation with no name is rejected
pass	an effect performed in a callee is discharged by the caller's handler
todo	an effect performed via a collection-extracted closure declines honestly (not-yet-reducible, not a false no-handler claim)
pass	an effect resolves past an intermediate frame that installs no handler
pass	an effect resolves through a deep chain of intermediate functions
pass	an effect result drives the bound of a PURE recursive helper called in the handle body
pass	an effect that declares an operation name twice is rejected
pass	an effect-built NESTED-compound value escapes the handle and a nested projection reads through it
pass	an effect-built heap list bound in a let is USED TWICE and retained across both uses
pass	an effect-performing helper called inside a recursive self-call's argument folds
pass	an effect-valued export is one coded error, not a leaked internal cascade
pass	an effectful condition is performed exactly once under the operator hoist
pass	an effectful condition of a same-constructor if is performed exactly once
pass	an effectful helper called twice (nested) in a self-call arg folds each inline independently
pass	an effectful helper performing UNDER A CONDITIONAL folds in a self-call arg
pass	an effectful helper performing in a NESTED if branch folds in a self-call arg
pass	an effectful helper performing in an if CONDITION folds in a self-call arg
pass	an effectful helper reading TWO driver parameters folds in a self-call arg
pass	an effectful helper that also reads an outer/driver parameter folds in a self-call arg
pass	an effectful helper whose own parameter name shadows a driver parameter folds in a self-call arg
pass	an effectful host arg flowing into a compound then a destructuring match is evaluated ONCE
pass	an effectful host arg to a multi-use function parameter is evaluated ONCE, not re-performed per use
pass	an effectful init before a failing `?` performs exactly once
pass	an effectful match scrutinee is performed exactly once across a two-position sink
pass	an effectful shared operand performs after the condition, exactly once
pass	an effectful walk over an INPUT list combines each element with a fresh perform
pass	an element is projected from a runtime-constructed tuple
pass	an element is projected from a tuple returned by a function
pass	an element is projected from a tuple returned by a nullary function
pass	an element pattern matches a list by its length and elements
pass	an element read across a concatenation boundary is the right value
pass	an element-arg trap fires BEFORE an out-of-bounds miss — construction precedes the index probe
pass	an emit/flush byte-writer seeded EMPTY — three emits accumulate, flush reads the frame back
pass	an empty (list) match-fallback beside an unsolved-Var arm grounds to the join's list type
pass	an empty Ast.Bytes round-trips through encode and decode
pass	an empty MAP fallback beside an unsolved-Var arm grounds — the Map-of-Maps face
pass	an empty SET literal in a match-Option fallback grounds through the join
pass	an empty binary form is the empty byte sequence
pass	an empty byte-string literal is the empty byte sequence
pass	an empty byte-string literal pattern matches the empty Bytes
pass	an empty do block in a parameterized def body is rejected as valueless
pass	an empty export clause is rejected
pass	an empty list binding pattern is refutable and rejected
pass	an empty list escaping to the host is rejected as undetermined, like a bare None
pass	an empty list in a variant position is rejected
pass	an empty list pattern matches only the empty list, not a non-empty one
pass	an empty list pattern matches the empty list
pass	an empty map is unequal to a non-empty map, not a type error
pass	an empty map passed to a recursive callee with a non-Int64 key param grounds its key type from the param
pass	an empty membership pattern #set() matches ANY set, and a duplicate element is idempotent
pass	an empty name-alias (map) compared against an empty record is a cross-kind type error
pass	an empty op effect clause is a malformed effect declaration
pass	an empty quote is rejected, not a crash
pass	an empty record pattern matches any record of its type
pass	an empty runtime String host arg marshals as a zero-length buffer
pass	an empty scrutinee matches an empty bin pattern
pass	an empty set in one if-branch unifies with a non-empty set in the other and its length reads correctly
pass	an empty set passed to a recursive callee with a non-Int64 element param grounds its element type from the param
pass	an empty string is unequal to a non-empty string
pass	an empty top-level annotation wraps no definition and is rejected
pass	an empty type annotation names the zero parts present
pass	an empty-compound quote pattern does not match a non-empty form
pass	an empty-compound quote pattern matches a quoted empty compound
pass	an empty-range slice of a non-empty string is Some of the empty string
pass	an empty-span String.slice at a multi-byte position has scalar-len zero
pass	an empty-string Ast.Str round-trips through encode and decode
pass	an empty-string Ast.Str round-trips through print and read
pass	an empty-tuple element on the HEAP path renders its (Tuple)-typed (tuple), not unit (type-directed, RULED-B ask-17874)
pass	an encode-decode round-trip preserves an ASCII string's byte length
pass	an entrypoint delegation lets a program reach its host function
pass	an entrypoint delegation reaches an effect performed in a recursive callee
pass	an entrypoint returning a comparison presents a Bool result at the boundary
pass	an entrypoint returning arithmetic presents an Int64 result at the boundary
pass	an entrypoint that delegates no effect is pure and makes no host call
pass	an enum PARAM member whose guest case order MISMATCHES the WIT remaps the disc by case NAME — the param twin of SHAPE 64
pass	an enumerate-and-rebuild round trip equals the source map by canonical identity
pass	an equality against a value outside the operand's range folds to false; in-range stays runtime
pass	an equality combined with a contradicting range folds to false
pass	an equality combined with a satisfiable range folds to the equality
pass	an equality or-ed with a covering range subsumes to the range
pass	an equality or-ed with a non-covering range keeps the extra point
pass	an equality point fact decides a DIFFERENT equality test in the then branch — the point-fact eq-fold face
pass	an equality point fact folds an inner range comparison, both directions — the point-fact analogue of the ordering fold
pass	an equality point fact sheds a then-branch arithmetic overflow guard, but the trap survives unguarded
pass	an erased newtype over a sum reads the inner payload, not the whole wrapper
pass	an erased single-variant newtype literal-payload match arm dispatches correctly at each scalar width
pass	an erased single-variant newtype wrapping a sum matches through the inner variants
pass	an erased-newtype nested-sum match with binder-only arms dispatches on the inner discriminant
pass	an erased-newtype's list payload split across empty and rest arms dispatches with vec-get
pass	an escaped value with an unresolved payload type is rejected as ambiguous, not for its export shape
todo	an escaping captured continuation is refused, not miscompiled
pass	an escaping closure captures two values and takes three arguments
pass	an escaping closure whose body binds a multi-variable let
pass	an escaping closure whose body is a match hits the literal arm
pass	an escaping closure whose body is a match hits the wildcard arm
pass	an escaping closure whose body performs is rejected (perform out of the handler's extent)
pass	an escaping continuation that itself RE-PERFORMS the handled effect re-enters its handler at apply
pass	an escaping tuple evaluates its trapping element
pass	an eval folds a quoted match whose #map pattern carries a (.. rest) marker (OPEN pattern)
pass	an eval folds a quoted match whose #record pattern carries a (.. rest) marker (OPEN pattern)
pass	an eval match binder shadowing a HEAP-typed outer name leaves the heap value intact
pass	an eval splice consumes a value extracted from a CHAMP map at run time
pass	an eval-reconstructed BARE pattern on a withheld constructor is also rejected (encapsulation soundness, metaprogramming route)
pass	an evaluated quasiquote builds a HEAP value the surrounding code consumes
pass	an exact RATIONAL host value crosses as two scalar num/den ops the guest recombines (#13)
pass	an exhaustive nested match compiles clean (no false non-exhaustiveness)
pass	an expect of a list index result is an integer usable in arithmetic
pass	an explicit Bytes annotation on a runtime bin result compares the same
pass	an explicit UInt64-max annotation is in range and runs (the bare-overflow check does not misfire)
pass	an explicit annotation overrides the default-float pragma
pass	an explicit annotation overrides the default-fraction pragma
pass	an explicit annotation overrides the default-integer pragma
pass	an explicit conversion makes the operation well-typed
pass	an explicit conversion off the truncating-display quantity is still exact (5 foot in inches = 60)
pass	an explicit conversion repairs a mixed-unit join
pass	an explicit float-width conversion makes a mixed-width operation well-typed
pass	an exponent-form float with an N suffix gets the R-suffix fix
pass	an export and a like-named metadata key do not collide
pass	an export mixing a scalar and a nested-tuple parameter returns a list from both
pass	an export mixing a scalar and a tuple parameter returns a BigInt from both
pass	an export mixing a scalar and a tuple parameter returns a list from both
pass	an export name with a digit-led kebab segment is rejected, not silently invalid
pass	an export name with a non-ASCII segment is rejected, not silently invalid
pass	an export whose name is not kebab-case crosses under a normalized extern name
pass	an export with a nested-tuple parameter returns a list from its leaves
pass	an export with a record-with-a-tuple-field parameter returns a list from its leaves
pass	an export with a tuple parameter before a scalar returns a list from both
pass	an export with two tuple parameters returns a list from both
pass	an exported Float64 addition runs over runtime args at IEEE precision
pass	an exported Float64 division runs over runtime args at IEEE precision
pass	an exported Float64 multiplication runs over runtime args
pass	an exported Float64 subtraction runs over runtime args
pass	an exported OPEN-ROW projector instantiates at importer widths the module never saw
pass	an exported UNANNOTATED helper instantiates at TWO types from the IMPORTER's call sites
pass	an exported closure whose body applies an arithmetic operator to a non-numeric operand is rejected
pass	an exported closure with a narrow-arg wide-result mismatched body is rejected, not miscompiled
pass	an exported closure with an annotation-mismatched body is rejected, not emitted invalid
pass	an exported entry with a String parameter is called with a string argument
pass	an exported function with an unannotated parameter is rejected as ambiguous
pass	an exported signed comparison runs over runtime args with signed order
pass	an exported two-Int64 addition runs over runtime args, not folded
pass	an expression INTERPRETER threads a Map environment through Let-shadowing scopes
pass	an expression tree built at run time is evaluated by matching its node variants
pass	an extracted closure passed through a helper then applied reclaims
pass	an extracted closure placed into a tuple then projected and applied reclaims
pass	an extracted closure re-stored into a new list then re-extracted and applied reclaims
pass	an i16 segment round-trips at its two's-complement minimum
pass	an i64 bin binding with the sign bit set is negative in both const and runtime folds
pass	an i64 le field at the sign boundary decodes negative from its high LAST byte
pass	an i64-overflowing bare literal grounds to BigInt from its comparison peer (no range fault)
pass	an identity closure composes at a single Int element type (not only String)
pass	an identity closure composes over a COMPOUND (tuple) element at one element type
pass	an identity closure composes through TWO chained recursive-generic maps at one element type
pass	an identity closure threads through a recursive-generic HOF at TWO element types
pass	an identity closure threads through a recursive-generic HOF at one element type
pass	an identity conversion unwraps the magnitude unchanged
pass	an if choosing between two nullary enum variants selects branchlessly on the discriminant
pass	an if on Type.eq selects a branch at compile time
pass	an if operand of a narrow add is width-normalized to the op
pass	an if operand of a narrow bitwise-and is width-normalized to the op
pass	an if operand of a narrow multiply is width-normalized to the op
pass	an if operand of a narrow op is width-normalized at each width that fits the slot
pass	an if over different-length list arms keeps each branch's own list
pass	an if over two quantities at the SAME unit joins normally
pass	an if over two same-dimension quantities at different units is a type mismatch (no auto-convert)
pass	an if over two trap-free bitwise arms selects branchlessly — else arm
pass	an if over two trap-free bitwise arms selects branchlessly — then arm
pass	an if returning false/true folds to the negation of a runtime condition
pass	an if returning true/false folds to the runtime condition itself
pass	an if whose both branches diverge is Never and always traps
pass	an if whose branch records differ in one field type names the differing field
pass	an if whose branch tuples differ in arity is a CDZ0203 branch-join mismatch naming the arity delta
pass	an if whose branch tuples differ in one element type names the differing position at the join
pass	an if whose branches do NOT refine to the same constant keeps both values
pass	an if whose branches refine to the same constant collapses to that constant (value parity)
pass	an if whose untaken arm would trap keeps the branch (no select of checked arith)
pass	an if with a false else-branch is the AND connective (truth table)
pass	an if with a float then-branch and an int-literal else-branch retypes the else int literal up
pass	an if with a narrow branch and a bare-literal branch computes at the narrow width
pass	an if with a true then / false else-branch is the negated-condition connective (truth table)
pass	an if with an int-literal then-branch and a float else-branch retypes the int literal up
pass	an if with identical branches folds even when the condition is a NaN-false float compare
pass	an if with identical branches folds to the branch when the condition is trap-free
pass	an if with identical branches still evaluates a TRAPPING condition
pass	an if-condition self-call preceding a branch perform folds via multi-value return
pass	an if-join of two capturing lambdas let-bound and called applies the selected closure
pass	an if-join of two identical capturing lambdas still reads the capture on the false arm
pass	an if-join over different-unit same-dimension quantities names the scale, not a shadowed declaration
pass	an if-operand arithmetic op with deferred-width branches is Int64, and its wide result is correct
pass	an if-selected partially-applied constructor is completed by application (eta-closure), both arms
pass	an if-tuple returned from a call, let-bound and projected twice
pass	an if-wrapped self-application is rejected in bounded time, not an inference hang
pass	an if→SELECT-eligible conditional with a performed condition and pure branches stays sound
pass	an ill-formed integer width NESTED in a compound annotation is rejected
pass	an ill-formed integer width in a type-declaration payload is rejected
pass	an ill-typed condition inside a let body is still caught (the check descends through the let)
pass	an ill-typed ctor-match scrutinee taints the whole match, not masked by well-typed arms
pass	an ill-typed inner-match scrutinee taints the outer match
pass	an ill-typed non-final do-statement is still caught though its value is discarded
pass	an imported def whose body is Ast.module reflects its DEFINING module, not the importer
pass	an imported helper reaches its own file's private definition when inlined
pass	an imported helper's performs discharge at a handler whose STATE is the importer's heap Map
pass	an imported name resolves to a sibling file's exported definition
pass	an imported nullary accessor projecting a field off a recursive self-reflected record descriptor folds
pass	an in-bounds slice yields Some of the bytes at that range
pass	an in-definition doc clause is valid and the definition computes
pass	an in-program handler OVERRIDES an effect's peer binding (the test-mock, no peer call)
pass	an in-program two-site handler runs INSIDE a host block beside a host call
pass	an in-range boundary constant argument to a narrow parameter runs (no over-rejection)
pass	an in-range constant argument to a narrow parameter computes at the parameter width
pass	an in-range constant arithmetic through a narrow parameter runs (no over-rejection)
pass	an in-range left shift on an unusual signed width computes at the declared width
pass	an in-range list element under a sibling-inferred width compiles and runs (no over-rejection)
pass	an in-range literal to a narrow effect-op parameter crosses and the arm observes it
pass	an in-range narrow-width wrapping-add constant folds unchanged
pass	an in-range unusual-width division computes at the declared width
pass	an index loop's loop-invariant List.len over a heap operand is still LICM-hoisted (value correct)
pass	an indexed list walk performing per element — element × advancing draw, summed
pass	an indexed read is not shared across a shadowing rebinding
pass	an indexed read of an updated list is not shared with the original's
pass	an init after the failing `?` never evaluates — even a provably-trapping one
pass	an inline HOF applies a bare operator passed as its function argument
pass	an inline Unit.define conflicting with a built-in conversion is rejected wherever it occurs
pass	an inline lambda with more than one body is rejected
pass	an inline-materialized BigInt constant operand is dropped, leaving no live heap objects
pass	an inline-never definition is emitted once and called
pass	an inline-never definition with a const dictionary still monomorphizes the dictionary
pass	an inlined helper matching a sum built FROM the fused binder plus an enclosing capture
pass	an inlined multi-use checked-arith argument is shared and computed directly into its slot
pass	an inner ABORT handle inside a MULTI-SHOT region — each forked branch runs its own abort
pass	an inner abort ELIDES an outer perform sequenced AFTER it (dead-path control)
pass	an inner abort in a LET-INIT preserves the outer advance committed before it (let-init collapse)
pass	an inner abort in a NON-FINAL do-statement elides the DEAD suffix after it (pre-abort advance kept)
pass	an inner abort preserves BOTH a PERFORMING-CONDITION advance AND an if-branch advance before it (ao10)
pass	an inner abort preserves TWO outer advances committed before it (multi-step outer trace)
pass	an inner abort preserves an OUTER advance committed in a MATCH-ARM body before it (arm do-shape)
pass	an inner abort preserves an OUTER advance committed in a MATCH-SCRUTINEE before it (scrutinee collapse)
pass	an inner abort preserves an OUTER advance committed in a STRICT OPERAND before it (operand-lift)
pass	an inner abort preserves an OUTER advance committed in an IF-BRANCH before it (branch do-shape)
pass	an inner abort preserves an outer advance committed in a NESTED strict operand (deeper-operand-lift)
pass	an inner abort whose ARGUMENT performs the outer effect commits BOTH the pre-abort and the in-arg advance
pass	an inner abortive handler preserves an OUTER effect's advance committed before the abort (do-shape)
pass	an inner arm performs TWO DISTINCT outer effects in one resume value — both thread per dispatch
pass	an inner arm resumes with the OUTER handler's result while both states advance
pass	an inner binding shadows an outer one only within its scope
pass	an inner handle of the SAME delegated effect discharges in-program inside the host block
pass	an inner handle's SEED expression performs against the outer handler
pass	an inner handler arm performs the OUTER effect mid-transition — both threads advance in lockstep per inner dispatch
pass	an inner handler's INIT state is computed by performing an enclosing effect
pass	an inner handler's SEED is a perform of an OUTER in-program effect
pass	an inner lambda captures an enclosing match-arm binder and is applied
pass	an inner lambda captures an enclosing tuple-pattern binder and is applied
pass	an inner map shared between two outer generations survives the loser's drop
pass	an inner match on a LOCALLY-BUILT Option from the payload (scalar state) — a non-state inner scrutinee beside the dispatch
pass	an inner runtime shadow defeats the outer constant fold
pass	an inner same-effect handle's RESULT feeds the outer handler's next perform
pass	an int LITERAL let-binder init annotated Float retypes to a float literal
pass	an int annotation to a narrower type offers an of-conversion wrap fix
pass	an int annotation to a wider type offers an of-conversion wrap fix
pass	an int argument to an UNREFERENCED float parameter is the sole arg-unify CDZ0301
pass	an int literal FIRST against a non-literal float retypes the literal up
pass	an int literal argument to a float parameter is one annotation-context fault with a retype fix
pass	an int match on a constant scrutinee is non-exhaustive even when the constant hits the sole arm
pass	an int-first arithmetic mix drops the trailing float literal's fractional form
pass	an int-literal-first comparison against a float var retypes the literal up
pass	an int-var comparison against a float literal drops the fractional form
pass	an int-vs-bool if branch clash is not coercible, so no float-retype fix is offered
pass	an int-width let-binder annotation mismatch offers the of-conversion wrap fix
pass	an integer if condition is a type error, not a running conditional
pass	an integer literal annotated Rational grounds to that integer over one
pass	an integer literal annotated a float still retypes to a float literal, not an of-int wrap
pass	an integer literal annotated a float type offers an add-the-fraction fix (Float64)
pass	an integer literal annotated a narrow float type offers an add-the-fraction fix (Float32)
pass	an integer literal in a type annotation's type position is rejected
pass	an integer literal match with no wildcard is non-exhaustive
pass	an integer literal pattern against a boolean scrutinee is a type error
pass	an integer literal pattern matches a runtime scrutinee
pass	an integer literal with digit separators
pass	an integer min via (if (< a b) a b) returns the smaller in either operand order
pass	an integer operand to a float operator offers an of-int coercion fix
pass	an integer segment after a byte-aligned bit-field run reads at the advanced offset
pass	an integer square root by Newton iteration converges to the floor at perfect squares and off-squares
pass	an integer value in a utf8 bin segment is a kind mismatch
pass	an integer width above the 64-bit ceiling is rejected
pass	an integer width from runtime data is rejected, keeping widths non-dependent
pass	an integer width reached through a let-bound constant folds and is honored
pass	an integer-valued float LITERAL let-binder init annotated Int drops the fraction
pass	an integer-valued float literal annotated a narrow integer type offers a drop-the-fraction fix (Int8)
pass	an integer-valued float literal annotated an integer type offers a drop-the-fraction fix (Int64)
pass	an integer-valued float with an N suffix also gets the R-suffix fix
pass	an integer-width mismatch under an operator offers an of-conversion wrap fix
pass	an interleaved insert-remove build leaves exactly the surviving keys
pass	an internal meta channel is not offered as a member suggestion
pass	an internally ill-typed unselected match arm body is a type error
pass	an interposing handler TRANSFORMS the host response before resuming (offset adapter)
pass	an intra-program handler interposes on a delegated effect, counts it, and forwards to the boundary
pass	an intra-program handler whose body never performs its effect is inert and runs to the body value
pass	an invalid nested-list-rest shape (a sub-pattern in the rest slot) is rejected even when the body ignores the inner binders
pass	an invalid nested-list-rest shape reports EXACTLY ONE rest-shape reject when the body uses the inner binders
pass	an irrefutable single-variant ctor map value sub-pattern binds its payload
pass	an irrefutable tuple parameter on an inline lambda is legal and applies (the control)
pass	an iterative fibonacci swaps two accumulators through the tail call
pass	an observed tail-recursive performer keeps constant stack on all three backends
pass	an octal-prefixed token is a malformed literal, not an unbound name
pass	an odd-width SIGNED shift result exceeding the declared width traps (Int24: 4194304<<1)
pass	an odd-width division overflow traps before the out-of-range value escapes into Int64 arithmetic
pass	an odd-width shift overflow traps before the escaped result poisons a CHAMP Set
pass	an odd-width signed division overflow traps above the i32 slot too (Int48 min / -1)
pass	an odd-width signed division overflow traps at the declared width (Int24 min / -1)
pass	an odd-width unsigned shift result exceeding the declared width traps (UInt4: 3<<3)
pass	an offered ordering is total and deterministic
pass	an op clause with a non-name keeps the specific must-be-named reject, not the generic clause one
pass	an op declared only on a LATER same-named effect explains the shadowing, not a typo list
pass	an op-arg match outer with a state-match inner reading the payload computes per dispatch
pass	an open sum match WITH an open-tail wildcard arm is exhaustive and compiles
pass	an open sum match without an open-tail wildcard arm is non-exhaustive
pass	an open sum nested as another sum's payload matches with an inner wildcard
pass	an open sum with a guarded named arm plus an open-tail arm is exhaustive
pass	an open sum's open-tail arm dispatches a NAMED-but-uncovered variant, not only unnamed ones
pass	an open sum's open-tail arm dispatches a variant the specific arms do not name
pass	an open-row function accepts a runtime MERGE result and reads the carried field
pass	an open-row function is applied at TWO different record widths in one program
pass	an open-row function over a Record.with result reads the UPDATED value
pass	an open-row projection reads list-element records in a fold AND a wider record at another site
pass	an open-row record passed through a projecting helper keeps its EXTRA fields readable
pass	an open-sum type crosses THREE modules concretely and matches at every stop
pass	an open-sum value bound by an open-tail arm is forwarded to another matcher with its variant intact
pass	an open-sum value matching no named arm in either matcher reaches the forwarded sentinel
pass	an open-sum value rides a generic tuple and matches from both slots
pass	an operation declared with no type is rejected
pass	an operation on mismatched types is rejected at compile time
pass	an operation with a RECORD parameter binds the compound and the arm reads its fields
pass	an operation with a TUPLE parameter binds the compound and the arm projects it
pass	an operator applied inside a lambda passed to an annotated named HOF
pass	an operator-arg record field-SET mismatch names the delta, not the raw unify message
pass	an operator-arg record field-TYPE mismatch names the differing field's types
pass	an operator-arg tuple ARITY mismatch names the element counts
pass	an opposite-direction and over a non-empty interval computes its non-constant value
pass	an opposite-direction or with a real gap computes its non-constant value
pass	an option value annotated with the wrong payload type is rejected
pass	an option<s64> field in a record result VALUE round-trips via the run/encode envelope both arms (no wit-world clause; a typed record/sum EXPORT is a separate gap)
pass	an option<s64> param field is read and rebuilt by the wrapper on both arms (via an imposed WIT world)
pass	an or connective with a non-boolean operand is a type error
pass	an or whose left operand short-circuits elides the performing right operand
pass	an ordering < of two same-bits Float32 literals const-folds false
pass	an ordering <= of two same-bits Float32 literals const-folds true
pass	an ordering > of two same-bits Float32 literals const-folds false
pass	an ordering >= of two same-bits Float32 literals const-folds true
pass	an ordering operator with a single operand curries into a partial-application closure
pass	an ordinary distinct-name mismatch adds no same-name disambiguation tail
pass	an ordinary over-applied user function keeps the anonymous arity message, not a constructor phrasing
pass	an ordinary unbound name (not a unit-builder arg) keeps the plain unbound message, not the redirect
pass	an ordinary-+ fold whose EXACT result overflows traps under any reassociation
pass	an out-of-Int8 payload projected through Option.expect under an Int64 result fits and runs
pass	an out-of-range Map.insert KEY via a sibling-inferred width is CDZ0302
pass	an out-of-range Map.insert value via a sibling-inferred width is CDZ0302
pass	an out-of-range Set.of element via a sibling-inferred width is CDZ0302
pass	an out-of-range UNSIGNED integer literal widens within the unsigned family
pass	an out-of-range checked integer conversion of a constant is rejected
pass	an out-of-range constant argument to a narrow parameter is range-checked at the call
pass	an out-of-range constant argument to an inline lambda's narrow parameter is range-checked
pass	an out-of-range float literal annotated a narrow integer type carries NO drop-fraction fix
pass	an out-of-range float literal is a malformed literal, not a non-finite value
pass	an out-of-range integer literal is a malformed literal, not an unbound name
pass	an out-of-range integer literal past every fixed width retypes to BigInt
pass	an out-of-range integer literal widens straight to the widest fitting width, not an intermediate
pass	an out-of-range integer literal widens to the smallest fitting width of the same signedness
pass	an out-of-range literal MAP KEY through a builder chain (Map.insert) is rejected
pass	an out-of-range literal MAP VALUE through a builder chain (Map.insert) is rejected
pass	an out-of-range literal SET element via Set.insert is rejected
pass	an out-of-range literal SET element via Set.of is rejected
pass	an out-of-range literal argument caught transitively through a two-call chain of narrow params
pass	an out-of-range literal bound under a narrow let-binder annotation is range-checked
pass	an out-of-range literal in a NESTED SUM payload takes the annotated inferred width and carries no retype fix
pass	an out-of-range literal in a RECORD nested inside a map VALUE is rejected — the descent recurses
pass	an out-of-range literal in a SET nested inside a set element is rejected — depth generality
pass	an out-of-range literal in a TUPLE nested inside a set element is rejected — the descent recurses
pass	an out-of-range literal in an INNER Map.insert of a builder chain is rejected — the descent recurses the chain
pass	an out-of-range signed checked conversion names its signed valid range
pass	an out-of-range signed literal names its valid range in the diagnostic
pass	an out-of-range unsigned literal names a range starting at zero
pass	an outer Int64 let binder survives an inner String shadow of the same name
pass	an outer definition references a sibling nested module by bare name
pass	an outer effect in BOTH the resume value and next-state slot folds to 57 (asb)
pass	an outer effect in a nested arm's RESUME-VALUE slot is served (as3)
pass	an outer effect in a nested arm's next-state slot across THREE chained performs folds to 61 (as1)
pass	an outer effect performed DIRECTLY in a nested arm's next-state slot folds to 6 (as2)
pass	an outer op-arg match with an inner state match reading the op-arg payload threads per dispatch
pass	an outer perform LET-LIFTED out of the next-state slot folds strict (as7 workaround)
pass	an over-applied FLOAT arithmetic operator is one CDZ0201 takes-exactly-2 with a delete fix
pass	an over-applied built-in operation is ONE CDZ0203 with a delete-surplus fix, not doubled with the wrong-arity decline
pass	an over-applied comparison operator is one CDZ0201 takes-exactly-2 with a delete fix
pass	an over-applied integer operator is one CDZ0201 takes-exactly-2 with a delete fix
pass	an over-ceiling SIGNED width far past the ceiling is rejected naming the written width
pass	an over-ceiling SIGNED width in an unused parameter is rejected
pass	an over-ceiling SIGNED width one past the ceiling is rejected naming the written width
pass	an over-ceiling integer width in an unused parameter is rejected, like a used one
pass	an over-the-64-bit-ceiling signed width well past 64 retypes to BigInt
pass	an over-the-64-bit-ceiling unsigned width retypes to BigInt
pass	an overflow pragma with an unknown mode is a malformed directive (CDZ0602)
pass	an overflow pragma with no signedness sub-form is malformed (CDZ0602)
pass	an overflowing literal in a RECORD op argument's narrow field is rejected
pass	an oversize constant if-branch consumed by a narrow + is rejected
pass	an oversize constant if-branch consumed by a narrow bitand is rejected
pass	an oversize constant if-branch inside a NESTED narrow + is rejected
pass	an oversize constant if-branch threaded through a let into a narrow + is rejected
pass	an oversize constant match-arm body consumed by a narrow + is rejected
pass	an overwrite-churn runtime map holds one entry with the last value winning
pass	an s-around-k ctl arm that ALSO performs an outer effect in the arm body folds — the two E5 fixes compose
pass	an s-around-k ctl arm whose K-ARGUMENT performs an outer effect folds
pass	an s-around-k ctl arm whose handle BODY performs an outer effect after the inner perform folds
pass	an un-annotated float if takes its branch width from a sibling Float32 arm and emits valid f32
pass	an un-annotated float match takes its arm width from a sibling Float32 arm and emits valid f32
pass	an un-annotated negative list element in an unsigned sibling-inferred list is CDZ0302
pass	an un-annotated over-max list element takes the sibling-inferred narrow width and is CDZ0302
pass	an un-projected tuple element is not evaluated, so its trap does not occur
pass	an un-read record field is not evaluated, so its trap does not occur
pass	an unannotated add narrowed by a UInt8 consumer keeps its overflow guard and traps
pass	an unannotated closure in a Some payload is typed from the Option's element arrow
pass	an unannotated closure in a user variant payload is typed from the declared arrow
pass	an unannotated closure is inferred through an unannotated recursive HOF parameter
pass	an unannotated closure parameter is grounded from its body and applied at runtime
pass	an unannotated closure typed Int8 from context computes an in-range constant
pass	an unannotated closure typed Int8 from context overflows a constant like an explicit Int8 param
pass	an unannotated closure with an unused parameter in a payload takes the declared parameter type
pass	an unannotated closure with distinct-typed params is inferred through a generic recursive HOF
pass	an unannotated generic branching sum fold infers its parameter's instantiation from the arms
pass	an unannotated generic instantiates at a FUNCTION type and a scalar in one body
pass	an unannotated generic recursive tree consumer whose result is independent of the payload infers
pass	an unannotated generic sum ACCUMULATOR infers its parameter from the values folded into it
pass	an unannotated helper re-exported through a chain instantiates at TWO types from the entry
pass	an unannotated predicate closure is inferred through a recursive counting HOF
pass	an unannotated program with a valid typing type-checks and runs
pass	an unannotated rest parameter gathers a heterogeneous tuple whose size is known
pass	an unannotated tuple-SWAP instantiates at two mixed scalar-heap element pairings in one program
pass	an unannotated two-argument closure is inferred through a generic recursive HOF
pass	an unbound CONSTRUCTOR applied in an uncalled def is rejected
pass	an unbound FIELD type in a never-constructed record type declaration is rejected
pass	an unbound PAYLOAD type in a never-constructed type declaration is still rejected
pass	an unbound TYPE name in an uncalled definition's annotation is still rejected
pass	an unbound argument to a USED parameter still rejects (surfaced via the reduced body)
pass	an unbound export BEFORE a valid main export is rejected
pass	an unbound export after a valid HELPER export is rejected
pass	an unbound handle head keeps its own unbound-name error
pass	an unbound match arm head over a recursive body is a coded CDZ0101, not silently accepted
pass	an unbound name as a parameter's annotation type is rejected
pass	an unbound name as an ARGUMENT to a known ctor in an uncalled def is rejected
pass	an unbound name close to a let binding suggests the binding with a replace fix
pass	an unbound name close to a top-level def suggests it with a replace fix
pass	an unbound name in a map key is a scope error, not a coerced string
pass	an unbound name in a resume's state position is rejected
pass	an unbound name in a short-circuited boolean operand is still rejected
pass	an unbound name in a type annotation's type position is rejected
pass	an unbound name in a type-argument position keeps CDZ0101, not the non-type message
pass	an unbound name in an integer width position surfaces a width-specific unbound-name error
pass	an unbound name in an uncalled def's handler-ARM resume argument is rejected
pass	an unbound name in an uncalled def's match-ARM BODY is rejected
pass	an unbound name in an uncalled def's match-arm GUARD is rejected
pass	an unbound name in an uncalled sibling definition is still rejected
pass	an unbound name in an unselected conditional branch is still rejected
pass	an unbound name in an unselected match arm is still rejected
pass	an unbound name inside an UNCALLED lambda body is still rejected
pass	an unbound unquote-splicing operand keeps its own unbound-name error, not a spurious non-list reject
pass	an unbound uppercase name in a type annotation's type position is rejected
pass	an unbound width name in a NON-first type-argument position is caught and names the ctor-appropriate example
pass	an unbound width name nested in a compound annotation is caught
pass	an unconditional cross-function abort folds via inline
pass	an unconsumed handler seed is unobserved when the body never performs, so its trap is elided (lazy-effect)
pass	an unconsumed resume next-state is unobserved, so its trapping expression is elided (lazy-effect)
pass	an undefined name in the 2nd+ position of a multi-name export clause is caught with a did-you-mean
todo	an under-applied built-in operation curries — completing it yields a value (should-work)
pass	an underscore export name crosses under a normalized extern name
pass	an underscore-letter identifier binds and resolves
pass	an underscore-prefixed function parameter binds its argument
pass	an underscore-prefixed match-arm binder is a usable binding, not a wildcard
pass	an underscore-prefixed token is an identifier, not an integer
pass	an undetermined Set-key element type is rejected CDZ0203 (annotate), not a codeless decline
pass	an ungranted effect hidden in a COLLECTION-stored closure is still rejected
pass	an ungranted effect hidden in a closure passed to a HOF is still rejected
pass	an unguarded same-variant fall-through restores exhaustiveness for a guarded variant match
pass	an unimported sibling definition is not in scope
pass	an unknown BASE unit inside a Unit.define is caught and named
pass	an unknown member name is rejected with a did-you-mean (CDZ0201)
pass	an unknown module operation too far for a confident typo lists the module's operations
pass	an unknown operation-type name keeps its unbound CDZ0101, not a spurious arrow reject
pass	an unknown prelude-module operation lists the available operations, not a confident single
pass	an unknown type in a NESTED record parameter annotation field names only the deep type
pass	an unknown type in a let-binder annotation is rejected
pass	an unknown type in a record LET-BINDER annotation names the type, not the field label
pass	an unknown type in a record VALUE annotation names the type, not the field label
pass	an unknown type in a record field inside a variant payload is rejected
pass	an unknown type in a record parameter annotation names the type, not the field label
pass	an unknown type in a variant payload is rejected at the declaration
pass	an unknown type in an effect operation arg is rejected at the declaration
pass	an unknown type in an effect operation result is rejected
pass	an unknown type nested in a List variant payload is rejected
pass	an unknown type nested in a let-binder List annotation is rejected
pass	an unknown type nested in an effect operation List arg is rejected
pass	an unknown unit near a real one suggests it with a delimiter-preserving symbol rename fix
pass	an unknown unit with no confident neighbour gets compose/declare guidance, not closest-matches
pass	an unproductive PARAMETERIZED self-recursion is a coded CDZ0204 — no base case, result never concrete
pass	an unproductive self-recursion is declined, not a compiler crash
pass	an unprojected element whose constant modulo-by-zero traps is elided but earns a CDZ0305 warning
pass	an unprojected element whose constant overflow traps is elided but earns a CDZ0305 warning
pass	an unprojected element whose constant zero-denominator Rational.of traps is elided but earns a CDZ0305 warning
pass	an unprojected tuple element that provably traps is elided but earns a CDZ0305 dead-trap warning
pass	an unquote nested inside a plain quote is a syntax error, not an active unquote
pass	an unquote of an expression with an unbound name is rejected, not quoted
pass	an unquoted variable is not captured by a list-rest-pattern binder
pass	an unquoted variable is not captured by a map-pattern value binder
pass	an unquoted variable is not captured by a same-named fn param in the template
pass	an unquoted variable is not captured by a same-named let binder in the template
pass	an unquoted variable is not captured by a same-named match-pattern binder in the template
pass	an unquoted variable is not captured by a tuple-pattern binder
pass	an unquoted variable is not captured by a variant-payload match-pattern binder
pass	an unread op argument is unobserved (arm ignores its param), so its trapping expression is elided (lazy-effect)
pass	an unread op-argument that performs a foreign effect still threads that perform's state advance
pass	an unrecognized annotation leaves its wrapped definition in effect
pass	an unrecognized pragma key is rejected rather than ignored
pass	an unrecognized string escape is rejected
pass	an unrelated-payload Option mismatch gets no match-it hint
pass	an unrepresentable closure export is one coded boundary error, not a shadowing decline
pass	an unsatisfiable equality still traps on a divide-by-zero operand
pass	an unsigned LEB128 encoder emits a single byte below the continuation threshold
pass	an unsigned LEB128 encoder emits the known-answer multibyte encoding
pass	an unsigned branch refinement elides an underflow guard — the operator's if x>0 example on an unsigned type
pass	an unsigned equality guard pins the exact value so a then-branch arith sheds its guard (value parity)
pass	an unsigned integer module offers no neg member
pass	an unsigned pattern segment reads the same byte as a non-negative integer
pass	an unsigned right shift fills with zeros, not the sign bit
pass	an unsigned runtime value is not accepted by a signed segment of the same width
pass	an unsigned segment reads the same sign-bit-only byte as 128
pass	an unsigned wrap of a value the range lattice proves in-range elides its mask
pass	an unsigned-byte entrypoint takes and returns a u8 at the boundary
pass	an unsolved type variable renders as underscore in a call-argument mismatch
pass	an unsolved type variable renders as underscore in a list-element clash
pass	an unsolved type variable renders as underscore in an if-branch join
pass	an untaken arm's overflowing operand does not trap under the operator hoist
pass	an unused ANONYMOUS-LAMBDA parameter surfaces the CDZ0306 unused-parameter warning + `_`-prefix fix
pass	an unused NON-NORMALIZING let init is eliminated but earns a CDZ0305 warning
pass	an unused let binding compiles and runs but the build surfaces a CDZ0306 unused-binding warning
pass	an unused let binding whose init does not reduce to a value is elided but earns a CDZ0305 warning
pass	an unused let binding whose init performs still advances the handler state
pass	an unused let binding whose init would divide by zero is elided, so its trap does not occur
pass	an unused let binding whose init would overflow is elided, so its trap does not occur
pass	an unused let-init that does not reduce to a value is eliminated, so it does not diverge
pass	an unused non-exported definition compiles and runs but the build surfaces a CDZ0306 unused-definition warning
pass	an unused tuple-pattern match binder warns exactly once (only the dead binder, `b`)
pass	an unused variant-payload match binder warns CDZ0306 with a `_`-prefix fix
pass	an unusual in-range width is a first-class type
pass	an unusual signed width overflows its own maximum and is rejected CDZ0304
pass	an unusual signed width underflows its own minimum and is rejected CDZ0304
pass	an unusual unsigned width overflows its own maximum and is rejected CDZ0304
pass	an unusual unsigned width underflows below zero and is rejected CDZ0304
pass	an unusual-width minimum modulo -1 is zero and does not overflow
pass	an unusual-width value that overflows its computed range is rejected at compile time
pass	an unwrap-transform-rewrap helper round-trips a newtype at runtime
pass	an unwrapped conversion result is a bare number under ordinary numeric rules
pass	an unwrapped length adds to an unwrapped time with no dimension error
pass	an updated-back RRB list EQUALS the never-updated build (update history-independence)
pass	an1 explicitly ASCRIBED draws — (: (E.next) Int64) in let and argument positions changes nothing observable
pass	an1 the *0 annihilator does not discard a trapping divisor ((* (/ a b) 0))
pass	an2 the self-subtract annihilator does not discard a trapping operand ((- (/ a b) (/ a b)))
pass	and evaluates the right operand when the left is true
pass	and performs the right operand's effect when the left is true
pass	and short-circuit does not perform the right operand's effect
pass	and short-circuits at run time: a false left operand skips the trapping right operand
pass	angle units radian and degree are first-class and exact within their own dimension
pass	annotating a nullary generic producer's result grounds the element and it runs
pass	annotating a quantity at a dimension the expression does not derive is an error
pass	annotating a quantity at a same-dimension DIFFERENT unit is accepted (dimension checked, not scale)
pass	annotating with a monomorphic sum applied to a type argument says it takes no type parameters
pass	another unknown-unit near-miss suggests the near unit
pass	ap1 an arm destructures an Option-of-list op ARGUMENT through the nested pattern
pass	applying a boolean is a type error
pass	applying a float is a type error
pass	applying a generic type constructor to a value names the type-argument position
pass	applying a monomorphic sum in value position says it takes no type parameters
pass	applying a non-function is a type error
pass	applying a non-generic prelude type to a value names it a type, not a function
pass	applying a nullary function names it and says it takes no arguments
pass	applying a nullary function with two surplus arguments pluralizes the count
pass	applying a plain value def keeps the type-named message, not the nullary-function wording
pass	applying a value to a NON-type value keeps the generic not-a-function message, not missing-colon
pass	applying an effect name names the category, not the leaked synthesized record type
pass	applying the annotation float coercion wrap recompiles and runs
pass	applying the integer-width of-conversion wrap clears the mismatch and runs
pass	applying the operator float coercion wrap recompiles and runs
pass	applying the out-of-range integer literal widen fix recompiles clean
pass	applying the result of a match whose arms return payload-capturing closures
pass	applying the unknown-unit rename fix clears the fault — the corrected unit runs
pass	applying the unused-parameter underscore fix (`y` to `_y`) silences the warning
pass	aq1 constant quotes hoist build-once and each depth-walk over the immortal Ast leaks its extraction dups
pass	aq2 fifty depth-walks over a hoisted constant quote leak LINEARLY (per-walk extraction dups)
pass	ar1 a WHOLE nested handle expression inside a handler ARM's resume value
pass	arithmetic between a char and a BigInt is rejected with a working two-step conversion fix
pass	arithmetic between a char and a FLOAT is rejected with a working two-step conversion fix
pass	arithmetic between a char and a Rational is rejected with a working two-step conversion fix
pass	arithmetic between a char and an Int64 is rejected with the plain Char.to-int fix
pass	arithmetic is generic over the integer type, sharing a built-in bound with itself
pass	arithmetic on a bound Rational payload computes and escapes exactly
pass	arithmetic on a let binding whose initializer diverges traps
pass	arithmetic on an unusual UNSIGNED width enforces its own range at compile time
pass	arithmetic on an unusual signed width enforces the type's OWN range at compile time
pass	arithmetic reached via an inlined diverging call argument traps
pass	arithmetic right shift
pass	arithmetic right shift of negative one is negative one
pass	arithmetic right shift preserves the sign bit for a negative operand
pass	arithmetic within one integer type
pass	arm-interned symbols keep content identity ACROSS dispatches — same content = same symbol
pass	as1 concat ASSOCIATIVITY — left-nested and right-nested three-way concats are structurally EQUAL and element-identical
pass	asserting a value has a type is Type.try-as fed to Option.expect
pass	at min times -1 the three overflow families diverge: checked None, wrapping min
pass	at most one rest parameter is allowed
pass	av1 the inner arm ABORTS with an OUTER draw as the abort value — the escaping value performs on the way out
pass	av2 the abort value SUBTRACTS two outer draws under a DOUBLING outer state — antisymmetry pins order inside the aborting arm
pass	av3 in a THREE-stack the innermost arm aborts with a MIDDLE draw — the escaping value advances the middle thread, later draws see it
pass	b(mul): a no-overflow obligation is DISCHARGED for x <= 100, (x * 2) <= MAXINT via positive-multiplier monotonicity
pass	b(sub): a no-underflow obligation is DISCHARGED — for x >= 0, (x - 1) >= MININT via monotonicity + a CHECKED numeral fact
pass	b4b denotation: a predicate Ast (<= x 100) denotes to the bounds obligation term le (Var 0) (Num 100)
pass	b4c(conjunctive) NEG-1: precondition covers only {le x 100} (missing ge x 0) — the 2-hyp proof is NOT licensed
pass	b4c(conjunctive) NEG-2 (reverse): precondition covers only {ge x 0} (missing le x 100) — NOT licensed though discharged via le
pass	b4c(conjunctive): two stacked @requires give a 2-hyp proof; licenses requires BOTH hyps covered by the precondition
pass	b4c(proven): @requires(<= x 100)/@ensures(<= ret MAXINT) on (f x)=x+1 discharges — P denoted as hyp, Q[ret:=body] as goal
pass	b4c(unprovable): @ensures(<= ret MAXINT) on unbounded (f x)=x+1 is NOT dischargeable — the proof tier correctly MISSES (CDZ-VERIFY)
pass	b63a a UInt64 logical right shift by one halves the value
pass	b63b a nonneg UInt64 logical shift satisfies the >= 0 tautology
pass	b63c a mask covering the 2^63-1 range of a UInt64 >> 1 is a no-op
pass	ba1 List.at swept across the bounds boundary of a runtime list answers Some in-bounds and None out (indices 0..4 over len 3)
pass	ba1 an EMPTY Bytes arg crosses the wasm host boundary
pass	ba2 fifty provably-in-bounds List.at reads (index = k mod len) never miss (the elision's legitimate cell)
pass	ba3 the exact bounds edge: index len-1 answers the last element, index len answers None
pass	bar1 Bytes.at over an owned-temporary Bytes.concat result reclaims it
pass	bar2 a borrowed bytes read by Bytes.at AND Bytes.len is not freed early
pass	bar3 Bytes.at over an owned-temporary Bytes.slice result reclaims the slice
pass	bar4 5000x Bytes.at over an owned-temporary Bytes.slice each reclaims (no leak drift)
pass	bar5 Bytes.at over an owned-temporary Bytes.compact result reclaims it
pass	bar6 Bytes.at over an owned-temporary String.to-bytes result reclaims it
pass	bar7 5000x Bytes.at over an owned-temporary Bytes.compact each reclaims (no leak drift)
pass	bare patterns on a CONCRETELY-exported type stay legal for importers
pass	bas1 a BASE-7 codec state — encode peels a value's digits MSB-first onto the list (recursive prepend-peel), decode folds the WHOLE accumulated digit run back and clears; concatenated encodes decode as positional composition
pass	bc1 a comparison of a draw feeds a BOOL-taking op whose arm NEGATES the state — the bool crosses the dispatch boundary
pass	bc1 short-circuit AND over two draws — the false-first row skips the second draw, observed by the branch draw
pass	bc2 monotonicity of THREE draws under a parity-dependent step — the and of two comparisons decides, the final state rides along
pass	bc2 short-circuit OR over two draws — the true-first row skips the second draw
pass	bc3 a nested and-of-or short-circuit tree over draws — each row exercises a distinct skip pattern
pass	bc3 an op RETURNS Bool consumed by a two-flag if ladder — a mod-3-dependent step makes all four paths reachable
pass	bch1 a BATCH-coalescing writer — write buffers answering the count until the THIRD item self-flushes answering a hundred plus the batch sum, sync force-flushes the partial answering its sum or -1 when empty, and two seed-shaped values land in DIFFERENT batches so both the partial-sync and the self-flush rows carry the seed
pass	bcn1 a BEACON whose sweep answer is a LET CHAIN INSIDE the resume's argument position — the arm binds the turn and the beam octant in nested lets scoped ONLY within the answer expression while the next-state tuple recomputes the same turn and the flash test inline outside their scope, log reads angle parity and flashes, and the seed aims the lamp so the two runs flash from different octants and drift apart by the accumulated turns
pass	bcp1 Bytes.compact of the EFFECT-GROWN rope inside the arm — the compacted flat rep reads exactly at a computed index and re-threads
pass	bcr1 a runtime bin construction round-trips through its own pattern with explicit wrap semantics (300 → u16 300 / u8 44)
pass	bcr2 fifty runtime bin construct+match cycles reclaim to zero
pass	bd1 the arm CLASSIFIES its state into a tri-band SUM — the body matches Lo/Mid/Hi per dispatch as the thread climbs through the bands
pass	bd2 the scoring CLOSURE captures a DRAW — the weight itself comes from the thread before the classified reads
pass	bdr1 a 50-concat deep Bytes rope's len walks the tree and the rope reclaims clean
pass	bdr2 a byte-scan with Bytes.at across every seam of a 50-concat rope counts exactly (scalar reads; fixed 1-cell residue)
pass	bdr3 a slice SPANNING the seams of a deep Bytes rope reads exact length and content (start+LENGTH contract)
pass	bf1 a growing BYTES FRAME as handler state — each op appends a u8 record, the final op bin-decodes head + rest length
pass	bf2 the frame grows by u16 BE records — dump decodes the SECOND record through a fixed-width first segment
pass	bf3 tagged-record round-trip — arms ENCODE (tag,val) u8 pairs into the frame state, the body decodes the REPLAYED frame
pass	bfl1 bare equality over flat Bytes.of runtime twins
pass	bfl2 bare equality across Bytes.concat vs Bytes.of flat twins
pass	bg1 a BOOL op result is the recursion CONDITION itself — the walk continues while draws stay true
pass	bg2 the recursion guard is an AND of a bool draw and a pure bound check — short-circuit must not skip the draw's state advance observation
pass	bg3 a BIGINT handler state — tripling per dispatch crosses the i64 boundary mid-thread, the exact multi-limb value survives
pass	bg4 BIGINT comparison in the arm routes tri-band verdicts — doubling walks past both thresholds, one row compares a genuine multi-limb value
pass	bhw1 a HIGH-WATER BYTES state — byte-lexicographic max through the thread; the multibyte lead 0xC3 must rank UNSIGNED above ASCII z, and the seeded third put only wins past 0xC3
pass	bid1 a SECOND-PRICE auction tracker — each bid answers the index of the CURRENT leader (a beaten high demotes to second, a middle bid bumps only second), and the closing draws read the winner index and the price he actually pays
pass	bign1 a beyond-i64 N-suffixed LITERAL renders and re-reads through every surface — the #5371 printer fence
pass	bin-encode segment ORDER holds for runtime-swapped operand values
pass	binary operator operands are evaluated left to right
pass	binding the same effect to a peer twice is rejected
pass	bis1 BINARY SEARCH against an ORACLE effect — the arm only answers -1/0/1 vs the hidden target, the body's recursive driver narrows (lo,hi) from the verdicts, dispatch count is data-dependent
pass	bit-field segments pack sub-byte values into one byte
pass	bit-field segments spanning a rope seam split the stitched 16 bits, not a leaf's
pass	bitwise AND masks bits
pass	bitwise AND of an integer and a float does not silently promote
pass	bitwise AND of two floats rejects — a float is not a bit pattern even without a mix
pass	bitwise AND with negative one is the operand (identity)
pass	bitwise AND with zero is zero (annihilator)
pass	bitwise OR combines bits
pass	bitwise OR of an integer and a float does not silently promote
pass	bitwise OR with negative one is all ones (annihilator)
pass	bitwise OR with zero is the operand (identity)
pass	bitwise XOR is its own inverse
pass	bitwise XOR of a value with itself is zero (self-inverse)
pass	bitwise XOR of an integer and a float does not silently promote
pass	bitwise XOR toggles bits
pass	bitwise XOR with negative one is the bitwise complement
pass	bitwise XOR with zero is the operand (identity)
pass	bk1 two runtime Blake3 digests of the same bytes bare-compare equal
pass	bk2 one-byte-different inputs bare-compare unequal digests
pass	bk3 runtime digests dedup as set members by content
pass	bk4 a Map keyed by a runtime digest is findable by a fresh recompute
pass	bk5 Blake3-of-encode runtime composition compares equal across recomputation
pass	bkf1 EXPONENTIAL BACKOFF with a seed-shaped cap — fail accrues the current delay into the total then doubles it clamped at the cap, ok resets the delay to one answering the accumulated wait, and the higher cap lets one seed keep doubling where the other saturates two steps earlier
pass	blc1 a BALANCE SCALE weighing TWO op arguments against each other — weigh's three-way arg-vs-arg comparison answers side and margin counting only LEFT wins, every weigh folds the signed difference into the running tilt, level reports the tilt's magnitude by sign-splitting WITHOUT abs tagging lean direction in the hundreds digit, and the seed loads the left pan of three weighings so one run balances twice level-right while the other tips left twice and reads a levelled beam last
pass	bmr1 bin segments read exactly across the seams of a 50-concat rope and reclaim
pass	bmr2 fifty bin matches over a deep rope leak zero per-match (the rest binding reclaims where slicing leaks)
pass	bms1 a BITMASK SET in one Int64 — set ORs a shifted bit, clear ANDs with the XOR-complement answering whether the bit was live, popcount peels the mask; the clear of an absent high bit is a no-op zero
pass	bmv1 BOYER-MOORE majority vote — the (leader,votes) state deposes on exhausted votes, the challenger seed flips the winner
pass	bnfE 2-tuple FIFO control — (front, back) only, no counter
pass	bo1 bare order over two runtime encode results compares content-lexicographically
pass	bo2 tuple-walk equality over two identical runtime encodes agrees
pass	bor1 a TWO-LIMB borrow decrement — subtracting from the low limb borrows from the high one when it underflows, and a borrow with no high limb left SATURATES both to zero
pass	both elements of a runtime-content fn-return tuple are projected directly and combined
pass	both same-effect handlers STATEFUL — the outer's advance survives the inner's forwards
pass	both sibling args consume the same binding independently
pass	boxed closures of two fn types in one sum: the unbuilt variant's dispatch is dead
pass	bp1 a BYTES payload inside an arm-built Result — even states Ok-wrap a one-byte frame, the body reads len and first byte
pass	bp2 a Bytes TRANSFORMER op — the body frames a byte, the arm decodes it, adds five, re-encodes; the body decodes the transform
pass	bp3 the arm REVERSES a three-byte frame — per-index rebuild through Bytes.at, positional weights pin the swap
pass	bp4 two one-byte op results CONCATENATED in the body — the joined frame's length and both bytes decode
pass	br1 a BYTES handler state grows one byte per recursive dispatch — the final frame's length and bytes pin the accumulation
pass	br2 the arm returns a SLICE of the growing Bytes state — a window over rope-accumulated bytes crosses dispatch
pass	branches performing DIFFERENT counts each thread their own post-state to the continuation
pass	brd1 a BORDA count over three candidates — each ballot awards two points to its first choice and one to its second answering the first choice's new total, lead answers the current leader with ties to the lowest id, and the seed steers ONE ballot's first choice producing a three-way tie on one run and a runaway winner on the other
pass	breaker capsubst: a non-capturing substitution takes the plain path
pass	breaker capsubst: the fresh binder clears the body's ids, not only s's
pass	breaker exists: DISJ2 preserves the premise's hypothesis
pass	breaker exists: EXISTS-intro preserves multiple hypotheses from the premise
pass	breaker exists: EXISTS-intro rejects a witness whose instance does not match the premise (unforgeability)
pass	breaker exists: EXISTS-intro with the witness variable also free in the body substitutes all occurrences
pass	breaker holsubst: a free occurrence beside a shadow substitutes selectively
pass	breaker holsubst: a shadowing binder blocks substitution
pass	breaker holsubst: the naive subst's documented capture hazard
pass	breaker specsubst: GEN of refl then SPEC substitutes the bound var throughout the body
pass	bri1 a constant BigInt (2^65) re-evaluated across fifty frames reclaims to zero (numeric heap constants build per-eval — not in the build-once family)
pass	bri2 a constant Rational compared against fifty runtime-built rationals reclaims to zero
pass	brk1 TYPED-BRACKET matching — opens push their code, a close must match the TOP of the expected stack (LIFO discipline), a wrong-type close flips the sticky invalid; the seed orders the closes right or wrong
pass	brv1 a sibling view's arm-end reclaim leaves the surviving view's window intact
pass	brv2 an inner slice-of-a-slice's reclaim leaves the OUTER view readable
pass	brw1 a BREW fermenter with a stuck-fermentation rescue — each day the gravity drops by the yeast health floored at ten (an inline min), a drop under three counts as STUCK earning a four-point yeast boost and a seven-hundred alarm with the standing gravity, a healthy day answers gravity and the yeast's low digit, pitching adds yeast, the read packs all three, and the weak seed sticks on DAY ONE (rescuing early) while the strong one ferments clean until the floor
pass	bs1 a BOOL toggle state — four draws alternate true/false from an input-dependent seed
pass	bs2 a LATCH — once an op arg is false, the state stays false and every later check answers false
pass	bse1 the COMBINED reclaim levers — a SumExpect view-reclaim fires INSIDE an arm-borrow arm, and the outer view is read AFTER
pass	bse2 a THOUSAND SumExpect view-reclaims into one shared parent, and the parent is read AFTER the loop
pass	bsi1 β-substitution discriminates KEYS from VALUES in a NATIVE #record — keys immune, values substituted
pass	bt1 a BOOL handler state TOGGLES per dispatch — three draws read the alternating flag, seeded by input parity
pass	bud1 a BUDGET-GATED accumulator — two spends consume the budget, the third answers the negated total from exhaustion, a refill re-arms exactly one more spend, the final read exposes the accumulated total
pass	bw1 BIT operations over draws — mask, set-bit, and a draw-driven shift count all read the live thread
pass	bw2 an XOR accumulator folds four draws through a recursion — bit-mixing order-sensitive under the stride
pass	bw3 shift-up then shift-back round trip over MASKED draws — both the value and the shift count come from the thread
pass	bw4 the ARM bit-mixes its argument with the live state — low nibble from the arg, bits 4-5 stamped from the state
pass	bwa1 THREE parallel bit-accumulators in one state — running AND, OR, and XOR folds over the drawn payloads, read as a sum
pass	bwf1 shift-left, arithmetic shift-right on a negative, and bitwise-AND folds agree with their runtime twins
pass	bwl1 a SPARE-CHAIN scorer — each roll adds its pins plus a DOUBLE when the previous two rolls summed to ten, threading a two-roll history through the state, and only the seed whose opening pair hits ten fires the bonus so the scores drift apart from the third row
pass	by1 a BYTES handler state grows two bytes per dispatch — each arm returns the pre-growth length
pass	by1 bytes built from THREE draws then read at a draw-picked index — Bytes.at follows the thread into the buffer
pass	by2 a SLICE view (start,LEN) of the Bytes state read per dispatch — in-range bytes returned, out-of-range answers -1
pass	by3 op-arg values WRAPPED to bytes at runtime accumulate in the state — the fourth emit sees three prior
pass	by4 nested SAME-effect handlers with independent BYTES states — the outer buffer self-doubles, the inner appends
pass	by5 the slmin11 slot-clobber shape on a BYTES slot, phi-merged Bytes.concat growth in a tuple state, 2 puts + 2 reads
pass	byp1 a Bytes entry param measured by Bytes.len reclaims
todo	byp2 a Bytes entry param destructured by a runtime bin match declines
todo	byp3 a Bytes entry param sliced declines (extraction past the length borrow)
pass	byte length agrees with the length of the encoded bytes
pass	byte length counts the normalized form, not the source spelling
pass	byte length is the UTF-8 byte count
pass	byte sequences are equal by their bytes in order
pass	byte-len and scalar-len DIVERGE on a multibyte String entry arg by the encoding width
pass	ca1c a closure over a performing nested-let-init capture folds via the capture-once hoist
pass	ca1m a capture-once closure applied TWICE shares the single draw across both applications
pass	capture-avoiding substitution renames a Forall binder to avoid capture (logical-form subst)
pass	capture-avoiding substitution renames a binder so the substituted term's free variable is not captured
pass	cbb1 a conditional whose BOTH branches are performing captures folds with only the taken branch drawing
pass	cbk1 a CIRCUIT-BREAKER state machine — two failures trip it open, open requests answer the -1 sentinel while a cooldown counts down to half-open, the half-open probe either restores closed on success or re-trips on failure, and the final draw reads the mode
pass	cbm1 a SEQUENTIAL double match on the same runtime list scrutinee is clean (the nested form traps the UAF detector)
pass	cbm2 a nested same-scrutinee match on an OPTION is clean (the list-scrutinee sibling traps)
pass	cbm3 a nested same-scrutinee match on a TUPLE plus a re-consume after is clean
pass	cbo1 a const compound (<) agrees with the three-way (Ordering.of) on the same operands (§331)
pass	cbo2 a compound (<) does NOT elide a runtime operand's construction effect (fold effect-safety guard)
pass	cbw1 a SHIFT (<<) threaded through a const recursion folds (const_eval delegation to core_of's fold_arith)
pass	cbw1 a nested same-scrutinee LIST match computes and fully reclaims (the #4685 fix witness)
pass	cbw2 a bitwise AND (&) threaded through a const recursion folds (const_eval delegation)
pass	cbw2 the nested re-match with no tail consume computes and reclaims
pass	cbw3 the nested re-match through a WILDCARD inner arm computes and reclaims (the header-read-only face)
pass	cc1 CHAR unit values in collections — scalar-value set dedup, ordering, literal match arms
pass	cc1 a closure over the fn PARAM built before the handle, applied twice inside with draws — capture stable, draws advance
pass	cc10 a draw bound in a PLAIN let OUTSIDE the closure init-let is captured once and folds
pass	cc11 a closure over a nested-let-init with a PURE init folds
pass	cc2 a closure escaping handle A is applied inside handle B — the A-capture is stable while B's draws feed the args
pass	cc3 a factory returning a closure over a performing-arg param folds via the capture-once hoist
pass	cc4 TWO closures over SEQUENTIAL draws inside one region — each captures its own read, applied after both bind
pass	cc5 composed closures over three draws — g(f(draw)) where f and g each captured an earlier read
pass	cc6b a closure bound OUTSIDE the outer handle applied inside a nested SHADOW region and after it — capture crosses both boundaries
pass	cc7 a draw-capturing closure threaded through a RECURSIVE helper — applied per frame, the leaf applies it to a fresh draw
pass	cc8 a closure carried in a TUPLE beside a scalar — destructured in one match and applied around advancing draws
pass	cc9 a closure capturing a value computed UNDER a handler escapes and applies after the handle
pass	ccs1 a constant-count left shift: the Int64.min edge is in range, +2^63 overflows
pass	ccs2 a valid constant-count right shift computes
pass	ccs3 a small constant-count left shift computes but still traps on value overflow
pass	ccs4 an out-of-range constant shift count traps unconditionally
pass	ccs5 a negative constant shift count traps
pass	cd1 a closure CAPTURES a draw then is called after LATER draws — the captured value must not re-read state
pass	cd4 a captured draw crosses a HIGHER-ORDER def boundary — helper applies the closure twice with different args
pass	cdf1 recursive AST leaf-count via Option-threaded walk under an explicit (const ...) demand surfaces the taken trap
pass	cdf2b decode-of-encode navigation (Result-correct) under (const ...) surfaces the taken trap
pass	cdf2v decode-of-encode navigation VALUE twin under Ast.encode demand
pass	cdf3 function-typed const param applied in const recursion under (const ...) surfaces the taken trap
pass	cdr1 a CIDER press splitting juice from pomace — pressing yields two-thirds of the hopper as juice (integer) with the REMAINDER going to pomace and the hopper emptying (a dry press answers nine-hundred with the pomace's low digit touching nothing), loading adds apples, the read packs juice hopper and pomace, and the seed's first pressing yields four-to-one on the full hopper against one-to-one on the meagre one with the split ratios diverging every round
pass	cds1 a COUNTDOWN alarm with snooze — tick decrements answering the remaining time until it FIRES at zero (answering a negated ten-plus-fire-count and auto-reloading the seed interval), snooze adds slack, and the shorter interval fires MID-STREAM where the longer one fires on the very last tick
pass	cdv1 integer DIV and REM threaded through a const recursion fold (const_eval arithmetic arm)
pass	cdv2 a const-recursion divide-by-ZERO is fail-loud CDZ0304, not a silent decline
pass	cdzw1 the cadenza backend round-trip preserves the SIGN of negative zero
pass	cdzw10 the cadenza backend round-trips a scalar Match — literal arms, arm selection, and a binder else
pass	cdzw11 the cadenza backend round-trips a runtime NESTED compound — a record with a tuple field and a list field
pass	cdzw12 the cadenza backend round-trips a USER-declared multi-variant sum value — the (type …) declaration re-emits
pass	cdzw13 the cadenza backend round-trips a PRELUDE-COLLIDING user variant head — the qualified (. Type Variant) spelling
pass	cdzw14 the cadenza backend round-trips a RECURSIVE user sum with a nested variant payload
pass	cdzw15 the cadenza backend round-trips a MatchSum CONSUMED through recursion — the #4942+#4954 interplay
pass	cdzw16 the cadenza backend round-trips a MULTI-PAYLOAD variant match — slot-i payload binders
pass	cdzw17 the cadenza backend round-trips a MULTI-leading-rest list pattern — (list a b c .. r)
pass	cdzw18 the cadenza backend round-trips a RECURSIVE fold THROUGH list matches
pass	cdzw19 the cadenza backend round-trips a runtime map with a DUPLICATE key — last-insert-wins survives the stored-order replay
pass	cdzw2 the cadenza backend round-trip preserves Int64.min
pass	cdzw20 the cadenza backend round-trips a GUARDED list arm whose condition reads the REST binder
pass	cdzw21 the cadenza backend round-trips a guarded arm FALLING THROUGH to a later SAME-SHAPE arm
pass	cdzw22 the cadenza backend round-trips a JOIN-RESOLVED bare (None) — the if-branch Option
pass	cdzw23 the cadenza backend round-trips a join-resolved Result — the Ok/Err twin
pass	cdzw24 the cadenza backend round-trips a MONOMORPHIC single-variant newtype — construct sites and a pass-through in one program
pass	cdzw25 the cadenza backend round-trips a bare (None) in a VARIANT-PAYLOAD position — the nested-Option join
pass	cdzw26 the cadenza backend round-trips a bare (None) as a LIST ELEMENT — the element-position join
pass	cdzw27 the cadenza backend preserves compound-compare SHORT-CIRCUIT — a differing first component never forces the trapping second
pass	cdzw28 the FORCING twin — equal first components force the second, and the div-by-zero trap surfaces identically
pass	cdzw29 the cadenza backend round-trips a GENERIC single-variant newtype instantiated at Int64
pass	cdzw3 the cadenza backend round-trip preserves the minimum positive subnormal Float64
pass	cdzw30 the cadenza backend round-trips a GENERIC two-variant sum — both arms, join-composed
pass	cdzw31 the cadenza backend round-trips a GENERIC sum MATCHED with a payload binder
pass	cdzw32 the cadenza backend round-trips a generic newtype with a COMPOUND payload matched back
pass	cdzw33 the cadenza backend round-trips a COMPOUND-INNER monomorphic newtype value
pass	cdzw34 two width-DIFFERENT .wrap Converts of one operand keep their OWN widths (the CSE-key fence), Int32-first order
pass	cdzw35 the ORDER-SWAPPED twin — UInt8-first must not narrow the Int32 wrap
pass	cdzw36 a MULTI-PAYLOAD single-variant newtype round-trips slot-wise through the cadenza hop
pass	cdzw37 a GUARDED sum arm with same-variant fall-through round-trips through the cadenza hop
pass	cdzw38 a NESTED variant pattern (Option-of-Option) round-trips through the cadenza hop
pass	cdzw39 a USER sum nested inside Option round-trips its nested-switch through the cadenza hop
pass	cdzw4 the cadenza backend round-trip keeps float division float (not int truncation) despite the shared surface operator
pass	cdzw40 a chained Map build + lookup + Option match round-trips through the cadenza hop
pass	cdzw41 Set.of + Set.contains on a runtime probe round-trips through the cadenza hop
pass	cdzw42 Set.of DUP-COLLAPSE observed through to-list length survives the cadenza hop
pass	cdzw43 a NESTED native #tuple pattern destructures and binds through the cadenza hop
pass	cdzw44 native and classic pattern spellings are interchangeable across value spellings
pass	cdzw45 a #map pattern matches by KEY — binds present keys order-free, falls through on an absent key
pass	cdzw46 a guard condition reads a native #tuple pattern's binders
pass	cdzw47 a Qty constructed at a bare-magnitude binder and consumed to its value round-trips the cadenza hop
pass	cdzw48 Char.from-int → match → Char.to-int round-trips the cadenza hop on both Option faces
pass	cdzw49 a def whose TAIL is a computed-magnitude Qty re-emits ((. Qty of) mag unit) through the cadenza hop
pass	cdzw5 the cadenza backend round-trip preserves a float comparison over a parameter
pass	cdzw50 a Qty whose magnitude is a CHECKED NARROW round-trips the cadenza hop with the wrapper intact
pass	cdzw51 a beyond-i64 BigInt constant re-emits re-compilably through the cadenza hop
pass	cdzw52 a nullary user-sum VALUE re-emits WITH its type declaration through the cadenza hop
pass	cdzw53 a SINGLE-VARIANT sum keeps its NOMINAL type through the cadenza hop
pass	cdzw54 a Qty whose magnitude is a MATCH of checked-narrow arms wraps whole through the cadenza hop
pass	cdzw55 a fully-static handled effect ERASES through the cadenza hop to its symbolic residual
pass	cdzw56 a RUNTIME-count handled effect hops as its state-threaded recursion residual
pass	cdzw57 a runtime BIN construction (mixed widths + endianness) round-trips the cadenza hop
pass	cdzw58 RUNTIME BigInt arithmetic (widen + add + mul beyond i64) round-trips the cadenza hop
pass	cdzw59 RUNTIME Rational arithmetic reduces to lowest terms through the cadenza hop
pass	cdzw6 the cadenza backend round-trip preserves a let-binding and an if over an integer parameter
pass	cdzw60 runtime Float64 arithmetic, comparison, and a NaN self-equality all round-trip the cadenza hop
pass	cdzw61 a runtime BIT-FIELD binary construction round-trips the cadenza hop
pass	cdzw62 SYMBOL values — runtime-selected equality and set dedup — round-trip the cadenza hop
pass	cdzw63 NESTED list-rest patterns (a rest at BOTH nesting levels) round-trip the cadenza hop
pass	cdzw64 runtime String.slice + String.byte-len round-trip the cadenza hop re-compilably
pass	cdzw65 a @requires contract ERASES through the cadenza hop to its inlined guard — enforcement identical
pass	cdzw66 RUNTIME closures in a data structure — built, runtime-indexed, applied — round-trip the cadenza hop
pass	cdzw67 a depth-24 nested tuple destructure round-trips the cadenza hop — the fold-budget fence
pass	cdzw68 MAP-KEYED closure dispatch round-trips the cadenza hop
pass	cdzw69 a GENERIC recursive sum (compound type-param payload) round-trips the cadenza hop
pass	cdzw7 the cadenza backend round-trips a nested let/if mixing int and float operators in one program
pass	cdzw70 a MULTI-EXPORT program round-trips the cadenza hop with every export callable
pass	cdzw71 STRING-LITERAL match arms round-trip the cadenza hop
pass	cdzw72 a 100k-deep TAIL recursion completes through the cadenza hop — TCO survives re-emit
pass	cdzw73 a THREE-deep guard chain on one variant cascades through the cadenza hop
pass	cdzw74 Result with heterogeneous payloads + Result-nested-in-Option round-trip the cadenza hop
pass	cdzw75 a let-bound PARTIAL APPLICATION reused twice round-trips the cadenza hop
pass	cdzw76 PARTIAL APPLICATIONS stored in a list and runtime-selected round-trip the cadenza hop
pass	cdzw77 MUTUAL recursion (with mutual tail calls at 100k depth) round-trips the cadenza hop
pass	cdzw78 closure COMPOSITION (a HOF returning a built closure) applied twice round-trips the cadenza hop
pass	cdzw79 float division by ZERO yields IEEE +inf (no trap) and two runtime +infs are BYTE-EQUAL — through the cadenza hop
pass	cdzw8 the cadenza backend round-trips recursive calls — self-recursion and mutual recursion
pass	cdzw80 Bytes concat/slice/at/value-eq composite round-trips the cadenza hop
pass	cdzw81 Qty values INSIDE compounds (record field + list elements) round-trip the cadenza hop
pass	cdzw82 COMPOUND KEYS — a set of tuples dedups structurally and a tuple-keyed map looks up — through the cadenza hop
pass	cdzw83 RECORD keys are field-order-INSENSITIVE — two spellings dedup in a set and compare equal — through the cadenza hop
pass	cdzw84 a DEPTH-5 linear single-payload chain unwraps through the cadenza hop — the #5546 un-flatten
pass	cdzw85 SUM values as set elements — Option dedup by payload + membership probe — through the cadenza hop
pass	cdzw86 STRING lexicographic ordering (prefix rule + equal boundary) round-trips the cadenza hop
pass	cdzw87 single-variant sum payload-element reads on an OPAQUE scrutinee round-trip the cadenza hop
pass	cdzw88 ARITY-1 TUPLE-payload newtype payload projections round-trip the cadenza hop
pass	cdzw89 NESTED single-variant sums — payload-slot nominal descent through the cadenza hop
pass	cdzw9 the cadenza backend round-trips a beyond-Int64 BigInt constant (emits (: n BigInt), not BigInt.of)
pass	ce01 checked addition over a RUNTIME operand yields Some when it fits
pass	ce02 checked addition over a RUNTIME operand yields None on overflow
pass	ce03 Record.merge of two RUNTIME-built records projects the merged field
pass	ce04 Tuple.concat of RUNTIME tuples destructures to the joined fields
pass	ce06 Bytes.of of a NON-literal (branch-selected) list lowers
pass	ce07 literal-tuple pattern matches a RUNTIME tuple
pass	ce08 equality of two RUNTIME-built Maps
pass	ce09 equality of two RUNTIME-built Sets
pass	ce10 equality of RUNTIME lists of records
pass	ce11 wrapping addition over a RUNTIME operand wraps
pass	ce12 checked multiplication over a RUNTIME operand yields Some when it fits
pass	ce93c CONST twin: Ast.encode of the same constant AST folds to the same length
pass	ce93s Ast.encode ALONE over a runtime AST yields canonical bytes
pass	ce93s2 runtime encode length EQUALS the const-folded length (consistency witness)
pass	cel1 a RULE-NINETY cellular automaton in one Int64 byte — step XORs the left-shifted and right-shifted worlds masked to eight bits answering the new generation, density popcounts between steps, and the seeds' initial worlds diverge into different orbits with different densities
pass	cepn1 CONST-EVAL destructures native #tuple/#list patterns — the #5325 second half
pass	cepn2 CONST-EVAL folds a sum-payload NATIVE #tuple sub-pattern — the #5412 extension of cepn1
pass	cfr1 a CONTINUED-FRACTION expander — each step peels the integer part of p/q answering it then inverts the remainder to (q, p-a*q), a drained fraction answers -1 forever, and the seeds share a denominator but one terminates two steps early so its tail is sentinels while the other is still peeling
pass	cgf01 leaf count via list-REST-pattern recursion folds (CDZ0304 detector) under FORCED (const ...) demand
pass	cgf02 leaf count via indexed walk with NON-const index folds (CDZ0304 detector) under FORCED (const ...) demand
pass	cgf04t leaf count over Ast.module — trap detector distinguishes fold from runtime walk under FORCED (const ...) demand
pass	cgf05 leaf count via SELF-recursive explicit-stack walk folds (CDZ0304 detector) under FORCED (const ...) demand
pass	cgf06 corpus-style linear collect under TRAP demand (H2 test) under FORCED (const ...) demand
pass	ch01 a bare prim passed as a function value applies
pass	ch01b eta-expanded closure control for prim-as-value
pass	ch01d bare operator to an UNCURRIED-annotated HOF applied (g a b)
pass	ch01e bare operator PARTIALLY applied — (let ((inc (+ 1))) (inc n))
pass	ch02 a closure stored in a record field projects and applies
pass	ch03 a closure stored in a LIST is fetched and applied
pass	ch04 a runtime-branch-selected closure applies
pass	ch08 String.from-bytes of RUNTIME-built bytes validates
pass	ch1 a FOUR-op result chain in one nested expression — each op transforms the last result while bumping the shared state differently
pass	ch2 the same FOUR-op chain flattened through LETs — one binding per hop must equal the nested form exactly
pass	ch21 CHAINED computed keys — the second perform's key is computed FROM the first's answer, both dispatches walk the two-lookup-match arm
pass	ch3 the chain hops CROSS two effects — F.b of G.p of F.a, each thread advancing only on its own hops
pass	ch4 the SAME op chained into itself — E.b of E.b of E.a, the middle hop's argument is already a hop
pass	chained Record.with at TWO DIFFERENT fields holds both updates
pass	chained Record.with updates on one field compose with the last write winning
pass	chained `?`s unwrap three List.at reads over a sufficient list
pass	chained and nested do-def shadows over a param each read the prior binding
pass	chained and short-circuits through nesting: a false outer-left skips a NESTED trapping operand
pass	chained guards of the same variant are tried in order
pass	chained runtime UInt8 wrapping ops each re-normalize to width (250+10 then *2 = 8)
pass	chaining two Unit.in conversions is a compile-time error — the inner one already unwrapped
pass	char to-int and from-int round-trip through the scalar value
pass	checked addition yields None on overflow instead of trapping
pass	checked addition yields Some of the sum when it fits
pass	checked multiplication reports overflow as None
pass	checked multiplication reports the Int64.min * -1 overflow as None (the checked companion of the trapping *)
pass	checked multiplication yields Some when it fits
pass	checked subtraction yields None on underflow instead of trapping
pass	checked subtraction yields Some of the difference when it fits
pass	chg1 a BATTERY controller with a protected reserve — charge clamps at one hundred, draw refuses any request that would dip below the seed-shaped reserve answering the negated shortfall with the level UNTOUCHED, and the same draw sequence trips the refusal at DIFFERENT points per seed with the refused row's level surviving to the next draw
pass	chk1 a closed pure handle with SCALAR state folds under (const ...)
pass	chk2 a closed handle with STRING state folds under (const ...)
pass	chm1 a const Map with CHAR keys to-lists key-sorted
pass	chm2 the sorted Char head surfaces a taken trap under the const demand (CDZ0304)
pass	chn1 a BUTTER churn — churning turns a QUARTER of the cream to butter floored at one whole scoop (the empty churn answering nine hundred), pouring past ten SPILLS the excess (an eight-hundred row with the excess and the running spill's low digit), the read packs butter cream and spills, and the seed's cream quarters to different scoops every churn with one pail spilling its pour and the other taking it whole
pass	cho1 a runtime-selected Char in a compound orders by full CODEPOINT at run time — a multi-byte scalar sorts above ASCII
pass	chr1 a capture-once closure driven by a RECURSIVE higher-order fn folds — its enclosing-param capture is threaded into the escaping closure env
pass	cj03 Ast.print of a RUNTIME AST value renders
pass	cj03n Ast.print of a runtime NESTED Ast renders the canonical s-expr text
pass	cj03r Ast.print of a RUNTIME AST value renders
pass	ck1 a Char set dedups repeated members
pass	ck1 a capturing closure that ESCAPES into a sibling def's initializer AND is applied directly is materialized ONCE
pass	ck2 Char set membership hits and misses
pass	ck2 a closure DOUBLED inside one collection literal AND applied directly runs (dup-counted per occurrence)
pass	ck4 a Map keyed by Char discriminates and looks up
pass	ck5b Symbol set dedup+membership, in-program strings
pass	ckp1 a CHECKPOINT/RESTORE protocol — save copies the live slot into the shadow slot, work mutates only the live one, restore copies the shadow back and later work resumes from the checkpoint
pass	ckr1 Set.to-list of Chars sorts on the RUST targets (wasm declines — the emit gap)
todo	ckr2 a String ENTRY param crosses on the RUST targets (wasm declines — the boundary gap)
pass	cl1 a NARROWING clamp arm — the [lo,hi] window shrinks by one each side per dispatch, three args meet three windows
pass	cla1 the arm runs a PURE data-dependent-depth recursion on the crossed ARGUMENT — collatz length computed wholly inside one dispatch frame, the state accumulates the lengths and a final read exposes the total
pass	clb1 a nested match RE-MATCHES the same owned runtime list scrutinee — Class-B LIST re-match reclaim (no double-free, no leak)
pass	clb1 the clear-low-bits idiom (<< (>> x k) k) computes and never overflows
pass	clb2 a bare left shift (not the clear-low-bits idiom) still overflows
pass	cle1 a closure capturing a branch-selected list reclaims after invocation
pass	cle2 TWO closures sharing one captured list both invoke and everything reclaims
pass	cle3 a heap-capturing closure built but NEVER invoked still reclaims
pass	cle4 a closure RETURNED from a helper (escaping env) reclaims after invocation
pass	clk1 a closure capturing a MAP reclaims after invocation
pass	clk2 a closure capturing a SET reclaims after invocation
pass	clk3 a closure capturing a STRING reclaims after invocation
pass	clk4 a closure capturing a runtime AST reclaims after invocation
pass	cll1 a const list ordering agrees with the RUNTIME list walk (cross-check, §331)
pass	cll2 a const BARE empty list orders against a populated sibling under the const demand (element unified from the sibling)
pass	cll3 a const (Ordering.of) of a bare empty vs populated list folds to Less (prefix), sibling-resolved
pass	closures built one per iteration each capture their OWN loop value
pass	closures capture successive SNAPSHOTS of a growing heap list
pass	closures extracted from a list by RUNTIME index and applied dispatch correctly
pass	closures stored as MAP VALUES dispatch by key with distinct captures
pass	clp1 a SATURATING counter — the arm clamps every transition to 0..10 via a pure helper, both bounds hit in one run
pass	clu1 an in-range constant List.update replaces a slot and keeps the length
pass	cm01 (const ...) in a resume ANSWER position folds
pass	cm02 closed MULTI-dispatch handle with LIST state folds under (const ...)
pass	cm02c (const ...) closed handle with STRING state
pass	cm03 (const ...) as a handler SEED folds
pass	cmh-list a const closed handle threading a growing LIST state folds its len query answers to 12
pass	cmh-map a const closed handle threading a growing MAP state folds its len query answers to 12
pass	cmh-set a const closed handle threading a growing SET state folds its len query answers to 12
pass	cmp1 a COMPOSE-ACCUMULATING closure state — each dispatch replaces the state with a new closure wrapping the OLD one (double-then-add over the previous function), applied at 1 per step
pass	cms1 closed handle with TUPLE state under (const ...)
pass	cms2 closed handle with RECORD state under (const ...)
pass	cms3 closed handle with MAP state under (const ...)
pass	cms4 closed handle with SET state under (const ...)
pass	cmt1 a COMPENSATING transaction log — each do applies its delta AND pushes the inverse onto the undo stack, each compensate pops the LAST inverse and applies it, unwinding in strict LIFO order back to the seed
pass	cmu1 runtime UInt64 checked-mul at the exact top: (2^32-1) x (2^32+1) = 2^64-1 fits
pass	cmu2 runtime UInt64 checked-mul just past the top: 2^32 x 2^32 = 2^64 yields None
pass	cmu3 runtime UInt64 checked-add wraps None at max, Some just below
pass	cmu4 runtime UInt64 checked-sub underflows None below zero, Some at exact zero
pass	cmul1 runtime checked-mul in range yields Some
pass	cmul2 runtime checked-mul just past Int64.max yields None (2^31 x 2^32)
pass	cmul3 runtime checked-mul hitting Int64.min EXACTLY fits (-2^31 x 2^32) — the naive-magnitude-check killer
pass	cmul4 runtime checked-mul of Int64.min by -1 yields None (the sign-flip overflow)
pass	cmul5 runtime checked-mul of Int64.min by 1 yields Some Int64.min (identity edge)
pass	cn-c1 Symbol equality under (const ...)
pass	cn-c2c a Set query composed inside a const-param fn folds under Ast.encode demand
pass	cn-c3 BigInt beyond-i64 multiply under (const ...)
pass	cn-c4 Rational exact division under (const ...)
pass	cn-c5 Qty unit algebra under (const ...)
pass	cn02a a NON-recursive const fn taken trap is a RUNTIME trap on the bare call (by-design: it may sit under a runtime branch)
pass	cn02b the Option twin — bare-call taken trap stays a runtime trap (by-design)
pass	cn02c the tuple twin — bare-call taken trap stays a runtime trap (by-design)
pass	cn02d the SAME shape under a (const ...) demand DOES surface CDZ0304 (the explicit fail-loud opt-in)
pass	cn1 a THREE-deep seed RELAY — each nested shadow's seed is a draw from its parent, strides differ per depth
pass	cn2 the inner shadow's RESULT flows up into the outer computation — a let-bound region value scaled beside an outer draw
pass	cnm1 runtime UInt8 checked-mul at the exact 255 top
pass	cnm2 runtime Int8 checked-mul hitting -128 EXACTLY fits (the signed exact-fit twin)
pass	cnm3 runtime Int8 checked-mul of -128 by -1 yields None (the narrow sign-flip)
pass	cnm4 runtime UInt16 checked-mul at the 65535 top
pass	cnw1 a capture-once closure wrapped by two nesting closures folds through all three layers
pass	cnw1 runtime UInt8 checked-add at the 255 boundary
pass	cnw2 a factory taking TWO performing args draws each once and shares both across applications
pass	cnw3 runtime Int8 checked-add at the signed 127 boundary
pass	cnw4 runtime Int8 checked-sub at the signed -128 boundary
pass	cnw5 runtime UInt16 checked-add at the 65535 top
pass	coc1 a compound (Ordering.of) does NOT elide a runtime operand's construction effect (fold effect-safety guard)
pass	coc2 an (Ordering.of) of a Char-leaf compound orders by codepoint (Char is a blessed leaf)
pass	combining a named rate with a length is a dimensional error
pass	compacting a byte sequence built at run time preserves its bytes
pass	compacting a runtime CONCAT ROPE materializes it, preserving the value and staying usable
pass	compacting a slice preserves its bytes
pass	compacting is the identity on value for a whole byte sequence
pass	compacting then TWO order-compares (no equality) is safe (adv-66 perimeter)
pass	compacting then a concat+len of the flat after an equality (no rope-left compare) is safe (adv-66 perimeter)
pass	compacting then reading the flat's LEN after an equality against the rope is safe (adv-66 perimeter)
pass	compare on a sum whose payload is BYTES orders by discriminant then Bytes payload lexicographically
pass	compare on a sum whose payload is a FLOAT is rejected CDZ0203 (no total order; the IEEE partial order never enters the walk)
pass	comparing a Float32 nan to a Float64 finite value is a cross-width type error
pass	comparing a Float32 nan to a Float64 nan is a cross-width type error
pass	comparing a boolean against a character names the scalar kind boundary
pass	comparing a char to a FLOAT is rejected with a working two-step conversion fix
pass	comparing a greater value to a lesser yields Greater
pass	comparing a lesser value to a greater yields Less
pass	comparing a list against an integer names the kind boundary
pass	comparing a map to a record is a type error
pass	comparing a newtype to its erased underlying type is a nominal-boundary error
pass	comparing a record against an integer names the kind boundary
pass	comparing a tuple against a list for equality is a cross-kind type error
pass	comparing a user sum against a record (different compound kinds) names the kind boundary
pass	comparing a user sum against a string names the kind boundary
pass	comparing an Int64 to a Bool with < is a type error
pass	comparing an Option to its payload reads at the argument site and carries a wrap-in-Some fix
pass	comparing an empty map to an empty record is a type error
pass	comparing equal values yields Equal
pass	comparing quantities of incompatible dimension is a compile-time error
pass	comparing quantities of one dimension at different scales converts before comparing
pass	comparing records whose field sets differ in size is a type error
pass	comparing records with different field-name sets is a type error
pass	comparing same-shape nominal types is a type error
pass	comparing sums with disjoint variant sets is a type error
pass	comparing the unit value against a non-unit is a type error
pass	comparing tuples of different lengths is a type error
pass	comparing two Chars is valid (a Char has a defined order)
pass	comparing two function literals with = is a type error
pass	comparing two function-typed parameters with = is a type error
pass	comparing two quantities of the same dimension yields a Bool
pass	comparing two same-shape nominal sum types is a type error, not false
pass	comparing two same-unit Float-inner quantities at RUNTIME rides the scalar float compare
pass	comparing two same-unit scalar-Int quantities at RUNTIME rides the scalar integer compare
pass	comparing two units of one dimension converts before comparing
pass	comparison on an unusual signed width orders by the sign-extended declared value (negative < zero)
pass	compile-time Ast.encode of the same Ast.Int folds to the identical length
pass	compile-time Ast.print of the same Ast folds to the identical text
pass	complementary comparisons over different operand pairs do not fold to a tautology
pass	complementary ordering comparisons fold their or to true and their and to false
pass	compose builds closures capturing TWO function values in one env, both orders
pass	compound equality forces through an equal-prefix element to the deciding element, whose trap occurs
pass	compound equality over a MIXED Float32/Float64 tuple: a differing f64 leaf makes it unequal
pass	compound equality over a MIXED Float32/Float64 tuple: each leaf canonicalized at its own width
pass	compound equality over a Rational leaf respects normalization (1/2 = 2/4)
pass	compound equality over a runtime BigInt leaf compares by the canonical bytes
pass	compound equality over a runtime BigInt leaf distinguishes different values
pass	compound equality over a runtime FLOAT32 NaN leaf: nan equals nan
pass	compound equality over a runtime FLOAT32 leaf: negative zero is not equal to positive zero
pass	compound equality over a runtime NaN float leaf: nan equals nan
pass	compound equality over a runtime Rational leaf compares by the normalized form
pass	compound equality over a runtime SLICE-view Bytes leaf compares by window content
pass	compound equality over a runtime float leaf: different floats compare unequal
pass	compound equality over a runtime float leaf: equal floats compare equal
pass	compound equality over a runtime float leaf: negative zero is not equal to positive zero
pass	compound equality short-circuits at the first differing element — a later trapping element is not forced
pass	computing a span with the Instants reversed (earlier minus later) traps rather than wrapping
pass	concatenating an empty byte sequence on the left is the identity
pass	concatenating an empty byte sequence on the right is the identity
pass	concatenating an empty string on the left is the identity
pass	concatenating an empty string on the right is the identity
pass	concatenating an empty tuple on the left is the identity
pass	concatenating an empty tuple on the right is the identity
pass	concatenating byte sequences built at run time appends their bytes in order
pass	concatenating lists of different element types is a type error
pass	concatenating lists with a runtime element appends in order and totals the length
pass	concatenating tuples preserves each element's type
pass	concatenating tuples with a runtime element reads elements from both operands
pass	concatenating two byte sequences appends their bytes in order
pass	concatenating two empty lists is the empty list
pass	concatenating two empty tuples is the empty tuple
pass	concatenating two large runtime RRB vectors preserves length and every element across the join
pass	concatenating two runtime strings and measuring the result
pass	concatenating two tuples appends their elements
pass	concatenating with the empty list on the left is the identity
pass	concatenating with the empty list on the right is identity
pass	concatenating with the empty list on the right yields the operand by value
pass	concatenation is associative by content
pass	concatenation with a run-time-selected operand joins the actual strings
pass	conditional propagation respects a shadowing rebind of the condition variable
pass	conjunction elimination CONJUNCT2 preserves both operand hypotheses
pass	conjunction elimination keeps BOTH operand hypotheses, not just the projected conjunct's
pass	conjunction is true exactly when both operands are true
pass	conjunction of two theorems sharing a hypothesis concatenates without dedup
pass	conjunction shields a trapping right operand when the left is false
pass	const execution folds a TAKEN nullary variant (Option.None) in Option-threaded AST navigation
pass	const execution folds a higher-order recursion — a closure applied per element
pass	const u32 and u16 bin bindings with their top bits set fold unsigned
pass	constant Bytes structural equality folds over unequal, length-differing, concat, and compact forms
pass	constant compound equality folds structurally over Option, None, tuple, and nesting
pass	constant conditions simplify around performs — kept branches dispatch, dropped ones do not
pass	constant tuple projections fold at boundary and multi-digit indices
pass	constant-succeeding tries as LIST-literal elements unwrap in place and the list builds
pass	constructing a bit-field from a value wider than its width is rejected
pass	constructing a byte sequence with a negative value is a type error
pass	constructing a byte sequence with a value out of range is a type error
pass	constructing a rational with a zero denominator traps
pass	constructing a signed segment at the minimum emits the sign-bit-only byte
pass	constructing a sized bytes segment whose value length differs from the size is rejected
pass	constructing a u8 segment from a value above its range is rejected
pass	constructing an unsigned segment from a negative value is rejected
pass	constructor pattern binders capture the payload uniformly
pass	consuming a runtime Option Float32 (not returning it) leaves zero live objects
pass	context that constrains a nullary variant's element makes Type.of concrete
pass	control: a SCALAR-param entry + a reachable recursive fn (no lift-op imports, no index shift)
pass	control: an UNCALLED stored closure with a CONSISTENT param still compiles and runs
pass	converting a BigInt back to a fixed width is checked and a compile-provable out-of-range narrowing is rejected
pass	converting a NEGATIVE integer to a char yields None
pass	converting a char to its integer scalar value is total
pass	converting a fixed-width integer to BigInt is explicit
pass	converting a float to its own width is the identity
pass	converting a scalar-valued integer to a char yields Some
pass	converting a surrogate code point to a char yields None
pass	converting a unit to another unit of the same dimension is an exact rational scale
pass	converting an out-of-range integer to a char yields None
pass	converting zero to a char yields Some — U+0000 (NUL) is a valid scalar
pass	cord1 SYNTHESIZED world: 2-field s64 record result (no wit-world clause) — the fully-constant record now hoists build-once (WIT static encoding), so it is a census-excluded immortal, NOT a per-call mortal leak
pass	cord4 IMPOSED world: RECORD param + 2-field scalar record result
pass	cord5 IMPOSED world: RECORD param + 20-field record result (the co02 shape, record param)
pass	cp1 a capture-once closure CAPTURED by a nested closure folds via the deep-copy hoist hygiene
pass	cp3 two performing draws in one capture-once closure init fold once each, in order
pass	cp4 a performing nullary factory applied twice shares its single draw via the capture-once fold
pass	cpc1 a conditionally-selected performing capture folds via the branch-aware distribution
pass	cpd1 a COMPOUND-INTEREST ladder — grow applies the seed rate percent truncating answering the new principal, skim withdraws answering the remainder, and the two-point rate difference compounds so the gap between the runs WIDENS every grow row while the skims subtract identically
pass	cpf1 a performing nullary factory called once and applied once folds capture-once
pass	cpp1 a PARTIAL operator passed directly to a recursive HOF
pass	cpp2 a partial built from a RUNTIME operand, stored in a record, projected, applied
pass	cpp3 partials of a COMPARISON operator select via a HOF
pass	cq01 host IMPORT: scalar param + RECORD result — guest reads a field
pass	cq02b host IMPORT: NULLARY + RECORD result, called ONCE
pass	cq02c host IMPORT: NULLARY + LIST result, called once
pass	cq02d NULLARY + RECORD result called TWICE with TWO respond clauses
pass	cq03 host IMPORT: scalar param + LIST result — guest measures it
pass	cq04 host IMPORT: RECORD param + scalar result (reverse direction)
pass	cq04b host IMPORT: RECORD arg + UNIT result (SHAPE-13 control in my namespace)
pass	cq04c record host-ARG with s64 result (no Int64.of)
pass	cr01 TWO exported members in one interface — both callable
pass	cr02 an export whose body runs an IN-GUEST handled effect
pass	cr03 an export combining an IN-GUEST handler AND a host import
pass	cr03b let-sequenced: handle FIRST, then host call (same combination, flat nesting)
pass	cr03c host call INSIDE the handled body
pass	cr03d combination with s64 host result (no Int64.of) — nested
pass	cr1 catch-and-reseed — a conditionally-aborting region's value (abort payload OR normal result) seeds a SECOND handle
pass	cr2 RETRY-until-success — a recursive driver re-runs a conditionally-aborting region until the drawn attempt passes, counting tries
pass	cr3 a BOUNDED retry budget — two attempts then give up, the fallback negating the last failed attempt
pass	crn1 a COMPOUND (tuple) resume value threaded across a NESTED same-effect straddle — each arm resumes (tuple state op-arg), read by projection before/inside/after the inner handle
pass	crn2 (perceus resume-seam gate pin) HEAP tuple state READ by projection AND threaded UNCHANGED via the state-tuple Proj, aliased across two dispatches — the tuple-Proj must classify the heap state element as COMPOUND, not scalar
pass	cross-unit Qty host results reject at the guest-side add
pass	cross-width widening a runtime narrow int with .of (UInt16.of a UInt8) emits (total)
pass	crr1 an export returns a three-element list (reachable return = shell plus node, two cells)
pass	crr2 an export returns a pair tuple (one reachable cell)
pass	crr3 an export returns a Some-wrapped scalar (one reachable cell)
pass	crr4 an export returns a runtime-concatenated String (one reachable cell)
pass	crr5 an export returns a two-field record (one reachable cell)
pass	cru1 a bool-materialized conditional range sheds the downstream overflow guard and computes
pass	cru2 a narrow conditional whose product fits the type sheds its guard
pass	cru3 a narrow conditional whose branch-union overflows the type keeps its guard and traps
pass	cru4 an unbounded conditional branch keeps the guard and traps on real overflow
pass	cs1 a COLLATZ-step arm — even states halve, odd states triple-plus-one, low digits of four reads trace the orbit
pass	csc1 a STRING comparison threaded through a const recursion folds (const_eval scalar-ordering arm)
pass	csc2 a BOOL comparison threaded through a const recursion folds (const_eval scalar-ordering arm)
pass	csd1 two occurrences of one constant list literal — branch-selected use, length reads, and runtime equality across the pair
pass	csd2 a fifty-frame recursion re-evaluating a constant list literal each frame reclaims to zero
pass	csg1 a binding SHARED into a sum ctor survives a payload-dropping fold and a later re-read — the CSG over-drop guard
pass	csg2 a binding shared TWICE into one node, both children recursed, re-read after — the double-share fold guard
pass	csm1 a mixed-operator and (< with <=) subsumes to the tighter inclusive bound
pass	csm2 a mixed-operator or (<= with <) subsumes to the looser inclusive bound
pass	csm3 a mixed-operator lower-bound or (> with >=) subsumes to the looser bound
pass	csm4 the mixed-operator subsumption keeps a trapping operand's trap
pass	cso1 a three-way (Ordering.of) MATCHED in a const recursion folds (const_eval Compare arm + nullary-Ordering match)
pass	cso2 a COMPOUND three-way (Ordering.of) in a const recursion folds (const_eval Compare arm, compound path)
pass	cspr1 construction SPREAD splices a runtime list into a literal (should-work: the value twin of the rest pattern)
pass	cst1 a CURSOR-STACK state — the tuple (buf,top) pushes by List.update-or-append at the cursor and pops by decrement, stale slots above the cursor are overwritten, the over-pop answers a sentinel
pass	ct01 bare runtime Int64.of over a UInt64 param-derived value (isolated T.of face)
pass	ct1 the LOOP BOUND is itself a draw — a first dispatch sizes the walk, the walk then draws that many times
pass	ctd1 two occurrences of one constant tuple — branch-selected use, projections, and runtime equality across the pair
pass	ctd2 a fifty-frame recursion re-evaluating a constant record each frame reclaims to zero
pass	ctt1 RATIONAL handler state threads exact arithmetic across dispatches
pass	ctt2 BIGINT handler state doubles past i64 across dispatches
pass	ctt3 CHAR handler state advances by code point across dispatches
pass	ctt4 BYTES-ROPE handler state grows by concat across dispatches
pass	ctt5 SYMBOL handler state swaps between interned symbols
pass	cu01 runtime NON-FINAL dependent-size utf8 segment decodes with a trailing field
pass	cu02 runtime FINAL dependent-size utf8 segment decodes
pass	cu03 CONTROL runtime non-final dependent-size BYTES segment
pass	cv1 an inner arm resumes with a CHAIN of two outer ops — O.b of O.a evaluated inside the arm, probe pins the double advance
pass	cv2 TWO asks each running the in-arm chain — the second chain starts where the first left the outer thread
pass	cv3 the in-arm chain routes through a PURE fn mid-hop — O.b of dbl of O.a, the pure call must not detach the thread
pass	cvp1 a variant host RESULT with a LIST payload lifts and is measured
pass	cvp2 a variant host RESULT with a RECORD payload lifts and projects
pass	cwl1 a CIPHER WHEEL that slips every third letter — encoding adds the offset mod twenty-six and every THIRD encode advances the wheel one notch (a nine-tagged row counting the slip), other rows carry the stroke's residue tag, the read packs offset strokes and slips, and the seed's starting offset wraps one alphabet where the other doesn't so the ciphertext rows share nothing
pass	cx1 a COUNTDOWN arm exhausts mid-sequence — positive states pass through, the floor returns a sentinel-free constant
pass	cx5b CONTROL: pure closure through the same recursive HOF
pass	cx5c LET-BOUND effectful closure invoked directly (no HOF)
pass	cx5d an effect-performing closure through a non-recursive one-shot helper fn-arg folds via the escaped-closure recovery
pass	cx5d effectful closure through a NON-recursive helper that calls it once
pass	cx6 an effect-performing closure through TWO nested one-shot helper hops folds via the escaped-closure recovery
pass	cx7 a closure performing TWO different effects passed as a fn argument
pass	cx8 an effect-performing closure RETURNED from a helper then invoked
pass	cx9a a performing closure through THREE nested one-shot helper hops folds
pass	cx9b the one-shot helper carries an EXTRA scalar arg alongside the closure
pass	cx9c TWO different performing closures through the same helper on separate calls
pass	cx9d sequential composition — one hop's answer feeds a second hop's closure
pass	cy1 a CYCLE detector — a bitmask accumulator of seen residues stops the walk on the first repeat
pass	cyc1 a BICYCLE cadence over four gear ratios — shift moves the gear clamped to the range answering the landed ratio's tens digit, pedal answers rpm times the ratio over a hundred accumulating distance, the big downshift clamps BOTH runs to the same bottom gear so the tails converge while the opening gears diverge, and the final row reads the odometer
pass	cz1 COLLATZ-driven dispatch counting — the walk observes each step, the counter state tallies data-dependent iteration counts
pass	d1 two-arm sum match where the FIRST ctor is disc 0 — the eqz leg of the branchless select, both orders
pass	d2 three None-producing built-in reads in one expression — List.at in and out of range plus Map.lookup hit and miss
pass	d3 Option wrapping a list of records (3 levels, no tuples) destructured to scalars
pass	d4 Option wrapping a bare list (2 levels) destructured to scalars
pass	d5 a USER sum with a list payload destructured to scalars deforests completely
pass	d6 CONTROL: the Option payload bound WHOLE (not destructured) then consumed by List.len reclaims
pass	da1 THREE draws inside one list literal passed to a helper — left-to-right element order, the post-call draw sees all three
pass	da2 draws as MAP-literal keys and values — two entries built from three sequential draws
pass	da3 draws as SET-literal elements — n=7 collides the first draw with the fixed element, n=6 collides the second
pass	day1 a DAY-ROLLOVER ledger — transactions accumulate in the day buffer, endday posts the net to the balance answers day and net and clears, and the second day starts from a clean buffer
pass	dbf1 a FOREIGN LEVY IN EACH REPLAY'S ANSWER ARGUMENT — both sequential resumes levy the outer handler while building their answers so the outer counter advances TWICE per dispatch once per replay, the surviving second replay carries the SECOND levy's value proving the discarded replay's levy still fired, and the seed shifts both levies together
pass	dbn1 a DEBOUNCE gate — emits fire only when the gap since the last fire is met, suppressed emits stash their value in the pending slot, and flush releases whatever the last suppression left
pass	dbr1 SEQUENTIAL DOUBLE RESUME in one arm — the arm resumes once discarding the outcome then resumes AGAIN with a shifted answer and state, a single-perform body so each resume replays just the tail, probing whether continuations are one-shot (second resume must be a defined error) or multi-shot (the second replay's value wins)
pass	dbr2 DOUBLE RESUME over a TWO-PERFORM body — each of the two dispatches replays its tail twice so the body runs through FOUR leaf executions, the multi-shot second-replay-wins rule composes multiplicatively (the surviving answer threads the second replay at BOTH depths), and the seed shifts the surviving pair together
pass	dbr3 BOTH REPLAY VALUES CONSUMED — the arm SUMS two sequential resumes so neither replay is discarded, the answer is the two replays' body values added (each replay runs the whole tail with its own answer), and the multi-shot second-replay-wins rule from the discard shape gives way to both-contribute when the arm keeps both
pass	dbr4 CONDITIONAL DOUBLE REPLAY — a positive state replays the tail twice with the second replay winning while a zero state resumes once, the seed places the single-replay frame at different depths so one run double-replays at BOTH dispatches and the other threads a single first dispatch into a doubled second, mixing the one-shot and multi-shot paths in a single machine
pass	dbr5 TRIPLE SEQUENTIAL REPLAY — three resumes in one do with the first two outcomes discarded, the LAST replay's value wins extending the second-wins law to n-th-wins, each replay shifting the answer by ten so a lowering that stops at two replays or returns any earlier replay's value is off by a fixed decade
pass	dbr6 the two replays thread DIFFERENT STATES — the discarded first replay advances the state additively while the surviving second replay DOUBLES it, the next dispatch answers from whichever state its replay actually threaded, and a lowering that reuses the first replay's state thread for the second shifts the later dispatch's whole answer
pass	dbt1 a DEBT AMORTIZATION schedule — pay accrues truncating interest at the seed-shaped rate first and the remainder reduces principal, answering the interest slice; left reads the balance; the higher rate leaks interest on every payment so the principals drift apart payment by payment
pass	dc01 Float64 const-param countdown trap surfaces CDZ0304
pass	dc02 tuple const-param countdown trap surfaces CDZ0304
pass	dc03 record const-param countdown trap surfaces CDZ0304
pass	dc04 (Option Int64) const-param recursion on the VALUE path folds to 99 under Ast.encode
pass	dc06 Map insert+lookup inside a const fn folds under Ast.encode
pass	dc1 a DEEP left-leaning concat chain — ten single-element concats build an 11-deep rope, random-access reads stay exact
pass	dc1 a THREE-hop def relay under ONE handler — each def draws then calls the next, weights pin the depth order
pass	dc2 the relay call's ARGUMENT is a draw — the callee draws again and combines, argument-before-body order pinned
pass	dc3 the MIDDLE relay hop is chosen by a draw — the branch decides between a two-draw and a one-draw callee, a tail draw pins the total
pass	dcw1 a generic ctor's list-of-TUPLE payload destructures to the leaf through the carried type
pass	dcw2 a generic ctor's list-of-RECORD payload destructures to the field through the carried type
pass	dcw3 a generic ctor's list-of-LIST payload destructures to the leaf through two vec walks
pass	dcy1 a FREQUENCY-DECAY tracker — every observe first HALVES all counts dropping the zeroed rows, then bumps the observed key by four; the whole-map transform and the single-key update compose in one arm
pass	dd1 a FIVE-level nested delegation chain threads every level's state per inner dispatch
pass	dd1b consecutive do-DEF draws — both binders hold their reads, the tail draw sees the doubled-twice state
pass	dd1d a bare DISCARDED draw before a let-bound draw — the discard advances the state the binder reads
pass	dd2 a delegation that SKIPS intervening handler levels routes to the right outer handler
pass	dd2 a do-DEF bound to a whole nested SHADOW handle — the def holds the inner region's value, the tail draw reads outer
pass	dd3 two inner effects FAN IN to one shared outer counter in body order
pass	dd5 a do-def BINDS a whole cross-effect handle region — the region's seed draws from the outer thread
pass	ddp1 a FIRST-TIME dedup stream over a Map of seen-counts — a first observation echoes the value, a repeat answers the negated repeat count, and the seed COLLIDES a computed key with a literal so the two runs disagree on which draws are repeats at all
pass	declaring a unit with a conversion conflicting with a built-in is an error
pass	decode of a Float tag with a non-finite (NaN) bit pattern yields the error case
pass	decode of a List tag whose element count exceeds the bytes present yields the error case
pass	decode of a List tag with an enormous element count does not attempt a giant build — yields the error case
pass	decode of a Str tag with an oversized length yields the error case
pass	decode of a truncated Float tag yields the error case, not a trap
pass	decode of an Int tag marked negative with a zero-length magnitude is non-canonical (no negative zero)
pass	decode of an Int tag whose magnitude is shorter than its declared length yields the error case
pass	decode of an Int tag with a leading-zero magnitude byte is non-canonical and yields the error case
pass	decode of an Int tag with a maximal magnitude-length yields the error case without overflowing
pass	decode of an Int tag with a truncated magnitude-length yields the error case
pass	decode of an empty byte string (no tag) yields the error case
pass	decode of an unknown tag byte yields the error case
pass	decoding a four-byte sequence for a code point above U+10FFFF yields none
pass	decoding a lone continuation byte yields none
pass	decoding a runtime ROPE Bytes as UTF-8 flattens before validating (well-formed)
pass	decoding a runtime ROPE Bytes that is ill-formed yields none
pass	decoding a surrogate code point encoded as UTF-8 yields none
pass	decoding a truncated multi-byte sequence yields none
pass	decoding an EMPTY byte sequence yields Some of the empty string
pass	decoding an encoded string round-trips to the same bytes (the inverse direction)
pass	decoding an overlong UTF-8 encoding yields none
pass	decoding bytes that are not a canonical AST yields the error case, not a trap
pass	decoding canonical bytes followed by a trailing byte yields the error case
pass	decoding ill-formed UTF-8 bytes yields none, not a trap
pass	decoding ill-formed bytes through a helper takes the None arm
pass	decoding well-formed UTF-8 bytes yields the string
pass	deeply nested pattern matching
pass	demoting a Float64 to Float32 rounds to the nearest binary32
pass	deq1 a DES priority queue built by sorted insert then drained is value-exact and RECLAIMS its rebuilt spine prefixes (was a superlinear leak)
pass	df1 a MONOMORPHIC user sum mirroring Option's shape (heap list payload) deforests completely
pass	df2 a GENERIC user sum at a heap list payload leaks its shells like Option (the one non-fusing cell)
pass	df3 the SAME generic sum at a scalar payload deforests completely
pass	df4 prelude Option at a scalar payload deforests completely (the leak needs the heap payload)
pass	dfr1 a generic sum's TUPLE payload destructured to scalars leaks its shells
pass	dfr2 a generic sum's RECORD payload destructured to a field leaks its shells
pass	dfr3d a TWO-payload generic ctor's heap field bound WHOLE and measured reclaims cleanly
pass	dg1 a DIGIT-extractor arm — each dispatch peels the low digit and floors the state by ten, three peels reverse the tail
pass	dgn1 digit-peel of a NEGATIVE state — truncated division and dividend-sign remainder agree through the thread, three negative digits
pass	dgr1 a DIGITAL-ROOT arm — nested fixed-point recursion (repeat digit-sum until single digit) runs wholly inside each dispatch, the accumulator's low digit rides along in the answer
pass	dh1 value-eq on two ARM-BUILT lists inside a dispatch — the borrowed-handle reclaim after = must not double-drop
pass	difference DISTRIBUTES over union but is NOT associative - both facts witnessed live
pass	difference cardinality with a runtime-element subtrahend
pass	difference is NOT commutative
pass	difference over a runtime-element set removes exactly that element
pass	different runtime Bytes nested in a tuple compare unequal
pass	differing trapping operands trap by selection only under the arith hoist
pass	digit-separator literals in decimal and hex drive runtime modular arithmetic
pass	dimensional equality is decided by canonical exponent map, not written form
pass	disjunction is false exactly when both operands are false
pass	disjunction shields a trapping right operand when the left is true
pass	distinct declarations fold to distinct contract-ids
pass	distinct large floats are not equal
pass	distinct names bound to the same string key the same map entry
pass	distinct seeds produce distinct generated values (the stream covers, it is not constant)
pass	distinct signatures sharing a core valtype shape dispatch distinct bodies
pass	distinct-magnitude runtime quantities in a Set stay separate
pass	distinct-sig byte-rope closure alongside a plain export — the closure
pass	distinct-sig byte-rope closure alongside a plain export — the plain
pass	distinct-sig byte-rope closures — the Bool→Bytes one
pass	distinct-sig byte-rope closures — the Int64→Bytes one
pass	distinct-sig collection result — the Bool→List closure
pass	distinct-sig collection result — the Int64→List closure
pass	distinct-sig compound result — the Bool→(Tuple Bool Int64) closure
pass	distinct-sig compound result — the Int64→(Tuple Int64 Int64) closure
pass	distinct-sig round-trip: TWO byte-rope consumers of different signatures — the Bool one
pass	distinct-sig round-trip: TWO byte-rope consumers of different signatures — the Int64 one
pass	distinct-sig round-trip: TWO collection consumers of different sigs — the List
pass	distinct-sig round-trip: TWO collection consumers of different sigs — the Map
pass	distinct-sig round-trip: TWO compound consumers of different sigs — the record one
pass	distinct-sig round-trip: TWO compound consumers of different sigs — the tuple one
pass	distinct-sig round-trip: TWO compound-arg closures of different sigs — the Record-arg one
pass	distinct-sig round-trip: TWO compound-arg closures of different sigs — the Tuple-arg one
pass	distinct-sig round-trip: a List consumer + a compound consumer of another sig — the compound
pass	distinct-sig round-trip: a List consumer + a compound consumer of another sig — the list
pass	distinct-sig round-trip: a List consumer + a scalar consumer of another sig — the list
pass	distinct-sig round-trip: a List consumer + a scalar consumer of another sig — the scalar
pass	distinct-sig round-trip: a SUM-result consumer + a COLLECTION-result consumer — the collection one
pass	distinct-sig round-trip: a SUM-result consumer + a COLLECTION-result consumer — the sum one
pass	distinct-sig round-trip: a byte-rope consumer + a scalar consumer of another sig — the byte-rope one
pass	distinct-sig round-trip: a byte-rope consumer + a scalar consumer of another sig — the scalar one
pass	distinct-sig round-trip: a compound consumer + a byte-rope consumer of different sigs — the byte-rope
pass	distinct-sig round-trip: a compound consumer + a scalar consumer of another sig — the compound
pass	distinct-sig round-trip: a compound consumer + a scalar consumer of another sig — the scalar
pass	distinct-sig round-trip: a compound-arg closure + a scalar-arg closure of another sig — the compound-arg one
pass	distinct-sig round-trip: a compound-arg closure + a scalar-arg closure of another sig — the scalar-arg one
pass	distinct-sig round-trip: a higher-order closure + a scalar closure of another sig — the higher-order one
pass	distinct-sig round-trip: a higher-order closure-typed arg on one group + a scalar closure on another
pass	distinct-sig: a String closure + a Bytes closure of different signatures — the String one
pass	distinct-sig: a byte-rope closure coexists with a SCALAR closure — the byte-rope one
pass	distinct-sig: a byte-rope closure coexists with a SCALAR closure — the scalar one
pass	distinct-sig: a collection + a compound + a byte-rope + a scalar group all coexist — the collection
pass	distinct-sig: a collection + a compound + a byte-rope + a scalar group — the byte-rope
pass	distinct-sig: a collection + a compound + a byte-rope + a scalar group — the compound
pass	distinct-sig: a collection + a compound + a byte-rope + a scalar group — the scalar
pass	distinct-sig: a compound group + a byte-rope group + a scalar group — the byte-rope
pass	distinct-sig: a compound group + a byte-rope group + a scalar group — the compound
pass	distinct-sig: a compound group + a byte-rope group + a scalar group — the scalar
pass	distinct-signature capturing closures alongside a parameterized plain export
pass	distinct-signature capturing closures alongside a parameterized plain export — the plain runs
pass	distinct-signature capturing producers
pass	distinct-signature closures alongside a plain export — the Int64->Bool closure runs
pass	distinct-signature closures alongside a plain export — the Int64->Int64 closure runs
pass	distinct-signature closures alongside a plain export — the plain export runs
pass	divergent multi-shot STRING lineages — each branch observes its own byte-length
pass	divergent multi-shot states carry HEAP lineages — each branch grows its own list
pass	dividing a quantity by a bare number keeps its dimension
pass	dividing a rational by a negative rational normalizes the quotient's sign
pass	dividing an unusual-width minimum by -1 overflows its declared range and is rejected CDZ0304
pass	dividing quantities divides their dimensions
pass	dividing two same-dimension quantities cancels to a dimensionless number
pass	division by negative one negates but still overflows at the minimum
pass	division by one keeps a boundary-crossing runtime operand
pass	division by zero traps
pass	division of an integer and a float does not silently promote
pass	division of the minimum integer by -1 overflows and traps
pass	dl1 TEN chained lets each drawing once — position weights 1..10 pin every binder to its draw
pass	dlp1 a binding CONSUMED into a later sibling's init (fresh-alloc child, branch-varying) reclaims clean — the #5424 D1 fence
pass	dlt1 a DELTA encoder and decoder sharing ONE previous-value slot — enc answers the difference storing the raw value, dec answers the reconstruction storing it, interleaving them CROSS-TALKS through the shared slot by design, and after the seed washes out of the second row the tails converge exactly
pass	dm1 a FOUR-level mixed destructure (option-list-tuple-record) reduces to scalars
pass	dmp1 a GENERIC multi-payload ctor's nested-LIST field destructures to vec-get, not arr-get
pass	dmp2 a multi-payload ctor's RECORD field holding a list resolves the list read to vec-get
pass	dmp3 a multi-payload ctor's TUPLE field whose first element is a list resolves to vec-get
pass	dn1 BRANCH-BUILT deep scrutinee — construction under an if inside the pattern spine, through the hop
pass	dn1 a FIVE-deep same-effect shadow tower — one draw per level plus a doubled draw at the innermost
pass	dn2 nested-switch DEFAULT arm — multi-depth patterns + wildcard through the cadenza hop
pass	dn2b an abort under FOUR resumptive frames — the unwind abandons all four pending sums (extends the three-frame pin)
pass	dn3 an ALTERNATING A/B/A/B tower where both effects share the op NAME next — each perform homes to its effect's innermost handler
pass	dn4 SKIP-LEVEL performs from the innermost region — A's draws cross the B and C frames to the outermost handler twice
pass	dn5 a tuple built INSIDE the inner region from BOTH effects' draws, destructured OUTSIDE — data crosses the region boundary
pass	do-discarded outer performs inside a HELPER under an inner handle thread across the level
pass	do1 ONE draw consumed by BOTH branches of an if with different weights — the binder is read exactly once per run
pass	double negation of a comparison is the bare comparison
pass	double negation of a runtime boolean is the identity
pass	doubling a subnormal crosses UP into the minimum normal exactly
pass	dpt1 divisibility by two agrees with the true modulo across signs and Int64.min
pass	dpt2 unsigned divisibility by a power of two agrees with the true modulo
pass	dqe1 FLAT tuple dual-use (projections + runtime equality) reclaims clean — the control the nested cells contrast
pass	dqe10 an eq'd nested operand ESCAPING whole through the branch arm leaks both sides today (escapee must survive the coming end-of-scope drop)
pass	dqe11 an eq'd nested operand whose COMPONENT escapes through the branch arm leaks both sides today (partial escape)
pass	dqe12 eq-only nested operands confined to a callee scope (nothing escapes) reclaim clean
pass	dqe13 the dqe4 shape with MATCH-destructure instead of tuple projection reclaims clean (the leak is projection-specific)
pass	dqe14 a RECORD with a heap (list) field dual-used by field-read + equality reclaims clean (named-field access releases its dup)
pass	dqe15 an Option with a heap (list) payload dual-used by match-extract + equality reclaims clean
pass	dqe16 a walker-conditioned branch escaping a NON-operand heap binding leaks the escapee's tree once
pass	dqe17 a walker-conditioned escape arm left UNTAKEN (operands unequal) reclaims clean — the dup is minted on the taken path only
pass	dqe18 a SCALAR-conditioned branch escaping a heap value reclaims clean — leg-2 requires the walker condition
pass	dqe19 leg-1 cross-scope: projection + walker on a RETURNED binding leaks its tree in the caller
pass	dqe2 nested runtime tuples under equality ALONE (two walks, no projections) reclaim clean
pass	dqe3 nested runtime tuples with an UNEQUAL leaf under equality reclaim clean
pass	dqe4 ONE nested operand dual-used (deep projection + equality) reclaims clean — borrow-only projected binder mints no spurious dup
pass	dqe5 BOTH nested operands dual-used (projections + equality) reclaim clean — borrow-only projected binders mint no spurious dup
pass	dqe6 a nested operand dual-used by projection + ORDERING walk reclaims clean — borrow-only projected binder mints no spurious dup
pass	dqe7 a nested operand dual-used by projection + CHAMP-key descent (insert one, look up by the equal twin) leaks its tree
pass	dqe8 a nested operand dual-used by projection + Set membership descent leaks its tree
pass	dqe9 TWO walkers (equality AND ordering) on the same nested operands with no projection reclaim clean
pass	draining the event queue yields entries in time-order with FIFO same-time ties
pass	drb1 a DRAWBRIDGE over a tidal channel — boat LET-BINDS the low-tide boolean ONCE and consumes it in TWO SEPARATE ifs, one selecting the answer's hundreds digit and one selecting the next-state tuple, a low-tide boat slips under advancing the tide by one and counting the crossing while a high-tide boat waits three tide steps crossing nothing, log reads tide and crossings without advancing, and the seed sets the starting tide so the runs disagree on which boats slip under
pass	drm1 a DRUM sequencer over sixteenth-note steps — each hit answers the firing drums as a bitmask (kick on quarters, snare on the backbeats, hat on every seed-shaped division), cnt counts non-silent steps, and the halved hat rate turns every other row SILENT while the kick and snare rows coincide exactly
pass	dropping a list pushed FROM a survivor must not free the shared spine
pass	dropping a map derived by persistent insert must not free nodes shared with the survivor
pass	dropping a rope built OVER a survivor must not free the shared child node
pass	dropping a set derived by insert must not free members shared with the survivor
pass	dropping an absent field from a record is rejected
pass	dropping fields from a record leaves the remaining fields
pass	drs1 Result Ok nested-payload destructure (the Option-leak sibling)
pass	drs2 Option-in-Option nested destructure to scalars
pass	drs3 an Option with a STRING payload destructured by a whole-binding arm deforests completely
pass	ds1 FOUR discarded draws in a do-chain before the kept one — every discarded dispatch still advances the thread
pass	ds2 the DISCARDED statement is itself an op-result chain — both hops advance the thread even though the value dies
pass	ds3 a DISCARDED handle expression whose interior draws from the outer thread — the frame opens, draws, closes, and the value dies
pass	ds4 a DISCARDED if whose arms draw DIFFERENT counts — the taken branch's advances survive the discard
pass	dsc1 a DISCARDED DIVISION NEVER TRAPS — the do-interior quotient is computed for no one and the zero divisor that would trap in value position is ELIDED with the rest of the dead expression, both seeds returning the constant tail unchanged, pinning that a pure trapping form in discard position is droppable (the CDZ0307 contract: a valueless pure form has no effect and a trap is not an effect)
pass	dst2 THREE levels: list of tuples of records, no Option wrapper
pass	dst5 a USER sum with a list payload destructured to scalars
pass	dst6 CONTROL: Option payload bound WHOLE (not destructured) then consumed by List.len
pass	dt2 THREE levels with NO Option wrapper (list of tuples of records) destructures and reclaims
pass	dth1 a DEAD-MAN'S switch — beat resets the miss counter answering the uptime tick, poll counts a miss answering it until the seed-shaped threshold LATCHES the alarm, and once latched EVERY answer is -9 forever (beats cannot clear it); the lower threshold latches six rows early and the long -9 tail pins the latch as absorbing
pass	du1 a DURATION newtype state accumulates seconds across dispatches — the arm unwraps, adds at nanosecond scale, rewraps
pass	du2 UInt64 overflow at the top of a DURATION state traps at RUNTIME — the second dispatch crosses MAX, n=0 threads unchanged
pass	dv1 truncated division and dividend-sign modulo over DRAWS — negative dividends exercise the toward-zero rule through dispatch
pass	dv2 the DIVISOR comes from the arm with a state-dependent sign — quotient and remainder track the alternating divisor exactly
pass	dv3 division by -1 of a RUNTIME draw — negation across the full non-MIN range, the MIN draw guarded to its own branch
pass	dvf1 integer Div and Rem folds agree with their runtime twins including negative truncation
pass	dw1 a list literal mixing an Int32-annotated element with a wider-inferred literal unifies to (List Int32) — every element renders at the unified element width, uniform across backends (fuzzer cdz-smith differential: rust once emitted a heterogeneous vec![i32,i64])
pass	dxb1 modular exponentiation with the EXPONENT BITS drawn from the handler — the body's square-and-multiply loop consumes the bit stream LSB-first while the arm peels the threaded exponent
pass	dzb1 a const divide-by-zero in an UNTAKEN if-branch does NOT compile-error; the taken branch returns its value
pass	dzb2 the SAME divide-by-zero branch traps at RUNTIME when the condition takes it
pass	dzb3 a const divide-by-zero in a MATCH arm traps at runtime, not at compile (the match twin)
pass	dzb4 a STATICALLY-UNCONDITIONAL const divide-by-zero is STILL fail-loud CDZ0304 (the carve-out holds)
pass	dzb5 a const REMAINDER-by-zero in a conditional branch demotes to the SAME div-by-zero kind (the % twin)
pass	dzc1 a conditionally-reached out-of-range shift demotes to a runtime trap (untaken path answers)
pass	dzc2 a conditionally-reached Int64.min/-1 overflow demotes likewise
pass	dzc3 a Rem-by-zero in an untaken MATCH arm demotes likewise
pass	dzw1 an EXPLICIT user (trap …) in a runtime branch does NOT warn CDZ0309 (only const-fold-origin traps warn)
pass	e10 a heap ctor DISCARDED as a do-statement elides its arg trap — the discard-spelling boundary
pass	eab1 a lifted String param CAPTURED by a closure is admitted as borrowed and reclaims
pass	eab1 an ABORTIVE arm at a NON-TAIL position the tail-resumptive fold cannot lift (under an effectful resumptive sibling `(E.other)`, so the hoist cannot raise the `if` to a branch tail) in a WHOLE-DEF-BODY handle lowers to a NON-LOCAL EXIT (the fold emits `Core::HandleAbort`, the wasm backend a bare return): the taken abort ABANDONS the pending `+` and yields the arm value, while the untaken branch adds normally. `(E.other)`'s effect still runs before the abort (the abort abandons the pending continuation, not an already-executed effect). This formerly declined CDZ0900 (v-effects HandleAbort CASE-1, co-verified v-wasm-opt emit).
pass	eab2 a lifted String param as an EFFECT-OP argument is admitted and reclaims
todo	eab3 a lifted Bytes param sliced through Option.expect is admitted and reclaims
pass	each definition in a module registers a reachable export field
pass	each literal kind quotes to its own single Ast leaf that escapes and renders
pass	each multi-shot branch OBSERVES its own divergent state via a trailing peek
pass	each multi-shot branch observes its own heap-lineage length
pass	each parameter of a wide six-param signature resolves to its own binder
pass	each recursion level installs its own handle around a def-bound perform
todo	eb1 a BigInt entry param in beyond-i64 arithmetic
pass	ecb1 an empty-LIST if-branch joins with a built list (tri-target)
pass	ecb2 an empty-MAP if-branch joins with a built map (tri-target)
pass	ecb3 an empty-SET if-branch joins with a built set (tri-target)
pass	ecb4 an empty-list MATCH arm joins with the bound rest (the map-rest sibling on lists)
pass	ecl1 a let-bound hop answer used TWICE folds
pass	ecl2 a let CHAIN through the hop answer folds
pass	ecl3 TWO let-bound hop answers fold
pass	ecl5 a let INSIDE the performing closure body folds
pass	ed1 TWO defs each install their OWN handler for one effect — main calls both, seeds and arms fully independent
pass	ed2 a handled def CALLED from inside another def's handle — the callee's region shadows the caller's mid-body
pass	ed3 a RECURSIVE def that handles per frame, called from a handled main — the base case draws from the deepest frame's handler
pass	ed4 one shared performing helper called under TWO different live handlers — each call homes to its caller's region
pass	effect state threads across a successful `?`
pass	egy1 EGYPTIAN multiplication — each step tests the multiplier's low bit (accumulating the doubled multiplicand when odd) answering odd*100 plus the running product's low digits, acc reads the exact product, and the seeds' bit patterns (all-ones versus alternating) fire the accumulation on different steps
pass	eh1 handler STATE-THREADING across a runtime-count recursion — through the cadenza hop
todo	el1 a List entry param summed by an indexed walk
pass	el2 TWO List entry params measure independently
pass	el3 an EMPTY List entry param has length zero
pass	el4 a List entry param's element read by a non-recursive List.at
pass	el5 a List-of-UInt8 entry param measures and reads an element
pass	el6 a List-of-Int32 entry param measures and reads an element
pass	el7 a List-of-Float64 entry param measures and compares an element
todo	el8 a nested List-of-List entry param measures both levels
pass	elc1 a local closure captures the lifted List entry param and is invoked twice
todo	elc2 a Map keyed by the lifted List entry param answers its value on lookup
todo	elc4 List.concat consumes two lifted List entry params into one measured list
pass	elc5 the capturing closure crosses a higher-order call boundary and answers from both invocations
pass	elc6 a MIXED scalar+non-scalar bare export param list still gates B2 off (opt-level equivalent)
pass	elc7 TWO closures capturing the same lifted List entry param keep B2 gated off (opt-level equivalent)
pass	ele1 the lifted List entry param is read in a handle BODY alongside a dispatch
pass	ele2 the lifted List entry param is read inside a handler ARM feeding the resume value
pass	ele3 the lifted List entry param SEEDS the handler state
todo	eln1 an EMPTY nested-list entry param has outer length zero
todo	eln2 a nested-list entry param whose FIRST inner list is empty reads a zero inner length
todo	eln3 a THREE-level nested-list entry param reads a leaf through two descents
pass	ema1 an INTEGER EMA state — each dispatch blends (3*ema + 100*v)/4 at 100x scale, convergence toward the fed value from both sides
pass	encoding a decoded string round-trips to the same bytes
pass	encoding and decoding a constructor-built AST round-trips to an equal value
pass	encoding and decoding a constructor-built compound AST round-trips
pass	encoding and decoding an Ast.Bool round-trips to an equal value
pass	encoding and decoding an Ast.Float round-trips to an equal value
pass	encoding and decoding an Ast.Str round-trips to an equal value
pass	end-to-end: a property run finds a failing input, then the shrinker converges to the minimal failing value
pass	eo1 an Option entry param delivers its Some payload
pass	eo2 an Option entry param takes the None arm
pass	eo3 an Option param BETWEEN scalars keeps positions
pass	eoc1 an Option entry param captured by a local closure applied twice
pass	eoc2 an Option entry param relayed to a helper def and unwrapped there (owned sum, unlike the borrowed list)
pass	eoc3 an Option entry param matched twice reclaims (the repeat-unwrap family needs a heap payload)
pass	eoi1 TWO Option entry params thread the flattened-leaf cursor independently
pass	eoi2 a String (mem-leaf) param and an Option (sum) param interleave in one signature
pass	eoi3 an Option<Int8> entry param's narrow Some payload widens correctly
pass	eop1 an Option entry param read inside a handle body composes with the fold
todo	eop2 an Option-of-list entry param measures its Some payload
todo	ep1 TWO String entry params concat in declaration order
pass	ep2 a String param BETWEEN two scalars keeps positional correctness
pass	ep3 one String param used TWICE (byte-len and content compare)
pass	ep4 an EMPTY String entry arg has byte-len zero (the ptr,0 edge)
pass	ep5 a Bytes and a String param in ONE signature
pass	ep6 a String entry param passed THROUGH to a helper
pass	eq1 structural EQUALITY of records built from two draws — a parity-dependent stride decides whether the fields line up
pass	eqmr1 a map-REST pattern inside a QUOTED match reifies OPEN and matches like its direct twin
pass	eqmr2 a record-REST pattern inside a QUOTED match reifies OPEN (the #6896 record twin)
pass	equality across incompatible dimensions is a compile-time error
pass	equality across types is rejected regardless of operand order
pass	equality against zero answers by value in both operand orders
pass	equality curries — applied to one operand it is a first-class predicate
pass	equality of ABSTRACT-type values is rejected — opacity covers comparability
pass	equality of a record with a subset of another's fields is a type error
pass	equality of a runtime boolean against the false literal negates it
pass	equality of a runtime boolean against the true literal is the boolean
pass	equality of an integer and a boolean is a type error
pass	equality of an integer and a float is rejected, not silently promoted
pass	equality of an integer and a string is a type error
pass	equality of two records with different field sets is a type error
pass	equality of two tuples of different arity is a type error
todo	equality on a TUPLE containing a closure is rejected CDZ0216 (NotEquatable — the fn leaf poisons the compound walk)
pass	equality on an unusual signed width holds for two equal sign-extended values
pass	equality over a Rational carried in a SUM payload respects normalization
pass	equality over a compound mixing a float and a Bytes leaf walks both
pass	equality over a partly-un-built sum with a free Err type compiles on every backend
pass	equality over a runtime float in a SUM payload compares by the float leaf
pass	equality over an int/float mix gets the retype fix too, not just ordering
pass	equality with zero selects eqz and computes correctly for both operand orders
todo	er1 a Rational entry param in exact arithmetic
todo	erp1 a Result entry param delivers its Ok payload
todo	erp2 a Result entry param with two scalar payloads declines pending the Result lift
pass	esc1 an ESCROW protocol — hold moves funds from balance to escrow only when covered, rollback returns the whole escrow to balance, and the over-balance hold bounces without touching either slot
pass	eva1 eval of a hand-built Ast.List reconstructs and executes it identically to a quoted form
pass	eva2 eval of a nested quoted form reconstructs the compound and folds
pass	eval does not execute an Ast selected from a collection at run time
pass	eval does not execute the result of a nested eval — no runtime AST interpreter
pass	eval does not execute the result of read — even over a constant string
pass	eval does not forge a private constructor NESTED in a compound
pass	eval is optional for macros and interactive use
pass	eval materializes a heap LIST from a spliced quasiquote
pass	eval of a NAME bound to a quote is rejected — the binding hides the construction
pass	eval of a bare Ast.Name for an unbound name is the ordinary unbound-name error
pass	eval of a bare quoted integer executes it to the integer value
pass	eval of a constructor-built Ast.List with a valid operator head computes its value
pass	eval of a fully-concrete imported constructor is legal from outside
pass	eval of a partially-exported type's EXPORTED constructor works
pass	eval of a partially-exported type's WITHHELD constructor is rejected
pass	eval of a quasiquote splicing a compile-time-known value
pass	eval of a quasiquote splicing a string works through String ops
pass	eval of a quasiquote with TWO unquotes splices a bound and a computed value in one form
pass	eval of a quasiquote-built form with a byte-string unquote folds
pass	eval of a quasiquote-built form with a float unquote folds
pass	eval of a quote carrying a symbol literal is rejected CDZ0101 — the reify bail as a perf-bound tripwire
pass	eval of a quoted HUGE integer literal declines CDZ0201 — BigInt storage does not widen eval
pass	eval of a quoted List.at folds the indexing operation to its Option result
pass	eval of a quoted RECURSIVE call folds through the compile-time evaluator
pass	eval of a quoted RECURSIVE-sum construction builds the heap spine
pass	eval of a quoted String.byte-len folds the string operation
pass	eval of a quoted boolean connective short-circuits
pass	eval of a quoted boolean executes it to the boolean value
pass	eval of a quoted byte-string literal folds to the bytes value
pass	eval of a quoted conditional reduces its comparison condition first
pass	eval of a quoted conditional takes the branch its boolean literal selects
pass	eval of a quoted disjunction short-circuits to true
pass	eval of a quoted division folds through the compile-time evaluator
pass	eval of a quoted equality yields the boolean value directly
pass	eval of a quoted expression folds INSIDE a handle body beside performs
pass	eval of a quoted float executes it to the float value
pass	eval of a quoted float feeds ordinary float arithmetic
pass	eval of a quoted integer literal grounds to Int64 (BigInt is AST storage, not eval width)
pass	eval of a quoted lambda application folds
pass	eval of a quoted let-binding form folds
pass	eval of a quoted list-construction form folds to the runtime list
pass	eval of a quoted match form folds to the selected arm
pass	eval of a quoted multiplication folds through the compile-time evaluator
pass	eval of a quoted not inverts its boolean operand
pass	eval of a quoted ordering comparison drives the branch
pass	eval of a quoted ordering comparison yields the boolean value directly
pass	eval of a quoted private constructor is withheld exactly as a direct reference — no forge
pass	eval of a quoted proof-variant match cannot destructure an abstract proof outside the kernel
pass	eval of a quoted remainder folds through the compile-time evaluator
pass	eval of a quoted string executes it to the string value
pass	eval of a quoted subtraction that goes negative preserves the sign
pass	eval of a quoted theorem constructor cannot forge an abstract theorem outside the kernel
pass	eval of a quoted tuple-construction form folds to the runtime tuple
pass	eval of a template splicing a runtime VALUE folds — the working side of the boundary
pass	eval of an Ast.Float NaN node executes to the NaN value — it does NOT construct an escaping node
pass	eval of an Ast.List with a non-operator head is the ordinary application type error
pass	eval of an exported smart constructor still works — the forge fix does not over-reject
pass	eval on a RUNTIME Ast parameter gives the compile-time-AST teaching message — the requirement is compile-time VISIBILITY, not Ast-ness
pass	eval on a runtime value in a let init gives the compile-time-AST teaching message
pass	eval on a runtime value in a match scrutinee gives the compile-time-AST teaching message
pass	eval on a runtime value in an arithmetic operand gives the compile-time-AST teaching message
pass	eval on malformed AST traps
pass	eval sees through macro expansion — a macro producing a (quote …) evals as the plain form
pass	eval through a splice whose operand is itself an Ast value is rejected with CDZ0201 (Ast in a numeric position)
pass	every Ast variant deconstructs to its own arm through a qualified (. Ast Ctor) pattern
pass	every arm of a three-variant enum match is exercised by RUNTIME calls
pass	every not-a-number value is equal to every not-a-number value
pass	evs1 pushing onto the shared immortal empty-vec singleton does not corrupt it — two empty lists stay independent
pass	ex1 a draw SEEDS a mid-body handler install for a different effect — the install expression itself performs
pass	exact rational arithmetic is exact and normalized
pass	exact rational division is total and exact for a nonzero divisor
pass	expansion recurses to a fixpoint through a tag whose body is another tagged template
pass	expect makes a checked-arithmetic result trap on overflow
pass	expect on a RUNTIME-absent optional traps with the canonical unreachable kind
pass	expect on an overflowing checked add traps
todo	expect traps on the absent case of a runtime optional
pass	expect traps on the err case of a RUNTIME result
pass	expect unwraps the ok case of a runtime result
pass	expect unwraps the present case of a runtime optional
pass	exponent-notation floats drive runtime arithmetic with a negative exponent live
pass	exporting a bare TYPE handle in a single module names the type-handle export
pass	exporting an EFFECT names the category, not a value definition
pass	extending a record adds a new field
pass	extending a record then dropping the added field returns the original
pass	extending a record with a non-#label (bare identifier) field-name operand is rejected
pass	extending a record with an already-present field is rejected
pass	extracting a high byte composes shift and mask
todo	ey1 a Symbol entry param keys a Map
pass	f32x1 the Float32 extremes (subnormal min, normal min, MAX) render shortest-f32 on every backend
pass	fa1 a FOLD-style accumulator threads through a performing recursion — acc doubles then absorbs each level's draw
pass	fa2 TWO accumulators through one performing recursion — running sum and prefix-sum-of-prefix-sums stay in step with the draws
pass	fa3 each fold level draws TWICE and mixes them asymmetrically — 10*first + second pins the intra-level draw order
pass	fa4 the accumulator IS a sum — each level's draw moves it around an A->B->C->A cycle capturing payloads on the way
pass	fa5 a TWO-effect fold — each level's step multiplies a draw from each thread, both threads advancing independently
pass	fac2 the BODY manufactures snapshot closures from op-answered values — two factories at different drawn states, both applied after the state moved on
pass	false is less than true
pass	fan1 a PUBLISH FAN-OUT — publish walks the subscriber map's enumeration and bumps EVERY value by rebuilding the whole map, a mid-run subscribe grows the fan and later publishes reach it
pass	fe1 a Float64 handler state HALVING per dispatch — three draws sum to exactly 1.75x the seed (exact binary fractions)
pass	fe2 a CLAMP-style Float64 arm (single-site resume) — below-state args are lifted to the rising floor
pass	fe3 an Int64 handler and a Float64 handler INTERLEAVED — integer ticks and exact float halving thread independently
pass	fe4 float comparisons route an integer match — the sign of the second draw picks the arm, exact identities verify inside
pass	fe5 Float64 arguments HALVED in the arm — exact binary fractions cross dispatch both directions, count folds in
pass	fe6 a Float64 TUPLE state — one slot advances additively, the other doubles, both exact across two dispatches
pass	ffs1 a FIRST-FREE-SLOT allocator over an Int64 bitmask — alloc scans for the lowest clear bit via a recursive probe answering its index, free clears a bit answering whether it was live, and the SEED pre-occupies slots so the two runs allocate around different holes
pass	fi0 a const float operation whose result is non-finite is a coded CDZ0201, not a codeless decline
pass	fi1 runtime float division by zero produces +infinity (huge, self-equal, positive)
pass	fi2 runtime float division of a negative by zero produces -infinity (self-equal)
pass	fi3 a runtime 0.0/0.0 NaN is CANONICAL: byte-equal to the literal, self-equal, Set-findable
pass	fi4 a runtime infinity-minus-infinity NaN is canonical (byte-equal to the literal)
pass	fib1 the FIBONACCI recurrence as a state transition — (a,b) becomes (b,a+b) per dispatch, five draws walk the sequence
pass	filter-via-effects — a STATEFUL predicate dispatch decides each element's survival
pass	first-match-wins holds with a duplicate string-literal arm
pass	fk1 NaN as a SET element dedups and is FINDABLE — canonical-bit float keys
pass	fk1 a -0.0 Map key misses a 0.0 probe and hits a -0.0 probe (lookup agrees with canonical-byte equality)
pass	fk2 NaN as a MAP key is FINDABLE — the map sibling of the fk1 set face
pass	fk2 canonical NaN self-contains as a Set element
pass	fk3 canonical NaN round-trips as a Map key
pass	fl1 a Float64 handler state through a two-site arm (threshold on the magnitude)
pass	float <= is NOT (< or =): they diverge on NaN because canonical-byte equality says nan = nan
pass	float >= is NOT (> or =): the same NaN divergence mirrors on the greater-or-equal side
pass	float division rounds under the fixed mode
pass	float division that does not divide evenly rounds to nearest
pass	float double negation round-trips signed zero — Num.neg of Num.neg 0.0 is +0.0, not -0.0
pass	float multiplication uses the arithmetic operator
pass	float subtraction uses the arithmetic operator
pass	float-first division of a mixed int/float pair does not silently promote
pass	float-first multiplication of a mixed int/float pair does not silently promote
pass	float-first subtraction of a mixed int/float pair does not silently promote
pass	floating-point uses the fixed rounding mode
pass	flt1 a TWO-STAGE sediment filter — pass traps the value's residue mod the seed then HALF of what remains answering the survivor, backwash resets the trap answering its contents, and the finer mesh traps MORE in stage one leaving less for stage two so the survivors differ while the last backwash CONVERGES
pass	fm1 a full-width mask on a UInt8 is a no-op (value parity)
pass	fm2 a full-width mask on a UInt16 is a no-op (value parity)
pass	fm3 a partial mask on a UInt8 still masks (not elided)
pass	fno1 a runtime NaN is self-EQUAL (canonical-byte) but self-UNORDERED under <= and >= (the intentional =/order split)
pass	fno2 runtime +/-infinity is FULLY ORDERED (only NaN is unordered, not all non-finite)
pass	four nested frames dispatched OUTERMOST-first — every outer perform escapes live inner frames
pass	four same-signature closure exports share one resource
pass	fr1 a TRIPLING Float64 thread crosses a fixed threshold — three compares catch the crossing depth, exact integer-valued floats
pass	fra1 a LOWEST-TERMS fraction state — each add cross-multiplies then renormalizes by an in-arm Euclid gcd, the n=1 seed collapses to exactly 1/1 mid-run
pass	frb1 a let-init perform's binder threads across a cross-def nested recursion (the #4511 fold, independent shape)
pass	frb2 TWO stacked let-init performs before the tail self-call fold (consecutive let-dispatch routing)
todo	frb3 a MUTUAL-SCC performer pair with a NON-TAIL re-entry declines cleanly pending the non-tail-resume increment (oracle corrected: hand-traced 2, was an untested 3)
pass	frl1 fifty frames each build and reduce a list — zero residue
pass	frl2 fifty frames each build and measure a string — zero residue
pass	frl3 fifty frames each construct and invoke a heap-capturing closure — zero residue
pass	frl4 fifty dispatches each resume a fresh heap answer — zero residue
pass	frm1 a LENGTH-PREFIXED frame protocol over a Bytes state — writers append [len,payload..] frames, the reader pops exactly one frame by its prefix and the drained read answers a sentinel
pass	frn1 a FURNACE interlock composing BOOL-RETURNING OPS under and or and not — hot answers whether the temperature clears the ignition line advancing one degree, cold answers the temperature's evenness advancing two, the and skips its right draw when hot reports low, the or skips its right draw when cold reports even, the not inverts a lone hot draw, every skip is visible in the tally's call count, and the seed's starting temperature makes one run short-circuit where the other evaluates both sides
pass	from-bytes decodes a 3-byte scalar split across BOTH seams of a 3-leaf rope
pass	from-bytes rejects a torn sequence whose invalid continuation sits in the NEXT leaf
pass	fs1 the SAME handle expression re-entered per recursion round — a FRESH region each iteration, seeds keyed by depth
pass	fs2 two SEQUENTIAL handles where the second's SEED is computed from the first's RESULT — regions chained by value
pass	fs3 THREE value-chained regions — each seed is the previous region's result, arms +1 / x2 / -5
pass	fse1 NaN handler state survives the seam — canonical equality says nan = nan across dispatches
pass	fse2 negative-zero handler state stays distinct from +0.0 across the seam (canonical bits)
pass	fse3c single grow op, literal float seed, float-mul next-state
pass	fse3e cancel arm alone — (- s s) self-subtraction in the answer
pass	fse3f FINITE computed seed (+ 1.0 0.5)
pass	fse3g INF seed via a RUNTIME param (corpus-style special passing)
pass	ftd1 many same-signature functions dispatched through a selector are value-exact (the #4754 functype-dedup witness)
pass	function arguments are evaluated left to right
pass	functions are single-arity and curried
pass	fx1 a Float64 state with a REGIME SWITCH in the arm — doubling below the threshold, halving above, three draws sum the trajectory
pass	fx2 float COMPARISON of two consecutive draws routes the branch — a negative seed FLIPS the order, a tail draw pins the doubling thread
pass	fx3 a FLOAT slot with a regime-switch beside an INT counter in one tuple state — mixed-width slots thread independently
pass	fx4 float sign TRICHOTOMY of a draw — positive scales, negative negates, exact 0.0 routes to the constant arm
pass	fx5 the float state SATURATES to infinity mid-thread — a squaring ladder crosses Float64.max, the arm's finite/inf verdict flips per dispatch
pass	fx6 NaN born in the ARM — s2−s2 is 0.0 while finite and NaN once the thread saturates; canonical equality distinguishes them per dispatch
pass	fx7 NEGATIVE ZERO through the state thread — canonical equality separates -0.0 from +0.0, and IEEE addition washes the sign out mid-thread
pass	fzk1 a FIZZBUZZ classifier with a seed-shaped first modulus — feed answers fifteen for both divisors, three for the first, five for the second, else the value itself (specials counted), and shrinking the first modulus from three to two RECLASSIFIES two of the five feeds including promoting a five-row to a fifteen-row
pass	fzk1 negative zero and positive zero are DISTINCT map keys (canonical-form key equality)
pass	fzk2 the zero-sign-keyed map escapes with BOTH entries, -0.0 in its own literal form
pass	fzk3 a runtime NaN map key is self-equal and collapses to ONE findable entry
pass	fzk4 runtime float-keyed map EQUALITY agrees with the canonical collapse
pass	fzk5 the float SPECIAL-KEY map escapes in the canonical raw-bits key order on every backend
pass	gbx1 a THREE-SPEED gearbox odometer — shift moves the gear clamped to the one-to-three range answering the landed gear, drive accrues time-times-the-gear's-speed into the odometer answering the total, and the seed's starting gear compounds through every drive so the odometers pull apart while the shift answers converge
pass	gc1 a EUCLID-step arm — each dispatch advances (a,b) one gcd step, low digits of the descent spell the trace
pass	gcd1 EUCLID with a logged trace — each remainder step performs, the accumulator sums the divisor chain, data-dependent step counts
pass	gcn1 a guard condition reads a binder bound by a record NESTED in the guard's Some pattern
pass	gd1 PURE guards over a draw-bound scrutinee — three guard tiers select on the drawn value, the trailing probe pins one advance
pass	gd2 pure guards on SUM-PAYLOAD binders — guarded and unguarded twins of each variant arm, five paths input-reachable
pass	gd3 the guard COMPARES the scrutinee draw to an earlier let-bound draw — two thread values meet in one pure predicate
pass	gd4 pure guards INSIDE the handler arm grade the live state — three dispatches cross the tier boundaries as the state climbs
pass	gd5 guards over TUPLE pattern binders — the predicate compares the tuple's own components, all three orderings reachable
pass	gdc1 an element-discarding generic (rewrap + peek) at two heap domains is correct on every backend
pass	geb1 a mask by -1 is NOT narrowing — the add guard stays and traps at MAX
pass	geb2 signed-rem range keeps the add guard — MAX + (% x 10) traps only when the remainder pushes over
pass	geb3 a small-mask range does not license subtracting Int64.min — always overflows, guard stays
pass	geb4 a 2-bit mask times 2^62 keeps the multiply guard — traps at 2 and 3, fits at 1
pass	generated FLOAT values are totally ordered (a trichotomy property over two generated floats)
pass	generated small-alphabet strings dedup in a Set by content across per-draw construction
pass	generated string keys OVERWRITE by content and the first word's final value is observable
pass	generic user-sum values dedupe as set elements and key a map by rope-payload content
pass	gex1 a sum variant covered ONLY by a guarded arm is non-exhaustive (rejected CDZ0210)
pass	gex2 a guarded arm plus an unguarded fall-through for the same variant IS exhaustive (guard-first ordering honored)
pass	gh1 a guard condition builds and consumes a heap temporary (guard taken)
pass	gh2 a FAILED guard's heap temporary reclaims before the next arm
pass	gib1 a user-generic Box wrap/unwrap over a runtime heap payload is value-exact and leaks the shells linearly
pass	gid1 a user-generic Box at TWO distinct heap domains (List + String) in one program is correct on every backend
pass	gl1 the let-lifted pure-guard equivalent of the gp1 dispatch shape — the pre-bound guard draw advances the state the arms read
pass	gl2 the let-lifted pure-guard equivalent of the gp2 cascade — both guard draws pre-bound, all three arms reachable
pass	glb1 a GLASSBLOWER'S bench — each blow thins the wall by a third of the gather FLOORED at one and halves the gather (a would-be wall under two CRACKS instead, both fields frozen), heating adds gather, the read packs wall gather and cracks, and the seed's opening gather thins fast toward the crack line on one bench while the other's gentle thirds leave wall to spare
pass	gld1 a GOLD-PANNING yield decay — pan answers the current yield then decays it by a third truncating plus one unguarded (the stream never hits the floor), move relocates resetting the yield to the seed base minus a wear cost that grows two per move, total accumulates, and the richer claim decays through DIFFERENT residues while both wear down in lockstep
pass	gnd1 a GONDOLA on a reflecting cable — move advances the car by the signed direction held in state binding the new position ONCE in a let consumed by the terminus test both answers and both next-states, REFLECTS at either terminus by negating the direction and counting the trip, answers pack the let-bound position an arithmetic direction bit and the trip count, span reads position and trips without advancing, and the seed places the start so one run reflects mid-sequence while the other reaches the far terminus one move later
pass	gng1 a NESTED generic Box-of-Pair over a runtime heap payload is value-exact and leaks the nested shells (~5.5/frame)
pass	gnl1 the nil arm of a multi-payload generic whose other arm nests a list pattern leaks the nil cell
pass	gp1 a PERFORMING guard on a wildcard pattern is a COMPILE ERROR (guards-side-effect-free, CDZ0407)
pass	gp1 a match GUARD inside the handler ARM compares the op PAYLOAD against the live STATE binder — the guard routes admit/reject per dispatch
pass	gp2 TWO guard arms whose conditions PERFORM (`(> (St.next) …)`) — a COMPILE ERROR (CDZ0407), guards-side-effect-free
pass	gp2e TWO pure guards cascade over a draw, the fallback re-performs — guard misses leave dispatch serviceable
pass	gp3 the guard condition calls a PURE HELPER over the state binder — purity analysis admits the def call in guard position
pass	gp4 a guard LADDER in the arm — two guarded arms with different state-derived thresholds classify each payload three ways
pass	gp5 the guard DESTRUCTURES the tuple payload AND reads the state — (guard (tuple a b) (> (+ a b) s)) admits by the pair's sum against the live threshold
pass	gp6 a CTOR-pattern guard with a SAME-CTOR non-guarded sibling admits per-dispatch across TWO dispatches (breaker #15)
pass	gp7 a LITERAL-payload arm and a same-ctor general arm both select per-dispatch across TWO dispatches (breaker #15 guardless face)
pass	gp8 a THREE-arm same-ctor ladder (two literals + general) selects each arm per-dispatch across THREE dispatches (breaker #15 ladder face)
pass	gr2 guarded match on a perform result INSIDE a two-site arm's served branch
pass	grafting through a DAG-shared user-sum subtree rebuilds one path and leaves the shared original intact
pass	graph REACHABILITY drains a worklist against a visited-set over a Map adjacency list
pass	grd1 a 2D GRID state — set rebuilds one CELL via List.update-within-List.update (row carved out, cell replaced, row reinstalled), the answer re-sums the whole grid and reads bound-check both levels
pass	greater-than at the integer extremes is signed
pass	greater-than comparison
pass	greater-than of an integer and a float is rejected
pass	greater-than on an unusual signed width orders a positive above the negative minimum
pass	greater-than-or-equal
pass	greater-than-or-equal of an integer and a float is rejected
pass	grh1 a GREENHOUSE auto-vent — sunshine heats the house and crossing thirty with the vents SHUT trips them open (an eight-hundred alarm row with the temperature's low digits), misting adds three humidity but an open vent bleeds one back (a nine-tagged row), the read packs temperature humidity and vents, and the seed's starting warmth trips the auto-vent on the FIRST sun for one run and the SECOND for the other so every later humidity row shifts
pass	grw1 PLANT growth with pruning stress — day grows by the current rate, prune cuts back to the target answering the clippings AND slows the rate by one (bottoming at one) but only when it actually cut, and the fast grower gets pruned TWICE (stressed twice) while the slow one skips the first prune entirely as a zero-clip row
todo	grx1 the entry List param flows through a two-deep non-recursive helper chain into a consuming concat
todo	grx2 a recursive scalar helper coexists with a non-recursive consuming use of the entry List param
todo	grx3 the entry List param threads a NON-TAIL mutual recursion (suma under +, sumb under *2+)
todo	grx4 the entry List param reaches a self-recursive summer through a non-recursive relay
pass	gry1 a GRAY-CODE generator — each tick answers the threaded counter's Gray encoding (n XOR n>>1) then advances; the body XOR-popcounts consecutive answers proving the single-bit-change law
pass	gs1 a user-declared GENERIC sum resolves by NAME in a param annotation — the applied (Container Int64) checks and the payload unwraps
pass	gs2 a TWO-parameter generic sum by name — (Pair Int64 Int64) in the annotation, both payload slots extracted
pass	gs3 the applied generic sum CROSSES a dispatch — (Container Int64) as an op result, unwrapped in the body after the arm wraps the state
pass	gs4 the applied generic wraps a HEAP payload — (Container (List Int64)) in the annotation, the list summed after unwrap
pass	gs5 the generic applied to ITSELF — (Container (Container Int64)) double-wraps and double-unwraps
pass	gs6 a TWO-VARIANT generic (Either a b) by name — parity routes construction between Left and Right, the annotated consumer matches both
pass	gs7 the applied generic in the op ARGUMENT position — the arm unwraps (Container Int64) payloads into the accumulating state
pass	gs8 the applied generic AS the handler state — (Container Int64) seeds the handle, the arm unwraps and re-wraps per dispatch
pass	gsc1 a GOLF scorecard with a birdie-streak multiplier — each hole answers its par delta where consecutive under-par holes MULTIPLY the negative contribution by the deepening streak, an over-par hole resets the streak, card totals, and the seed shifts one hole's strokes so the streak bonus doubles differently
pass	gsx1 ONE arm instantiates the same generic sum at TWO types — a Container Int64 and a Container String built, unwrapped, and combined per dispatch
pass	gtx2 a generic transformer's tuple-result closure with a consumer that PROJECTS the element (vars pinned by the consumer body)
pass	gtx3 a generic transformer's tuple-result closure at a SINGLE domain with a discarding consumer
pass	gtx5 a generic transformer's Option-result closure with a discarding consumer at two domains (the nominal-sum cell is green on every target)
pass	gwl1 a scalar-guarded wildcard falling through to a self-tail-call iterates (tail-depth)
pass	gwl2 a value-eq-guarded wildcard falling through to a tail-call iterates (tail-depth + scratch)
pass	gwl3 a value-eq match scrutinee in a self-tail-call loop (scrutinee scratch)
pass	gwl4 a value-eq guard on a literal-probe arm in a tail-call loop (probe-else scratch)
pass	gwl5 a value-eq guard on a sum-match arm with a call scrutinee in a tail-call loop (sum-cont scratch)
pass	gws1 GROWING-STATE shared-let — the push arm let-binds a value derived from the argument the CURRENT length and the seed bias, answers the binder packed with the length, and threads the LIST GROWN by the binder; the total draws all three stored values back out, so a binder aliased against the post-push list would corrupt both the rows and the total
pass	ha1 an INNER op's arguments draw from the OUTER handler — two outer draws cross the inner dispatch boundary in order
pass	ha2 outer-draw feeds the INNER op, whose result feeds an OUTER op again — a three-hop cross-handler value chain
pass	ha3 INTERLEAVED outer-inner-outer draws in ONE argument list — each draw dispatches to its own handler in sequence
pass	ha4 an inner op's arguments dispatch to the SAME inner handler — same-effect draws inside the op's own arg list, then an outer draw
pass	halving the minimum subnormal flushes to zero while a double-min stays subnormal
pass	handler op-param and state binders stay hygienic when colliding with perform-site names
pass	handler-arm bindings and perform-site bindings stay in their own scopes across the fold
pass	hash-only set ops keep working on float-leaf tuples that have no blessed order
pass	hb1 a BOOL op argument computed by comparing a draw's residue — the arm branches on the delivered flag against live state
pass	hc1 a THREE-deep pure helper chain whose LEAF performs — two top-level calls thread the state through the depth
pass	hc1 an empty-captures COMBINATOR boxed as a first-class value and applied via call_indirect reclaims its compound shells
pass	hc2 a CAPTURING closure boxed as a value stays EXCLUDED from the combinator shell-reclaim (leak, never double-free)
pass	hc2 helpers at TWO depths both perform — the call-site draw, mid's draw, and leaf's draw arrive in order
pass	hc3 the SAME performing helper under two SEQUENTIAL handlers — each handle interprets its draws with its own arm and seed
pass	hc4 a performing helper COMPOSED with itself — probe(probe(draw)), three dispatches through two call frames
pass	hc5 a helper RETURNS a tuple of two draws — the caller destructures it and re-performs
pass	hcd1 dropping a closure whose capture is a runtime-BUILT list cascades the reclaim
pass	hcd2 dropping a closure capturing an IMMORTAL trie + scalar reclaims the mortal env and no-ops the immortal
pass	hce1 tuple-index projection of a captured tuple in a closure body folds and runs (dqe leg-1 through closure conversion)
todo	hce2 a borrowing op (=) over a captured value in a closure body declines pending the ownership proof (todo)
pass	hcn1 a closure CAPTURING another closure crosses and dispatches through it
pass	hcn2 a closure capturing a runtime-built MAP looks up by the call argument
pass	hcn3 dropping a closure with a CHAMP capture cascades the reclaim to zero
pass	hcp1 a captured tuple returned WHOLE from a host-called closure works (projection is the ICE, not the capture)
pass	hcp2 a closure capturing an IMMORTAL 33-trie plus a runtime scalar crosses and reads through the immortal
pass	hcp3 a closure capturing a runtime-BUILT list crosses and reads it (mortal capture retained by the host-held handle)
pass	hcx1 tuple-index projection of a captured tuple in a host-called closure body FOLDS — the projection reads the captured tuple env cell, not the inlined element (was the chr1 ICE's effects-free face: no-local-slot)
pass	hcx2 a NESTED tuple-index projection of a captured tuple in a host-called closure body FOLDS — the projection chain stays runtime over the captured env cell (the nested face of the hcx1 no-local-slot ICE)
pass	hcz1 dropping a closure whose body RETURNED its captured TUPLE reclaims cleanly (read-site dup balances the env-cell drop)
pass	hcz2 dropping a closure whose body RETURNED its captured LIST reclaims cleanly (read-site dup balances the env-cell drop)
pass	hcz3 dropping a closure whose body RETURNED its captured MAP reclaims cleanly
pass	hcz4 dropping a closure whose body RETURNED its captured SET reclaims cleanly
pass	hcz5 dropping a closure whose body RETURNED its captured WRAPPED compound (record holding a tuple) reclaims cleanly
pass	hczm1 a captured TUPLE escaping TWICE in the returned value reclaims cleanly (per-occurrence escape-dup)
pass	hczm2 a captured TUPLE READ once (projection) AND escaping once reclaims cleanly
pass	hczm3 a captured LIST escaping TWICE reclaims cleanly (vec-rep twin of hczm1)
pass	hczm4 a captured MAP escaping TWICE reclaims cleanly (CHAMP twin)
pass	heap string elements of a hoisted list if select by branch
pass	heap-valued handler state above an inner abort keeps threading
pass	het1 ONE effect with THREE result types — Int64, Bool, and String ops share a single scalar thread, each consumed by its own idiom in one run
pass	heterogeneous constructions take ONE malformed-collection code across list, map, and set
pass	hex and binary literals drive a runtime mask-extract across radix notations
pass	hexadecimal, binary, and decimal spellings of one value are equal
pass	hfr1 a handler fold's list result crosses the return boundary (two threaded draws, two reachable cells)
pass	hgl1 an HOURGLASS with flips — tick drains three grains from the top clamped at empty, flip swaps the bulbs answering the new top (total minus old top), and the two totals CONVERGE mid-stream (both read 6,3,0 after the first flip drains) then DIVERGE again at the second flip which restores each glass's own total
pass	hi1 an arm INSTALLS a fresh handle and resumes with its result — a whole handler lifecycle inside one dispatch
pass	hi2 the arm-installed handle's body draws from the arm's ENCLOSING frame — fresh inner frame and outer dispatch in one arm
pass	hi3 the arm-installed handle's OWN arm resumes with an OUTER draw — the rv face nested inside another handler's dispatch
pass	hi4 the arm-installed handle ABORTS with an outer draw — the av face nested inside another handler's dispatch
pass	hld1 an INVENTORY hold/settle/release protocol — hold reserves against available (on-hand minus already-held) answering the running held total or the NEGATED available on reject, settle deducts held from on-hand, release just drops the holds, and the seeds reject DIFFERENT holds
pass	hmg1 a HAMMING-distance tracker with an XOR-folding reference — cmp answers the popcount of the value against the reference remembering the value, lock folds the last value INTO the reference by XOR answering the old one, and the seed reference propagates through both locks so the distance rows diverge after the first fold
pass	hnc1 the HANOI smallest-disk cycle — the small disk moves on every ODD move cycling pegs in the seed-picked direction (answering peg-times-ten plus one), even moves answer the small disk's resting peg, count reads the total, and the two directions trace MIRROR peg sequences that re-converge at the mid-cycle zero rows
pass	ho2 a PURE factory returns a closure applied to a draw — the captured constant is pure, only the argument is drawn
pass	hof1 a higher-order APPLY-TWICE over a draw — the fn value crosses the call while the thread advances underneath
pass	hoh1 a HANDLE WHOSE INIT IS ITSELF A WHOLE HANDLE EXPRESSION — the outer Fibonacci pair-walker's starting tuple is computed by an inner two-dispatch counter handle that answers seed-plus-one while doubling its own state, both inner draws land in the outer init tuple, the inner handler is fully torn down before the outer installs, and the seed steers the inner counter so the two runs walk Fibonacci from different gates
pass	hoh2 the INIT-COMPUTING INNER HANDLE CARRIES POST-RESUME TOLLS — the outer handler's starting value comes from an inner two-dispatch handle whose arms each add a hundredfold toll after their resume, the inner pyramid fully unwinds during INIT evaluation before the outer handler exists, and the outer draws then advance by sevens from the toll-laden seed
pass	hoh3 the INNER HANDLE'S INIT PERFORMS ON THE LIVE OUTER HANDLER — the starting value packs two outer levies drawn while only the outer handler exists, the levies advance the outer state in order before the inner frame installs, and both inner draws then echo the levy-built seed one tick apart
pass	hoh4 a FOREIGN DRAW INSIDE THE INNER ARM'S ANSWER — the inner handler's arm draws the OUTER effect while building its resume answer so the outer state thread advances once for the body's opening draw and once inside the inner dispatch, the arm-side draw sees the state the body-side draw left behind, and the seed shifts only the inner tenfold addend
pass	hoh5 REPEATED FOREIGN DRAWS THREAD THE OUTER LADDER THROUGH BOTH INNER DISPATCHES — each inner step's answer folds in an outer draw so the outer state climbs by sevens across the inner dispatches while the inner state doubles, both ladders' second rungs land in the thousandfold addend, and either thread resetting between dispatches collapses a distinct digit
pass	hoh6 the OUTER THREAD SPANS INIT AND ARM DRAWS — the inner handler's starting state comes from an outer draw and its arm folds a SECOND outer draw into the answer, one continuous outer thread runs through the init-time draw into the arm-time draw seven apart, and a thread reset at the frame-install boundary collapses the gap
pass	host calls are observed in the order they were made
pass	host calls in the seed AND the next-state slot — the FINAL dispatch's state call is elided
pass	host calls issue only from the TAKEN arm of a fused match and in arm order
pass	hp1 a RECURSIVE fn installs a fresh same-effect handler per frame — the base case draws from the deepest, each frame from its own
pass	hp2 per-frame handlers with a POST-ORDER draw — each frame draws its own state AFTER the recursive call returns
pass	hp3 the recursive call sits in the SEED — each frame's handler is seeded by the whole subtree below it
pass	hr1 a HANDLE expression as a record-literal FIELD value — the region's result sits beside a pure field
pass	hr2 a HANDLE expression as a middle LIST element — the region's result sits between pure elements
pass	hr3 a HANDLE expression as a map-literal VALUE — the region's result is stored under a key and looked up after
pass	hr4 a whole HANDLE region as another effect's op ARGUMENT — B's region computes the value A's dispatch consumes
pass	hrn1 a HORNER polynomial evaluator with a MID-STREAM base swap — feed folds acc*x+c answering the running value, swapx replaces the base answering the old one, and the seed shapes the INITIAL base so the pre-swap accumulations diverge while the post-swap coefficients ride on top
pass	hs1 a LIST state threads through the mutual-SCC fold correctly but leaks the final state linearly (the xat2-class threading leak on the mutrec fold)
pass	hs1 an inner SAME-effect handle's result is the match SCRUTINEE — the selected arm and the trailing draw hit the OUTER state
pass	hs2 an OUTER-seeded inner handle builds a tuple scrutinee — the destructured arm re-performs against the outer
pass	hs3 an inner SAME-effect handle's result is the IF condition — the taken branch re-performs against the outer
pass	hst1 a HISTOGRAM state with in-arm BUCKETING — observe divides into decade buckets and counts, mode walks the sorted enumeration to answer the densest bucket
pass	hst1 a TUPLE handler state rebuilt per tail-resume threads exactly (the state-accumulator face)
pass	hta1 a 24-field runtime tuple builds a VALID artifact and every field projects (high-arity, verified by running)
pass	hta2 a 20-parameter function builds a VALID artifact and sums all params (high param-count, verified by running)
pass	hwp1 a UInt8-width list entry param read inside a handle body composes with the fold
pass	hygiene fix does not over-reach: a different-named template binder needs no rename
pass	hygiene: a spliced unquote sits beside the template's own use of the renamed binder
pass	hygiene: an unquote is uncaptured through two nested same-named template binders
pass	hygiene: two distinct unquotes each colliding with a different template binder are both renamed
pass	hys1 a HYSTERESIS gate — the output turns on at eight and off at three, HOLDING between the thresholds; the same mid-band feed answers differently depending on which side entered it
pass	i64-EXTREME indices — MAX and MIN as List.at arguments both answer the None fallback; a truncating index marshal would wrap into range
pass	i8s1 an Int8 handler state walked to the TOP of its range under a guard — the +40 stride stops exactly where one more step would overflow
pass	ic2 an INLINE performing if-condition — the taken branch's draw reads the condition-advanced state
pass	ic3 CHAINED if-else-if where each condition draws — three rows land in three different arms
pass	ic4 a helper with a performing IF-condition called twice — each call re-evaluates the condition against the advanced state
pass	ic5 a recursive LOOP whose exit condition draws per iteration — the iteration count is state-determined
pass	ic6 a draw-conditioned branch selects BETWEEN two effects — the untaken effect's state is untouched by that row
pass	icp1 lifted String AND Bytes params feed an effect arm's answer
pass	icp2 the original ch05 face RESURRECTED — literal string MATCH on a lifted param
pass	icp3 a lifted String compared and measured through an if-chain (the viachain shape on a param)
pass	id1 handling an ALREADY-DISCHARGED effect: outer handle of an effect the inner fully consumed
pass	idc1 a unary def-call's allocated list bound once and referenced twice stays one shared object
pass	idc2 a NULLARY def-call's allocated list bound once and referenced twice stays one shared object
pass	identical PURE reads of a perform-built list — safe to share, values agree
pass	identical PURE subterms around distinct performs — pure sharing must not merge dispatches
pass	identical negative zeros nested in a tuple compare equal
pass	identically-built ropes on both sides of a nested compare are equal (control)
pass	ieh1 an immortal 33-element list as handler STATE threads unchanged across three resumes (runtime-indexed reads)
pass	ieh2 an immortal 33-element list as an op ARGUMENT crosses the dispatch seam (runtime-indexed read in the handler; miss cell via the small twin)
pass	ieh3 MIXED handler state (mortal counter + immortal list in a tuple) advances the counter and re-threads the immortal across resumes
pass	if branches of records differing by a misspelled field offer the rename fix
pass	if1 Float64.Infinity minus itself is NaN (canonical-byte equal to nan)
pass	if1 a WHOLE nested handle expression as an IF's CONDITION beside outer draws
pass	if2 one divided by Float64.Infinity is exactly zero
pass	if2 the Float64.Infinity CONSTANT compares byte-equal to a runtime-produced inf
pass	if3 negated Infinity is below every finite float
pass	if4 Infinity plus a finite float saturates to Infinity
pass	if5 runtime division by zero produces Infinity and crosses the boundary
pass	if6 named Infinity constant as effect handler seed threads and compares
pass	ifb1 fifty frames of persistent update against a constant list never corrupt the constant
pass	ifb2 a let-bound constant tuple referenced twice: the WHOLE `(tuple t t)` is a constant compound RETURN, so it hoists build-once (WIT static encoding) to a single census-excluded immortal — 0 mortal allocations
pass	ifcap1 a captured TUPLE escaping via BOTH mutually-exclusive if-arms reclaims cleanly (anti-over-dup control: one dup per PATH, not two)
pass	ifcap2 a captured TUPLE escaping via ONE if-arm only reclaims cleanly
pass	ijs1 an if-join binder CONCAT-shared into both arms reclaims clean — the #5382 dup-skip exclusion
pass	ijs2 the in-place-reuse family still fires the if-join dup-skip — tight reclaim preserved
pass	ijs3 a rope (String.concat) if-join shared binder reclaims clean on both arm positions
pass	ikc1 a Map keyed by a hoisted constant list is found by a runtime-built equal key
pass	ikc2 a handler seeded with a hoisted constant list threads functional updates per dispatch
pass	il1 strict O-I-O-I body interleave — two independent threads advance in lockstep, positional weights pin the dispatch order
pass	il2 the interleave WIDTH is data-driven — parity picks one-or-two O draws per I tick, a helper sums each O burst
pass	im2min5 a performing self-recursive walk then a TRAILING draw — the callee-advanced state reaches the continuation (finding #12 face)
pass	imc1 an EMBEDDED constant tuple in a compound RETURN now hoists build-once (the fixed-shape value-encode assembler gained build-once sections); only the runtime-`n` outer tuple is mortal
pass	imc2 a constant tuple as a returned LIST element now hoists build-once (the collection-return assembler gained build-once); only the list vec + the runtime-`n` tuple are mortal
pass	immortal-nullary FBIP witness: a recursive Nat walk REBUILDS the spine (FBIP-reuses S cells) and returns a fresh terminal (Z), then re-folds it — value-correct + the rebuilt immortal Z is shared (0-leak after the immortal fix)
pass	import { __ast__ } binds the sibling module's canonical AST as a compound Ast value
pass	import { __ast__ } reflects a module containing char and symbol literals (reflection is total)
pass	importing a name a module does not export is rejected
pass	imr1 a per-name import rename binds the export under the alias and runs
pass	imr2 per-name renames disambiguate a uniform export name from two modules
pass	imr3 one export imported under TWO aliases — both bind to the same def
pass	imr4 a MIXED list — plain import and rename side by side
pass	imr5 the ORIGINAL name is NOT bound after a rename (unbound reject)
pass	imr6 an alias colliding with another import is a colliding-import reject
pass	imr7 a renamed FUNCTION import applies through the alias
pass	indexed reads at different indices of one list are not shared
pass	indexing a byte sequence out of bounds yields None
pass	indexing a byte sequence returns Some of the byte at that position
pass	indexing a byte sequence with a negative index yields None
pass	indexing a list bound from a sum payload yields the element
pass	indexing a list in bounds yields Some of the element
pass	indexing a list out of bounds yields None
pass	indexing a list with a negative index yields None
pass	indexing a runtime string past its last scalar yields None
pass	indexing a slice is relative to the slice's start
pass	indexing an empty list yields None
pass	infinities order beyond every finite value
pass	inline-always on a recursive definition is rejected
pass	insert-order does not leak into Set.to-list enumeration order
pass	inserting a key already present replaces its value, not the size
pass	inserting a key of a different type into a map is a type error
pass	inserting a present element is a no-op value
pass	inserting a runtime element adds it or is a no-op if already present
pass	inserting a runtime element yields a set that contains it
pass	inserting a value of a different type into a map is a type error
pass	inserting an element yields a set containing it
pass	inserting into the empty map then looking a key up yields the value
pass	int8 wrap truncates and sign-extends the low 8 bits of a runtime value
pass	integer division by a negative divisor truncates toward zero
pass	integer division of a negative dividend truncates toward zero, not toward negative infinity
pass	integer division truncates toward zero
pass	integer to byte (truncate to 0-255)
pass	integer to byte wraps on overflow
pass	interleaved do-discarded performs at BOTH nest levels thread their own states independently
pass	interleaved insert-remove churn at 500 leaves exactly the odd keys
pass	interleaved same/different Instants keep FIFO within a time and ascending across times
pass	interning the same string twice yields equal symbols
pass	intersection cardinality with a runtime-element operand
pass	intersection distributes over union on runtime-element sets
pass	intersection is associative
pass	intersection is commutative over runtime-element sets
pass	intersection of a set with itself is the set (idempotent)
pass	intersection of a trie with its churned-back subset is the subset itself
pass	intersection with the empty set is the empty set
pass	inv1 MODULAR INVERSE by extended Euclid in the arm — the iteration count is value-dependent, the Bezout coefficient normalizes through the double-mod idiom, and the body verifies v times inverse is one mod 97
pass	io1 a shared pure subexpression reads the same value twice (value parity: shared, self-subtract, commuted)
pass	io2 a shared min-select reads the same value twice (value parity, both operand orders)
pass	io3 a shared subexpression keeps its overflow trap (computed once still traps)
pass	irb1 a constant 33-element list in RETURN position now hoists build-once (the collection-return assembler gained build-once); both constant if-branch lists are census-excluded immortals → 0 mortal
pass	irb2 a constant nested tuple in RETURN position builds mortal cells (outer+inner; same assembler path as imc1)
pass	irj1 an if-JOIN whose arms resume with MISMATCHED value types is rejected per-arm CDZ0201 (not escaped to InvalidWasm)
pass	irj2 an if-JOIN whose arms resume with MISMATCHED next-STATE types is rejected per-arm CDZ0201
pass	irj3 a VALID if-JOIN where both arms resume the op-result type folds — the per-arm check does not over-reject
pass	is1 a draw-SELECTED match arm installs a nested same-effect shadow — outer state resumes after the branch-local region
pass	is2 the ARM's resume-value is a nested SAME-effect handle — a fresh shadow instantiated per dispatch from the live state
pass	is3 the ARM's NEXT-STATE is computed by a nested SAME-effect handle — the shadow feeds the outer state thread
pass	iso-b a let-bound escaped-closure helper-call answer inside the handle body folds — the recovery forces the re-parented load-time ref onto the structural walk so it resolves against the rebuilt let (was a false CDZ0101)
pass	itf1 an immortal 33-trie compares structurally equal to a runtime-BUILT equal list, and the runtime side reclaims
pass	itf2 an immortal 33-trie as a champ MAP key is hit by a runtime-built equal probe key, which reclaims
pass	itf3 fifty frames of List.concat off an immortal 33-trie (runtime-indexed reads) reclaim every produced trie
pass	itf4 an immortal 40-element Set compares structurally equal to a runtime-BUILT equal Set, and the runtime side reclaims
pass	itf5 fifty frames of Set.union off an immortal 40-element Set (runtime-keyed probes) reclaim every produced set
pass	ivl1 INTERVAL OVERLAP counting — each add answers how many existing intervals the newcomer overlaps (closed-interval test lo<=b and a<=hi) before inserting itself; the seeded interval slides in or out of range
pass	iwr1 a SIGNED Int8 handler state — wrapping-add crosses the sign boundary through the thread, the widened answers expose the sign-extension
pass	ix1 the op argument INDEXES a list held in a two-slot state — in-range reads project, out-of-range yields the arm's fallback
pass	jin1 a SEPARATOR-JOIN builder — the comma inserts only BETWEEN elements via the count field, and an EMPTY piece still consumes a separator slot
pass	jmp1 a TIRING frog on a ladder — each jump advances by the current stride which then SHRINKS by one bottoming at one, rest restores the seed stride answering the distance so far, and the longer opening stride tires through a different lattice (five-four-three versus three-two-one) so the gap between the frogs WIDENS at every row
pass	jns1 a JOHNSON twisted-ring counter on TWO BOOL state slots and a pulse count — pulse shifts q into p and the NEGATION of p into q answering a bit-packed readout of both new flags plus an equality bit and the count, align SWAPS the flags only when they differ tagging swap and hold answers differently and touching no count, and the seed loads the ring so one run walks the four-phase cycle from inside while the other enters at the seam
pass	join-site records with a genuinely different field set get no rename fix
pass	jointly-total guards over one variant are still non-exhaustive (guards are opaque to the checker)
pass	jos1 JOSEPHUS elimination — every dispatch advances the cursor k-1 around the SHRINKING ring by modulo, removes the survivor there by filtered rebuild, and answers the eliminated id
pass	keyed lookups at different keys are not shared
pass	kgt1 a KADANE max-subarray tracker — feed extends or RESTARTS the running sum via a four-way comparison lattice answering the live sum, best remembers the peak from a -99 sentinel, and the seed flips the middle feed's sign so one run restarts there while the other extends through it
pass	knt1 a KNIGHT walker on an eight-by-eight board — hop attempts the L-move answering the landing square's index or -1 REFUSED off-board with the position held, cnt reads the completed-hop count, and the seed's starting rank makes the SAME move list bounce twice on one run and once on the other
pass	ksd1 a record-fold projection consumed by String.concat survives the loop dup-skip — the #4139/#5090 UAF fence
pass	ksd2 the SCALAR-sibling control — an Int64 co-field never took the broken path and must stay green
pass	ksd3 the PATTERN-BINDER form of the ksd1 grandchild consume is CLEAN — bind-early destructure takes the tight path
pass	ksd4 the TUPLE-pattern form of the grandchild consume is CLEAN like the record-pattern twin
pass	ksd5 a DEPTH-3 projection chain still fires the grandchild fence — no UAF at any chain depth
pass	lae1 a list annotation with a conflicting element type is rejected (CDZ0203)
pass	lae2 a matching list annotation is transparent
pass	lam1 INLINE and LET-BOUND lambdas applied to one draw — both closure forms read the same drawn value
pass	lao1 List.at over an owned temporary list reclaims it (no leak)
pass	lao2 a borrowed list read by List.at is not freed early (owner reclaims)
pass	lap1 a STOPWATCH with lap splits — tick advances the clock by a seed stride, lap answers the split since the last mark and remembers it, best tracks the MINIMUM split (seeded -1 sentinel replaced on first lap), and the middle lap of three is the unique best on both seeds
pass	lar1 a List.at-extracted INNER LIST bound by Some and only borrowed leaks the inner vec
pass	lar2 a List.at-extracted SCALAR element reclaims fully (the flat-list control)
pass	large-set algebra over a multi-level CHAMP trie computes correct union/intersection/difference
pass	lba1 an op taking a LIST argument — the arm folds the batch into the scalar state by recursion; the second batch is BUILT FROM the first's answer and the empty batch is a no-op
pass	lbe1 List.at over a runtime list at a masked nonnegative index reads in bounds
pass	lbe2 List.at over a runtime list at a masked index past the end yields None
pass	lbe3 List.at over a runtime list at a plain negative index yields None
pass	lbr1 a List entry param relayed to a helper def that only borrows its length
pass	lc1 SELF-CONCAT sharing — the same list appears twice in one concat, then the original grows and is re-read unchanged
pass	lck1 a COMBINATION-LOCK matcher — press advances the cursor against the three-digit secret whose MIDDLE digit is seed-shaped, a wrong press falls back to one if it re-matches the first digit else to zero (answering the negated or zero fallback), and open pays a hundred-plus-count on a full match; the SAME eight presses open the lock in the FIRST half on one seed and the SECOND half on the other
pass	lcr1 List.len over an owned-temporary List.concat reclaims it (no leak)
pass	lcr2 List.len over an owned-temporary List.update reclaims it (no leak)
pass	lcr3 a borrowed List.concat result read twice is not freed early
pass	lcr4 20000 owned-temporary List.concat each reclaim (no leak drift)
pass	lcs1 an LCS DP-ROW state — each fed character rebuilds the whole dynamic-programming row against the fixed pattern via a recursive fold reading the OLD row while writing the NEW
pass	ldd1 an EVENT-LEDGER state — a List of (tag,value) tuples appends per log dispatch, and replay folds only the MATCHING tag's values (absent tag folds to zero)
pass	ldg1 a LEADING-DIGIT counter — feed strips each value to its leading digit through a recursive divide-down callee, a hit on the seed-wanted digit answers digit*10 plus the running hit count while a miss answers the bare digit, and the two seeds hunt DIFFERENT digits through one shared stream
pass	le multi-byte fields read little-endian at runtime offsets, unsigned and signed
pass	le-ax of a ground add that OVERFLOWS Int64 traps during evaluation — it cannot wrap to forge a false bound
pass	le1 a SINGLE-element const Set.to-list folds
pass	le2 an EMPTY const Set.to-list folds to the empty list
pass	le3 a single-entry const Map.to-list reads its lone (k v)
pass	led1 BULLY leader election — registrations grow the candidate set, elect scans for the highest id, and deregistering the ELECTED leader (its id threaded through the body) forces re-election from the survivors
pass	left shift
pass	lep1 a String literal list-element pattern on an Int64 list is a type error (CDZ0201)
pass	lep2 an Int literal list-element pattern on a String list is a type error (CDZ0201)
pass	lep3 a well-typed list-element literal pattern dispatches by value
pass	less-than at the integer extremes is signed, not unsigned
pass	less-than compares negative operands by signed order
pass	less-than-or-equal
pass	less-than-or-equal of an integer and a float is rejected
pass	let bindings' initializers are evaluated in binding order
pass	let-bound perform results stored into record fields
pass	lf1 a recursive index-walk over a DRAW-BUILT list, scaled by an earlier draw — the walk itself is pure
pass	lf2 a PERFORMING recursive walk — each element visit draws, pairing element order with state order
pass	lf3 the arm pushes TWO elements per dispatch (both lengths read from the PRE-push list) — the third draw reads length 5
pass	lf4 a per-element dispatch comparing each element against the RISING state — amplify-or-pass per visit
pass	lf5 TWO lists walked in LOCKSTEP with a per-pair 2-arg dispatch — length-guarded via projection helpers
pass	lfs1 an EIGHT-BIT Fibonacci LFSR — step shifts right injecting the XOR of taps zero and two as the new high bit answering the register, peek masks the low nibble, and the two seeds fall into DIFFERENT orbits (one decays toward the injected-bit ladder, the other rings the high bits immediately)
pass	lft1 an ELEVATOR load limiter — board adds the passenger's weight answering the load or REFUSING with the negated overage when the seed-shaped capacity would be exceeded (the load untouched), alight subtracts clamped at empty, trips counts refusals, and the same boarding sequence is refused ONCE on the roomy car and TWICE on the tight one with different overage readings
pass	lgt1 a TRAFFIC light with a demand sensor — tick counts down green-three yellow-one red-seed cycling on zero (answers pack color-times-ten plus remaining), demand during a LONG red shortens the remainder to one answering a hundred plus the old value (otherwise it just reads), and the seed-shaped red length changes both the red row and the demand's rebate
pass	lhn1 a LUHN checksum accumulator — feed doubles every second digit by POSITION PARITY subtracting nine from two-digit doubles, chk answers 1 on a multiple of ten else the residue, and the seed-fed digit doubles on both runs but only one crosses the subtract-nine threshold
pass	lhv1 a list mixing Int64 and Bool is a malformed collection (CDZ0201)
pass	lhv2 a list mixing Int64 and Float64 is malformed — no silent numeric promotion (CDZ0201)
pass	lhv3 a list of records with different field sets is malformed (CDZ0201)
pass	lhv4 a list of tuples of different arity is malformed (CDZ0201)
pass	li1 draws choose BOTH the List.update target and the List.at read index — the collection edit follows the thread
pass	li2 a list BUILT from pushed draws then read at a draw-picked index — construction and consumption share one thread
pass	li3 draw PARITY picks prepend vs push while building — the final shape encodes the whole draw sequence
pass	lif1 List.len of an if whose both arms are owned producers reclaims (join = Owned)
pass	lif2 a mixed owned/borrowed if-arm does not free the borrowed param under a sibling read (join = Borrowed)
pass	lif3 5000 borrowed-arm if paths do not double-free the param (no trap/drift)
pass	list concatenation is associative by content
pass	list construction strictly evaluates an EFFECTFUL element argument — the perform runs even when = short-circuits
pass	list construction strictly evaluates an EFFECTFUL element argument — the perform runs even when the list is discarded
pass	list construction strictly evaluates its element arguments — a trapping argument traps in an = operand
pass	list construction strictly evaluates its element arguments — a trapping argument traps under List.len
pass	list construction traps at a first-DIFFERING position too — the = short-circuit does not save the trapping arg
pass	list ordering recurses into string elements by content across mixed reps
pass	lists are equal by elements in order
pass	lists built by PUSH and by CONCAT compare equal by content, and order stays decisive
pass	literal payload refinement works in a non-head element position
pass	literal refinement on two elements of one list pattern
pass	lk1 LIST keys — structural dedup in a set + list-keyed map lookup — through both targets
pass	lk1 a STRING-keyed lock — the arm compares the string payload against the stored key by equality; parity picks the key, a fail counter rides along
pass	lk1 divergent update aliases leave no live heap objects after the run
pass	lk2 a ROLLING-code lock — each successful match advances the key index through a list of string keys, |n mod 3| picks the start
pass	lk2b a closure capturing a BRANCH-SELECTED list across an interleaved update leaves no live heap objects
pass	lk3 LOCKOUT after two failures — the fail counter flips a dead flag that rejects even the correct key thereafter
pass	lk3 a persistent update inside a handler ARM plus continuation read leaves no live heap objects
pass	lk4 shared Map and Set operands across inserts leave no live heap objects
pass	lk5 a FIVE-level delegation chain with a heap LIST state at the outermost level leaves no live objects
pass	lk6 SIBLING handles each with a heap MAP state leave no live objects
pass	lky1 a LEAKY-BUCKET meter — arrive fills toward the seed-shaped capacity answering only the OVERFLOW spill (level clamps at the brim), drain leaks five clamped at empty answering the new level, and the small bucket spills twice where the large one never does
pass	ll1 a LIST-OF-LISTS handler state — each dispatch appends a fresh row, the drain reads a middle row's middle element
todo	llb1 a recursive do-local fn capturing a do-LOCAL BINDING computes (should-work: lexical capture is uniform)
pass	llo1 List.len over an owned temporary list reclaims it (no leak)
pass	llo2 a borrowed list read by List.len is not freed early (owner reclaims)
pass	llt1 a list length >= 0 is a tautology (folds to the then-branch value)
pass	llt2 a match on a list length never hits a negative arm
pass	lm1 a branch-selected owned list scrutinee is reclaimed after the match
pass	lm1 a let-destructured const closure forwards PER-SITE — let and match faces agree, never cross-specialize
pass	lm2 a helper-built branch-selected list scrutinee reclaims across the call
pass	lm3 a branch-selected list-of-tuples scrutinee reclaims shell and element shells (#3770)
pass	lm5 a rest-binder match reclaims the branch-selected scrutinee and its tail (#3756)
pass	lm6 a fall-through wildcard arm reclaims the unmatched branch-selected list
pass	lnr1 a const Set.to-list of a LONE non-orderable element still declines (the sort-by-skip soundness face)
pass	lnr2 a const Map.to-list with a LONE non-orderable KEY still declines
pass	lnr3 an EMPTY set of a non-orderable element type IS order-trivial — const to-list folds to 0
pass	lo1 THIRTY dispatches in one region — the fold scales past the corpus's usual handful, arithmetic sum exact
pass	lo2 a NINE-level shadow tower (outer + eight) — one draw per level, the deepest doubling, strides 1-8
pass	lo3 EIGHT ops in one effect, called in shuffled order — dispatch-table width, one op advancing mid-sequence
pass	loc1 a LIST-OF-CLOSURES pipeline state — each add pushes a stage function, run folds the input through every staged closure in order, later stages compose after earlier runs
pass	lok1 a CANAL lock filling toward a seed pool level — enter answers the full gap, each equalize raises the lock two clamped at the pool answering the new level, exit answers a hundred plus the trip count when the levels MATCH or the remaining gap negated, and the low pool completes passage on the third equalize while the high pool is still twelve short at the same row
pass	lol1 a list-of-LISTS scrutinee matched and read via List.len reclaims (the #3778 bonus)
pass	looking up an absent key yields None
pass	lp1 a LIST handler state — each feed pushes and returns the pre-push length, a summing walk reads the final list
pass	lp1 multi-length LIST arms with a head-pair rest binder — decision-tree dispatch through the hop
pass	lpc1 a LAMPORT CLOCK — local events tick the counter, receives jump it to max(local,remote)+1; the stale remote (already-past timestamp) still ticks by one through the max
pass	lps1 a non-matching literal payload falls through to the same-variant binder
pass	lps2 a literal-only sum arm with no binder fall-through is non-exhaustive
pass	lru1 a CAPACITY-2 LRU cache — put inserts as most-recent and answers the evicted key or 0, get answers the value refreshing recency or -1 on miss, and the SEED collides the computed key with a literal so one run UPDATES where the other EVICTS
pass	lrx1 a runtime List.push extends and its length counts
pass	lrx2 a shared list binding consumed then re-read is retained, not mutated
pass	lrx3 a shared list param consumed then passed to a self-call sibling is retained
pass	ls1 a LIST-state snapshot crosses mid-growth — the held list is immutable while the state keeps growing, sum and final length both pinned
pass	lsb1 a bounded value shifted by a bounded count computes the masked result
pass	lsb2 a negative bounded operand shifted by a bounded count stays sound (Int8)
pass	lsb3 a bounding box that overflows keeps the guard and traps at a genuinely-overflowing input
pass	lsu1 a mask covering a UInt64 top-byte logical shift is elided (value parity)
pass	lsu2 a mask covering a UInt32 high-nibble logical shift is elided
pass	lsu3 a mask that does not cover the shifted range is kept (UInt64 x>>8)
pass	lt1 the arm SWAPS its list state's ends per dispatch — the head alternates between the original ends
pass	lt2 the arm DOUBLES every list element per dispatch (via a projection helper) — the sum geometrically grows
pass	lus1 a runtime List.update traps on a wrapping or out-of-bounds index, succeeds in bounds
pass	lv1 a List of TUPLES from the empty (list) literal seed — pushed in one arm, indexed in a sibling
pass	m2m1 a MAP-OF-MAPS handler state — each dispatch bumps a (category key) cell, the drain reads across both levels
pass	main returns Float64.Infinity as a boundary value
pass	mal1 a whole-module ALIAS import projects a member fn
pass	map THEN filter compose — one combinator's output list is the next's input
pass	map equality is independent of insertion order
pass	maps reached via Map.swap and Map.take equal their directly-built twins
pass	match arms of records differing by a misspelled field offer the rename fix
pass	match on a Result returned by a fallible function
pass	match on a function-returned sum takes the other variant at runtime
pass	match on a sum value returned by a function selects the runtime variant
pass	matching a lookup from a computed-key map literal selects the present-value arm
pass	matching a runtime Bytes.at Option binds the byte in the Some arm
pass	matching a runtime Bytes.at Option takes the None arm past the end
pass	matching against zero probes the normalized narrow value, not the raw wide slot
pass	matching falls through to else when no literal matches
pass	matching on a function value is a type error
pass	matching on a partial application is a type error
pass	matching on a sliced string selects the literal arm
pass	matching on a string produced by an expression
pass	matching on integer literals
pass	matching on string literals
pass	mcs1 a mask covering a logical-shift's proven range is elided (value parity)
pass	mcs2 a mask narrower than the shifted range still masks (not elided)
pass	md1 a MODULAR ring state — the thread walks a size-7 ring with stride 3, entry point reduced mod 7 at the seed
fail	md2 the arm DRAINS a Map entry per dispatch — Map.remove keyed by the op arg, a re-take of the same key routes to the miss path
fail	md3 the drained value RE-FILES under a shifted key — the third take HITS the re-filed entry, the final take finds it moved on
pass	mdc1 a two-list merge-walk reuses one list WHOLE in one arm and advances it in the other — the join-point divergent-consume leak (merge-sort's minimal core)
pass	mde1 the two #map entry spellings (raw pair and = pair) build EQUAL maps (the #4589 dual-read; operator: maps+records unify on =)
pass	mdi1 a PITCH-CLASS walker over MIDI notes — transpose shifts the held note answering its class mod twelve, octave answers the note divided by twelve, two fifths then a double-octave drop then a fourth and a tone walk the circle, and the seeds' classes rotate through DIFFERENT residues while the octave rows differ by the same offset
pass	mdk1 a folded-duplicate-key map escapes as the canonical single entry (last write wins)
pass	mdk2 a folded-duplicate-key map EQUALS the map built by a single insert of the surviving entry
pass	mdk3 inserting at the folded-duplicate key overwrites the ONE canonical entry
pass	me1 a MIXED-endian frame crosses the dispatch boundary as an op ARGUMENT — the arm decodes big and little fields independently
pass	me2 the arm RE-ENCODES its two decoded fields with SWAPPED endianness and the body decodes the swap
pass	mec1 sorted enumeration AFTER remove-churn — five keyed inserts (one seed-colliding overwrite), two removes, the surviving key walk pins order and count
pass	member access chains through a nested record
pass	member access is written explicitly as the dot form in the canonical tree
pass	member access of a field the record does not have is a type error
pass	member access of a missing field is a type error
pass	member access of an absent field on a function-returned record is a type error
pass	member access on a boolean is a type error
pass	member access on a conditionally-chosen record, other branch
pass	member access on a map is a type error
pass	member access on a name-alias (map …) is a type error
pass	member access on a non-record is a type error
pass	member access on a record chosen by a conditional projects the field
pass	member access on a string is a type error
pass	member access on a tuple by NAME points at the numeric-index form
pass	member access on a tuple is a type error
pass	membership of a present element is true
pass	membership of a runtime element in a runtime-element set
pass	membership of a runtime symbol is by content, not derivation
pass	membership of an absent element in the empty set is false
pass	membership of an absent element is false, not a trap
pass	merge- and extend-REACHED records with runtime leaves equal the directly-built record
pass	merging records that share a field name is rejected
pass	merging records with a runtime field value carries both operands' fields
pass	merging records with runtime values on both sides binds each field to its own value
pass	merging two disjoint records then projecting one side recovers it
pass	merging two empty records is the empty record
pass	merging two records with disjoint fields unions their fields
pass	merging with the empty record on the left is the identity
pass	merging with the empty record on the right is the identity
pass	mf1 a helper performing TWO different effects — both handlers discharge it, both states advance per call
pass	mf2 the SAME two-effect helper under the OPPOSITE handler nesting order — distinct effects commute
pass	mf3 one performing helper called from an ARM's resume-value AND the body — three calls, one advancing thread
pass	mfp1 a nested #map element with an ABSENT const key over a runtime-value map falls through — the #5472 fence
pass	mfp2 the single-map-arm twin — absent key falls to the WILDCARD, not an error
pass	mge1 an effect performed in a match GUARD is rejected (CDZ0407 — guards must be side-effect-free)
pass	mha1 an op with TWO heap arguments — both read by the arm, both reclaim
pass	mha2 the arm resumes with the op's OWN heap argument as the answer (arg becomes answer)
pass	mha3 the arm TRANSFORMS the heap arg into the answer (prepend then resume)
pass	mhn1 a constant tuple holding a 33-element list child — double occurrence, projections, and runtime equality (mixed hoist declined today; value-fenced either way)
pass	mhn2 fifty frames persistently updating THROUGH the projection of a constant tuple's 33-element list child never corrupt the constant
pass	mhn3 a small constant list holding a 33-element list child — the gate applies per NODE, and the mixed outer declines while remaining value-correct
pass	mi1 enumeration DELTA across dispatches — dumps before and after keyed inserts, collision rows shrink the delta
pass	mi2 SORTED Set enumeration survives a draw-keyed insert — positional reads, the collision row exposes the missing third slot
pass	mi3 the arm AGGREGATES map values via a to-list walk — the n=1 row OVERWRITES the seed value, shrinking the total
pass	mia1 a MATCH over one op's Option result sits in ANOTHER op's argument position — unwrap-then-perform composed twice, verdicts flip
pass	mixed bool and int splices evaluate in one template
pass	mixed comparison operators across if arms keep their own arms
pass	mixed operators across the if arms keep their own arms
pass	mixed param-and-literal Float32 branches compute
pass	mixed payload widths across the variant set dispatch and read correctly
pass	mixed-sign Rational add and multiply normalize signs through one runtime chain
pass	mixed-type @param sites share one Param effect and drive control flow
pass	mixed: two closure exports alongside a plain export — driving the plain export
pass	mixing a prefixed unit across DIFFERENT dimensions is still a compile-time error
pass	mixing nullary and unary constructors in one match
pass	mixing signed and unsigned of the same width does not silently promote
pass	mixing two float widths without a conversion does not silently promote
pass	mixing two integer widths without a conversion does not silently promote
pass	mixing two integer widths without an explicit conversion is rejected
pass	mixing two numeric types without an explicit conversion does not silently promote
pass	mixing units of DIFFERENT dimensions is still a compile-time error
pass	mk1 a MAP handler state keyed by the op ARGUMENT — per-key counters, lookup-or-default then insert per dispatch
pass	mk2 map keys DERIVED from prior draws — n=1 collides the derived key with the first insert, shrinking the map
pass	mk21 a scalar-map handler whose second put uses a COMPUTED key — the checked-add scratch and the Option-handle slot stay width-partitioned
pass	mk3 the arm SHRINKS the map state via remove — re-removing the same key is idempotent, a missing key is a no-op
pass	mki1 a MAP+KEYSET invariant pair — every put and remove maintains set = keys(map) across both structures in one transition, and paired membership checks verify both sides agree; the n=5 seed collapses the two puts into one key
pass	mkv1 a MARKOV bigram counter — each feed keys the map by the (previous, current) PAIR built from the state and the argument, so the same value arriving after different predecessors lands in different buckets
pass	ml1 a Map whose VALUES are tuples — a draw-keyed insert of a drawn pair, lookups destructure both hit and neighbor
pass	mlr1 a Map.lookup-extracted INNER LIST bound by Some and only borrowed leaks the inner vec
pass	mlr2 a List.at-extracted INNER LIST consumed by a concat still leaks the extraction's retain
pass	mlr3 a TUPLE projection of a list field borrowed by List.len reclaims fully (no Option shell, no retain)
pass	mm1 a MAP of MAPS chains two runtime-keyed lookups through the cadenza hop
pass	mm1 a MIN/MAX tracking arm — three feeds tighten the (lo,hi) tuple state, readers project the final spread
pass	mml1 a MULTIMAP handler state — (Map Int64 (List Int64)) buckets grow per-key across dispatches; put answers the new bucket length and total sums a bucket later
pass	mmx1 a LAZY-INIT extrema tracker — Option (Tuple min max) state starts None, the first feed initializes both bounds to the value, later feeds widen, range reads answer max-min with the uninitialized read answering zero
pass	mnc1 a GREEDY change-making counter — pay walks the denomination ladder accumulating quotients by repeated divide-and-remainder answering the coin count, the seed decides whether a SEVEN-coin exists in the ladder, and amounts touching the seven diverge (two versus eight coins for eight cents) while seven-free amounts agree
pass	mns1 a MIN-TRACKING stack state — (stack, min) pushes thread the heap and the scalar together, mid-run and final min reads
pass	mo1 a TWO-op effect fully shadowed — the inner handler re-interprets BOTH ops with different arms
pass	mo2 THREE-deep same-effect shadowing — each depth's draws thread its own state, interleaved before/inside/after
pass	mo4 a SAME-effect draw in the inner handle's SEED homes to the OUTER handler — the shadow starts at the body, not the seed
pass	mo5 LIST-state shadowing — inner and outer thread independent heap lists, growth interleaved
pass	modular exponentiation by SQUARING halves the exponent and branches on parity
pass	modulo by -1 is zero even at the minimum integer
pass	modulo by zero traps
pass	modulo gives the remainder
pass	modulo of an integer and a float does not silently promote
pass	modulo of two floats rejects — the operator is integer-only, not merely promotion-free
pass	moi1 a runtime CHAMP built ascending equals the same keys built descending (insert-order canonicality)
pass	mp1 draws pick the Map INSERT key and the LOOKUP key — hit-old, hit-updated, and miss all reachable by input
pass	mp2 map VALUES are draws inserted in key order — weighted lookups replay the draw sequence through the map
pass	mp3 a draw picks the Map.remove key — lookups of all three keys show exactly one hole where the thread pointed
pass	mpf1 a MONOMORPHIC two-payload ctor's list field destructured by a nested pattern works
pass	mpf2 a GENERIC two-payload ctor's TUPLE field destructured by a nested pattern works (leaking like the family)
pass	mps1 two occurrences of one constant Map — branch-selected use, lookups, and runtime equality across the pair
pass	mps2 fifty frames alternating two constant Maps reclaim to zero
pass	mps3 fifty frames of persistent Map.insert against a constant Map never corrupt the constant (sibling lookup stays pristine)
pass	mps4 fifty frames of runtime-indexed lookups over an immortal 40-key deep CHAMP hit every key and the miss cell
pass	mps5 fifty frames of persistent Map.insert against an immortal 40-key deep CHAMP never corrupt it (interior-node copy fence)
pass	mr1 THREE Map.remove GENERATIONS share structure — each ancestor still holds what its descendant removed
pass	mr1 mutual recursion drawing at EVERY level — even levels weight their draw x10, odd levels x1
pass	mr2 the mutual pair draws from DIFFERENT effects — ev advances P, od advances Q, both threads interleave down the descent
pass	mr3 the NEXT callee in the mutual group is picked by draw parity — the descent path itself follows the thread
pass	mr4 an ACCUMULATOR threads the mutual pair — ev doubles it plus a draw, od triples it plus a draw, alternating scales
pass	mr5 TWENTY alternating mutual levels with the scaling accumulator — depth stress on the cross-function fold
pass	mrd1 a byte-string literal and a const-folded Bytes.of with identical content compare equal at runtime
pass	mrd2 a Blake3-baked constant Bytes used twice with drops between — equal hashes, thirty-two bytes each, no live objects
pass	mrf1 a macro-introduced RECURSIVE helper computes like its plainly-written twin (should-work: expansion precedes the core guarantees)
pass	mrf1 fifty frames of persistent Map.remove against an immortal 40-key deep CHAMP never corrupt it (the remove-side FBIP fence)
pass	mrf2 a runtime-map remove drops the length, misses the removed key, and keeps the neighbors
pass	mrg1 a TWO-POINTER MERGE across two handlers — each holds a sorted list, the body pulls whichever head is smaller each step, and the sentinel head lets one side drain
todo	mrs1 a two-resume arm over a HEAP-allocating body declines cleanly (non-tail resume, later increment)
pass	mrs2 a PURE-body two-resume arm re-computes the continuation and sums (multi-shot on a re-computable body = 100; v-effects-ruled intended)
pass	mrt1 a METRONOME with a downbeat accent — tick advances the beat wrapping at the seed-shaped bar length (counting bars), accent answers a hundred plus the bar count ON a downbeat or the beats remaining until the next one, and the four-beat bar catches the second accent ON the downbeat while the three-beat bar misses both
pass	mrv1 REMOVE-then-REINSERT churn on a Map state — del answers the removed value (0 when absent); for n=98 the second del hits the n+1 key it planted
pass	ms1 a WHOLE nested handle expression as a MATCH's SCRUTINEE — the region builds an Option, the chosen arm draws again
pass	ms1 an op returns a THREE-variant sum built from state parity — two sequential performing scrutinees, the second sees the advanced state
pass	ms2 match ARMS themselves draw after the performing scrutinee advanced the state — arm-selected continuation of the same thread
pass	ms3 parity-sum dispatch inside a RECURSIVE walk — each level draws a Mode and accumulates by variant as the thread advances
pass	ms4 the C arm RE-MATCHES its own payload's parity — a nested pure match inside an arm of the performing-scrutinee match
pass	msc1 a masked runtime shift count wraps mod the mask (>> x (& k 63))
pass	msc1 a tail-mutual performer pair folds with per-member state advance (the #4575 shape, independent trace)
pass	msc2 a THREE-function tail SCC (a→b→c→a) folds with ordered state advance
pass	msc2 a masked runtime left-shift count elides the count guard but keeps the value-overflow trap
pass	msc3 a mutual pair where only ONE member performs folds (degenerate half-performing SCC)
pass	msc3 a narrow UInt8 masked left-shift with an out-of-width count traps
pass	msr1 a branch-selected owned STRING scrutinee dispatched by literal arms reclaims
pass	msr2 a branch-selected owned BYTES scrutinee dispatched by a literal arm reclaims
pass	msr4 a runtime-BUILT Ast scrutinee matched and reduced to a scalar reclaims
pass	msr5 an owned TUPLE-with-heap-element scrutinee reclaims shell and element
pass	msr6 a NESTED match — outer list arm rebinds inner records — reclaims both shells (#3778)
pass	mts1 a Map whose VALUES are TUPLES — per-key (count,sum) stats update through tuple rebuild inside the arm, the answer packs the fresh pair
pass	multi-argument application is curried application
pass	multi-export Set-result closures — the singleton one
pass	multi-export Set-result closures — three sharing one call
pass	multi-export collection result — the first list closure
pass	multi-export collection result — the second list closure
pass	multi-export compound result — the first closure's tuple
pass	multi-export compound result — the second closure's tuple
pass	multi-export compound result — three capturing closures share one call
pass	multi-export record result — canonical field order
pass	multi-export record result — the second closure, with a negative leaf
pass	multi-limb BigInt map keys stay distinct and look up by full value
pass	multi-limb BigInt set elements deduplicate by full value
pass	multi-limb BigInts equal in the LOW limb compare by the HIGH limb
pass	multi-shot resumes carry DIVERGENT states; two performs branch 2x2
pass	multibyte symbols order by UNSIGNED content bytes - a CJK scalar sorts after z
pass	multiplication
pass	multiplication of an integer and a float does not silently promote
pass	multiplication of the minimum integer by -1 overflows and traps
pass	multiplication past the maximum integer overflows and traps
pass	multiplication- and division-REACHED runtime Rationals equal the constructed canonical form
pass	multiply-by-negative-one strength reduction keeps its operand's own trap
pass	multiply-by-zero is NOT an annihilator for FLOAT — nan * 0.0 is nan, not 0.0
pass	multiplying a compound (tuple) by a number is a cross-kind type error, not invalid wasm
pass	multiplying a runtime integer by negative one is checked negation in both operand orders
pass	multiplying a scaled-unit quantity by the literal one keeps its dimension and displays scaled
pass	multiplying quantities multiplies their dimensions
pass	must-hold: a heap-param entry + a NON-recursive nested fn CAPTURING the param compiles and runs
pass	mutual recursion over an Ast value const-folds a leaf count (under Ast.encode demand)
pass	mutually-recursive do-local functions nested in an inlined helper recurse
pass	mutually-recursive newtypes erase and construct and match end to end
pass	mutually-recursive variant payloads do not false-positive as unknown types
pass	mwv1 a MIXED-WIDTH scalar-payload variant host ARG delivers each arm's payload
pass	mwv2 the SAME mixed-width variant wrapped in a RECORD host arg
pass	mx1 a THREE-arg op mixing Int64/String/Bool — one arm consumes all three kinds beside the live state
pass	mx1 mutual recursion with a (List Int64) accumulator (scalar element control)
pass	mx2 mixed-arg op with BOTH args draw-derived — the int arg is a draw, the string arg branches on a second draw
pass	mx3 a TUPLE-arg op chained through a tuple RESULT — the second dispatch consumes the first's destructured output
pass	mx4 a FOUR-param op mixing two Int64s, a NARROW UInt8, and a Bool — the narrow slot rides between wide ones and the flag doubles the first argument
pass	named member access on a List redirects to the List operation module
pass	named member access on a Map redirects to the Map operation module
pass	named member access on a Set redirects to the Set operation module
pass	named member access on a String redirects to the String operation module
pass	named member access on a sum redirects to a match template over its variants
pass	named member access on a user sum spells its own variants and payload binders in the match template
pass	nan1 NaN EQUALITY is canonical-byte TRUE while NaN ORDERING is IEEE-unordered FALSE — the float-comparison asymmetry
pass	narrow Int32 (if cmp 1 0) materializes correctly and a non-0/1 branch pair keeps its values
pass	narrow signed power-of-two div/rem truncates toward zero (Int32)
pass	narrow unsigned comparison division and remainder compose unsigned over a high-bit operand
pass	narrowing a BigInt-valued if back to Int64 works over both branches
pass	narrowing a constant BigInt out of the target range is rejected at compile time
pass	narrowing a runtime Int64 to a fixed width with .of range-checks (in range passes, out of range traps)
pass	narrowing a runtime UInt64 to UInt8 with .of range-checks (in range passes, out of range traps)
pass	native #list equality is ORDERED — a different element order is NOT equal
pass	native #map equality is by entry set, order-INDEPENDENT
pass	native #record equality is by field set, order-INDEPENDENT
pass	native #set equality is by element set, order-INDEPENDENT
pass	native #set of #tuples is order-independent over compound elements
pass	native nested #record-in-#list equality recurses
pass	nb1 a perform under NOT in a condition (the negated dispatch gate)
pass	ncb1 a narrow signed (+ a 1) by a constant drops the dead lower bound, keeps the upper edge
pass	ncb2 a narrow signed (- a 1) by a constant drops the dead upper bound, keeps the lower edge
pass	ne1 an OPTION built from a draw crosses dispatch as an op ARGUMENT — the arm matches it against the live state
pass	negating a runtime Int64 near the boundary is exact just above the min
pass	negating a runtime Int64 overflows at Int64.min
pass	negating a runtime narrow integer just above its min is exact
pass	negating a runtime narrow integer traps at the width's own min, not the Int64 min
pass	negation inverts a boolean
pass	negative integer to byte uses two's complement
pass	negative multi-limb BigInt div truncates toward zero and rem takes the dividend's sign
pass	negative zero and zero are distinct map keys
pass	negative zero crosses the parameter boundary preserving its sign
pass	negative zero encodes to bytes distinct from positive zero
pass	negative zero is distinct in the canonical value form
pass	negative zero is not equal to positive zero
pass	negative zero round-trips through the codec by byte identity
pass	negative zero stays distinct from zero through the hoisted float compare
pass	nei1 cross-effect interleave: an inner B handler's arm performs the OUTER A effect, whose state advances across two B draws
pass	nested MIXED-direction shifts do not combine — left-then-right is two operations
pass	nested SAME-effect handlers isolate STATE — inner performs never advance the outer counter
pass	nested handlers with colliding arm-local bindings each keep their own scope (no compounding leak)
pass	nested mixed-match tail loops compose — an outer accumulator loop driving an inner seeking loop
pass	nested patterns deconstruct recursively
pass	nested patterns with literals
pass	nested quasiquote embeds a FLOAT via the inner unquote
pass	nested quasiquote: a double unquote at level 2 decrements back to active and splices (exact structure)
pass	nested quasiquote: a level-1 unquote splices even while a level-2 one is deferred (same template)
pass	nested quasiquote: a level-2 deferred unquote is INDEPENDENT of the enclosing binding
pass	nested quasiquote: an inner unquote at level 2 is deferred, not spliced (exact structure)
pass	nested recursive performers THROUGH A NON-RECURSIVE PASS-THROUGH also fold (the pass-through inlines to the recursive leaf)
pass	nested recursive performers through a non-recursive intermediate — out-state threads across the hop
pass	nested recursive performers — an inner walk's out-state threads across the outer recursion boundary
pass	nested same-direction shifts by constants combine into one shift by the sum, keeping the left-shift overflow trap
pass	nested-Option equality observes the OUTER variant and matches identical nesting
pass	neu1 KAHAN COMPENSATED SUMMATION as a handler state — the (sum,comp) pair recovers a small addend that naive summation absorbs at the 2^53 boundary, the naive control confirms the absorption
pass	nfc1 runtime-constructed strings are NFC-normalized on every backend
pass	nfe1 runtime +inf Ast.Float encodes to non-empty bytes (op93 non-finite tag path)
pass	nfe2 +inf and -inf runtime encodes have EQUAL byte length (sibling tags 18/19)
pass	nfe3 tuple-walk equality says +inf and -inf encodes DIFFER (distinct tags)
pass	nfe4 two runtime NaN encodes are byte-identical via the tuple walk (canonical NaN form)
pass	nfp1 Ast.print renders a runtime +inf Ast.Float as inf.0
pass	nfp2 Ast.print renders a runtime NaN Ast.Float as NaN.0
pass	nfp3 Ast.print renders a -inf leaf inside a list as -inf.0
pass	ng1 the arm NEGATES alternate draws — a Bool flip in a tuple state signs the rising thread (+,-,+)
pass	nh1 nested handlers with LIST state at BOTH levels — both reclaim
pass	nh2 the inner arm DELEGATES to the outer with heap state at both levels
pass	nh3 the inner handle RESULT is a heap value consumed by the outer body
pass	nim1 a NESTED constant sum immortal is FBIP-safe — a map over it does not mutate the original
pass	nim2 nested constant-sum immortals differing at DEPTH stay distinct (deep mark does not conflate)
pass	nk1 two runtime-computed NaNs dedup to ONE set member (canonical NaN)
pass	nk2 +inf, -inf, and NaN are THREE distinct set members
pass	nk3 a NaN Map KEY is findable by another runtime NaN (hash and equality agree)
pass	nl2 the nested-record binder in LIST-ELEMENT pattern position — the head record's field binds, the rest carries full records
pass	nm1 NEGATIVE remainder is TRUNCATED (sign of the dividend) uniformly — bare and through a handler arm
pass	nm1 two nested constant masks collapse to their bitwise-AND (value parity)
pass	nm2 NEGATIVE division TRUNCATES toward zero uniformly — bare and through a handler arm
pass	nm2 a left-constant nested mask collapses to a single AND
pass	nm3 a non-subset nested mask pair collapses correctly
pass	nm3 the only overflowing DIVISION (Int64.min / -1) is a CONSTANT-fold reject — the runtime-divisor form runs for every other divisor
pass	nm4 Int64.min divided by RUNTIME divisors — every non-(-1) divisor has an exact Int64 quotient
pass	nm4 a narrow Int8 nested mask grounds the folded constant to the width
pass	nmb1 a RUNTIME Int8 multiply by -1 computes in range and traps at the MIN/-1 boundary (the narrow-width runtime companion of the checked-mul ladder)
pass	nmm1 a TWO-LEVEL Map state — group puts lookup-modify-reinsert the inner CHAMP, answers pack both level sizes, reads route through both levels with a miss sentinel
pass	no forgotten-call hint when the expected type is itself a function (fn-vs-fn mismatch)
pass	no forgotten-call hint when the fully-applied result would still differ from the expected type
pass	no over-conservatism: a trap-free masked operand still folds the tautology
pass	no1 an inner-op arm resume value performing an outer op that reads the inner state folds (the #4540 re-bind, independent shape)
pass	no2 TWO outer performs in one inner-arm resume value fold with ordered state advance
pass	no3 the inner state read both AROUND and INSIDE the outer perform folds
pass	non-finite floats cross inside a compound as their own leaves, not collapsing the compound
pass	noo1 an op whose result is NESTED Option (Option Int64) — the arm classifies the state into None / Some None / Some (Some s), the body's nested match distinguishes all three in one run
pass	not over a narrow Int32 comparison folds to the complement
pass	not over an unsigned comparison uses the unsigned op
pass	not over each Int64 comparison folds to the complement (value parity)
pass	np1 a NESTED record value built from one draw — projections through two levels read the same drawn base
pass	npd1 a nested TUPLE destructure inside a Some arm survives the cadenza hop byte-idempotently
pass	npd2 a nested RECORD destructure inside a Some arm folds Proj-over-record-literal at FIRST emit — the #5137 fixpoint fence
pass	nrr1 an export returns a list of two tuples (four reachable cells, compositional)
pass	nrr2 an export returns a Some-wrapped list (three reachable cells)
pass	nrr3 an export returns a record with a list field (three reachable cells)
pass	nrr4 an export returns a list of lists (six reachable cells)
pass	nrr5 an export returns a tuple carrying a runtime String (two reachable cells)
pass	nrs1 a ROVER state — nested record {pos:{x,y}, steps}; each move applies SIGNED deltas to both inner fields via Record.with and answers the manhattan distance
pass	ns1 a RECORD rides inside a sum payload through dispatch — the arm builds it from two draws, the body matches then projects
pass	ns2 the sum-wrapped record round-trips FLATTENED — matched, field-updated with a draw, re-wrapped, echoed, re-matched at top level
pass	ns3 TWO record-payload variants selected by state parity — each arm projects its own record shape
pass	ns4 a record payload whose FIELD is itself a sum-wrapped record — helper unboxers traverse two wrap layers built from one dispatch
pass	ns5 the nested two-layer match FOLDS when the scrutinee is a pure literal — only op-built scrutinees push it off the fold
pass	nsa1 a narrow Int8 addition computes at the max boundary and traps one past it
pass	nss1 a narrow Int8 subtraction range-checks both edges
pass	nst1 the arm's NEXT-STATE expression divides by a state-derived quantity — both signs thread, the zero seed traps
pass	nt1 a NESTED tuple state (a (b c)) — the arm destructures two levels and rebuilds with three different strides
pass	ntk1 NESTED-tuple keys — structural equality one level down, map lookup + set dedup
pass	nts1 a non-tail recursive-sum consumer over a SHARED spine does not over-reclaim it (sum then len both read it)
pass	nullary constructor patterns are uniform with unary
pass	nullary variants of a payload-carrying sum order by discriminant
pass	nwc1 a produced non-aliased signed width crosses widened and sign-extended (Int24 wrap)
pass	nx1 a SUBTRACTING stride crosses zero — negative states thread and sum correctly
pass	nx2 a 2^62 seed threads exactly — the difference of consecutive draws recovers the stride
pass	nx3 the arm branches on the op arg's SIGN — negation on the negative path, n and -n both exercised
pass	nx4 an alternating-sign GEOMETRIC stride (*-2) — the sign flips per dispatch and the sum telescopes
pass	nx5 i64::MIN-adjacent op arguments — the arm's subtraction stays in range and the two dispatches differ by exactly the state stride
pass	nz1 bare = distinguishes +0.0 from a TRUE runtime -0.0
pass	nz2 {+0.0, -0.0} are TWO set members
pass	nz3 -0.0 is NOT a member of {+0.0}
pass	nz4 a Map discriminates +0.0 and -0.0 keys
pass	nz5 the zeros compare = only to themselves yet BEHAVE differently under division
pass	oa1 an Option-returning op's None arm aborts LOCALLY — the local bail-out in a match arm homes correctly
pass	oak1 an op ARGUMENT of type Rational feeds exact arithmetic into the arm
pass	oak2 an op ARGUMENT of type BigInt accumulates multi-limb in the state
pass	oak3 an op ARGUMENT of type Map is queried by the arm and drives the state
pass	oax1 two nested OR-by-constants collapse to their bitwise-OR (value parity)
pass	oax2 two nested XOR-by-constants collapse to their bitwise-XOR (value parity)
pass	oax3 a double complement (^ (^ x -1) -1) is the identity
pass	oc1 an Option-of-TUPLE accumulator — None seeds on first step, Some carries (total,count) advancing both
pass	oc2 an Option FLOWS between two ops of one effect — find produces Some/None, use consumes it as an op ARG
pass	oc3 a DOUBLE-wrapped Option resume value — None vs Some(None) vs Some(Some v) all distinguished by the body
pass	oc4 an Option state with a RESET cycle — put promotes None to Some or accumulates inside Some; reset reports and returns to None; a fresh put re-promotes
pass	oc5 TWO Option slots in one tuple state — independent put lifecycles, a SWAP op exchanges them, reads confirm the exchange
pass	oc6 an Option-of-TUPLE state — the Some payload is a (total,count) pair updated element-wise per dispatch, a final read reports both
pass	od1 lexicographic order through an immortal 33-trie discriminates at the last element (both directions + shorter-prefix)
pass	od2 string order across fifty rope seams discriminates at the final divergent char
pass	odd-width CHECKED arithmetic traps at the odd boundary, not the machine slot (Int24 max + 1)
pass	odd-width values as Set keys dedupe at their own width (UInt4: wrap 3 = wrap 19)
pass	odd-width wrap lands exactly at its own boundary (UInt4 16->0, Int24 2^23 -> -2^23)
pass	odf1 a TOKEN-BUCKET rate limiter with overdraft penalties — spend succeeds only when the bucket covers it (else a penalty tick and a 0 answer), refill saturates at the cap, and the final draw reads the accumulated penalty count
pass	odo1 a BASE-100 ODOMETER — the list of wheels absorbs each tick with a CASCADING CARRY that rebuilds cells by List.update and GROWS the list when the top wheel overflows
pass	olc1 an Option-of-LIST handler state lifecycle — None is uninitialized, push initializes-or-appends, take scores and RESETS to None, a later push re-initializes
pass	olf1 String ordering folds agree with their runtime twins (proper, prefix, and empty edges)
pass	olf2 Bool ordering folds agree with their runtime twins
pass	olf3 List ordering agrees const and runtime, inlined and let-bound
pass	olf4 a BARE empty list orders against a populated sibling once inference unifies before the orderability check
pass	olf5 a NESTED bare empty list orders against its populated sibling (the unify-then-check recursion)
pass	one @param accessor performed TWICE consumes two host responses in order
pass	one @param read bound ONCE fans out to a map key, a set probe, and arithmetic
pass	one RRB list shared as two map VALUES diverges by List.update without cross-talk
pass	one effect's result flows as the ARGUMENT to a DIFFERENT effect's op under nested handlers
pass	one encode-decode cycle is byte-stable
pass	one entrypoint's host authority is not reachable by another that does not delegate it
pass	one generic identity instantiated at a scalar, a heap string, and a compound in one program
pass	one generic sum instantiated at two different types in one program
pass	one host effect with TWO ops interleaves its calls in program order
pass	one module's export clause does not affect a same-named member of another module
pass	one of several same-signature closure exports is made and called by the host
pass	one of two DIFFERENT-signature closure exports is made and called
pass	one perform result flows through let, record, projection, tuple, destructure, and match
pass	one performing closure applied twice observes the handler state stepping between calls
pass	one staged partial application fans out to TWO second stages sharing the first capture
pass	one string content hashes identically from flat, rope, and view reps in one program
pass	one type parameter used at several depths in one multi-payload variant
pass	oos1 a NESTED-Option STATE ladder — None to Some None to Some Some v and back, advance climbs one rung per dispatch and classify decodes all three inhabitants between steps
pass	op1 a WHOLE nested handle expression as an op's ARGUMENT beside an outer draw
pass	op1 the STD Option as handler state — None seeds to Some on first feed, the payload accumulates thereafter
pass	op2 an Option RESUME value from a single-site arm — Some carries the excess, None answers the shortfall row
pass	ope1 an Option.expect payload consumed per-iteration is retained (no drift)
pass	ope2 a chained Option.expect(Option.expect s) payload consumed per-iteration is retained
pass	ope3 a Unit-payload Option.expect beside a sum-match runs (IMM_UNIT sentinel dropped, valid module)
pass	ope4 a chained Unit-payload Option.expect runs (nested sentinel drop, valid module)
pass	opposite-direction comparisons over a disjoint gap fold their and to false
pass	opposite-direction comparisons that cover the line fold their or to true
pass	oqc1 an extraction-Some payload consumed by an OPAQUE user-fn call reclaims (the call balances the retain)
pass	oqc2 an extraction-Some payload consumed by a CLOSURE application reclaims likewise
pass	oqc3 an extraction-Some payload consumed by a RECURSIVE callee leaks the retain (the true Stage-B negative witness)
pass	oqc4 an extraction-Some payload passed as an op ARGUMENT balances at the dispatch boundary
pass	or evaluates the right operand when the left is false
pass	or performs the right operand's effect when the left is false
pass	or short-circuit does not perform the right operand's effect
pass	or short-circuits at run time: a true left operand skips the trapping right operand
pass	or1 an open-row projector applied to TWO record widths where one field is a fresh DRAW — per-call-site slots under effect state
pass	or2 the handler ARM builds a record and the body projects it open-row at two widths — arm-built rows cross the dispatch boundary
pass	or3 a RECORD handler state projected open-row inside the arm — the arm's row instantiation is independent of the body's
pass	ordering a boolean against a number names the scalar kind boundary, not an internal clash
pass	ordering a string against an integer is a type error regardless of operand order
pass	ordering a tuple against a list is a cross-kind type error
pass	ordering a tuple against an integer names the kind boundary
pass	ordering an integer against a boolean is a type error
pass	ordering an integer against a float is rejected, not silently promoted
pass	ordering an integer against a string is a type error
pass	ordering two integers of different sign is rejected, not silently reinterpreted
pass	ordering two integers of different width is rejected, not silently widened
pass	ordering two rationals a hair apart at the i64 boundary is exact
pass	ordering two same-kind tuples of different arity names the arity delta, not a kind boundary
pass	org1 an ORGAN with three ranks and a stop selector — chord passes THREE pipe volumes and the state's selector picks WHICH argument sounds through a nested-if ladder folding it into the played total and stepping the selector two ranks around, swell re-aims the selector by the played total leaving the total untouched, and the seed sets the opening stop so the two runs sound different ranks from the same three-volume chords at every dispatch
pass	orp1 an open-row projection ignoring a heap-list sibling eliminates the dead field at compile time
pass	orp2 an open-row helper projects a field carrying a performing draw through the fold
pass	orq1 an ORDER-BOOK spread tracker — bid raises the best bid and ask lowers the best ask (both answering the spread or -1 while a side is empty), a CROSSED book resets both sides answering zero, and the seed only shifts the opening bid so the first spread differs while the tightening ladder and the cross land identically
pass	osx3 a Bytes.slice of an immortal constant source matched by Some and borrowed leaks the view
pass	osx4 the immortal constant source alone is census-excluded (the zero baseline for osx3)
pass	otc1 Map.len over an owned-temporary map reclaims it (no live objects)
pass	otc2 Set.len over an owned-temporary set reclaims it (no live objects)
pass	otc3 a borrowed map read by Map.len then reused by an insert is not freed early
pass	otc4 300x Set.contains over an owned-temporary set each reclaims (no leak/UAF drift)
pass	otc5 300x Map.lookup(Some) over an owned-temporary map each reclaims AFTER the value-dup (no UAF)
pass	outer handler state threads AROUND a completed inner handle of a different effect
pass	ov1 an if CONDITION from LET-bound draws — two draws feed a comparison that routes the branch
pass	ovb1 a const OVERFLOW branch not taken computes the taken branch (the value face, like dzb1)
pass	ovb2 the SAME overflow branch traps 'integer overflow' at RUNTIME when the condition takes it
pass	ovb3 a shift-COUNT-out-of-range branch stays the kind-less 'unreachable' (the discrimination)
pass	ovb4 the OTHER Div overflow — Int64.min / -1 — demotes to the overflow kind (not divide-by-zero)
pass	over-applying a bare variant constructor names the constructor and its arity
pass	over-applying a constructor by several arguments is a type error
pass	over-applying a constructor is a type error, not a silent argument drop
pass	over-applying a lambda by an extra argument is a type error
pass	over-applying a member op names the operation and its arity with a delete-surplus fix
pass	over-applying a named function by an extra argument is a type error
pass	over-applying an arity-1 member op uses the singular argument
pass	overflow of the default integer traps deterministically
pass	overwriting ONE deep list value leaves the neighbors AND the original map live
pass	ow1 a wrap PRAGMA round-trips the cadenza hop as explicit per-node wrapping members
pass	pa1 a SAME-effect perform as another op's ARGUMENT — the arg dispatch advances the state the outer dispatch reads
pass	pa1 two SIBLING nested handles as the two arguments of one 2-ary op — each region draws the outer thread in arg order
pass	pa2 TWO same-effect draws as the TWO arguments of one op — left-to-right arg order, the arm reads the post-args state
pass	pa3 THREE-deep nested same-effect arg-feed — scale(scale(next)) with a state-advancing scale arm
pass	pah1 a heap LIST argument crosses INTO the peer and is consumed by the provider (arg-direction lowering)
pass	pak1 a peer op returning a (List of a user sum) crosses and its length is read
pass	pal1 PARITY-ALTERNATING two-effect draws — one recursive driver picks which effect to draw per hop, both threads advance interleaved
pass	parsing a length-framed message and rebuilding it yields the original bytes
pass	partial application captures a let-bound value in the residual lambda
pass	partial application captures a runtime parameter in the residual lambda
pass	pas1 PASCAL'S TRIANGLE row advanced through effect state — next rebuilds the row by a recursive pairwise-sum answering the row total (a power of two), coef reads the SEED-KEYED binomial coefficient mid-descent and after, and the out-of-range read answers -1
pass	passing one newtype where a different newtype is expected names both nominal types
pass	pattern matching over AST distinguishes forms
pass	pbi1 a peer op returning a BigInt crosses as a handle and value-equality holds
pass	pbk1 a request-struct {prompt,max-tokens} crosses to a peer and its String completion escapes
pass	pbr1 PER-BRANCH resume with different value AND stride — the else branch jumps the state 50, flipping the next dispatch's route
pass	pbr2 THREE-way match-branch resumes — each residue class answers and strides differently, three dispatches walk the classes
pass	pby1 a peer BYTES result escapes the entrypoint via the with-methods fused envelope
pass	pca1 a map argument crosses INBOUND to a peer and its length is read there
pass	pca2 a set argument crosses INBOUND to a peer and its length is read there
pass	pca3 a record of a string and a scalar crosses INBOUND to a peer, both fields read there
pass	pca4 a tuple of a string and a scalar crosses INBOUND to a peer, both fields read there
pass	pcc1 a partial ctor in an inline tuple completes when projected and applied
pass	pcc1 fifty peer-returned lists consumed on the consumer side reclaim to zero (cross-boundary census)
pass	pcc2 a partial ctor in a let-bound tuple completes when projected and applied
pass	pcc3 a partial ctor in a record field completes when projected and applied
pass	pcl1 a peer op returning a list crosses as a handle and its length is read
pass	pcl2 a peer op returning a tuple with a variable-length list element crosses, both fields read
pass	pcl3 an element of a peer-returned list is read and used
pass	pcm1 a peer op returning a map crosses as a handle and its length is read
pass	pcm2 a value of a peer-returned map is read by key and used
pass	pcm3 a peer-returned map enumerates via Map.to-list and its values fold to a sum
pass	pcm4 a peer op returning an EMPTY map crosses and reads length zero
pass	pcm5 a peer op returning a map of lists crosses and a value list's length is read
pass	pcm6 a peer-returned map enumerates in canonical key order regardless of insert order
pass	pcm7 a peer-returned map accepts a further local insert over the shared runtime
pass	pcs1 a peer op returning a set crosses as a handle and its length is read
pass	pcs2 membership of a peer-returned set is queried for a present and an absent element
pass	pcs3 a peer op returning an EMPTY set crosses and reads length zero
pass	pcs4 a peer-returned set unions with a locally-built set over the shared runtime
pass	pd1 a list shared into a RECORD field survives an update through the original binding
pass	pd10 a shared BYTES operand survives a concat — both views keep their own len
pass	pd11 a shared STRING operand survives a concat — both views keep their own byte-len
pass	pd2 a helper updates its list PARAM — the caller's original is intact after the call
pass	pd3 a CLOSURE capturing a list sees the original across an interleaved update
pass	pd4 a RECURSIVE per-level List.update chain zeroes a copy while the original survives
pass	pd5 TWO divergent update aliases from one list each see only their own write
pass	pd6 a shared update at index 35 of a 40-element list crosses the RRB leaf boundary persistently
pass	pd7 a List.update inside a HANDLER ARM's resume value leaves the original readable in the continuation
pass	pd8 a shared MAP operand survives an insert — both views keep their own len
pass	pd9 a shared SET operand survives an insert — both views keep their own len
pass	pdl1 a PENDULUM with friction — each swing hands the active quantity across losing one (the phase field tracks which side holds it), a dead pendulum answers -1 forever, and the tall drop is still swinging at the sixth swing while the short one dies at the fifth with the zero-crossing row pinning the exact stop
pass	performing a duplicate-operation effect reports one declaration-site error, not a leaked record-field consequent
pass	performing a string-parameter operation with a non-string argument is a type error
pass	performing an op with a Bool argument under an Int64-parameter operation is a type error
pass	performing an op with a String argument under an Int64-parameter operation is a type error
pass	performing an operation with a wrong-type argument for a compound parameter is a type error
pass	performing an operation with an argument of the wrong type is a type error
pass	performs INLINE in record fields fold (records are not a strict-ctor boundary)
pass	performs as Map keys: each key a distinct state read, lookup by the computed key
pass	performs as Set.of elements build the set with stepping state and dedup applies to the RESULTS
pass	performs as list-literal elements land at their POSITIONS in perform order
pass	performs in BOTH and-operands — the second fires when the first passes
pass	performs in BOTH operands of an or consume state only on the reached paths
pass	performs in DISCARDED do positions still run — the effect count is the observable
pass	performs in match-arm bodies fire ONLY for the selected arm — counter witnesses the count
pass	performs in the ELEMENTS of a tuple / list CONSTRUCTOR thread left-to-right
pass	performs in the FIELD VALUES of a RECORD constructor thread in written order
pass	performs in the VALUES of a MAP constructor thread and the entry reads back correctly
pass	permutation invariance of a commutative fold holds for generated runtime inputs
pass	permutation invariance of a commutative fold holds — the property oracle passes
pass	pf1 an AGGREGATOR arm — the op returns the running sum of every value fed to it, three feeds pin the accumulation
pass	pfx1 a PREFIX-SUM table state — add appends the running total to the list, range answers pre[j]-pre[i] via two fallible reads with an out-of-bounds sentinel
pass	pg1 a PUT/GET store — put returns the value it displaces, a counter tracks writes, get reads the survivor
pass	pgn1 a PAGINATION cursor over a seed-sized collection — next serves up to the page size answering the count actually served (the LAST page runs short), a drained cursor answers -1, rewind resets answering the pages served, and the smaller collection drains one page EARLIER so its short page and its -1 land at different rows
todo	phr1 an export returning its lifted List param verbatim declines (ownership-transfer fence)
todo	phr2 an export with a lifted List param returning a FRESH list declines (the no-flow admission edge)
todo	phr3 an export extracting a param element into a fresh returned list declines (copied flow)
pass	phs5 inner guard made CONSTANT (latent never checked) — each tick delivers one quantum which either RAISES the temperature by a degree or, exactly at the melting point with latent heat unpaid, pays two units of latent answering a tagged fifty-plus row, and the warm seed climbs straight through while the cold seed hits the plateau and stalls there
pass	pid1 an integer P+I CONTROLLER stepping a plant toward the setpoint 20 — each step folds the error into the integral, applies u = (2*err + integral) / 4 with a NEGATIVE dividend on the overshoot face, and the closing draw reads the wound-up integral
pass	pim1 constant-payloaded immortal variants keep payload identity and stay census-excluded (scalar + deep heap child)
pass	pir1 a sum of two masked nibbles is guard-free and exact
pass	pir2 a masked nibble times three is guard-free and exact
pass	pir3 a narrow UInt8 sum of two masked values fits the width guard-free
pass	pir4 a masked-nibble subtraction with a negative result is guard-free and exact
pass	pir5 a chained masked-nibble sum propagates its range and stays guard-free
pass	pjr1 a consuming projection of a let-bound tuple's list child is retained (re-projection reads original)
pass	pjr2 a doubly-nested projected list consumed then re-read is retained (projection-depth face)
pass	pk1 a BEGIN/END protocol arm — a Bool flag rejects double-begin and end-without-begin, the sequence encodes the violations
pass	pkc1 fifty let-bound multi-consumed peer results reclaim to zero (the leak keys to escape, not consumption)
pass	pkg1 FIRST-FIT bin packing over two capacity-ten bins — place walks the bins in order answering bin-index-times-a-hundred plus the remaining room (or -1 when nothing fits), loads packs both levels, and the seed's first item cascades every later placement
pass	pla1 a list argument crosses inbound to a peer and is read by the peer op
pass	plr1 a peer LIST result escapes the entrypoint via the fused envelope
pass	plt2 the LOOP-DRIVEN plateau tracker — the same three-let two-if arm that declines straight-line folds fine when one recursive driver walks the feed list
pass	pm1 LITERAL match arms grade the state INSIDE the handler arm — a mod-4 walker crosses all four literal rows
pass	pnc1 a peer op returning a nested compound crosses as one handle and is projected
pass	pop1 a POPCORN kettle — each heat raises the temperature five degrees answering how many NEW kernels popped (thresholds every three degrees from the seed base, the count derived by differencing the running total), bowl reads the total, and the cool kettle pops a single late kernel while the hot one accelerates through five
pass	popping a field yields its value and the remaining record
pass	popping a tuple with a runtime element separates the head from the rest
pass	popping a tuple yields element zero and the remaining tuple
pass	popping an absent field is rejected
pass	por1 a peer Option result escapes the entrypoint via the fused envelope
pass	pp1 a PING-PONG data thread across two handlers — A's answer feeds B's argument and B's answer feeds A's next argument, additive and modular-multiplicative arms advance independently
pass	pq1 a PRIORITY-POP protocol — pushes append unsorted (priority,value) pairs, popmin scans for the strict minimum priority (first-of-equals wins) answers its value and removes it by filtered rebuild
pass	pr1 List.prepend onto a MERGED vector grows the front without disturbing the seam
pass	prefix `(- x)` as an OPERAND of another operator is a partial subtraction, suggest-`Num.neg` at the operand-mismatch site too
pass	prefix `(- x)` in value (annotation) position is a partial subtraction, rejected with a suggest-`Num.neg` (CDZ0203)
pass	prepend across the RRB 32→33 boundary front-inserts and reads back at every index
pass	prepend leaves the original list binding unchanged (persistent, copy-on-write)
pass	prepend shifts the existing elements one position toward the tail
pass	prepending an element of a different type onto a list is a type error
pass	prepending an element onto a list inserts it at the front
pass	prepending onto the empty list yields a one-element list
pass	print of a NEGATIVE Ast.Float round-trips through read (sign survives the text path)
pass	print of a negative-zero Ast.Float preserves the sign through read (text-path signed-zero)
pass	print of a quote containing a float renders re-readably
pass	print of an Ast.Bool renders the bare word and read inverts it
pass	print of an Ast.Bytes escapes a non-printable byte as \xNN
pass	print of an Ast.Float renders a re-readable decimal and read inverts it
pass	print of an Ast.Str renders a quoted literal with escapes and read inverts it
pass	print of an exponent-scale Ast.Float round-trips through read
pass	print of an integer-valued Ast.Float keeps its float form through read
pass	print renders a HUGE Ast.Int losslessly — the full 26-digit decimal, no truncation
pass	print renders a bare Ast.Bool as its exact keyword text
pass	print renders a bare Ast.Bytes as its exact b"…" byte-literal text
pass	print renders a bare Ast.Int as its exact decimal text
pass	print renders a bare Ast.Name as its exact identifier text
pass	print renders a bare NEGATIVE Ast.Int as its exact signed text
pass	print renders a nested compound with a string leaf as its exact canonical text
pass	print renders a single-element Ast.List as one parenthesized element
pass	print renders a template that splices an Ast value — the non-executing path works
pass	print renders an empty Ast.List as the empty-parens form
pass	print then read an Ast.Float at full 17-significant-digit precision round-trips
pass	print then read an Ast.Float at the maximum finite magnitude round-trips
pass	print then read an Ast.Float at the smallest subnormal round-trips
pass	print then read an Ast.Int at i64::MIN round-trips (text-path two's-complement boundary)
pass	print then read of a BEYOND-i64 Ast.Int round-trips (text path is arbitrary-precision)
pass	print then read of a BEYOND-i64 NEGATIVE Ast.Int round-trips (sign survives at arbitrary precision)
pass	print then read of a LIST carrying a beyond-i64 Ast.Int round-trips (bignum token bounded by parens)
pass	print then read of a NEGATIVE Ast.Int round-trips (sign survives the text path)
pass	print then read of a digit-led Ast.Name reclassifies it per the number/identifier boundary
pass	print then read of a keyword-spelled Ast.Name reclassifies it as the boolean literal
pass	print then read of a string-spelled Ast.Name reclassifies it as an Ast.Str
pass	print then read of an Ast.Int at i64::MAX+1 round-trips (the exact reader handoff seam)
pass	print then read of an Ast.Int at i64::MIN-1 round-trips (the negative reader handoff seam)
pass	print then read round-trips an Ast.Bytes carrying escapes and high bytes
pass	print then read round-trips an Ast.Bytes through the text path
pass	print/read is IDEMPOTENT after one trip and read normalizes whitespace to one canonical tree
pass	print/read round-trips a 12-deep nest and a 25-wide list
pass	print/read round-trips a tree carrying every leaf kind in one canonical form
pass	print/read round-trips escape-laden, multibyte, and empty string leaves
pass	prk1 a PARKING-LOT fee meter — enter stamps the time, exit charges the seed-shaped first-hour rate plus two per further hour CAPPED at fifteen (a zero-duration stay is free), rev totals the day, and the cap row is identical across seeds while the uncapped rows and the total differ
pass	prn1 a PAREN-DEPTH validator — open and close chars move the depth, an underflow flips the STICKY invalid flag whose every later answer is the sentinel, and the seed picks a balanced or underflowing stream
pass	projecting a field is independent of the order fields are written
pass	projecting a field of a non-compound argument is rejected at the call site
pass	projecting a just-built tuple does not evaluate the unprojected sibling's trap
pass	projecting a record onto an absent field is rejected
pass	projecting a record restricts it to the named fields
pass	projecting a record with a NON-ADJACENT duplicate label is rejected (a repeat separated by another label still rejects)
pass	projecting a record with a TRIPLED label is rejected (the duplicate-label check is not adjacency-limited to a pair)
pass	projecting a record with a duplicate label is rejected
pass	projecting a tuple built from runtime elements folds to the projected element
pass	projecting a tuple passed as a function parameter yields the element, never an invalid component
pass	projecting an if-of-tuples does not evaluate the untaken branch's unprojected sibling
pass	projecting an if-of-tuples evaluates the taken branch's projected element, so its trap occurs
pass	projecting the result of a function that threads a tuple parameter must not trap
pass	projecting the safe element of a tuple elides the discarded trapping element's trap
pass	promoting a Float32 literal to Float64 promotes the binary32 VALUE, not the un-demoted f64 payload (const fold)
pass	promoting a Float32 to Float64 is exact
todo	prr1 a RUNTIME print-then-read Ast round-trip declines pending runtime Ast.read (constant strings only today)
pass	prs1 TWIN parking meters — feed adds coins times the seed rate to one meter answering its minutes, tick decrements BOTH clamped at zero answering how many now read expired, and the slower rate expires meter one two ticks earlier walking the expired count up to two while the faster rate holds at one
pass	ps1 a 100k-iteration tail loop that PERFORMS every iteration — dispatch itself must run in constant stack
pass	psc1 chained peer String ops flow a result into an arg then the second result escapes
pass	pse1 the full (-> String String) converse crosses a peer and its String result escapes main
pass	psr1 a PREPEND-rope accumulator — each push prepends its piece so the OLDEST piece rides at the END, the closing slice proves the reversal order and the piece width varies by seed
pass	pst1 a POSTMARK desk where every stamp weight is a LET CHAIN COMPUTED AT THE PERFORM SITE — the second stamp's argument let-binds the first answer's tens digit doubled-plus-one, the third stamp's argument chains two lets folding the second answer's low digit with the audit's remainder before a mod-seven clamp, stamp folds the weight into the total counting stamps and echoing weight and count, audit reads the total mod one hundred, and the seed sets the first weight so every downstream let chain carries different bindings between the runs
pass	ptr1 a peer compound (tuple) result escapes the entrypoint via the fused envelope
pass	pts1 a 2D POINT under quarter-turn rotations and translations — rot maps (x,y) to (y,-x) answering a sign-tagged composite, mv translates answering the Manhattan norm, quad reads the quadrant, and the seeds trace different orbits around the origin
pass	pure guards on fused-match arms read the payload binder and an enclosing capture
pass	pushing an element of a different type onto a list is a type error
pass	pushing an element onto a list appends it
pass	pushing onto a SHARED perform-built list — the original stays len 2 beside the grown copy
pass	pushing through ONE alias of a doubly-held list leaves the sibling field intact
pass	pw1 a TRIPLING state crosses fixed thresholds — three compares catch the crossing at input-dependent depth
pass	px1 observed tail performer at depth 100 — values must agree while the stack face is under repair
pass	py2a1 probe: a TWO-ARGUMENT op combine(a,b) whose arm uses BOTH args and the captured state in the resume answer (a*s+b); two dispatches pass different arg pairs while the state threads
pass	py3n1 probe: a THREE-LEVEL handler nest (A over B over C) where the innermost body draws from ALL THREE effects — each op resumes a distinct transform of its own handler's state, so the three independent state threads (A doubles, B adds five, C adds nine hundred) route to their correct handlers and pack into separate digit ranges
pass	py3t1 probe: op resumes a THREE-element tuple built from the state (s, 2s, s+100); the body destructures all three fields in one match and packs them into distinct digit ranges, so a field-order swap or a dropped element scrambles the result
pass	py3v1 probe: op resumes a 3-VARIANT user Sum (Lo/Mid/Hi) selected by captured state via nested if; the body matches all three arms — wider tag space than 2-variant Ok/Err, both dispatches land in different variants as state threads across the seed boundary
pass	py3w1 probe: a THREE-WAY branch on the SIGN of (op-arg minus captured state) — cmd(v) scales by 10 when v>s, by 100 when v<s, answers zero when equal, threading a different advance per branch; two dispatches with different args cross the state boundary so both the greater and the equal/less arms fire across seeds
pass	pyab1 probe: resume answer BRANCHES on op arg vs captured state (if v>s then 100v else v+s); with v=1 the branch FLIPS by seed — only seed 0 crosses the threshold, so the first dispatch scales by 100 for n=0 but adds for higher seeds
pass	pyad1 an ABORTING arm (no resume) whose answer DRAWS a distinct outer effect after a resuming sibling threads the state — the stop arm discards its continuation and answers a hundredfold of the state it inherited from the prior tick's next-state plus a foreign draw, so the aborted value reflects the threaded state not the seed
pass	pyaf1 probe: a DATA-DEPENDENCY CHAIN across dispatches — each step(x) resumes x+s and the result feeds the NEXT dispatch's argument, so the three dispatches form a fold where the arg thread and the handler-state thread advance independently
pass	pyarg1 probe: the op takes an ARGUMENT that feeds BOTH the resume answer AND the next-state — add(x) answers (+ (* s 100) x) and threads (+ s x); three dispatches pass different args (5, 2, 7) so the per-call argument enters both the answer and the state advance, and the state thread depends on the arguments (not a fixed increment)
pass	pyat1 probe: answer-hole dispatching nested handle with a TRANSFORMING inner handler (doubles state, two inner dispatches) = 22, distinct fold value from pyre6
pass	pyb1 a BOOL-NEGATING POST-RESUME arm under a SHORT-CIRCUIT body — each frame NEGATES whatever the resumed rest-of-body returns and the or-body's short circuit decides HOW MANY negating frames stack, one seed answers true immediately stacking one negation while the other draws twice stacking two, and the double negation restores what the single negation flips
pass	pyb2 THREE PARITY PROBES under a nested and-chain with negating frames — each draw answers its state's evenness and each frame negates the rest-of-body on the way out, the and-chain short-circuits at the FIRST odd state so the seeds stack one or two negations from the same three-probe body, and the final parity of stacked frames decides the branch
pass	pybf1 probe: op flag(b) carries a BOOL argument and the arm branches the whole resume on it — true scales the state advancing +1, false adds a hundred doubling; two dispatches pass true then false so both branches fire and the next-state choice differs per branch
pass	pybr1 probe: an op RETURNS a Bool computed from a comparison of the captured state — ge(t) answers (>= s t) and threads +1; the body uses the two boolean draws as if-guards selecting different constants, so the threaded state flips the first guard across seeds while the second stays true
pass	pybw1 probe: BITWISE and/or/xor threaded through the handler — tick answers (| (& s 6) (^ s 3)) threading (+ s 1), seed (n%3)+5, so each dispatch combines the live state through AND, XOR, then OR; three dispatches exercise bitwise-logical ops over threaded state and wasm/rust agreement on &, |, ^
pass	pyc1 the RESUME CALL INSIDE A TUPLE CONSTRUCTOR — the arm builds a pair of the resumed rest-of-body value and a state increment then destructures BOTH through a tuple pattern whose binders feed a doubled-plus-witness combine, the resume value passing through construction and pattern-binding rather than any direct binder, and the frames disagree on both tuple fields
pass	pyce1 a RESUMING arm answers with the state PLUS a draw of a distinct outer effect — the tick arm's resume answer folds in a foreign perform whose handler tail-resumes a constant, so the answer carries state + foreign value across the effect boundary and both dispatches in the body see their own foreign draw
pass	pych1 probe: the HANDLE expression sits inside ONE branch of an if — the effect region is installed only when the guard holds; the other branch is a pure constant, so both arms type the same and the handler is set up conditionally at runtime
pass	pych2 probe: an op RESUMES an (Option Char) computed from the handler state via Char.from-int — the alphabet position tracks the state, the body matches Some/None and reads the char back via Char.to-int; a Char (inside an Option) round-trips through the resume seam and the threaded state advances the code point per dispatch
pass	pycl1 probe: the handler arm CLOSES OVER a free variable from the enclosing lexical scope — base = (* n 7) is let-bound OUTSIDE the handle, and the arm's resume answer reads it (+ (* s 100) base); base is constant across dispatches while the state threads, so the fold must capture the outer binding into the arm's closure correctly (not lose it or recompute)
pass	pyco1 probe: the handled body RETURNS A CLOSURE that captured a drawn value, and the closure is applied AFTER the handle has returned — the captured draw must survive the handler teardown and be usable outside the effect region
pass	pyd1 DOUBLE REPLAY UNDER A POST-RESUME TOLL — the arm replays the tail twice inside a do then adds a thousandfold toll to the do's value, the toll fires ONCE per dispatch (not once per replay) on the second replay's outcome, and the seed shifts the surviving replay value and the toll together
pass	pyd2 the PRE-SUSPEND BINDING SURVIVES BOTH REPLAYS — a hundredfold toll is let-bound before either resume so the saved slot must ride through the discarded first replay AND the surviving second one before its single consumption, a slot refreshed per replay or dropped by the discard would misprice the hundreds, and the low digits carry the surviving replay's identity
pass	pydm1 probe: resume answer packs INTEGER DIVISION and MODULO of the captured state (div*10 + mod) while the next-state advances by 7, so across three dispatches the quotient and remainder both shift and a div/mod confusion or wrong thread would scramble the packed digits
pass	pydv1 probe: NEGATIVE integer division threaded through the handler — seed is -(n%3), tick answers (/ (* s 7) 2) and threads (- s 3), so the state goes negative and each dispatch divides a negative product by 2 (truncate-toward-zero: -49/2 = -24, -21/2 = -10); the three dispatches exercise signed division/sign semantics under state threading, and wasm vs rust must agree on truncation direction
pass	pyf1 the RESUME VALUE FED TO A FOREIGN PERFORM — each inner arm hands whatever the resumed rest-of-body returned to the outer scaler which doubles it plus its own advancing state, the two scalings compose innermost-first during the unwind so the outer handler transforms the pyramid twice with different offsets, and the answer is the twice-scaled fold
pass	pyfb1 a let-bound nullary FOREIGN counter draw read by BOTH the resume answer and the threaded next-state under multi-dispatch folds once-per-dispatch (freeze-once) — the arm binds k to a distinct-effect counter and uses it in the answer (+ s k) and next-state (* s k); before the fix the next-state re-ran the perform triangularly, now the foreign counter advances exactly once per A-dispatch
pass	pyfb2-discarded: A's arm performs B (value DISCARDED by do) then resumes purely; witnesses the foreign effect runs EXACTLY ONCE per dispatch even when its value is dropped, and the pure A-body value is preserved (correct = N beats)
pass	pyfb3-nextstate-binder: a let-bound nullary FOREIGN perform read by the threaded next-state under multi-dispatch now FOLDS once-per-dispatch (freeze-once, was the triangular extra-perform miscompile) — the B counter fires exactly N times for N A-dispatches, read back via a non-incrementing total
pass	pyfb3-valonly: let-bound EFFECTFUL foreign draw k read ONLY by the resume VALUE (next-state pure) — the do-peel/let-peel value-position case that FOLDS (k runs once/dispatch), contrast pyfb3 where k feeds next-state and still miscompiles
pass	pyfb4-single: pyfb3's shape (effectful let-bound k read by next-state) but a SINGLE A dispatch — folds correctly because there's no second dispatch to re-run the let (the distinguisher: multi-dispatch triggers the triangular miscompile, single-dispatch does not; as7-class)
pass	pyfd1 probe: FLOAT64 division threaded through the handler — seed 1.0/2.0/4.0 by n%3, tick answers (/ s 2.0) and threads (+ s 3.0), so three dispatches read s, s+3, s+6 and halve each; the sum is (3s+9)/2, exact in Float64 (halves stay representable), exercising float division + addition under state threading with wasm/rust agreement
pass	pyfl1 probe: a FLOAT64 handler state threaded by half-step increments — tick answers the current float state and threads (+ s 0.5), so three dispatches read s, s+0.5, s+1.0 and their sum is 3s + 1.5; the seed is selected by n mod 3 among 0.0/1.0/2.0 so the float thread varies and exact half-steps stay representable
pass	pyfn1 probe: BARE foreign perform in a TWO-HOLE arm's NEXT-STATE hole (not a nested handle) — should FOLD unlike a nested handle in next-state
pass	pyft1 a BARE foreign perform in the post-resume toll position FOLDS — a distinct outer effect drawn directly (not wrapped in a nested handle) as the toll term folds correctly, the boundary pin showing the toll decline is scoped to a nested handle installation not to effect activity in the toll
pass	pyfv1 a FOREIGN perform inline in the resume VALUE under a nested inner handler drawn TWICE — (tick () s (resume (+ s (B.beat)) (+ s 1))) — folds correctly: the outer B.beat runs EXACTLY ONCE per A dispatch because the resume value is spliced into the continuation once per dispatch and the next-state (+ s 1) is pure, so nothing re-threads the perform. Pins the value-position side of the foreign-perform-in-arm family (do-stmt fix pyfb1 landed 5208ad1f3): a foreign perform reaching the VALUE folds, only one threaded into the NEXT-STATE duplicates. Guards against a future freeze-once next-state fix regressing the value-position fold. B.beat returns bs+1 threading its own state, so dispatch1 adds 1, dispatch2 adds 2
pass	pyh1 the RESUME RESULT FED THROUGH A RECURSIVE PURE HELPER — each arm digit-sums whatever the resumed rest-of-body returned before adding a hundredfold state toll, the helper recursion runs during the unwind on a value that does not exist until the tail completes, and the seed drives the two frames' digit-sums through different collapse depths
pass	pyhc1 probe: the resume ANSWER is computed by a RECURSIVE top-level helper applied to the state — sumto x = x(x+1)/2 via self-recursion, so each tick resumes (sumto s) threading (+ s 1); a recursive helper call (not a simple arithmetic expr) sits in the answer position and must run to completion per dispatch while the state threads
pass	pyhn1 probe: the resume NEXT-STATE is computed by a top-level pure HELPER function (nxt s = 2s+1) called from the arm — each dispatch answers ten-times the state and threads nxt(s), so the state follows a 2x+1 recurrence across three dispatches and the cross-function next-state call must thread correctly
pass	pyhof1 probe: the op takes a FUNCTION argument and the arm APPLIES it to the captured state before resuming — apply(f) resumes (f s); two dispatches pass different closures (multiply-by-ten then add-hundred) while the state threads, so the higher-order argument is invoked inside the handler arm at the current state
pass	pyi1 a TOLLED ARM UNDER A BRANCHING BODY — the first draw's answer picks which body branch performs the second draw so the two frames' tolls wrap DIFFERENT continuations per seed, the positive seed rides the hundred branch and the zero seed the two-hundred branch, and each frame's toll composes with whichever branch its continuation actually contains
pass	pyi2 BRANCHES WITH UNEQUAL DISPATCH COUNTS under a tolled arm — the drawn parity routes to a one-draw branch or a TWO-draw branch so the seeds stack two or three tolled frames from the same program, the odd path's extra frame adds both an answer factor and a toll, and a lowering assuming a fixed frame count per body misprices the deeper seed
pass	pyi3 the SHADOWING ARM'S SELF-PERFORM HITS A TOLLED OUTER ARM — the inner arm draws the effect it handles and the draw routes to the outer handler whose thousandfold toll then wraps everything downstream of that dispatch including the inner region's completion, two outer frames stack their tolls around the whole computation, and mispricing either toll or misrouting the self-perform shifts separate digit ranges
pass	pyid1 probe: the INNER handler's arm itself draws from the OUTER effect (legal: outer encloses inner) — each inner I.now resumes its state plus a fresh outer O.get, so the two handler states thread independently and the inner answer folds in an outer sub-draw taken at the outer's current state
pass	pyif1 probe: the arm branches on state parity and RESUMES in BOTH if-branches with DIFFERENT answer and next-state — even s resumes (* s 100) threading (+ s 1), odd s resumes (* s 10) threading (+ s 3); three dispatches follow a data-dependent path through the two resume sites, so the tail fold must handle two distinct resume calls that reconverge (each its own answer AND its own state advance)
pass	pyj1 a PURE HELPER CALL AS THE TOLL — each frame's post-resume toll is a squaring def applied to the captured state so the toll routes through a real function call during the unwind, the two frames square different offsets, and the call rung joins the toll side of the ladder as pyh1 joined its consumer side
pass	pyj2 a RECURSIVE HELPER AS THE TOLL — each frame's post-resume toll digit-sums a thirteen-fold compound of its captured state so the unwind runs a data-dependent recursion per frame, the two frames' digit-sums collapse different products, and the recursion depth itself varies with the seed through the toll argument
pass	pyk1 an INDEX-WEIGHTED post-resume toll — the state pairs a value with a dispatch counter and each frame's toll is their PRODUCT so the very first frame's toll is zeroed by its own index while the second frame pays value-times-one, a product of two captured fields distinguishing which frame's pair fed which toll beyond what either field alone could
pass	pyk2 the TOLL MULTIPLIES THE OP ARGUMENT BY THE DISPATCH COUNTER — every capture source meets in one product with the argument from the perform site and the counter from the tuple state, each unwinding frame recalls ITS OWN pair so the first frame pays three-times-one and the second five-times-two, and any cross-frame mixing of argument and counter misprices the thousands
pass	pyk3 a FIBONACCI-ROTATING TUPLE STATE UNDER TOLLS — each dispatch answers a positional pack of both fields then rotates the pair Fibonacci-style while its toll charges a thousandfold of the FIRST field as captured, the rotation means each frame's captured first field is the previous frame's second, and a toll reading the rotated tuple instead of the captured one shifts the thousands by the rotation
pass	pyl1 a HEAP LIST BUILT AND MEASURED INSIDE THE TOLL — each frame's post-resume toll constructs a list whose LENGTH depends on the captured state then charges a hundredfold of it, the heap allocation happens during the unwind, and the state-gated size means the seeded frame builds the longer list
pass	pyl2 a GROWING LIST AS THE STATE THREAD — the seed becomes the list's head and every dispatch answers head-tenfold-plus-length while pushing the old length, both the seeded head and the growing length land in every answer, and a thread that rebuilds the list or drops the push loses one digit family each
pass	pyl3 HEAP-LIST STATE THROUGH DIVERGENT REPLAYS — the discarded replay pushes a NINE and the survivor pushes a FIVE onto the shared starting list, the next dispatch reads slot one and finds the survivor's five never the discarded nine, and the seeded head rides both answers while the five stamps only the second
pass	pyl4 the HEAP TOMBSTONE READS THE ARM'S OWN PRE-REPLAY LIST — both replays push onto the thread yet the tombstone that survives reads the arm's ORIGINAL binding with length one and the seeded head, neither replay's push contaminating the captured list, the heap twin of the capture-not-live law
pass	pylc1 probe: arm binds a LET CHAIN of intermediates and the resume answer + next-state both reference earlier let-bindings (a used in b, both used in the resume)
pass	pylh2 a NON-dispatching nested handle LET-HOISTED before resume FOLDS — the inner handle is bound in a let (evaluated before resume) and its value used post-resume; because its body performs nothing the narrowed dispatching-only toll guard lets it fold, confirming the guard keys on whether the nested handle's body dispatches, not on the let-hoist structure
pass	pyls1 probe: a LIST-STATE handler accumulates via prepend across the seam — push(v) threads (List.prepend l v) as the next-state and answers the new length, while a head op reads the most-recently-pushed element; the seed-scaled final push makes the head reflect the seed
pass	pym1 TWO OPS WITH DIFFERENT POST-RESUME TOLLS INTERLEAVED — hi answers plain state adding a thousandfold toll while lo answers doubled state adding only a hundredfold one, the body alternates hi lo hi so the unwind interleaves toll KINDS in reverse dispatch order, and a lowering that applies one op's toll shape to the other's frame misprices by an order of magnitude
pass	pymap1 probe: a MAP (CHAMP) handler state threaded with insert + lookup + len — seed {0: n%3}; each tick answers (lookup key 0) + 100*(current size) and threads a Map with a new key=size val=size inserted, so the map GROWS per dispatch and the answer reads both a stable key and the live size; three dispatches exercise a heap Map value threaded and rebuilt across the resume seam
pass	pymf1 a MULTI-SHOT arm resumes twice with each resume answer drawing a STATEFUL foreign counter — the two replays see distinct foreign values because the counter advances once per shot in arm evaluation order, so a shared or cached foreign draw across shots would collapse the sum and the distinct values prove per-replay re-evaluation
pass	pymm1 probe: the resume answer packs MAX*10 + MIN of the op-arg and the captured state — clamp(v) puts the larger in the tens place and the smaller in the ones, so the arm's if picks the ordering and two dispatches with different args straddle the threaded state
pass	pymo1 probe: a TWO-OP effect sharing one threaded state — inc answers s and threads (+ s 5), get answers (* s 10) and threads s unchanged; the body interleaves inc/get/inc/get so the two ops read and advance the SAME handler state in alternation, testing that the tail-resumptive fold threads state across DISTINCT ops of one effect (not just repeated dispatch of a single op)
pass	pymo3 probe: a THREE-OP effect over a TUPLE state where each op touches a different field — adda answers a and threads (a+1, b), addb answers b and threads (a, b+10), rd answers a*100+b and threads unchanged; the body interleaves adda/addb/adda/addb/rd so three distinct ops read and independently advance the two fields of one shared tuple state through the tail-resumptive fold
pass	pymo4 probe: NEGATIVE modulo threaded through the handler — seed -((n%3)+1), tick answers (% (* s 7) 3) and threads (- s 2), so the state is negative and each dispatch takes a negative dividend mod 3 (truncated remainder: takes the sign of the dividend, e.g. -14%3 = -2, -7%3 = -1); three dispatches exercise signed-remainder semantics under state threading, and wasm vs rust must agree on the remainder sign
pass	pymr1 probe: a MAP-STATE handler with insert-then-read-across-the-seam — put(k,v) threads Map.insert as the next-state, and a later fetch(k) must see the value the earlier put wrote (heap read-after-write through the resume seam), while a fetch of the seeded key still returns the seed
pass	pymr2 probe: MUTUALLY-RECURSIVE driver functions each perform the effect — evn k = (E.tick) + odd(k-1), odd k = 100*(E.tick) + evn(k-1); starting evn(4) alternates evn/odd frames, so four E.tick dispatches originate from a mutual-recursion call chain and must route to the handler and thread state s0..s0+3 in call order (evn's tick weighted 1, odd's tick weighted 100)
pass	pymt1 probe: a GEOMETRIC (tripling) state thread — each tick answers the current state and threads (* s 3), so three dispatches read s, 3s, 9s; packing them into separate digit ranges makes a wrong multiplier or a mis-threaded state visibly scramble the powers-of-three progression
pass	pymt2 probe: a THREE-WAY data-dependent resume — the arm branches on (% s 3) via nested if and resumes differently in each of three branches (s%3=0 -> (* s 100) thread +1; =1 -> (* s 10) thread +2; else -> s thread +3); four dispatches walk a state-dependent path across all three resume sites, so the tail fold reconverges three distinct resume calls each with its own answer AND state advance
pass	pymu1 probe: a two-op effect where bump answers UNIT (advances state only, no meaningful value) and peek answers (* s 100) reading the state — interleaved via let/do as peek,bump,peek,bump,peek; the UNIT-answering op threads (+ s 3) while the reader leaves state unchanged, testing a Unit resume-answer alongside a value-answer in one shared-state tail fold
pass	pymx1 probe: a MAP-STATE handler with REMOVE across the seam — rm(k) threads Map.remove as the next-state and answers the shrunk length, so a later get(k) of the removed key returns None while a get of a surviving key still returns its value; delete-then-lookup consistency through the resume seam
pass	pyn1 POST-RESUME TOLLS AT BOTH NESTING LEVELS — the outer levy adds a thousandfold toll around a body whose second summand is a WHOLE INNER HANDLE running two hundredfold-tolled ticks, the inner pyramid unwinds completely inside the outer frame's replay before the outer toll settles, and the seed moves only the outer answer and toll leaving the inner arithmetic fixed
pass	pynd1 probe: a NESTED do-block as the handler BODY (do A (do B C)) sequences four dispatches, discarding all but the final expression's value — this is the shape that broke the ML round-trip before the printer fix (6345bd197 keeps the inner block boundary); pins that the fixed printer preserves nested-do semantics and the discarded dispatches still advance the state
pass	pynd2 probe: a NESTED do-block as a LET body — (let ((k 5)) (do A (do B (+ C k)))) exercises the fn/let greedy-body position of the printer round-trip fix (6345bd197 covered handle/let/fn bodies); the let binding survives into the innermost expression while the discarded dispatches advance the handler state
pass	pynd3 probe: nested handlers of TWO effects where the INNER handler's arm performs the OUTER effect — Out.oo answers (* s 10) threading (+ s 1), In.ii answers (+ t (Out.oo)) threading (+ t 1); each In.ii dispatch performs a fresh Out.oo that must route past the inner In handler to the outer Out handler and thread Out's state INDEPENDENTLY, so the two Out.oo calls see s0 then s0+1 (a cross-effect delegation the tail fold must not shadow or double-count)
pass	pynd4 probe: THREE nested handlers (A/B/C) where each inner arm delegates OUTWARD — C.cc performs both B.bb and A.aa, B.bb performs A.aa, A.aa is the base; each perform must route past the intervening handlers to its own handler and thread that effect's state independently, so A's counter advances across ALL delegated performs (from C, from B, and directly) while B and C keep their own
pass	pynd5 probe: the handled body calls a HELPER FUNCTION that performs the effect — (def (twice) (+ (E.tick) (E.tick))) is called from inside the handle body, so two of the three tick dispatches originate in a SEPARATE function frame yet must route to the enclosing handler and thread its state in call order (twice's two ticks then the direct tick)
pass	pynm1 probe: op takes an (Option (Option Int64)) arg and the arm NESTED-MATCHES it two levels deep to pick the resume; Some(Some x) adds x, Some(None) doubles, None scales-by-10 — two dispatches hit different nested arms
pass	pyos1 probe: an (Option Int64) HANDLER STATE threaded across three dispatches — tick answers (Some v -> v*10 / None -> -1) and threads (Some v -> Some(v+1) / None -> Some 0); the seed is None when n%3=0 else Some(n%3), so the state slot carries a two-arm SUM VALUE and a match reads/rebuilds it per dispatch as it threads
pass	pypb1 probe: the arm BRANCHES the whole resume (answer AND next-state) on the captured state's PARITY — even states scale-by-10 advancing +3, odd states add 100 doubling; three dispatches walk a parity-alternating thread so both branches fire and the next-state choice steers the following dispatch's parity
pass	pyq1 a FOREIGN TOLLED PERFORM WHOSE CONTINUATION ESCAPES THE INNER REGION — the inner body performs on the tolled OUTER handler so the outer levy's continuation spans past the inner handle's close, the inner toll settles INSIDE that escaping continuation while the outer levy's own toll wraps the whole remainder including the tenfold scaling and the first draw's addition, and the composition orders three tolls across two frames and a region boundary
pass	pyq2 the ESCAPING LEVY AS THE INNER BODY'S LAST FORM — the tolled inner draw comes first and the foreign tolled levy second so the levy's continuation carries only the addition the inner toll and the region close, the outer toll wraps that short escape while the inner toll still settles inside it, and swapping the two summands crosses into the declined fold face
pass	pyq3 the ESCAPING LEVY'S VALUE IS SCALED INTO THE INNER FOLD — the last inner dispatch is a thousandfold-scaled outer levy so the escaping continuation carries the scaling the inner toll and the region close while the levy's answer lands in the inner body's own arithmetic, and both frames' tolls price their own captures around the shared value
pass	pyr1 POST-RESUME ARITHMETIC in the arm — each tick's arm ADDS a thousandfold toll to whatever the resumed rest-of-body eventually returns, three dispatches stack three tolls that unwind INNERMOST-FIRST after the body's positional fold completes, each toll keyed to the state AT ITS OWN DISPATCH so a reordered unwind or a stale-state toll misprices the pyramid, and the seed shifts every toll together
pass	pyr10 the LET-BOUND REPLAY VALUE ROUTES AND FEEDS BOTH BRANCHES — each arm binds the rest-of-body value then an if picks between adding the state or doubling the value plus tenfold state, the seeds split the INNER frame's branch while the outer frame holds steady, and both the branch decision and the surviving arithmetic reuse the same binder across the suspend
pass	pyr2 MULTIPLICATIVE post-resume tolls — each dispatch's arm MULTIPLIES the resumed rest-of-body value by its own state-plus-two factor, two dispatches compose two factors around the body's positional fold, and because the factors differ per dispatch the product pins the unwind pairing (which factor saw which intermediate) beyond what addition can distinguish
pass	pyr3 the RESUME RESULT LET-BOUND and BRANCHED ON — each arm binds what the resumed rest-of-body returned then an if on that value picks between tripling-plus-state and adding a hundredfold state, the outer frame's branch decision depends on what the INNER frame's toll produced, and the seed flips the inner frame between the two branches so the runs unwind along different arithmetic paths entirely
pass	pyr4 an IF CONDITION DIRECTLY ON THE RESUME CALL — each arm tests whether the resumed rest-of-body value clears the line and answers small-plus-state when it does or a thousand-plus-state when it does not, the branch values are chosen so the outer frame always takes the OPPOSITE branch from the inner one, and the seed flips which branch the inner frame starts on so the two runs produce a thousandfold-apart answers from the same machine
pass	pyr5 a MATCH SCRUTINIZING THE RESUME CALL — each arm matches the resumed rest-of-body value mod three across three literal-guard arms adding a repdigit toll plus its own dispatch state, the inner frame's toll decides which arm the outer frame lands in, and the seed moves the inner frame across the arm boundary so the runs exit through different toll pairs
pass	pyr6 the RESUME RESULT LET-BOUND into a PURE COMBINE — no branch at all, each arm binds the resumed rest-of-body value and answers twice-it-plus-state, the minimal let-bound-resume shape isolating the binder itself from any downstream control flow, doubling per frame so the unwind order is pinned by magnitude
pass	pyr7 a MATCH BINDER on the resume result — the arm matches the resumed rest-of-body value against a zero literal falling through to a BINDER arm that reuses the value doubled-plus-state, the binder arm is the match twin of the let form the two-hole refold folds, and the runs disagree at both frames through the binder path
pass	pyr8 a POST-RESUME TOLL over a DESTRUCTURED TUPLE STATE — the arm matches the pair out of state then adds a thousandfold toll packing BOTH fields as captured at its own dispatch, the two frames' tolls unwind innermost-first with different field pairs, and a toll reading the post-resume tuple or the other frame's binding misprices both digits at once
pass	pyr9 a BARE-BINDER MATCH on the resume result — the single match arm binds whatever the resumed rest-of-body returned with no literal alternatives at all and answers it tripled-plus-state, the minimal match-scrutinee-binder shape, tripling per frame so the unwind pairing is pinned by magnitude
pass	pyrd1 probe: the handled body calls a RECURSIVE DRIVER (loop k acc) that performs E.tick once per recursion level and accumulates — 4 dispatches originate from successive recursive frames of loop, not lexically in the handle body, and each must route to the handler and thread its state (s0..s0+3) so the accumulation sums the per-dispatch answers (s*10) in call order
pass	pyre6 a nested CLOSED HANDLE in the RESUME-ANSWER hole of a two-hole (non-tail) arm compiles correctly — the inner handle is a self-contained pure computation reducing to a constant that feeds the resume's answer while the next-state is plain arithmetic, and because the answer-hole position (unlike the next-state hole) survives the two-hole refold the whole nest folds to the same value as substituting the inner handle's literal result
pass	pyre6-distinct a nested closed handle over a DISTINCT effect in the resume-answer hole of a two-hole arm — the inner handle discharges its own effect F entirely (a self-contained pure result) and feeds the outer E-handler's resume answer, confirming the answer-hole refold is correct regardless of whether the nested handle shares the outer effect or uses a fresh one
pass	pyre7 a nested CLOSED HANDLE in the RESUME-ANSWER hole of a TAIL-resumptive arm (no post-resume toll) — the arm resumes in tail position so it folds via the tail thread path rather than the two-hole refold that pyre6 exercises; the inner handle self-discharges its own E to a constant that is added to the threaded state and fed as the answer, and because the answer position is reduced the whole nest folds to the same value as substituting the inner handle's literal result (referential transparency across the thread path)
pass	pyres1 probe: a RESULT-typed resume answer with parity-selected payload — step answers (Ok (* s 10)) on even state and (Err (+ s 100)) on odd, threading (+ s 1); the body folds Err to its negation, so the two dispatches cross the Ok/Err boundary and a sum-type answer with a varying payload rides the resume seam per dispatch
pass	pyrp1 probe: a recursive driver MULTIPLIES the per-dispatch answers — prod k acc = prod(k-1, acc * (E.tick)); tick answers (+ s 1) threading (+ s 1), seed (n%3)+1, so three dispatches yield (s0+1)*(s0+2)*(s0+3) as a running product (distinct from the additive pyrd1 accumulator; exercises multiplicative accumulation over threaded state)
pass	pyrs1 probe: a (Result Int64 Int64) HANDLER STATE threaded across three dispatches — tick answers (Ok v -> v*10 / Err e -> -e) and threads (Ok v -> Ok(v+1) / Err e -> Ok e, recovering on the first tick); seed Err 7 when n%3=0 else Ok(n%3), so the state slot carries a two-arm SUM and the Err arm recovers into Ok mid-thread
pass	pys2 a STRING STATE THREAD GROWING THROUGH RESUMES — weave answers the current rope and appends a marker into the state thread, the seed picks ropes of DIFFERENT LENGTHS so the two draws' concatenated byte length separates the runs, and the rope value must flow through the tail resume's answer and state positions intact
pass	pysd1 probe: a SET-STATE handler enforces DEDUP across the seam — add(k) threads Set.insert and answers the new size, so re-adding an already-present key does not grow the set; a has(k) op reads membership, confirming the CHAMP set's dedup and contains both survive the resume threading
pass	pysh1 the SAME EFFECT SHADOWED with DIFFERENT TOLL SHAPES — the outer handler charges a thousandfold toll and serves the first draw while an inner handler over the SAME effect charges only a hundredfold toll and serves the two draws inside its region, the inner pyramid settles its cheap tolls before the outer frame's expensive one, and routing any draw to the wrong depth changes both which toll fires and which state answers
pass	pysh2 the SHADOW UNINSTALLS BETWEEN OUTER DRAWS — an outer draw precedes the shadowing region and a THIRD draw follows it, the middle draw routes to the inner cheap-toll handler whose region closes before the third draw re-routes to the OUTER expensive-toll arm continuing the outer state where the first draw left it, and a shadow that leaks past its region steals both the third draw's state and its toll
pass	pysh3 the SHADOW'S OWN INIT DRAWS FROM THE HANDLER IT IS ABOUT TO SHADOW — the inner handle over the same effect computes its starting value by performing on the still-unshadowed outer arm, the inner region then serves its one draw from that outer-drawn seed with the cheap toll, a final outer draw confirms the outer state advanced through the init, and both outer frames settle their expensive tolls around the whole inner region
pass	pysh4 probe: SAME-EFFECT nested handlers where the inner handle E SHADOWS the outer — outer tick answers (* s 100) threading (+ s 1) seed n%3, inner tick answers (* s 10) threading (+ s 2) seed 50; the body performs an outer tick, then (inside the inner handle) two ticks that must bind to the INNER handler, then a final outer tick — so the two E handlers of the SAME effect coexist and the inner correctly shadows only within its body while the outer state threads across the ticks outside it
pass	pysh4 the SHADOWING ARM PERFORMS THE EFFECT IT HANDLES — the inner arm's own body draws E while building its answer and that draw routes to the OUTER arm because a handler arm runs OUTSIDE its own region, the outer's decorated answer folds into the inner's, and an arm that captured its own region instead would recurse on itself
pass	pysh5 REPEATED SELF-PERFORMS THREAD BOTH STATE LADDERS — two shadowed draws each route their arm's inner self-perform to the outer handler so the outer state ladder advances once per inner dispatch while the inner state doubles independently, both ladders' progressions land in both answers, and either ladder stalling or the self-perform re-entering the inner region misprices a distinct digit range
pass	pysh5 probe: the outer arm's RESUME ANSWER contains a nested closed handle of a DISTINCT effect F — tick answers (+ (* s 100) <handle F 7 ...>) threading (+ s 1), where the inner handle F is closed (ff answers t*2, threads t+1, body (F.ff)+(F.ff) = 14+16 = 30); the closed distinct-effect handle in the answer position reduces to 30 and adds to each outer dispatch, testing a nested closed handle in the answer hole (pyre6 axis) driven per-dispatch by the outer fold
pass	pysh6 a TRIPLE SHADOW CHAIN OF SELF-PERFORMS — three handlers over the same effect stack and each inner arm's self-perform routes exactly ONE level out so the body's single draw cascades level by level to the outermost arm and the answers fold back in reverse, each level stamping its own state band, and any arm skipping a level or capturing its own region breaks a distinct digit band
pass	pysh7 SIBLING SHADOW REGIONS SHARE ONE OUTER THREAD — two side-by-side handles over the same effect each self-perform to the shared outer arm, the outer state advances through the FIRST sibling into the SECOND so their answers carry consecutive rungs, and an outer thread that forks per region or resets between siblings collapses the rung gap
pass	pysh7 probe: THREE-level SAME-EFFECT nested shadowing — three handle E for one effect E, seeds n%3 / 5 / 20, answers (* s 1000) / (* s 100) / (* s 10) all threading (+ s 1); the body performs an outer tick, then a mid tick, then two innermost ticks, so each nested handle E shadows the ones outside it only within its own body and each level threads its own state independently
pass	pysh8 probe: a variable LEFT-SHIFT by the threaded handler state — tick answers (<< 1 s) threading (+ s 1), seed (n%3)+1, so three dispatches compute 1<<s0, 1<<(s0+1), 1<<(s0+2); the shift amount is data (the live state), exercising variable-shift semantics under state threading and wasm/rust agreement on the shift
pass	pysm1 probe: op takes an Option-typed ARGUMENT and the arm MATCHES it to choose the resume answer and next-state (Some carries an addend, None doubles the thread) — distinct from ops that RETURN Option
pass	pysq1 SEQUENTIAL SAME-EFFECT REGIONS WITH DIFFERENT TOLL RATES — two top-level handles over one effect run one after the other with independent seeds arms and rates, the first region's hundredfold toll and the second's two-hundredfold never cross, and a stale frame or shared arm table from the first region misprices the second
pass	pysq2 a TOLLED REGION FOLLOWED BY AN UNTOLLED ONE — the first region's arm carries hundredfold post-resume tolls and its two frames unwind before the second region installs a plain tail-resumptive arm over the same effect, the second region's draws fold clean with no residual toll machinery, and toll infrastructure leaking across the region boundary inflates the ten-thousands
pass	pystr1 probe: a STRING-STATE handler threads String.concat across the seam — grow() appends a fixed suffix answering the new length, so two grows extend the seed string and a size read returns the accumulated length; heap String state survives the resume threading
pass	pystr2 probe: a STRING handler state that GROWS per dispatch — tick answers (String.scalar-len s) and threads (String.concat s "x"), so each dispatch reads the current length then appends; seed is a 1/2/3-char string by n%3, exercising a heap String value threaded and rebuilt across the resume seam
pass	pyt1 a POST-RESUME TOLL that PERFORMS A FOREIGN EFFECT — each inner tick resumes then adds an OUTER levy to what the rest-of-body returned, the two levies fire during the innermost-first unwind so the outer handler's counter advances in unwind order not dispatch order, and the seed sets the levy schedule the unwinding frames drain
pass	pyt2 the FOREIGN LEVY INSIDE THE RESUME'S ANSWER ARGUMENT — each inner tick levies the outer handler WHILE BUILDING its answer so the levies fire in DISPATCH order, the exact mirror of the post-resume toll whose levies fire in unwind order, and the pair pins that argument-position performs precede the suspend while post-resume performs follow the replay
pass	pyt3 probe: a THREE-FIELD tuple state where one op reads and advances all fields — state (a b c) seed (n%3, 10, 100); tick answers a cross-field weighted sum (a + 2b + 3c) and threads (a+1, b+10, c+100), so all three fields advance independently per dispatch and the answer mixes them; three dispatches exercise a wide compound state through the tail fold
pass	pyt4 a POST-RESUME TOLL THAT TRAPS — each frame's toll divides a hundred by its captured pre-resume state so the zero seed's FIRST frame traps at unwind AFTER the whole body already ran to completion, the nonzero seed pays both quotient tolls cleanly, and a lowering that evaluates the toll before the resume would trap before the body ran at all
pass	pyt5 the BODY TRAPS UNDER A PENDING TOLL — the replayed rest-of-body divides by the drawn answer so the zero seed traps INSIDE the resumed continuation while the frame's thousandfold toll is still pending, the trap wins over the pending post-resume work, and the nonzero seed completes the body and pays the toll
pass	pyt6 the WOULD-TRAP REPLAY IS DISCARD-ELIDED — the double-replaying arm's first replay runs a body whose division would trap on the zero seed, but the replay's value is discarded so the pure trapping tail is elided per the discard law and only the second replay's quotient survives, while the nonzero seed shows the first replay otherwise runs
pass	pyt7 the INIT TRAPS IN VALUE POSITION — the handler's starting value divides by the seed's residue so the zero seed traps BEFORE any frame installs or any dispatch runs, completing the trap-position triple: init traps eagerly, a discarded pure trap is elided, and a toll trap fires only at unwind
pass	pyt8 the SURVIVING REPLAY'S ANSWER ARGUMENT TRAPS — the second resume's argument divides by the state so the zero seed traps while BUILDING the replay that would win, value position beats discard elision, and the nonzero seed rides the clean quotient through the surviving replay
pass	pytd1 probe: op RETURNS a tuple resume value; the body DESTRUCTURES each dispatch's tuple result via match, two dispatches thread state so the tuple fields differ per call
pass	pytf2-ans probe: TAIL arm with a bare foreign perform in the ANSWER hole folds (contrast: same foreign perform in the next-state hole declines)
pass	pyth2 a NON-dispatching nested handle in the post-resume toll position FOLDS — the inner handle installs a handler but its body performs nothing (a constant), so unlike a dispatching nested handle (which the two-hole refold declines) the non-dispatching one reduces to its constant and the toll charges that constant per frame
pass	pytn1 a nested CLOSED HANDLE in the NEXT-STATE hole of a TAIL-resumptive arm folds via the thread path — the inner handle self-discharges its own E to a constant (42) that becomes the threaded next-state; because a tail arm threads its next-state through the distinct tail lowering (not the two-hole refold) it reduces the closed handle to its value correctly, so the whole nest folds to the same value as substituting the literal. CONTRAST pyre3 (the SAME dispatching-handle-in-next-state under a TWO-HOLE arm) which miscompiled and now DECLINES — the value-position decline set is two-hole-arm-specific; tail arms fold nested handles in both the answer hole (pyre7) and the next-state hole (this)
pass	pyts1 a nested CLOSED HANDLE in the outer handle's SEED under a TAIL-resumptive arm folds via the thread path — the inner handle self-discharges its own E to a constant (42) that is added into the seed; a tail arm's fold reduces the closed handle in the seed to its value, so the whole nest folds to the same value as substituting the literal. Completes the tail triple with the answer hole (pyre7) and the next-state hole (pytn1); CONTRAST the TWO-HOLE seed (pyse1) which declines — the value-position decline set is two-hole-arm-specific, tail arms fold a nested handle in ALL positions
pass	pyts2 probe: a TUPLE-STATE FIELD SWAP — the state is a pair, each tick answers a digit-packed function of both fields (a*10+b) and threads the SWAPPED pair (b, a) as the next-state, so the two dispatches read the fields in opposite roles and an order-insensitive swap or a wrong thread flips the packed value
pass	pyu8W16 a checked-narrowing UInt16.of of the state in the resume answer folds — width-16 twin of pyu8a1
pass	pyu8W32 an Int32.wrap of the state in the resume answer folds — signed width-32 twin of pyu8r1
pass	pyu8a1 a checked-narrowing UInt8.of of the state in the resume answer folds — three widened reads pack after the .of range-check emits
pass	pyu8r1 a narrow UInt8 resume answer over an Int64 handler state folds — UInt8.wrap of the growing state (256 wraps to 0) feeds each answer while the state threads +1
pass	pyu8t1 a UInt8 handler state threaded across three dispatches with a WIDENED Int64 resume answer folds — the narrow state advances via wrapping-add (255+5 wraps to 4) while each answer widens with Int64.of
pass	pyu8w1 a UInt8 handler state near the 255 boundary threaded by UInt8.wrapping-add wraps past 255 through zero — each tick answers the widened state (Int64.of) and the three packed reads fold to the wrapped walk
pass	pyv1 the POST-RESUME TOLL KEYED TO THE OP ARGUMENT — each dispatch answers argument-plus-state threading their sum then adds a hundredfold toll of ITS OWN argument after the replay, the two arguments differ so the unwinding tolls must each recall the argument their dispatch received across the suspend, and a toll reading the other frame's argument or the state instead shifts the hundreds
pass	pyv2 a NON-COMMUTATIVE DIFFERENCE TOLL — each frame charges a hundredfold of the op argument MINUS the captured state so swapping the operands negates the toll, the two dispatches subtract different pairs, and the seed that RAISES the state SHRINKS the answer through both differences at once
pass	pyw1 the TOLL COMPUTED BEFORE THE SUSPEND CONSUMED AFTER THE REPLAY — each arm let-binds a hundredfold toll from its pre-resume state then resumes and adds the SAVED binding to whatever the rest-of-body returned, the binding must ride the continuation across the suspend and the replay, and a lowering that recomputes the toll from post-resume state or drops the saved slot shifts every frame's contribution
pass	pyw2 TWO PRE-SUSPEND BINDINGS RIDE THE CONTINUATION TOGETHER — the arm let-binds a hundredfold and a tenfold toll from different state offsets BEFORE resuming then sums both saved slots after the replay, both bindings must survive the suspend side by side, and dropping or recomputing either slot shifts a distinct digit range
pass	pyx1 a PASSIVE PEEK READS THE SURVIVING REPLAY'S STATE — the tick double-replays with the discarded replay adding one and the survivor TRIPLING the state, a second op then reads the state without advancing it, and the peek must see the survivor's tripled thread rather than the discarded increment or the pre-replay value
pass	pyx2 a POKE OVERWRITES THE THREAD FROM ITS ARGUMENT — the ticks advance the state incrementally while a poke answers the OLD state and replaces the thread wholesale with its argument, the third dispatch reads the transplanted seven rather than any incremental descendant, and a poke that merges instead of replacing or answers the new value shifts separate digit ranges
pass	pyz3 REPLAYS DIVERGING ONLY IN THE ANSWER — both resumes thread the SAME next-state but answer one apart so the second replay's plus-one signature must appear at BOTH dispatch depths of the two-perform body, and a lowering that collapses same-state replays into one or returns the first answer drops the low bit at each depth
pass	pyz4 the TOLL ATTACHES TO ONLY THE SECOND REPLAY — the first resume stands bare and discarded while the second carries a thousandfold post-resume toll, the toll fires once on the surviving replay's outcome and never for the bare one, and a lowering sharing one toll shape across both replay sites of the arm double-charges
pass	q5 a FIVE-argument op — the arm folds all five positions with distinct weights, two calls permute the arguments
pass	q6 all FIVE arguments are draws — left-to-right evaluation order pinned at width five by distinct weights
pass	qkm1 a same-unit Float quantity Map key round-trips with value discrimination
pass	qkm2 a CROSS-scale Map probe is a compile-time type error (the mixed-scale = conversion does not extend to collection ops)
pass	qkm3 an explicitly converted cross-scale probe (Unit.in + re-wrap) hits the same-unit key
pass	qqb1 fifty quasiquote-spliced runtime-Ast trees are value-exact and leak linearly (the splice-build face)
pass	qqs1 eval of a quasiquote splicing a module-const unquote reconstructs and folds
pass	qqs2 an eval-of-splice composes inside a larger expression
pass	qrm1 WEIGHTED QUORUM voting with a punitive veto — votes accumulate weight answering whether the threshold is met, veto ZEROES the tally and RAISES the threshold by two, tally packs weight and quorum-bit, and the seed sets the initial threshold so the same votes pass on one seed and never on the other
pass	qs1 two perform-drawn quantities of one dimension ADD — same-unit combine over two crossings
pass	quasiquote constructs AST with selective evaluation
pass	quasiquote makes AST construction readable
pass	quasiquote nests with inner unquote evaluated
pass	quasiquotes unquoting a runtime variable and a literal build equal ASTs
pass	quo1 a WEIGHTED QUORUM vote — each member's weight tallies ONCE (revotes no-op via the voted set), unknown members answer a sentinel, and the pass bit flips when the tally crosses the quorum
pass	quote of a byte-string literal reifies to an Ast.Bytes node
pass	quote of a let reifies as an Ast.List with a let head and round-trips
pass	quote of a nested quote form is inert — the inner quote is not evaluated
pass	quote produces an AST value without evaluating
pass	quote reifies a grammar-keyword head as an ordinary Ast.Name
pass	quote reifies a let form's head as an ordinary Ast.Name without establishing scope
pass	quote reifies an fn form's head as an ordinary Ast.Name without binding its parameter
pass	quote-built and constructor-built equal trees encode byte-identically
pass	quote-built and guest-built Asts compare across const/runtime payload boundaries
pass	quote-vs-constructor AST equality holds regardless of operand order
pass	quoted Asts as SET elements dedup by structural content
pass	quoting an empty compound produces an empty Ast.List
pass	ra1 a RACE between two effect threads — a recursive walk draws BOTH per round until the fast thread catches the slow one's head start
pass	ra2 the race walk's exit value SEEDS a third effect's handle — two-thread termination feeding the position matrix
pass	raising a BigInt-magnitude quantity to a power runs the unbounded multiply
pass	raising a COMPUTED (derived) quantity to a power squares its magnitude and dimension
pass	raising a quantity to a compile-time power composes the unit and the magnitude
pass	raising a runtime computed quantity to a power squares the runtime magnitude
pass	rak1 an op RESULT of type Rational resumes an exact fraction
pass	rak2 an op RESULT of type BigInt resumes a multi-limb value
pass	rak3 an op RESULT of type Symbol resumes an interned symbol by state
pass	rak4 an op RESULT of type Bytes resumes a built rope
pass	rak5 an op RESULT of type Map resumes a built map the body queries
pass	rational division by the ZERO rational is a divide-by-zero, rejected CDZ0304 when provable
pass	rational multiplication of mixed signs is a negative reduced rational (sign on numerator): (-1/2)·(3/5) = -3/10
pass	rational multiplication of two negatives is positive and reduced: (-2/3)·(-3/4) = 1/2
pass	rational subtraction can produce a negative result with the sign on the numerator: 1/4 - 1/2 = -1/4
pass	rbp1 native and classic RECORD destructuring PARAMS bind identically — M3 spelling parity
pass	rbw1 a three-hundred-arm integer match dispatches to the right arm
pass	rbw2 a four-hundred-binding let chain threads sequentially
pass	rbw3 a one-million-frame add-one recursion answers (the non-tail spine does not exhaust the stack)
pass	rc1 a RECORD handler state — the arm projects both fields and rebuilds with different advances, sums pin two dispatches
pass	rc2 the arm updates ONE record-state field chosen by the fields' own parity — three snapshots pin the alternating writes
pass	rc3 a record state whose FIELD is a tuple — the counter ticks while the pair walks a Fibonacci step per dispatch
pass	rcw1 a hundred-constructor sum dispatches to the seventy-seventh arm
pass	rcw2 a hundred-field record projects its first and last fields
pass	rcw3 a fifty-element tuple projects its first and last positions
pass	rcw4 a sixty-four-KiB string literal measures across the wasm page boundary
pass	rcw5 a wide record reads every field and sums them at their sorted indices
pass	re-inserting a LIST row into a map replaces that row and leaves the original map's row intact
pass	re-interning a symbol's recovered string yields an equal symbol, and the string content-matches
pass	re-wrapping the intermediate with Qty.of makes a two-step conversion well-formed
pass	reaching an effect neither handled nor delegated is rejected at compile time
pass	read classifies a lone punctuation token as an Ast.Name and it round-trips
pass	read normalizes numeric token spellings to canonical float values
pass	read of a bare b token is an Ast.Name, not a byte-literal
pass	read of a leading-'+' BEYOND-i64 integer token is an Ast.Name too (both sides of the boundary)
pass	read of a leading-'+' integer token is an Ast.Name, not an Ast.Int (within i64)
todo	read of malformed text is a coded reject, not a wrong value
pass	read of text that is not a well-formed s-expression rejects with CDZ0201
pass	read of text with trailing content after the first s-expression rejects with CDZ0201
pass	read of the empty string rejects with CDZ0201 (no s-expression, coded like its malformed siblings)
pass	reading a Rational numerator drops the owned rational and leaves no live heap objects
pass	reading a byte from a sequence built with a RUNTIME element widens it to the Option payload
pass	reading a field of a record built from runtime elements folds to the field
pass	reading a member from an if-of-records resolves each branch's field by its own sorted order
pass	reading a string's scalar addresses scalar values, not bytes
pass	reading a string's scalar in bounds yields Some of the char
pass	reading a string's scalar out of bounds yields None
pass	reading the text a printer produced round-trips to an equal value
pass	reclaim across a nested-compound projection from a peer result
pass	record equality is independent of the order fields are written
pass	record event payloads ride pqueue insort and read both fields from the head
pass	record fields render in canonical (key-sorted) order regardless of source order
pass	record keys differing only in the float field stay distinct and look up
pass	record ordering compares in canonical sorted field order, not written order
pass	record ordering falls to the later canonical field when the earlier ties
pass	record reshaping is subset comparison as explicit projection
pass	records reached via MERGE and via WITHOUT dedupe with the direct build as one Set element
pass	recovering two quantities' magnitudes takes their remainder as bare numbers
pass	recursion is detected through a nested do around the self-call
pass	recursive factorial with a match base case
pass	recursive fibonacci over a RUNTIME argument computes the predecessors correctly
pass	recursive fibonacci with literal match base cases
pass	recursive repeated-squaring modpow over BigInt computes a Mersenne-modulus power
pass	recursive-sum equality discriminates a difference at the DEEPEST node
pass	recursive-sum equality is decided by RUNTIME parameters, both regimes in one export
pass	recursive-sum equality over FLOAT payloads compares by canonical float bytes along the walk
pass	recursively STACKED same-effect handlers — the perform resolves to the DEEPEST frame
pass	red1 the SHADOW-INIT DRAW composed with one shadowed draw — the inner handle over the same effect seeds itself from the outer arm then serves a single draw, and the outer post-resume toll composes with the inner draw threading through the outer continuation (ruled correct by the distinct-effect differential: an inner handler over a DIFFERENT effect computes the identical value)
pass	red2 the SHADOW-INIT DRAW with a PURE inner region — the control for the composition law: with no shadowed draw the outer toll fires exactly once per outer frame and the value matches the naive deep-handler model
pass	redeclaring a built-in unit with its own conversion is admissible
pass	redeclaring a user-declared unit with the same conversion is admissible
pass	reifying a constant NaN into an Ast.Float is rejected (no canonical value form)
pass	reifying a finite float into an Ast.Float is unaffected by the non-canonical guard
pass	reifying a positive-infinity float into an Ast.Float is rejected
pass	rejection sampling draws until the refinement holds — an only-even generator produces an even value
pass	rejection sampling into a narrow range refinement produces only in-range values
pass	rejection sampling terminates on fuel exhaustion when the refinement is unsatisfiable
pass	remainder (%) of quantities of incompatible dimension is CDZ0501
pass	remainder (%) on a floating-point quantity is CDZ0301 (no float remainder)
pass	remainder (%) on same-dimension integer quantities keeps the unit (7m % 3m = 1m)
pass	remainder BY the state — truncated dividend-sign semantics hold as the state walks through negative and positive divisors
pass	remainder by negative one annihilates to zero at every input including the minimum
pass	remainder by one folds to zero but does not discard a trapping runtime operand
pass	remainder on two strings is rejected — the whole family, including %
pass	remove-then-REINSERT of a key with a new value equals the directly-built map
pass	removing a key drops its association and the size
pass	removing a key drops the ASSOCIATION and keeps the sibling — a lookup-after check, not just size
pass	removing a runtime element drops it or is a no-op if absent
pass	removing a runtime element lowers the cardinality only when present
pass	removing an absent key leaves the map unchanged
pass	removing an element yields a set without it
pass	removing from a 3-entry collision node leaves a live 2-entry node; removing to 1 collapses to canonical
pass	resolving a head against a prelude symbol rejects a length-mismatched prefix
pass	resolving a name in a shadowing environment returns the innermost binding's slot
pass	resuming with a Bool answer under an Int64-result operation is a type error
pass	resuming with a Bool next-state under an Int64-seeded handler is a type error
pass	resuming with a String answer under an Int64-result operation is a type error
pass	resuming with a String next-state under an Int64-seeded handler is a type error
pass	resuming with a matching arithmetic next-state under an Int64 seed folds and runs
pass	resuming with a same-shape tuple next-state under a tuple seed folds and runs
pass	resuming with a value of the wrong type for the operation's result is a type error
pass	resuming with a wrong-type value for a compound result type is a type error
pass	rg1 the arm ROTATES the tuple state (a b c)->(b c a) per dispatch and returns the evicted head — four weighted reads wrap the ring
pass	rg2 the rotation DIRECTION flips on the evicted head's parity — even rotates left, odd rotates right, negative-odd exercises truncated mod
pass	rh1 an arm resumes with a LIST answer and the body's read reclaims it
pass	rh2 an arm resumes with an arm-BUILT STRING answer, body measures it
pass	rh3 heap answers across TWO dispatches both reclaim
pass	rh4 heap STATE and heap ANSWER together — both reclaim
pass	rha1 a resumed op with a LIST argument — the arm reads it, everything reclaims
pass	rha2 a resumed op with an in-program STRING argument reclaims
pass	rha3 heap ARG and heap STATE together in one dispatch — both reclaim
pass	rha4 an op ARGUMENT becomes the next STATE — the replaced state reclaims
pass	rhe1 a ROUND-HALF-TO-EVEN halving accumulator — each dispatch adds v/2 rounded half-to-even, the parity of the truncated quotient decides which halves bump
pass	rla1 a runtime List.at in bounds reads the element (Some)
pass	rla2 a runtime List.at out of bounds takes the None arm
pass	rle1 RUN-LENGTH tracking — the 3-tuple state carries (last,run,best); the n=5 seed extends one run to five, n=7 breaks it at two
pass	rle2 a RUN-LENGTH ENCODER state — equal-to-last feeds bump the tail pair's count via List.update at len-1, fresh values push a new pair; the n=5 seed merges the middle literal into one long run
pass	rlm1 a SLIDING-WINDOW rate limiter — allow prunes expired timestamps then admits only under the cap of two per ten ticks; the seeded third request either bounces inside the window or lands beyond it and steals a later slot
pass	rls1 left shift multiplies by 2^count; value overflow and out-of-range count trap
pass	rlv0 a returned 2-element list keeps exactly its own 2 cells alive
pass	rlv1 a returned list plus a DISCARDED sibling temp — live count stays exactly the result
pass	rlyB single-use arm-perform control (answer only, state untouched) — B's handler arm PERFORMS the outer A (same op name, same signature) doubling the argument and folding A's answer into B's state, so every relay draw advances BOTH threads and the identity question routes THROUGH an arm-perform under schema-hash-only dispatch
pass	rmn1 MIN %/-1 is 0 (not a trap) on both fold and runtime — the remainder path avoids the divide-overflow trap
pass	rmn1 a ROMAN-NUMERAL accumulator with subtractive correction — feeding a numeral LARGER than its predecessor retro-subtracts the predecessor twice (the IX rule), the seed picks the second numeral as five or one, and the correction fires at DIFFERENT positions so the totals cross (nineteen versus twenty-three)
pass	rmn2 a RUNTIME MIN %/-1 returns 0 while the same operand under / -1 would trap (rem is total, div is not)
pass	rmp1 a RECORD state wrapping a Map plus a counter — computed perform keys; the arm answers 10*lookup + the ADVANCED counter so both fields are observed
pass	rn1 a RUNNING-AVERAGE state (sum,count) — three feeds then a truncating divide read, negative total exercises toward-zero
pass	rnc1 a runtime Option carrying a user sum is matched by a nested constructor pattern
pass	rop1 recursive const (Option Int64) param countdown reaching a trap folds to CDZ0304 (was a wasm hang)
pass	rop2 recursive const (Option Int64) param recursion on the value path folds to 99
pass	rop3 payload extracted to a bare Int64 const param before recursing folds (CDZ0304)
pass	rot1 a ROTATION-CIPHER state — enc shifts a letter index with the double-mod negative-normalization idiom in the arm, tune drives the shift NEGATIVE and the normalization recovers the canonical class
pass	round-trip byte-rope consumer alongside a plain export — the consumer
pass	round-trip byte-rope consumer alongside a plain export — the plain
pass	round-trip: a HIGHER-ORDER closure arg composed with a SUM result
pass	round-trip: a HIGHER-ORDER closure — its argument is itself a closure built in-guest
pass	round-trip: a String-returning consumer of a closure
pass	round-trip: a closure taking a NESTED compound (Tuple of Tuples) arg
pass	round-trip: a closure taking a SUM (Option) arg built in-guest
pass	round-trip: a closure taking a String arg built in-guest
pass	round-trip: a compound consumer + a byte-rope consumer of the same closure — the byte-rope
pass	round-trip: a compound consumer + a byte-rope consumer of the same closure — the compound
pass	round-trip: a compound consumer alongside a plain export — the plain
pass	round-trip: a compound-arg closure whose consumer returns a compound
pass	round-trip: a consumer applies a closure taking a List arg built in-guest
pass	round-trip: a consumer applies a closure taking a Record arg built in-guest
pass	round-trip: a consumer applies a closure taking a Tuple arg built in-guest
pass	round-trip: a consumer applies the handed-back closure and returns Bytes
pass	round-trip: a consumer applies the handed-back closure and returns a List
pass	round-trip: a consumer applies the handed-back closure and returns a tuple
pass	round-trip: a consumer returns a Map from the closure result
pass	round-trip: a consumer returns a Map whose VALUE is a list
pass	round-trip: a consumer returns a Result (Err type pinned) from the closure result
pass	round-trip: a consumer returns a Result reaching BOTH variants
pass	round-trip: a consumer returns a Set built from REPEATED closure application
pass	round-trip: a consumer returns a Set built from the closure result
pass	round-trip: a consumer returns a byte-rope built from a single closure result
pass	round-trip: a consumer returns a list of tuples from repeated closure application
pass	round-trip: a consumer returns a record built from the closure result
pass	round-trip: a consumer returns a tuple CONTAINING a list built from the closure result
pass	round-trip: a consumer returns an Option built from the closure result
pass	round-trip: a consumer returns an Option of a tuple
pass	round-trip: a consumer returns an Option whose payload is a List
pass	round-trip: a higher-order closure applied to TWO distinct inner closures
pass	round-trip: a higher-order closure whose inner closure CAPTURES and is applied twice
pass	round-trip: a higher-order closure whose inner closure takes an UNANNOTATED compound param
pass	round-trip: a producer's returned closure captured an inner closure built in-guest
pass	round-trip: a scalar consumer + a List consumer of the same closure — the list
pass	round-trip: a scalar consumer + a List consumer of the same closure — the scalar
pass	round-trip: a scalar consumer + a compound consumer of the same closure — the compound
pass	round-trip: a scalar consumer + a compound consumer of the same closure — the scalar
pass	round-trip: a scalar consumer and a byte-rope consumer of the same closure — the byte-rope one
pass	round-trip: a scalar consumer and a byte-rope consumer of the same closure — the scalar one
pass	round-trip: an UNANNOTATED inner closure with a List param via the context arrow
pass	rowacc5 the walk carries an ACCUMULATOR — the post-recursion read still sees the callee-advanced state (finding #12 accumulator face)
pass	rp1 a 50-concat deep rope's byte-len walks the tree and the rope reclaims clean
pass	rp1 two-site arm + a state-REPLACING second op over a SCALAR state
pass	rp2 a RECORD op argument read by field beside an inner sum-state match — the record-payload face of the per-dispatch arm
pass	rp2 a char-scan with String.at across every seam of a 50-concat rope counts exactly (per-read leak family at scale)
pass	rp3 a slice SPANNING the seams of a deep rope reads exact length and content
pass	rp3 the record payload carries a NESTED record — two-level field projection inside the arm beside the state match
pass	rpc1 a RIPPLE-CARRY counter — inc answers the number of bits FLIPPED by that increment (the XOR of old and new popcounted, the amortized-analysis witness), pop reads the live bit count, and the seeds start at 10 and 0 so the carry chains fire at different steps
pass	rpl1 an OP-LOG REPLAY state — apply advances the value and logs its delta, replay re-applies the WHOLE log to the current value keeping the log intact, so a second replay after more logging compounds
pass	rpn1 an RPN EVALUATOR — pushes stack operands, the operator ops pop two and push the result; the (n+3)*2 program evaluates through five dispatches with stack depths and intermediates exposed
todo	rpp1 a scalar-fielded Record entry param projects both fields
todo	rpp2 an open-row helper projects a field of the boundary Record param
todo	rpp3 a Record entry param with a heap list field measures both
todo	rpp4 a scalar Tuple entry param projects both positions
pass	rps2 the RPS judge with the opponent move HOISTED through match binders — the LCG advance and the mod-3 move bind once per dispatch instead of recomputing per branch, same protocol and answers
pass	rq1 a RATIONAL handler state — three exact fractional adds (1/2 + 1/3 + 1/6) land on a WHOLE value — the canonical n/1 render pinned
pass	rq2 floor/ceil/numerator/denominator READ a rational state — canonicalization (8/4 to 2/1) and negative rounding both pinned
pass	rq3 a FRACTIONAL drain with a sign guard — subtracting 1/3 per tick until the rational state crosses zero, the recursion counts full ticks
pass	rr1 RECORD handler state updated by one arm, field-read by a SIBLING arm plus a third counter arm — the record twin of the collection cross-arm shape
pass	rr1 one draw consumed THREE times (squared, scaled, summed) — a single dispatch, the binder multiply-read
pass	rr2 the SQUARE of a difference of two draws — composite arithmetic over one advancing thread, zero row included
pass	rrb1 a ROUND-ROBIN scheduler with a skip mask — turn advances cyclically to the next unskipped worker answering its id (or -1 when all four are out), skip marks a worker answering the popcount, and the SEED sets the starting position so the same skip sequence yields different service orders
pass	rrl1 a REFILL-ON-READ rate limiter — every take first refills the bucket by the seed rate clamped at capacity ten then serves or answers the negated shortfall (tokens kept on refusal), and the faster drip keeps the refusals shallow while the slow one starves deeper on the same request stream
pass	rrm1 a NESTED record-rest re-match peels two residual layers with a RUNTIME field value
pass	rrm2 record-rest boundary faces: if-selected scrutinee, ALL-fields-named EMPTY residual, and rest-only gathering the whole record
pass	rrow1 Record.without over RUNTIME leaves drops the field and the rest projects
pass	rrow2 Record.project over RUNTIME leaves keeps the named fields readable
pass	rrow3 a Record.merge result with a COMPUTED runtime leaf VALUE-EQUALS the directly-written literal across field order
pass	rs1 a RECORD state where each op owns a FIELD — bump advances a, scale multiplies b, interleaved
pass	rs1 a RESULT built from draw parity crosses dispatch as an op ARGUMENT — Ok scales with state, Err folds in negated
pass	rs2 the arm updates the record state via Record.with — field b is held by the functional update across dispatches
pass	rs3 a NESTED record state — the arm functionally updates the inner record's x by y and bumps the outer counter
pass	rsh1 (seq-203 #5823 reduce-share SOUNDNESS gate) a handler-applying fn reached via a beta-copy at a DOUBLING fan-out: each f_i applies f_{i-1} TWICE, f0 delegates its lambda arg b under (handle R 5 … resume s s), so f0 answers 5 and f_i doubles it. The (head->resolved-body) reduce_cache key (which killed the 2^N compile-hang) SHARES the fan-out's identical (body,args) reductions across sites; the handler resumption is threaded correctly THROUGH the shared reductions, so main = 5*2^5 = 160. A future reduce_cache-key change that re-opened the re-parent scope contamination would corrupt the shared effect reductions and flip this value (v-effects a39; co-verified v-compiler-perf byte-compare fixed==parent). Complements the pure-capturing 09-functions adv->202 twin.
pass	rsl1 a Result whose Ok payload is a HEAP LIST snapshot of the state — the first snap Errs on the empty list, the second Oks the grown snapshot, both cross resume
pass	rsl1 a recursive-sum linked list is built and folded at runtime
pass	rsn1 a shift with a nested checked-add value operand keeps both guards
pass	rsp1 a ROLE-INVERTING parity splitter — feed routes each value to the even or odd accumulator, flip INVERTS the routing so post-flip evens land in the odd bucket, the flip answers the packed snapshot
pass	rsr1 a signed right shift is arithmetic (sign-extending)
pass	rsv1 DETERMINISTIC reservoir sampling — an LCG threaded beside the reservoir decides keep-or-replace by count-modulus, seeds route which offers displace the kept element
pass	rsw1 the arm's Ok/Err verdict FLIPS between two identical performs as the state passes the payload — both variants cross and unwrap
pass	rt1 Ok/Err resume values with a DEPENDENT second dispatch — the sign of the falling state flips Ok to Err
pass	rt2 an Err SHORT-CIRCUITS a recursive Result walk — Ok accumulates, the first Err multiplies out and stops
pass	rt3 the Err VALUE seeds a RECOVERY region — a fallback same-effect handle runs inside the Err arm
pass	ruf1 a twice-unwrapped payload persistently updated through one binding reads original through the other (the two-sided balance fence)
pass	ruf2 the two-sided balance fence over a twice-unwrapped Result Ok payload
pass	ruf3 the two-sided balance fence over a twice-unwrapped user-generic payload
pass	run-length encode/decode round-trips a runtime list of tuples
pass	runtime < on a self operand is false for finite AND NaN (irreflexive, stays false)
pass	runtime <= on a self operand is true for finite but FALSE for NaN (no reflexivity fold)
pass	runtime >= and > on a self operand mirror <= and < including the NaN self case
pass	runtime Ast.encode of a nested Ast.List serializes via the recursive walk
pass	runtime Ast.encode of an Ast.Int serializes via the ast-encode heap op, canonical length
pass	runtime Ast.print renders a doubly-nested Ast.List
pass	runtime Ast.print renders a nested Ast.List byte-identical to the compile-time fold
pass	runtime Ast.print renders a top-level Ast.Int to its decimal text
pass	runtime BigInt add/subtract carries and borrows exactly across the 64-bit limb boundary
pass	runtime BigInt addition runs on the arbitrary-precision runtime
pass	runtime BigInt arithmetic leaves no live heap objects (balanced)
pass	runtime BigInt equality holds for values reached by different arithmetic
pass	runtime BigInt remainder runs on the arbitrary-precision runtime
pass	runtime Blake3.of equals the compile-time fold of the same bytes (section-9 byte-identity)
pass	runtime Bools order false below true with compare and equality agreeing
pass	runtime Bytes ordering is content-lexicographic over unsigned bytes — the blessed total order
pass	runtime Bytes value-equality distinguishes different content
pass	runtime Char equality and ordering compare by Unicode code point (scalar i32)
pass	runtime Float32 arithmetic computes at f32 precision — 0.1 + 0.2 equals 0.3 exactly at binary32
pass	runtime Float32 ordering compares at f32 precision — same-bits literals are not ordered
pass	runtime Rational DIVISION inverts the divisor and reduces to lowest terms
pass	runtime Rational MULTIPLICATION of two parameter-built fractions reduces to lowest terms
pass	runtime Rational arithmetic adds two parameter-built fractions exactly
pass	runtime Rational arithmetic that cancels to zero normalizes to canonical zero over one
pass	runtime Rational arithmetic that reduces to a whole normalizes to denominator one
pass	runtime Set.of at THREE different element types in one program all build via per-site monomorphic folds
pass	runtime Set.of at two different element types in one program each build via a per-site monomorphic fold
pass	runtime String.concat is associative — (a·b)·c equals a·(b·c), both equal the flat concatenation
pass	runtime UInt64 division and remainder above the Int64 boundary compute unsigned
pass	runtime addition computes in range and traps on overflow, not wrapping
pass	runtime bitwise and/or/xor over two Int64 operands compute correctly
pass	runtime char-literal match dispatches by Unicode code point
pass	runtime division and remainder agree across ALL FOUR sign quadrants
pass	runtime float equality compares by canonical byte form
pass	runtime float inequality compares by canonical byte form
pass	runtime float multiplication overflows to infinity, not a trap or a wrap
pass	runtime float ordering is a strict/non-strict partial order over finite values
pass	runtime float ordering treats negative and positive zero as equal, unlike equality
pass	runtime multi-limb BigInt division satisfies q*d + r = n with a bounded remainder
pass	runtime multiplication computes in range and traps on overflow
pass	runtime shaped equality of two owned List-Float64 operands reclaims both (no live objects)
pass	runtime signed division traps on divide-by-zero and on Int64.min over -1
pass	runtime signed division truncates toward zero and remainder takes the dividend sign
pass	runtime signed remainder of Int64.min by -1 is zero, not an overflow
pass	runtime string ordering compares bytes UNSIGNED (a multi-byte scalar exceeds ASCII)
pass	runtime string ordering makes a proper prefix less than its extension
pass	runtime string ordering surfaces all four relational operators
pass	runtime structural equality of two owned cons-lists reclaims both operands (no live objects)
pass	runtime structural equality over a GENERIC RECURSIVE sum walks the whole tree
pass	runtime structural equality over a payload-carrying sum agrees on both backends
pass	runtime subtraction computes in range and traps on overflow at both boundaries
pass	runtime tuple concatenation preserves element order across the seam
pass	runtime unsigned division is by magnitude and traps on divide-by-zero
pass	runtime-packed nibbles MATCH back out through bits patterns - the pack-unpack identity
pass	runtime-payload `?`s in BOTH arms of a RUNTIME-scrutinee match each resolve the boundary
pass	runtime-payload `?`s in BOTH branches of a runtime if take their own continuations
pass	rur1 an unsigned right shift is logical (zero-filling)
pass	ruw1 a fresh Result Ok with a list payload matched twice leaks the payload like Option
pass	ruw2 a fresh user-generic sum with a list payload matched twice leaks the payload identically
pass	ruw2b a PARAM boxed-sum scrutinee matched twice, first arm CONSUMES the payload (corpus twin of the b2_disjoint lib test)
pass	ruw3 a fresh Option matched THREE times still reads three — the census counts objects not refs
pass	rv1 the inner handler ARM resumes with an OUTER draw — the resume VALUE expression performs against the enclosing handler
pass	rv2 the inner arm's resume value SUBTRACTS two outer draws — cross-handler order inside the arm, with a doubling outer state
pass	rw1 a nested handle at a performing RECURSION's exit leaf, then a trailing outer draw
pass	rw1 two sequential Record.with updates each take a DRAW — the second write sees the advanced state, projections read both back
pass	rw2 draw PARITY picks WHICH field Record.with updates — projections of both fields show exactly one write landed
pass	rw3 each Record.with value PROJECTS from the record it updates plus a draw — self-referential functional updates chain through the thread
pass	rwc1 an open-row accessor over runtime records of two widths, fifty frames, reclaims completely
pass	rwc2 an open-row accessor reads a HEAP (list) field through the row, fifty frames, reclaims
pass	s2a Char const-param equality in const recursion trap surfaces CDZ0304
pass	s2b INLINE record projection in the recursive argument trap surfaces CDZ0304
pass	s2c INLINE tuple destructure in the recursive body trap surfaces CDZ0304
pass	s2d record-returning const helper projected in the recursive argument trap surfaces CDZ0304
pass	s6a wrapper binding shadows an outer binding the BODY reads
pass	s6d the floated wrapper init can trap — division by the argument stays ORDERED before the conditional
pass	sa1 STRING op arguments measured into a scalar state — the empty string is a real zero-length argument
pass	sa2 a string BUILT from a prior draw's branch becomes the next op's argument — the tag arm reads the post-pick state
pass	sa3 STRING resume values — the arm builds a rope per dispatch branching op-arg vs live state, body concatenates two
pass	sab1 a CONDITIONALLY-aborting handle inside the SEED — the abort value or the fall-through both become the outer init
pass	salg1 the set-algebra laws hold over two runtime HAMT sets — idempotence, commutativity, self-difference, membership
pass	same-Instant queue entries resume in FIFO insertion order (§3.4 tie-break)
pass	same-arity tuple match arms share an equal element and sink the differing one
pass	same-effect shadowing with ADVANCING states — the outer state survives the inner handle and resumes advanced
pass	same-effect shadowing with ADVANCING states, HEAP (tuple) state — the outer heap seed survives the inner handle and threads correctly across the straddle
pass	same-inner-type quantities of one dimension add cleanly and run
pass	same-key churn over HEAP (rope String) values drops each stale value and keeps the last
pass	same-key-set record match arms share an equal field and sink the differing one
pass	same-length list match arms share an equal element and sink the differing one
pass	same-payload different-variant sum keys are DISTINCT map keys
pass	same-typed List operands to + reject without a phantom Int64 clash
pass	same-typed String operands to % reject without a phantom Int64 clash
pass	same-typed String operands to + reject without a phantom Int64 clash
pass	same-variant sums order by PAYLOAD when the discriminant ties
pass	same-width integers of different signedness do not promote (signedness alone is a mismatch)
pass	sar1 a recursion-built set accumulator to-list reads its sorted head under the trap detector (CDZ0304)
pass	sar2 a recursive const walk SUMS a folded Set.to-list
pass	sat1 reading through an empty-PREFIX split-at folds to the projected element
pass	sat2 reading through an empty-SUFFIX split-at folds to the projected element
pass	sat3 an interior split's suffix element is read correctly (not an empty-side-only fold)
pass	sb1 String.to-bytes of a draw-picked string read at a draw index — the text-to-bytes bridge follows the thread twice
pass	sb1 an op ARG drawn at a shadow boundary — both the arg draw and the consuming dispatch home to the INNER handler
pass	sb3 the op ARG draws from effect B while the dispatch homes to effect A — two paired dispatches, both states advance
pass	sb4 the innermost seed is a COMPOSITE of two effects' dispatches — B's draw feeds A's add, the result seeds a fresh B shadow
pass	sbd1 two occurrences of one constant Bytes literal — branch-selected use, length reads, and runtime equality across the pair
pass	sbd2 a fifty-frame recursion re-evaluating a constant Bytes literal each frame reclaims to zero
pass	sbn1 a runtime negative round-trips through signed i16/i8 segments with exact wrap semantics
todo	sbn2 a runtime utf8 CONSTRUCTION segment declines pending the encode increment (utf8 is pattern-only)
pass	sc1 STRING literal-arm dispatch on a draw — the hi arm re-performs and measures, the lo arm is constant
pass	sc1 a THREE-link seed CHAIN of DISTINCT effects — each handle's whole region value seeds the next
pass	sc1 runtime String.scalar-at round-trips the cadenza hop — multi-byte scalar indexing + out-of-range
pass	sc2 EQUALITY of two string draws routes the branch — the n=3 row crosses the threshold between draws
pass	scalar length counts Unicode scalar values, not bytes
pass	scalar length of a supplementary-plane character is one scalar value
pass	scaling a Float64 quantity by a bare integer is a numeric-type mismatch
pass	scaling a quantity by a bare number of its own numeric type keeps its dimension
pass	scaling a quantity by a dimensionless quantity keeps its dimension
pass	scaling an integer quantity by a bare integer of its own type keeps its dimension
pass	scan emits the seed plus one accumulator per element (n+1 outputs)
pass	scan over an empty iterator emits just the seed
pass	scan threads its accumulator left-to-right (an order-sensitive folder)
pass	scan's kth emitted accumulator is the running fold of the first k elements
pass	scc1 a HIGH-WATER STRING state — the arm compares each drawn string lexicographically against the champion, keeps the max, and the final read exposes the winner's length
pass	sd1 a draw COUNT drives string repetition through a recursion — String.byte-len pins how many times the thread said to concat
pass	sd1 the #set literal resolves first-class (D2): membership, len, and order-independent equality with an insert-built set
pass	sd2 draw parity picks WHICH string each op returns — the concat order of two draws is visible in content equality
pass	sd3 a draw-bounded String.slice window — start and end both come from the thread, byte-len pins the window width
pass	se1 a SET handler state with dedup dynamics — re-adding an existing element leaves the size fixed
pass	se1 five draws collected into a Set under a cycling state — duplicates collapse, len and membership pin the distinct draws
pass	se2 MEMBERSHIP of a draw decides a branch that draws again — the set gates the thread's continuation
pass	se2 a membership-GATED arm — first visit admits and records, a revisit answers negated without growing
pass	se3 a DRAIN-style arm removes on hit — the second take of the same key routes to the miss path
pass	se3 a set BUILT from cycling draws probed by a LATER state read — the in-cycle probe hits, the off-cycle probe misses
pass	self-difference of a 100-element trie IS the canonical empty set by equality
pass	self-identity comparison over a TRAPPING lazy binding preserves the trap (does not fold to a constant)
pass	self-identity comparison over a runtime checked-shift binding preserves the out-of-range trap
pass	self-identity fold over a TRAP-FREE (pure) lazy binding still fires
pass	self-identity subtraction over a TRAPPING lazy binding preserves the trap (does not fold to 0)
pass	self-operand division is NOT folded to one — it still traps at zero
pass	self-operand subtraction and xor are zero, and-or are the operand, on a runtime value
pass	sequenced memoize helpers with a local let thread the Map-state out-state (the memo-spine shape)
pass	set algebra descends into FLOAT-leaf tuple elements
pass	set algebra over a 3-entry collision node splits and rebuilds the colliding keys by content
pass	set algebra unifies a rope String element with its flat twin across operands
pass	set equality is independent of the order elements are written
pass	set union is associative on generated inputs (the order merges are grouped does not matter)
pass	set union is commutative on generated inputs (a grow-only-set CRDT merge converges)
pass	set union is idempotent on generated inputs (re-delivering a set changes nothing)
pass	sets reached via DIFFERENCE and INTERSECTION equal the directly-built set
pass	several independent do-local functions each resolve their own call
pass	several same-shaped recursive defs each resolve their own parameter
pass	sf1 an inner SAME-effect handler's arm re-performs the effect it discharges — the re-perform routes to the OUTER handler, both states advance independently
pass	sf2 the inner arm's NEXT-STATE re-performs to the outer — the forward sits in the state-thread position, not the resume value
pass	sfl1 a PERFECT-SHUFFLE position tracker — each shuffle doubles the tracked card's position mod seven (the out-shuffle orbit on eight cards, position seven fixed), where packs position and shuffle count, and the two seeds ride the SAME 3-cycle orbit entered at different points so the rows are rotations of each other
pass	sfu1 TWO-SENSOR weighted fusion — each read folds the sample into a running weighted mean by est*wsum plus v*w over the grown weight (truncating), the trusted sensor carries weight three while the seed shapes the second sensor's trust, and the final draw reads the accumulated weight
pass	sg1 a string BUILT by a recursive walk of draws — one H/L character per dispatch, exact content compared
pass	sg2 a stable PREFIX slice of a growing rope — String.slice (start,END) of the state per dispatch, prefix identical across growth
pass	sg3 a ONE-SHOT string lock — the arm string-compares op arg vs state, consuming the key on the first match
pass	sg4 TWO-SIDED rope growth — the op arg's sign picks append-right vs prepend-left, exact content across three sign patterns
pass	sg5 the GROWING string state is a MAP KEY per dispatch — each draw looks up the current rope in a literal map
pass	sg6 the rope's LENGTH PARITY routes its own growth — odd appends two, even appends one; four draws read 1,3,4,5
pass	sg7 byte-len vs scalar-len DIVERGE on a growing multi-byte rope — each dispatch reads the difference (one per accent)
pass	sga1 the arm answers with SET ALGEBRA over its state and an argument-built set — union, intersection, and difference sizes cross dispatch
pass	sh1 a NESTED handler for the SAME effect shadows the outer — inner draws hit the inner state, the draw after hits the outer
pass	sh2d a let bound in a handle body, read by a NESTED handle's seed — folds (let-crossing seed freshened)
pass	sh2n the config-fetch idiom with its perform LET-LIFTED — folds (let-crossing seed freshened)
pass	sh2x a let binder feeds a nested handle's SEED and is read again AFTER the region — the freshened seed reference and the tail reference are the same binder
pass	shadowing resolves nearest-wins through the scope-skip index
pass	shb1 a RUNTIME left-shift computes below the value-overflow boundary and traps at it (fold-vs-runtime agree; << is checked *2^n)
pass	shd1 DYNAMIC shift amounts from the state — two drawn widths, the value-63 draw traps the checked shift overflow
pass	shift-by-zero is a no-op on a runtime operand for both directions
pass	shl1 an inner let SHADOWS a draw binder — the shadow scales it locally, the original stays visible after the shadow closes
pass	shr1 a let-bound inner list referenced twice in a returned list stays one shared object (four cells)
pass	shr2 a let-bound inner list referenced twice in a returned tuple stays one shared object (three cells)
pass	shr3 two separately-built identical inner lists are distinct objects (six cells, the unshared control)
pass	shrinking an @invariant newtype to EMPTY traps at the rebuild — the invariant catches the crossing
pass	shrinking reports the minimal failing input
pass	shv1 the RIGHT-shift twin: a trapping lhs surfaces before the const out-of-range count trap
pass	shv2 a trapping lhs surfaces before a RUNTIME out-of-range shift count
pass	shv3 an OVERFLOW-trapping lhs surfaces its own kind before the shift count trap
pass	shx1 two SIBLING handles of one effect in one expression keep independent states
pass	shx2 a SEQUENTIAL second handle is seeded by the first handle's result
pass	shx3 a helper installing a FRESH handle per call gets a fresh state each time
pass	si1 a WHOLE inner handle expression as an outer handler's SEED
pass	si2 the SEED handle ABORTS — the outer state is the abort value, the seed body's tail never runs
pass	sibling match arms each let-binding a DIFFERENT-WIDTH value get disjoint scratch slots, not an invalid component
pass	sibling patterns over a two-sum tuple diverge on PER-POSITION literals
pass	sibling performs feeding a subtraction witness the advancing state non-commutatively
pass	sibling recursive calls sequenced through let bindings thread state
pass	sibling-recursive effect threading is left-to-right (order-observing)
pass	sie1 a SIEVE SEGMENT — each sieve dispatch clears every multiple of its prime by STRIDED List.update writes in one recursive walk, count answers survivors, probes read prime and composite slots
pass	signed 8-bit addition that overflows its width is rejected at compile time
pass	signed div by two is exact at Int64.min
pass	signed power-of-two div/rem truncates toward zero (Int64 + narrow Int32)
pass	signed zeros INSIDE a sum payload are DISTINCT Set keys (the ±0.0 edge survives the wrapper)
pass	signed-wrap at a NARROW width wraps mod 2^width (Int8 127 + 1 = -128)
pass	signed-wrap does NOT wrap an UNSIGNED overflow — unsigned falls through to the Trap default (CDZ0304)
pass	signed-wrap: a const-FOLDED recursion that overflows wraps per-op (not CDZ0302/CDZ0304)
pass	signed-wrap: a constant * overflow wraps (Int64.max * 2 = -2)
pass	signed-zero Float32 tuple-key components stay distinct at binary32 width
pass	signed-zero float components make DISTINCT tuple keys
pass	signed-zero float fields make distinct record keys
pass	single row ops on a map-borne record compute — without alone and extend alone
pass	size after a runtime-key insert reflects replace-vs-add
pass	size after a runtime-key remove reflects whether the key was present
pass	sk1 a FILTERED fold — only even draws accumulate but every draw advances the thread, kept-count and span both pinned
pass	sk4 a Set of TUPLES as handler state with three arms — insert in one arm, contains in a sibling
pass	sk4c a closure through the one-shot helper capturing an outer let binder folds via the escaped-closure recovery (pinned-capture memo preserved)
pass	sky1 SKYLINE visibility with demolition — feeds answer whether the building tops all before it, demolish removes EVERY copy of the max forcing the threshold to recompute from survivors
pass	sl1 a STRING slot grows by a mod-picked suffix and its BYTE-LEN seeds a nested handle — the composed face of the slot-clobber fix
pass	slc1 a SINGLE-consumed String.slice result leaks its dup (the minimal post-#4425 residue cell)
pass	slc2 a slice-of-slice (two view layers) multi-consumed leaks both layers' dups
pass	slc3 a slice inside a DUAL-USED tuple (projection + walker) stacks the dqe leg-1 leak on the slice residue
pass	slicing a byte sequence built at run time yields Some of the sub-range
pass	slicing a runtime byte sequence past the end yields None
pass	slicing a runtime byte sequence with a negative start yields None
pass	slicing past the end of a byte sequence yields None
pass	slicing with a negative start yields None
pass	slicing with a start near i64::MAX is out of range, not a trap
pass	slicing with start+len overflowing i64 is out of range, not a wrong slice
pass	slmin11 tuple(scalar,string) state, put rebuilds the tuple with a branch-picked String.concat suffix, 2 puts + 2 reads (invalid-wasm slot-clobber: the rope-retain dup and the counter step must not share a scratch slot at two widths)
pass	smj1 a sum match whose BOTH arms are owned producers reclaims (join = Owned)
pass	smj2 a borrowed sum payload read twice under its live scrutinee is not freed early
pass	smj3 5000x a borrowed sum payload read twice does not double-free (leak-drift stress)
pass	smk1 a STRING-KEYED Map state whose keys are BUILT IN THE ARM — parity routes each value to a concat-computed bucket, accumulate-or-insert answers the bucket total
pass	sms1 scalar-indexed String.slice isolates a multi-byte codepoint and round-trips on a RUNTIME string
pass	smy1 a SYMBOL-KEYED Map state — the op takes a Symbol and routes accumulation by interned identity, the same label from two different dispatches lands in one bucket
pass	sn1 SIGNED Int8 shifts — arithmetic >> sign-extends; << range-checks the negative boundary
pass	sn1 a #set literal with RUNTIME elements dedups at construction
pass	sn1 a snapshot op resumes the WHOLE tuple state as a value — the body destructures the live pair after mixed bumps
pass	sn2 TWO whole-state snapshots straddle interleaved bumps — the first snapshot is IMMUTABLE, the pair differ by exactly the bumps between them
pass	sn2 a #set of STRING elements content-hashes against a runtime-built equal probe (with literal dedup)
pass	sn3 the empty #set() literal composes with insert
pass	sn3 the snapshot crosses to a DEF helper — the crossed state tuple is consumed by a function parameter, not a body match
pass	sos1 a SET-OF-STRINGS state with ARM-BUILT rope keys — the concat-built key dedups structurally against the FLAT seed literal, parity routes which inserts are no-ops
pass	sp1 SCALAR param + 2-field record result lifts (minimal face, no spill)
pass	sp1 a FLIPPED tuple-payload spine (rec-ref at Elem 0) RECLAIMS the full spine (position-matrix cell)
pass	sp2 SCALAR param + 20-field record result lifts (spill-sized, same fix)
pass	sp2 a RECORD-payload spine RECLAIMS the full spine (position-matrix cell)
pass	sp3 SCALAR param + record result with an Option field (Some side)
pass	sp3 a THREE-tuple payload spine (rec-ref at Elem 2) RECLAIMS the full spine (position-matrix cell)
pass	sp3n SCALAR param + record result with an Option field (None side, branch-selected)
pass	sp4 SCALAR param + bare option result
pass	sp5 SCALAR param + list result
pass	sp6 TWO scalar params + 2-field record result (multi-scalar face)
pass	sp7 SCALAR param + variant-with-payload result (sum face of the same gate)
pass	spc1 a CONSTANT-origin sum spine (branch-selected literal) builds mortal and its walk leaks the full spine (joins the sp position matrix)
pass	spj1 a single-use projected list consume stays on the FBIP fast path
pass	splice flattens where unquote nests
pass	splicing a non-list value into a quasiquote is a type error
pass	splicing a string value into a quasiquote is a type error (a string is not a list to splice)
pass	splicing an integer literal directly is a type error
pass	splitting a tuple at a position yields a prefix and a suffix
pass	splitting a tuple at its full arity yields an empty suffix
pass	splitting a tuple at zero yields an empty prefix
pass	splitting a tuple beyond its arity is rejected
pass	splitting a tuple with a runtime element addresses the prefix and suffix
pass	spr1 a SPRINKLER scheduler on a depleting tank — water runs a zone for the minimum of its indexed duration and the remaining tank (shortfalls accumulated), refill restores the seed-shaped capacity answering the shortfall so far, and the small tank starves two zones in the first pass and one in the second while the large tank never starves
pass	spr1 a sum payload heap child consumed while the scrutinee is live is retained
pass	spr2 a threaded sum's payload consumed per-iteration is retained (no drift)
pass	spx1 a Bool entry param selects a branch
todo	spx2 a Char entry param declines pending a boundary representation
pass	spx3 a UInt64 entry param increments in its own width
pass	spx4 Int8 and Int16 entry params sign-extend and sum
pass	sq1 two SEQUENTIAL handles of the SAME inner effect, each seeded by a fresh outer draw — independent inner threads, one advancing outer thread
pass	sqm1 SQUARE-AND-MULTIPLY over the state — (base,acc) squares every dispatch, multiplies on 1-bits mod 1000, the n-bit's effect observed by a final read
pass	sqte1 a Symbol tuple element compares by content in a runtime tuple equality
pass	sr1 a performing self-recursive walk's result SEEDS an inner handle, then a trailing outer draw
pass	sr2 a Result-typed op whose Err carries a STRING reason — the body matches Ok payload vs the reason by string equality
pass	sr3 the Err reason is BUILT in the arm — String.concat of a prefix and a sign-picked tag, matched against literals in the body
pass	srf1 fifty runtime-keyed persistent Set.removes against an immortal 40-element HAMT never corrupt it (the remove-side set FBIP fence)
pass	srf2 intersection and difference off the immortal 40-set with a runtime probe: exact results, immortal untouched
pass	srm1 a set-rest residual EXCLUDES a named element even when construction DUPLICATED it, with a runtime member
todo	srm2 a NESTED set-rest re-match peels two residual layers (should-work: the residual is a first-class set)
pass	srw1 a GROWING STRING state with a computed slice window per dispatch — each grow appends two chars, the arm answers the byte-len of an interior window built from the drawn offset
pass	ss1 a STRING handler state grows per dispatch — each arm returns the pre-growth length, growth is op-arg-branchy
pass	ss1 a TAIL self-loop walk over a tuple-payload sum spine RECLAIMS the full spine (INC1 pt3 crossed the #4597 fix's boundary)
pass	ss2 a STRING state GROWS by a parity-picked suffix per dispatch — byte-len pins the concatenation history
pass	ss2 string state grows across a DO sequence — discarded draws still advance the rope
pass	ss2 the NON-tail walk over the same tuple-payload spine RECLAIMS identically (the tail/non-tail readings agree at this boundary)
pass	ss3 fifty framed tail walks over fresh tuple-payload spines RECLAIM per-frame (was per-frame full-spine leak; flipped by the tuple-payload increment)
pass	ss3 nested SAME-effect handlers with independent STRING states — inner self-doubles, outer appends
pass	ss5 an op ARG selects WHICH tuple-state slot to bump — index-routed slot mutation, four dispatches revisit slot 0
pass	ss6 the slot SELECTOR is derived from the state itself — |a mod 3| routes each bump, a same-slot revisit and a two-slot walk both pinned
pass	ss7 a 2-ary op carries SELECTOR and MAGNITUDE — which slot and by how much are both payload-driven, a trailing read pins slot 0
pass	ssc1 String.slice indices are SCALAR (codepoint) positions, not bytes — mid-codepoint cuts are impossible by construction
pass	ssc1 a String entry param captured by a local closure applied twice
pass	ssc2 a String entry param relayed to a helper def that borrows its byte-length
pass	ssc3 a String entry param read inside a handle body alongside a dispatch
pass	ssd1 two occurrences of one constant String literal — branch-selected use, byte-length reads, and runtime equality across the pair
pass	ssd2 a fifty-frame recursion re-evaluating a constant String literal each frame reclaims to zero
pass	ssm2 the SEISMOGRAPH at three readings — deviation over four logs an event (peak by inline max, baseline nudged up), quiet drifts one signed step, and the seeds disagree on whether the THIRD reading is an aftershock event or a quiet drift so even the event counts split
pass	st1 a handle expression as ONE operand of an enclosing pure sum — the pure operand and the handled region compose
pass	st1 the SAME effect handled at THREE depths — each draw resolves to the innermost open frame, outer frames resume as inner ones close
pass	st2 TWO sibling handle expressions as operands of one sum — independent regions with different arms and seeds
pass	st2 draws BETWEEN the installs of a three-deep tower — each thread advances only while it is the innermost, sum pins the interleave
pass	st3 the SHADOWING frame's arm draws the SAME effect — its dispatch escapes its own extent and lands on the frame it shadows
pass	stacked line comments on a top-level form are all seen through
pass	stg1 a STONE-TAKING game — take removes up to k stones clamped at the pile answering pile-times-ten plus whose turn it was, the player landing the pile at zero answers a hundred-plus-their-id, and the smaller pile ends three moves early so its tail is drained-pile wins for alternating players
pass	stk2 a STICKY-ERR Result state — pushes accumulate while Ok, the over-20 sum flips the state to an absorbing Err whose pushes answer the negated code, reset reports-and-restores
pass	stl1 Set.to-list folds under the Ast.encode const-param demand path
pass	stl2 a (const Set.to-list) result is consumed by runtime List.len outside the block
pass	sto2 insertion-order INSENSITIVITY — {3,1,2} vs {2,3,1} to-list equality via tuple walk
pass	sto3 to-list length + sum are order-independent invariants
pass	sto4 Set.to-list of a 3-1-2 built set is the SORTED 1,2,3
pass	sto5 a STRING set to-list is lexicographically sorted
pass	sto6 a 20-element multi-node set to-list starts sorted 1,2,3
pass	sto7 negatives and gaps — {100,-5,7} to-list is sorted
pass	sto8 non-prefix string set to-list orders lexicographically ab,b,bb
pass	stp1 TIERED flat-fee billing — use accrues usage answering it, bill answers the current tier's flat fee (five under ten, twelve under twenty-five, twenty above) resetting usage, total reads the collected fees, and the seed pre-loads usage so the FIRST bill lands in a different tier while the later cycles converge
pass	straight-line add-then-subtract keeps the value but is not cancelled to the operand
pass	string REVERSE walks scalars back-to-front and anti-commutes with concatenation
pass	string concatenation
pass	string equality
pass	string indexing addresses Unicode scalar values, not bytes
pass	string indexing at or beyond the length yields None
pass	string indexing past a supplementary-plane scalar lands on the next scalar
pass	string indexing returns Some of the character
pass	string inequality
pass	string length
pass	string length counts scalar values after normalization
pass	string ordering walks a 400-byte MULTI-CHUNK common prefix to the deciding final byte
pass	string slicing yields Some of the substring
pass	string to bytes (UTF-8)
pass	string to bytes encodes multi-byte characters
pass	string to bytes produces the exact UTF-8 byte values, not just the right count
pass	string-literal arms discriminate a PROPER PREFIX and hit across seam positions
pass	strings differing only in Unicode normalization are equal
pass	structural equality holds component-wise
pass	structural equality of two Ast.Int wider than Int64 compares by full value
pass	structural equality on an Ast.List is element-COUNT sensitive
pass	structural equality on an Ast.List is element-ORDER sensitive
pass	sts1 a persistent Set.insert against a constant Set leaves the constant without the new element (two-sided contains fence)
pass	sts2 fifty frames of runtime-indexed membership over a 40-element constant Set hit every element and the miss cells
pass	sts3 fifty frames of runtime-keyed Set.insert against a 40-element constant Set never corrupt it (sibling stays without the key)
pass	stt1 a SET-OF-TUPLES state seeded by Set.of — structural dedup across dispatches: the repeated pair does not grow the set and the order-swapped pair only counts when the components differ
pass	stt1 a runtime-built binary Exp tree evals exactly and RECLAIMS the whole tree (branching-walk calibration)
pass	stt2 a tree-to-tree transform then eval preserves meaning and RECLAIMS BOTH trees (transform calibration)
pass	su1 a SUM-typed state machine — Idle transitions to Run on first dispatch, Run accumulates thereafter
pass	su2 a THREE-variant cyclic machine — the op arg selects the transition, both Mid exits exercised
pass	su3 a RECURSIVE sum state — the arm grows a Peano tower by two per dispatch, a recursive fn measures it
pass	su4 a sum variant carrying a HEAP list — Empty seeds on first put, Full grows the payload thereafter
pass	su5 the sum STATE escapes as the handle's value via a dump op — matched OUTSIDE the handler
todo	su6d BOTH handlers sum-state, same effect, TWO inner dispatches — declines (2nd arm copy's variant match hits the scalar-probe path)
pass	su6f a SCALAR-state outer shadowed by a SUM-state inner cycler — the inner machine transitions twice
pass	su6g a SUM-state outer shadowed by a SCALAR-state inner — mixed state kinds across the shadow boundary
pass	subset record comparison is explicit projection, not an overloaded equality
pass	substitution fires inside an implication (subst recurses into Imp)
pass	subtracting quantities of incompatible dimension is a compile-time error
pass	subtraction
pass	subtraction below the minimum integer overflows and traps
pass	subtraction of an integer and a float does not silently promote
pass	subtraction of two strings is rejected
pass	subtraction of two symbols is rejected
pass	sum type constructors are in scope after declaration
pass	sw1 a three-slot state SWAPS its pair while a counter ticks — the encoding exposes position, order, and dispatch count at once
pass	sw2 a SLIDING WINDOW of the last two payloads — the arm keeps a bounded list state and resumes the window sum
pass	sw3 the window CAPACITY is itself state — a grow op resizes the window mid-stream, windowed MAX tracked across the resize
pass	sw4 windowed AVERAGE with the truncating divide — window sum over live length, a negative window exercises toward-zero
pass	sw5 DISTINCT-count over the window — a Set built per dispatch from the last-3 list measures dedupe, n=7 collides with the constant feed
pass	swapped-operand complements do not fold — less-than or flipped greater-equal is not a tautology
pass	swd1 a SLIDING-WINDOW dedup state — the last-three list slides via push-and-drop-head rebuilt in the arm, membership answers flip as elements age out
pass	swm1 a SLIDING-WINDOW maximum of width 3 — each push appends and evicts the oldest element once the window is full, answering the max scanned from the LIVE window, and the seeds disagree on which pushes set new maxima
pass	swp1 an ATOMIC slot swap — the swap dispatch exchanges both tuple fields in one transition, reads before and after pin the exchange
pass	swt1 VALUE-YIELDING map ops in the arm — Map.swap answers the PRIOR value it replaced and Map.take answers the value it removed, both tuple-projected in the arm with absent-key sentinels
pass	sy1 SYMBOL unit values in collections — set dedup + equality, through the cadenza hop
pass	sy1 a RUNTIME-built symbol (Symbol.of of a computed string) is intern-consistent with the compile-time literal: eq, Map-key hit, Set contains (+ the miss control)
pass	sy2 a SYMBOL toggle state — equality routes the resume value while the arm flips fast/slow per dispatch
pass	sy3 the SYMBOL state is a MAP KEY per dispatch — the a→b→c walk ends at a missing key
pass	sy4 sequential draws written into record FIELDS via Record.with symbol selectors — chained functional updates
pass	sy5 SYMBOL op args as COMMANDS — inc/dbl/nop route both the answer and the state transition
pass	sy7 a SYMBOL mode BESIDE an accumulator in the tuple state — symbol equality routes the go arm between idle and run, stop resets the mode
pass	syd1 an op returns a SYMBOL picked by state parity — symbol equality gates a branch that draws again
pass	symbol identity follows String normalization
pass	symbol ordering is reflexive at the boundary — less-than-or-equal on equal content
pass	symbol ordering — greater-than agrees with the byte order
pass	symbol ordering — greater-than-or-equal is false when strictly less
pass	symbols interned from GENERATED strings dedup by content in a symbol set
pass	symbols interned from different strings are unequal
pass	symbols order by content-lexicographic UTF-8 byte order — less-than
pass	t1(div0) NEGATIVE: an UNBOUNDED divisor is NOT provably non-zero — the divide-by-zero trap STAYS
pass	t1(div0): the divide-by-zero obligation NEQ b 0 is DISCHARGED for b > 0 — so a/b cannot trap
pass	t1(match) NEGATIVE: a NON-total arm set (one Bool arm missing) does NOT discharge COVERS — the match can still trap
pass	t1(match): the exhaustiveness obligation COVERS is DISCHARGED for a TOTAL arm set — the match cannot trap
pass	t1(oob) NEGATIVE: with only the LOWER bound (>= i 0), the out-of-bounds obligation is NOT complete — the trap STAYS
pass	t1(oob): the out-of-bounds obligation (0<=i) AND (i<len) is DISCHARGED from the two bound preconditions
pass	t1(trap) NEGATIVE: a trap guard NOT contradicted by the precondition is NOT unreachable — the trap STAYS
pass	t1(trap): an explicit trap under guard (lt x 0) is UNREACHABLE when @requires(>= x 0) — the trap is dead
pass	t2t1 a TUPLE-to-TUPLE transformer op chained twice — the arm swaps components and salts with the state, both crossings exact
pass	ta2 an abort INSIDE the inner frame of a same-effect tower — Bail innermost, both E threads keep their positions
pass	ta3a a THREE-argument op — the arm folds all three positions with the live state, two calls see it advance
pass	take-while and drop-while split a leading run and reassemble to the original
pass	take-while composed after a map threads both transformers' closures
pass	take-while excludes the failing element and everything after it (content, not just count)
pass	take-while where every element satisfies the predicate keeps the whole run
pass	take-while where the first element fails the predicate returns the empty run
pass	tax1 PROGRESSIVE tax brackets with a seed-shaped edge — assess splits the income into three bands via chained match binders over clamped compounds (ten twenty and thirty percent truncating), accumulating into the audit total, and the WIDER bracket taxes every income LESS while the sub-bracket income taxes to zero on both
pass	tb1 a def RETURNS a tuple of two draws — the caller destructures the multi-value result of a performing helper
todo	tdd1 a runtime-operand `?` under the do-def binding idiom computes like its let twin (should-work: both boundary-tail idioms are the documented form)
todo	trr1 a runtime-Result `?` in a fallible-boundary fn unwraps Ok and PROPAGATES Err (should-work; today declines the runtime operand — try-operator lane)
pass	tdr1 a RESERVOIR gate with a ratcheting threshold — inflow raises the level, gate releases HALF truncating only above the threshold (which ratchets up by one after every release), and the LOWER starting threshold releases on the very first gate while the higher one holds, the runs re-converging on the final held gate
pass	tel1 a TELESCOPE tracker with a slew-rate limit — track names a target locking within five degrees (else a nine-hundred miss with the gap's low digits), slew moves the azimuth toward the target by at most SEVEN degrees (a two-sided inline clamp of the signed difference) tagging an exact arrival, the read packs azimuth lock and arrival, and one seed starts within locking range so every row down to the read disagrees with the far-parked run
pass	textually-identical performs are DISTINCT state-advancing reads, not a common subexpression
pass	the #rational(n d) alias reads to the same rational literal and compiles
pass	the AND connective shields the guarded branch's trap (c=false does not evaluate it)
pass	the ARM ENCODES its scalar op argument into framed Bytes and resumes them — body decodes
pass	the ARM decodes a Bytes op argument with a bin pattern and resumes a parsed field
pass	the AST is a sum type deconstructible by pattern matching
pass	the BARE (unsuffixed) over-Int64 counterpart still range-checks as CDZ0302 (a grounding, not a mismatch)
pass	the Bool generator covers both values across seeds (it is not a constant)
pass	the CHECKED ground axiom le-ax cannot forge a FALSE order fact (5 <= 3) — the axiom base is consistent
pass	the COLLATZ walk counts steps to 1 and tracks the trajectory peak
pass	the DES Duration constructors ms / us / ns each scale to the right nanosecond magnitude
pass	the EMPTY string keys a map — hit by an empty-concat rope, distinct from a 1-char sibling
pass	the ETA axiom is minted via new_axiom as a hypothesis-free theorem
pass	the FBIP accumulator (List.push as recursion tail) is unaffected by the owned-operand reclaim
pass	the Float codec round-trips a negative value
pass	the INFINITY axiom is minted via new_axiom (an infinite type exists)
pass	the Int codec round-trips i64::MIN — the two's-complement boundary
pass	the Int64 control: a nested wide add with if operands needs no normalization
pass	the Int64 total order holds at its extremes
pass	the Int64.max and Int64.min CONSTANTS compare equal to their boundary values at runtime
pass	the Int8 max boundary literal fits in a runtime branch
pass	the JOSEPHUS survivor agrees between the modular recurrence and a list elimination simulation
pass	the LAST Map.to-list entry over runtime String keys is the byte-largest
pass	the LEB128 byte composition runs on a runtime operand
pass	the LET-BOUND twin of finding #20 — a state byte LET-bound as a Bytes then decoded by a from-bytes match in a tail-resumptive arm folds (flatten+pin fix landed; regression-guard)
pass	the LUHN checksum doubles alternate digits from the right with the nine-fold correction
pass	the NULLARY scalar-newtype escape renders the nominal (: 5 W), agreeing with the param'd path
pass	the OLD 2-operand `Record.extend (name value)` pair form is rejected (migrated to 3 operands)
pass	the OLD 2-operand `Record.with (name value)` pair form is rejected (migrated to 3 operands)
pass	the OR (negated-condition) connective short-circuits the guarded branch's trap at c=false
pass	the OR short-circuits on a true first operand — the second perform must NOT fire
pass	the PASSING FLOOR of adv-20's bisection — a state byte LET-bound as a Bytes and consumed by Bytes.len (NO decode-match) in a tail-resumptive arm folds correctly
pass	the Qty unit-position misuse fires at a value-annotation site too
pass	the RUNTIME division identity a=(a/b)*b+(a%b) holds across all four sign quadrants
pass	the SAME op name on two DIFFERENT effects — each qualified perform routes to its own handler
pass	the SAME runtime set as both operands of union intersection and difference computes each law live
pass	the SELECT (choice) axiom is minted via new_axiom as a hypothesis-free theorem
pass	the STATE is the divisor — a descending thread crosses zero and the exact dispatch that hits it traps
pass	the STATE-outer nesting of the dual-sum arm threads fresh payloads across three dispatches — the peel's state-match threading face
pass	the Sign sum orders Neg below Pos per its declaration
pass	the WIDTH-indexed Int constructor with two width arguments names the width arity
pass	the WIDTH-indexed Int constructor with zero width arguments names the width arity
pass	the WIDTH-indexed UInt constructor with zero width arguments names the width arity
pass	the `?` sigil in head position names the `try` spelling, not a bare unbound name
pass	the absorption law does not discard a trapping runtime operand
pass	the absorption law holds on runtime-element sets
pass	the accumulator transform applies with the self-call in either operand
pass	the accumulator transform declines a non-associative combine with the self-call first
pass	the accumulator transform declines when the base is not the operator's identity
pass	the accumulator transform preserves a product (factorial) shape
pass	the additive-zero identity preserves a boundary-crossing runtime operand
pass	the agent-harness loop spine runs model-ask then value-dispatches a tool over turns
pass	the agent-return shape: three peers with a Cedar-gated escaping string result
pass	the and SHORT-CIRCUITS: the second operand's perform must NOT fire (state proves it)
pass	the and-complement folds to zero on an unsigned type but the or-complement is the width all-ones not -1
pass	the arithmetic operator rejects a float-first mixed operand pair
pass	the arm DECODES a Bytes op argument to a String — multibyte UTF-8 survives the crossing
pass	the arm ENUMERATES its Map state to a list of tuples and resumes the enumeration
pass	the arm slices a crossed multibyte String at a scalar boundary — the slice window respects UTF-8
pass	the arm's nested handler is instantiated FRESH per dispatch — independent inner state
pass	the arm-shape rule is FRAME-RELATIVE: a nested single-site handler dispatching mid-sequence is invisible
pass	the arrow type constructor with zero arguments names the arrow shape and its minimum
pass	the b2 match predicate LICENSES the elision: the discharged no-overflow proof matches the obligation and its hyps are covered by the node precondition
pass	the b2 match predicate REJECTS a proof discharged under a FOREIGN hypothesis not in the node precondition (soundness — no elision under wrong assumptions)
pass	the bare-symbol Qty unit misuse fires at a value-annotation site
pass	the bare-symbol Qty unit misuse fires nested inside a List type
pass	the bitwise absorption law folds x-and-(x-or-y) and x-or-(x-and-y) to x on runtime operands
pass	the bitwise self-identities hold on a runtime narrow UInt8 (x & x = x, x | x = x, x ^ x = 0)
pass	the bitwise-AND-with-zero annihilator does not discard a trapping runtime operand
pass	the bitwise-AND-with-zero annihilator does not swallow a COMPILE-PROVABLE overflow in the discarded operand
pass	the bitwise-AND-with-zero annihilator folds to zero when the discarded constant operand is total
pass	the boolean absorption laws absorb with swapped operand order too
pass	the boolean absorption laws reduce a-and-(a-or-b) and a-or-(a-and-b) to a
pass	the boolean complement-law fold keeps a trapping operand's trap
pass	the boolean less-than operator agrees with compare over Bool
pass	the boolean less-than operator agrees with the three-way comparison
pass	the boolean literals
pass	the boolean ordering operator places (Some 3) below None like the three-way compare
pass	the boolean-literal coercion composes over a runtime Symbol equality
pass	the built variant is the multi-arg one, the unbuilt arm still gets its call type
pass	the built-in Ast is transformed as an ordinary value
pass	the built-in list is folded by an element-with-rest pattern
pass	the byte codec round-trips a digit-led Ast.Name that the text path would reclassify
pass	the byte codec round-trips a keyword-spelled Ast.Name that the text path would reclassify
pass	the byte codec round-trips a string-spelled Ast.Name that the text path would reclassify
pass	the byte codec round-trips an Ast.Int wider than Int64
pass	the byte length of a run-time multibyte string exceeds its scalar length
pass	the byte length of a run-time-selected string reads the actual handle
pass	the byte length of a runtime string equals the length of its encoded bytes
pass	the byte-len of a multi-byte String.slice exceeds its scalar-len (the two measures diverge)
pass	the canonical NaN crosses the boundary as a not-a-number float
pass	the churned-to-empty map REGROWS with correct structure (no residue corrupts re-insertion)
pass	the closure state is REPLACED per dispatch by one capturing the arm's OWN binder
pass	the closure's inner arm performs an OUTER effect through the closure boundary
pass	the commutative equality swap-fold declines a trapping operand and keeps both compares
pass	the compare walk recurses list-of-sums-of-lists with discriminant-first at depth
pass	the compile-time contract-id fold is byte-identical to the host Contract (golden #3238)
pass	the complement laws fold x-and-not-x to zero and x-or-not-x to all-ones on a signed runtime value
pass	the composed contract-id is deterministic — the same declaration folds to the same id
pass	the composed multibyte slice view equals its literal twin and keys a Map
pass	the composed slice view EQUALS its flat twin and keys a Map by canonical content
pass	the compound comparator drives a full INSERTION SORT over runtime tuples
pass	the conditional-shot-count arm's SINGLE-shot branch (same program, other input)
pass	the constructor-built width module carries the same wrap as the named width
pass	the constructor-built width module projects the same max bound as the named width
pass	the constructor-built width module projects the same min bound as the named width
pass	the cross-handler op-arg lift fires 100 times inside a recursive accumulator loop
pass	the default-fraction pragma with its type argument omitted is malformed (CDZ0602)
pass	the default-integer pragma is definition-site scoped, not use-site
pass	the difference contains the elements not in the second set
pass	the difference of a set with itself is empty
pass	the difference of a set with the empty set is the set (identity)
pass	the difference of sets of TUPLES removes elements by their whole-tuple content
pass	the direct value-annotation site also carries the add fix for a missing record field
pass	the direct value-annotation site also carries the tuple add fix for too few elements
pass	the display truncation does not lose the stored magnitude (Qty.value reads it back)
pass	the dropped-variant exhaustiveness hazard is rejected at the declaration
pass	the dual-absorption fold keeps a trapping absorbed operand's short-circuit form
pass	the element list of an Ast.List escapes as a (List Ast) value and renders its structure
pass	the element tie survives producer self-composition
pass	the elided-unit one-binder handler arm for a unit op compiles and runs
pass	the elided-unit zero-binder handler arm for a unit op compiles and runs
pass	the else-branch of an if threads a performed state to the continuation
pass	the empty and None arms of a list-payload split dispatch to their own values
pass	the empty byte sequence has length zero
pass	the empty reader literal is the empty symbol
pass	the empty set has cardinality zero
pass	the empty set is equal to the empty set
pass	the empty set pattern matches every set
pass	the empty string has length zero
pass	the empty string is a matchable literal
pass	the empty symbol converts to the empty string
pass	the empty symbol equals itself
pass	the en1 helper called ONCE folds (the single-site control)
pass	the entrypoint adds one to its runtime argument
pass	the entrypoint branches on its runtime argument
pass	the entrypoint compares its runtime argument to a bound
pass	the entrypoint multiplies its runtime argument
pass	the entrypoint returns its runtime argument unchanged
pass	the entrypoint returns its runtime boolean argument
pass	the entrypoint sums its two runtime arguments
pass	the equality-and-range contradiction fold keeps a trapping operand's trap
pass	the event queue pops the earliest-Instant entry first (pop-min)
pass	the exact Float32 finite max fits in a runtime branch
pass	the excluded C=-1 multiplier keeps the div_s guard and MIN*-1 still traps
pass	the expander rewrites a directly-written node even when chunks != holes+1 (no re-check)
pass	the expander threads the hole list to the tag even with zero chunks and zero holes
pass	the exponent marker is case-insensitive
pass	the false branch: the condition's perform fires, the branch's does NOT (same program)
pass	the false-literal coercion negates a float ordering compare, turning an unordered pair true
pass	the filtered rebuild EQUALS the direct selection build (selection is canonical)
pass	the finite cross-width comparison rejects the same way (the behavior the nan path matches)
pass	the first failing `?` short-circuits; a later `?` never runs
pass	the flat-value baseline for the borrowed-rope-eq leak-neutrality (same map/value-box residual)
pass	the float ordering-versus-equality split on the zero pair
pass	the four rational-to-Int64 projections of one runtime rational store in a list and read back distinct
pass	the full contract-descriptor record (Bytes/String/Ast fields) materializes whole at runtime; scalar fields read
pass	the function-signature delta hint also fires at a value-annotation site
pass	the gcd face: a reducible num/den pair from performs canonicalizes (4/8 → 1/2)
pass	the generated Param accessor carries the @param's declared type into arithmetic
pass	the generated-set convergence property has discriminating power — two different generated sets are NOT equal
pass	the generator is a deterministic function of its seed (same seed, same value) — folds
pass	the generator reproduces its stream from a runtime seed (same seed, same value) — runs
pass	the greater-than operator on chars follows scalar order
pass	the guard-MISS path re-performs in the fallback arm — dispatch continues past a failed guard
pass	the guard-TRUE path of the perform-scrutinee match (no fallback entry)
pass	the guarded-let conditional takes its else-branch when the conjunction is false
pass	the handler STATE is a CLOSURE the arm replaces with one capturing the perform-time op argument
pass	the heap list a handle BUILDS escapes the handle and is consumed outside it
pass	the hygiene rename recurses into a NESTED compound match pattern
pass	the idempotence fold keeps a repeated trapping operand's trap
pass	the idempotent bitwise collapse holds on a repeated RUNTIME operand
pass	the identity function applied to a boolean returns the boolean
pass	the identity function applied to an integer returns the integer
pass	the identity-less reducer-echo round-trips the real message shape into a step (via an imposed WIT world)
pass	the implication-extended kernel Thm stays unforgeable — Thm.Seq of a bogus implication outside is CDZ0214
pass	the inclusive Int8 boundaries fit in a runtime branch and one-past rejects
pass	the inclusive-bounds collapse keeps a trapping operand's trap
pass	the inner sum-state match routed through a HELPER DEF taking the payload as a parameter — a def boundary in the peeled arm body
pass	the int-to-float generator is order-preserving (a < b implies of a <= of b — NON-decreasing)
pass	the integer-only remainder operator rejects a float operand (CDZ0301, no float fold)
pass	the interleaved middle op SERVES when made two-site itself (the shape-not-position witness)
pass	the intersection contains the elements in both sets
pass	the intersection of disjoint sets is empty
pass	the intersection of sets of TUPLES matches elements by their whole-tuple content
pass	the keeping + bitwise self-identities hold on a runtime SIGNED narrow Int8 at a negative value
pass	the kernel defines T (truth) as a constant and DERIVES |- T through the rules
pass	the kernel proves reflexivity end-to-end: refl t yields a theorem whose conclusion is (t = t)
pass	the kernel proves the identity theorem ⊢ (λx.x) y = y via BETA — the first-theorem milestone
pass	the kernel proves the tautology ⊢ p ⇒ p via DISCH (implication introduction) over ASSUME
pass	the kernel's ABS rule lifts an equation under a binder: from |- t=u derive |- (λx.t)=(λx.u)
pass	the kernel's ASSUME rule yields the sequent {p} |- p
pass	the kernel's BETA rule reduces an application of a lambda: (λv.body) arg = body[arg/v]
pass	the kernel's DEDUCT_ANTISYM rule: from A |- p and B |- q derive (A-q)++(B-p) |- p=q
pass	the kernel's EQ_MP rule: from |- p=q and G |- p derive G |- q (hypotheses unioned)
pass	the kernel's GEN (∀-introduction) enforces its free-variable side-condition
pass	the kernel's MK_COMB rule: equals applied to equals are equal — from f=g and x=y derive (f x)=(g y)
pass	the kernel's SPEC (∀-elimination) instantiates the quantified body with the witness
pass	the kernel's conjunction: CONJ unions hypotheses, CONJUNCT1/2 project (hyps survive)
pass	the kernel's disjunction introduction (DISJ1/DISJ2) preserves hypotheses
pass	the kernel's existential introduction (EXISTS) checks the witness and preserves hypotheses
pass	the kernel's modus ponens (⇒-elimination): from ⊢ p⇒q and ⊢ p derive ⊢ q
pass	the largest factorial that fits the integer type computes exactly
pass	the le modifier encodes a u16 little-endian
pass	the leaf-path drill stops at a deeper field-set difference, naming the immediate field
pass	the left payload of the taken arm traps before the right payload
pass	the length of a byte sequence is its byte count
pass	the length of a concatenation is the sum of the two lengths
pass	the length of a fixed-width construction is the sum of its segment widths
pass	the length of a grown list is its element count
pass	the length of a list looked up from a map by a runtime key
pass	the length of a prepended-onto list grows by one
pass	the length of a runtime byte sequence is its byte count
pass	the length of a slice is the slice's byte count
pass	the let-bound-after-helper observation is state-type-general (scalar counter)
pass	the let-form of the dual-resume-slot arm computes (the always-worked oracle twin)
pass	the let-twin of the do-def-into-perform-arg body computes the same value
pass	the list-recursion shape of the two-self-call BigInt fold also types and folds
pass	the logical-shift-drops-all-bits fold does not discard a trapping runtime operand
pass	the magnitude of a bare-scaled quantity reads back
pass	the manifest is DERIVED from what entrypoints delegate, not from effect declarations
pass	the mark op's dup-flag RESULT counts repeats while the set state dedupes
pass	the match-it hint also fires at a binop unify site over an optional read
pass	the maximum Int64 in hexadecimal reads as an integer
pass	the maximum Int64 literal reads as an integer
pass	the maximum signed 64-bit value is a folding constant
pass	the maximum signed 8-bit value is its per-width bound
pass	the maximum unsigned 8-bit value is its per-width bound
pass	the maximum valid scalar U+10FFFF round-trips through to-int
pass	the member-access spelling of an over-applied constructor names it dotted
pass	the minimum Int64 literal reads as an integer
pass	the minimum integer scrutinee misses a zero-based jump table
pass	the minimum signed 64-bit value is a folding constant
pass	the minimum signed 8-bit value is its per-width bound
pass	the mirror order (non-literal float then int literal) offers the identical literal retype
pass	the mirror per-arm mix — a crossing #sym sibling over a String scrutinee — is rejected
pass	the mirrored subtract-then-add chain computes at both boundaries without a spurious trap
pass	the mismatched-pair rejection is order-independent
pass	the model-oracle property has DISCRIMINATING power — a BROKEN model (counts every insert) diverges from Map.len
pass	the module entrypoint is the def named main regardless of its position
pass	the multiplicative-one and subtractive-zero identities preserve a boundary-crossing runtime operand
pass	the multiply-by-zero annihilator does not discard a trapping runtime operand
pass	the multiply-by-zero annihilator does not swallow a COMPILE-PROVABLE overflow in the discarded operand
pass	the multiply-by-zero annihilator folds to zero when the discarded constant operand is total
pass	the narrow Int8 nested (* (* x 2) 4) traps on BOTH inner and outer overflow
pass	the natural invariant construction over a VIOLATING perform result traps through the handler
pass	the negative power agrees with dividing into the dimensionless one
pass	the new map from a runtime-key swap holds the new value at that key
pass	the new map from a runtime-key take has the key removed
pass	the newtype nominal-boundary comparison error fires regardless of operand order
pass	the newtype-vs-inner comparison on a newtype parameter carries the heuristic unwrap fix
pass	the non-exhaustive Bool match arm-insert is symmetric on the missing literal
pass	the non-fused Some-shell control reads a heap binder twice with the same reclaim (no fusion)
pass	the nullary-first if returns the None arm on the base path
pass	the number of elements counts distinct elements
pass	the operator's uniform all-Bytes/String contract descriptor materializes whole at runtime
pass	the opposite-direction disjoint-and fold keeps a trapping operand's trap
pass	the other parity of a mutually-recursive pair
pass	the outer discriminant is decisive over any inner content for nested sums
pass	the per-key linear-scan model oracle has DISCRIMINATING power — a value-forgetting model diverges at every present key
pass	the permutation-invariance property has discriminating power — an order-dependent fold fails it
pass	the pipeline operator splices the value as a call's first argument
pass	the pipeline operator threads a value into a named function
pass	the power form cubes an integer-magnitude quantity exactly
pass	the power form derives the same dimension as repeated multiplication
pass	the pre-update record survives a CONDITIONAL Record.with in a branch join
pass	the prelude binds Constructor values not pre-applied Sums
pass	the prelude module keeps working beside a same-named colliding variant
pass	the primitives compose to a compile-time tagged contract-id of the expected length
pass	the program manifest is the union of its entrypoints' delegations
pass	the quoted #map-rest pattern's rest binder captures the RESIDUAL entries
pass	the read primitive skips a line comment inside program text
pass	the reader literal reads to Symbol.of
pass	the ready-queue is a plain FIFO — spawned-ready tasks run in enqueue order
pass	the record-perform workaround — `let`-bind then read by `(. r field)` projection — folds once each (finding #8 control)
pass	the recursion DEPTH itself is a draw — the walk runs mod-4-of-state iterations, including the zero-depth face
pass	the remainder takes the sign of the dividend for a negative dividend
pass	the remainder takes the sign of the dividend for a negative divisor
pass	the removed `contract` module directive is now an unknown-directive reject
pass	the removed `input` module directive is now an unknown-directive reject
pass	the removed `output` module directive is now an unknown-directive reject
pass	the rendered capitalized record type round-trips as a valid annotation and runs
pass	the repeated-conjunct absorb keeps a trapping nested operand's trap
pass	the rest binder carries the unmatched runtime-keyed remainder
pass	the retired Tuple.cat name is rejected with the Tuple.concat rename fix-it (CDZ0603)
pass	the retired Tuple.pop name is rejected with the Tuple.remove rename fix-it (CDZ0603)
pass	the reversed extreme ordering is false
pass	the round trip tracks the produced closure's captured value
pass	the runtime UTF-8 encoding of a string equals its exact byte literal
pass	the runtime control for the Float32-to-Float64 promote reads the f32 value at its own width
pass	the runtime narrow-width add in range does not trap (the control)
pass	the runtime-if list takes its else branch and measures that length
pass	the runtime-list version of a tail fold compiles and folds correctly
pass	the same conditional delegation makes no host call when the branch is not taken
pass	the same declared effect is handled in-program by one entrypoint and delegated by another
pass	the same exported addition over a second runtime arg pair recomputes
pass	the same function called under two handlers is discharged by each in turn
pass	the same list is still live after the if when the consuming branch is not taken
pass	the same list not escaped (the None branch) is still usable after the join
pass	the same literal-payload refinement on a MULTI-variant sum works (the boxed control)
pass	the same literals at Float64 are NOT demoted and const-fold not-equal (adv-61 width control)
pass	the same mixed-scale comparison is EXACT over Rational (keeps the fractional scale)
pass	the same nullary variant constrained to different concrete types reflects different types
pass	the same program with a different response gives a different deterministic result
pass	the same two-self-call recursive fold at Int64 still types as Int64 (the defer re-grounds to the operands' type)
pass	the same without-extend chain on a LIST-borne record computes
pass	the same-core-shape sibling dispatches ITS body, not the first
pass	the same-direction subsumption fold keeps a trapping operand's trap
pass	the same-name disambiguation also fires at a value annotation
pass	the same-unit wrap resolves the dimension fault — the wrapped quantity sum runs
pass	the scalar length of a GENUINELY-runtime multi-byte string walks the UTF-8 leaf
pass	the scalar length of a runtime multi-byte string counts scalars, not bytes
pass	the scalar length of a runtime string spanning every UTF-8 width
pass	the scalar-len of an in-bounds String.slice equals its span (end - start), independent of byte width
pass	the scheduler terminates on zero primaries and detects deadlock when live primaries have no work
pass	the second distinct-signature closure export returns its own type
pass	the second element of a runtime-content fn-return tuple is projected directly
pass	the selected operand decides a hoisted comparison
pass	the selected operand decides a hoisted float equality
pass	the sequent-shaped kernel Thm is unforgeable — building Thm.Seq outside the kernel is CDZ0214
pass	the set De Morgan law relates difference-over-union to intersection-of-differences on runtime sets
pass	the set rest binder holds exactly the scrutinee elements minus the named ones
pass	the shadow stack UNWINDS — each level's post-region perform reaches ITS enclosing frame
pass	the shift-by-zero identities preserve a runtime narrow UInt8 (x << 0 = x, x >> 0 = x)
pass	the shrinking search terminates via its fuel bound rather than searching unboundedly
pass	the sibling-inferred list-element width check is order-independent (the bare element first)
pass	the sign of ZERO propagates through multiply and add per IEEE at runtime
pass	the size of a heterogeneous tuple is its element count
pass	the size of the empty tuple is zero
pass	the slice overflow guard holds on a chained slice-of-a-slice
pass	the span between two equal Instants is zero — the inclusive lower boundary of `since`
pass	the split-comparison reassociation declines a trapping leaf and keeps its trap
pass	the stacked contract's PRE fires through the @ensures layer BEFORE the perform (abortive observer)
pass	the state-SWAP idiom — the op argument becomes the state and the OLD state returns
pass	the string primitive tuple constructor is not shadowed by a same-named binding
pass	the structured descriptor's Bytes id field projects byte-exact and eliminates the record
pass	the structured descriptor's String name field projects and eliminates the record
pass	the suggested unwrap type-checks and runs — the fix is total for a single-variant newtype
pass	the swap and take value-yielding forms agree with insert and remove on the resulting map
pass	the taken arm's checked add still overflows under the operator hoist
pass	the tautology fold keeps a trapping shared operand
pass	the third `?` List.at read SHORT-CIRCUITS over a two-element list
pass	the three-way comparison is deconstructed by an exhaustive match
pass	the three-way comparison orders Bool with false below true
pass	the three-way comparison orders Float64 by numeric value — Equal
pass	the three-way comparison orders Float64 by numeric value — Greater
pass	the three-way comparison orders Float64 by numeric value — Less
pass	the three-way comparison orders chars by scalar value — Equal
pass	the three-way comparison orders chars by scalar value — Greater
pass	the three-way comparison orders chars by scalar value — Less
pass	the three-way comparison orders strings lexicographically
pass	the three-way comparison over a genuinely-runtime String computes content-lexicographically
pass	the three-way comparison over a runtime greater scalar yields Greater
pass	the three-way comparison over a runtime lesser scalar yields Less
pass	the three-way comparison over a runtime scalar performs the operand exactly once
pass	the three-way comparison over an equal runtime String yields Equal
pass	the three-way comparison over equal runtime scalars yields Equal
pass	the three-way comparison over runtime booleans orders false before true
pass	the transitive-arith width flow climbs TWO arith levels down to the literal
pass	the true-literal coercion of a float ordering compare returns the compare, NaN stays false
pass	the two algebraic subset formulations agree on subset, superset, and equal operands
pass	the two variants of Result compare unequal
pass	the type-valued-param checking boundary fires even when the param is unreferenced in the body
pass	the underlying numeric type obeys the numeric core — no silent promotion under a unit
pass	the union contains the elements of either set
pass	the union of sets of TUPLES holds a shared tuple once and preserves component order
pass	the unit recovered from a runtime-magnitude quantity is dimensionally checked
pass	the unit value
pass	the unit value observes its total order — equal to itself, never less
pass	the unit-op response-cursor discriminator: a nonzero ping row is not misread by the later get (adv-65)
pass	the untaken branch's runtime div-by-zero does not fire
pass	the unusual-width overflow check is width-parametric: a SECOND width (Int 12) also enforces its own range
pass	the updated-back list KEYS a Map like the pristine build (RRB update leaves no key trace)
pass	the value-yielding insert of a new key adds the entry
pass	the value-yielding insert reports None when the key is new
pass	the value-yielding insert reports the prior value at a runtime key
pass	the value-yielding insert reports the value it replaced
pass	the value-yielding remove of an absent key leaves the map unchanged
pass	the value-yielding remove reports None when the key is absent
pass	the value-yielding remove reports the dropped value at a runtime key
pass	the value-yielding remove reports the value it dropped
pass	the well-typed branch of the mutual check returns the Ok result
pass	the widening carries a full-width magnitude through BigInt
pass	the widest unsigned range bound renders exactly (u128 arithmetic, not i64)
pass	the width constructor applied to 64 projects the signed 64-bit maximum
pass	the width-indexed and aliased annotations of one value do not conflict
pass	the width-indexed and aliased float annotations of one value do not conflict
pass	the winning abort's value reads the op arg and the seed state
pass	the without-extend chain on a NESTED-map-borne record computes (boundary — double lookup materializes fresh)
pass	the work-around — threading the captured value as an explicit parameter — compiles and runs
pass	the wrap-in-variant fix's constructor is derived generically from the user sum, not hardcoded
pass	the wrapping additive and multiplicative identities preserve a boundary operand exactly
pass	the wrapping identities preserve a runtime narrow UInt8 exactly (add 0, mul 1)
pass	the wrapping-mul zero annihilator does not drop a trapping operand
pass	the x times minus-one strength reduction negates a normal operand and still traps at Int64.min
pass	the zero-leading list rest is IRREFUTABLE — it is accepted in a binding position and binds the whole list
pass	the zero-recursion abortive-callee shape with a pending continuation folds too (static shape)
pass	the λ-extended kernel Thm stays unforgeable — Thm.Seq with an Abs term outside is CDZ0214
pass	the ∀-extended kernel Thm stays unforgeable — Thm.Seq of a bogus universal outside is CDZ0214
pass	three DISCARDED performs on a do-spine still advance the state
pass	three closures with a SHARED signature cross as two resource types
pass	three dispatches of an outer-arg-match inner-state-match arm each read their own payload not dispatch one's
pass	three distinct closure signatures cross as three resource types
pass	three events at the same Instant drain in FIFO insertion order (tie-break at N=3)
pass	three keys sharing one 32-bit hash build a 3-entry CHAMP collision node (collision_insert append)
pass	three leaf variants in one quoted form each dispatch their own tag
pass	three list-append dispatches then a two-param range over a computed-index List.at emits valid wasm (ListAt index floated above the high-water)
pass	three same-variant literal arms fall through in order
pass	three sums in a tuple bind three distinct payloads
pass	three-way compare orders (Some 3) below None per the declared discriminant order
pass	ti1 A-B-A-B interleaved draws in ONE expression — each effect threads its own state independently
pass	ti1h the HEAP-state twin of ti1 — two DIFFERENT effects, BOTH threading a TUPLE (heap) state, interleaved A-B-A draws with values crossing
pass	ti2b the inner arm's resume-VALUE performs the outer effect on EVERY dispatch — three dispatches, both states advancing
pass	ti3 cross-effect resume-value feed PLUS direct body interleave — B's arm and the body both advance A
pass	ti4 THREE-effect chained cross-feed — C's arm performs B, B's arm performs A, two C dispatches walk the whole chain twice
pass	ti5 a FOREIGN outer perform inside the inner region — A advances inside B's body, the post-region draw sees it
pass	tid1 a TIDAL predictor on a triangle wave — read advances one hour answering the level (rising to the seed amplitude then falling), moor answers one when the current level covers the draft or the negated shortfall, and the smaller amplitude peaks EARLIER so the same moor probes catch one tide still rising and the other already ebbing
pass	tie1 a TWO-POINTER merge of IMPLICIT arithmetic streams — the heads bind once through match binders, take answers the smaller advancing that stream's index, a TIE answers fifty-plus-the-value advancing the a-side, and the seed offsets stream a so one run opens with a tie and the other buries its tie mid-stream
pass	tk-ann1 an ASCRIBED empty-Map seed (: Map.empty (Map (Tuple Int64 Int64) Int64)) used across three arms — the annotation grounds the element type every arm sees
pass	tk1 a Map state with TUPLE keys built from consecutive draws — record twice, query a hit and a miss
pass	tk3 an UNASCRIBED empty-Map seed with tuple keys solved in one arm, read in siblings — cross-arm element propagation (3 arms)
pass	tkp1 a TAKEN conditionally-demoted const divide-by-zero names its kind (not unreachable)
pass	tkp2 a TAKEN conditionally-demoted const Int64.min/-1 names the overflow kind
pass	tkp3 a TAKEN const rem-by-zero in a MATCH arm names the divide kind
pass	tkp4 a const-demoted expect-on-None and its runtime twin trap IDENTICALLY (divergence fence)
pass	tl1 THREE live handlers, each drawn in one expression — every draw dispatches past the two inner frames to its own
pass	tl2 a THREE-frame value pipeline — innermost pick feeds middle dbl feeds outermost send, then an outer draw stamps the hundreds
pass	tl3 the MIDDLE frame's arm resumes with an OUTERMOST draw — rv-face at depth, dispatched from under a third live frame
pass	tl4 SAME effect handled at two depths — the inner handle shadows for its extent, the outer thread resumes after it closes
pass	tle1 an exact-fit 32-element constant list hoists build-once — branch-selected use, length reads, and runtime equality across the pair
pass	tle2 a 33-element constant list (one past the build-once gate) builds per-eval and reclaims to zero
pass	tle3 fifty frames alternating exact-fit-32 and 33-element constant lists reclaim to zero (no half-marked node pins structure)
pass	tle4 fifty frames of persistent update against an exact-fit 32-element hoisted constant never corrupt it (both nodes marked — the header+arr trap)
pass	tle5 fifty frames of persistent update against a hoisted 33-element (multi-level trie) constant never corrupt it
pass	tlf1 Set.to-list of TUPLE elements orders identically const and runtime
pass	tlf2 Set.to-list of SUM elements orders identically const and runtime (discriminant then payload)
pass	tlf3 Map.to-list with SUM keys orders identically const and runtime
pass	tm2 a THREE-function mutual SCC — two tail legs route by value, the third combines the cycle result with a post-put draw
pass	tmb1 the RESUME VALUE DISCARDED in a do sequence — each arm resumes for EFFECT ONLY then answers a tombstone keyed to its own dispatch state, the body's positional fold and the inner frame's tombstone are both thrown away so only the FIRST dispatch's tombstone survives the unwind, and the seed is visible solely through that first frame's captured state
pass	tmb2 a SILENT POST-RESUME LEVY — each inner arm replays the tail then levies the outer handler DISCARDING the levy's value and answers a tombstone, the levies are observable ONLY through the outer audit read after the inner handle completes, and a dead-code elision that drops the valueless levy (its answer feeds nothing) shifts the audit by ten while leaving the inner result untouched
pass	tmb3 DOUBLE REPLAY THEN TOMBSTONE — the arm replays the tail twice discards BOTH outcomes and answers a state-keyed tombstone, neither replay's value survives yet both replays still run the body, and a lowering that returns either replay's outcome instead of the tombstone shifts the whole answer
pass	tmb4 a FOREIGN LEVY FEEDS THE DISCARDED REPLAY'S ANSWER — the arm resumes with an outer levy as the answer then discards the replay and answers a tombstone, the levy still fires and advances the outer thread even though its value flowed only into abandoned work, and the outer body's later levy reads five higher proving the discarded dataflow's effect landed
pass	tmp1 a SIMULATED-ANNEALING acceptance schedule — cool decays the temperature by nine-tenths truncating, accept takes any improvement and any worsening still under the heat counting the accepts, and the hot seed accepts EVERYTHING the cold seed rejects while both take the improving move
pass	tmr1 a TRIPLE-MODULAR-REDUNDANCY voter — set writes one of three channel slots, vote answers the majority value or -1 counting the all-differ disagreements, and the seed corrupts channel zero so one run stays unanimous while the other walks through a majority flip, a full disagreement, and a healed two-vote majority
pass	tnkB inline-thrice twin driven by the level difference — each siphon binds a quarter of the always-positive gap moving it A-to-B answering the transfer, pour tops tank A, levels reads the residual gap, and the seed sets tank B so the same siphon sequence converges through DIFFERENT geometric ladders to the same final gap (A stays the fuller tank throughout)
pass	to-bytes of a sliced multibyte string is read twice and both reads see the right bytes
pass	tok1 a TOKENIZER protocol — next-tok skips leading spaces, answers the token's byte-len, and threads the REST of the string; the drained stream answers zero forever
pass	tol1 a TOLL PLAZA with an exact-change lane — a five rides the exact lane counting itself and feeding the till, an overpayment needs the till to COVER the change (the till keeps the toll, the change flows back) else the car is DELAYED with the till untouched, an underpayment bounces with a four-hundred tag, the read packs till exact and delayed, and the seed's float lets one plaza serve every car while the other delays all three overpayers
pass	tow1 a TUG-OF-WAR marker with a latching win line — each pull moves the marker toward its side answering the position until crossing plus-or-minus ten LATCHES the result (every later pull answers the frozen plus-or-minus hundred), where reads the final marker, and the offset seed loses THREE pulls early so its frozen tail overlaps the other run's still-live rows
pass	tp1 TWO ops each own a tuple-state SLOT — lo advances field 0, hi doubles field 1, interleaved
pass	tp2 a SWAP op exchanges the tuple-state slots — interleaved readers observe the exchange
pass	tp3 a NESTED tuple state ((a b) c) — the arm rebuilds the inner Fibonacci pair and bumps the outer counter per dispatch
pass	tp4 readers and a SPIN writer share one tuple state — sum flows into slot a while old a shifts to b
pass	tpc1 TWO-PHASE COMMIT across two handlers — the coordinator prepares both sides, commits both only when both prepared, otherwise aborts both restoring the balances; the pass and fail paths both verified
pass	tph1 a generic ctor with TWO list payloads destructures both through nested patterns
pass	tpl1 a DELIMITER-JOIN rope builder — the first fragment lands bare, every later one is prefixed with the separator, the empty-vs-seeded start decides whether the FIRST dispatch takes the bare branch, and multibyte fragments keep the byte-length answers honest
pass	tps1 a LETTERPRESS galley — setting a word takes its width plus a space when the twelve-em line holds it, else BREAKS the line (counted, the word opening the next line at its bare width), justifying pads the line to twelve recording the gap as respacing, the read packs lines width and respacing, and the seed's headline stub pushes one galley's words onto broken lines while the other sets them flush with a ZERO-gap justify
pass	tpwJ a cross-scope shared-let in a resumptive arm — a let-bound NESTED-IF (outer cond reads a state field, inner reads the param) feeding 3+ consumers across 3+ dispatches with a state-reading second op, the thread-path no-local-slot face closed by 8276ad1a6
pass	tpx1 CHAINED shared-lets through a three-way arm — the step arm binds the advanced column THEN a second binder derived from the first, routes on the second binder's residue mod three, and every branch resumes a different mix of the two binders as answer and next-state; the closing peek reads the threaded column, exercising the collapse gate on a multi-binder arm
pass	tq1 a Q-shadow inside a TWO-effect region — P draws thread THROUGH the shadow while Q is locally rebound
pass	tq2 the Q-shadow's SEED mixes P and Q draws — both outer threads advance before the shadow opens, and resume after
pass	tq3 a Q-shadow wraps only the MIDDLE argument of a pure call — its neighbors dispatch to the outer P and Q frames
pass	tq4 BOTH effects shadowed in one inner region — two fresh threads run their course, both outer threads untouched
pass	tq5 a DEF installs the Q-shadow and draws P from inside it — the P dispatch crosses the def AND the shadow to the caller's frame
pass	tr1 a TUPLE snapshot resume value — each dispatch returns (state, state*10), two snapshots differ by the stride
pass	tr2 TWO string fields in the tuple with only one phi-grown — the sibling rope field is undisturbed (slot-clobber extended witness)
pass	trb1 the TRIBONACCI recurrence with a window readout — step rolls (a,b,c) to (b,c,a+b+c) answering the new term, peek answers the live window sum without advancing, and the seed shapes the leading term so the whole orbit shifts while fib1's two-term twin stays covered separately
fail	tree HEIGHT and BALANCE derive from insertion order over the same BST shape
pass	tri1 sign TRICHOTOMY of a draw — negative, zero-literal, and positive rows each route distinctly
pass	triple negation of a runtime boolean is a single negation
pass	triple-nested same-op performs — each argument is the inner perform's result
pass	trm1 a NESTED tuple-rest re-match peels two layers, through an if-selected scrutinee, with a RUNTIME element
pass	trm1 an export returns a two-element Set (one reachable cell, constructor-application render)
pass	trm2 an EXACT-arity tuple leaves the rest binder EMPTY, and a rest-only pattern gathers the whole tuple
pass	trm2 an export returns a one-entry Map (one reachable cell)
pass	trm3 an export returns a BigInt built from the scalar param (one reachable cell)
pass	trm4 an export returns an exact Rational from the scalar param (three reachable cells)
todo	trm5 a parameterized export returning a Symbol declines truthfully pending the runtime Sym render
pass	trn6 six dispatches — coin unlocks or wastes (wasted coins answer a deepening negative count), push passes and relocks or bounces (bounces answer their own negative count), the seed decides whether the gate STARTS unlocked, and the first push diverges the runs from the very first row
pass	true is greater than false
pass	true is not less than false
pass	truncating a rational whose integer part exceeds Int64 traps rather than wrapping
pass	tryh1 fifty try-unwraps of a HEAP (list) Ok payload under a Result boundary reclaim to zero
pass	ts1 a tuple-returning op SPLIT-refed — each destructured half feeds a DIFFERENT later op against advancing state
pass	tsqS a TWO-STACK amortized FIFO queue — enq pushes the in-stack, deq pops the out-stack and reverse-refills from the in-stack only when empty, so an element enqueued between refills never overtakes one already staged
pass	tt1 a NESTED-tuple op ARG destructured two levels inside the arm — both dispatches read the live state
pass	tt2 NESTED-tuple resume values across two dispatches — outer destructured by match, inner read by PROJECTION
pass	tt3 a tuple ROTATED through two chained dispatches — each rotation folds the state into the moved slot
pass	tt4 a MIXED String+Int tuple state — the arm grows the rope and folds the op arg into the counter per dispatch
pass	tt5 an inner PAIR swapped on counter parity — a nested-tuple state machine, both slots and the counter observed
pass	ttc1 a hole built from the runtime entry param round-trips through the tag weave
pass	tth1 TORTOISE-AND-HARE cycle detection — the handler owns the successor function and counts calls, the body's two-speed recursive driver meets inside the cycle and the call tally rides in the answer
pass	tug1 a TUPLE state pairing TWO collections — a List and a Map advance through different ops, and a cross op reads BOTH halves in one answer
pass	tuple access on a non-tuple is a type error
pass	tuple access on a record is a type error
pass	tuple access on a tuple chosen by a conditional projects the element
pass	tuple elements are accessed by index
pass	tuple elements are evaluated left to right
pass	tuple elements consume and read a shared binding in both orders
pass	tuple elements holding a remove-reached set and a drained map equal the direct tuple
pass	tuple keys differing only in the FLOAT component stay distinct and look up
pass	tuple set elements order by string content across reps then the scalar tiebreak
pass	tuples are deconstructed by pattern matching
pass	tv1 tuple VALUES with scalar keys across three arms — element propagation is value-position too
pass	tw1 arms of TWO different effects call one shared PURE helper — square-plus-one applied to each live state
pass	tw2 an explicit FEEDBACK loop — each call feeds the previous result back as the op's first argument beside a fresh draw
pass	twl1 a TAKE-WHILE pull stream — the body's recursive driver keeps drawing until the arm hands back a divisible-by-four value, accumulating the survivors and counting all pulls including the terminator
pass	twn1 TWO structurally IDENTICAL effect declarations (same op name, same signature, different effect names) nested as a tower — interleaved draws must dispatch to the CORRECT handler by effect identity alone, the sharpest post-schema-hash identity probe: same-shape effects with different seeds thread independently
pass	two @param accessors INTERLEAVED (a b a) consume per-op response queues in perform order
pass	two @param sites generate two accessors under one Param effect
pass	two @param sites with the same name are rejected as a duplicate effect operation
pass	two @params seed two NESTED handlers independently
pass	two @params seeding nested handlers in the WRONG order is caught by an asymmetric row
pass	two BigInt accumulators co-threaded through recursion compute a fibonacci
pass	two BigInts differing only PAST BIT 64 stay distinct as set elements
pass	two COMPOUND keys sharing a full hash collide via the nested champ_eq walk in one collision node
pass	two DIFFERENT ops sharing the outer-arg-match inner-state-match arm shape each thread their own payload
pass	two DIFFERENT user sums keep the generic same-kind mismatch, not the kind boundary
pass	two DISTINCT let-bound host calls each captured by its own escaping closure fire once each in order (adv-62)
pass	two Float32 literals that demote to the same bits const-fold equal
pass	two Float64.Infinity constants dedup in a set
pass	two IDENTICAL function types produce no fault (function argument type-checks clean)
pass	two IDENTICAL performs are distinct dispatches — never CSE'd into one
pass	two LEXICALLY-NESTED handlers of the same effect partition the performs by region
pass	two MUTUALLY-recursive functions dispatching on a recursive sum compute its parity
pass	two MUTUALLY-recursive sum types fold across their boundary
pass	two PREPEND versions over one shared tail read independently and the tail survives both
pass	two `?`s in one boundary both unwrap (nested happy path)
pass	two adapter records with different state types drive a take-while fold in one program
pass	two adjacent modules declared inside a function body compose
pass	two bare nullary variants reflect the same undetermined type
pass	two bare-None record fields (let-bound) keep their distinct Option element types — no cross-contamination
pass	two bare-None record fields passed as a DIRECT arg keep their distinct Option element types
pass	two bool-materialized ints compared fold to a boolean equality (value parity)
pass	two capturing closures stored as runtime tuple elements keep distinct captures
pass	two capturing closures stored in a LIST are selected at runtime and the capture reads after
pass	two chars with different scalar values are unequal
pass	two closure handles swap parameter slots through repeated tail calls
pass	two closures from one factory capture distinct values
pass	two composed times-minus-one strength reductions round-trip a normal operand but still trap at Int64.min
pass	two compound arguments each cross as their own handle in one peer call
pass	two const top-bit-set u64 bin bindings in one fn both fold unsigned
pass	two constant records with the same fields in different written order are equal
pass	two constant sums with the same payload but different variants are not equal
pass	two crossed LISTS compare structurally in the arm — same content from different builders
pass	two deep ropes built in OPPOSITE orders compare equal by content
pass	two definitions imported under the same name are rejected
pass	two dependent utf8 bin segments each sized by its own preceding length byte
pass	two dependent-size segments each bind the count a preceding segment named
pass	two different variants of the SAME sum compare unequal
pass	two differing runtime Ok values of a multi-parameter sum compare unequal by a heap walk
pass	two differing runtime sum values compare unequal by a heap walk
pass	two distinct called zero-arm scalar matches each report (no false-merge to one)
pass	two distinct doubles print to distinct text — 0.3 and 0.1+0.2 do not round-trip-collide
pass	two distinct map-keys are distinct entries
pass	two distinct mixed sums sharing a byte-identical immortal nullary terminal keep per-type identity
pass	two distinct names bound to the same value key the same map entry
pass	two distinct occurrences of the same unbound name are each reported, not merged to one
pass	two distinct type names are not a duplicate
pass	two distinct-named modules in one scope coexist
todo	two do-def-bound performs whose sum mixes handler-state width with a narrow param declines cleanly (not-yet-reducible, not an invalid module)
pass	two do-local function declarations are mutually recursive
pass	two effects bound to the same interface share one peer instance (both ops run)
pass	two effects declared with the same name are distinct, not one merged effect
pass	two effects each declaring a same-named operation do not collide
pass	two empty byte sequences are equal
pass	two empty strings are equal
pass	two equal chars compare equal
pass	two equal runtime quantities in a Set dedup to one element
pass	two equalities to different constants are an and-contradiction that does not subsume
pass	two equalities to different constants under or are the 2-point set neither collapses to
pass	two expects of one bound optional share its resident handle
pass	two exports normalizing to the same kebab extern name are rejected
pass	two fallible reads of one Bytes parameter share its resident handle slot
pass	two fallible reads of one collection parameter share its resident handle slot
pass	two functions both body-solved to Int64->Bool reflect equal arrows
pass	two functions defined by mutual recursion compute a result
pass	two functions with the same body-solved arrow but different bodies reflect equal types
pass	two guarded list-match arms over the same length classify by the head's sign
pass	two heap-list accumulators swap slots through every tail call
pass	two host calls consume their responses in order
pass	two host responses combine in call order through a non-commutative operator
pass	two host-seeded SIBLING handlers — each region's seed consumes its response in evaluation order
pass	two inclusive bounds at the same point collapse to equality
pass	two inclusive bounds collapse to equality at a negative point
pass	two inclusive bounds collapse to equality over an unsigned width
pass	two keys sharing a full 32-bit hash occupy one CHAMP collision node as distinct Set elements
pass	two let-sequenced sibling self-calls thread out-state via multi-value return
pass	two lists are concatenated into one flat list
pass	two lists of different length are unequal, not a type error
pass	two literal list elements in one arm conjoin — both must match
pass	two literal-payload arms of the same ctor variant each fire on their own literal
pass	two maps of different sizes are unequal, not a type error
pass	two maps with different keys are unequal, not a type error
pass	two module exports are selected by a runtime branch and applied as values
pass	two module functions are mutually recursive
pass	two mutually-recursive functions instantiate one generic chooser at different types
pass	two narrow record fields are projected and added
pass	two narrow tuple elements are projected and added
pass	two narrow tuple elements are projected and compared
pass	two nested distinct effects both tail-resumptive fold via the threading path
pass	two nested intra-program handlers compose inside-out
pass	two nested recursive const-closure drivers (filter under fold) emit valid wasm and compute correctly
pass	two newtypes over the SAME inner type do not cross-assign
pass	two pattern keys match with one satisfied by the runtime-keyed entry
pass	two perform-drawn quantities of one dimension ADD — same-unit combine over two crossings
pass	two performs as the two ARGUMENTS of a pure USER function thread the state left-to-right
pass	two performs bound by nested lets thread the handler state in order
pass	two performs of a MULTI-parameter op each combine both args with the state advancing between them
pass	two rationals denoting the same number are equal regardless of how they were written
pass	two recursion-built linked lists compare equal by a deep heap walk
pass	two recursion-built linked lists of different lengths compare unequal by a heap walk
pass	two refutable literal fields in one record arm compose (both must match)
pass	two residuals from ONE curry source hold distinct first-stage captures
pass	two ropes pack CROSSWISE into a tuple by a runtime select and both unpack live
pass	two runtime Bytes values of equal content compare equal (rope vs flat)
pass	two runtime Ok values of a multi-parameter sum compare equal by a heap walk
pass	two runtime String entry args compare by content including multibyte
pass	two runtime maps built in different insertion orders compare equal by content
pass	two runtime maps with the same keys but a different value are unequal
pass	two runtime records compare equal by a heap walk
pass	two runtime strings compare equal by their contents
pass	two runtime sum values compare equal by a heap walk
pass	two runtime sums with the same payload but different variants compare unequal by a heap walk
pass	two runtime tuples compare equal by a heap walk
pass	two runtime-selected splices keep their operand ORDER in a non-commutative form
pass	two same-dimension quantities with differing inner numerics retype the inner literal
pass	two same-signature Bytes-returning closures share one call — first
pass	two same-signature Bytes-returning closures share one call — second
pass	two same-signature String-returning closures share one call
pass	two same-signature closures alongside a plain export all coexist
pass	two same-signature closures alongside a plain export — the plain export runs
pass	two same-unit Qty host results combine guest-side as a same-dimension add
pass	two same-variant ctor list elements refining the payload by literal fall through, not trap
pass	two scalar matches of different widths beside i32 handles in one recursive emit frame stay valid
pass	two sequential lookups on the same Map with perform-threaded keys stay independent
pass	two sets of TUPLES built in different orders compare equal through the compound elements
pass	two sets of different sizes are unequal, not a type error
pass	two sets with different elements are unequal, not a type error
pass	two sibling Option fields of different arg do not alias in the value-encode descriptor
pass	two sibling memoize demands of one key: the first's put threads to the second
pass	two sibling modules may each define a private helper of the same name
pass	two slices of one parent both read after the parent is dead
pass	two specializations of one driver on different closures do not collide in the spec memo
pass	two stacked erased newtypes wrapping a sum peel both no-op layers before the inner discriminant
pass	two structural values of the same shape are equal
pass	two successive cross-function folds thread out-state left-to-right in the continuation
pass	two successive self-recursive folds thread state between them
pass	two sum arms with textually identical bodies still bind their own variant's payload
pass	two sum types whose names differ only by punctuation stay DISTINCT types
pass	two two-site ops INTERLEAVED A-B-A fold (order does not matter when all arms are multi-site)
pass	two type-value operands in arithmetic name that arithmetic is not defined on Type
pass	two unequal runtime strings compare false by their contents
pass	two variants of the same sum compare unequal regardless of operand order
pass	two variants past the first each carry a different nested sum, over a runtime discriminant
pass	two whole-module alias imports project a uniformly-named export with no collision
pass	u1 a UInt64 state thread ABOVE Int64.max — high-half values survive dispatch, mod-10 digits pin the sequence
pass	u3 UInt64 arguments ABOVE Int64.max echo through dispatch exactly — 2^63+41 and u64-max both survive, count pins trips
pass	u4 a UInt64 ABOVE Int64.max rides as a SUM payload — parity picks Lo/Hi, the Hi arm range-checks the high half
pass	u64h1 the u64 host response compared WITHOUT T.of arrives intact
pass	u64h2 T.of over the u64 host response (isolated, in-range 1000)
pass	u64s1 a UInt64 handler state ABOVE the i64 boundary — unsigned comparison in the arm stays correct as the thread advances past 2^63
pass	ubr1 a dependent-size utf8 segment decodes across a rope seam (prefix in the base, string spanning the concat)
pass	ubr2 fifty dependent-size utf8 decodes over a deep rope leak linearly (~2 per decode)
pass	uc1 user NULLARY ctors as first-class values — set dedup + runtime-selected match, through the cadenza hop
pass	ug1 THREE update GENERATIONS share structure — a, update(a), update(update(a)) all readable with distinct views, the untouched slot threads to the newest
pass	uint16 wrap truncates a wide runtime value to the low 16 bits
pass	uint8 wrap of a negative multiple of 256 is its low byte, zero
pass	uint8 wrap of a negative runtime value reads the low byte as unsigned
pass	uint8 wrap truncates a wide runtime value to the low 8 bits
pass	una1 a runtime unsigned less-than orders by magnitude (i64.lt_u), the runtime dual of the const-fold
pass	und1 an IDEMPOTENT-UNDO counter — the tuple state carries (value,last-delta); undo reverses the last delta exactly once and answers it, the second undo answers zero and no-ops
pass	under (pragma overflow (signed wrap)) a RUNTIME signed + overflow WRAPS (stage-2c runtime codegen)
pass	under (pragma overflow (signed wrap)) a constant signed + overflow WRAPS instead of CDZ0304
pass	under (pragma overflow (unsigned wrap)) a RUNTIME unsigned + overflow WRAPS to 0
pass	under a wrap pragma a NON-overflowing + is unchanged (2 + 3 = 5)
pass	under-applying a concrete-payload constructor names its payload type
pass	under-applying a unary constructor (Some) is a type error, not a fabricated unit payload
pass	under-applying a unary constructor is a type error, not a fabricated unit payload
pass	unequal-depth payload paths bind distinctly
pass	union cardinality with a runtime-element operand
pass	union dedups overlapping elements by content, counted once
pass	union is associative
pass	union is commutative
pass	union of a set with itself is the set (idempotent)
pass	union with a DERIVED empty (self-difference) is identity at trie scale
pass	union with the empty set is the set itself
pass	unit and the empty tuple are the same value
pass	unit arithmetic on a map-extracted quantity re-enters the map typed
pass	unit is the empty tuple
pass	unp1 native #-form patterns over UNTYPED scrutinees compile and infer — the #5429 M3 Phase-2 fence
pass	unp2 native #list and #record patterns over UNTYPED scrutinees — the #5436 kind-completion of unp1
pass	unquote in quasiquote evaluates normally and embeds
pass	unquote outside quasiquote is a syntax error
pass	unquote with more than one operand inside a quasiquote is malformed
pass	unquote-splicing a list of Ast values splices by identity
pass	unquote-splicing a list of nested lists lifts each list element into an Ast.ListCtor
pass	unquote-splicing a list with a RUNTIME Ast element splices it by identity at runtime
pass	unquote-splicing lifts a boolean list to Ast.Bool leaves
pass	unquote-splicing lifts a float list to Ast.Float leaves
pass	unquote-splicing lifts a string list to Ast.Str leaves
pass	unquote-splicing outside quasiquote is a syntax error
pass	unquote-splicing splices list elements into parent
pass	unsigned 8-bit addition that overflows its width is rejected at compile time
pass	unsigned branch refinement stays value-correct at the domain edges (0-lower-bound tautologies)
pass	unsigned comparison orders by magnitude, not by signed interpretation
pass	unsigned comparisons against zero simplify and agree with the true comparison
pass	unsigned div by two is magnitude at UInt64.max
pass	unsigned power-of-two div/rem strength-reduces (UInt32)
pass	unsigned power-of-two div/rem strength-reduces (UInt64, by magnitude)
pass	unsigned subtraction below zero is rejected at compile time
pass	unsigned-wrap wraps an unsigned overflow (UInt8 255 + 1 = 0)
pass	updating a list index replaces that element, leaving others
pass	updating a list slot with an element of a different type is a type error
pass	updating a record field changes its type to the new value's
pass	updating a record field preserves the other fields' values
pass	updating a record field replaces its value
pass	updating an absent record field is rejected
pass	using an Option where its bare payload is expected says to match it, with no mechanical fix
pass	utf1 the THREADED Bytes state goes UTF-8-invalid mid-run — a lead byte alone flips decode to None, the completion byte restores it and a bad continuation leaves it broken
pass	uv2 a @requires-guarded fn fed by DRAWS — the contract checks effectful argument values at each call
pass	uv3 the handler ARM calls a @requires-guarded helper — the contract checks the live state per dispatch
pass	uv4 a RELATIONAL @ensures (ret > x) over a TWO-draw body — the postcondition compares the effectful result to the arg, twice
pass	uwr1 a NARROW UInt8 handler state — wrapping-add accumulates modulo 256 through the thread, each dispatch answers the widened running value
pass	valid variant shapes (nullary / generic / record payload) do not false-positive as malformed
pass	value-eq CONTROL: a runtime slice compares equal to a flat Bytes of the same content
pass	vig1 a VIGENERE-style rolling-key cipher — enc adds and dec subtracts the key byte selected by the ADVANCING key index mod 3, both mod 26, so interleaved enc/dec draws consume ONE shared key stream and the seed shapes the middle key byte
pass	vnd1 a VENDING machine with a change float — insert accumulates credit, buy vends only when the credit covers the seven-cent price AND the float covers the change (answering the change, growing the float by price minus change, zeroing credit), refusing otherwise with distinct negated codes, and the seed's starting float decides whether ANY sale completes
pass	vres1 a variant-with-payload host RESULT delivers the payload arm (w10c shape)
pass	vres2 a MIXED-WIDTH variant host RESULT delivers each arm across three dispatches
pass	vres3 a NEGATIVE s64 payload through the variant host-result join
pass	vse1 a record with SIBLING Option-sum and Option-Bytes fields value-encodes the sum payload intact — the Option-Bytes sibling must not alias its bytes descriptor onto the Option-sum field
pass	vse10 two RESULT fields at different type args each encode their own Err payload — the same generic decl at two instantiations shares no descriptor
pass	vse11 two LIST fields of different element types encode independently — the list control of the generic-instantiation family
pass	vse12 a RUNTIME-BUILT record with String + Bytes fields escapes via the runtime value-encode walker (the reducer-Event shape)
pass	vse7 TWO Option-sum sibling fields at DIFFERENT sum decls each keep their own value-form — the first instantiation's descriptor must not stamp the second
pass	vse9 the Option-sum field sorting FIRST keeps both siblings intact — the sort-order control of the sibling-descriptor pair
pass	vtc1 a VOTE-TO-CLOSE quorum — second registers answering the count until the seed-shaped threshold CLOSES the motion (answering a hundred plus the count; later ops answer minus a hundred), withdraw decrements while open clamping at zero, and the lower threshold closes one second earlier turning the tail into closed sentinels
pass	vwp1 a VOLUME-WEIGHTED average price tracker — trade accrues (price*qty, qty) answering the running notional, vwap answers the truncated notional-over-volume or -1 before any trade, and the LEADING -1 row drives the whole packed total negative while the seed shifts one trade's price through both later readouts
pass	wa1 wrapping-add as the next-state — a near-MAX seed wraps to a concrete near-MIN value on the second draw
pass	wa2 wrapping-add of the op ARG and state in the resume value — wrap, exact-MAX, and identity rows
pass	wa3 the wrap WALK — three draws from a MAX seed step MAX, MIN, MIN+1 through wrapping-add(+1), checked '+' beside
pass	wc1 the inner arm's resume value draws from TWO different outer effects — both threads advance per inner dispatch
pass	wen1 an enum host-import RESULT lifts and selects the guest arm
pass	wep1 SIXTEEN scalar entry params cross the boundary and sum (the max-flat-params edge)
todo	wfp1 SEVENTEEN scalar entry params decline pending the memory-indirect convention
pass	wfp2 EIGHT list entry params are sixteen flat values — the lifted boundary edge sums their lengths
todo	wfp3 NINE list entry params are eighteen flat values and decline
pass	when two abortive performs sit on one spine the FIRST (leftmost) abort wins
pass	widening a runtime UInt8 to UInt64 zero-extends (total, emits)
pass	widening a runtime UNSIGNED narrow integer to Int64 zero-extends (total, emits)
pass	widening a runtime narrow-width unsigned integer to Int64 zero-extends (total, emits)
pass	widening a runtime signed narrow integer to Int64 sign-extends (total, emits)
pass	with the Option operand on the LEFT, the quantity-add clash names the (Option (Qty …)) type
pass	with three abortive performs on a strict spine the leftmost still wins
pass	without a default-fraction pragma the same (/ 1 3) is ordinary integer division — 0, not 1/3
pass	without- and with-REACHED records with runtime leaves equal the directly-built record
pass	wl1 a List-of-UInt8 entry param's extreme elements read back exactly (255 zero-extends, 0 reads 0)
pass	wl2 a List-of-Int32 entry param's negative element sign-extends on read
pass	wl3 three list widths (UInt8, Float64, Int64) coexist as params of one entry wrapper
pass	wl4 a List-of-Float64 entry param's element survives load and return exactly
pass	wlkC single-wall control — reflect only at the floor, ceiling unguarded
pass	wnd1 a WIND-CHILL stepper — gust sets the wind answering temp minus twice the wind CLAMPED at minus thirty (extremes counted), warm raises the temperature answering it, ext reads the extreme count, and only the cold seed drives one gust past the clamp so its rows ride deep negative while the warm seed never clamps
pass	wnw1 a BISECTION searcher whose starting window is picked by a MATCH IN THE HANDLE'S INIT POSITION — the init destructures a seed tuple and an if ladder inside the match arm chooses among three windows, narrow halves the window toward the guess counting probes and answering side midpoint and count, a CLOSED window answers a frozen readout without probing, span reads the gap, and the tight seed window closes two probes early so its last narrows freeze while the wide window is still hunting
pass	wp2 a CHECKED-SHIFT computed perform arg feeding the two-lookup-match arm — shl scratch width does not alias the Option handle
pass	wp3 a THREE-param op with two computed args through the two-lookup-match arm — every arg slot and the handle slot stay disjoint
pass	wrap-narrowed values fill BOTH segments of a dispatching frame — the wrapped tag selects the arm
pass	wrapping a non-integer source is rejected
pass	wrapping addition of in-range operands is ordinary addition
pass	wrapping addition wraps modulo two to the sixty-fourth on overflow
pass	wrapping an unwrapped number re-enters the units world at the new unit
pass	wrapping arithmetic algebraic identities hold at the range boundaries
pass	wrapping arithmetic over runtime operands wraps at run time
pass	wrapping arithmetic uses the named wrapping form of the operator
pass	wrapping multiplication wraps rather than trapping
pass	wrapping subtraction of in-range operands is ordinary subtraction
pass	wrapping subtraction wraps modulo two to the sixty-fourth on underflow
pass	wrapping-add on a runtime Int8 wraps at its signed boundary
pass	wrapping-add on a runtime UInt8 wraps modulo 256
pass	wrapping-add wraps a boundary-crossing runtime Int64 at the max boundary
pass	wrapping-arithmetic identities compute the identity value over a runtime operand
pass	wrapping-mul of the minimum integer by -1 wraps back to the minimum
pass	wrapping-mul on a runtime UInt8 wraps modulo 256
pass	wrapping-mul wraps a boundary-crossing runtime Int64
pass	wrapping-sub of a runtime value from itself is zero at every input
pass	wrapping-sub of the minimum integer from zero wraps back to the minimum
pass	wrapping-sub on a runtime Int8 wraps at its signed min boundary
pass	wrapping-sub on a runtime UInt8 wraps modulo 256
pass	wrapping-sub wraps a boundary-crossing runtime Int64 at the min boundary
pass	wrd1 a DIGIT-LADDER distance scorer — feed answers the per-digit Hamming distance between the three-digit guess and the seed-picked target computed by a recursive digit-walk callee, closest tracks the minimum, and the two targets rank the SAME guesses in different orders with each run holding a different exact-hit row
pass	wtr1 the WATER-JUG puzzle over a five-cap and a three-cap jug — fill tops a jug, pour transfers the minimum of the source's content and the destination's headroom, empty drains, every answer packs both jugs as a*10+b, and the seed picks WHICH jug the routine works from so one run reaches the classic four-measure states while the other cycles
pass	wx1 the state thread WRAPS at Int64.max — three draws straddle the wraparound and the comparisons see the seam
pass	wx2 Int64.min and Int64.max cross the dispatch as OP ARGUMENTS and come back exact — a count arm tallies the trips
pass	wx3 Int64.min and Int64.max ride as SUM payloads through dispatch — variant chosen by state parity, payloads verified exact
pass	wx4 the ARM doubles its argument with wrapping-mul — MAX wraps to -2, MIN to 0, a small value stays exact, count rides along
pass	wx5 the state STEPS by wrapping-sub of MAX each draw — two hops cross the seam in opposite directions
pass	wx6 wrapping increments CHAINED hop-to-hop — MAX-1 crosses the seam inside a nested three-op chain, count pins the trips
pass	xar1 a handler arm List.at-extracts from a captured nested list per dispatch — the retain leaks once per performance
pass	xar3 the arm-extract with the dispatch crossing a NON-recursive helper def answers and leaks once
pass	xar5 a RECURSIVE performer with the arm capturing a main-local heap list folds and answers (the threaded heap capture leaks per dispatch through the resume-escape fence)
pass	xas1 a recursive performer with the arm capturing a main-local SCALAR let folds (threaded as an extra spec param)
pass	xas2 a recursive performer with the arm capturing main's PARAM directly folds (params thread into the lifted def)
pass	xat1 TWO scalar main-local lets captured by the arm thread as extra spec params
pass	xat2 a mixed HEAP-list + scalar capture threads and answers but leaks the threaded list
pass	xc1 xor by the same value twice cancels to the operand (runtime, constant, commuted, masked w)
pass	xc2 a trapping discarded xor operand keeps its trap (the cancellation must not drop it)
pass	xcl1 the arm applies a main-local CLOSURE per dispatch through the recursive specialization (leaks the threaded cell)
pass	xf1 an escaped-closure hop answer indexes a const-folded Set.to-list
pass	xf2 two IDENTICAL runtime encodes dedup as ONE set member (membership over arena Bytes)
pass	xf3 Set.contains finds an arena-sourced Bytes member via a FRESH encode
pass	xf4 a Map keyed by encode-Bytes discriminates two different encodes
pass	xf5 a handler threads a SET state and the final answer materializes its sorted head
pass	xfr1 CLAMPED transfers between two accounts — each transfer moves min(requested, available) and answers the amount actually moved, the final draw reads the signed imbalance, and only one seed clamps the last transfer
pass	xh1 the INNER arm performs the OUTER op with a COMPUTED key and the outer arm is the two-lookup-match Map shape — the width-partition crosses a handler boundary
pass	xhs1 CROSS-HANDLER shared-let — the inner step arm let-binds the advanced column, PERFORMS the outer note with it mid-arm (accumulating), then resumes packing the binder with the note's answer while threading the binder as next-state; the seed bias shifts every column so both the inner rows and the outer accumulator diverge
pass	xhsA the binder feeds the outer perform and the answer but the state threads the OLD column — boundary variant
pass	xhsB the binder feeds the outer perform and the threaded state but the answer is only the note result — boundary variant
pass	xhsC the outer perform takes a CONSTANT while the binder feeds answer and state — boundary variant
pass	xhsD the DROPPED-frozen-arg complement — the outer note arm IGNORES its op param (answers the count, threads count plus one) while the inner step arm still let-binds the advanced column performs the outer note with it and resumes packing both; the frozen argument is dropped not escaped, and the fold is CORRECT
pass	xhsE the COMPUTED perform-arg variant — the inner arm let-binds the advanced column but performs the outer note with the binder PLUS ONE (a compound of the binder, not the binder itself), resuming and threading as before; the freeze inlines the compound's arm-local refs to their drain-safe inits so it folds
pass	xhsF the MULTI-PERFORM variant — the inner step arm performs the outer note TWICE with the same shared binder before resuming with both answers' low digits and threading the binder; two frozen arguments must drain-bind independently in one arm
pass	xhsG CONDITIONAL mid-arm foreign perform — the note rides only the THEN branch
pass	xhsGGrow conditional mid-arm foreign perform WITH a growing List.push next-state — composition of the conditional-selector freeze (7106ad497) and the growing-state correct-fold (95f5ab8d2)
pass	xhsGdeep the perform buried TWO selector levels deep — an if-in-if routes on parity then magnitude and only the even-and-large leaf performs the outer note, the other three leaves answer distinct tags without performing; the seeds route their FIRST step through different silent leaves before both reach the performing leaf on the second
pass	xhsGmatch the MATCH twin of the conditional mid-arm foreign perform — the same then-else split expressed as a boolean match scrutinee, exercising the match-arm out-state merge path of the selector freeze
pass	xhsGrow GROWING-STATE shared-let with a mid-arm foreign perform — the step arm derives its binder from the list LENGTH, performs the outer note with it, answers the binder packed with the note's low digit, and threads the list GROWN by the binder; the growing next-state excludes the collapse so the distribute path must keep the two binder copies coherent
pass	xhsH the foreign perform AS the selector — the note's answer is let-bound then ROUTES the branch (over nine takes a 7 tag, under packs the answer's low digit), both branches silent, the perform running exactly once as condition input; the inverse of the G family where the perform hides inside a branch
pass	xhsMultiCtrl the inline control for the merged multi-slot probe — the binder inlined at every use
todo	xhsRec recursive-DRIVER + shared-let + mid-arm foreign perform — drive recursively performs I.tick each iter; the tick arm let-binds c2, performs O.note(c2) mid-arm, resumes packing nv, threads c2. Collapse excluded by in_recursive_specialize -> distribute; does the shared binder diverge like xhsGrow?
pass	xhsTwo two DISTINCT outer handlers — inner step performs O.note(c2) then P.tick(c2), both foreign, both with the shared binder; tests #fa freeze for two handlers at different drain levels
pass	xme1 an effect declared in a LIBRARY module is handled by the importing entry
pass	xme3 TWO library modules share ONE effect — both performers route to the entry's single handler
pass	xme4 a library exports a RUN-WRAPPER installing its own handler — the entry only calls it
pass	xop1 a let-bound extracted Option matched TWICE leaks twice — the unpaired dup is per-unwrap
pass	xop2 a bounds-MISS extraction (None path) from a nested list still leaks one cell
pass	xop3 the extracted Option unwrapped in ANOTHER def leaks the same as the local unwrap
pass	xop4 a FRESH compound-payload Option matched twice leaks its payload (one unwrap balances, the second does not)
pass	xop5 a helper-BUILT compound-payload Option matched twice in the caller leaks the same payload
pass	xsw1 a CROSS-SWAPPING scalar pair — each dispatch computes both successors then installs them SWAPPED (a gets the b-derived value, b the a-derived), lineage crosses sides every step
pass	za1 literal-arm dispatch on a draw — the MATCHED arm performs again, both calls exercised
pass	za2 a COMPUTED scrutinee (difference of two draws) selects the performing arm at one input only
pass	za3 NESTED literal-arm dispatch — each level matches a let-bound draw, the innermost arm reads a third
pass	zc1 dual-spine zip exiting at the INNER arm (ys empties mid-decomposition): the arm returns acc without consuming xh/xt
pass	zc2 dual-spine zip exiting at the OUTER arm after walking both spines to equal length
pass	zc3 dual-spine zip exiting IMMEDIATELY (xs empty on entry)
pass	zero divided by a runtime value folds to zero but keeps the zero-divisor guard
pass	zgz1 a ZIGZAG codec accumulator — enc maps signed to unsigned (2v for non-negative, -2v-1 for negative) and folds the code into the state sum, dec UN-zigzags the accumulated sum whose parity decides the sign, and the seeds put the decode answers on opposite signs
pass	zpl1 a ZIPLINE with a brake zone — gliding advances the position by speed times the hang time and gravity feeds the speed capped at nine, a brake in the zone past ten sheds four speed floored at one (a clean one-tag) while an early brake SCRAPES (counted, shedding one), the read packs position speed and scrapes, and the seed's launch speed reaches the zone before the brake on one line while the other scrapes short of it
pass	zx4 the caller retains xs and reads it AFTER the zip consumed it (over-release fence for exit-drop)
pass	zx5 an exit arm that RETURNS the enclosing binder's tail (escape-gate fence: that drop must stay suppressed)
pass	α-equivalence (aconv) recognizes (λx.x) and (λy.y) as the same term up to bound-variable renaming
pass	α-equivalence handles nesting and distinguishes a bound variable from a same-numbered free variable
pass	Type.ast reflects a DIRECT user-generic instantiation (Box Int64), coherent with the value-mediated form
pass	Type.ast of a WRONG-ARITY direct user-generic application is rejected, not silently reflected
pass	Type.ast does not suppress a fault inside a (Type.of e) argument
pass	a macro-introduced SIBLING def with a caller-spliced name binds visibly at the enclosing scope
pass	a macro-introduced SIBLING def with a macro-internal name stays hygienic-local, not enclosing-visible
pass	Type.ast of a non-type value (an effect name) is a CODED rejection, not a codeless decline
pass	a macro-introduced SIBLING FN def with a caller-spliced name binds visibly (callable) at the enclosing scope
pass	a macro-introduced SIBLING FN def with a macro-internal name stays hygienic-local, not enclosing-callable
pass	a macro emitting a wrapping (do def…) at a statement position SPLICE-FLATTENS its sibling defs (multi-def idiom)
pass	a macro-internal name in a splice-flattened wrapping (do def…) stays hygienic-local
pass	a macro splicing a wrapping (do def def) introduces SEVERAL caller-named sibling defs, all visible
pass	d307a a discarded effect-op perform in statement position RUNS (advances handler state) and does NOT warn CDZ0307
pass	d307b a PURE discarded expression still warns CDZ0307 — the #7761 fix does not over-suppress
pass	a macro-introduced SIBLING type with a caller-spliced name binds visibly; its qualified ctor T.V rides on it
pass	a macro-introduced SIBLING type with a macro-internal name stays hygienic-local
pass	a macro-introduced type's BARE variant ctor follows the ctor name's own provenance (macro-internal is qualified-only)
pass	a macro-introduced type with a caller-spliced bare ctor name binds the bare constructor visibly
pass	sqtv1 a runtime Symbol tuple element renders as the (Symbol.of …) constructor, not a bare string
pass	a misspelled Ast module member Sym is rejected CDZ0201 with Symbol leading the did-you-mean (prefix-rank)
pass	one macro splices a TYPE and a fn over it via a wrapping (do …) — the type splice-flattens alongside the def
pass	a macro-internal type in a wrapping (do …) splice stays hygienic-local
