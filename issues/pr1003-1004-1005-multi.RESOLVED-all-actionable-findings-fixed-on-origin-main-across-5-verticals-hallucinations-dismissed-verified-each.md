# PR#1003/#1004/#1005 — verified Copilot comments (multi-owner)

(PR#1005's two sread-eval.cdz:184 "Logic Error" comments — undefined 'restore' + lambda arg/Var mismatch —
were HALLUCINATIONS: grep finds no restore/Thunk/Lambda in sread-eval.cdz; the actual :184 is a @test.
DISMISSED, see ledger + [[amazon-q-hallucinates-cdz-cad-code]] class.)

## PR#1003 reducer.wit ×3 → v-agent-harness (kernel WIT contract)

blame owner = v-agent-harness (cdz-kernel). All three are WIT-contract design points:
- (id 3695637177, :83) return a CURSOR instead of a full list for a potentially-large prefix (pagination).
- (id 3695638213, :40) `effect-id` is the await-continuation key, but `fold.apply` returns
  `effect-request`s with NO ids and `resumes` only appears on result/timer events — so the guest can't
  know the assigned id at request time. Contract should specify ids are assigned DETERMINISTICALLY in
  emission order (guest-predictable) or add a correlation mechanism.
- (id 3695638216, :69) `effect-request.payload: list<u8>` can't distinguish an intentionally-empty payload
  from ABSENCE; Rust `EffectRequest` uses `Option<Payload>` — make the WIT field `option<list<u8>>` to
  match. All design/API on the v0.1 kernel WIT; owner's call on which to adopt for v0.

## PR#1004

### cadenza-ast/src/lib.rs:5 (id 3695666777) → v-syntax
blame `fa0161bdc` "syntax: extract cadenza-ast bottom crate". Crate-level docs reference `constitution.md
§X` — a PLACEHOLDER (`§X`), inaccurate. Cite a real stable section/heading or drop the constitution ref.

### xtask/src/fleet.rs:473 (id 3695666790) → v-fleet-tooling
The wake doc (just fixed to `fleet wake <to> --operator-message` in `5691ca99c`) documents BRIDGE
behavior, but bridge.js:63 actually invokes `fleet wake --operator-message <agent>` (flag BEFORE
positional). Both work (clap is order-agnostic), but since this line documents the bridge specifically, it
should match the bridge's actual `--operator-message <agent>` order OR be phrased order-independently.
Doc-only. (This is a re-flag of the just-landed 5691ca99c fix — the fix used `<to> --operator-message`
while the bridge uses the reverse; reconcile to order-independent wording.)

### fleet/slack-bridge/bridge.js:74 (id 3695666796) → v-slack-bridge
The exit handler (added for the PR#999 wake-exit-code fix `fe1e090d8`) only checks `if (code)`; a child
killed by a SIGNAL reports `code === null` + `signal` set, so a signal-killed wake ISN'T logged. Capture
+ log the `signal` case too (`if (code || signal) console.error(... exited ${code} signal ${signal})`).
Robustness follow-on to the PR#999 slack fix.

## PR#1005

### sread.cdz:598 (id 3695694091) → v-compiler-ml — CLOBBERABLE MARKER
`__illwidth__` is a normal def-table entry keyed by `name-id("__illwidth__")`; `record-def` is `Map.insert`,
so a later user `def __illwidth__ …` OVERWRITES the marker and `has-illwidth-marker` stops detecting the
invalid-width condition — and `name-id` is a HASH, so a collision could false-positive/negative. Use an
unreachable key (the reserved `nameId = 0` that `unsupported()` uses) or a dedicated `Tree` marker field so
user code can't clobber it and hash collisions can't affect it. Correctness/robustness of the width-reject.

### fleet/slack-bridge/run.sh:66 (id 3695694101) → v-slack-bridge
The TMUX-export workaround is now redundant + its explanation stale: `cargo xtask fleet wake` targets the
tmux server by explicit session (default `main`), bypassing the `in_tmux()`/$TMUX gate. Remove the block
(reduces complexity + confusing wake-path logs).
