# PR #1211 review comment — guide/src/content/chapters/Ordering.tsx (v-guide)

Mirrored from https://github.com/camshaft/cadenza/pull/1211 (PR: "cand: v-guide — a3cb1bf10").
Follow-on to my #1187 Ordering-chapter notes — the reworked Bytes paragraph added a Text clause.

## Bytes paragraph's new "the same way Text compares" clause reads as a Text rule + is redundant (Copilot, Ordering.tsx:113) — doc
> This paragraph is explaining `Bytes` ordering, but the new sentence fragment "the same way Text
> compares in dictionary order" reads as if the rule is about `Text` rather than `Bytes` (and is
> redundant with the earlier `Text` example). Consider removing the `Text` clause and stating the
> `Bytes` lexicographic rule directly.

State the Bytes lexicographic rule directly; drop the redundant Text cross-reference.
