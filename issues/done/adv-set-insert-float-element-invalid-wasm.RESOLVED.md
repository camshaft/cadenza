# MISCOMPILE (invalid wasm, breaker): Set.insert of a FLOAT element fails at load (element-boxing gap; map keys fine)

INVALID-COMPONENT miscompile on trunk@513e08556: Set.insert of any Float element (constant or runtime param) fails to compile at load ('wasm[0]::function[5]'), check-clean, no diagnostic. Isolation: constant Set.of float literals fold fine (2); float MAP keys fully correct incl. canonical-form NaN unification and -0.0/0.0 distinctness (verified this tick); only the set-ELEMENT insert path breaks — likely the element-boxing gate has no Ty::Float arm (the exact class of the historical empty-set String box-int bug pinned in 13-strings). Heads-up: any set-float 'pass' before this fix is unreliable (single inserts crash). Graded case Fails. The float map-key control faces are promotable once this fixes — I'll pin them together.

Corpus-bugfix notes: box_op_ty (select.rs:1485+) DOES have Float arms (box-float/box-float32), and a CONSTANT float set compiles+folds. So the gap is likely in how the set-INSERT emit invokes/coerces boxing for a runtime float element (valtype/coemit mismatch), NOT a missing box_op arm. Same class as the historical empty-set String box-int bug (13-strings pin).
