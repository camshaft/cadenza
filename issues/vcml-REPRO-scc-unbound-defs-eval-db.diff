diff --git a/implementation/compiler-ml/src/eval-db.cdz b/implementation/compiler-ml/src/eval-db.cdz
index e20734318..df18f2ae3 100644
--- a/implementation/compiler-ml/src/eval-db.cdz
+++ b/implementation/compiler-ml/src/eval-db.cdz
@@ -87,43 +87,24 @@ def eval-core-d(c: Core, env: Map(Int64, Int64), defs: Map(Int64, (List(Int64),
         | Option.Some(c) => (if (c == 0) then eval-core-d(els, env, defs) else eval-core-d(thn, env, defs))
         | Option.None(_) => Option.None(unit))
   | Core.CCall(name, args) =>
-      // slice-B3: a recursive call is a NON-inlining name reference. Look the def up in the def-env, eval the
-      // args in the CURRENT env, bind them positionally into a FRESH env, then eval the body under it. An
-      // unknown name (empty def-env — the B1a/existing 2-arg path) or a declining arg → None. INLINE (not a
-      // factored helper) — see the CFnRefVar note re: a compiler mutual-recursion monomorphization bug.
-      (match Map.lookup(defs, name) with
-        | Option.None(_) => Option.None(unit)
-        | Option.Some(pb) =>
-            (match pb with
-              | (params, body) =>
-                  (match eval-args(args, 0, env, defs) with
-                    | Option.None(_) => Option.None(unit)
-                    | Option.Some(vals) =>
-                        eval-core-d(body, bind-params(params, vals, 0, (Map.empty : Map(Int64, Int64))), defs))))
-  | Core.CFnRef(name) =>
-      // HIGHER-ORDER: a def-name as a first-class fn VALUE. A function value IS its name (top-level defs are
-      // closed — no capture), represented as the Int64 name. Some(name); the CFnRefVar site dispatches it.
-      Option.Some(name)
-  | Core.CFnRefVar(fBinder, args) =>
-      // HIGHER-ORDER apply: call the fn-value held in binder `fBinder` (a fn-typed param). Look up the binder's
-      // value = the callee def-NAME (put there by a CFnRef arg), then dispatch by name EXACTLY like CCall's
-      // INLINE body. NOTE: the def-env dispatch is INLINED here (duplicated from the CCall arm) rather than
-      // factored into a shared `apply-def-by-name` fn — factoring it created a NEW member of eval-core-d's
-      // mutual-recursion SCC, which trips a compiler-ml/rcdzc monomorphization bug (spurious CDZ0101 unbound
-      // param; see queue/mlrepro-new-fn-in-eval-core-mutual-recursion-cycle-spurious-unbound-param.md). Once
-      // that host bug is fixed, both arms can share `apply-def-by-name`. An unbound binder / non-fn value /
-      // unknown name → None (declines, never a wrong value).
-      (match Map.lookup(env, fBinder) with
-        | Option.None(_) => Option.None(unit)
-        | Option.Some(name) => (match Map.lookup(defs, name) with
-            | Option.None(_) => Option.None(unit)
-            | Option.Some(pb) =>
-                (match pb with
-                  | (params, body) =>
-                      (match eval-args(args, 0, env, defs) with
-                        | Option.None(_) => Option.None(unit)
-                        | Option.Some(vals) =>
-                            eval-core-d(body, bind-params(params, vals, 0, (Map.empty : Map(Int64, Int64))), defs)))))
+      // REPRO (monomorph SCC bug): factored into apply-def-by-name (a NEW member of eval-core-d's mut-rec SCC).
+      apply-def-by-name(name, args, env, defs)
+  | Core.CFnRef(_) => Option.None(unit)      // exhaustiveness only (HEAD lower-db has these variants)
+  | Core.CFnRefVar(_, _) => Option.None(unit)
+
+/// REPRO helper: byte-identical to the former inline CCall body, but as a SEPARATE top-level fn that calls
+/// eval-core-d + eval-args (joining the mutual-recursion SCC). This factoring triggers CDZ0101 unbound `defs`
+/// at whole-program monomorphization (see queue/mlrepro-new-fn-in-eval-core-mutual-recursion-cycle-...).
+def apply-def-by-name(name: Int64, args: List(Core), env: Map(Int64, Int64), defs: Map(Int64, (List(Int64), Core))) =
+  (match Map.lookup(defs, name) with
+    | Option.None(_) => Option.None(unit)
+    | Option.Some(pb) =>
+        (match pb with
+          | (params, body) =>
+              (match eval-args(args, 0, env, defs) with
+                | Option.None(_) => Option.None(unit)
+                | Option.Some(vals) =>
+                    eval-core-d(body, bind-params(params, vals, 0, (Map.empty : Map(Int64, Int64))), defs))))
 
 /// Apply a binary op: 43 +, 45 -, 42 *, 47 /, and the relational ops 60 `<`, 61 `==`, 62 `>`, 63 `<=`,
 /// 64 `>=`, 65 `!=`. Relational ops yield a Bool encoded as 0/1 (Core represents Bool as an Int); each of
