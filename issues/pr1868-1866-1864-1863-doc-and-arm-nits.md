# PRs #1868 / #1866 / #1864 / #1863 review comments — LOW doc/test

## PR #1868 (cdz-kernel, v-agent-harness — the #1858 durability-doc fix) — 2 LOW/doc
- kernel.rs:694 — the corrected comment contrasts "in-memory session log" vs "on disk", but the v0
  durability contract is phrased differently (log-store's actual boundary); align the wording to the v0
  contract's terms.
- name_store.rs:245 — the doc says pruning is "not gated on an fsync'd EffectResult", but the log store's
  v0 durability contract [may define durability differently]; match the contract's language. LOW/doc.
  (These are the reword of my #1858 boundary note — verify the new wording matches the v0 log-store contract.)

## PR #1866 (spec/07-type-system.sexp:231, corpus-bugfix) — LOW/grammar
Doc text has grammar glitches ("the same the …"); clean up. LOW.

## PR #1864 (rcdzc/tests.rs:24383, v-inference) — test-precision
The comment says the nested-generic case covers "both arms", but the test only executes the `Some` input;
the None/other arm isn't exercised. Add the other-arm input or narrow the comment. LOW-MED/test-coverage.

## PR #1863 (cdz-agent-host/src/host.rs:308, v-agent-harness-host) — doc
The docs claim creds/region come ONLY from env vars and profile/SSO/IMDS aren't enabled — verify vs the
actual aws-config feature set (post-#1841 the feature set changed); align the doc. LOW/doc.
