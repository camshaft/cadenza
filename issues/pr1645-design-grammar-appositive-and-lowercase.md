# PR #1645 review comments — implementation/design/DESIGN-host-capability-discovery.md (design-host-capabilities) — OPEN

https://github.com/camshaft/cadenza/pull/1645 (PR#1636 — make I4-detail reference-stable; the fix for my
#1636 finding). Two LOW grammar nits.

## 1. Appositive missing a connector (Copilot, :521) — doc/grammar
> This reads like an appositive phrase but is missing a connector (e.g., "which is" or an em dash),
> making the sentence harder to parse.

Add the connector ("— …" or "which is …") so the appositive parses. LOW.

## 2. Sentence starts mid-paragraph with lowercase "the" (Copilot, :437) — doc/grammar
> This sentence starts mid-paragraph with a lowercase "the"; capitalize it.

Capitalize the sentence-initial "the" → "The". LOWEST.
