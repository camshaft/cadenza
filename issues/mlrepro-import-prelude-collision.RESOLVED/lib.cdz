type T =
  | Foo(Int64)
  | List(List(T))

def sz(n: T) = match n with
  | T.Foo(_) => 1
  | T.List(es) => 1 + szl(es)

def szl(es: List(T)) = match es with
  | [] => 0
  | [h, .. r] => sz(h) + szl(r)

export { T.*, sz }
