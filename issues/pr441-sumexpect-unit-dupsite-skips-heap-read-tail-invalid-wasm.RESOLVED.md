# MISCOMPILE (invalid wasm): Unit-typed SumExpect dup-site skips emit_heap_read_tail -> IMM_UNIT sentinel not dropped (pr402/pr388 Unit-in-heap class)

Copilot inline on merged PR #441 (backend/wasm/select.rs:1171+7487, comment ids 3592691424+3592691434). CONFIRMED on trunk: get_op returns Ok(None) for Ty::Unit, so a Unit-typed SumExpect can be marked a dup-site, and the dup-site fast-path SKIPS emit_heap_read_tail (select.rs:7486) — where Unit's IMM_UNIT sentinel would be dropped. Result: extra handle left on the stack for a Unit-typed block -> invalid wasm. Same Unit-across-heap class as pr402 (box_op/get_op Ok(None)) and pr388 (closure Unit result). FIX: guard the SumExpect dup-site branch so Ty::Unit still goes through emit_heap_read_tail (drop). Repro: a SumExpect extracting a Unit payload consumed while the sum is live. Fix on trunk. Quotes + links in queue file.
