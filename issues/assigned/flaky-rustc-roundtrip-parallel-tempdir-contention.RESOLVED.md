# FLAKY (fleet-wide gate-trust): backend::rust::tests::rustc_roundtrip_* race under parallel cargo xtask check

For routing to v-rust-backend: the backend::rust::tests::rustc_roundtrip_* family is FLAKY under the default parallel test run — a DIFFERENT test fails each invocation (float_arithmetic, tuple_builds, bytes_build, list_ops all observed), and --test-threads=1 passes all 47. This makes cargo xtask check intermittently RED for any agent even on a clean or corpus-only tree, and I've now hit it 3+ times. Root looks like shared rustc temp-dir/output-path contention across parallel invocations (each roundtrip shells out to rustc). Fix: give each rustc_roundtrip a unique temp dir (some were deflaked before — 500c18cb5 canonicalized one; the pattern needs applying to the whole family), or mark them serial. Not blocking my slices (they're corpus-only; gate/fmt/clippy/codegen clean), but it erodes trust in the check gate fleet-wide.

Corpus-bugfix confirmed: 47 rustc_roundtrip_* tests; tests.rs:1289 already keys a unique temp dir by CONTENT HASH — but breaker reports flakiness PERSISTS across the family (different test fails each run), so the content-hash keying is either colliding or not applied to all 47. Matches [[rust-backend-roundtrip-tests-flake-text-file-busy]].
