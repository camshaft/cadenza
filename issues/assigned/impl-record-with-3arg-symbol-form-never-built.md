# IMPL GAP: Record.with 3-arg #symbol form DESIGNED (DESIGN-record-update-syntax.md) but NEVER BUILT

Operator flagged (twice now, incl tonight): the guide still shows the OLD wrong Record.with syntax, and the 3-arg #symbol form appears NOT IMPLEMENTED yet. STATE: DESIGN-record-update-syntax.md is on trunk (operator DECIDED 2026-07-15: Record.with takes 3 operands — record, a #symbol field selector, value — on BOTH surfaces), but the design-record-update-syntax agent STOPPED without landing an impl, and I find NO Record.with in the corpus/impl (only Record.without/Record.project). So the feature was designed but never built. Operator confirmed the exact target form tonight: `(Record.with rec #price 9)` (s-expr) / `Record.with(rec, #price, 9)` (ML) — the 2nd operand is a #SYMBOL field-selector, NOT the old grouped `(price 9)` pair that rendered as a misleading `price(9)` call. ALSO the operator wants the LITERAL record-update sugar they now have to be usable — DESIGN doc mentions `{ r with x = 1 }` sugar; confirm what literal form actually landed and that it desugars to the NEW 3-arg Record.with. ASK: please assign this as a fix/impl task (it's a real feature-impl gap, not triage) — implement the 3-operand `Record.with r #field v` on both surfaces per DESIGN-record-update-syntax.md (resolved.rs/prelude + s-expr parse of the #symbol operand + ML surface + corpus cases + gate), and reconcile the `{ r with … }` literal sugar to desugar onto it. Owner: this is rcdzc + cadenza-syntax (v-syntax owns the surface/sugar; the Prim/lowering is rcdzc). Once it LANDS + is runnable, the guide chapter can be corrected to the real form (I'm telling v-guide the guide fix is BLOCKED on the impl — no point documenting a non-existent API, same lesson as the property-testing chapter). Route it.

Corpus-bugfix confirmed: prelude.rs:527-528 has only record-project/record-without, NO record-with. DESIGN doc on trunk at implementation/design/DESIGN-record-update-syntax.md. The  keyword IS in the lexer (token.rs:196). Per the design doc: the { r with } BRACE-SUGAR direction was DROPPED — target is purely the 3-operand  (s-expr) /  (ML), 2nd operand a #symbol field-selector.
